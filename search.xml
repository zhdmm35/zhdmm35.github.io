<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Nginx实现FLV视频流代理转发：跨域与鉴权踩坑全记录</title>
    <url>/2026/03/02/Nginx%E5%AE%9E%E7%8E%B0FLV%E8%A7%86%E9%A2%91%E6%B5%81%E4%BB%A3%E7%90%86%E8%BD%AC%E5%8F%91%EF%BC%9A%E8%B7%A8%E5%9F%9F%E4%B8%8E%E9%89%B4%E6%9D%83%E8%B8%A9%E5%9D%91%E5%85%A8%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<p>前端用 flv.js 播放 HTTP-FLV 视频流时，经常会遇到跨域、HTTPS 混合内容、鉴权等问题。本文记录了我用 Nginx 反向代理解决这些问题的完整过程，以及踩过的坑：HEAD 误判、GET 才触发 401、上游地址混淆等。</p>
<span id="more"></span>

<h2 id="一、问题背景：为什么要做-FLV-代理？"><a href="#一、问题背景：为什么要做-FLV-代理？" class="headerlink" title="一、问题背景：为什么要做 FLV 代理？"></a>一、问题背景：为什么要做 FLV 代理？</h2><p>前端用 flv.js 播放 HTTP-FLV 时，通常会遇到两个现实问题：</p>
<h3 id="1-跨域（CORS）"><a href="#1-跨域（CORS）" class="headerlink" title="1. 跨域（CORS）"></a>1. 跨域（CORS）</h3><p>原始视频流地址来自另一个域名&#x2F;IP，例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">http://19.15.67.125/slvideo_SYY_172/hls/47c18e6792155981a28f3951977db417.flv?token=...</span><br></pre></td></tr></table></figure>

<p>浏览器直接请求会被 CORS 拦截，导致无法播放。</p>
<h3 id="2-HTTPS-页面混合内容（Mixed-Content）"><a href="#2-HTTPS-页面混合内容（Mixed-Content）" class="headerlink" title="2. HTTPS 页面混合内容（Mixed Content）"></a>2. HTTPS 页面混合内容（Mixed Content）</h3><p>如果前端页面是 <code>https://</code>，而视频源站是 <code>http://</code>，浏览器会直接拦截或提示不安全。</p>
<p>最稳的方式是：<strong>让前端始终请求”同源&#x2F;同协议”的代理地址</strong>，由 Nginx 去转发到源站。</p>
<p>为了解决这些问题，我选择用 <strong>Nginx 反向代理</strong>：前端请求 Nginx，本机&#x2F;同域返回并加 CORS 头，Nginx 转发到真实源站。</p>
<h2 id="二、先把关系搞清楚：三类地址最容易混淆"><a href="#二、先把关系搞清楚：三类地址最容易混淆" class="headerlink" title="二、先把关系搞清楚：三类地址最容易混淆"></a>二、先把关系搞清楚：三类地址最容易混淆</h2><p>这次踩坑里最容易搞错的是：**哪个是”源站”，哪个是”Nginx 代理服务器”**。</p>
<table>
<thead>
<tr>
<th>类型</th>
<th>地址</th>
</tr>
</thead>
<tbody><tr>
<td>Nginx 代理服务器（我的机器）</td>
<td><code>http://10.144.32.219:18001</code></td>
</tr>
<tr>
<td>真实 FLV 源站（视频平台&#x2F;摄像头出口）</td>
<td><code>http://19.15.67.125</code></td>
</tr>
<tr>
<td>前端最终应该请求的地址</td>
<td><code>http://10.144.32.219:18001/video-proxy/...</code></td>
</tr>
</tbody></table>
<blockquote>
<p>一开始我误把 <code>10.144.32.219</code> 当成上游源站写进了 <code>proxy_pass</code>，导致逻辑变成”代理转发到自己”，怎么都不对。</p>
</blockquote>
<h3 id="经验结论"><a href="#经验结论" class="headerlink" title="经验结论"></a>经验结论</h3><p><strong>以 <code>/api/video/play</code> 返回的 <code>data.httpflv</code> 的 host 为准</strong>：它返回的是 <code>19.15.67.125</code>，那 Nginx 的 <code>proxy_pass</code> 就必须指向 <code>19.15.67.125</code>。</p>
<h2 id="三、一个很隐蔽的坑：curl-I-会误导你（HEAD-≠-GET）"><a href="#三、一个很隐蔽的坑：curl-I-会误导你（HEAD-≠-GET）" class="headerlink" title="三、一个很隐蔽的坑：curl -I 会误导你（HEAD ≠ GET）"></a>三、一个很隐蔽的坑：<code>curl -I</code> 会误导你（HEAD ≠ GET）</h2><p>我最开始用：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -I <span class="string">&quot;http://19.15.67.125/xxx.flv?token=...&quot;</span></span><br></pre></td></tr></table></figure>

<p>结果看到 <code>200 OK</code>，还以为源站没问题。</p>
<p>但后来改用真正拉数据的方式：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -v --range 0-1023 --output NUL <span class="string">&quot;http://19.15.67.125/xxx.flv?token=...&quot;</span></span><br></pre></td></tr></table></figure>

<p>马上暴露真实情况：<strong>401 Unauthorized</strong>。</p>
<h3 id="为什么会这样？"><a href="#为什么会这样？" class="headerlink" title="为什么会这样？"></a>为什么会这样？</h3><ul>
<li><code>curl -I</code> 发的是 <strong>HEAD</strong> 请求，只要响应头，不取 body</li>
<li>很多流媒体&#x2F;鉴权服务对 HEAD 的处理很”宽松”或直接返回空头；真正的权限校验是在 GET 才触发</li>
<li>所以<strong>判定视频流是否可拉，必须用 GET（最好带 Range）测试</strong></li>
</ul>
<blockquote>
<p><strong>结论：不要用 HEAD 判断 FLV 是否可用，请用 <code>GET + Range</code></strong></p>
</blockquote>
<h2 id="四、核心方案：Nginx-反向代理配置"><a href="#四、核心方案：Nginx-反向代理配置" class="headerlink" title="四、核心方案：Nginx 反向代理配置"></a>四、核心方案：Nginx 反向代理配置</h2><h3 id="最终配置（固定上游-路径透传）"><a href="#最终配置（固定上游-路径透传）" class="headerlink" title="最终配置（固定上游 + 路径透传）"></a>最终配置（固定上游 + 路径透传）</h3><p>我选择在独立端口 <code>18001</code> 上单独开一个 server，并限定只匹配 <code>/video-proxy/</code>，避免误伤其它请求。</p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="section">server</span> &#123;</span><br><span class="line">    <span class="attribute">listen</span>       <span class="number">18001</span>;</span><br><span class="line">    <span class="attribute">server_name</span>  _;</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 仅匹配视频流代理路径，避免无关请求干扰</span></span><br><span class="line">    <span class="section">location</span> /video-proxy/ &#123;</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 处理 OPTIONS 预检请求（跨域必备）</span></span><br><span class="line">        <span class="attribute">if</span> (<span class="variable">$request_method</span> = <span class="string">&#x27;OPTIONS&#x27;</span>) &#123;</span><br><span class="line">            <span class="attribute">add_header</span> <span class="string">&#x27;Access-Control-Allow-Origin&#x27;</span> <span class="string">&#x27;*&#x27;</span> always;</span><br><span class="line">            <span class="attribute">add_header</span> <span class="string">&#x27;Access-Control-Allow-Methods&#x27;</span> <span class="string">&#x27;GET, OPTIONS&#x27;</span> always;</span><br><span class="line">            <span class="attribute">add_header</span> <span class="string">&#x27;Access-Control-Allow-Headers&#x27;</span> <span class="string">&#x27;Range, If-Range, User-Agent, Content-Type&#x27;</span> always;</span><br><span class="line">            <span class="attribute">add_header</span> <span class="string">&#x27;Access-Control-Max-Age&#x27;</span> <span class="number">3600</span> always;</span><br><span class="line">            <span class="attribute">add_header</span> <span class="string">&#x27;Content-Length&#x27;</span> <span class="number">0</span>;</span><br><span class="line">            <span class="attribute">return</span> <span class="number">204</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 转发到真实 FLV 源站（末尾 / 保证路径透传）</span></span><br><span class="line">        <span class="attribute">proxy_pass</span> http://19.15.67.125/;</span><br><span class="line">        <span class="attribute">proxy_http_version</span> <span class="number">1</span>.<span class="number">1</span>;</span><br><span class="line">        <span class="attribute">proxy_set_header</span> Host <span class="number">19.15.67.125</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 透传客户端信息（可选，但建议）</span></span><br><span class="line">        <span class="attribute">proxy_set_header</span> X-Real-IP <span class="variable">$remote_addr</span>;</span><br><span class="line">        <span class="attribute">proxy_set_header</span> X-Forwarded-For <span class="variable">$proxy_add_x_forwarded_for</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment"># FLV 流媒体关键：Range/If-Range + 关闭缓冲 + 长超时</span></span><br><span class="line">        <span class="attribute">proxy_set_header</span> Range <span class="variable">$http_range</span>;</span><br><span class="line">        <span class="attribute">proxy_set_header</span> If-Range <span class="variable">$http_if_range</span>;</span><br><span class="line">        <span class="attribute">proxy_buffering</span> <span class="literal">off</span>;</span><br><span class="line">        <span class="attribute">proxy_read_timeout</span> <span class="number">3600s</span>;</span><br><span class="line">        <span class="attribute">proxy_send_timeout</span> <span class="number">3600s</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment"># CORS 响应头（建议 always，避免偶发错误没有跨域头）</span></span><br><span class="line">        <span class="attribute">add_header</span> <span class="string">&#x27;Access-Control-Allow-Origin&#x27;</span> <span class="string">&#x27;*&#x27;</span> always;</span><br><span class="line">        <span class="attribute">add_header</span> <span class="string">&#x27;Access-Control-Expose-Headers&#x27;</span> <span class="string">&#x27;Content-Length, Content-Range, Accept-Ranges&#x27;</span> always;</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 不缓存（按需）</span></span><br><span class="line">        <span class="attribute">add_header</span> <span class="string">&#x27;Cache-Control&#x27;</span> <span class="string">&#x27;no-cache, no-store, must-revalidate&#x27;</span> always;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="为什么-proxy-pass-末尾一定要有-？"><a href="#为什么-proxy-pass-末尾一定要有-？" class="headerlink" title="为什么 proxy_pass 末尾一定要有 /？"></a>为什么 <code>proxy_pass</code> 末尾一定要有 <code>/</code>？</h3><table>
<thead>
<tr>
<th>写法</th>
<th>效果</th>
</tr>
</thead>
<tbody><tr>
<td><code>proxy_pass http://19.15.67.125/;</code></td>
<td>会把 <code>/video-proxy/</code> 前缀剥掉后透传到源站 ✅</td>
</tr>
<tr>
<td><code>proxy_pass http://19.15.67.125;</code></td>
<td>路径拼接规则会变，容易把 <code>/video-proxy/</code> 一起带过去造成 404 ❌</td>
</tr>
</tbody></table>
<p>这点是流媒体代理里最常见的”明明能连上但总 404”的坑。</p>
<h2 id="五、前端拼接规则"><a href="#五、前端拼接规则" class="headerlink" title="五、前端拼接规则"></a>五、前端拼接规则</h2><p><strong>原始地址</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">http://19.15.67.125/slvideo_SYY_172/hls/xxx.flv?token=xxx</span><br></pre></td></tr></table></figure>

<p><strong>前端最终请求（同源代理）</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">http://10.144.32.219:18001/video-proxy/slvideo_SYY_172/hls/xxx.flv?token=xxx</span><br></pre></td></tr></table></figure>

<p>示例代码（核心思路是只取 pathname + search）：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> originalFlv = data.<span class="property">httpflv</span>; <span class="comment">// http://19.15.67.125/xxx.flv?token=...</span></span><br><span class="line"><span class="keyword">const</span> url = <span class="keyword">new</span> <span class="title function_">URL</span>(originalFlv);</span><br><span class="line"><span class="keyword">const</span> proxyPath = url.<span class="property">pathname</span> + url.<span class="property">search</span>;</span><br><span class="line"><span class="keyword">const</span> flvUrl = <span class="string">`http://10.144.32.219:18001/video-proxy<span class="subst">$&#123;proxyPath&#125;</span>`</span>;</span><br><span class="line"><span class="comment">// or 用 window.location.origin（同域时更方便）</span></span><br></pre></td></tr></table></figure>

<h2 id="六、验证与排错"><a href="#六、验证与排错" class="headerlink" title="六、验证与排错"></a>六、验证与排错</h2><h3 id="1-语法检查并重载（Windows）"><a href="#1-语法检查并重载（Windows）" class="headerlink" title="1) 语法检查并重载（Windows）"></a>1) 语法检查并重载（Windows）</h3><figure class="highlight bat"><table><tr><td class="code"><pre><span class="line">nginx.exe -t</span><br><span class="line">nginx.exe -s reload</span><br></pre></td></tr></table></figure>

<h3 id="2-端口监听确认"><a href="#2-端口监听确认" class="headerlink" title="2) 端口监听确认"></a>2) 端口监听确认</h3><figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">netstat <span class="literal">-ano</span> | findstr :<span class="number">18001</span></span><br></pre></td></tr></table></figure>

<h3 id="3-用-GET-Range-测试代理"><a href="#3-用-GET-Range-测试代理" class="headerlink" title="3) 用 GET + Range 测试代理"></a>3) 用 GET + Range 测试代理</h3><figure class="highlight bat"><table><tr><td class="code"><pre><span class="line">curl -v --range <span class="number">0</span>-<span class="number">1023</span> --output <span class="built_in">NUL</span> &quot;http://<span class="number">10</span>.<span class="number">144</span>.<span class="number">32</span>.<span class="number">219</span>:<span class="number">18001</span>/video-proxy/slvideo_SYY_172/hls/xxx.flv?token=xxx&quot;</span><br></pre></td></tr></table></figure>

<p>期望：<code>200</code> 或 <code>206 Partial Content</code>，<strong>不是 401&#x2F;403&#x2F;502&#x2F;504</strong></p>
<h3 id="4-如果出现-401：这是鉴权问题，不是跨域问题"><a href="#4-如果出现-401：这是鉴权问题，不是跨域问题" class="headerlink" title="4) 如果出现 401：这是鉴权问题，不是跨域问题"></a>4) 如果出现 401：这是鉴权问题，不是跨域问题</h3><p>这次对话里最关键的真相是：<strong>源站对 curl 的 GET 请求返回 401 Unauthorized</strong>。</p>
<p>这说明：</p>
<ul>
<li>token 可能很快过期（几秒到几分钟）</li>
<li>或者源站除了 token 还校验 Referer&#x2F;Cookie&#x2F;Authorization&#x2F;User-Agent</li>
<li>代理配置再完美，<strong>鉴权不过依旧会 401</strong></li>
</ul>
<h4 id="可选：透传常见鉴权-header"><a href="#可选：透传常见鉴权-header" class="headerlink" title="可选：透传常见鉴权 header"></a>可选：透传常见鉴权 header</h4><figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="attribute">proxy_set_header</span> Referer <span class="variable">$http_referer</span>;</span><br><span class="line"><span class="attribute">proxy_set_header</span> Cookie  <span class="variable">$http_cookie</span>;</span><br><span class="line"><span class="attribute">proxy_set_header</span> Authorization <span class="variable">$http_authorization</span>;</span><br><span class="line"><span class="attribute">proxy_set_header</span> User-Agent <span class="variable">$http_user_agent</span>;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>安全提醒：透传 Cookie&#x2F;Authorization 之前最好加访问控制（白名单&#x2F;内网），避免变成开放代理。</p>
</blockquote>
<h2 id="七、Windows-部署补充"><a href="#七、Windows-部署补充" class="headerlink" title="七、Windows 部署补充"></a>七、Windows 部署补充</h2><p>Windows 下 Nginx <strong>放哪都能跑</strong>，但会影响：</p>
<ul>
<li><code>include mime.types;</code> 这类相对路径能否找到</li>
<li><code>logs/</code> 是否有写权限</li>
<li>你是否能稳定重载&#x2F;开机自启</li>
</ul>
<p>推荐固定目录：<code>C:\nginx\</code></p>
<p>用”明确前缀和配置文件”的方式启动：</p>
<figure class="highlight bat"><table><tr><td class="code"><pre><span class="line"><span class="function">C:\<span class="title">nginx</span>\<span class="title">nginx.exe</span> -<span class="title">p</span> <span class="title">C</span>:\<span class="title">nginx</span>\ -<span class="title">c</span> <span class="title">conf</span>\<span class="title">nginx.conf</span></span></span><br></pre></td></tr></table></figure>

<p>重载同理：</p>
<figure class="highlight bat"><table><tr><td class="code"><pre><span class="line"><span class="function">C:\<span class="title">nginx</span>\<span class="title">nginx.exe</span> -<span class="title">p</span> <span class="title">C</span>:\<span class="title">nginx</span>\ -<span class="title">c</span> <span class="title">conf</span>\<span class="title">nginx.conf</span> -<span class="title">s</span> <span class="title">reload</span></span></span><br></pre></td></tr></table></figure>

<h2 id="八、踩坑总结"><a href="#八、踩坑总结" class="headerlink" title="八、踩坑总结"></a>八、踩坑总结</h2><table>
<thead>
<tr>
<th>坑</th>
<th>解决方案</th>
</tr>
</thead>
<tbody><tr>
<td>host 搞错</td>
<td>以 API 返回的 <code>httpflv</code> host 为准</td>
</tr>
<tr>
<td>HEAD 验证误导</td>
<td>必须用 <code>GET + Range</code> 测试</td>
</tr>
<tr>
<td>401 当成跨域问题</td>
<td>401 是鉴权问题，代理不能绕过权限</td>
</tr>
<tr>
<td>Windows Nginx 路径混乱</td>
<td>固定 <code>-p</code> 和 <code>-c</code> 参数启动</td>
</tr>
<tr>
<td>端口不通</td>
<td>Windows 防火墙&#x2F;安全组都要开放</td>
</tr>
</tbody></table>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>通过 Nginx 反向代理，我们成功解决了 FLV 视频流的跨域和 HTTPS 混合内容问题。关键在于：搞清楚源站地址、用正确的方式验证、理解 401 是鉴权而非跨域问题。希望这篇踩坑记录能帮到你！</p>
<hr>
<blockquote>
<p>💡 <strong>提示</strong>：如果需要在 HTTPS 环境下使用，只需将代理 server 配到 443 端口并配置 SSL 证书即可。</p>
</blockquote>
]]></content>
      <categories>
        <category>后端开发</category>
      </categories>
      <tags>
        <tag>Nginx</tag>
        <tag>FLV</tag>
        <tag>跨域</tag>
        <tag>反向代理</tag>
        <tag>踩坑记录</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring Boot多数据源开发实践：从踩坑到解决</title>
    <url>/2026/02/28/Spring-Boot%E5%A4%9A%E6%95%B0%E6%8D%AE%E6%BA%90%E5%BC%80%E5%8F%91%E5%AE%9E%E8%B7%B5%EF%BC%9A%E4%BB%8E%E8%B8%A9%E5%9D%91%E5%88%B0%E8%A7%A3%E5%86%B3/</url>
    <content><![CDATA[<p>最近开发水文监测系统时遇到了多数据源切换问题：明明存在的表却报 “Table doesn’t exist”。本文记录排查过程和解决方案，希望能帮助遇到类似情况的朋友。</p>
<span id="more"></span>

<h2 id="问题背景"><a href="#问题背景" class="headerlink" title="问题背景"></a>问题背景</h2><p>大断面是水文监测中的重要概念，用于展示河道&#x2F;水库的横断面形状。本次任务是为前端图表组件提供适配的数据接口。</p>
<p>系统配置了三个数据源：</p>
<table>
<thead>
<tr>
<th>数据源</th>
<th>数据库</th>
<th>用途</th>
</tr>
</thead>
<tbody><tr>
<td>MASTER</td>
<td>hydrology</td>
<td>主业务数据库</td>
</tr>
<tr>
<td>SLAVE</td>
<td>hydrology</td>
<td>从库（默认关闭）</td>
</tr>
<tr>
<td>WATER</td>
<td>ysq</td>
<td>水务数据库（独立连接）</td>
</tr>
</tbody></table>
<h2 id="问题发现"><a href="#问题发现" class="headerlink" title="问题发现"></a>问题发现</h2><h3 id="错误现象"><a href="#错误现象" class="headerlink" title="错误现象"></a>错误现象</h3><p>接口测试时直接报错：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Table &#x27;hydrology.st_rvsect_b&#x27; doesn&#x27;t exist</span><br></pre></td></tr></table></figure>

<p>这个表明明存在，为什么会报不存在？</p>
<h3 id="问题分析"><a href="#问题分析" class="headerlink" title="问题分析"></a>问题分析</h3><p>经过排查发现，<code>ST_RVSECT_B</code> 表实际存储在 <strong>WATER 数据源</strong>，但代码直接调用了 Mapper 层，绕过了 Service 层的 <code>@DataSource</code> 注解，导致使用了默认的 MASTER 数据源。</p>
<h3 id="错误代码示例"><a href="#错误代码示例" class="headerlink" title="错误代码示例"></a>错误代码示例</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// StationArchiveBiz.java - 错误示例</span></span><br><span class="line"><span class="meta">@Autowired</span></span><br><span class="line"><span class="keyword">private</span> StRvsectBMapper stRvsectBMapper;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> CrossSectionResponseDTO <span class="title function_">getCrossSection</span><span class="params">(String stcd)</span> &#123;</span><br><span class="line">    <span class="comment">// 直接调用 Mapper，绕过了数据源切换！</span></span><br><span class="line">    List&lt;StRvsectB&gt; sectionPoints = stRvsectBMapper.selectLatestSectionPointsByStcd(stcd);</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这种写法的问题在于：<strong>多数据源切换是通过 AOP 实现的，只有在 Service 层的方法上添加 <code>@DataSource</code> 注解才能生效</strong>。直接调用 Mapper 会使用默认数据源。</p>
<h2 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h2><h3 id="修复代码"><a href="#修复代码" class="headerlink" title="修复代码"></a>修复代码</h3><p>在 Service 层添加方法并配置数据源注解：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// IStRvsectBService.java</span></span><br><span class="line">List&lt;StRvsectB&gt; <span class="title function_">selectLatestSectionPointsByStcd</span><span class="params">(String stcd)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// StRvsectBServiceImpl.java</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="meta">@DataSource(value = DataSourceType.WATER)</span>  <span class="comment">// 关键！</span></span><br><span class="line"><span class="keyword">public</span> List&lt;StRvsectB&gt; <span class="title function_">selectLatestSectionPointsByStcd</span><span class="params">(String stcd)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> stRvsectBMapper.selectLatestSectionPointsByStcd(stcd);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>然后在 Biz 层调用 Service：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// StationArchiveBiz.java - 修复后</span></span><br><span class="line"><span class="meta">@Autowired</span></span><br><span class="line"><span class="keyword">private</span> IStRvsectBService stRvsectBService;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> CrossSectionResponseDTO <span class="title function_">getCrossSection</span><span class="params">(String stcd)</span> &#123;</span><br><span class="line">    <span class="comment">// 通过 Service 调用，正确切换数据源</span></span><br><span class="line">    List&lt;StRvsectB&gt; sectionPoints = stRvsectBService.selectLatestSectionPointsByStcd(stcd);</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="架构示意"><a href="#架构示意" class="headerlink" title="架构示意"></a>架构示意</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Controller/Biz 层</span><br><span class="line">       │</span><br><span class="line">       ▼</span><br><span class="line">   Service 层  ◄── @DataSource 注解在这里生效</span><br><span class="line">       │</span><br><span class="line">       ▼</span><br><span class="line">   Mapper 层</span><br><span class="line">       │</span><br><span class="line">       ▼</span><br><span class="line">   数据库（根据注解切换）</span><br></pre></td></tr></table></figure>

<h2 id="响应格式重构"><a href="#响应格式重构" class="headerlink" title="响应格式重构"></a>响应格式重构</h2><p>解决数据源问题后，还需要重构响应格式以适配前端图表组件。</p>
<h3 id="新旧格式对比"><a href="#新旧格式对比" class="headerlink" title="新旧格式对比"></a>新旧格式对比</h3><p><strong>旧格式：</strong></p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;points&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="punctuation">&#123;</span> <span class="attr">&quot;distance&quot;</span><span class="punctuation">:</span> <span class="number">0</span><span class="punctuation">,</span> <span class="attr">&quot;elevation&quot;</span><span class="punctuation">:</span> <span class="number">95</span> <span class="punctuation">&#125;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;warningWaterLevel&quot;</span><span class="punctuation">:</span> <span class="number">120</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;waterHistory&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span>...<span class="punctuation">]</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p><strong>新格式：</strong></p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;information&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;normz&quot;</span><span class="punctuation">:</span> <span class="number">120</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;actz&quot;</span><span class="punctuation">:</span> <span class="number">69.797</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;dsflz&quot;</span><span class="punctuation">:</span> <span class="number">125</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;ckflz&quot;</span><span class="punctuation">:</span> <span class="number">127</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;fsltdz&quot;</span><span class="punctuation">:</span> <span class="number">119</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;flcnhgwl&quot;</span><span class="punctuation">:</span> <span class="number">124</span></span><br><span class="line">  <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;riverbedDataSource&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="punctuation">&#123;</span> <span class="attr">&quot;x&quot;</span><span class="punctuation">:</span> <span class="number">0</span><span class="punctuation">,</span> <span class="attr">&quot;y&quot;</span><span class="punctuation">:</span> <span class="number">95</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="punctuation">&#123;</span> <span class="attr">&quot;x&quot;</span><span class="punctuation">:</span> <span class="number">10</span><span class="punctuation">,</span> <span class="attr">&quot;y&quot;</span><span class="punctuation">:</span> <span class="number">92</span> <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;dataSource&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="punctuation">&#123;</span> <span class="attr">&quot;z&quot;</span><span class="punctuation">:</span> <span class="number">120</span><span class="punctuation">,</span> <span class="attr">&quot;q&quot;</span><span class="punctuation">:</span> <span class="number">100</span> <span class="punctuation">&#125;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;白垢&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;riverbedNames&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;起点距(m)&quot;</span><span class="punctuation">,</span> <span class="string">&quot;高程(m)&quot;</span><span class="punctuation">]</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<h3 id="DTO-设计"><a href="#DTO-设计" class="headerlink" title="DTO 设计"></a>DTO 设计</h3><p>使用内部类组织嵌套结构，保持代码整洁：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">CrossSectionResponseDTO</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> Information information;</span><br><span class="line">    <span class="keyword">private</span> List&lt;RiverbedPoint&gt; riverbedDataSource;</span><br><span class="line">    <span class="keyword">private</span> List&lt;FlowPoint&gt; dataSource;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> List&lt;String&gt; riverbedNames;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Data</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">Information</span> &#123;</span><br><span class="line">        <span class="keyword">private</span> BigDecimal normz;    <span class="comment">// 正常蓄水位</span></span><br><span class="line">        <span class="keyword">private</span> BigDecimal actz;     <span class="comment">// 兴利库容</span></span><br><span class="line">        <span class="keyword">private</span> BigDecimal dsflz;    <span class="comment">// 设计洪水位</span></span><br><span class="line">        <span class="keyword">private</span> BigDecimal ckflz;    <span class="comment">// 校核洪水位</span></span><br><span class="line">        <span class="keyword">private</span> BigDecimal fsltdz;   <span class="comment">// 汛限水位</span></span><br><span class="line">        <span class="keyword">private</span> BigDecimal flcnhgwl; <span class="comment">// 防洪高水位</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Data</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">RiverbedPoint</span> &#123;</span><br><span class="line">        <span class="keyword">private</span> Double x;  <span class="comment">// 起点距</span></span><br><span class="line">        <span class="keyword">private</span> Double y;  <span class="comment">// 高程</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Data</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">FlowPoint</span> &#123;</span><br><span class="line">        <span class="keyword">private</span> BigDecimal z;  <span class="comment">// 水位</span></span><br><span class="line">        <span class="keyword">private</span> BigDecimal q;  <span class="comment">// 流量</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><h3 id="多数据源开发的注意事项"><a href="#多数据源开发的注意事项" class="headerlink" title="多数据源开发的注意事项"></a>多数据源开发的注意事项</h3><ol>
<li><strong>永远通过 Service 层访问数据库</strong>，不要在 Biz&#x2F;Controller 层直接注入 Mapper</li>
<li><strong>数据源注解放在 Service 实现类的方法上</strong>，确保 AOP 拦截生效</li>
<li><strong>跨数据源查询要考虑事务问题</strong>，必要时需要分布式事务方案</li>
</ol>
<h3 id="业务术语要确认清楚"><a href="#业务术语要确认清楚" class="headerlink" title="业务术语要确认清楚"></a>业务术语要确认清楚</h3><p>本次开发中遇到了术语的坑：</p>
<table>
<thead>
<tr>
<th>术语</th>
<th>含义</th>
<th>单位</th>
</tr>
</thead>
<tbody><tr>
<td>兴利水位</td>
<td>水位高度</td>
<td>m</td>
</tr>
<tr>
<td>兴利库容 (ACTCP)</td>
<td>水的体积</td>
<td>10^6m³</td>
</tr>
</tbody></table>
<p>数据库中没有”兴利水位”字段，只有”兴利库容”，开发前务必与业务方确认字段含义。</p>
<h3 id="空数据处理"><a href="#空数据处理" class="headerlink" title="空数据处理"></a>空数据处理</h3><p>不同类型测站的数据完整性不同，接口应正确处理空数据情况：</p>
<table>
<thead>
<tr>
<th>测站类型</th>
<th>断面数据</th>
<th>特征水位</th>
<th>水位-流量</th>
</tr>
</thead>
<tbody><tr>
<td>水库站 (RR)</td>
<td>可能无</td>
<td>有</td>
<td>通常无</td>
</tr>
<tr>
<td>河道站 (ZQ)</td>
<td>可能有</td>
<td>有</td>
<td>有</td>
</tr>
</tbody></table>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>多数据源开发中，数据源切换的时机和位置非常重要。记住一个原则：<strong>让数据源切换发生在正确的层次</strong>。</p>
<p>希望这篇文章对你有帮助！</p>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>水文</category>
      </categories>
      <tags>
        <tag>踩坑记录</tag>
        <tag>Spring Boot</tag>
        <tag>MyBatis</tag>
        <tag>多数据源</tag>
      </tags>
  </entry>
  <entry>
    <title>aioffice 反向代理开发：将 AI 编程工具额度转化为 OpenAI 兼容 API</title>
    <url>/2026/03/01/aioffice-%E5%8F%8D%E5%90%91%E4%BB%A3%E7%90%86%E5%BC%80%E5%8F%91%EF%BC%9A%E5%B0%86-AI-%E7%BC%96%E7%A8%8B%E5%B7%A5%E5%85%B7%E9%A2%9D%E5%BA%A6%E8%BD%AC%E5%8C%96%E4%B8%BA-OpenAI-%E5%85%BC%E5%AE%B9-API/</url>
    <content><![CDATA[<p>aioffice 是一款基于 VSCode 二次开发的 AI 编程工具，提供了 AI 对话、代码补全等功能。为了能在其他工具（如 opencode、Continue 等）中复用 aioffice 的 AI 调用额度，我开发了一个反向代理服务，将 aioffice 的后端 API 转化为标准的 OpenAI 兼容格式。</p>
<span id="more"></span>

<h2 id="技术分析"><a href="#技术分析" class="headerlink" title="技术分析"></a>技术分析</h2><h3 id="通信架构分析"><a href="#通信架构分析" class="headerlink" title="通信架构分析"></a>通信架构分析</h3><p>通过分析 aioffice 的源代码和配置文件，发现：</p>
<table>
<thead>
<tr>
<th>项目</th>
<th>值</th>
</tr>
</thead>
<tbody><tr>
<td>后端服务器地址</td>
<td><code>http://14.103.247.146:8094</code></td>
</tr>
<tr>
<td>API 格式</td>
<td>OpenAI 兼容（<code>/v1/chat/completions</code>）</td>
</tr>
<tr>
<td>认证方式</td>
<td>Bearer Token（sk- 前缀的 API Key）</td>
</tr>
<tr>
<td>Token 存储位置</td>
<td>SQLite 数据库</td>
</tr>
</tbody></table>
<p>Token 存储在 SQLite 数据库中：</p>
<ul>
<li><strong>路径</strong>：<code>%APPDATA%/AiOffice/User/globalStorage/state.vscdb</code></li>
<li><strong>键名</strong>：<code>coai.auth.token</code>（登录凭证）、<code>coai.api.key</code>（API 调用密钥）</li>
</ul>
<h3 id="源码定位方法"><a href="#源码定位方法" class="headerlink" title="源码定位方法"></a>源码定位方法</h3><p>aioffice 基于 Void Editor 开发，代码位于安装目录的 <code>resources/app</code> 下（未打包为 asar）。通过搜索关键字符串定位到核心逻辑：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">resources/app/out/vs/workbench/workbench.desktop.main.js</span><br></pre></td></tr></table></figure>

<p>关键代码片段：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> <span class="variable constant_">COAI_API_URL</span> = <span class="string">&quot;http://14.103.247.146:8094&quot;</span>;</span><br><span class="line"><span class="comment">// ...</span></span><br><span class="line"><span class="variable language_">this</span>.<span class="property">apiClient</span> = <span class="keyword">new</span> <span class="title class_">CoaiApiClient</span>(<span class="function">() =&gt;</span> <span class="variable constant_">COAI_API_URL</span>, <span class="function">() =&gt;</span> <span class="variable language_">this</span>.<span class="title function_">getToken</span>(), <span class="variable language_">this</span>.<span class="property">requestService</span>);</span><br></pre></td></tr></table></figure>

<h2 id="架构设计"><a href="#架构设计" class="headerlink" title="架构设计"></a>架构设计</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌─────────────────┐      ┌──────────────────┐      ┌─────────────────┐</span><br><span class="line">│  任意客户端      │ ──→ │  本地代理服务     │ ──→ │  aioffice 后端  │</span><br><span class="line">│  (Cursor等)     │      │  localhost:18273 │      │  14.103.247.146 │</span><br><span class="line">└─────────────────┘      └──────────────────┘      └─────────────────┘</span><br><span class="line">                                │</span><br><span class="line">                                ↓</span><br><span class="line">                         ┌──────────────┐</span><br><span class="line">                         │ 自动提取 Token│</span><br><span class="line">                         │ (SQLite读取) │</span><br><span class="line">                         └──────────────┘</span><br></pre></td></tr></table></figure>

<h3 id="核心模块"><a href="#核心模块" class="headerlink" title="核心模块"></a>核心模块</h3><table>
<thead>
<tr>
<th>模块</th>
<th>职责</th>
</tr>
</thead>
<tbody><tr>
<td><code>token.js</code></td>
<td>从 SQLite 数据库读取认证信息，支持自动刷新</td>
</tr>
<tr>
<td><code>proxy.js</code></td>
<td>转发请求到 aioffice 后端，处理流式响应</td>
</tr>
<tr>
<td><code>chat.js</code></td>
<td><code>/v1/chat/completions</code> 路由处理</td>
</tr>
<tr>
<td><code>models.js</code></td>
<td><code>/v1/models</code> 路由处理</td>
</tr>
</tbody></table>
<h2 id="踩坑记录"><a href="#踩坑记录" class="headerlink" title="踩坑记录"></a>踩坑记录</h2><h3 id="坑一：better-sqlite3-编译失败"><a href="#坑一：better-sqlite3-编译失败" class="headerlink" title="坑一：better-sqlite3 编译失败"></a>坑一：better-sqlite3 编译失败</h3><p><strong>问题</strong>：最初选择 <code>better-sqlite3</code> 作为 SQLite 驱动，但 Windows 上需要 Visual Studio 编译工具：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">gyp ERR! find VS could not find Visual Studio installation to use</span><br></pre></td></tr></table></figure>

<p><strong>解决</strong>：改用纯 JavaScript 实现的 <code>sql.js</code>，无需编译：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// package.json</span></span><br><span class="line">&#123;</span><br><span class="line">  <span class="string">&quot;dependencies&quot;</span>: &#123;</span><br><span class="line">    <span class="string">&quot;sql.js&quot;</span>: <span class="string">&quot;^1.10.0&quot;</span>  <span class="comment">// 替代 better-sqlite3</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>sql.js</code> 的 API 略有不同，需要注意：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> initSqlJs = <span class="built_in">require</span>(<span class="string">&#x27;sql.js&#x27;</span>);</span><br><span class="line"><span class="keyword">const</span> fs = <span class="built_in">require</span>(<span class="string">&#x27;fs&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 初始化（异步）</span></span><br><span class="line"><span class="keyword">const</span> <span class="variable constant_">SQL</span> = <span class="keyword">await</span> <span class="title function_">initSqlJs</span>();</span><br><span class="line"></span><br><span class="line"><span class="comment">// 读取数据库文件</span></span><br><span class="line"><span class="keyword">const</span> fileBuffer = fs.<span class="title function_">readFileSync</span>(dbPath);</span><br><span class="line"><span class="keyword">const</span> db = <span class="keyword">new</span> <span class="variable constant_">SQL</span>.<span class="title class_">Database</span>(fileBuffer);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 查询（返回格式不同）</span></span><br><span class="line"><span class="keyword">const</span> result = db.<span class="title function_">exec</span>(<span class="string">&quot;SELECT value FROM ItemTable WHERE key = ?&quot;</span>);</span><br><span class="line"><span class="comment">// result[0].values[0][0] 获取第一行第一列的值</span></span><br></pre></td></tr></table></figure>

<h3 id="坑二：Payload-Too-Large-错误"><a href="#坑二：Payload-Too-Large-错误" class="headerlink" title="坑二：Payload Too Large 错误"></a>坑二：Payload Too Large 错误</h3><p><strong>问题</strong>：在 opencode 中使用时报错：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">PayloadTooLargeError: request entity too large</span><br></pre></td></tr></table></figure>

<p><strong>原因</strong>：Express 的 <code>body-parser</code> 默认限制请求体为 100kb，而 AI 对话的上下文可能很大。</p>
<p><strong>解决</strong>：增加请求体大小限制：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 原来</span></span><br><span class="line">app.<span class="title function_">use</span>(express.<span class="title function_">json</span>());</span><br><span class="line"></span><br><span class="line"><span class="comment">// 修改为</span></span><br><span class="line">app.<span class="title function_">use</span>(express.<span class="title function_">json</span>(&#123; <span class="attr">limit</span>: <span class="string">&#x27;50mb&#x27;</span> &#125;));</span><br></pre></td></tr></table></figure>

<h3 id="坑三：opencode-配置缺少-apiKey"><a href="#坑三：opencode-配置缺少-apiKey" class="headerlink" title="坑三：opencode 配置缺少 apiKey"></a>坑三：opencode 配置缺少 apiKey</h3><p><strong>问题</strong>：即使代理会自动处理认证，opencode 仍需要一个 apiKey 字段才能发送请求。</p>
<p><strong>解决</strong>：在 opencode 配置中添加任意 apiKey：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;aioffice&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;ao&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;npm&quot;</span><span class="punctuation">:</span> <span class="string">&quot;@ai-sdk/openai-compatible&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;models&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;claude-sonnet-4-6-thinking&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;claude-sonnet-4-6-thinking&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;glm-4.7&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;glm-4.7&quot;</span> <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;options&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;apiKey&quot;</span><span class="punctuation">:</span> <span class="string">&quot;sk-fandai&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;baseURL&quot;</span><span class="punctuation">:</span> <span class="string">&quot;http://localhost:18273/v1&quot;</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<h3 id="坑四：后台进程不稳定"><a href="#坑四：后台进程不稳定" class="headerlink" title="坑四：后台进程不稳定"></a>坑四：后台进程不稳定</h3><p><strong>问题</strong>：在 Windows 上使用 <code>&amp;</code> 后台运行 Node.js 进程不稳定，容易退出。</p>
<p><strong>解决</strong>：使用 <code>start</code> 命令在新窗口中运行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cmd //c <span class="string">&quot;start /min cmd /c node src/index.js&quot;</span></span><br></pre></td></tr></table></figure>

<h2 id="最终实现"><a href="#最终实现" class="headerlink" title="最终实现"></a>最终实现</h2><h3 id="项目结构"><a href="#项目结构" class="headerlink" title="项目结构"></a>项目结构</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">fandai/</span><br><span class="line">├── src/</span><br><span class="line">│   ├── index.js          # 入口，Express 服务</span><br><span class="line">│   ├── config.js         # 配置（端口、后端地址）</span><br><span class="line">│   ├── routes/</span><br><span class="line">│   │   ├── chat.js       # /v1/chat/completions</span><br><span class="line">│   │   └── models.js     # /v1/models</span><br><span class="line">│   └── services/</span><br><span class="line">│       ├── token.js      # Token 自动提取</span><br><span class="line">│       └── proxy.js      # 请求转发</span><br><span class="line">├── package.json</span><br><span class="line">└── README.md</span><br></pre></td></tr></table></figure>

<h3 id="核心代码"><a href="#核心代码" class="headerlink" title="核心代码"></a>核心代码</h3><p><strong>Token 自动提取</strong> (<code>services/token.js</code>)：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> initSqlJs = <span class="built_in">require</span>(<span class="string">&#x27;sql.js&#x27;</span>);</span><br><span class="line"><span class="keyword">const</span> fs = <span class="built_in">require</span>(<span class="string">&#x27;fs&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">readFromDatabase</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> <span class="variable constant_">SQL</span> = <span class="keyword">await</span> <span class="title function_">initSqlJs</span>();</span><br><span class="line">  <span class="keyword">const</span> fileBuffer = fs.<span class="title function_">readFileSync</span>(<span class="variable constant_">DB_PATH</span>);</span><br><span class="line">  <span class="keyword">const</span> db = <span class="keyword">new</span> <span class="variable constant_">SQL</span>.<span class="title class_">Database</span>(fileBuffer);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">const</span> result = db.<span class="title function_">exec</span>(<span class="string">&quot;SELECT value FROM ItemTable WHERE key = &#x27;coai.api.key&#x27;&quot;</span>);</span><br><span class="line">  <span class="keyword">return</span> result.<span class="property">length</span> &gt; <span class="number">0</span> ? result[<span class="number">0</span>].<span class="property">values</span>[<span class="number">0</span>]?.[<span class="number">0</span>] : <span class="literal">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>请求转发</strong> (<code>services/proxy.js</code>)：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">proxyChatCompletion</span>(<span class="params">req, res</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> apiKey = <span class="keyword">await</span> <span class="title function_">getApiKey</span>();</span><br><span class="line"></span><br><span class="line">  <span class="keyword">const</span> response = <span class="keyword">await</span> axios.<span class="title function_">post</span>(</span><br><span class="line">    <span class="string">`<span class="subst">$&#123;COAI_API_URL&#125;</span>/v1/chat/completions`</span>,</span><br><span class="line">    req.<span class="property">body</span>,</span><br><span class="line">    &#123;</span><br><span class="line">      <span class="attr">headers</span>: &#123; <span class="string">&#x27;Authorization&#x27;</span>: <span class="string">`Bearer <span class="subst">$&#123;apiKey&#125;</span>`</span> &#125;,</span><br><span class="line">      <span class="attr">responseType</span>: req.<span class="property">body</span>.<span class="property">stream</span> ? <span class="string">&#x27;stream&#x27;</span> : <span class="string">&#x27;json&#x27;</span></span><br><span class="line">    &#125;</span><br><span class="line">  );</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (req.<span class="property">body</span>.<span class="property">stream</span>) &#123;</span><br><span class="line">    res.<span class="title function_">setHeader</span>(<span class="string">&#x27;Content-Type&#x27;</span>, <span class="string">&#x27;text/event-stream&#x27;</span>);</span><br><span class="line">    response.<span class="property">data</span>.<span class="title function_">on</span>(<span class="string">&#x27;data&#x27;</span>, <span class="function"><span class="params">chunk</span> =&gt;</span> res.<span class="title function_">write</span>(chunk));</span><br><span class="line">    response.<span class="property">data</span>.<span class="title function_">on</span>(<span class="string">&#x27;end&#x27;</span>, <span class="function">() =&gt;</span> res.<span class="title function_">end</span>());</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    res.<span class="title function_">json</span>(response.<span class="property">data</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="使用方式"><a href="#使用方式" class="headerlink" title="使用方式"></a>使用方式</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 安装依赖</span></span><br><span class="line">npm install</span><br><span class="line"></span><br><span class="line"><span class="comment"># 启动代理</span></span><br><span class="line">npm start</span><br><span class="line"></span><br><span class="line"><span class="comment"># 配置其他工具</span></span><br><span class="line"><span class="built_in">export</span> OPENAI_API_BASE=http://localhost:18273/v1</span><br><span class="line"><span class="built_in">export</span> OPENAI_API_KEY=any</span><br></pre></td></tr></table></figure>

<h2 id="可用模型"><a href="#可用模型" class="headerlink" title="可用模型"></a>可用模型</h2><p>代理支持 aioffice 后端的所有模型，包括：</p>
<table>
<thead>
<tr>
<th>模型</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>glm-4.7</code> &#x2F; <code>glm-5</code></td>
<td>智谱 GLM 系列</td>
</tr>
<tr>
<td><code>gpt-5.2</code> &#x2F; <code>gpt-5.3-codex</code></td>
<td>GPT 系列</td>
</tr>
<tr>
<td><code>claude-sonnet-4-6-thinking</code></td>
<td>Claude 思维链模型</td>
</tr>
<tr>
<td><code>gemini-3-pro-preview</code></td>
<td>Gemini 3 Pro</td>
</tr>
</tbody></table>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><p>这个项目的核心价值在于：</p>
<ul>
<li><strong>协议转换</strong>：将私有 API 转化为标准 OpenAI 格式</li>
<li><strong>认证自动化</strong>：自动提取和刷新 Token，用户无感知</li>
<li><strong>通用兼容</strong>：任何支持 OpenAI API 的工具都能使用</li>
</ul>
<p>开发过程中主要踩了以下几个坑：</p>
<ol>
<li>SQLite 驱动在 Windows 上编译问题 → 用纯 JS 实现的 <code>sql.js</code></li>
<li>Express 请求体大小限制 → 增加到 50mb</li>
<li>客户端配置要求 → 填充任意 apiKey</li>
<li>Windows 后台进程不稳定 → 用 <code>start</code> 命令</li>
</ol>
<p>最终都用比较简单的方式解决了，整个项目代码量不大，但实用性很强。</p>
<hr>
<p><strong>相关链接</strong>：<a href="https://github.com/zhdmm35/aioffice-proxy">项目仓库 - GitHub</a></p>
]]></content>
      <categories>
        <category>后端开发</category>
      </categories>
      <tags>
        <tag>反向代理</tag>
        <tag>踩坑记录</tag>
        <tag>Node.js</tag>
        <tag>OpenAI</tag>
        <tag>SQLite</tag>
      </tags>
  </entry>
  <entry>
    <title>Hello World</title>
    <url>/2026/02/27/hello-world/</url>
    <content><![CDATA[<p>欢迎来到我的技术博客！这是第一篇测试文章，用于验证博客系统是否正常工作。</p>
<span id="more"></span>

<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p>
<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>
]]></content>
      <categories>
        <category>其他</category>
      </categories>
      <tags>
        <tag>Hexo</tag>
      </tags>
  </entry>
  <entry>
    <title>博客搭建记录</title>
    <url>/2026/02/27/%E5%8D%9A%E5%AE%A2%E6%90%AD%E5%BB%BA%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<p>终于拥有了自己的技术博客！这篇文章记录了整个搭建过程，从 Hexo 安装到 GitHub Pages 部署，希望能帮助到想搭建博客的朋友。</p>
<span id="more"></span>

<h2 id="技术选型"><a href="#技术选型" class="headerlink" title="技术选型"></a>技术选型</h2><table>
<thead>
<tr>
<th>组件</th>
<th>选择</th>
<th>理由</th>
</tr>
</thead>
<tbody><tr>
<td>静态生成器</td>
<td>Hexo</td>
<td>中文生态好，主题丰富，文档完善</td>
</tr>
<tr>
<td>主题</td>
<td>NexT</td>
<td>简约清爽，功能强大，支持暗色模式</td>
</tr>
<tr>
<td>托管</td>
<td>GitHub Pages</td>
<td>免费，稳定，自带 HTTPS</td>
</tr>
</tbody></table>
<h2 id="搭建步骤"><a href="#搭建步骤" class="headerlink" title="搭建步骤"></a>搭建步骤</h2><h3 id="1-环境准备"><a href="#1-环境准备" class="headerlink" title="1. 环境准备"></a>1. 环境准备</h3><p>确保已安装：</p>
<ul>
<li>Node.js 18+</li>
<li>Git</li>
</ul>
<h3 id="2-安装-Hexo"><a href="#2-安装-Hexo" class="headerlink" title="2. 安装 Hexo"></a>2. 安装 Hexo</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install -g hexo-cli</span><br><span class="line">hexo init blog</span><br><span class="line"><span class="built_in">cd</span> blog &amp;&amp; npm install</span><br></pre></td></tr></table></figure>

<h3 id="3-安装-NexT-主题"><a href="#3-安装-NexT-主题" class="headerlink" title="3. 安装 NexT 主题"></a>3. 安装 NexT 主题</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install hexo-theme-next</span><br></pre></td></tr></table></figure>

<p>修改 <code>_config.yml</code> 启用主题：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">theme:</span> <span class="string">next</span></span><br></pre></td></tr></table></figure>

<h3 id="4-配置站点信息"><a href="#4-配置站点信息" class="headerlink" title="4. 配置站点信息"></a>4. 配置站点信息</h3><p>编辑 <code>_config.yml</code>：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">title:</span> <span class="string">我的技术博客</span></span><br><span class="line"><span class="attr">author:</span> <span class="string">zhlifeiy35</span></span><br><span class="line"><span class="attr">language:</span> <span class="string">zh-CN</span></span><br><span class="line"><span class="attr">timezone:</span> <span class="string">&#x27;Asia/Shanghai&#x27;</span></span><br></pre></td></tr></table></figure>

<h3 id="5-配置主题功能"><a href="#5-配置主题功能" class="headerlink" title="5. 配置主题功能"></a>5. 配置主题功能</h3><p>创建 <code>_config.next.yml</code> 文件，配置：</p>
<ul>
<li>暗色&#x2F;亮色模式切换</li>
<li>本地搜索</li>
<li>代码高亮与复制</li>
<li>分类和标签页面</li>
</ul>
<h3 id="6-安装搜索插件"><a href="#6-安装搜索插件" class="headerlink" title="6. 安装搜索插件"></a>6. 安装搜索插件</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install hexo-generator-searchdb</span><br></pre></td></tr></table></figure>

<h3 id="7-部署到-GitHub-Pages"><a href="#7-部署到-GitHub-Pages" class="headerlink" title="7. 部署到 GitHub Pages"></a>7. 部署到 GitHub Pages</h3><ol>
<li>在 GitHub 创建 <code>用户名.github.io</code> 仓库</li>
<li>安装部署插件：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install hexo-deployer-git</span><br></pre></td></tr></table></figure></li>
<li>配置 <code>_config.yml</code>：<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">deploy:</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">git</span></span><br><span class="line">  <span class="attr">repo:</span> <span class="string">https://github.com/用户名/用户名.github.io.git</span></span><br><span class="line">  <span class="attr">branch:</span> <span class="string">main</span></span><br></pre></td></tr></table></figure></li>
<li>部署：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo clean &amp;&amp; hexo generate &amp;&amp; hexo deploy</span><br></pre></td></tr></table></figure></li>
</ol>
<h2 id="博客功能"><a href="#博客功能" class="headerlink" title="博客功能"></a>博客功能</h2><ul>
<li><input checked="" disabled="" type="checkbox"> 简约清爽的界面</li>
<li><input checked="" disabled="" type="checkbox"> 暗色&#x2F;亮色模式切换</li>
<li><input checked="" disabled="" type="checkbox"> 本地搜索</li>
<li><input checked="" disabled="" type="checkbox"> 代码高亮与一键复制</li>
<li><input checked="" disabled="" type="checkbox"> 分类和标签</li>
<li><input checked="" disabled="" type="checkbox"> RSS 订阅</li>
<li><input disabled="" type="checkbox"> 评论系统（计划中）</li>
<li><input disabled="" type="checkbox"> 自定义域名（计划中）</li>
</ul>
<h2 id="日常写作流程"><a href="#日常写作流程" class="headerlink" title="日常写作流程"></a>日常写作流程</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 新建文章</span></span><br><span class="line">hexo new post <span class="string">&quot;文章标题&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 本地预览</span></span><br><span class="line">hexo server</span><br><span class="line"></span><br><span class="line"><span class="comment"># 生成并部署</span></span><br><span class="line">hexo clean &amp;&amp; hexo generate &amp;&amp; hexo deploy</span><br><span class="line"></span><br><span class="line"><span class="comment"># 或一键部署</span></span><br><span class="line">hexo d -g</span><br></pre></td></tr></table></figure>

<h2 id="后续计划"><a href="#后续计划" class="headerlink" title="后续计划"></a>后续计划</h2><ul>
<li><input disabled="" type="checkbox"> 完善 About 页面</li>
<li><input disabled="" type="checkbox"> 添加评论系统（Giscus）</li>
<li><input disabled="" type="checkbox"> 绑定自定义域名</li>
<li><input disabled="" type="checkbox"> 开始持续写作！</li>
</ul>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>整个搭建过程比想象中简单，Hexo + NexT 的组合对中文用户非常友好。从零到上线只需要不到一小时。</p>
<p>接下来就是开始积累内容了。技术博客不仅是知识分享的平台，更是自己成长的记录。坚持写作，持续进步！</p>
<hr>
<blockquote>
<p>如果这篇文章对你有帮助，欢迎在 GitHub 上 Follow 我~</p>
</blockquote>
]]></content>
      <categories>
        <category>其他</category>
      </categories>
      <tags>
        <tag>Hexo</tag>
        <tag>博客</tag>
        <tag>NexT</tag>
      </tags>
  </entry>
  <entry>
    <title>让AI写出产品级前端：frontend-design skill实战对比</title>
    <url>/2026/03/02/%E8%AE%A9AI%E5%86%99%E5%87%BA%E4%BA%A7%E5%93%81%E7%BA%A7%E5%89%8D%E7%AB%AF%EF%BC%9Afrontend-design-skill%E5%AE%9E%E6%88%98%E5%AF%B9%E6%AF%94/</url>
    <content><![CDATA[<p>你有没有发现，让 AI 写前端代码时，出来的页面总有一种”能用但不精致”的感觉？组件像默认样式拼装、颜色只用一两个、层级靠字体大小硬拉开——功能是实现了，但就是不像一个成熟的产品。</p>
<p>今天我用一个任务管理界面的实际案例，对比展示<strong>使用 frontend-design skill 前后的差异</strong>。看完你就会明白：一个小小的 skill，如何让 AI 输出从”表单页”进化成”产品级界面”。</p>
<span id="more"></span>

<h2 id="问题背景"><a href="#问题背景" class="headerlink" title="问题背景"></a>问题背景</h2><p>在 Claude Code 中开发前端界面时，默认情况下 AI 会：</p>
<ul>
<li>使用基础的 HTML&#x2F;CSS 结构</li>
<li>选择安全但不突出的配色方案</li>
<li>布局偏”表单流”，缺乏视觉节奏</li>
<li>组件样式偏”系统默认”</li>
</ul>
<p>这对于内部管理后台或许够用，但如果你要做<strong>演示、作品集、面向用户的产品界面</strong>，这样的输出明显不够”有设计感”。</p>
<p><strong>frontend-design skill</strong> 正是为了解决这个问题而生的。</p>
<h2 id="效果对比"><a href="#效果对比" class="headerlink" title="效果对比"></a>效果对比</h2><p>先看两张截图的直观对比：</p>
<h3 id="使用-skill-前"><a href="#使用-skill-前" class="headerlink" title="使用 skill 前"></a>使用 skill 前</h3><p><img data-src="/images/posts/frontend-design-skill/before.png" alt="使用skill前"></p>
<p>典型的”浅色表单 + 管理后台”风格：</p>
<ul>
<li>白底、浅灰分割线、轻微阴影</li>
<li>UI 很”系统默认”：输入框、按钮、分段选择都偏素</li>
<li>整体更像”页面组件拼起来”，而不是统一的视觉语言</li>
</ul>
<h3 id="使用-skill-后"><a href="#使用-skill-后" class="headerlink" title="使用 skill 后"></a>使用 skill 后</h3><p><img data-src="/images/posts/frontend-design-skill/after.png" alt="使用skill后"></p>
<p>深色现代风格（偏 glass &#x2F; neon &#x2F; dashboard）：</p>
<ul>
<li>大面积深色背景 + 暗部渐变，营造氛围</li>
<li>卡片有明显”层叠深度”：更强阴影、柔和边框、高光描边</li>
<li>整体更像”有品牌的应用”，而不是”通用模板页”</li>
</ul>
<h2 id="核心差异分析"><a href="#核心差异分析" class="headerlink" title="核心差异分析"></a>核心差异分析</h2><h3 id="1-设计风格"><a href="#1-设计风格" class="headerlink" title="1. 设计风格"></a>1. 设计风格</h3><table>
<thead>
<tr>
<th>维度</th>
<th>使用前</th>
<th>使用后</th>
</tr>
</thead>
<tbody><tr>
<td>整体风格</td>
<td>浅色表单流</td>
<td>深色现代风</td>
</tr>
<tr>
<td>视觉语言</td>
<td>组件拼装感</td>
<td>统一的设计系统</td>
</tr>
<tr>
<td>品牌感</td>
<td>无明显特征</td>
<td>有氛围、有质感</td>
</tr>
</tbody></table>
<h3 id="2-布局结构"><a href="#2-布局结构" class="headerlink" title="2. 布局结构"></a>2. 布局结构</h3><p><strong>使用前</strong>：纵向信息流</p>
<ul>
<li>顶部标题 → 中间筛选 → 下方列表式任务卡片</li>
<li>空白较多，信息分散</li>
</ul>
<p><strong>使用后</strong>：Dashboard 布局</p>
<ul>
<li>顶部工具栏（搜索、筛选、排序、主按钮同一行）</li>
<li>网格卡片（两张并排），更像”看板”</li>
<li>信息层级集中：卡片上方是优先级与状态，正文是标题，底部是操作区</li>
</ul>
<h3 id="3-UI-组件差异"><a href="#3-UI-组件差异" class="headerlink" title="3. UI 组件差异"></a>3. UI 组件差异</h3><table>
<thead>
<tr>
<th>组件类型</th>
<th>使用前</th>
<th>使用后</th>
</tr>
</thead>
<tbody><tr>
<td>筛选控件</td>
<td>分段按钮&#x2F;胶囊按钮</td>
<td>下拉选择 + 搜索框 + 强 CTA</td>
</tr>
<tr>
<td>状态展示</td>
<td>表单控件风格</td>
<td>Badge&#x2F;Chip 系统（P1&#x2F;P3、待办）</td>
</tr>
<tr>
<td>操作按钮</td>
<td>文字按钮”编辑&#x2F;删除”</td>
<td>图标按钮 + 主按钮”开始”</td>
</tr>
</tbody></table>
<h3 id="4-颜色方案"><a href="#4-颜色方案" class="headerlink" title="4. 颜色方案"></a>4. 颜色方案</h3><p><strong>使用前</strong>：浅色中性 + 单一强调色</p>
<ul>
<li>主背景白，边框浅灰</li>
<li>强调色偏蓝紫</li>
<li>整体对比弱、氛围轻</li>
</ul>
<p><strong>使用后</strong>：深色中性 + 高对比强调色</p>
<ul>
<li>主色深灰&#x2F;黑，辅以渐变阴影</li>
<li>强调色明确：绿色 CTA，优先级用红&#x2F;绿 badge</li>
<li>视觉对比强，信息更”跳出来”</li>
</ul>
<h3 id="5-整体视觉效果"><a href="#5-整体视觉效果" class="headerlink" title="5. 整体视觉效果"></a>5. 整体视觉效果</h3><p>使用 skill 后的界面显著更强，体现在：</p>
<ul>
<li><strong>统一性</strong>：同一套圆角、阴影、边框亮度贯穿全页</li>
<li><strong>层级感</strong>：背景 → 容器 → 卡片 → 徽标 → 按钮，层次分明</li>
<li><strong>节奏</strong>：留白更”设计化”，卡片间距和对齐更舒服</li>
<li><strong>情绪</strong>：有”氛围”，不像偏”办公表单感”</li>
</ul>
<h2 id="如何使用-frontend-design-skill"><a href="#如何使用-frontend-design-skill" class="headerlink" title="如何使用 frontend-design skill"></a>如何使用 frontend-design skill</h2><p>在 Claude Code 中，只需在开发前端界面时说：</p>
<blockquote>
<p>使用 frontend-design skill 帮我做一个任务管理界面</p>
</blockquote>
<p>或者直接：</p>
<blockquote>
<p>&#x2F;frontend-design 做一个任务管理看板</p>
</blockquote>
<p>skill 会自动：</p>
<ol>
<li>引导你确认设计风格（现代&#x2F;极简&#x2F;玻璃拟态等）</li>
<li>建立统一的视觉系统</li>
<li>选择合适的配色方案</li>
<li>设计有层级感的信息架构</li>
<li>输出产品级的组件代码</li>
</ol>
<h2 id="适用场景"><a href="#适用场景" class="headerlink" title="适用场景"></a>适用场景</h2><table>
<thead>
<tr>
<th>场景</th>
<th>推荐使用</th>
<th>原因</th>
</tr>
</thead>
<tbody><tr>
<td>作品集&#x2F;演示</td>
<td>✅ 强烈推荐</td>
<td>需要视觉冲击力</td>
</tr>
<tr>
<td>面向用户的产品</td>
<td>✅ 强烈推荐</td>
<td>品牌感、用户体验</td>
</tr>
<tr>
<td>Landing Page</td>
<td>✅ 强烈推荐</td>
<td>需要吸引眼球</td>
</tr>
<tr>
<td>内部管理后台</td>
<td>⚠️ 可选</td>
<td>功能优先，朴素也可</td>
</tr>
<tr>
<td>打印&#x2F;高信息密度</td>
<td>⚠️ 可选</td>
<td>深色不适合打印</td>
</tr>
</tbody></table>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><ol>
<li>**skill 的本质是”规范”**：它不是魔法，而是让 AI 遵循一套产品级设计规范</li>
<li><strong>视觉系统很重要</strong>：统一圆角、阴影、间距比”画得好看”更关键</li>
<li><strong>层级感 &gt; 装饰</strong>：好的设计是让用户快速找到信息，而不是堆砌效果</li>
<li><strong>深色模式更适合展示</strong>：高对比度让信息更突出，适合演示和产品界面</li>
</ol>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>frontend-design skill 让 AI 从”写代码”进化到”做产品”。如果你也厌倦了千篇一律的”表单页”，不妨试试这个 skill——它会让你的前端输出，从”能用”变成”想用”。</p>
<hr>
<blockquote>
<p>💡 <strong>提示</strong>：frontend-design skill 是 Claude Code 的内置技能，无需额外安装，直接调用即可。</p>
</blockquote>
]]></content>
      <categories>
        <category>AI经验</category>
      </categories>
      <tags>
        <tag>Claude Code</tag>
        <tag>Skill</tag>
        <tag>前端设计</tag>
        <tag>AI编程</tag>
        <tag>效率提升</tag>
      </tags>
  </entry>
  <entry>
    <title>6 个启动连环坑：从 MySQL 用户到前端 401 的完整链路打通</title>
    <url>/2026/06/09/2026-06-09-six-fixes/</url>
    <content><![CDATA[<h2 id="导读"><a href="#导读" class="headerlink" title="导读"></a>导读</h2><blockquote>
<p>编译跑绿后，启动链路又冒出 6 个连环坑：MySQL 没 <code>building_*</code> 用户、property-service 写死内网 IP、admin 种子用户密码是 SHA512×2、<code>is_enable=1</code> 居然是禁用、gateway 报 JWT NPE、前端访问报 401。本篇一次性梳理这 6 个坑的根因、修复方案、排查顺序，以及哪些是”反直觉的项目约定”。</p>
</blockquote>
<span id="more"></span>

<div class="audio-player">
  <div class="audio-section">
    <h4>🎧 文章导读</h4>
    <audio controls preload="metadata">
      <source src="/audio/2026-06-09-six-fixes/narration.mp3" type="audio/mpeg">
    </audio>
  </div>
  <div class="audio-section">
    <h4>🎵 背景音乐</h4>
    <audio controls loop preload="metadata">
      <source src="/audio/2026-06-09-six-fixes/bgm.mp3" type="audio/mpeg">
    </audio>
  </div>
</div>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>环境编译通过只是开始。真正启动 admin-service 时，才发现问题像洋葱一样一层层剥开：连不上 MySQL → admin 用户密码哈希算法不对 → <code>is_enable</code> 字段语义反过来 → 启动后 NPE → 前端 401。</p>
<p>这一篇把 6 个修复合并在一起，因为它们是一条链上的——前一个修不完后一个根本看不到。</p>
<p><img data-src="/images/posts/2026-06-09-six-fixes/fig1_001.jpg" alt="6 个连环坑链路示意"><br><em>图1：从 MySQL 到 401 的启动链路</em></p>
<h2 id="坑-1：MySQL-没有-building-用户"><a href="#坑-1：MySQL-没有-building-用户" class="headerlink" title="坑 1：MySQL 没有 building_* 用户"></a>坑 1：MySQL 没有 <code>building_*</code> 用户</h2><h3 id="现象"><a href="#现象" class="headerlink" title="现象"></a>现象</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CannotGetJdbcConnectionException: Failed to obtain JDBC Connection; </span><br><span class="line">nested exception is java.sql.SQLException: </span><br><span class="line">Access denied for user &#x27;building_admin&#x27;@&#x27;localhost&#x27; (using password: YES)</span><br></pre></td></tr></table></figure>

<h3 id="根因"><a href="#根因" class="headerlink" title="根因"></a>根因</h3><p><code>bootstrap-dev.yml</code> 写死了 <code>username: building_admin / password: building_admin@123</code>，但 MySQL 里<strong>只有 root、travel、系统用户</strong>——从来没人建过 <code>building_*</code> 系列用户。</p>
<h3 id="修复"><a href="#修复" class="headerlink" title="修复"></a>修复</h3><p>8 个用户 + 授权 SQL：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">USER</span> IF <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> <span class="string">&#x27;building_admin&#x27;</span>@<span class="string">&#x27;localhost&#x27;</span>   IDENTIFIED <span class="keyword">BY</span> <span class="string">&#x27;building_admin@123&#x27;</span>;</span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">USER</span> IF <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> <span class="string">&#x27;building_bpm&#x27;</span>@<span class="string">&#x27;localhost&#x27;</span>     IDENTIFIED <span class="keyword">BY</span> <span class="string">&#x27;building_bpm@123&#x27;</span>;</span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">USER</span> IF <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> <span class="string">&#x27;building_env&#x27;</span>@<span class="string">&#x27;localhost&#x27;</span>     IDENTIFIED <span class="keyword">BY</span> <span class="string">&#x27;building_env@123&#x27;</span>;</span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">USER</span> IF <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> <span class="string">&#x27;building_biz&#x27;</span>@<span class="string">&#x27;localhost&#x27;</span>     IDENTIFIED <span class="keyword">BY</span> <span class="string">&#x27;building_biz@123&#x27;</span>;</span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">USER</span> IF <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> <span class="string">&#x27;building_smart&#x27;</span>@<span class="string">&#x27;localhost&#x27;</span>   IDENTIFIED <span class="keyword">BY</span> <span class="string">&#x27;building_smart@123&#x27;</span>;</span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">USER</span> IF <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> <span class="string">&#x27;building_through&#x27;</span>@<span class="string">&#x27;localhost&#x27;</span> IDENTIFIED <span class="keyword">BY</span> <span class="string">&#x27;building_through@123&#x27;</span>;</span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">USER</span> IF <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> <span class="string">&#x27;building_jobs&#x27;</span>@<span class="string">&#x27;localhost&#x27;</span>    IDENTIFIED <span class="keyword">BY</span> <span class="string">&#x27;building_jobs@123&#x27;</span>;</span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">USER</span> IF <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> <span class="string">&#x27;building_nacos&#x27;</span>@<span class="string">&#x27;localhost&#x27;</span>   IDENTIFIED <span class="keyword">BY</span> <span class="string">&#x27;building_nacos@123&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">GRANT</span> <span class="keyword">ALL</span> PRIVILEGES <span class="keyword">ON</span> building_admin.<span class="operator">*</span>   <span class="keyword">TO</span> <span class="string">&#x27;building_admin&#x27;</span>@<span class="string">&#x27;localhost&#x27;</span>;</span><br><span class="line"><span class="keyword">GRANT</span> <span class="keyword">ALL</span> PRIVILEGES <span class="keyword">ON</span> building_bpm.<span class="operator">*</span>     <span class="keyword">TO</span> <span class="string">&#x27;building_bpm&#x27;</span>@<span class="string">&#x27;localhost&#x27;</span>;</span><br><span class="line"><span class="comment">-- ... 共 8 个 GRANT</span></span><br><span class="line"></span><br><span class="line">FLUSH PRIVILEGES;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>[!warning] 关键发现<br>项目记录说”11 个数据库”，实际只有 <strong>8 个 unique</strong>：</p>
<ul>
<li><code>admin + aided</code> 共用 <code>building_admin</code></li>
<li><code>through + security</code> 共用 <code>building_through</code></li>
<li><code>property + warning</code> 共用 <code>building_biz</code></li>
</ul>
<p>反直觉的项目约定——<strong>服务 ≠ 库</strong>。</p>
</blockquote>
<h2 id="坑-2：property-service-写死内网-IP"><a href="#坑-2：property-service-写死内网-IP" class="headerlink" title="坑 2：property-service 写死内网 IP"></a>坑 2：property-service 写死内网 IP</h2><p><code>property-service/src/main/resources/bootstrap-dev.yml</code> 3 处 IP 全部指向 <code>10.152.238.245</code>（堡垒机内网）：</p>
<table>
<thead>
<tr>
<th>行</th>
<th>字段</th>
<th>改前</th>
<th>改后</th>
</tr>
</thead>
<tbody><tr>
<td>5</td>
<td><code>NACOS_SERVER</code></td>
<td><code>10.152.238.245:8848</code></td>
<td><code>127.0.0.1:8848</code></td>
</tr>
<tr>
<td>10</td>
<td><code>MYSQL_SERVER</code></td>
<td><code>10.152.238.245:3306</code></td>
<td><code>127.0.0.1:3306</code></td>
</tr>
<tr>
<td>14</td>
<td>Redis host</td>
<td><code>10.152.238.245</code></td>
<td><code>127.0.0.1</code></td>
</tr>
</tbody></table>
<p>估计是某次开发连堡垒机调测忘了改回来。<strong>其他服务都是 <code>127.0.0.1</code>，唯独 property 是内网</strong>。</p>
<p><img data-src="/images/posts/2026-06-09-six-fixes/fig2_001.jpg" alt="property 内网 IP 残留配置"><br><em>图2：property 写死内网 IP</em></p>
<h2 id="坑-3：admin-种子用户密码算法"><a href="#坑-3：admin-种子用户密码算法" class="headerlink" title="坑 3：admin 种子用户密码算法"></a>坑 3：admin 种子用户密码算法</h2><p>admin 起来后登录报 <code>用户名或密码错误</code>。一开始以为是 hash 算法不对——确实不对，但不是 BCrypt，是 <strong>SHA512 × 2 次 + 盐</strong>。</p>
<h3 id="加密逻辑（service-util-LoginPwdCryptoUtil-java）"><a href="#加密逻辑（service-util-LoginPwdCryptoUtil-java）" class="headerlink" title="加密逻辑（service-util/.../LoginPwdCryptoUtil.java）"></a>加密逻辑（<code>service-util/.../LoginPwdCryptoUtil.java</code>）</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">HASH_INTERATIONS</span> <span class="operator">=</span> <span class="number">2</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> String <span class="title function_">enCryptPassword</span><span class="params">(String password, String salt)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> Hex.encodeHexString(</span><br><span class="line">        hash(StringUtils.getBytesUtf8(password), StringUtils.getBytesUtf8(salt), HASH_INTERATIONS));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>算法总结：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">password_hash = SHA512( SHA512(salt + password) )   转 hex 字符串</span><br><span class="line">salt         = Base64(32 字节随机)</span><br></pre></td></tr></table></figure>

<h3 id="Python-复现（已验证）"><a href="#Python-复现（已验证）" class="headerlink" title="Python 复现（已验证）"></a>Python 复现（已验证）</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> hashlib, base64, os</span><br><span class="line"></span><br><span class="line">password = <span class="string">&quot;123456&quot;</span></span><br><span class="line">salt_bytes = os.urandom(<span class="number">32</span>)</span><br><span class="line">salt = base64.b64encode(salt_bytes).decode(<span class="string">&#x27;utf-8&#x27;</span>)</span><br><span class="line"></span><br><span class="line">h = hashlib.sha512()</span><br><span class="line">h.update(salt_bytes)</span><br><span class="line">h.update(password.encode(<span class="string">&#x27;utf-8&#x27;</span>))</span><br><span class="line">hashed = h.digest()                   <span class="comment"># 第一次</span></span><br><span class="line"></span><br><span class="line">h = hashlib.sha512()</span><br><span class="line">h.update(hashed)</span><br><span class="line">hashed = h.digest()                   <span class="comment"># 第二次</span></span><br><span class="line"></span><br><span class="line">password_hash = hashed.<span class="built_in">hex</span>()           <span class="comment"># 转 hex</span></span><br></pre></td></tr></table></figure>

<h3 id="插-admin-种子用户"><a href="#插-admin-种子用户" class="headerlink" title="插 admin 种子用户"></a>插 admin 种子用户</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">INSERT INTO</span> sys_user (</span><br><span class="line">  user_id, user_name, password, password_salt,</span><br><span class="line">  user_status, real_name, is_enable, tenant_id,</span><br><span class="line">  delete_flag, face_pic_status, face_pic_audit_status,</span><br><span class="line">  create_id, create_name, create_time,</span><br><span class="line">  update_id, update_name, update_time</span><br><span class="line">) <span class="keyword">VALUES</span> (</span><br><span class="line">  <span class="string">&#x27;1&#x27;</span>, <span class="string">&#x27;admin&#x27;</span>,</span><br><span class="line">  <span class="string">&#x27;&lt;上面算的 hash&gt;&#x27;</span>, <span class="string">&#x27;&lt;上面算的 salt&gt;&#x27;</span>,</span><br><span class="line">  <span class="string">&#x27;1&#x27;</span>, <span class="string">&#x27;admin&#x27;</span>, <span class="number">0</span>, <span class="string">&#x27;000000&#x27;</span>,     <span class="comment">-- is_enable=0 才是启用</span></span><br><span class="line">  <span class="string">&#x27;0&#x27;</span>, <span class="string">&#x27;0&#x27;</span>, <span class="string">&#x27;0&#x27;</span>,</span><br><span class="line">  <span class="string">&#x27;admin&#x27;</span>, <span class="string">&#x27;admin&#x27;</span>, NOW(),</span><br><span class="line">  <span class="string">&#x27;admin&#x27;</span>, <span class="string">&#x27;admin&#x27;</span>, NOW()</span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<blockquote>
<p>[!warning] 实战坑<br><code>real_name</code> 填”超级管理员”被 MySQL 拒（<code>Data too long</code>），改 <code>admin</code> 才过——这列虽然 <code>varchar(64)</code> 但表可能是 <code>utf8</code> 而非 <code>utf8mb4</code>。<strong>遇到这种错就缩短内容</strong>。</p>
</blockquote>
<h2 id="坑-4：is-enable-1-居然是禁用"><a href="#坑-4：is-enable-1-居然是禁用" class="headerlink" title="坑 4：is_enable=1 居然是禁用"></a>坑 4：<code>is_enable=1</code> 居然是禁用</h2><p>修完密码又报错 <code>账号已被锁定</code>。看源码（<code>UserController.java:195</code>）：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (sysUser.getIsEnable() != <span class="literal">null</span> &amp;&amp; DictConstants.DISABLE_FLAG.equals(sysUser.getIsEnable())) &#123;</span><br><span class="line">    <span class="keyword">return</span> BaseResult.fail(<span class="literal">null</span>, <span class="string">&quot;账号已被锁定，请联系管理员启用&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>DictConstants.DISABLE_FLAG = &quot;1&quot;</code>——**<code>is_enable=1</code> 居然是禁用的意思**。完全违反 Spring&#x2F;Java 命名直觉。</p>
<table>
<thead>
<tr>
<th>字段</th>
<th>我以为</th>
<th>项目实际</th>
</tr>
</thead>
<tbody><tr>
<td><code>is_enable = 0</code></td>
<td>禁用</td>
<td><strong>启用</strong> ✅</td>
</tr>
<tr>
<td><code>is_enable = 1</code></td>
<td>启用</td>
<td><strong>禁用</strong> ❌</td>
</tr>
</tbody></table>
<blockquote>
<p>[!warning] 锁定陷阱<br>密码错 5 次自动设 <code>is_enable = &quot;1&quot;</code> 锁定。<strong>调试期不要乱试密码</strong>——一旦锁了，要直接 SQL 改 <code>is_enable=0</code>，不然怎么试都”已被锁定”。</p>
</blockquote>
<p><img data-src="/images/posts/2026-06-09-six-fixes/fig3_001.jpg" alt="is_enable 反向语义示意"><br><em>图3：is_enable&#x3D;1 居然是禁用</em></p>
<h2 id="坑-5：gateway-启动-3-个-NPE"><a href="#坑-5：gateway-启动-3-个-NPE" class="headerlink" title="坑 5：gateway 启动 3 个 NPE"></a>坑 5：gateway 启动 3 个 NPE</h2><h3 id="现象-1"><a href="#现象-1" class="headerlink" title="现象"></a>现象</h3><table>
<thead>
<tr>
<th>#</th>
<th>Bean</th>
<th>触发原因</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td><code>devServicRouteLocator</code></td>
<td><code>CommonProperties.deployments</code> 为 null</td>
</tr>
<tr>
<td>2</td>
<td><code>dynamicRouteNacosListener</code></td>
<td><code>NacosConfigProperties</code> bean 不存在（<code>enabled: false</code> 干掉了）</td>
</tr>
<tr>
<td>3</td>
<td><code>jwtTokenService</code></td>
<td><code>JwtAuthProperties.jwts</code> 为 null</td>
</tr>
</tbody></table>
<h3 id="根因-1"><a href="#根因-1" class="headerlink" title="根因"></a>根因</h3><p>Nacos 拒绝带连字符的 group 名（控制台报”不允许非法字符”），但项目代码默认是 <code>smart-building</code>。<strong>网关问 Nacos 拿 <code>smart-building</code> 组 → 404 → 配置全空</strong>。</p>
<h3 id="修复-1"><a href="#修复-1" class="headerlink" title="修复"></a>修复</h3><p><code>bootstrap.yml</code> 5 处 <code>$&#123;NACOS_GROUP:smart-building&#125;</code> 全部改成 <code>smart_building</code>：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring.cloud.nacos.discovery.group:</span> <span class="string">smart_building</span></span><br><span class="line"><span class="attr">spring.cloud.nacos.config.group:</span> <span class="string">smart_building</span></span><br><span class="line"><span class="comment"># ... shared-configs[0..2].group</span></span><br></pre></td></tr></table></figure>

<h3 id="JWT-NPE-怎么挖出来的"><a href="#JWT-NPE-怎么挖出来的" class="headerlink" title="JWT NPE 怎么挖出来的"></a>JWT NPE 怎么挖出来的</h3><p>第 3 个 NPE 的堆栈只指到 <code>JwtTokenService.&lt;init&gt;:54</code>，源码在 <code>com.xxkj:common-auth</code> jar 里。<strong>诊断路径</strong>：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 反编译 jar</span></span><br><span class="line">javap -p -classpath common-auth-2.6.0-SNAPSHOT.jar com.xxkj.auth.service.JwtTokenService</span><br><span class="line">javap -p -classpath ... com.xxkj.auth.properties.JwtAuthProperties</span><br><span class="line">javap -v -classpath ... com.xxkj.auth.model.JwtAuthInfo   <span class="comment"># 找 @ConfigurationProperties 的 prefix</span></span><br></pre></td></tr></table></figure>

<p>反编译结果：<code>JwtAuthProperties</code> 的 prefix &#x3D; <code>icfg.auth</code>，字段 <code>jwts</code> 继承 <code>BaseAuthInfo</code>（含 <code>secret</code>、<code>accessTokenExpiration</code>、<code>refreshTokenExpiration</code> 等）。</p>
<h3 id="Nacos-完整配置"><a href="#Nacos-完整配置" class="headerlink" title="Nacos 完整配置"></a>Nacos 完整配置</h3><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">icfg:</span></span><br><span class="line">  <span class="attr">admin:</span></span><br><span class="line">    <span class="attr">service-id:</span> <span class="string">admin</span></span><br><span class="line">  <span class="attr">auth:</span></span><br><span class="line">    <span class="attr">jwts:</span></span><br><span class="line">      <span class="attr">secret:</span> <span class="string">smart-park-default-jwt-secret-please-change-in-production</span></span><br><span class="line">      <span class="attr">accessTokenExpiration:</span> <span class="number">3600</span></span><br><span class="line">      <span class="attr">refreshTokenExpiration:</span> <span class="number">86400</span></span><br><span class="line">      <span class="attr">autoRefresh:</span> <span class="literal">true</span></span><br><span class="line">      <span class="attr">singleLogin:</span> <span class="literal">false</span></span><br><span class="line">      <span class="attr">issuer:</span> <span class="string">smart-park</span></span><br><span class="line">  <span class="attr">common:</span></span><br><span class="line">    <span class="attr">deployments:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">serviceId:</span> <span class="string">through-service</span></span><br><span class="line">        <span class="attr">deployPort:</span> <span class="number">18134</span></span><br><span class="line">      <span class="comment"># ... 共 11 个服务</span></span><br><span class="line">    <span class="attr">feign-url:</span></span><br><span class="line">      <span class="attr">aided:</span> <span class="string">http://127.0.0.1:18100</span></span><br><span class="line">      <span class="attr">admin:</span> <span class="string">http://127.0.0.1:18200</span></span><br><span class="line">      <span class="attr">through:</span> <span class="string">http://127.0.0.1:18134</span></span><br><span class="line">      <span class="attr">security:</span> <span class="string">http://127.0.0.1:18135</span></span><br><span class="line">      <span class="attr">iot:</span> <span class="string">http://127.0.0.1:18350</span></span><br></pre></td></tr></table></figure>

<h2 id="坑-6：前端-401-不一定是-token-错"><a href="#坑-6：前端-401-不一定是-token-错" class="headerlink" title="坑 6：前端 401 不一定是 token 错"></a>坑 6：前端 401 不一定是 token 错</h2><h3 id="现象-2"><a href="#现象-2" class="headerlink" title="现象"></a>现象</h3><p>前端 <code>npm run dev</code> 起来后点登录：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;status&quot;</span><span class="punctuation">:</span> <span class="number">500</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;message&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Connection refused: no further information: /127.0.0.1:18200&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>把 <code>VUE_APP_GATEWAY</code> 改成 <code>http://localhost:10001</code>（本地网关）后，<strong>报 401</strong>。</p>
<h3 id="根因-2"><a href="#根因-2" class="headerlink" title="根因"></a>根因</h3><p><code>JwtAuthProperties.publicApiPatterns</code>（反编译 <code>com.xxkj:common-auth</code>）是<strong>白名单</strong>——只有路径在这个白名单里，网关的 <code>AccessControlFilter</code> 才放行到下游。<strong>没配的话默认是空集</strong> → 任何 <code>/admin/**</code> 请求都被网关直接 401。</p>
<h3 id="修复-2"><a href="#修复-2" class="headerlink" title="修复"></a>修复</h3><p>Nacos <code>application.yml</code> 加 <code>publicApiPatterns</code>：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">icfg:</span></span><br><span class="line">  <span class="attr">auth:</span></span><br><span class="line">    <span class="attr">publicApiPatterns:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">/admin/user/login</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">/admin/user/logout</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">/admin/captcha/image</span></span><br></pre></td></tr></table></figure>

<h3 id="前端两处配套改"><a href="#前端两处配套改" class="headerlink" title="前端两处配套改"></a>前端两处配套改</h3><p><code>E:\nanwang\zhyq-admin - no module\.env.development</code> 第 10 行：</p>
<figure class="highlight diff"><table><tr><td class="code"><pre><span class="line"><span class="deletion">- VUE_APP_GATEWAY = &#x27;http://10.182.5.212:8080&#x27;</span></span><br><span class="line"><span class="addition">+ VUE_APP_GATEWAY = &#x27;http://localhost:10001&#x27;</span></span><br></pre></td></tr></table></figure>

<p><code>vue.config.js</code> 第 39-41 行：</p>
<figure class="highlight diff"><table><tr><td class="code"><pre><span class="line">  &#x27;/gateway&#x27;: &#123;</span><br><span class="line">    target: process.env.VUE_APP_GATEWAY,</span><br><span class="line">    secure: false,</span><br><span class="line">    changeOrigin: true,</span><br><span class="line"><span class="deletion">-   //  pathRewrite: &#123; &#x27;^/gateway&#x27;: &#x27;&#x27; &#125;</span></span><br><span class="line"><span class="addition">+   pathRewrite: &#123; &#x27;^/gateway&#x27;: &#x27;&#x27; &#125;</span></span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>[!warning] 三种白名单的区别</p>
<ul>
<li><code>loginRelaApis</code> — Gateway 拦截这些路径，<strong>加 token</strong> 再返回</li>
<li><code>logoutRelaApis</code> — Gateway 拦截这些路径，<strong>清除 token</strong></li>
<li><code>publicApiPatterns</code> — <strong>免鉴权</strong>路径（不校验 token 就能访问）</li>
</ul>
<p>登录接口要同时配 <code>loginRelaApis</code> + <code>publicApiPatterns</code> 才能正确工作。</p>
</blockquote>
<h2 id="6-个坑的链路总结"><a href="#6-个坑的链路总结" class="headerlink" title="6 个坑的链路总结"></a>6 个坑的链路总结</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">✅ MySQL 用户（坑1） ─→ ✅ property IP（坑2） ─→ ✅ admin 启动</span><br><span class="line">                                                    │</span><br><span class="line">                          ❌ 密码错（坑3） ─→ ❌ is_enable=1 锁定（坑4） ─→ ❌ JWT NPE（坑5）</span><br><span class="line">                                                                                │</span><br><span class="line">                                                                                ▼</span><br><span class="line">                                                              ❌ 前端 401（坑6）</span><br></pre></td></tr></table></figure>

<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><h3 id="排查顺序的智慧"><a href="#排查顺序的智慧" class="headerlink" title="排查顺序的智慧"></a>排查顺序的智慧</h3><p>6 个坑看似随机，但<strong>顺序排查</strong>效率最高：先连得上（用户、IP），再起得来（启动参数、JWT），最后访问得通（白名单、路由）。<strong>不要跳着来</strong>——后面的问题依赖前面的解决状态。</p>
<h3 id="反直觉约定的来源"><a href="#反直觉约定的来源" class="headerlink" title="反直觉约定的来源"></a>反直觉约定的来源</h3><ul>
<li><code>is_enable=1</code> 是禁用：Blade 框架通用约定，但违反 Spring&#x2F;Java 直觉</li>
<li>密码是 SHA512×2：Blade 框架的旧版加密，未切到 BCrypt</li>
<li>8 个库不是 11 个：项目历史变更</li>
</ul>
<p><strong>有 <code>DictConstants.X</code> 这种常量时，永远看常量、不要猜</strong>。</p>
<h3 id="401-≠-token-失效"><a href="#401-≠-token-失效" class="headerlink" title="401 ≠ token 失效"></a>401 ≠ token 失效</h3><p>看到 401 第一反应是”token 失效”，但<strong>更常见的是白名单没配</strong>——尤其是登录接口本身被 401，是经典”白名单缺登录路径”症状。<strong>白名单问题优先排查</strong>。</p>
<h3 id="500-网关放行了"><a href="#500-网关放行了" class="headerlink" title="500 &#x3D; 网关放行了"></a>500 &#x3D; 网关放行了</h3><p>看到 500 + <code>Connection refused: 18200</code>，<strong>是好消息</strong>——说明网关这层完全 OK，问题只在下游服务没起。<strong>别去改网关</strong>。</p>
<h3 id="javap-是-jar-反编译必杀器"><a href="#javap-是-jar-反编译必杀器" class="headerlink" title="javap 是 jar 反编译必杀器"></a>javap 是 jar 反编译必杀器</h3><p>JDK 自带 <code>javap</code>，反编译私有 jar 不需要额外装。常用命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">javap -p ClassName              <span class="comment"># 看方法签名</span></span><br><span class="line">javap -v ClassName              <span class="comment"># 看字节码（含注解）</span></span><br><span class="line">javap -classpath xxx.jar ClassName  <span class="comment"># 看 jar 里某个类</span></span><br></pre></td></tr></table></figure>

<h2 id="下一步"><a href="#下一步" class="headerlink" title="下一步"></a>下一步</h2><p>启动链路打通了，但 admin 启动后菜单还是空——<code>sys_role</code> &#x2F; <code>sys_resource</code> 都没数据。下一篇讲怎么补 token、登录后权限、以及 980.sql 数据导入。</p>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>南网</category>
      </categories>
      <tags>
        <tag>Spring Cloud</tag>
        <tag>Gateway</tag>
        <tag>Feign</tag>
        <tag>JWT</tag>
        <tag>Nacos</tag>
        <tag>启动修复</tag>
      </tags>
  </entry>
  <entry>
    <title>接手烂尾的 Spring Cloud 微服务项目：从零搭建 26 模块编译环境</title>
    <url>/2026/06/09/2026-06-09-spring-cloud-env-setup/</url>
    <content><![CDATA[<h2 id="导读"><a href="#导读" class="headerlink" title="导读"></a>导读</h2><blockquote>
<p>接手一个停更 2 年的 Spring Cloud Alibaba 微服务项目，13 个业务服务、11 个独立数据库、26 个 Maven 模块，本地 Maven 仓库空空如也，<code>mvn compile</code> 一跑就是红海一片。这篇记录我从零搭建编译环境的完整过程，包括 Maven 配置重写、私有依赖安装、22 个 SNAPSHOT jar 的手工恢复，以及最终 26 模块 BUILD SUCCESS 的关键命令。</p>
</blockquote>
<span id="more"></span>

<div class="audio-player">
  <div class="audio-section">
    <h4>🎧 文章导读</h4>
    <audio controls preload="metadata">
      <source src="/audio/2026-06-09-spring-cloud-env-setup/narration.mp3" type="audio/mpeg">
    </audio>
  </div>
  <div class="audio-section">
    <h4>🎵 背景音乐</h4>
    <audio controls loop preload="metadata">
      <source src="/audio/2026-06-09-spring-cloud-env-setup/bgm.mp3" type="audio/mpeg">
    </audio>
  </div>
</div>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>智慧园区云平台（smart-park-cloud），项目 GroupId <code>cn.csg.building</code>，主干停留在 2023-12-27，<code>dist_sync</code> 分支最后提交 2025-07-30。JDK 1.8 + Spring Boot 2.5.7 + Spring Cloud 2020.0.5 + Spring Cloud Alibaba 2021.1——这一套组合在 2024 年都算”老古董”了，更别说还有私有的 <code>com.xxkj</code> 框架、Elastic-Job 1.x、Oracle JDBC 12c 这些早已不在公共仓库里的依赖。</p>
<p>最初的工作预期是”拉下来就能跑”，结果 <code>mvn compile</code> 报 <code>Could not find artifact com.xxkj:xxkj-framework:2.6.0-SNAPSHOT</code>——本地仓库没有，连这个 jar 在 Maven Central 上根本搜不到。本文记录怎么从这种”四无”状态（无文档、无私服账号、无原开发、无新提交）一步步把编译跑绿。</p>
<p><img data-src="/images/posts/2026-06-09-spring-cloud-env-setup/fig1_001.jpg" alt="项目模块依赖结构图"><br><em>图1：26 个 Maven 模块依赖关系</em></p>
<h2 id="项目概况"><a href="#项目概况" class="headerlink" title="项目概况"></a>项目概况</h2><table>
<thead>
<tr>
<th>维度</th>
<th>值</th>
</tr>
</thead>
<tbody><tr>
<td>项目名</td>
<td>smart-park-cloud（智慧园区云平台）</td>
</tr>
<tr>
<td>GroupId</td>
<td><code>cn.csg.building</code></td>
</tr>
<tr>
<td>技术栈</td>
<td>Spring Boot 2.5.7 + Spring Cloud 2020.0.5 + Spring Cloud Alibaba 2021.1</td>
</tr>
<tr>
<td>Java 版本</td>
<td>JDK 1.8.0_261（<code>E:\jdk</code>）</td>
</tr>
<tr>
<td>Maven</td>
<td><code>E:\maven363\apache-maven-3.6.3</code></td>
</tr>
<tr>
<td>本地仓库</td>
<td><code>F:\LocalWarehouse</code></td>
</tr>
<tr>
<td>当前分支</td>
<td><code>dist_sync</code>（2025-07-30 最后提交）</td>
</tr>
<tr>
<td>主分支</td>
<td><code>master</code>（停在 2023-12-27）</td>
</tr>
</tbody></table>
<p><strong>模块规模</strong>：13 个微服务 + 11 个独立数据库 + 9 个业务服务，共 <strong>26 个 Maven 模块</strong>。</p>
<blockquote>
<p>[!warning] 看似简单的项目结构<br>13 个服务看着不多，但 <code>through-service</code> 一项就引用 7 个其他服务（admin、aided、security、smart、property、env、warning），所有跨服务调用都通过 OpenFeign 走 Nacos 服务发现。任何一环缺依赖，整链都编译不过。</p>
</blockquote>
<p><img data-src="/images/posts/2026-06-09-spring-cloud-env-setup/fig2_001.jpg" alt="编译报错场景"><br><em>图2：mvn compile 报错信息</em></p>
<h2 id="问题背景"><a href="#问题背景" class="headerlink" title="问题背景"></a>问题背景</h2><p>接到项目后第一件事是 <code>mvn clean compile</code>，结果：</p>
<ol>
<li><strong>内网 Maven 私服超时</strong>：默认配置指向 <code>10.219.23.13:8081</code>，本机根本连不上，每次依赖解析都要等 30 秒 timeout</li>
<li><strong>本地仓库空空如也</strong>：除了系统自带的 jar，项目私有的 <code>com.xxkj</code> 系列全部缺失</li>
<li><strong>私有 jar 从 git 删了</strong>：<code>integrate-iot</code> 模块引用的 7 个 system scope jar 被标记为 “no jar”，git history 里也丢了</li>
<li><strong>失效的依赖</strong>：<code>com.sun.jna:examples:1.0.0</code> 在全球公共仓库都不存在，但项目里没有任何代码用</li>
</ol>
<h2 id="核心内容"><a href="#核心内容" class="headerlink" title="核心内容"></a>核心内容</h2><h3 id="一、分析项目结构"><a href="#一、分析项目结构" class="headerlink" title="一、分析项目结构"></a>一、分析项目结构</h3><p>先把 13 个微服务 + 11 个数据库摸清。翻了所有 <code>bootstrap*.yml</code> 和 Flyway 迁移脚本后，整理出服务依赖矩阵：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">service-util (基础工具)</span><br><span class="line">   └── service-model (POJO/DTO)</span><br><span class="line">          ├── admin-api ~ env-api (10 个 API 模块)</span><br><span class="line">          ├── admin-service, aided-service, bpm-service</span><br><span class="line">          ├── env-service, property-service, security-service</span><br><span class="line">          ├── smart-service, through-service, warning-service</span><br><span class="line">          ├── integrate-d3, integrate-iot (外部集成)</span><br><span class="line">   └── xxkj-framework (私有框架)</span><br><span class="line">          ├── xxkj-framework-dependencies (BOM)</span><br><span class="line">          └── service-gateway, service-schedule (网关与调度)</span><br></pre></td></tr></table></figure>

<p><strong>关键发现</strong>：<code>through-service</code> 是依赖最重的核心模块，它要调 7 个其他服务的 Feign client，单独编译它就得先编译整个依赖链。</p>
<h3 id="二、Maven-环境配置"><a href="#二、Maven-环境配置" class="headerlink" title="二、Maven 环境配置"></a>二、Maven 环境配置</h3><h4 id="2-1-找到真正在用的-settings-xml"><a href="#2-1-找到真正在用的-settings-xml" class="headerlink" title="2.1 找到真正在用的 settings.xml"></a>2.1 找到真正在用的 settings.xml</h4><p>最初以为 Maven 配置在 <code>C:\Users\张\.m2\settings.xml</code>，但 IDEA 启动后报”找不到 com.xxkj:xxkj-framework”。仔细看 Maven 帮助才意识到：**IDEA 用的 Maven 是 <code>E:\maven363</code>**，不是 PATH 里的默认。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 找到 IDEA 实际用的 settings.xml</span></span><br><span class="line"><span class="built_in">ls</span> -la <span class="string">&quot;E:\maven363\apache-maven-3.6.3\conf\settings.xml&quot;</span></span><br><span class="line"><span class="comment"># 原文件 600+ 行，里面激活了大量内网私服 profile</span></span><br></pre></td></tr></table></figure>

<h4 id="2-2-重写-settings-xml"><a href="#2-2-重写-settings-xml" class="headerlink" title="2.2 重写 settings.xml"></a>2.2 重写 settings.xml</h4><p>原配置问题严重：</p>
<ul>
<li>激活 5+ 个内网私服 profile</li>
<li>默认每次解析都先打 <code>10.219.23.13</code>（堡垒机）</li>
<li>每次依赖解析卡 30 秒超时</li>
</ul>
<p>新配置思路：<strong>移除所有内网仓库，只保留阿里云镜像</strong>。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">settings</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">mirrors</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">mirror</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">id</span>&gt;</span>aliyun-public<span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">name</span>&gt;</span>aliyun maven<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">url</span>&gt;</span>https://maven.aliyun.com/repository/public<span class="tag">&lt;/<span class="name">url</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">mirrorOf</span>&gt;</span>central,jcenter,public<span class="tag">&lt;/<span class="name">mirrorOf</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">mirror</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">mirror</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">id</span>&gt;</span>aliyun-spring<span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">name</span>&gt;</span>aliyun spring<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">url</span>&gt;</span>https://maven.aliyun.com/repository/spring<span class="tag">&lt;/<span class="name">url</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">mirrorOf</span>&gt;</span>spring-milestones,spring-snapshots<span class="tag">&lt;/<span class="name">mirrorOf</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">mirror</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">mirrors</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">settings</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>修改后依赖解析速度从 <strong>30s+&#x2F;次 降到 &lt; 1s&#x2F;次</strong>。</p>
<blockquote>
<p>[!tip] 小技巧<br>备份原配置很重要——后续可能要对比 <code>blade.yaml</code> 在内网配置了什么。<code>settings.xml.bak2</code> 是个明显的标记文件。</p>
</blockquote>
<h3 id="三、私有依赖安装"><a href="#三、私有依赖安装" class="headerlink" title="三、私有依赖安装"></a>三、私有依赖安装</h3><h4 id="3-1-从别人给的本地仓库提取"><a href="#3-1-从别人给的本地仓库提取" class="headerlink" title="3.1 从别人给的本地仓库提取"></a>3.1 从别人给的本地仓库提取</h4><p>关键素材：<code>E:\nanwang\xxkj</code> —— 之前同事给的本地仓库目录，里面有 22 个 <code>com.xxkj</code> SNAPSHOT jar。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 复制到 IDEA 真正用的本地仓库</span></span><br><span class="line"><span class="built_in">cp</span> -r <span class="string">&quot;E:\nanwang\xxkj\*&quot;</span> <span class="string">&quot;F:\LocalWarehouse\&quot;</span></span><br></pre></td></tr></table></figure>

<p>但是 Maven 报”找不到 jar”——因为<strong>手动复制文件不会生成 <code>_remote.repositories</code> 元数据</strong>。正确做法是用 <code>install:install-file</code> 安装：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mvn install:install-file \</span><br><span class="line">  -Dfile=xxkj-framework-2.6.0-SNAPSHOT.jar \</span><br><span class="line">  -DgroupId=com.xxkj \</span><br><span class="line">  -DartifactId=xxkj-framework \</span><br><span class="line">  -Dversion=2.6.0-SNAPSHOT \</span><br><span class="line">  -Dpackaging=jar</span><br></pre></td></tr></table></figure>

<p>22 个 jar 一个个装确实麻烦，写了个 PowerShell 循环脚本，10 分钟搞定。</p>
<h4 id="3-2-从-git-恢复-7-个-system-scope-jar"><a href="#3-2-从-git-恢复-7-个-system-scope-jar" class="headerlink" title="3.2 从 git 恢复 7 个 system scope jar"></a>3.2 从 git 恢复 7 个 system scope jar</h4><p>项目里 <code>integrate-iot</code> 模块用 system scope 引用了 3 个目录下的 jar：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.dataphin<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>dataphinlibs<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.0<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">scope</span>&gt;</span>system<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">systemPath</span>&gt;</span>$&#123;project.basedir&#125;/dataphinlibs/dataphin-api-1.0.jar<span class="tag">&lt;/<span class="name">systemPath</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>这些 jar 在 <code>git status</code> 里显示被删除（项目配置 “no jar”）。从 git history 里翻到 2024 年的提交，逐个 <code>git checkout &lt;hash&gt; -- path/to/jar</code>，恢复 7 个 jar。</p>
<h3 id="四、缺失依赖补齐"><a href="#四、缺失依赖补齐" class="headerlink" title="四、缺失依赖补齐"></a>四、缺失依赖补齐</h3><h4 id="4-1-Elastic-Job-1-x-6-个-jar"><a href="#4-1-Elastic-Job-1-x-6-个-jar" class="headerlink" title="4.1 Elastic-Job 1.x 6 个 jar"></a>4.1 Elastic-Job 1.x 6 个 jar</h4><p><code>com.dangdang:elastic-job-*</code> 在 Maven Central 上还有，但被阿里云镜像拉黑（License 问题）。手动从 Central 下载：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mvn dependency:get -Dartifact=com.dangdang:elastic-job-lite-core:2.1.5</span><br><span class="line">mvn dependency:get -Dartifact=com.dangdang:elastic-job-lite-spring:2.1.5</span><br><span class="line"><span class="comment"># ... 共 6 个</span></span><br></pre></td></tr></table></figure>

<h4 id="4-2-Oracle-JDBC-12c"><a href="#4-2-Oracle-JDBC-12c" class="headerlink" title="4.2 Oracle JDBC 12c"></a>4.2 Oracle JDBC 12c</h4><p><code>com.oracle:ojdbc8:12.2.0.1.0</code> 因为 Oracle License 协议问题不在 Maven Central。下载后用 install-file 安装：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mvn install:install-file \</span><br><span class="line">  -Dfile=ojdbc8.jar \</span><br><span class="line">  -DgroupId=com.oracle \</span><br><span class="line">  -DartifactId=ojdbc8 \</span><br><span class="line">  -Dversion=12.2.0.1.0 \</span><br><span class="line">  -Dpackaging=jar</span><br></pre></td></tr></table></figure>

<h4 id="4-3-swift-java-sdk-与-kaptcha"><a href="#4-3-swift-java-sdk-与-kaptcha" class="headerlink" title="4.3 swift-java-sdk 与 kaptcha"></a>4.3 swift-java-sdk 与 kaptcha</h4><p>项目用了一个很老的 Swift Pass SDK 和 kaptcha 验证码。这些在 Maven Central 都搜不到，但 <code>E:\nanwang\swift-java-sdk\</code> 和 <code>E:\nanwang\kaptcha\</code> 目录里有现成的 jar。同样用 <code>install-file</code> 安装。</p>
<h3 id="五、无用依赖处理"><a href="#五、无用依赖处理" class="headerlink" title="五、无用依赖处理"></a>五、无用依赖处理</h3><p><code>service-util/pom.xml</code> 里有两段 <code>com.sun.jna</code> 依赖：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.sun.jna<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>examples<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.0.0<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p><code>com.sun.jna:examples:1.0.0</code> 在全球公共 Maven 仓库均不存在（<strong>1.0.0 版本就没发布过 examples</strong>），而且项目代码里没有任何 <code>import com.sun.jna.example</code>。直接注释掉。</p>
<p><img data-src="/images/posts/2026-06-09-spring-cloud-env-setup/fig3_001.jpg" alt="模块编译状态汇总"><br><em>图3：26 模块编译成功状态</em></p>
<h2 id="编译验证"><a href="#编译验证" class="headerlink" title="编译验证"></a>编译验证</h2><p>最终命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> <span class="string">&quot;/e/nanwang/smart-park-cloud - no jar&quot;</span></span><br><span class="line"><span class="built_in">export</span> JAVA_HOME=/e/jdk</span><br><span class="line"><span class="built_in">export</span> PATH=<span class="variable">$JAVA_HOME</span>/bin:<span class="variable">$PATH</span></span><br><span class="line">mvn compile -U -T 4</span><br></pre></td></tr></table></figure>

<p><strong>26 个模块全部编译通过</strong>（总耗时 1分38秒）：</p>
<table>
<thead>
<tr>
<th>模块类别</th>
<th>数量</th>
<th>状态</th>
</tr>
</thead>
<tbody><tr>
<td>父 POM</td>
<td>1</td>
<td>✅ SUCCESS</td>
</tr>
<tr>
<td>基础（util&#x2F;model）</td>
<td>2</td>
<td>✅ SUCCESS</td>
</tr>
<tr>
<td>业务 API 模块</td>
<td>10</td>
<td>✅ SUCCESS</td>
</tr>
<tr>
<td>业务 Service</td>
<td>9</td>
<td>✅ SUCCESS</td>
</tr>
<tr>
<td>集成模块</td>
<td>2</td>
<td>✅ SUCCESS</td>
</tr>
<tr>
<td>框架</td>
<td>2</td>
<td>✅ SUCCESS</td>
</tr>
<tr>
<td><strong>总计</strong></td>
<td><strong>26</strong></td>
<td><strong>✅ SUCCESS</strong></td>
</tr>
</tbody></table>
<h2 id="关键路径备忘"><a href="#关键路径备忘" class="headerlink" title="关键路径备忘"></a>关键路径备忘</h2><table>
<thead>
<tr>
<th>用途</th>
<th>路径</th>
</tr>
</thead>
<tbody><tr>
<td>项目根目录</td>
<td><code>E:\nanwang\smart-park-cloud - no jar</code></td>
</tr>
<tr>
<td>JDK</td>
<td><code>E:\jdk</code></td>
</tr>
<tr>
<td>Maven</td>
<td><code>E:\maven363\apache-maven-3.6.3</code></td>
</tr>
<tr>
<td>Maven 配置</td>
<td><code>E:\maven363\apache-maven-3.6.3\conf\settings.xml</code></td>
</tr>
<tr>
<td>Maven 备份</td>
<td><code>E:\maven363\apache-maven-3.6.3\conf\settings.xml.bak2</code></td>
</tr>
<tr>
<td>本地仓库（IDEA 使用的）</td>
<td><code>F:\LocalWarehouse</code></td>
</tr>
<tr>
<td>别人给的源仓库</td>
<td><code>E:\nanwang\xxkj</code></td>
</tr>
</tbody></table>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><h3 id="教训-1：IDEA-的-Maven-配置在-m2-之外"><a href="#教训-1：IDEA-的-Maven-配置在-m2-之外" class="headerlink" title="教训 1：IDEA 的 Maven 配置在 .m2 之外"></a>教训 1：IDEA 的 Maven 配置在 .m2 之外</h3><p>很多人以为 Maven 配置都在 <code>~/.m2/settings.xml</code>，但 **IDEA 自带 Maven 时会用自己捆绑的 <code>maven/conf/settings.xml</code>**。找错配置改半天无效，浪费时间。先确认 <code>File → Settings → Build → Maven</code> 里 IDEA 实际用的 Maven home 路径。</p>
<h3 id="教训-2：直接复制-jar-不等于安装"><a href="#教训-2：直接复制-jar-不等于安装" class="headerlink" title="教训 2：直接复制 jar 不等于安装"></a>教训 2：直接复制 jar 不等于安装</h3><p>Maven 本地仓库不只是 jar 文件，还有 <code>_remote.repositories</code>、<code>_maven.repositories</code>、<code>*.lastUpdated</code> 等元数据文件。<strong>手动复制 jar 后必须用 <code>mvn install:install-file</code> 重新安装</strong>，让 Maven 生成正确的元数据。否则 Maven 会一直报”找不到”。</p>
<h3 id="教训-3：阿里云镜像不是万能的"><a href="#教训-3：阿里云镜像不是万能的" class="headerlink" title="教训 3：阿里云镜像不是万能的"></a>教训 3：阿里云镜像不是万能的</h3><p>阿里云镜像默认拉黑了一些有 License 争议的包：</p>
<ul>
<li><code>com.dangdang:elastic-job-*</code>（GPL 协议争议）</li>
<li><code>com.oracle:ojdbc8</code>（Oracle OTN 协议）</li>
<li><code>com.sun.jna:examples</code>（根本不存在）</li>
</ul>
<p>这些需要从 Maven Central 单独下载，或用 <code>install-file</code> 安装本地 jar。</p>
<h3 id="教训-4：备份原配置永远是对的"><a href="#教训-4：备份原配置永远是对的" class="headerlink" title="教训 4：备份原配置永远是对的"></a>教训 4：备份原配置永远是对的</h3><p><code>settings.xml.bak2</code> 这一字命名虽然丑，但比覆盖原配置好得多。后续遇到”线上环境跑得好好的，本地怎么都不通”的情况，至少能 diff 出”我改了什么”。</p>
<h3 id="教训-5：先编译再启动"><a href="#教训-5：先编译再启动" class="headerlink" title="教训 5：先编译再启动"></a>教训 5：先编译再启动</h3><p>很多同事一上来就 <code>mvn spring-boot:run</code>，结果依赖问题、配置问题、环境问题全混在一起。<strong>先 <code>mvn compile</code> 把编译跑绿</strong>，再 <code>mvn package -DskipTests</code> 打 jar，最后才启动。问题分层定位，效率高 3 倍。</p>
<h2 id="未完成事项"><a href="#未完成事项" class="headerlink" title="未完成事项"></a>未完成事项</h2><ul>
<li><input disabled="" type="checkbox"> 本地数据库搭建（需从虚拟机导出 SQL，或用项目内 Flyway 迁移脚本初始化）</li>
<li><input disabled="" type="checkbox"> Nacos 配置中心搭建（需在 Nacos 中导入各服务的配置）</li>
<li><input disabled="" type="checkbox"> Redis &#x2F; RocketMQ 本地安装</li>
<li><input disabled="" type="checkbox"> 各微服务联调测试</li>
</ul>
<p>环境编译只是万里长征第一步，真正的考验是从空数据库到第一个 HTTP 接口调通——这个下一篇讲。</p>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>接手停更 2 年的微服务项目，环境搭建往往是最消耗心力的一步。<strong>耐心分析、增量修复、保留备份</strong>是核心心法。下一步就是把 11 个数据库的 214 个 Flyway 迁移脚本跑起来，让服务真的能连上 DB。</p>
<!-- 下一篇预告：《6 个启动连环坑：MySQL 用户、property IP、admin 种子、前端 401》 -->
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>南网</category>
      </categories>
      <tags>
        <tag>Spring Cloud</tag>
        <tag>微服务</tag>
        <tag>Maven</tag>
        <tag>环境搭建</tag>
        <tag>编译踩坑</tag>
      </tags>
  </entry>
  <entry>
    <title>登录 token 补齐与项目改动全量汇总</title>
    <url>/2026/06/10/2026-06-10-login-token-and-summary/</url>
    <content><![CDATA[<h2 id="导读"><a href="#导读" class="headerlink" title="导读"></a>导读</h2><blockquote>
<p>链路通了，admin 也起来了，但登录返回的响应没有 token——因为 token 是 Gateway 加的，不是 admin 加的。本篇解决：登录 token 补齐 + Nacos 分组统一 + admin 注册 Nacos + 980.sql 数据导入，以及一份完整的项目改动清单（5 个我们改的代码文件 + 3 个 dist_sync 分支已有的改动）。</p>
</blockquote>
<span id="more"></span>

<div class="audio-player">
  <div class="audio-section">
    <h4>🎧 文章导读</h4>
    <audio controls preload="metadata">
      <source src="/audio/2026-06-10-login-token-and-summary/narration.mp3" type="audio/mpeg">
    </audio>
  </div>
  <div class="audio-section">
    <h4>🎵 背景音乐</h4>
    <audio controls loop preload="metadata">
      <source src="/audio/2026-06-10-login-token-and-summary/bgm.mp3" type="audio/mpeg">
    </audio>
  </div>
</div>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>昨天把启动链路打通，今天登录后端点通了，但新问题又冒出来：</p>
<ol>
<li>登录响应没有 <code>token</code> 字段</li>
<li>Gateway 报 <code>Load balancer does not contain an instance for the service admin</code></li>
<li>登录后 <code>/user/info</code> 返回的菜单是空的</li>
</ol>
<p>每个问题背后都藏着一个独立的修复故事。</p>
<p><img data-src="/images/posts/2026-06-10-login-token-and-summary/fig1_001.jpg" alt="登录调用链架构图"><br><em>图1：前端→Gateway→admin 调用链</em></p>
<h2 id="问题-1：登录响应没有-token"><a href="#问题-1：登录响应没有-token" class="headerlink" title="问题 1：登录响应没有 token"></a>问题 1：登录响应没有 token</h2><h3 id="现象"><a href="#现象" class="headerlink" title="现象"></a>现象</h3><p>本地 <code>POST /admin/user/login</code> 返回：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;respCode&quot;</span><span class="punctuation">:</span> <span class="string">&quot;200&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;data&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;realName&quot;</span><span class="punctuation">:</span> <span class="string">&quot;admin&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;login_serial&quot;</span><span class="punctuation">:</span> <span class="string">&quot;1:1781057888640&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;roleCodeList&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;super_admin&quot;</span><span class="punctuation">]</span></span><br><span class="line">    <span class="comment">// 没有 token 和 loginUser 字段</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>线上返回却有 token 字段——差别在 Gateway。</p>
<h3 id="根因"><a href="#根因" class="headerlink" title="根因"></a>根因</h3><p><strong>token 是在 Gateway 层加的，不是 admin-service 自己加的</strong>。</p>
<p>线上调用链：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">前端 → Gateway(:10001) → ModifyAuthRespFilter 拦截 login → jwtTokenService.afterLoginProcess() → 加 token → 转发到 admin-service</span><br></pre></td></tr></table></figure>

<p><code>ModifyAuthRespFilter</code>（在 <code>xxkj-framework/.../service-gateway/.../ModifyAuthRespFilter.java</code>）会检查请求路径是否在 <code>jwtAuthProperties.getLoginRelaApis()</code> 集合中，匹配才拦截并加 token。</p>
<h3 id="修复"><a href="#修复" class="headerlink" title="修复"></a>修复</h3><p>Nacos 配置 <code>application.yml</code>（在 <code>smart-building</code> 分组下）原来只有 <code>publicApiPatterns</code>，**缺 <code>loginRelaApis</code> 和 <code>logoutRelaApis</code>**：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">icfg:</span></span><br><span class="line">  <span class="attr">auth:</span></span><br><span class="line">    <span class="attr">loginRelaApis:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">/admin/user/login</span></span><br><span class="line">    <span class="attr">logoutRelaApis:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">/admin/user/logout</span></span><br><span class="line">    <span class="attr">publicApiPatterns:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">/admin/user/login</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">/admin/user/logout</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">/admin/captcha/image</span></span><br></pre></td></tr></table></figure>

<h3 id="三个白名单字段的区别"><a href="#三个白名单字段的区别" class="headerlink" title="三个白名单字段的区别"></a>三个白名单字段的区别</h3><table>
<thead>
<tr>
<th>字段</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td><code>loginRelaApis</code></td>
<td>Gateway 拦截这些路径，<strong>加 token</strong> 再返回</td>
</tr>
<tr>
<td><code>logoutRelaApis</code></td>
<td>Gateway 拦截这些路径，<strong>清除 token</strong></td>
</tr>
<tr>
<td><code>publicApiPatterns</code></td>
<td><strong>免鉴权</strong>路径（不校验 token 就能访问）</td>
</tr>
</tbody></table>
<blockquote>
<p>[!warning] 配置错位代价高<br>登录接口需要同时配 <code>loginRelaApis</code> 和 <code>publicApiPatterns</code>——前者让 Gateway 加 token，后者让 Gateway 放行请求。少了任何一个，登录都会失败。</p>
</blockquote>
<h2 id="问题-2：Nacos-分组名不一致"><a href="#问题-2：Nacos-分组名不一致" class="headerlink" title="问题 2：Nacos 分组名不一致"></a>问题 2：Nacos 分组名不一致</h2><h3 id="现象-1"><a href="#现象-1" class="headerlink" title="现象"></a>现象</h3><p>登录成功后访问 <code>/admin/user/info</code> 报 500：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Load balancer does not contain an instance for the service admin</span><br></pre></td></tr></table></figure>

<h3 id="根因-1"><a href="#根因-1" class="headerlink" title="根因"></a>根因</h3><p>Gateway 和 admin-service 的 Nacos 分组默认值不一致：</p>
<table>
<thead>
<tr>
<th>服务</th>
<th>默认分组</th>
<th>实际</th>
</tr>
</thead>
<tbody><tr>
<td>Gateway</td>
<td><code>$&#123;NACOS_GROUP:smart_building&#125;</code>（下划线）</td>
<td>❌ 与其他服务不同</td>
</tr>
<tr>
<td>admin-service</td>
<td><code>$&#123;NACOS_GROUP:smart-building&#125;</code>（中划线）</td>
<td>✅</td>
</tr>
<tr>
<td>其他 10 个服务</td>
<td><code>$&#123;NACOS_GROUP:smart-building&#125;</code>（中划线）</td>
<td>✅</td>
</tr>
</tbody></table>
<p>10 个服务用中划线，<strong>只有 Gateway 用下划线</strong>。导致 Gateway 发现不了其他服务。</p>
<h3 id="修复-1"><a href="#修复-1" class="headerlink" title="修复"></a>修复</h3><p><strong>只改了 Gateway 一个文件</strong>，5 处 <code>smart_building</code> → <code>smart-building</code>：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># xxkj-framework/xxkj-framework-services/service-gateway/src/main/resources/bootstrap.yml</span></span><br><span class="line"><span class="attr">spring.cloud.nacos.discovery.group:</span> <span class="string">smart-building</span></span><br><span class="line"><span class="attr">spring.cloud.nacos.config.group:</span> <span class="string">smart-building</span></span><br><span class="line"><span class="string">shared-configs[0].group:</span> <span class="string">smart-building</span>      <span class="comment"># application.yml</span></span><br><span class="line"><span class="string">shared-configs[1].group:</span> <span class="string">smart-building</span>      <span class="comment"># application-auth.yml</span></span><br><span class="line"><span class="string">shared-configs[2].group:</span> <span class="string">smart-building</span>      <span class="comment"># application-$&#123;spring.profiles.active&#125;.yml</span></span><br></pre></td></tr></table></figure>

<p><strong>Nacos 配置迁移</strong>：</p>
<ul>
<li>在 <code>smart-building</code> 分组下创建 <code>application.yml</code>（内容同原 <code>smart_building</code> 分组下的）</li>
<li>删除 <code>smart_building</code> 分组下的 <code>application.yml</code></li>
</ul>
<blockquote>
<p>[!warning] 改 group 名的连锁反应<br>改 group 名之前<strong>必须</strong>先把 Nacos 上的配置复制到新 group，否则所有服务会发现不了配置，全部 404。</p>
</blockquote>
<h2 id="问题-3：admin-service-没注册到-Nacos"><a href="#问题-3：admin-service-没注册到-Nacos" class="headerlink" title="问题 3：admin-service 没注册到 Nacos"></a>问题 3：admin-service 没注册到 Nacos</h2><h3 id="现象-2"><a href="#现象-2" class="headerlink" title="现象"></a>现象</h3><p>即使分组统一了，admin-service 仍然注册不上 Nacos。</p>
<h3 id="根因-2"><a href="#根因-2" class="headerlink" title="根因"></a>根因</h3><p><code>admin-service</code> 的 <code>bootstrap-dev.yml</code> 第 6 行：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">register-enabled:</span> <span class="literal">false</span></span><br></pre></td></tr></table></figure>

<p>dev 配置<strong>关闭了 Nacos 服务注册</strong>。原作者本地开发时可能不需要服务发现（通过 <code>feign-url</code> 直连），但 <strong>Gateway 的 <code>AccessControlFilter</code> 需要通过服务发现找到 admin 来查权限</strong>。</p>
<h3 id="修复-2"><a href="#修复-2" class="headerlink" title="修复"></a>修复</h3><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># service-provider/admin-service/src/main/resources/bootstrap-dev.yml</span></span><br><span class="line"><span class="attr">register-enabled:</span> <span class="literal">false</span>   <span class="string">→</span>   <span class="attr">register-enabled:</span> <span class="literal">true</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p>[!tip] 注册 ≠ 服务发现<br>Nacos 注册（discovery）让其他服务通过服务名找到你；Nacos 配置（config）让你从 Nacos 拉配置。dev 默认关注册是因为本地可以用 <code>feign-url</code> 直连——但 Gateway 是个例外，它<strong>必须</strong>通过 Nacos 发现下游服务。</p>
</blockquote>
<p><img data-src="/images/posts/2026-06-10-login-token-and-summary/fig2_001.jpg" alt="Nacos 分组迁移示意"><br><em>图2：Nacos 分组名统一迁移</em></p>
<h2 id="问题-4：本地数据库缺基础数据"><a href="#问题-4：本地数据库缺基础数据" class="headerlink" title="问题 4：本地数据库缺基础数据"></a>问题 4：本地数据库缺基础数据</h2><h3 id="现象-3"><a href="#现象-3" class="headerlink" title="现象"></a>现象</h3><p>登录成功后 <code>/user/info</code> 返回：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;menus&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;resFunctions&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;parkId&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">null</span></span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;parkName&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">null</span></span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;orgId&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">null</span></span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>前端显示”无权限访问”。</p>
<h3 id="根因-3"><a href="#根因-3" class="headerlink" title="根因"></a>根因</h3><p>本地数据库只有 Flyway 建的空表，没有基础数据：</p>
<ul>
<li><code>sys_park</code>：空的 → 没有园区</li>
<li><code>sys_organization</code>：空的 → 没有机构</li>
<li><code>sys_user_park</code>：空的 → 用户没分配园区</li>
<li><code>sys_user_organization</code>：空的 → 用户没分配机构</li>
<li><code>sys_resource</code>：空的 → 没有菜单资源</li>
<li><code>sys_role_resource</code>：空的 → 角色没有权限</li>
</ul>
<p><code>UserController.userInfo()</code> 里 <code>resourceService.selectUserResources(appId, userId, endpoint, parkId)</code> —— parkId 为 null 导致查不到菜单。</p>
<h3 id="修复-3"><a href="#修复-3" class="headerlink" title="修复"></a>修复</h3><p>使用 <code>E:\nanwang\项目会用压缩包\980.sql</code>（从线上 <code>10.182.5.215</code> 导出的 <code>building_admin</code> 完整 mysqldump）直接导入：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mysql -h 127.0.0.1 -u building_admin -p<span class="string">&#x27;building_admin@123&#x27;</span> building_admin &lt; 980.sql</span><br></pre></td></tr></table></figure>

<h3 id="导入后数据量"><a href="#导入后数据量" class="headerlink" title="导入后数据量"></a>导入后数据量</h3><table>
<thead>
<tr>
<th>表</th>
<th>行数</th>
</tr>
</thead>
<tbody><tr>
<td>sys_user</td>
<td>6365</td>
</tr>
<tr>
<td>sys_park</td>
<td>8</td>
</tr>
<tr>
<td>sys_organization</td>
<td>664</td>
</tr>
<tr>
<td>sys_user_park</td>
<td>1116</td>
</tr>
<tr>
<td>sys_user_organization</td>
<td>42510</td>
</tr>
<tr>
<td>sys_resource</td>
<td>702</td>
</tr>
<tr>
<td>sys_role_resource</td>
<td>1870</td>
</tr>
<tr>
<td>sys_role</td>
<td>75</td>
</tr>
<tr>
<td>sys_user_role</td>
<td>242</td>
</tr>
</tbody></table>
<blockquote>
<p>[!warning] dump 的副作用<br>这个 dump 有 <code>DROP TABLE IF EXISTS</code>，会覆盖现有表结构和数据。导入后 Flyway 的 <code>flyway_schema_history</code> 表会被删掉（dump 里没有这张表），但不影响启动——Flyway 检测到表已存在会跳过建表。</p>
</blockquote>
<p><img data-src="/images/posts/2026-06-10-login-token-and-summary/fig3_001.jpg" alt="改动文件 diff 汇总"><br><em>图3：5 个文件改动分布</em></p>
<h2 id="项目改动全量汇总"><a href="#项目改动全量汇总" class="headerlink" title="项目改动全量汇总"></a>项目改动全量汇总</h2><p>以 <code>E:\nanwang\原后端\smart-park-cloud - no jar\smart-park-cloud - no jar</code>（完全没改过的原始副本）为基准，对比当前代码的所有差异。</p>
<h3 id="我们改的（5-个代码文件-环境配置）"><a href="#我们改的（5-个代码文件-环境配置）" class="headerlink" title="我们改的（5 个代码文件 + 环境配置）"></a>我们改的（5 个代码文件 + 环境配置）</h3><h4 id="1-admin-service-src-main-resources-bootstrap-dev-yml"><a href="#1-admin-service-src-main-resources-bootstrap-dev-yml" class="headerlink" title="1. admin-service/src/main/resources/bootstrap-dev.yml"></a>1. <code>admin-service/src/main/resources/bootstrap-dev.yml</code></h4><p><strong>1 行改动</strong>：<code>register-enabled: false</code> → <code>true</code></p>
<p><strong>原因</strong>：dev 配置关闭了 Nacos 服务注册，Gateway 的 <code>AccessControlFilter</code> 找不到 admin 服务。</p>
<h4 id="2-property-service-src-main-resources-bootstrap-dev-yml"><a href="#2-property-service-src-main-resources-bootstrap-dev-yml" class="headerlink" title="2. property-service/src/main/resources/bootstrap-dev.yml"></a>2. <code>property-service/src/main/resources/bootstrap-dev.yml</code></h4><p><strong>4 处 IP 替换</strong>：<code>10.152.238.245</code> → <code>127.0.0.1</code></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">NACOS_SERVER:10.152.238.245:8848  →  127.0.0.1:8848</span><br><span class="line">MYSQL_SERVER:10.152.238.245:3306  →  127.0.0.1:3306</span><br><span class="line">redis host: 10.152.238.245        →  127.0.0.1</span><br></pre></td></tr></table></figure>

<h4 id="3-through-service-src-main-resources-bootstrap-dev-yml"><a href="#3-through-service-src-main-resources-bootstrap-dev-yml" class="headerlink" title="3. through-service/src/main/resources/bootstrap-dev.yml"></a>3. <code>through-service/src/main/resources/bootstrap-dev.yml</code></h4><p><strong>3 处新增</strong>：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 新增1：关闭 Nacos 配置中心（本地不走 Nacos 拉配置）</span></span><br><span class="line"><span class="attr">config:</span></span><br><span class="line">  <span class="attr">enabled:</span> <span class="literal">false</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 新增2：admin 服务名</span></span><br><span class="line"><span class="attr">icfg:</span></span><br><span class="line">  <span class="attr">admin:</span></span><br><span class="line">    <span class="attr">service-id:</span> <span class="string">admin</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 新增3：feign-url 直连地址补全</span></span><br><span class="line"><span class="attr">feign-url:</span></span><br><span class="line">  <span class="attr">through:</span> <span class="string">http://127.0.0.1:18134</span></span><br><span class="line">  <span class="attr">security:</span> <span class="string">http://127.0.0.1:18135</span></span><br><span class="line">  <span class="attr">iot:</span> <span class="string">http://127.0.0.1:18350</span></span><br></pre></td></tr></table></figure>

<h4 id="4-service-gateway-src-main-resources-bootstrap-dev-yml"><a href="#4-service-gateway-src-main-resources-bootstrap-dev-yml" class="headerlink" title="4. service-gateway/src/main/resources/bootstrap-dev.yml"></a>4. <code>service-gateway/src/main/resources/bootstrap-dev.yml</code></h4><p>**新增 <code>deployments</code> 和 <code>feign-url</code>**：11 个服务的本地端口映射。</p>
<h4 id="5-service-util-pom-xml"><a href="#5-service-util-pom-xml" class="headerlink" title="5. service-util/pom.xml"></a>5. <code>service-util/pom.xml</code></h4><p><strong>注释掉 <code>com.sun.jna</code> 两个依赖</strong>：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!--</span></span><br><span class="line"><span class="comment">&lt;dependency&gt;</span></span><br><span class="line"><span class="comment">    &lt;groupId&gt;com.sun.jna&lt;/groupId&gt;</span></span><br><span class="line"><span class="comment">    &lt;artifactId&gt;examples&lt;/artifactId&gt;</span></span><br><span class="line"><span class="comment">    &lt;version&gt;1.0.0&lt;/version&gt;</span></span><br><span class="line"><span class="comment">&lt;/dependency&gt;</span></span><br><span class="line"><span class="comment">--&gt;</span></span><br></pre></td></tr></table></figure>

<p><strong>原因</strong>：<code>com.sun.jna:examples:1.0.0</code> 在全球公共 Maven 仓库均不存在，项目内零代码引用。</p>
<h3 id="环境配置改动（非代码）"><a href="#环境配置改动（非代码）" class="headerlink" title="环境配置改动（非代码）"></a>环境配置改动（非代码）</h3><table>
<thead>
<tr>
<th>#</th>
<th>改动</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td><code>E:\maven363\...\settings.xml</code></td>
<td>重写，移除内网仓库，只保留阿里云镜像</td>
</tr>
<tr>
<td>2</td>
<td>本地 Maven 仓库 <code>F:\LocalWarehouse</code></td>
<td>新增 xxkj、ojdbc8、swift-java-api、kaptcha、elastic-job 等 jar</td>
</tr>
<tr>
<td>3</td>
<td>Nacos 配置</td>
<td>新建 <code>smart-building</code> 分组，含 <code>loginRelaApis</code> 等；删除旧的 <code>smart_building</code> 分组</td>
</tr>
<tr>
<td>4</td>
<td>本地数据库 <code>building_admin</code></td>
<td>导入 <code>980.sql</code>（线上完整 dump）</td>
</tr>
<tr>
<td>5</td>
<td>Git 恢复的 jar（7 个）</td>
<td><code>integrate-iot/&#123;dataphinlibs,massmslibs,mqslibs&#125;/*.jar</code> 从 git 恢复</td>
</tr>
</tbody></table>
<h3 id="拿到手就已经改了的（不是我们改的）"><a href="#拿到手就已经改了的（不是我们改的）" class="headerlink" title="拿到手就已经改了的（不是我们改的）"></a>拿到手就已经改了的（不是我们改的）</h3><p>以下文件在原始副本里就已经和 git 上的不同了，说明是别人在 <code>dist_sync</code> 分支上提交的：</p>
<h4 id="1-PassUserGrantServiceImpl-java"><a href="#1-PassUserGrantServiceImpl-java" class="headerlink" title="1. PassUserGrantServiceImpl.java"></a>1. <code>PassUserGrantServiceImpl.java</code></h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 执行成功时 → 执行成功或失败时</span></span><br><span class="line"><span class="keyword">if</span> (Objects.equals(grantExecStatus, <span class="string">&quot;2&quot;</span>) || Objects.equals(grantExecStatus, <span class="string">&quot;3&quot;</span>)) &#123;</span><br><span class="line">    updateGrant.setDistSyncStatus(PaasDistSyncStatus.PENDING.getStatus());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>属于 <code>dist_sync</code> 分支的业务功能：下发失败也标记为待同步。</p>
<h4 id="2-AlarmingEvenSinkServiceImpl-java"><a href="#2-AlarmingEvenSinkServiceImpl-java" class="headerlink" title="2. AlarmingEvenSinkServiceImpl.java"></a>2. <code>AlarmingEvenSinkServiceImpl.java</code></h4><ul>
<li>方法重命名：<code>antiGravityVerification</code> → <code>allowGenRepeatAlarmRecord</code></li>
<li>返回值取反：3 处 <code>return this.doesXxx(...)</code> → <code>return !this.doesXxx(...)</code></li>
</ul>
<p>属于 <code>dist_sync</code> 分支的 bug 修复：原逻辑返回值语义反转导致重复告警判断错误。</p>
<h4 id="3-AlarmingRecordMapper-xml"><a href="#3-AlarmingRecordMapper-xml" class="headerlink" title="3. AlarmingRecordMapper.xml"></a>3. <code>AlarmingRecordMapper.xml</code></h4><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 新增排序 --&gt;</span></span><br><span class="line">ORDER BY alarm_time DESC</span><br><span class="line">LIMIT 1</span><br></pre></td></tr></table></figure>

<p>属于 <code>dist_sync</code> 分支的功能修改。</p>
<h3 id="未改动的文件（git-显示有差异但实际没改）"><a href="#未改动的文件（git-显示有差异但实际没改）" class="headerlink" title="未改动的文件（git 显示有差异但实际没改）"></a>未改动的文件（git 显示有差异但实际没改）</h3><table>
<thead>
<tr>
<th>文件</th>
<th>实际情况</th>
</tr>
</thead>
<tbody><tr>
<td><code>QrCodeByIdTypeUtils.java</code></td>
<td>只有换行符（CRLF&#x2F;LF）差异，内容完全一致</td>
</tr>
<tr>
<td><code>gateway/bootstrap.yml</code></td>
<td>只有换行符差异，内容完全一致</td>
</tr>
</tbody></table>
<h2 id="改动总结"><a href="#改动总结" class="headerlink" title="改动总结"></a>改动总结</h2><p><strong>5 个文件的本地环境适配</strong>，全部是配置文件（<code>bootstrap-dev.yml</code> 和 <code>pom.xml</code>），没有改任何业务逻辑代码。</p>
<p><strong>核心目的</strong>：</p>
<ol>
<li>让服务连本地 Nacos&#x2F;MySQL&#x2F;Redis（改 IP、开注册）</li>
<li>让 Gateway 知道本地各服务的端口（加 deployments 和 feign-url）</li>
<li>解决一个编译依赖缺失（注释掉 jna）</li>
</ol>
<p><strong>零 Java 业务代码改动</strong>——这是接手烂尾项目最理想的状况：环境层全清，业务层不碰。</p>
<h2 id="当前状态"><a href="#当前状态" class="headerlink" title="当前状态"></a>当前状态</h2><table>
<thead>
<tr>
<th>节点</th>
<th>状态</th>
</tr>
</thead>
<tbody><tr>
<td>前端 → Gateway → admin</td>
<td>✅ 通</td>
</tr>
<tr>
<td>登录返回 token</td>
<td>✅ 已修复</td>
</tr>
<tr>
<td>admin-service 注册 Nacos</td>
<td>✅ 已修复</td>
</tr>
<tr>
<td>Nacos 分组统一</td>
<td>✅ 已修复</td>
</tr>
<tr>
<td>数据库基础数据</td>
<td>✅ 已导入</td>
</tr>
<tr>
<td>登录后菜单&#x2F;权限</td>
<td>✅ 应该正常</td>
</tr>
<tr>
<td>其他服务（through、aided 等）</td>
<td>⏳ 未启动</td>
</tr>
</tbody></table>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><h3 id="改动分类的纪律"><a href="#改动分类的纪律" class="headerlink" title="改动分类的纪律"></a>改动分类的纪律</h3><p>接手别人代码时，<strong>先做分类</strong>：我们改的 vs 别人改的 vs 仅换行符差异。这条纪律帮我后续排查问题时能直接定位”这改动是不是我的”——避免改了一个看似无关的代码导致预期外 bug。</p>
<h3 id="数据库-dump-的局限性"><a href="#数据库-dump-的局限性" class="headerlink" title="数据库 dump 的局限性"></a>数据库 dump 的局限性</h3><p><code>980.sql</code> 解决了基础数据问题，但同时带来：</p>
<ul>
<li><strong>不能复用生产数据</strong>：含真实手机号、身份证号，导入到本地 dev 环境存在合规风险</li>
<li><strong>Flyway 历史丢失</strong>：dump 没 <code>flyway_schema_history</code> 表，启动 Flyway 会重新比对 baseline，可能造成 migrate 失败</li>
<li><strong>测试隔离差</strong>：所有人共用同一份数据，单元测试不可重复</li>
</ul>
<p>长远看应该用 Flyway 迁移脚本 + 种子数据脚本代替完整 dump。</p>
<h3 id="反编译-jar-是必备技能"><a href="#反编译-jar-是必备技能" class="headerlink" title="反编译 jar 是必备技能"></a>反编译 jar 是必备技能</h3><p><code>com.xxkj:common-auth</code> 这种私有 jar 的字段名，靠 <code>javap</code> 反编译就能拿到。这比猜测快 10 倍。</p>
<h2 id="下一步"><a href="#下一步" class="headerlink" title="下一步"></a>下一步</h2><p>基础环境全部到位，下一篇进入真正的业务功能开发：钉钉同步补 <code>sys_user_park</code> 写入 + 园区绑定一级公司 + MyBatis OGNL 那个经典踩坑。</p>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>南网</category>
      </categories>
      <tags>
        <tag>JWT</tag>
        <tag>Nacos分组</tag>
        <tag>数据导入</tag>
        <tag>改动汇总</tag>
        <tag>Git</tag>
      </tags>
  </entry>
  <entry>
    <title>钉钉同步写 sys_user_park + 园区绑定一级公司：从功能到 OGNL 经典踩坑</title>
    <url>/2026/06/11/2026-06-11-ding-park-bind/</url>
    <content><![CDATA[<h2 id="导读"><a href="#导读" class="headerlink" title="导读"></a>导读</h2><blockquote>
<p>今天一口气做了 2 个 feature + 1 个构建命令摸清：钉钉同步补 <code>sys_user_park</code> 占位记录、园区绑定一级公司（顶级 org + 互斥）、admin-service 打包 fat jar。feature 走的是 spec-first + subagent-driven，3 轮 spec review + 3 轮 code review。但 06-11 集成测试<strong>第一次就跑出 500</strong>——<code>saveCompanyBind</code> 调 <code>updateParkOrganization</code> 时，generic mapper 对 Date 字段 OGNL 求值崩了。**1 行 fix：删掉多余的 <code>WebExtUtils.iniCreate(model);</code>**。这条经验已经写进全局 CLAUDE.md。</p>
</blockquote>
<span id="more"></span>

<div class="audio-player">
  <div class="audio-section">
    <h4>🎧 文章导读</h4>
    <audio controls preload="metadata">
      <source src="/audio/2026-06-11-ding-park-bind/narration.mp3" type="audio/mpeg">
    </audio>
  </div>
  <div class="audio-section">
    <h4>🎵 背景音乐</h4>
    <audio controls loop preload="metadata">
      <source src="/audio/2026-06-11-ding-park-bind/bgm.mp3" type="audio/mpeg">
    </audio>
  </div>
</div>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>业务方反馈”钉钉同步过来的用户进不了门”——门禁&#x2F;人脸下发找不到匹配的用户归属。<code>handleDingUserSync()</code> 写 <code>sys_user</code> 和 <code>sys_user_organization</code> 没问题，但<strong>没写 <code>sys_user_park</code>（用户-园区关系）</strong>。同时 <code>sys_park_organization</code> 桥表历史脏数据严重（顶级公司 + 子部门混着绑），业务方要求<strong>只配一级公司</strong>且<strong>一个公司只能被一个园区绑</strong>。</p>
<p>两个 feature 同根问题，一起做完刚好闭环。spec 提前写好（<code>docs/superpowers/specs/2026-06-10-ding-user-sync-user-park-design.md</code>、<code>docs/superpowers/specs/2026-06-11-park-company-bind-design.md</code>），3 轮 spec review 把模糊地带全聊清楚，再交给 subagent 执行。</p>
<p><img data-src="/images/posts/2026-06-11-ding-park-bind/fig1_001.jpg" alt="人-部门-公司-园区关系图"><br><em>图1：南网权限模型四级关系</em></p>
<h2 id="Feature-1：钉钉同步写-sys-user-park"><a href="#Feature-1：钉钉同步写-sys-user-park" class="headerlink" title="Feature 1：钉钉同步写 sys_user_park"></a>Feature 1：钉钉同步写 sys_user_park</h2><h3 id="业务背景"><a href="#业务背景" class="headerlink" title="业务背景"></a>业务背景</h3><p>南网项目的权限模型：用户 → <code>sys_user_park</code> 关联 → 园区 → 一级公司 → 子部门 → 数据权限。</p>
<p>钉钉是中台用户来源的主渠道。<code>UserSyncServiceImpl.handleDingUserSync()</code> 每天定时把钉钉通讯录里的员工同步进 <code>sys_user</code> 和 <code>sys_user_organization</code>，但**一直没有写 <code>sys_user_park</code>**。结果：</p>
<ul>
<li>园区级门禁权限下发找不到匹配的用户归属</li>
<li>园区消息推送按园区过滤时直接被忽略</li>
<li>园区级统计（<code>SELECT ... FROM sys_user WHERE park_id = ?</code>）永远是空</li>
</ul>
<h3 id="设计思路"><a href="#设计思路" class="headerlink" title="设计思路"></a>设计思路</h3><h4 id="核心数据关系"><a href="#核心数据关系" class="headerlink" title="核心数据关系"></a>核心数据关系</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">人 → 部门 → 公司 → 园区</span><br></pre></td></tr></table></figure>

<p>钉钉返回的 <code>pDeparts</code> 是 JSON 字符串，形如 <code>[[叶子部门id, 公司id, 序号]]</code>。第一组 <code>[0]</code> 是叶子部门，第二组 <code>[1]</code> 是公司 id。**园区维度只认”公司”**——因为一个公司对应一个园区（项目里的强假设），所以我们要的不是叶子部门，而是 <code>pDeparts[0][1]</code>。</p>
<h4 id="为什么不走”先全量同步再后处理”"><a href="#为什么不走”先全量同步再后处理”" class="headerlink" title="为什么不走”先全量同步再后处理”"></a>为什么不走”先全量同步再后处理”</h4><p>考虑过两个备选：</p>
<ul>
<li><strong>方案 A</strong>：<code>handleDingUserSync()</code> 只写 <code>sys_user</code>，园区关系走单独定时任务从 <code>sys_user_organization</code> 反推——这条路不通，链路长且依赖组织树完整性</li>
<li><strong>方案 B</strong>：<code>handleDingUserSync()</code> 里同步写 <code>sys_user_park</code>——<strong>采用</strong>。一个事务里把三张表都写好，下游门禁&#x2F;人脸&#x2F;统计全部能直接读到园区归属</li>
</ul>
<h4 id="桥表查不到时怎么办"><a href="#桥表查不到时怎么办" class="headerlink" title="桥表查不到时怎么办"></a>桥表查不到时怎么办</h4><p>公司 id 到 <code>park_id</code> 的映射走 <code>sys_park_organization</code> 桥表。但桥表里不一定有（比如新注册的公司还没建园区）：</p>
<ul>
<li><strong>跳过</strong>：不写 <code>sys_user_park</code> 记录，依赖下游业务容忍空 <code>park_id</code>——<strong>否决</strong>，下游会真的出问题</li>
<li><strong>写占位记录（<code>park_id = NULL</code>）</strong>：保留用户，运维通过 <code>WHERE park_id IS NULL</code> 找到待补登——<strong>采用</strong>。运维可观测性比”静默丢数据”重要</li>
</ul>
<h3 id="关键实现"><a href="#关键实现" class="headerlink" title="关键实现"></a>关键实现</h3><h4 id="1-解析钉钉-pDeparts-拿公司-id"><a href="#1-解析钉钉-pDeparts-拿公司-id" class="headerlink" title="1. 解析钉钉 pDeparts 拿公司 id"></a>1. 解析钉钉 <code>pDeparts</code> 拿公司 id</h4><p>在 <code>DataPhinUserResp</code> 内嵌类 <code>DataPhinUser</code> 上加了一个 <code>getCompanyIdFromPDeparts()</code> 纯函数：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// DataPhinUserResp.java:127-153</span></span><br><span class="line"><span class="meta">@JsonIgnore</span></span><br><span class="line"><span class="keyword">public</span> String <span class="title function_">getCompanyIdFromPDeparts</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (!StringUtils.hasText(<span class="built_in">this</span>.pDeparts)) <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        List&lt;List&lt;Object&gt;&gt; list = JsonUtils.parse(<span class="built_in">this</span>.pDeparts.trim(), List.class);</span><br><span class="line">        <span class="keyword">if</span> (list == <span class="literal">null</span> || list.isEmpty()) <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">        List&lt;Object&gt; firstGroup = list.get(<span class="number">0</span>);</span><br><span class="line">        <span class="keyword">if</span> (firstGroup == <span class="literal">null</span> || firstGroup.size() &lt; <span class="number">2</span>) <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">        <span class="type">Object</span> <span class="variable">companyId</span> <span class="operator">=</span> firstGroup.get(<span class="number">1</span>);</span><br><span class="line">        <span class="keyword">return</span> companyId == <span class="literal">null</span> ? <span class="literal">null</span> : String.valueOf(companyId);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123; <span class="keyword">return</span> <span class="literal">null</span>; &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>几个关键决定：</p>
<ul>
<li>**<code>@JsonIgnore</code>**：不参与 JSON 序列化&#x2F;反序列化，避免被框架误判</li>
<li><strong>异常吞掉返回 null</strong>：钉钉的 <code>pDeparts</code> 格式千奇百怪，任何一个解析失败都不应该让整个同步任务挂掉</li>
<li><strong><code>size() &lt; 2</code> 防御</strong>：钉钉的 <code>pDeparts</code> 在某些边界情况下只有 <code>[[叶子部门id]]</code> 一项（公司 id 缺失），直接 <code>get(1)</code> 会 <code>IndexOutOfBoundsException</code></li>
<li><strong>三段判空</strong>：<code>pDeparts</code> 字符串、解析后的 list、第一组 list，分别对应三个层级的失败</li>
</ul>
<h4 id="2-索引对齐：构造一个同源的-Map"><a href="#2-索引对齐：构造一个同源的-Map" class="headerlink" title="2. 索引对齐：构造一个同源的 Map"></a>2. 索引对齐：构造一个同源的 Map</h4><p>这是改动里最容易踩坑的地方：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// UserSyncServiceImpl.java:71-76</span></span><br><span class="line">Map&lt;String, DataPhinUser&gt; dingUserByDingId = comingDingUsers.stream()</span><br><span class="line">    .filter(u -&gt; !existDingUserMap.containsKey(u.getUserId()))  <span class="comment">// 与 toAddUsers 同步</span></span><br><span class="line">    .collect(Collectors.toMap(DataPhinUser::getUserId, Function.identity()));</span><br></pre></td></tr></table></figure>

<p>直觉上要拿 <code>DataPhinUser</code> 的字段（比如 <code>getCompanyIdFromPDeparts()</code>）就该写 <code>comingDingUsers.get(i)</code>——但 <code>toAddUsers</code> 已经被 <code>filter</code> 过了，索引完全错位了。</p>
<p><strong>隐藏的强约束</strong>：Map 的 filter 条件必须与 <code>toAddUsers</code> 严格一致。否则 <code>toAddUsers</code> 里有 X，但 <code>dingUserByDingId</code> 里没有 X，循环里 <code>dingUser = dingUserByDingId.get(addUser.getDingUserId())</code> 就拿到 null，然后 <code>continue</code> 跳过——<strong>静默少写 <code>sys_user_park</code> 记录</strong>。</p>
<h4 id="3-写-sys-user-park-占位记录"><a href="#3-写-sys-user-park-占位记录" class="headerlink" title="3. 写 sys_user_park 占位记录"></a>3. 写 <code>sys_user_park</code> 占位记录</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// UserSyncServiceImpl.java:193-224（简化版）</span></span><br><span class="line"><span class="keyword">for</span> (UserDTO addUser : toAddUsers) &#123;</span><br><span class="line">    <span class="type">DataPhinUser</span> <span class="variable">dingUser</span> <span class="operator">=</span> dingUserByDingId.get(addUser.getDingUserId());</span><br><span class="line">    <span class="keyword">if</span> (dingUser == <span class="literal">null</span>) <span class="keyword">continue</span>;          <span class="comment">// Map filter 漂移时的兜底</span></span><br><span class="line">    <span class="type">String</span> <span class="variable">companyId</span> <span class="operator">=</span> dingUser.getCompanyIdFromPDeparts();</span><br><span class="line">    <span class="keyword">if</span> (companyId == <span class="literal">null</span>) <span class="keyword">continue</span>;         <span class="comment">// pDeparts 解析失败的兜底</span></span><br><span class="line"></span><br><span class="line">    <span class="type">ParkOrganizationDTO</span> <span class="variable">q</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ParkOrganizationDTO</span>();</span><br><span class="line">    q.setOrgId(companyId);</span><br><span class="line">    List&lt;ParkOrganizationDTO&gt; parkOrgList = parkOrganizationService.listByCondition(q);</span><br><span class="line">    <span class="type">String</span> <span class="variable">parkId</span> <span class="operator">=</span> parkOrgList.isEmpty() ? <span class="literal">null</span> : parkOrgList.get(<span class="number">0</span>).getParkId();</span><br><span class="line"></span><br><span class="line">    <span class="type">UserParkDTO</span> <span class="variable">dto</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">UserParkDTO</span>();</span><br><span class="line">    dto.setUserId(addUser.getUserId());</span><br><span class="line">    dto.setOrgId(companyId);</span><br><span class="line">    dto.setParkId(parkId);</span><br><span class="line">    dto.setDeleteFlag(<span class="string">&quot;0&quot;</span>);</span><br><span class="line">    dto.setCreateName(<span class="string">&quot;中台数据同步&quot;</span>);</span><br><span class="line">    userParkService.addSelective(dto);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>两个 <code>continue</code> 兜底</strong>：第一处防御 Map filter 漂移，第二处防御 <code>pDeparts</code> 解析失败。</p>
<h3 id="测试与验证"><a href="#测试与验证" class="headerlink" title="测试与验证"></a>测试与验证</h3><ul>
<li><strong>单元测试</strong>：<code>getCompanyIdFromPDeparts()</code> 13&#x2F;13 通过，覆盖空值、格式错误、嵌套层级、<code>null</code> 元素、数字&#x2F;字符串 id</li>
<li><strong>编译验证</strong>：<code>mvn -pl service-provider/admin-service -am clean package -DskipTests</code> BUILD SUCCESS</li>
<li><strong>Code review</strong>：3 轮 spec review + 3 轮 code quality review，每轮都有实质调整</li>
</ul>
<h3 id="风险与后续"><a href="#风险与后续" class="headerlink" title="风险与后续"></a>风险与后续</h3><blockquote>
<p>[!warning] R1 整方法回滚<br><code>@Transactional(rollbackFor = Exception.class)</code>，单条 <code>addSelective</code> 抛异常会回滚整次同步。短期不修，长期拆成”按用户独立事务”。</p>
</blockquote>
<blockquote>
<p>[!warning] R6 filter 漂移<br>Map filter 和 <code>toAddUsers</code> filter 必须同步改，否则静默少写 <code>sys_user_park</code>。当前靠 code review 人工对照，建议提取公共 filter 方法。</p>
</blockquote>
<p><img data-src="/images/posts/2026-06-11-ding-park-bind/fig2_001.jpg" alt="sys_user_park 写入流程"><br><em>图2：钉钉同步写 sys_user_park</em></p>
<h2 id="Feature-2：园区绑定一级公司"><a href="#Feature-2：园区绑定一级公司" class="headerlink" title="Feature 2：园区绑定一级公司"></a>Feature 2：园区绑定一级公司</h2><h3 id="业务背景-1"><a href="#业务背景-1" class="headerlink" title="业务背景"></a>业务背景</h3><p>南网项目权限模型的中间一环”园区↔组织”绑得乱七八糟：</p>
<ul>
<li>园区 <code>番禺基地</code> 绑了 5 个 org，其中 3 个是二级公司（钉钉同步过来的子部门）</li>
<li>同一个 org（<code>110007195</code>）被两个不同园区同时绑了</li>
<li>还有 1116 条 <code>sys_user_park</code> 记录挂着这些混乱绑定</li>
</ul>
<p>业务方要求：<strong>园区维度只能配置一级公司</strong>（<code>org_type IS NULL</code> 且 <code>parent_id</code> 为空），并且<strong>一个公司只能被一个园区绑</strong>。</p>
<h3 id="设计思考"><a href="#设计思考" class="headerlink" title="设计思考"></a>设计思考</h3><h4 id="新建表-vs-复用旧表"><a href="#新建表-vs-复用旧表" class="headerlink" title="新建表 vs 复用旧表"></a>新建表 vs 复用旧表</h4><p>第一反应是新建一张 <code>sys_park_top_company</code>，但现实是：</p>
<ul>
<li>现有 <code>sys_park_organization</code> 已经有 <code>selectParkOrganizationList</code>、<code>updateParkOrganization</code> 这些<strong>现成</strong>的批量查&#x2F;upsert 方法</li>
<li><code>ParkOrganizationServiceImpl</code> 已经注入了 <code>ParkMapper</code>，复用等于 <strong>0 行 mapper 新增</strong></li>
<li>历史子级绑定的 1116 条 <code>sys_user_park</code> 记录如果不动，<strong>新建表 &#x3D; 旧数据继续生效 + 新功能并行</strong>，反而更乱</li>
</ul>
<p>最终选了<strong>复用旧表 + 加过滤 SQL</strong> 的方案。新功能<strong>只读&#x2F;写顶级公司</strong>，历史子级绑定<strong>作为只读数据保留</strong>。</p>
<h4 id="互斥校验的位置"><a href="#互斥校验的位置" class="headerlink" title="互斥校验的位置"></a>互斥校验的位置</h4><p>互斥校验放在 service 层而不是 SQL 层：</p>
<ul>
<li>MySQL 没合适的唯一索引能表达”org_id 全局唯一”</li>
<li>数据库级约束会<strong>带 N 个失败回滚</strong>，报错信息对前端不友好</li>
<li>业务校验可以<strong>一次性收集所有冲突</strong></li>
</ul>
<h3 id="关键实现-1"><a href="#关键实现-1" class="headerlink" title="关键实现"></a>关键实现</h3><h4 id="1-顶级公司查询-SQL"><a href="#1-顶级公司查询-SQL" class="headerlink" title="1. 顶级公司查询 SQL"></a>1. 顶级公司查询 SQL</h4><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- OrganizationMapperExt.xml --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">&quot;listTopLevelOrgs&quot;</span> <span class="attr">resultMap</span>=<span class="string">&quot;BaseResultMap&quot;</span>&gt;</span></span><br><span class="line">    SELECT org_id, org_name, org_code</span><br><span class="line">    FROM sys_organization</span><br><span class="line">    WHERE (parent_id = &#x27;&#x27; OR parent_id IS NULL)</span><br><span class="line">      AND org_type IS NULL</span><br><span class="line">      AND delete_flag = &#x27;0&#x27;</span><br><span class="line">      AND org_id NOT IN (&#x27;TSRY&#x27;, &#x27;LSRY&#x27;)</span><br><span class="line">    ORDER BY sort</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p><code>parent_id = &#39;&#39;</code> 是平台约定，<code>parent_id IS NULL</code> 是<strong>数据兜底</strong>——实测历史数据里有 1 条 NULL。</p>
<h4 id="2-互斥校验：service-层-2-次批量查"><a href="#2-互斥校验：service-层-2-次批量查" class="headerlink" title="2. 互斥校验：service 层 2 次批量查"></a>2. 互斥校验：service 层 2 次批量查</h4><p><strong>最大忌讳是 for 循环里逐个查 park 名称</strong>——请求 10 个 orgId 就 10 次 SQL。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 第 1 次：批量查这批 orgId 的现有绑定</span></span><br><span class="line">List&lt;ParkOrganizationDTO&gt; existBinds = baseMapper.selectParkOrganizationList(orgIds);</span><br><span class="line"></span><br><span class="line"><span class="comment">// in-memory 收集冲突（O(n) 遍历，不查 DB）</span></span><br><span class="line">Map&lt;String, String&gt; conflictOrgToPark = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;();</span><br><span class="line">Set&lt;String&gt; conflictParkIds = <span class="keyword">new</span> <span class="title class_">HashSet</span>&lt;&gt;();</span><br><span class="line"><span class="keyword">for</span> (ParkOrganizationDTO bind : existBinds) &#123;</span><br><span class="line">    <span class="keyword">if</span> (!bind.getParkId().equals(req.getParkId())) &#123;  <span class="comment">// 不是当前 park = 真冲突</span></span><br><span class="line">        conflictOrgToPark.put(bind.getOrgId(), bind.getParkId());</span><br><span class="line">        conflictParkIds.add(bind.getParkId());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (!conflictOrgToPark.isEmpty()) &#123;</span><br><span class="line">    <span class="comment">// 第 2 次：批量查冲突 park 的名称（1 次 SQL，不是 N 次）</span></span><br><span class="line">    List&lt;ParkDTO&gt; parks = parkMapper.getParkList(<span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;(conflictParkIds));</span><br><span class="line">    <span class="comment">// 第 3 次：批量查冲突 org 的名称（1 次 SQL）</span></span><br><span class="line">    List&lt;OrganizationDTO&gt; orgs = organizationMapper.getOrgList(<span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;(conflictOrgToPark.keySet()));</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BizException</span>(<span class="string">&quot;公司 X 已被园区 park_A 绑定...&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>无论前端传 1 个还是 100 个 orgId，DB 查询次数<strong>稳定在 2-3 次</strong>。</p>
<h3 id="集成测试：MyBatis-OGNL-Date-坑"><a href="#集成测试：MyBatis-OGNL-Date-坑" class="headerlink" title="集成测试：MyBatis OGNL Date 坑"></a>集成测试：MyBatis OGNL Date 坑</h3><blockquote>
<p>[!bug] 经典踩坑<br>集成测试调用 <code>POST /parkOrganization/saveCompanyBind</code>，<strong>直接 500</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">java.lang.IllegalArgumentException: invalid comparison: java.lang.String and java.util.Date</span><br><span class="line">    at org.apache.ibatis.ognl.OgnlOps.compareWithConversion(OgnlOps.java:97)</span><br><span class="line">    at org.apache.ibatis.ognl.ASTNotEq.getValueBody(ASTNotEq.java:53)</span><br><span class="line">    ...</span><br><span class="line">    at ParkOrganizationServiceImpl.updateParkOrganization:62</span><br><span class="line">    at ParkOrganizationServiceImpl.saveCompanyBind:239</span><br></pre></td></tr></table></figure>
</blockquote>
<h3 id="根因"><a href="#根因" class="headerlink" title="根因"></a>根因</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// saveCompanyBind（错误版本）</span></span><br><span class="line">WebExtUtils.iniCreate(model);  <span class="comment">// ← 这行把 model.createTime 设成 Date 类型</span></span><br><span class="line"><span class="built_in">this</span>.updateParkOrganization(model);</span><br></pre></td></tr></table></figure>

<p><code>updateParkOrganization</code> 内部会调一个<strong>通用 mapper 方法</strong> <code>baseMapper.listByCondition(model)</code>，XML 是<strong>generic</strong>的——对 DTO 的<strong>所有字段</strong>都做 <code>&lt;if test=&quot;xxx != null and xxx != &#39;&#39;&quot;&gt;</code> 动态拼接：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">&quot;createTime != null and createTime != &#x27;&#x27;&quot;</span>&gt;</span>  <span class="comment">&lt;!-- Date 跟 &#x27;&#x27; 比 --&gt;</span></span><br><span class="line">    AND create_time = #&#123;createTime&#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">if</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>OGNL 在评估 <code>createTime != &#39;&#39;</code> 时，<strong>Date 和 String 类型不一致抛错</strong>。MyBatis-Plus 这套 generic mapper 写法在 DTO 字段都是 String&#x2F;Integer 时没问题，<strong>只要混入 Date 字段就翻车</strong>。</p>
<h3 id="修法"><a href="#修法" class="headerlink" title="修法"></a>修法</h3><p>**删掉那行 <code>WebExtUtils.iniCreate(model);</code>**。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// saveCompanyBind（修复后）</span></span><br><span class="line"><span class="built_in">this</span>.updateParkOrganization(model);</span><br></pre></td></tr></table></figure>

<p><code>updateParkOrganization</code> 内部对自己构造的 inner DTO <strong>自己会调 iniCreate</strong>，外层传进来的 model 根本不需要再初始化一次。这是**典型的”老代码 copy-paste 没清干净”**。</p>
<h3 id="教训"><a href="#教训" class="headerlink" title="教训"></a>教训</h3><blockquote>
<p>[!warning] 三个复盘要点</p>
<ol>
<li><p><strong>Plan 阶段直接抄了 N+1 时代用过的 <code>iniCreate(model)</code><strong>，没意识到重构后 <code>updateParkOrganization</code> 已经自己处理。外层 <code>iniCreate</code> 既是</strong>多余</strong>也是<strong>有害</strong>（设了 Date 字段触发 OGNL 报错）。</p>
</li>
<li><p><strong>Plan review 时应当模拟调用链跑一遍</strong>，而不只是光看代码对不对。我在 review 阶段自己提了”<code>@NotEmpty</code> 注解可能 dead code”等小问题，<strong>但漏看了这一行</strong>——因为我没有顺着 <code>saveCompanyBind → updateParkOrganization → listByCondition → XML</code> 这条链跑一遍调用流。</p>
</li>
<li><p><strong>generic listByCondition 接收任意 model 时，</strong>不要<strong>设置 Date 字段</strong>。这是平台层 generic mapper 写法的固有脆弱性——所有字段都用 <code>!= &#39;&#39;</code> 校验，Date 类型天然不兼容。</p>
</li>
</ol>
</blockquote>
<p>补救 commit <code>f14cd9be4 fix: remove redundant iniCreate call causing Date OGNL comparison error</code> 是个 <strong>1 行 fix</strong>——这种 1 行 fix 越短，说明根因越简单，<strong>也越说明 plan review 阶段本应该抓到</strong>。</p>
<h3 id="4-个集成测试场景全过"><a href="#4-个集成测试场景全过" class="headerlink" title="4 个集成测试场景全过"></a>4 个集成测试场景全过</h3><table>
<thead>
<tr>
<th>场景</th>
<th>输入</th>
<th>预期</th>
<th>实际</th>
</tr>
</thead>
<tbody><tr>
<td>1. 首次绑定</td>
<td>park&#x3D;番禺基地, orgIds&#x3D;[110007195]</td>
<td>200 OK，DB 写入</td>
<td>✅ 通过</td>
</tr>
<tr>
<td>2. 互斥拒绝</td>
<td>park_A 已绑 110007195，park_B 再绑</td>
<td>200 OK + 错误信息</td>
<td>✅ 通过</td>
</tr>
<tr>
<td>3. 空 orgIds</td>
<td>park&#x3D;X, orgIds&#x3D;[]</td>
<td>拒绝 ARGS_ILLEGAL</td>
<td>✅ 通过</td>
</tr>
<tr>
<td>4. 替换绑定</td>
<td>park_A 已绑 110007195，换绑 136602494</td>
<td>upsert 成功</td>
<td>✅ 通过</td>
</tr>
</tbody></table>
<p><img data-src="/images/posts/2026-06-11-ding-park-bind/fig3_001.jpg" alt="MyBatis OGNL Date 报错堆栈"><br><em>图3：saveCompanyBind 集成测试 500</em></p>
<h2 id="构建命令：JDK-8-Maven-am"><a href="#构建命令：JDK-8-Maven-am" class="headerlink" title="构建命令：JDK 8 + Maven -am"></a>构建命令：JDK 8 + Maven -am</h2><h3 id="最终命令（可直接复制）"><a href="#最终命令（可直接复制）" class="headerlink" title="最终命令（可直接复制）"></a>最终命令（可直接复制）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> <span class="string">&quot;/e/nanwang/smart-park-cloud - no jar&quot;</span></span><br><span class="line"><span class="built_in">export</span> JAVA_HOME=/e/jdk</span><br><span class="line"><span class="built_in">export</span> PATH=<span class="variable">$JAVA_HOME</span>/bin:<span class="variable">$PATH</span></span><br><span class="line">mvn -pl service-provider/admin-service -am clean package -DskipTests</span><br></pre></td></tr></table></figure>

<h3 id="为什么必须用-JDK-8"><a href="#为什么必须用-JDK-8" class="headerlink" title="为什么必须用 JDK 8"></a>为什么必须用 JDK 8</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[ERROR] /E:/nanwang/smart-park-cloud - no jar/service-util/src/main/java/cn/csg/building/admin/utils/QrCodeByIdTypeUtils.java:[12,16] 找不到符号</span><br><span class="line">[ERROR]   符号:   类 BASE64Encoder</span><br><span class="line">[ERROR]   位置: 程序包 sun.misc</span><br></pre></td></tr></table></figure>

<p><code>sun.misc.BASE64Encoder</code> 是 Sun 内部类，<strong>JDK 9+ 模块化后被移除</strong>。环境 PATH 默认指向 JDK 17，但项目源码按 JDK 8 写的。</p>
<h3 id="为什么必须用-am"><a href="#为什么必须用-am" class="headerlink" title="为什么必须用 -am"></a>为什么必须用 <code>-am</code></h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[ERROR] Could not find artifact cn.csg.building:service-util:jar:1.0.0-SNAPSHOT</span><br></pre></td></tr></table></figure>

<p><code>service-model</code> 依赖 <code>service-util</code>，但 <code>service-util</code> 是项目内部模块（不会发布到公网）。单跑时 Maven 不知道要从源码构建。**<code>-am</code>（also-make）**：自动把 admin-service 依赖链上的所有模块按顺序构建。</p>
<h3 id="产物位置"><a href="#产物位置" class="headerlink" title="产物位置"></a>产物位置</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">E:\nanwang\smart-park-cloud - no jar\service-provider\admin-service\target\admin-service-1.0.0-SNAPSHOT.jar</span><br></pre></td></tr></table></figure>

<p>约 <strong>104 MB</strong>（Spring Boot fat jar），10 个模块全过，耗时 ~1.5 分钟。</p>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><h3 id="Spec-first-subagent-driven-跑通"><a href="#Spec-first-subagent-driven-跑通" class="headerlink" title="Spec-first + subagent-driven 跑通"></a>Spec-first + subagent-driven 跑通</h3><p>3 轮 spec review + 3 轮 code review，每轮都有实质调整：</p>
<ul>
<li><strong>Spec review 第 1 轮</strong>：明确”顶级公司 &#x3D; <code>parent_id=&#39;&#39; AND org_type IS NULL</code>“的判定</li>
<li><strong>Spec review 第 2 轮</strong>：决定<strong>不删历史子级绑定</strong>——业务侧没拍板前不动脏数据</li>
<li><strong>Code review 第 1 轮</strong>：subagent 自己抓到 <code>selectParkOrganizationList</code> 是不是真有 <code>List&lt;String&gt;</code> 入参</li>
<li><strong>Code review 第 2 轮</strong>：抓住”互斥校验用 2 次批量查而不是 N+1”</li>
</ul>
<p>每轮 review 成本 ~15 分钟，但抓到的问题如果流入集成测试阶段，<strong>单次 debug 至少 30 分钟</strong>。</p>
<h3 id="Plan-review-缺一次”调用链跑读”"><a href="#Plan-review-缺一次”调用链跑读”" class="headerlink" title="Plan review 缺一次”调用链跑读”"></a>Plan review 缺一次”调用链跑读”</h3><p>OGNL 那个坑暴露的最深层问题是：plan review 阶段我<strong>只看代码片段</strong>，没有顺着调用链把 <code>saveCompanyBind → updateParkOrganization → listByCondition → generic XML</code> 跑一遍。</p>
<blockquote>
<p>[!danger] 教训登记</p>
<ul>
<li>[2026-06-11] Rule correction: Plan review 阶段必须 trace 一次 controller → service → mapper 调用链（预防 generic mapper 写法的 OGNL Date 坑）</li>
</ul>
</blockquote>
<h3 id="后续-TODO"><a href="#后续-TODO" class="headerlink" title="后续 TODO"></a>后续 TODO</h3><ul>
<li><input disabled="" type="checkbox"> 业务侧拍板<strong>历史子级绑定</strong>是清理还是迁移</li>
<li><input disabled="" type="checkbox"> 互斥并发量监控，达到阈值时加 <code>SELECT FOR UPDATE</code></li>
<li><input disabled="" type="checkbox"> 调研是否要复用 generic mapper 的 <code>listByCondition</code>—— 改成 <code>&lt;if test=&quot;createTime != null&quot;&gt;</code> 形式</li>
</ul>
<h2 id="关联"><a href="#关联" class="headerlink" title="关联"></a>关联</h2><ul>
<li>[[2026-06-11 工作记录]]</li>
<li>[[钉钉同步写sys_user_park&#x2F;功能设计与实现]]</li>
<li>[[园区绑定一级公司&#x2F;功能设计与实现]]</li>
<li>[[2026-06-11 admin-service构建命令记录]]</li>
</ul>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>南网</category>
      </categories>
      <tags>
        <tag>MyBatis</tag>
        <tag>Spring Cloud</tag>
        <tag>钉钉同步</tag>
        <tag>OGNL</tag>
        <tag>园区绑定</tag>
        <tag>互斥校验</tag>
      </tags>
  </entry>
  <entry>
    <title>pass_user 死亡覆盖链与 spec 错误判断：从集成测试挖出 421 条脏数据</title>
    <url>/2026/06/12/2026-06-12-pass-user-death-chain/</url>
    <content><![CDATA[<h2 id="导读"><a href="#导读" class="headerlink" title="导读"></a>导读</h2><blockquote>
<p>6&#x2F;11 实现的”钉钉同步触发 pass_user 同步”功能本身端到端跑通，但暴露出 6&#x2F;11 spec 文档第 46 行的一个<strong>事实错误判断</strong>：把 <code>PassUserServiceImpl.synSysUserToPassUser()</code> 里的 <code>setParkId(DEFAULT_PARK_ID)</code> 误判为”无效死代码”。<strong>实际是有效过滤</strong>。更糟的是下午挖出了真正的死亡覆盖链：admin 端 <code>UserMapperExt.xml</code> 的 <code>listByConditionExt</code> SQL <strong>没 select <code>urp.park_id</code> 列</strong>，导致 <code>user.getParkId()</code> 永远是 NULL，421 条 <code>pass_user.park_id</code> 全被污染成 NULL。</p>
</blockquote>
<span id="more"></span>

<div class="audio-player">
  <div class="audio-section">
    <h4>🎧 文章导读</h4>
    <audio controls preload="metadata">
      <source src="/audio/2026-06-12-pass-user-death-chain/narration.mp3" type="audio/mpeg">
    </audio>
  </div>
  <div class="audio-section">
    <h4>🎵 背景音乐</h4>
    <audio controls loop preload="metadata">
      <source src="/audio/2026-06-12-pass-user-death-chain/bgm.mp3" type="audio/mpeg">
    </audio>
  </div>
</div>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>集成测试是揭露设计缺陷的最佳工具。这次集成测试同时挖出了三个问题：</p>
<ol>
<li>spec 文档第 46 行的事实错误判断（<code>setParkId(DEFAULT_PARK_ID)</code> 是死代码 → 实际是有效过滤）</li>
<li>死亡覆盖链的真正根因（<code>UserMapperExt.xml</code> SELECT 漏列）</li>
<li>421 条历史脏数据（其中 322 条本应是番禺基地用户）</li>
</ol>
<p>整个过程是一个标准的”反向归因”案例：表象和根因完全相反。</p>
<p><img data-src="/images/posts/2026-06-12-pass-user-death-chain/fig1_001.jpg" alt="死亡覆盖链路流程图"><br><em>图1：pass_user.park_id 死亡覆盖链</em></p>
<h2 id="第一阶段：集成测试-spec-错误判断"><a href="#第一阶段：集成测试-spec-错误判断" class="headerlink" title="第一阶段：集成测试 + spec 错误判断"></a>第一阶段：集成测试 + spec 错误判断</h2><h3 id="测试场景-T1：mock-用户没进-pass-user"><a href="#测试场景-T1：mock-用户没进-pass-user" class="headerlink" title="测试场景 T1：mock 用户没进 pass_user"></a>测试场景 T1：mock 用户没进 pass_user</h3><p>我准备了一个 mock 用户 <code>MOCK_TEST_PASS_001</code>，<code>USERID</code>、<code>DEPARTMENT</code>、<code>PDEPARTS</code> 三个字段都填了，调用 <code>POST /sync/mockDingUserSync</code> 后去查 <code>building_through.pass_user</code> 表，发现<strong>没有这个用户</strong>。</p>
<p>第一反应：”Feign 调用失败了” 或 “through-service 那边有 bug”。</p>
<p>但先用 <code>OpenFeign</code> 的日志和 <code>triggerPassUserSync</code> 的返回值（返回的是 affected rows 数）确认了一个关键事实：返回值是 <code>335</code>，<strong>不是 0</strong>。这意味着 <code>synSysUserToPassUser</code> 确实跑了，而且<strong>写了 335 个用户</strong>——只是没写我的 mock 用户。</p>
<h3 id="反查源码"><a href="#反查源码" class="headerlink" title="反查源码"></a>反查源码</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// PassUserServiceImpl.java:442-466</span></span><br><span class="line"><span class="type">UserDTO</span> <span class="variable">userDTO</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">UserDTO</span>();</span><br><span class="line">userDTO.setParkId(DictConstants.DEFAULT_PARK_ID);  <span class="comment">// ← 这行是有效过滤</span></span><br><span class="line">userDTO.setDeleteFlag(DictConstants.DEL_FLAG_NO);</span><br><span class="line">pagingCondition.setEntity(userDTO);</span><br><span class="line"><span class="comment">// ...</span></span><br><span class="line">Page&lt;UserDTO&gt; userPage = userClient.paging(pagingCondition).getData();</span><br></pre></td></tr></table></figure>

<p><code>userClient.paging(queryCondition)</code> 走的是 building 内部统一的查询协议，<strong>会用 <code>userDTO</code> 的所有非空字段做 WHERE 过滤</strong>。所以这一行的含义是：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">WHERE park_id = &#x27;284cd7570daf5d1ca8e121bc025f7a02&#x27; AND delete_flag = &#x27;0&#x27;</span><br></pre></td></tr></table></figure>

<p>——只查”番禺基地”且未删除的用户。</p>
<p>反查 <code>DictConstants.DEFAULT_PARK_ID</code>：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// service-util/.../DictConstants.java:765</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">DEFAULT_PARK_ID</span> <span class="operator">=</span> <span class="string">&quot;284cd7570daf5d1ca8e121bc025f7a02&quot;</span>;</span><br></pre></td></tr></table></figure>

<p>这是硬编码的”番禺基地” park_id。也就是说，<strong>所有非番禺基地的钉钉用户，pass_user 同步后仍然不会出现</strong>。</p>
<h3 id="spec-错误判断的事实纠正"><a href="#spec-错误判断的事实纠正" class="headerlink" title="spec 错误判断的事实纠正"></a>spec 错误判断的事实纠正</h3><p>6&#x2F;11 写的 spec 文档第 46 行断言 <code>setParkId(DEFAULT_PARK_ID)</code> 是”无效死代码”，<strong>这个判断是错的</strong>。我反复看了 <code>PassUserServiceImpl.synSysUserToPassUser()</code> 的源码后确认：这一行<strong>是有效过滤</strong>，它把同步范围限定在”番禺基地”。</p>
<h3 id="业务正确性问题"><a href="#业务正确性问题" class="headerlink" title="业务正确性问题"></a>业务正确性问题</h3><p><code>DEFAULT_PARK_ID</code> 是番禺基地的硬编码。含义：<strong>所有非番禺基地的钉钉用户，pass_user 同步后不会出现</strong>。</p>
<blockquote>
<p>[!warning] 当前生产环境的潜在影响<br>现有 1161 个 sys_user 钉钉用户，假设大部分 park_id &#x3D; <code>284cd757...</code>（番禺基地），那 pass_user 同步功能基本正常。但如果：</p>
<ol>
<li>未来扩展到非番禺基地的新园区</li>
<li>钉钉侧新增了非番禺基地的部门&#x2F;人员</li>
<li>桥表里有非番禺基地的 org_id</li>
</ol>
<p>这些用户的 pass_user 记录<strong>永远不会出现</strong>，会直接影响 <code>/gateway/through/passUser/page</code> 接口的展示完整性。</p>
</blockquote>
<h3 id="测试方法学的反面案例"><a href="#测试方法学的反面案例" class="headerlink" title="测试方法学的反面案例"></a>测试方法学的反面案例</h3><p><strong>测试通过的 mock 数据</strong>：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;USERID&quot;</span><span class="punctuation">:</span> <span class="string">&quot;MOCK_TEST_PASS_003&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;DEPARTMENT&quot;</span><span class="punctuation">:</span> <span class="string">&quot;[136602494]&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;PDEPARTS&quot;</span><span class="punctuation">:</span> <span class="string">&quot;[[1, 136602494, 1]]&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p><code>136602494</code> 在桥表里映射到番禺基地 → <code>sys_user_park.park_id = 284cd757...</code> → 满足 <code>setParkId(DEFAULT_PARK_ID)</code> 过滤 → <strong>写入 pass_user</strong>。</p>
<p><strong>测试失败的 mock 数据</strong>：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;USERID&quot;</span><span class="punctuation">:</span> <span class="string">&quot;MOCK_TEST_PASS_001&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;DEPARTMENT&quot;</span><span class="punctuation">:</span> <span class="string">&quot;[59096189]&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;PDEPARTS&quot;</span><span class="punctuation">:</span> <span class="string">&quot;[[1, 59096189, 1]]&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p><code>59096189</code> 不在桥表（之前测试硬删了）→ <code>sys_user_park.park_id = NULL</code> → <strong>被过滤</strong>。</p>
<p><img data-src="/images/posts/2026-06-12-pass-user-death-chain/fig2_001.jpg" alt="421 条脏数据分布"><br><em>图2：421 条 NULL park_id 量化结果</em></p>
<h2 id="第二阶段：死亡覆盖链的真正根因"><a href="#第二阶段：死亡覆盖链的真正根因" class="headerlink" title="第二阶段：死亡覆盖链的真正根因"></a>第二阶段：死亡覆盖链的真正根因</h2><h3 id="现象升级"><a href="#现象升级" class="headerlink" title="现象升级"></a>现象升级</h3><p>上午只判断了”setParkId(DEFAULT_PARK_ID) 是有效过滤”（事实纠正），下午继续深挖发现真正致命的是 admin 端 mapper 漏 SELECT 列表。</p>
<p><code>pass_user.page</code> 接口（<code>POST /gateway/through/passUser/page</code>）按 parkId 过滤时，几乎所有数据都查不到（即使不是 NULL 的，parkId 也是错的）。</p>
<h3 id="死亡覆盖链"><a href="#死亡覆盖链" class="headerlink" title="死亡覆盖链"></a>死亡覆盖链</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sys_user 行 → UserMapperExt.xml listByConditionExt</span><br><span class="line">              → LEFT JOIN sys_user_park urp on u.user_id = urp.user_id</span><br><span class="line">              → SELECT u.user_id, u.user_name, ...  (漏了 urp.park_id!)</span><br><span class="line">              → UserDTO.parkId = null  (BaseResultMap 映射的字段没数据)</span><br><span class="line">              → line 478 passUserDTO.setParkId(user.getParkId())  →  null</span><br><span class="line">              → pass_user.park_id = NULL</span><br></pre></td></tr></table></figure>

<p><strong>3 行代码的死亡覆盖链</strong>：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// PassUserServiceImpl.java:445-478</span></span><br><span class="line"><span class="type">UserDTO</span> <span class="variable">userDTO</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">UserDTO</span>();</span><br><span class="line">userDTO.setParkId(DictConstants.DEFAULT_PARK_ID);  <span class="comment">// line 447: 设请求 parkId=番禺基地</span></span><br><span class="line">PagingCondition&lt;UserDTO&gt; pagingCondition = <span class="keyword">new</span> <span class="title class_">PagingCondition</span>&lt;&gt;();</span><br><span class="line">pagingCondition.setEntity(userDTO);</span><br><span class="line">Page&lt;UserDTO&gt; userPage = userClient.paging(pagingCondition).getData();</span><br><span class="line"><span class="comment">// 拿到 322 个番禺用户（userDTO.parkId 参与 WHERE 过滤）</span></span><br><span class="line"><span class="keyword">for</span> (UserDTO user : userPage.getRecords()) &#123;</span><br><span class="line">    <span class="type">PassUserDTO</span> <span class="variable">passUserDTO</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">PassUserDTO</span>();</span><br><span class="line">    passUserDTO.setParkId(user.getParkId());  <span class="comment">// line 478: user.parkId=NULL → 覆盖</span></span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>**为什么 line 447 看起来”起作用”但实际”无效”**：</p>
<ul>
<li>修复前 <code>setParkId(DEFAULT_PARK_ID)</code> 设的是 <code>userDTO.parkId = &quot;284cd757...&quot;</code> ✓</li>
<li>admin-service WHERE 过滤：<code>urp.park_id = &#39;284cd757...&#39;</code> → 只返 322 个番禺用户 ✓</li>
<li><strong>但 SELECT 没返回 park_id</strong> → userDTO.parkId 永远是 NULL ✗</li>
<li>322 个番禺用户被 sync 进了 pass_user，<strong>但 park_id 全是 NULL</strong> ✗</li>
</ul>
<h3 id="量化脏数据"><a href="#量化脏数据" class="headerlink" title="量化脏数据"></a>量化脏数据</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> pu.real_name, pu.park_id, urp.park_id <span class="keyword">as</span> expected</span><br><span class="line"><span class="keyword">FROM</span> building_through.pass_user pu</span><br><span class="line"><span class="keyword">JOIN</span> building_admin.sys_user u <span class="keyword">ON</span> pu.sys_user_id <span class="operator">=</span> u.user_id</span><br><span class="line"><span class="keyword">JOIN</span> building_admin.sys_user_park urp <span class="keyword">ON</span> u.user_id <span class="operator">=</span> urp.user_id <span class="keyword">AND</span> urp.delete_flag <span class="operator">=</span> <span class="number">0</span></span><br><span class="line"><span class="keyword">WHERE</span> pu.park_id <span class="keyword">IS</span> <span class="keyword">NULL</span> <span class="keyword">OR</span> pu.park_id <span class="operator">!=</span> urp.park_id;</span><br><span class="line"><span class="comment">-- 命中 421 条，全是 pu.park_id = NULL</span></span><br></pre></td></tr></table></figure>

<p>**421 条 <code>pass_user.park_id IS NULL</code>**，其中 <strong>322 条本应是番禺基地用户</strong>（park_id &#x3D; 284cd757…）。</p>
<p><img data-src="/images/posts/2026-06-12-pass-user-death-chain/fig3_001.jpg" alt="UserMapperExt SQL 修复对比"><br><em>图3：UserMapperExt 漏 SELECT 列修复</em></p>
<h2 id="修复方案"><a href="#修复方案" class="headerlink" title="修复方案"></a>修复方案</h2><h3 id="修复-1：让新-sync-的-pass-user-写入正确的-park-id"><a href="#修复-1：让新-sync-的-pass-user-写入正确的-park-id" class="headerlink" title="修复 1：让新 sync 的 pass_user 写入正确的 park_id"></a>修复 1：让新 sync 的 pass_user 写入正确的 park_id</h3><p><strong>改动文件 1</strong>：<code>UserMapperExt.xml</code></p>
<p>SELECT 加 <code>,urp.park_id</code>，JOIN 改为子查询解决多园区用户重复问题：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 修改前 --&gt;</span></span><br><span class="line">SELECT DISTINCT u.user_id, u.user_name, ...</span><br><span class="line">FROM sys_user u</span><br><span class="line">LEFT JOIN sys_user_park urp on u.user_id = urp.user_id</span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- 修改后 --&gt;</span></span><br><span class="line">SELECT DISTINCT u.user_id, u.user_name, ..., urp.park_id</span><br><span class="line">FROM sys_user u</span><br><span class="line">LEFT JOIN (</span><br><span class="line">    SELECT user_id, MIN(park_id) as park_id</span><br><span class="line">    FROM sys_user_park</span><br><span class="line">    GROUP BY user_id</span><br><span class="line">) urp on u.user_id = urp.user_id</span><br></pre></td></tr></table></figure>

<blockquote>
<p>[!note] 为什么用子查询而不是直接 LEFT JOIN<br>同一个 user 可能在多个园区（<code>sys_user_park</code> 一对多），直接 LEFT JOIN 会让用户被 DISTINCT 出去后<strong>任一园区的 park_id 都可能漏选</strong>。子查询 <code>MIN(park_id) GROUP BY user_id</code> 保证每个 user 拿到一个<strong>确定的</strong> park_id 值。</p>
</blockquote>
<p>**故意不加 <code>WHERE delete_flag=&#39;0&#39;</code>**，与原 LEFT JOIN 行为一致（保持不过滤是原行为，加了反而引入行为变更）。</p>
<p><strong>改动文件 2</strong>：<code>PassUserServiceImpl.java</code></p>
<p>注释掉 line 447 <code>setParkId(DEFAULT_PARK_ID)</code>，让扫描覆盖所有园区：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// line 445-448</span></span><br><span class="line"><span class="comment">// 临时注释掉 setParkId：让扫描覆盖所有园区，配合 admin 端 mapper 改动让 pass_user 拿真实 parkId</span></span><br><span class="line"><span class="comment">// userDTO.setParkId(DictConstants.DEFAULT_PARK_ID);   &lt;!-- 注释 --&gt;</span></span><br><span class="line">PagingCondition&lt;UserDTO&gt; pagingCondition = <span class="keyword">new</span> <span class="title class_">PagingCondition</span>&lt;&gt;();</span><br><span class="line"><span class="type">UserDTO</span> <span class="variable">userDTO</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">UserDTO</span>();</span><br><span class="line">userDTO.setDeleteFlag(DictConstants.DEL_FLAG_NO);</span><br></pre></td></tr></table></figure>

<h3 id="修复-2：清理-321-条历史-NULL-park-id-数据"><a href="#修复-2：清理-321-条历史-NULL-park-id-数据" class="headerlink" title="修复 2：清理 321 条历史 NULL park_id 数据"></a>修复 2：清理 321 条历史 NULL park_id 数据</h3><blockquote>
<p>[!warning] 增量同步对历史脏数据无效<br>修复后 <code>synSysUserToPassUser</code> 走增量逻辑：<code>if (!userIds.contains(user.getUserId()))</code> 跳过已有。后果：<strong>321 个老 NULL park_id 的番禺用户永远不会被更新</strong>。必须单独 UPDATE。</p>
</blockquote>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">UPDATE</span> pass_user pu</span><br><span class="line"><span class="keyword">JOIN</span> building_admin.sys_user u <span class="keyword">ON</span> pu.sys_user_id <span class="operator">=</span> u.user_id</span><br><span class="line"><span class="keyword">JOIN</span> building_admin.sys_user_park urp <span class="keyword">ON</span> u.user_id <span class="operator">=</span> urp.user_id <span class="keyword">AND</span> urp.delete_flag <span class="operator">=</span> <span class="number">0</span></span><br><span class="line"><span class="keyword">SET</span> pu.park_id <span class="operator">=</span> urp.park_id</span><br><span class="line"><span class="keyword">WHERE</span> pu.park_id <span class="keyword">IS</span> <span class="keyword">NULL</span>;</span><br><span class="line"><span class="comment">-- 预期: 更新 321 条（322 个番禺 - 1 个 MOCK_MIN_001 = 321）</span></span><br></pre></td></tr></table></figure>

<h3 id="验证证据"><a href="#验证证据" class="headerlink" title="验证证据"></a>验证证据</h3><p><strong>修复后跑一次 <code>synSysUserToPassUser</code>，268 条新 pass_user 完美匹配 sys_user_park.park_id</strong>：</p>
<table>
<thead>
<tr>
<th>pass_user.real_name</th>
<th>pass_user.park_id</th>
<th>sys_user_park.park_id</th>
<th>匹配</th>
</tr>
</thead>
<tbody><tr>
<td>李泓希</td>
<td>59258e4f…</td>
<td>59258e4f…</td>
<td>✅</td>
</tr>
<tr>
<td>陈启操</td>
<td>a69dd3d6…</td>
<td>a69dd3d6…</td>
<td>✅</td>
</tr>
<tr>
<td>消防巡检卡</td>
<td>a69dd3d6…</td>
<td>a69dd3d6…</td>
<td>✅</td>
</tr>
</tbody></table>
<p><strong>Page 接口验证</strong>（parkId&#x3D;番禺基地 过滤）：</p>
<ul>
<li>修复前：<code>totalCount=0</code>（pass_user.park_id 全是 NULL，过滤后空集）</li>
<li>修复后：<code>totalCount=2</code> (MOCK_MIN_001 + MOCK_PANYU_001)</li>
</ul>
<h2 id="4-个踩坑总结"><a href="#4-个踩坑总结" class="headerlink" title="4 个踩坑总结"></a>4 个踩坑总结</h2><h3 id="坑-1：line-478-setParkId-user-getParkId-永远会覆盖-line-447"><a href="#坑-1：line-478-setParkId-user-getParkId-永远会覆盖-line-447" class="headerlink" title="坑 1：line 478 setParkId(user.getParkId()) 永远会覆盖 line 447"></a>坑 1：line 478 setParkId(user.getParkId()) 永远会覆盖 line 447</h3><p>表面上 <code>setParkId(DEFAULT_PARK_ID)</code> 像是”死代码”——其实<strong>判断方向反了</strong>：</p>
<ul>
<li>错判断：line 447 是死代码，没生效</li>
<li>真实：line 447 有效（WHERE 过滤返 322 个番禺用户），但 line 478 用 NULL 覆盖了 park_id 字段</li>
<li>后果：322 个番禺 user 进了 pass_user，但 park_id 是 NULL</li>
</ul>
<p><strong>教训</strong>：判断”代码无效”前必须<strong>先 trace 完整的 setter → 调用 → mapper → 落库链路</strong>。</p>
<h3 id="坑-2：spec-plan-阶段误判-BaseResultMap-缺-park-id-映射"><a href="#坑-2：spec-plan-阶段误判-BaseResultMap-缺-park-id-映射" class="headerlink" title="坑 2：spec&#x2F;plan 阶段误判 BaseResultMap 缺 park_id 映射"></a>坑 2：spec&#x2F;plan 阶段误判 BaseResultMap 缺 park_id 映射</h3><ul>
<li>spec 写”改 2 个文件 4 处”，但 plan 写了”改 3 个文件”（多了 UserMapper.xml）</li>
<li>实际查证 BaseResultMap line 30-31 已经有 <code>&lt;result column=&quot;park_id&quot; property=&quot;parkId&quot; jdbcType=&quot;VARCHAR&quot;/&gt;</code></li>
<li>教训：<strong>写 plan 前先 <code>grep</code> 实际文件，不要凭印象判断</strong></li>
</ul>
<h3 id="坑-3：增量同步对历史脏数据无效"><a href="#坑-3：增量同步对历史脏数据无效" class="headerlink" title="坑 3：增量同步对历史脏数据无效"></a>坑 3：增量同步对历史脏数据无效</h3><ul>
<li>修复前 sync 写了 421 条 NULL park_id 的 pass_user</li>
<li>修复后 <code>synSysUserToPassUser</code> 走增量逻辑，跳过已有</li>
<li>后果：321 个老 NULL park_id 的番禺 user 永远不会被更新</li>
<li>教训：<strong>所有”增量同步”逻辑对老数据无效</strong>，必须单独 UPDATE 或 DELETE 重建</li>
</ul>
<h3 id="坑-4：MyBatis-子查询行为一致性"><a href="#坑-4：MyBatis-子查询行为一致性" class="headerlink" title="坑 4：MyBatis 子查询行为一致性"></a>坑 4：MyBatis 子查询行为一致性</h3><ul>
<li>新子查询 <code>LEFT JOIN (SELECT user_id, MIN(park_id) FROM sys_user_park GROUP BY user_id) urp</code></li>
<li>故意不加 <code>WHERE delete_flag=&#39;0&#39;</code>，与原 <code>LEFT JOIN sys_user_park urp</code>（不过滤）行为一致</li>
<li>原则：<strong>修复不要引入额外行为变更</strong></li>
</ul>
<h2 id="排查方法论"><a href="#排查方法论" class="headerlink" title="排查方法论"></a>排查方法论</h2><ul>
<li><strong>返回值优先</strong>：<code>synSysUserToPassUser</code> 返回 335 直接告诉我是过滤问题（写入了 335 个番禺用户，只是 mock 用户没在里面）。<strong>永远先看返回值再下结论</strong></li>
<li><strong>完整 trace</strong>：从 controller → service → mapper → generic XML，每一层都要追到。这次 line 478 的 NULL 来自 6&#x2F;10 的 mapper 改动，跨 3 天的代码层才暴露</li>
<li><strong>脏数据量化</strong>：发现 bug 后<strong>第一件事</strong>是量化脏数据量（”421 条 NULL，其中 322 条是番禺”）。这决定了修复优先级和清理工作量</li>
</ul>
<h2 id="代码规范"><a href="#代码规范" class="headerlink" title="代码规范"></a>代码规范</h2><ul>
<li>“看起来无效”和”无效”是两回事。遇到可疑代码，必须<strong>追到底</strong>——读 caller 的实现才能判断”它真的没用”还是”它就是过滤条件”</li>
<li>spec 写下”无效死代码”前应该能引用具体证据（哪行覆盖、哪行忽略）。6&#x2F;11 spec 没给证据就下了结论，6&#x2F;12 集成测试推翻</li>
<li>写代码、读代码、写 spec、读 spec，<strong>每一个环节都可能引入错误</strong>。6&#x2F;11 我同时是实现者和 spec 写作者，<strong>缺乏第二视角</strong>。建议以后 spec 写完、实现开始前，让另一个人过一遍 spec 的事实性判断</li>
</ul>
<h2 id="测试覆盖"><a href="#测试覆盖" class="headerlink" title="测试覆盖"></a>测试覆盖</h2><ul>
<li>集成测试的”端到端”必须真的”端到端”——从 mock 入口到最终落库表，<strong>每一张表都查一遍</strong>。这次只查了 <code>pass_user</code> 没查 <code>sys_user_park</code>，导致没发现 mapper 的 SELECT 漏列</li>
<li>测试场景要包含”数据正确性”：除了”有没有写”，还要查”写得对不对”（pass_user.park_id 是不是等于 sys_user_park.park_id）</li>
</ul>
<h2 id="业务影响"><a href="#业务影响" class="headerlink" title="业务影响"></a>业务影响</h2><blockquote>
<p>[!note] 影响范围<br>修复后所有<strong>新写入</strong>的 pass_user.park_id 都是正确的。<strong>但历史 321 条 NULL 数据</strong>需要单独清理（在运维窗口统一处理）。在清理完成前，<code>/gateway/through/passUser/page</code> 按 parkId 过滤会少返回 321 个番禺用户。</p>
</blockquote>
<h2 id="待办事项"><a href="#待办事项" class="headerlink" title="待办事项"></a>待办事项</h2><ul>
<li><input disabled="" type="checkbox"> <strong>清理 321 条历史 NULL park_id 数据</strong>（运维窗口统一处理）</li>
<li><input disabled="" type="checkbox"> <strong>验证 Page 接口的完整性</strong>：在历史数据清理后，重新跑一次 <code>parkId=番禺基地</code> 过滤，确认 totalCount 包含 322 条</li>
<li><input disabled="" type="checkbox"> <strong>修改 spec 文档第 46 行</strong>（必改）：把”无效死代码”改成”有效过滤 + 行为约束”</li>
<li><input disabled="" type="checkbox"> <strong>新提 issue&#x2F;spec</strong>：”pass_user 同步支持多园区”——调研其他园区 pass_user 是怎么同步的，再决定是否去掉 <code>setParkId(DEFAULT_PARK_ID)</code> 这一行</li>
</ul>
<h2 id="关键代码位置"><a href="#关键代码位置" class="headerlink" title="关键代码位置"></a>关键代码位置</h2><ul>
<li><code>service-provider/admin-service/src/main/resources/mapper/UserMapperExt.xml:7-37</code> — SELECT + JOIN 改子查询</li>
<li><code>service-provider/through-service/src/main/java/cn/csg/building/through/service/impl/PassUserServiceImpl.java:447</code> — 注释掉 setParkId</li>
</ul>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>南网</category>
      </categories>
      <tags>
        <tag>MyBatis</tag>
        <tag>集成测试</tag>
        <tag>多园区</tag>
        <tag>死亡覆盖链</tag>
        <tag>Bug修复</tag>
      </tags>
  </entry>
  <entry>
    <title>pass_user 字段同步 + 人员列表联动 + 代码审查：折中方案与前端 import 来源踩坑</title>
    <url>/2026/06/15/2026-06-15-pass-user-fields-and-org-tree/</url>
    <content><![CDATA[<h2 id="导读"><a href="#导读" class="headerlink" title="导读"></a>导读</h2><blockquote>
<p>今天三个主题：钉钉同步补 pass_user 的手机号和部门（折中方案）、人员列表页面部门下拉按园区联动（前端 import 来源同名坑）、代码审查记录（已合并改动）。其中<strong>最值得记录的是折中方案的取舍</strong>——为什么”换部门同步”主动放弃，以及<strong>前端 import 同名函数指向不同后端接口</strong>这种隐蔽问题。</p>
</blockquote>
<span id="more"></span>

<div class="audio-player">
  <div class="audio-section">
    <h4>🎧 文章导读</h4>
    <audio controls preload="metadata">
      <source src="/audio/2026-06-15-pass-user-fields-and-org-tree/narration.mp3" type="audio/mpeg">
    </audio>
  </div>
  <div class="audio-section">
    <h4>🎵 背景音乐</h4>
    <audio controls loop preload="metadata">
      <source src="/audio/2026-06-15-pass-user-fields-and-org-tree/bgm.mp3" type="audio/mpeg">
    </audio>
  </div>
</div>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>钉钉同步的核心链路跑通后，<code>pass_user</code> 还缺两个字段：手机号、部门。看似简单的同步需求，挖下去发现要做完”完美方案”得改 3 处，且 admin 重建组织关系是拦路虎。</p>
<p>同时前端人员列表部门下拉一直不能按所选园区联动——表面看是后端 bug，深入 trace 发现是<strong>前端 import 来源错了</strong>：<code>organizationTree</code> 同名函数在不同文件，调的是不同后端接口。</p>
<p><img data-src="/images/posts/2026-06-15-pass-user-fields-and-org-tree/fig1_001.jpg" alt="折中方案示意图"><br><em>图1：折中方案示意图</em></p>
<h2 id="Part-1：折中方案选型"><a href="#Part-1：折中方案选型" class="headerlink" title="Part 1：折中方案选型"></a>Part 1：折中方案选型</h2><h3 id="完整方案要改-3-处，其中-admin-重建最麻烦"><a href="#完整方案要改-3-处，其中-admin-重建最麻烦" class="headerlink" title="完整方案要改 3 处，其中 admin 重建最麻烦"></a>完整方案要改 3 处，其中 admin 重建最麻烦</h3><p>要让”已有钉钉人员换部门”也同步到 pass_user，需要打通三处：</p>
<ol>
<li><strong>admin 侧重建组织关系</strong> — <code>UserSyncServiceImpl</code> 第 225 行至今是个 <code>TODO</code>：钉钉用户换部门时，<code>sys_user_organization</code> 根本没被更新</li>
<li><strong>paging 查询把 org_id 带出来</strong> — pass_user 同步是重新分页查 sys_user，而 <code>listByConditionExt</code> SELECT 里没有 org_id</li>
<li><strong>pass_user 加 UPDATE 分支</strong> — 原方法只有 INSERT</li>
</ol>
<p>第 1 处是拦路虎：admin 的组织重建涉及软删旧关联、批量重建，还要顾及用户列表页等共用 <code>listByConditionExt</code> 的逻辑，改动面大、容易碰坏别处。</p>
<h3 id="为什么选择折中"><a href="#为什么选择折中" class="headerlink" title="为什么选择折中"></a>为什么选择折中</h3><p>评估下来：手机号更新只差第 3 处（1 处），部门更新要三处全做。考虑到<strong>换部门是低频事件</strong>，而 admin 重建的风险和工作量都不划算，最终选择：</p>
<ul>
<li>✅ 手机号更新（已有人员改号 → 同步）</li>
<li>✅ 新人部门首次写入（首次进 pass_user 就带上 org_id）</li>
<li>✅ 顺带补齐历史空 org_id</li>
<li>❌ <strong>已有人员换部门 → 不同步</strong>（接受这个误差，留到以后再做 admin 重建）</li>
</ul>
<h3 id="单位-部门的关系：前端搞定，后端只存一个-org-id"><a href="#单位-部门的关系：前端搞定，后端只存一个-org-id" class="headerlink" title="单位&#x2F;部门的关系：前端搞定，后端只存一个 org_id"></a>单位&#x2F;部门的关系：前端搞定，后端只存一个 org_id</h3><p>排查中发现页面上”所属部门”显示成两级（如 <code>西部检修试验分公司 &gt; 电气一次检修部</code>），一度以为要存两个字段。查清楚后发现：</p>
<ul>
<li><code>sys_organization</code> 是树结构（有 <code>parent_id</code>），部门是子级、公司是父级</li>
<li>这个两级显示是<strong>前端</strong>做的——后端只返回 <code>org_id</code>，前端 <code>getParentNames</code>（<code>staffList.vue:2630</code>）沿 <code>parent_id</code> 往上爬拼出来</li>
<li>所以 <code>pass_user</code> 只需存<strong>部门 org_id</strong>（子级），单位自动从树推出来，<strong>不用加列、不用单独同步单位</strong></li>
</ul>
<h2 id="Part-2：实现"><a href="#Part-2：实现" class="headerlink" title="Part 2：实现"></a>Part 2：实现</h2><h3 id="改动-1：admin-让-paging-带出部门-org-id"><a href="#改动-1：admin-让-paging-带出部门-org-id" class="headerlink" title="改动 1：admin 让 paging 带出部门 org_id"></a>改动 1：admin 让 paging 带出部门 org_id</h3><p><code>UserMapperExt.xml</code> 的 <code>listByConditionExt</code> 仿照已有的 <code>park_id</code> 子查询，加一个把用户<strong>部门 org_id</strong> 带出来的子查询。关键是”怎么取到部门那行”——钉钉用户的 org 是 <code>[部门, 公司]</code>，取 <code>parent_id</code> 非空的子级就是部门（公司是顶级、parent 为空）：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 部门 org_id：钉钉用户 org 为 [部门, 公司]，取 parent_id 非空的子级 */</span></span><br><span class="line"><span class="keyword">LEFT</span> <span class="keyword">JOIN</span> (</span><br><span class="line">    <span class="keyword">SELECT</span> uo2.user_id, <span class="built_in">MIN</span>(uo2.org_id) <span class="keyword">AS</span> org_id</span><br><span class="line">    <span class="keyword">FROM</span> sys_user_organization uo2</span><br><span class="line">    <span class="keyword">JOIN</span> sys_organization o2 <span class="keyword">ON</span> o2.org_id <span class="operator">=</span> uo2.org_id <span class="keyword">AND</span> o2.delete_flag <span class="operator">=</span> <span class="string">&#x27;0&#x27;</span></span><br><span class="line">    <span class="keyword">WHERE</span> uo2.delete_flag <span class="operator">=</span> <span class="string">&#x27;0&#x27;</span> <span class="keyword">AND</span> o2.parent_id <span class="keyword">IS</span> <span class="keyword">NOT NULL</span> <span class="keyword">AND</span> o2.parent_id <span class="operator">&lt;&gt;</span> <span class="string">&#x27;&#x27;</span></span><br><span class="line">    <span class="keyword">GROUP</span> <span class="keyword">BY</span> uo2.user_id</span><br><span class="line">) uorg <span class="keyword">ON</span> u.user_id <span class="operator">=</span> uorg.user_id</span><br></pre></td></tr></table></figure>

<p>这里用聚合子查询（每用户聚出一行）而不是直接 JOIN，是为了避免一个用户有多条 org 关联时把主查询的行数撑爆——这是借鉴旁边 <code>park_id</code> 子查询的成熟写法。</p>
<h3 id="改动-2：through-加-UPDATE-分支-INSERT-补-org-id"><a href="#改动-2：through-加-UPDATE-分支-INSERT-补-org-id" class="headerlink" title="改动 2：through 加 UPDATE 分支 + INSERT 补 org_id"></a>改动 2：through 加 UPDATE 分支 + INSERT 补 org_id</h3><p><code>PassUserServiceImpl.synSysUserToPassUser</code> 原来只有”新人 INSERT”。改成 INSERT + UPDATE 两条路，并且 UPDATE 加了<strong>脏检查</strong>——只有手机号变了、或 org_id 需要补，才进更新队列：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">String</span> <span class="variable">newPhone</span> <span class="operator">=</span> EncryptionUtils.phoneEncryptToHex(user.getPhone());</span><br><span class="line"><span class="type">boolean</span> <span class="variable">phoneChanged</span> <span class="operator">=</span> !Objects.equals(existing.getPhone(), newPhone);</span><br><span class="line"><span class="type">boolean</span> <span class="variable">orgNeedsFill</span> <span class="operator">=</span> StringUtils.isBlank(existing.getOrgId())</span><br><span class="line">        &amp;&amp; StringUtils.isNotBlank(user.getOrgId());</span><br><span class="line"><span class="keyword">if</span> (phoneChanged || orgNeedsFill) &#123;</span><br><span class="line">    <span class="comment">// ...构造 upd：setId/setPhone/setOrgId(若需)/标 distSyncStatus=PENDING/setUpdateTime...</span></span><br><span class="line">    toUpdate.add(upd);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>[!warning] 为什么必须做脏检查<br>如果无脑把所有关联用户都标 <code>distSyncStatus=PENDING</code>，每次钉钉同步都会触发<strong>全员分区&#x2F;设备重同步</strong>——哪怕没一个人变。脏检查确保只有真正变化的才重标 PENDING。</p>
</blockquote>
<h3 id="reviewer-建议加-Transactional-——-不能加"><a href="#reviewer-建议加-Transactional-——-不能加" class="headerlink" title="reviewer 建议加 @Transactional —— 不能加"></a>reviewer 建议加 @Transactional —— 不能加</h3><p>code review 时有人建议给 <code>synSysUserToPassUser</code> 加 <code>@Transactional</code> 保证原子性。<strong>这条不能采纳</strong>：该方法在 for 循环里调了 Feign <code>userClient.paging()</code>，整方法加事务等于<strong>在数据库事务里夹网络 IO</strong>——事务在等 HTTP 返回时一直占着连接和锁，是明确的反模式。真要原子性，得把两次 batch 写抽到独立的事务方法里，但不划算（这方法本身可重入、失败下次自愈）。</p>
<h3 id="测试与验证"><a href="#测试与验证" class="headerlink" title="测试与验证"></a>测试与验证</h3><p>由于 dev 库没有真实钉钉用户，用 <code>POST /sync/mockDingUserSync</code> 注入测试用户（部门用真实存在的 <code>136541718</code> 电气一次检修部）。三条路径实测全绿：</p>
<table>
<thead>
<tr>
<th>路径</th>
<th>验证</th>
</tr>
</thead>
<tbody><tr>
<td>INSERT 新人</td>
<td>pass_user 写入 phone + <strong>org_id&#x3D;136541718</strong>（以前会是空）</td>
</tr>
<tr>
<td>UPDATE 改手机号</td>
<td>phone 跟着变、<code>distSyncStatus=PENDING</code>、update_time 刷新</td>
</tr>
<tr>
<td>UPDATE 补空 org_id</td>
<td>真实用户”蓝文文”org_id 从 null → 61955385（外部厂家）</td>
</tr>
</tbody></table>
<h3 id="折中方案的代价"><a href="#折中方案的代价" class="headerlink" title="折中方案的代价"></a>折中方案的代价</h3><p>这次的核心决策是**主动放弃”换部门同步”**——不是做不到，是代价（admin 组织重建）不划算。换部门低频，而手机号变更、新人入库是高频场景，折中方案用最小改动覆盖了主要收益。</p>
<p>后续如果业务确实需要”换部门也同步”，回头补 admin 侧的 <code>UserSyncServiceImpl:225</code> 组织重建即可，pass_user 这边的 UPDATE 分支已经 ready。</p>
<h2 id="Part-3：人员列表部门下拉按园区联动"><a href="#Part-3：人员列表部门下拉按园区联动" class="headerlink" title="Part 3：人员列表部门下拉按园区联动"></a>Part 3：人员列表部门下拉按园区联动</h2><h3 id="需求"><a href="#需求" class="headerlink" title="需求"></a>需求</h3><p>便捷通行”人员列表”页面（<code>/through/staffManage/staffList/staffList</code>）搜索栏有”园区”+”部门”两个筛选条件。</p>
<ul>
<li><strong>原状</strong>：选了园区，部门下拉显示的是<strong>当前登录人园区</strong>绑定的公司（后端写死 <code>baseUser.getParkId()</code>），与所选园区无关</li>
<li><strong>需求</strong>：搜索栏选了园区 X，部门下拉只显示 X 绑定的公司</li>
</ul>
<h3 id="调用链（trace-到-mapper）"><a href="#调用链（trace-到-mapper）" class="headerlink" title="调用链（trace 到 mapper）"></a>调用链（trace 到 mapper）</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">前端 allocateOrg()</span><br><span class="line">  → organizationTree(params)        // @/api/common</span><br><span class="line">  → POST /admin/organization/selectCurParkOrgTree</span><br><span class="line">  → OrganizationController#getCurParkOrgTree</span><br><span class="line">  → OrganizationServiceImpl#getCurParkOrgTree</span><br><span class="line">  → loadCurParkTree(...)            // 递归构建树</span><br></pre></td></tr></table></figure>

<p><strong>关键写死点</strong>：<code>OrganizationServiceImpl#getCurParkOrgTree</code> 第 338 行</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">parkOrganizationDTO.setParkId(baseUser.getParkId());  <span class="comment">// 忽略前端传入</span></span><br></pre></td></tr></table></figure>

<h3 id="关键踩坑：前端同名-organizationTree-指向两个不同的后端接口"><a href="#关键踩坑：前端同名-organizationTree-指向两个不同的后端接口" class="headerlink" title="关键踩坑：前端同名 organizationTree 指向两个不同的后端接口"></a>关键踩坑：前端同名 <code>organizationTree</code> 指向两个不同的后端接口</h3><p><img data-src="/images/posts/2026-06-15-pass-user-fields-and-org-tree/fig3_001.jpg" alt="前端 import 来源"><br><em>图3：前端 import 来源</em></p>
<blockquote>
<p>[!warning] 同名 organizationTree，指向两个不同的后端接口<br>前端有两个 <code>organizationTree</code> 函数，<strong>名字相同但调不同后端接口</strong>：</p>
<table>
<thead>
<tr>
<th>import 来源</th>
<th>后端接口</th>
<th>方法</th>
<th>行为</th>
</tr>
</thead>
<tbody><tr>
<td><code>@/api/common</code></td>
<td><code>/organization/selectCurParkOrgTree</code></td>
<td><code>getCurParkOrgTree</code></td>
<td>按园区绑定过滤</td>
</tr>
<tr>
<td><code>@/api/system/user/user</code></td>
<td><code>/organization/organizationTree</code></td>
<td><code>selectOrganizationTree</code></td>
<td>按用户权限返回全量，<strong>不读 parkId</strong></td>
</tr>
</tbody></table>
<p><code>staffList.vue:1276</code> 原本 import 的是 <strong>user.js</strong> 那个，所以即便后端改好了 <code>getCurParkOrgTree</code>，传 parkId 也完全不生效——前端压根没调那个接口，照返回权限内的全量组织树，表现为”选了番禺基地还返回一大堆数据”。</p>
</blockquote>
<h3 id="最终改动（3-处）"><a href="#最终改动（3-处）" class="headerlink" title="最终改动（3 处）"></a>最终改动（3 处）</h3><h4 id="1-后端：parkId-兜底"><a href="#1-后端：parkId-兜底" class="headerlink" title="1. 后端：parkId 兜底"></a>1. 后端：parkId 兜底</h4><p><code>OrganizationServiceImpl.java#getCurParkOrgTree</code> 第 338 行：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 原</span></span><br><span class="line">parkOrganizationDTO.setParkId(baseUser.getParkId());</span><br><span class="line"><span class="comment">// 改为（前端传了就用，没传兜底当前登录人，向后兼容）</span></span><br><span class="line">parkOrganizationDTO.setParkId(</span><br><span class="line">        StringUtils.isNotEmpty(organizationDTO.getParkId())</span><br><span class="line">                ? organizationDTO.getParkId() : baseUser.getParkId());</span><br></pre></td></tr></table></figure>

<h4 id="2-前端：allocateOrg-参数化"><a href="#2-前端：allocateOrg-参数化" class="headerlink" title="2. 前端：allocateOrg 参数化"></a>2. 前端：allocateOrg 参数化</h4><p><code>staffList.vue</code>：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 原</span></span><br><span class="line"><span class="title function_">allocateOrg</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="title function_">organizationTree</span>(&#123;&#125;).<span class="title function_">then</span>(<span class="function"><span class="params">res1</span> =&gt;</span> &#123; <span class="variable language_">this</span>.<span class="property">orgTreeDataTwo</span> = res1 &#125;)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 改为</span></span><br><span class="line"><span class="title function_">allocateOrg</span>(<span class="params">parkId</span>) &#123;</span><br><span class="line">  <span class="title function_">organizationTree</span>(parkId ? &#123; parkId &#125; : &#123;&#125;).<span class="title function_">then</span>(<span class="function"><span class="params">res1</span> =&gt;</span> &#123; <span class="variable language_">this</span>.<span class="property">orgTreeDataTwo</span> = res1 &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>搜索栏的 watcher 早已 <code>this.allocateOrg(val)</code> 传参，这处一接上就联动。空值传 <code>&#123;&#125;</code> → 后端兜底当前登录人园区。</p>
<h4 id="3-前端：import-来源修正"><a href="#3-前端：import-来源修正" class="headerlink" title="3. 前端：import 来源修正"></a>3. 前端：import 来源修正</h4><p><code>staffList.vue:1276</code>：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 原</span></span><br><span class="line"><span class="keyword">import</span> &#123; organizationTree &#125; <span class="keyword">from</span> <span class="string">&#x27;@/api/system/user/user&#x27;</span></span><br><span class="line"><span class="comment">// 改为</span></span><br><span class="line"><span class="keyword">import</span> &#123; organizationTree &#125; <span class="keyword">from</span> <span class="string">&#x27;@/api/common&#x27;</span></span><br></pre></td></tr></table></figure>

<h3 id="验证"><a href="#验证" class="headerlink" title="验证"></a>验证</h3><p><code>sys_park_organization</code> 查询确认番禺基地（<code>parkId = 284cd7570daf5d1ca8e121bc025f7a02</code>）绑定 <strong>5 个公司</strong>：</p>
<table>
<thead>
<tr>
<th>org_id</th>
<th>名称</th>
</tr>
</thead>
<tbody><tr>
<td>136602494</td>
<td>西部检修试验分公司</td>
</tr>
<tr>
<td>c5e47144b0e514223df3947bfcd2f948</td>
<td>园区总部</td>
</tr>
<tr>
<td>59900345</td>
<td>公司本部</td>
</tr>
<tr>
<td>59969486</td>
<td>信息通信分公司</td>
</tr>
<tr>
<td>b9546c9600e99e789d7d86313c4941d6</td>
<td>园区二级部门</td>
</tr>
</tbody></table>
<p>验证步骤：重启 admin-service → 前端刷新 → 搜索栏选番禺基地 → 部门下拉应正好是这 5 个。</p>
<h3 id="已知限制"><a href="#已知限制" class="headerlink" title="已知限制"></a>已知限制</h3><ul>
<li><strong>新增弹窗切园区不联动</strong>：<code>changeParkId</code>（<code>staffList.vue:2005</code>）仍是空实现</li>
<li><strong>编辑回填</strong>：<code>updateButton</code> 不调 <code>allocateOrg(res.parkId)</code></li>
<li><strong><code>orgTreeDataThree</code> 共用</strong>：搜索栏、新增弹窗、钉钉用户弹窗共用同一个 <code>orgTreeDataTwo</code> 变量，互相影响</li>
</ul>
<p><img data-src="/images/posts/2026-06-15-pass-user-fields-and-org-tree/fig2_001.jpg" alt="代码审查表"><br><em>图2：代码审查表</em></p>
<h2 id="Part-4：代码审查记录"><a href="#Part-4：代码审查记录" class="headerlink" title="Part 4：代码审查记录"></a>Part 4：代码审查记录</h2><h3 id="1-UserMapperExt-xml-—-listByConditionExt-补部门-org-id"><a href="#1-UserMapperExt-xml-—-listByConditionExt-补部门-org-id" class="headerlink" title="1. UserMapperExt.xml — listByConditionExt 补部门 org_id"></a>1. <code>UserMapperExt.xml</code> — <code>listByConditionExt</code> 补部门 org_id</h3><p><strong>审查结论</strong>：✅ 正确</p>
<ul>
<li><code>parent_id IS NOT NULL AND parent_id &lt;&gt; &#39;&#39;</code> 正确区分了”部门”和”公司”</li>
<li><code>delete_flag = &#39;0&#39;</code> 在 WHERE 和 JOIN 两处都过滤</li>
<li><code>MIN(uo2.org_id)</code> 处理一人多部门，取一个，注释写了一人一部门，MIN 只是兜底</li>
<li>LEFT JOIN 意味着没有部门的用户 orgId 返回 null，不会丢失用户行</li>
</ul>
<h3 id="2-PassUserServiceImpl-java-—-synSysUserToPassUser-补-orgId-UPDATE-分支"><a href="#2-PassUserServiceImpl-java-—-synSysUserToPassUser-补-orgId-UPDATE-分支" class="headerlink" title="2. PassUserServiceImpl.java — synSysUserToPassUser 补 orgId + UPDATE 分支"></a>2. <code>PassUserServiceImpl.java</code> — <code>synSysUserToPassUser</code> 补 orgId + UPDATE 分支</h3><p><strong>审查结论</strong>：✅ 正确</p>
<ul>
<li><strong>Map 替换 List</strong>：查找从 O(n) 降到 O(1)</li>
<li><strong>filter sysUserId 非空</strong>：排除了手动创建和访客的 pass_user</li>
<li><strong>add 分支 setOrgId</strong>：依赖 admin 端 mapper 改动返回的 orgId 字段</li>
<li><strong>update 分支</strong>：<code>if (StringUtils.isBlank(existing.getOrgId()))</code> 只补空值，不会覆盖已有的 orgId，安全</li>
</ul>
<p><strong>注意点</strong>：</p>
<ul>
<li><code>listByCondition(queryDTO)</code> 加载全量 pass_user 到内存建 Map，数据量大时注意内存</li>
<li>update 分支没同步 real_name 和 face_pic，如果后续需要可以加，当前不改没问题</li>
</ul>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><h3 id="折中方案是工程实践的核心"><a href="#折中方案是工程实践的核心" class="headerlink" title="折中方案是工程实践的核心"></a>折中方案是工程实践的核心</h3><p>“换部门同步”看似只是”再加一行代码”，深入追下去要做 3 处改动，其中 admin 重建是拦路虎。<strong>主动放弃低频场景</strong>比”硬上完整方案”更专业——这要求对业务频率、改动风险有量化判断。</p>
<h3 id="前端-import-同名函数的隐蔽性"><a href="#前端-import-同名函数的隐蔽性" class="headerlink" title="前端 import 同名函数的隐蔽性"></a>前端 import 同名函数的隐蔽性</h3><p><code>organizationTree</code> 这个名字很常见，但<strong>不同文件里的同名函数调不同后端接口</strong>。这正是全局规则 <code>[2026-06-11] Plan review 必须 trace 完整调用链</code> 的延伸：<strong>不仅要 trace 到 mapper XML，前端 import 的真实指向也要落实</strong>。</p>
<h3 id="Transactional-不是万能药"><a href="#Transactional-不是万能药" class="headerlink" title="@Transactional 不是万能药"></a>@Transactional 不是万能药</h3><p><code>synSysUserToPassUser</code> 加 <code>@Transactional</code> 看似合理，但<strong>事务 + Feign 网络 IO &#x3D; 反模式</strong>。事务持有连接和锁，HTTP 调用阻塞 → 连接占用久 → 整个系统跟着慢。</p>
<h3 id="折中方案要有”回头补”的路径"><a href="#折中方案要有”回头补”的路径" class="headerlink" title="折中方案要有”回头补”的路径"></a>折中方案要有”回头补”的路径</h3><p>放弃”换部门同步”时，明确记录”回头补 admin 侧 UserSyncServiceImpl:225 组织重建即可”——这不是甩锅，是给未来的自己留路标。<strong>代码 + spec + obsidian 笔记三者对齐</strong>，未来才不会忘了为什么放弃。</p>
<h2 id="关联"><a href="#关联" class="headerlink" title="关联"></a>关联</h2><ul>
<li>[[2026-06-15 工作记录]]</li>
<li>[[代码审查记录]]</li>
<li>[[钉钉同步pass_user字段&#x2F;功能设计与实现]]</li>
<li>[[人员列表部门按园区联动]]</li>
</ul>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>南网</category>
      </categories>
      <tags>
        <tag>钉钉同步</tag>
        <tag>折中方案</tag>
        <tag>Vue</tag>
        <tag>代码审查</tag>
        <tag>部门联动</tag>
      </tags>
  </entry>
  <entry>
    <title>dist_sync 分支集成测试报告：9 项功能 12 个用例全过</title>
    <url>/2026/06/16/2026-06-16-dist-sync-integration-test/</url>
    <content><![CDATA[<h2 id="导读"><a href="#导读" class="headerlink" title="导读"></a>导读</h2><blockquote>
<p>dist_sync 分支自 6&#x2F;10 起的所有改动（19 个提交 + 6&#x2F;16 未提交的”部门+园区更新”），今天做端到端集成测试。<strong>9 项功能 12 个用例全过，未发现阻断性缺陷</strong>。1 处 <code>dist_sync_status</code> 表现差异已查明为下游消费者正常行为（非 bug）。这篇不是技术深挖，是<strong>测试方法 + DB 证据 + 关键发现的完整记录</strong>，适合需要做类似集成测试的同行参考。</p>
</blockquote>
<span id="more"></span>

<div class="audio-player">
  <div class="audio-section">
    <h4>🎧 文章导读</h4>
    <audio controls preload="metadata">
      <source src="/audio/2026-06-16-dist-sync-integration-test/narration.mp3" type="audio/mpeg">
    </audio>
  </div>
  <div class="audio-section">
    <h4>🎵 背景音乐</h4>
    <audio controls loop preload="metadata">
      <source src="/audio/2026-06-16-dist-sync-integration-test/bgm.mp3" type="audio/mpeg">
    </audio>
  </div>
</div>

<h2 id="测试概览"><a href="#测试概览" class="headerlink" title="测试概览"></a>测试概览</h2><p><strong>测试对象</strong>：<code>dist_sync</code> 分支自 2026-06-10 起 Panyb 的钉钉同步功能线全部改动（19 个提交 + 6&#x2F;16 未提交的”部门+园区更新”）。</p>
<p><strong>测试方式</strong>：通过 admin <code>/sync/mockDingUserSync</code> 端点造数据 → MCP 查 <code>building_admin</code>&#x2F;<code>building_through</code> 两库校验。</p>
<p><strong>测试时长</strong>：1.5 小时（含 DB 落库验证、跨服务 Feign 调用跟踪、脏数据恢复）。</p>
<p><strong>产出物</strong>：<code>E:\nanwang\smart-park-cloud - no jar\docs\integration-test-2026-06-16\</code>（测试清单、执行脚本、mock 数据、HTTP 响应）</p>
<p><img data-src="/images/posts/2026-06-16-dist-sync-integration-test/fig1_001.jpg" alt="测试矩阵表"><br><em>图1：测试矩阵表</em></p>
<h2 id="一、结论"><a href="#一、结论" class="headerlink" title="一、结论"></a>一、结论</h2><p><strong>9 项功能全部通过。</strong> 未发现阻断性缺陷。1 处 <code>dist_sync_status</code> 表现差异已查明为下游消费者正常行为（非 bug）。</p>
<table>
<thead>
<tr>
<th>#</th>
<th>功能</th>
<th>提交</th>
<th>结果</th>
</tr>
</thead>
<tbody><tr>
<td>A</td>
<td>钉钉新建用户写 sys_user_park</td>
<td>aa0176 &#x2F; f9f30e &#x2F; 0313f1</td>
<td>✅</td>
</tr>
<tr>
<td>B</td>
<td>园区绑定一级公司（companyList + saveCompanyBind + 互斥 + Date OGNL 修复）</td>
<td>fb1857、00609c、66de7d、84d5c0、4278ba、db42b、f14cd9</td>
<td>✅</td>
</tr>
<tr>
<td>C</td>
<td>钉钉同步触发 pass_user 补写（Feign 串联 + 幂等 + 容错）</td>
<td>083a7c &#x2F; e8b9dd</td>
<td>✅</td>
</tr>
<tr>
<td>D</td>
<td>pass_user 多园区 park_id 正确</td>
<td>935da0 &#x2F; 834fa7 &#x2F; e26e97</td>
<td>✅</td>
</tr>
<tr>
<td>E</td>
<td>账号列表 园区名&#x2F;部门名 筛选</td>
<td>ee6917</td>
<td>✅</td>
</tr>
<tr>
<td>F</td>
<td>用户名重复追加随机数</td>
<td>0eec7bec</td>
<td>✅</td>
</tr>
<tr>
<td>G</td>
<td>getCurParkOrgTree 传 parkId 过滤</td>
<td>f1e59e3bf</td>
<td>✅</td>
</tr>
<tr>
<td>H</td>
<td>user&#x2F;page 返回 parkName + orgId</td>
<td>746740305</td>
<td>✅</td>
</tr>
<tr>
<td>I</td>
<td>更新用户重建部门+园区（6&#x2F;16 未提交）+ dirty check</td>
<td>工作区</td>
<td>✅</td>
</tr>
</tbody></table>
<h2 id="二、环境"><a href="#二、环境" class="headerlink" title="二、环境"></a>二、环境</h2><ul>
<li>admin-service(18200)、through-service(18134) 运行中，进程 11:19 启动，紧接 class 编译（11:19），<strong>已加载当前源码（含 6&#x2F;16 未提交改动）</strong>，未重新构建</li>
<li>直连 admin 无需鉴权 token；curl 用 <code>C:/Windows/System32/curl.exe</code>（Git 自带 curl 共享库损坏）</li>
<li>warning-service(18131) 未运行，其未提交改动（告警防重逻辑）不在本次范围（用户确认无关）</li>
</ul>
<h2 id="三、关键用例与-DB-证据"><a href="#三、关键用例与-DB-证据" class="headerlink" title="三、关键用例与 DB 证据"></a>三、关键用例与 DB 证据</h2><h3 id="T1-新建用户全链路（Feature-A-C-D）"><a href="#T1-新建用户全链路（Feature-A-C-D）" class="headerlink" title="T1 新建用户全链路（Feature A+C+D）"></a>T1 新建用户全链路（Feature A+C+D）</h3><p>mock 用户 MOCK_T1_001：dept&#x3D;59096187（运行服务部）、company&#x3D;59969486（信息通信分公司→番禺基地）</p>
<table>
<thead>
<tr>
<th>表</th>
<th>字段</th>
<th>实际值</th>
</tr>
</thead>
<tbody><tr>
<td>sys_user</td>
<td>user_from</td>
<td>Ding</td>
</tr>
<tr>
<td>sys_user_organization</td>
<td>org_id</td>
<td>59096187 运行服务部</td>
</tr>
<tr>
<td>sys_user_park</td>
<td>org_id &#x2F; park_id</td>
<td>59969486 &#x2F; 番禺基地</td>
</tr>
<tr>
<td>pass_user</td>
<td>org_id &#x2F; park_id</td>
<td>59096187 &#x2F; 番禺基地</td>
</tr>
</tbody></table>
<p>→ <strong>新人四表全写、park_id 与子级部门 org_id 均正确</strong>。</p>
<h3 id="T3-用户名重复追加随机数（Feature-F）"><a href="#T3-用户名重复追加随机数（Feature-F）" class="headerlink" title="T3 用户名重复追加随机数（Feature F）"></a>T3 用户名重复追加随机数（Feature F）</h3><p>MOCK_T3_001 的 MOBILE&#x3D;13900000099 与探针 user_name 冲突 → <code>sys_user.user_name = 1390000009910</code>（手机号 + 2 位随机”10”）。</p>
<h3 id="T4-换部门、同公司（Feature-I-重建-sys-user-organization）"><a href="#T4-换部门、同公司（Feature-I-重建-sys-user-organization）" class="headerlink" title="T4 换部门、同公司（Feature I 重建 sys_user_organization）"></a>T4 换部门、同公司（Feature I 重建 sys_user_organization）</h3><p>T4 创建（运行服务部）→ 更新（通信互联部 59096190），公司不变（59969486）：</p>
<ul>
<li>sys_user_organization：59096187 → 59096190 ✅（全删全建）</li>
<li>sys_user_park：公司未变 → <strong>dirty check 跳过</strong>，仍番禺 ✅</li>
<li>pass_user：org_id → 59096190、dist_sync_status&#x3D;PENDING、update_time &gt; create_time ✅</li>
</ul>
<h3 id="T5-跨园区调岗（Feature-I-更新-sys-user-park）"><a href="#T5-跨园区调岗（Feature-I-更新-sys-user-park）" class="headerlink" title="T5 跨园区调岗（Feature I 更新 sys_user_park）"></a>T5 跨园区调岗（Feature I 更新 sys_user_park）</h3><p>T5 创建（番禺&#x2F;59969486）→ 更新（深圳蓄能 62444075 &#x2F; 生产技术部 62368098）：</p>
<ul>
<li>sys_user_park：org_id→62444075、park_id 番禺→深蓄电厂 ✅</li>
<li>sys_user_organization：→62368098 生产技术部 ✅</li>
<li>pass_user：org_id&#x3D;62368098、park_id&#x3D;深蓄电厂、update_time&gt;create_time ✅</li>
</ul>
<h3 id="T6-T8-园区绑定公司（Feature-B）"><a href="#T6-T8-园区绑定公司（Feature-B）" class="headerlink" title="T6-T8 园区绑定公司（Feature B）"></a>T6-T8 园区绑定公司（Feature B）</h3><ul>
<li>companyList&#x2F;深蓄电厂 → 34 家一级公司，62444075 绑定前 boundParkId&#x3D;null ✅</li>
<li>saveCompanyBind {深蓄,[62444075]} → success，DB 桥表更新，companyList 复查 boundParkId&#x3D;深蓄电厂 ✅</li>
<li>互斥：再绑清蓄电厂 → <code>FAIL_OPERATE「深圳蓄能发电有限公司已被深蓄电厂绑定」</code>，DB 不变 ✅</li>
<li>（f14cd9 的 Date OGNL 修复：saveCompanyBind 调用全程无 500，间接验证）</li>
</ul>
<h3 id="T9-账号列表筛选（Feature-E）"><a href="#T9-账号列表筛选（Feature-E）" class="headerlink" title="T9 账号列表筛选（Feature E）"></a>T9 账号列表筛选（Feature E）</h3><ul>
<li>parkName&#x3D;番禺 + realName&#x3D;T → totalCount&#x3D;4（T1&#x2F;T3&#x2F;T4&#x2F;claudetestA，全番禺）✅</li>
<li>organizationName&#x3D;通信互联 → totalCount&#x3D;68 ✅</li>
</ul>
<h3 id="T10-user-page-返回字段（Feature-H）"><a href="#T10-user-page-返回字段（Feature-H）" class="headerlink" title="T10 user&#x2F;page 返回字段（Feature H）"></a>T10 user&#x2F;page 返回字段（Feature H）</h3><p>查 MOCK_T1_001 → 记录含 <code>orgId=59096187</code>、<code>parkName=番禺基地</code> ✅</p>
<h3 id="T11-园区部门树过滤（Feature-G）"><a href="#T11-园区部门树过滤（Feature-G）" class="headerlink" title="T11 园区部门树过滤（Feature G）"></a>T11 园区部门树过滤（Feature G）</h3><p>getCurParkOrgTree parkId&#x3D;番禺 → 仅返回番禺绑定的 5 家公司（公司本部&#x2F;园区总部&#x2F;信息通信分公司&#x2F;园区二级部门&#x2F;西部检修试验分公司）✅</p>
<h3 id="T12-dirty-check（Feature-I）"><a href="#T12-dirty-check（Feature-I）" class="headerlink" title="T12 dirty check（Feature I）"></a>T12 dirty check（Feature I）</h3><p>T4 第 3 次同步（数据不变）：sys_user_organization 仅 1 条、create_time 停在更新时刻（04:13:53），无重建 ✅</p>
<h2 id="四、观察与说明"><a href="#四、观察与说明" class="headerlink" title="四、观察与说明"></a>四、观察与说明</h2><h3 id="1-dist-sync-status-T4-PENDING-T5-NONE（非-bug）"><a href="#1-dist-sync-status-T4-PENDING-T5-NONE（非-bug）" class="headerlink" title="1. dist_sync_status: T4&#x3D;PENDING &#x2F; T5&#x3D;NONE（非 bug）"></a>1. dist_sync_status: T4&#x3D;PENDING &#x2F; T5&#x3D;NONE（非 bug）</h3><p>UPDATE 分支均设 PENDING（T4 佐证）。T5 的 PENDING 被定时任务 <code>PassDistSyncer</code>（60s&#x2F;90s 轮询）消费：<code>resolveShouldSync(深蓄电厂)</code> 判定”总部数据无需同步”→ 标 NONE。<strong>属下游消费者正常行为，被测功能本身正确</strong>。</p>
<h3 id="2-探针-org-id-NULL-的正确性"><a href="#2-探针-org-id-NULL-的正确性" class="headerlink" title="2. 探针 org_id&#x3D;NULL 的正确性"></a>2. 探针 org_id&#x3D;NULL 的正确性</h3><p>早期探针 PROBE_001 用 <code>DEPARTMENT=[136602494]</code>（顶级公司）→ pass_user.org_id&#x3D;NULL。<strong>这是设计正确行为</strong>：<code>listByConditionExt</code> 的 org_id 子查询只取 <code>parent_id</code> 非空的子级部门，顶级公司被过滤。</p>
<p>真实钉钉部门语义：DEPARTMENT&#x3D;子级部门、公司取自 PDEPARTS。后续 T1-T5 改用真实子级部门（如运行服务部 59096187）后 org_id 正确。</p>
<h3 id="3-全量同步副作用（已恢复）"><a href="#3-全量同步副作用（已恢复）" class="headerlink" title="3. 全量同步副作用（已恢复）"></a>3. 全量同步副作用（已恢复）</h3><p><code>handleDingUserSync</code> 是全量同步：每次 mock 只传部分用户会把其他存量钉钉用户软删（delete_flag&#x3D;1）。测试过程累计软删全部 <strong>5295 个 Ding 用户（含真实员工）</strong>。</p>
<blockquote>
<p>[!warning] 软删风险<br><strong>测试后已执行 <code>UPDATE sys_user SET delete_flag=&#39;0&#39; WHERE user_from=&#39;Ding&#39;</code>（Changed 5295），全部恢复。</strong> 软删可逆，关联表数据未丢失。</p>
<p>但这暴露了一个<strong>全量同步的设计缺陷</strong>——mock 部分用户就会影响其他用户。生产环境如果误用 mock 端点（不带 IP 白名单）会导致<strong>全员软删</strong>。</p>
</blockquote>
<h3 id="4-测试数据保留（未删）"><a href="#4-测试数据保留（未删）" class="headerlink" title="4. 测试数据保留（未删）"></a>4. 测试数据保留（未删）</h3><ul>
<li>sys_user：MOCK_T1&#x2F;T3&#x2F;T4&#x2F;T5_001、探针 PROBE_001</li>
<li>sys_park_organization：62444075（深圳蓄能）绑定至深蓄电厂</li>
<li>pass_user：上述用户的便捷通行记录</li>
</ul>
<p>按用户要求保留，供复查。</p>
<h2 id="五、不在本次范围"><a href="#五、不在本次范围" class="headerlink" title="五、不在本次范围"></a>五、不在本次范围</h2><ul>
<li>warning-service 告警防重逻辑（<code>AlarmingEvenSinkServiceImpl</code>、<code>AlarmingRecordMapper.xml</code>）——与钉钉同步无关，warning 未运行</li>
<li><code>PassUserGrantServiceImpl</code> grantExecStatus&#x3D;3 同步标记、<code>service-util/pom.xml</code> 注释 jna 依赖——无关功能线</li>
<li>TODO :251（被删除钉钉用户的关联数据清理）——6&#x2F;16 设计明确标注”本次不做”</li>
</ul>
<h2 id="六、集成测试方法论"><a href="#六、集成测试方法论" class="headerlink" title="六、集成测试方法论"></a>六、集成测试方法论</h2><h3 id="1-Mock-端点是隔离生产数据的关键"><a href="#1-Mock-端点是隔离生产数据的关键" class="headerlink" title="1. Mock 端点是隔离生产数据的关键"></a>1. Mock 端点是隔离生产数据的关键</h3><p><img data-src="/images/posts/2026-06-16-dist-sync-integration-test/fig2_001.jpg" alt="mock 端点调用链"><br><em>图2：mock 端点调用链</em></p>
<p><code>mockDingUserSync</code> 端点<strong>绕过 iot-middle 直接接收 mock 数据</strong>，真实链路走 Spring 容器 + 真实 MyBatis + 真实 MySQL + 真实事务管理。这样：</p>
<ul>
<li>不污染生产数据（IP 白名单）</li>
<li>不依赖中台（mock 自己造）</li>
<li>真实环境跑真实代码（不脱裤子放屁）</li>
</ul>
<h3 id="2-全链路-vs-单服务测试"><a href="#2-全链路-vs-单服务测试" class="headerlink" title="2. 全链路 vs 单服务测试"></a>2. 全链路 vs 单服务测试</h3><p>这次没有单独测”admin 写 sys_user_park”或”through 写 pass_user”——而是<strong>端到端跑 4 张表的一致性</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sys_user (admin 写)</span><br><span class="line">sys_user_organization (admin 写)</span><br><span class="line">sys_user_park (admin 写)</span><br><span class="line">pass_user (through Feign 触发写)</span><br></pre></td></tr></table></figure>

<p>单服务测试容易暴露局部 OK 但全局不一致的 bug（如昨天发现的死亡覆盖链）。</p>
<h3 id="3-用真实部门-vs-模拟数据"><a href="#3-用真实部门-vs-模拟数据" class="headerlink" title="3. 用真实部门 vs 模拟数据"></a>3. 用真实部门 vs 模拟数据</h3><p>早期用 <code>DEPARTMENT=[136602494]</code>（顶级公司）测试，结果 pass_user.org_id&#x3D;NULL。这暴露的不是 bug 而是<strong>设计行为</strong>——org_id 子查询只取子级部门。</p>
<p><strong>教训</strong>：测试数据要尽量用真实业务数据，不能凭空造”理想值”。</p>
<h3 id="4-反面案例的发现价值"><a href="#4-反面案例的发现价值" class="headerlink" title="4. 反面案例的发现价值"></a>4. 反面案例的发现价值</h3><p>mock 数据 <code>MOCK_TEST_PASS_001</code> 用 <code>DEPARTMENT=[59096189]</code>（已硬删），导致 pass_user 找不到这个用户。<strong>这一开始被认为是 bug，深入排查才挖出死亡覆盖链</strong>。</p>
<p><strong>反面案例</strong>比正面案例更有价值——正面案例成功不代表真实业务成功，反面案例的失败往往暴露设计假设。</p>
<h3 id="5-dist-sync-status-含义的多层解读"><a href="#5-dist-sync-status-含义的多层解读" class="headerlink" title="5. dist_sync_status 含义的多层解读"></a>5. dist_sync_status 含义的多层解读</h3><p>T4&#x3D;PENDING、T5&#x3D;NONE 是下游消费者的不同响应：</p>
<ul>
<li>T4：换部门（park 不变）→ 园区侧不需重新下发 → PENDING（备用）</li>
<li>T5：跨园区 → 总部无需下发到其他园区 → NONE（”总部数据无需同步”）</li>
</ul>
<p>不要把 NONE 当成 bug——它是有意义的业务状态。</p>
<h3 id="6-软删后必须做恢复"><a href="#6-软删后必须做恢复" class="headerlink" title="6. 软删后必须做恢复"></a>6. 软删后必须做恢复</h3><p>全量同步副作用是<strong>已知风险</strong>——mock 部分用户会软删其他用户。<strong>测试前准备好恢复 SQL</strong>：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">UPDATE</span> sys_user <span class="keyword">SET</span> delete_flag<span class="operator">=</span><span class="string">&#x27;0&#x27;</span> <span class="keyword">WHERE</span> user_from<span class="operator">=</span><span class="string">&#x27;Ding&#x27;</span> <span class="keyword">AND</span> delete_flag<span class="operator">=</span><span class="string">&#x27;1&#x27;</span>;</span><br></pre></td></tr></table></figure>

<p>这条 SQL 必须在测试结束后<strong>无条件执行</strong>——避免给生产环境留下脏数据。</p>
<h2 id="七、产出物"><a href="#七、产出物" class="headerlink" title="七、产出物"></a>七、产出物</h2><table>
<thead>
<tr>
<th>项</th>
<th>路径</th>
</tr>
</thead>
<tbody><tr>
<td>测试清单</td>
<td><code>docs/integration-test-2026-06-16/测试清单.md</code></td>
</tr>
<tr>
<td>执行脚本</td>
<td><code>docs/integration-test-2026-06-16/run-tests.sh</code></td>
</tr>
<tr>
<td>mock 数据</td>
<td><code>docs/integration-test-2026-06-16/mock/*.json</code></td>
</tr>
<tr>
<td>接口响应</td>
<td><code>docs/integration-test-2026-06-16/out/*.resp</code></td>
</tr>
<tr>
<td>本报告</td>
<td><code>E:\obs\zhishiku\项目\南网项目\dist_sync分支集成测试报告-2026-06-16.md</code></td>
</tr>
</tbody></table>
<p><img data-src="/images/posts/2026-06-16-dist-sync-integration-test/fig3_001.jpg" alt="DB 落库对比"><br><em>图3：DB 落库对比</em></p>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><h3 id="集成测试发现的-bug-比单元测试更有价值"><a href="#集成测试发现的-bug-比单元测试更有价值" class="headerlink" title="集成测试发现的 bug 比单元测试更有价值"></a>集成测试发现的 bug 比单元测试更有价值</h3><p>单元测试 mock 一切外部依赖，集成测试直面真实链路。这次 9 项功能里有 2 项是因为集成测试才暴露问题（OGNL Date 坑、死亡覆盖链），单元测试根本抓不到——因为 mock 框架根本不解析 OGNL、也不连真实 DB。</p>
<h3 id="端到端要看每一张表"><a href="#端到端要看每一张表" class="headerlink" title="端到端要看每一张表"></a>端到端要看每一张表</h3><p>查 <code>pass_user</code> 找不到用户，第一反应”Feign 失败”，但<strong>只看一张表</strong>。后来查 <code>sys_user_park.park_id</code> 才发现 mapper SELECT 漏列——多查一张表就把根因暴露了。</p>
<h3 id="软删是测试的”金标准”"><a href="#软删是测试的”金标准”" class="headerlink" title="软删是测试的”金标准”"></a>软删是测试的”金标准”</h3><p>mock 数据准备要”端到端验证”：调 mock 端点后，<strong>先去查中间表是不是预期的值</strong>，再去看最终表。这次先查 <code>sys_user_park.park_id</code>，再查 <code>pass_user.park_id</code>，顺藤摸瓜找到 mapper 漏列。</p>
<h3 id="测试报告的写作纪律"><a href="#测试报告的写作纪律" class="headerlink" title="测试报告的写作纪律"></a>测试报告的写作纪律</h3><p>好的集成测试报告要回答 4 个问题：</p>
<ol>
<li><strong>测了什么</strong>：9 项功能 + 12 个用例</li>
<li><strong>怎么测的</strong>：mock 端点 + DB 落库验证</li>
<li><strong>结果如何</strong>：全过 + 1 处非阻断差异</li>
<li><strong>怎么恢复</strong>：5295 条软删数据已恢复</li>
</ol>
<h3 id="集成测试不能覆盖的事"><a href="#集成测试不能覆盖的事" class="headerlink" title="集成测试不能覆盖的事"></a>集成测试不能覆盖的事</h3><ul>
<li>warning-service 告警防重逻辑（warning 未启动）</li>
<li>被删除钉钉用户的关联数据清理（6&#x2F;16 设计明确不做）</li>
<li>dist_sync 分支已有的 <code>PassUserGrantServiceImpl</code> grantExecStatus&#x3D;3 同步标记</li>
</ul>
<p><strong>这些需要在后续的专项测试里覆盖</strong>——一次集成测试不是万能的。</p>
<h2 id="下一步"><a href="#下一步" class="headerlink" title="下一步"></a>下一步</h2><p>集成测试通过后，下一步是部署到 dev 环境做真实用户的端到端验证。这次 mock 数据能跑通不代表真实中台数据能跑通——后续需要：</p>
<ul>
<li>用真实 Dataphin API 响应样本做测试</li>
<li>验证 PDEPARTS 废弃后的新格式</li>
<li>验证头像同步的网络可达性</li>
</ul>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>南网</category>
      </categories>
      <tags>
        <tag>集成测试</tag>
        <tag>dist_sync</tag>
        <tag>端到端测试</tag>
        <tag>多表一致性</tag>
      </tags>
  </entry>
  <entry>
    <title>PDEPARTS 字段下线与钉钉同步真实格式链路测试报告</title>
    <url>/2026/06/22/2026-06-22-pdeparts-offline/</url>
    <content><![CDATA[<h2 id="导读"><a href="#导读" class="headerlink" title="导读"></a>导读</h2><blockquote>
<p>今天捅了两个马蜂窝：<strong>中台 Dataphin 下线了 PDEPARTS 字段</strong>，导致所有依赖该字段解析公司归属的 Java 逻辑静默失效（<code>getCompanyIdFromPDeparts</code> &#x2F; <code>getDepartmentAsList</code> 返回 null），1542 个 Ding 用户 100% 缺 sys_user_park；<strong>用真实格式跑 12 个端到端测试用例</strong>，又挖出 4 个新 bug（增量更新取消删除标记失效、园区链路仍依赖 PDEPARTS、pass_user 链路同样依赖 PDEPARTS、部门 vs 公司数据源不一致）。修复方案是改用部门 UUID 沿 parent_id 递归上溯查桥表（CTE），以及补回 synSysUserToPassUser ADD 分支漏设的 distSyncStatus。</p>
</blockquote>
<span id="more"></span>

<div class="audio-player">
  <div class="audio-section">
    <h4>🎧 文章导读</h4>
    <audio controls preload="metadata">
      <source src="/audio/2026-06-22-pdeparts-offline/narration.mp3" type="audio/mpeg">
    </audio>
  </div>
  <div class="audio-section">
    <h4>🎵 背景音乐</h4>
    <audio controls loop preload="metadata">
      <source src="/audio/2026-06-22-pdeparts-offline/bgm.mp3" type="audio/mpeg">
    </audio>
  </div>
</div>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>钉钉用户同步链路异常：园区侧永远收不到人员数据，门禁通行功能受影响。最初以为是某个 service 的小 bug，深挖才发现是中台数据模型层变更（<code>PDEPARTS</code> 字段下线）导致整条链路静默失效。</p>
<p>这种”上游变更、下游无感”的故障最难排查——没有异常日志，没有错误码，只有”业务不工作”的最终表现。</p>
<h2 id="问题背景"><a href="#问题背景" class="headerlink" title="问题背景"></a>问题背景</h2><p>钉钉用户数据从南网中台 Dataphin 流入园区平台，经过四个环节最终下发到各园区：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">中台拉数据 → admin 写本地（sys_user / sys_user_organization / sys_user_park）</span><br><span class="line">   → Feign 调 through 补写总部 pass_user</span><br><span class="line">   → 定时任务通过 ROMA 下发园区</span><br></pre></td></tr></table></figure>

<p>这条链路上任一环节数据缺失，下游就会拿到空值，最终表现为”园区收不到人员”。</p>
<p>本次问题正是链路最上游的数据模型发生了变更——中台 Dataphin 的 <code>h_user</code> 表下线了 <code>PDEPARTS</code> 字段，同时 <code>department</code> 字段从数字 JSON 数组格式（如 <code>[494216765]</code>）变成了 UUID 字符串（如 <code>e6a63c82-80bc-4a75-8d71-67675cfcf5ec</code>）。</p>
<p><img data-src="/images/posts/2026-06-22-pdeparts-offline/fig1_001.jpg" alt="Dataphin 数据流断链"><br><em>图1：Dataphin 数据流断链</em></p>
<h2 id="排查思路"><a href="#排查思路" class="headerlink" title="排查思路"></a>排查思路</h2><h3 id="从现象入手：trace-整条同步链路"><a href="#从现象入手：trace-整条同步链路" class="headerlink" title="从现象入手：trace 整条同步链路"></a>从现象入手：trace 整条同步链路</h3><p>用户最初报告的现象是”园区写不进 pass_user”。从终端往回追溯，pass_user 是 through 服务写的，入口是 <code>PassUserClient.synSysUserToPassUser()</code> 这个 Feign 调用。</p>
<p><strong>关键教训</strong>：本地 mock 数据的 PDEPARTS 是手工构造的、格式正确，所以本地测试看起来都正常——这埋下了后面一个重要教训。</p>
<h3 id="查真实库找证据：dist-sync-status-分布异常"><a href="#查真实库找证据：dist-sync-status-分布异常" class="headerlink" title="查真实库找证据：dist_sync_status 分布异常"></a>查真实库找证据：dist_sync_status 分布异常</h3><p><img data-src="/images/posts/2026-06-22-pdeparts-offline/fig2_001.jpg" alt="dist_sync_status 分布"><br><em>图2：dist_sync_status 分布</em></p>
<p>用 MCP 查 building_through 库的 pass_user 表，按 <code>dist_sync_status</code> 分组统计：</p>
<ul>
<li><strong>NONE 454 条</strong>（标记为”总部数据无需同步”，正常）</li>
<li><strong>ING 236 条</strong>（卡在”已下发待 ACK”状态）</li>
<li><strong>null 7 条</strong>（新创建的 pass_user 完全没有 status）</li>
</ul>
<p>null 状态这 7 条很可疑——追溯到 <code>PassUserServiceImpl.synSysUserToPassUser()</code> 的 ADD 分支，发现了第二个 Bug：**新增分支漏设 <code>distSyncStatus</code>**。</p>
<h3 id="查-0618-生产-dump：Ding-用户-100-缺-sys-user-park"><a href="#查-0618-生产-dump：Ding-用户-100-缺-sys-user-park" class="headerlink" title="查 0618 生产 dump：Ding 用户 100% 缺 sys_user_park"></a>查 0618 生产 dump：Ding 用户 100% 缺 sys_user_park</h3><p>继续往上翻：admin 服务的 <code>synSysUserToPassUser</code> 是从 sys_user 关联 sys_user_park 拿 parkId 的。如果 sys_user_park 没写，下游 passUserDTO.parkId 就是 null。</p>
<p>用 Python 解析 <code>E:/nanwang/项目会用压缩包/数据库sql文件/db0618(1).sql</code> 这个 12MB 的生产快照：</p>
<blockquote>
<p><strong>1542 个 <code>user_from=&#39;Ding&#39;</code> 的真实钉钉用户里，0 个有 sys_user_park 记录（100% 缺失）</strong>。只有 182 个（11.8%）有 sys_user_organization 记录——这 11.8% 是老格式时代的存量。</p>
</blockquote>
<h3 id="用户提供关键证据：pDeparts-null-的真实响应"><a href="#用户提供关键证据：pDeparts-null-的真实响应" class="headerlink" title="用户提供关键证据：pDeparts&#x3D;null 的真实响应"></a>用户提供关键证据：pDeparts&#x3D;null 的真实响应</h3><p>转折点来了。用户提供了真实的 Dataphin 响应样本：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">DataPhinUser(userId=$:LWCP_v1:$4G18hbvCSH4n5IRfVMkxGoF3Ti+7WKR7,</span><br><span class="line">  openId=wBSKiPJ5gTiPjwcR0gB1xMNQiEiE,</span><br><span class="line">  department=e6a63c82-80bc-4a75-8d71-67675cfcf5ec,  // UUID</span><br><span class="line">  pDeparts=null,                                       // 彻底没了</span><br><span class="line">  name=马漫欢, mobile=15602386855, ...)</span><br></pre></td></tr></table></figure>

<p>再去查 <code>DataPhinClientImpl.queryDingUser()</code> 的”全字段探测”日志——它调 <code>setReturnFields(null)</code> 探测 Dataphin <code>h_user</code> 表所有可用列，返回的 metaColumns 完整列表里<strong>根本没有 PDEPARTS</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">PROFILEPHOTOURL, EXTATTR, DINGTALKUNIONID, STATE, EMAIL, HOMEPHONE, ENTRYDATE,</span><br><span class="line">OFFICEPHONE, EMPLOYEENUMBER, OBJECTID, POSITION, ..., PARENTID, PASSWORD,</span><br><span class="line">TITLE, CREATEDTIME, WECHATUSERID, SORTKEY, MOBILE</span><br></pre></td></tr></table></figure>

<p><strong>没有 PDEPARTS</strong>。也就是说，字段在源表里被删了，不是代码忘了请求。这个证据把根因坐实。</p>
<h2 id="根因分析"><a href="#根因分析" class="headerlink" title="根因分析"></a>根因分析</h2><blockquote>
<p>[!danger] 根因描述<br>中台 Dataphin <code>h_user</code> 源表下线了 <code>PDEPARTS</code> 字段，同时 <code>department</code> 字段格式从数字 JSON 数组变为 UUID 字符串。所有依赖这两个字段解析公司归属和部门挂靠的 Java 逻辑（<code>getCompanyIdFromPDeparts</code>、<code>getDepartmentAsList</code>）静默返回 null&#x2F;空集合，导致 sys_user_park 和 sys_user_organization 全部缺失，下游 pass_user 拿不到 parkId，整条下发链路断裂。</p>
</blockquote>
<p><code>DataPhinUserResp.java:135-153</code> 的 <code>getCompanyIdFromPDeparts()</code>，在 pDeparts 为空时直接返回 null：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> String <span class="title function_">getCompanyIdFromPDeparts</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (!StringUtils.hasText(<span class="built_in">this</span>.pDeparts)) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">null</span>;  <span class="comment">// ← pDeparts=null，直接返回 null</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// ...解析 [[dept, company, seq]] 取 [0][1]...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>链路从此处开始崩塌：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sys_user 没有 sys_user_park 关联</span><br><span class="line">   → listByConditionExt（UserMapperExt.xml 的 LEFT JOIN）查不到 park_id</span><br><span class="line">   → synSysUserToPassUser 写入 passUserDTO.setParkId(null)</span><br><span class="line">   → PassDistSyncer.resolveShouldSync(null) 返回 false</span><br><span class="line">   → 标记 NONE&quot;总部数据无需同步&quot;</span><br><span class="line">   → 永不下发到园区</span><br></pre></td></tr></table></figure>

<h2 id="次要-Bug：synSysUserToPassUser-ADD-分支漏设-distSyncStatus"><a href="#次要-Bug：synSysUserToPassUser-ADD-分支漏设-distSyncStatus" class="headerlink" title="次要 Bug：synSysUserToPassUser ADD 分支漏设 distSyncStatus"></a>次要 Bug：synSysUserToPassUser ADD 分支漏设 distSyncStatus</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// PassUserServiceImpl.java:471-488（ADD 分支，漏了一行）</span></span><br><span class="line">passUserDTO.setParkId(user.getParkId());</span><br><span class="line">passUserDTO.setOrgId(user.getOrgId());</span><br><span class="line">passUserDTO.setDeleteFlag(DictConstants.DEL_FLAG_NO);</span><br><span class="line">WebExtUtils.iniCreate(passUserDTO);</span><br><span class="line"><span class="comment">// ← 这里没有 setDistSyncStatus(PENDING)！</span></span><br><span class="line">toAdd.add(passUserDTO);</span><br></pre></td></tr></table></figure>

<p>对比同方法 <code>createTempPassUser()</code> 第 184-186 行是有这个状态的——唯独 ADD 分支漏了。</p>
<blockquote>
<p>[!info] 为什么是 null 而不是其他值<br><code>pass_user.dist_sync_status</code> 列在仓库里没有任何 ALTER 迁移脚本，列定义 <code>DEFAULT = null</code>、<code>IS_NULLABLE=YES</code>——是手动加的列。通用 mapper 的 <code>addSelective</code> 只插非 null 字段，不设就是 null。而 <code>findByDistSyncStatus</code> 查询是 <code>WHERE dist_sync_status = &#39;PENDING&#39;</code>（PassUserMapper.xml:1378-1381），null 状态永远查不到。</p>
</blockquote>
<p>数据库佐证：building_through 库里 7 条 <code>dist_sync_status IS NULL</code> 的记录全是 6&#x2F;12-6&#x2F;16 的测试数据。</p>
<h2 id="关于-EncryptionUtils-的排查弯路"><a href="#关于-EncryptionUtils-的排查弯路" class="headerlink" title="关于 EncryptionUtils 的排查弯路"></a>关于 EncryptionUtils 的排查弯路</h2><p>排查过程中走了两次弯路，值得记录。一开始怀疑两个地方：</p>
<ol>
<li><del><code>synSysUserToPassUser</code> 里 <code>phoneEncryptToHex(user.getPhone())</code> 二次加密</del>——实际 <code>EncryptionUtils.phoneEncryptToHex</code> 是<strong>幂等</strong>的（只在 <code>length==11</code> 时加密，已加密的 64 位 hex 原样返回），不会二次加密</li>
<li><del><code>idCardSubTail(null, 4)</code> NPE</del>——<code>idCardSubTail</code> 内部 <code>StringUtils.isEmpty(null)</code> 为 true 直接返回 null，不会 NPE</li>
</ol>
<blockquote>
<p>[!warning] 教训<br><strong>下结论前先读 utils 的实现</strong>，不要靠方法名望文生义。这两个误报各占去半小时排查时间。</p>
</blockquote>
<h2 id="修复方案"><a href="#修复方案" class="headerlink" title="修复方案"></a>修复方案</h2><h3 id="PDEPARTS-相关（主因）"><a href="#PDEPARTS-相关（主因）" class="headerlink" title="PDEPARTS 相关（主因）"></a>PDEPARTS 相关（主因）</h3><ol>
<li>**废弃 <code>getCompanyIdFromPDeparts()</code>**——字段没了，方法彻底作废</li>
<li><strong>改用 PARENTID UUID 定位组织</strong>——新 schema 里 <code>department</code>（映射自 PARENTID）是父级组织节点 UUID。要从这个 UUID 出发，沿 <code>sys_organization.parent_id</code> 往上找公司级节点</li>
<li><strong>重建 <code>sys_park_organization</code> 桥表</strong>——现在存的是老数字 id（<code>59096189</code>、<code>136602494</code>），新 UUID 对不上，查 park_id 永远查不到</li>
</ol>
<h3 id="synSysUserToPassUser-ADD-分支（次要-Bug）"><a href="#synSysUserToPassUser-ADD-分支（次要-Bug）" class="headerlink" title="synSysUserToPassUser ADD 分支（次要 Bug）"></a>synSysUserToPassUser ADD 分支（次要 Bug）</h3><p>补一行：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">passUserDTO.setDistSyncStatus(PaasDistSyncStatus.PENDING.getStatus());</span><br></pre></td></tr></table></figure>

<p>并刷存量 7 条 null 状态数据：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">UPDATE</span> pass_user</span><br><span class="line"><span class="keyword">SET</span> dist_sync_status <span class="operator">=</span> <span class="string">&#x27;PENDING&#x27;</span></span><br><span class="line"><span class="keyword">WHERE</span> dist_sync_status <span class="keyword">IS</span> <span class="keyword">NULL</span></span><br><span class="line">  <span class="keyword">AND</span> delete_flag <span class="operator">=</span> <span class="string">&#x27;0&#x27;</span>;</span><br></pre></td></tr></table></figure>

<h2 id="真实格式链路测试报告"><a href="#真实格式链路测试报告" class="headerlink" title="真实格式链路测试报告"></a>真实格式链路测试报告</h2><h3 id="字段映射变更（已适配生产）"><a href="#字段映射变更（已适配生产）" class="headerlink" title="字段映射变更（已适配生产）"></a>字段映射变更（已适配生产）</h3><table>
<thead>
<tr>
<th>Java 字段</th>
<th>旧 <code>@JsonProperty</code></th>
<th>新 <code>@JsonProperty</code></th>
</tr>
</thead>
<tbody><tr>
<td><code>userId</code></td>
<td><code>USERID</code></td>
<td><strong><code>DINGID</code></strong></td>
</tr>
<tr>
<td><code>openId</code></td>
<td><code>OPENID</code></td>
<td><strong><code>DINGTALKUNIONID</code></strong></td>
</tr>
<tr>
<td><code>department</code></td>
<td><code>DEPARTMENT</code></td>
<td><strong><code>PARENTID</code></strong></td>
</tr>
<tr>
<td><code>orgEmail</code></td>
<td><code>ORGEMAIL</code></td>
<td><strong><code>EMAIL</code></strong></td>
</tr>
<tr>
<td><code>avatar</code></td>
<td><code>AVATAR</code></td>
<td><strong><code>PROFILEPHOTOURL</code></strong></td>
</tr>
<tr>
<td><code>userOrder</code></td>
<td><code>USERORDER</code></td>
<td><strong><code>SORTKEY</code></strong></td>
</tr>
<tr>
<td><code>tsUserId</code></td>
<td><code>TSUSERID</code></td>
<td><strong><code>OBJECTID</code></strong></td>
</tr>
</tbody></table>
<h3 id="mock-数据格式（真实生产格式）"><a href="#mock-数据格式（真实生产格式）" class="headerlink" title="mock 数据格式（真实生产格式）"></a>mock 数据格式（真实生产格式）</h3><ul>
<li><code>PARENTID</code>：单个 UUID（如 <code>e6a63c82-80bc-4a75-8d71-67675cfcf5ec</code>）</li>
<li><code>PDEPARTS</code>：<code>null</code>（生产不再返回）</li>
<li>4 个 mock 用户用不同 UUID 模拟”同部门 → 换部门 → 跨园区”</li>
</ul>
<h3 id="HTTP-层测试结果"><a href="#HTTP-层测试结果" class="headerlink" title="HTTP 层测试结果"></a>HTTP 层测试结果</h3><table>
<thead>
<tr>
<th>用例</th>
<th>HTTP</th>
<th>data</th>
<th>备注</th>
</tr>
</thead>
<tbody><tr>
<td>T6 companyList</td>
<td>200</td>
<td>4 条</td>
<td>✅</td>
</tr>
<tr>
<td>T7 saveCompanyBind</td>
<td>200</td>
<td>null</td>
<td>✅</td>
</tr>
<tr>
<td>T8 互斥</td>
<td>200</td>
<td>FAIL_OPERATE</td>
<td>✅ 业务失败符合预期</td>
</tr>
<tr>
<td>T1 同步</td>
<td>200</td>
<td>1</td>
<td>✅</td>
</tr>
<tr>
<td>T2 幂等</td>
<td>200</td>
<td>1</td>
<td>✅</td>
</tr>
<tr>
<td>T3 重名</td>
<td>200</td>
<td>1</td>
<td>✅</td>
</tr>
<tr>
<td>T4-create</td>
<td>200</td>
<td>1</td>
<td>✅</td>
</tr>
<tr>
<td>T4-update 换部门</td>
<td>200</td>
<td>1</td>
<td>✅</td>
</tr>
<tr>
<td>T12 dirty check</td>
<td>200</td>
<td>1</td>
<td>✅</td>
</tr>
<tr>
<td>T5-create</td>
<td>200</td>
<td>1</td>
<td>✅</td>
</tr>
<tr>
<td>T5-update 跨园区</td>
<td>200</td>
<td>1</td>
<td>✅</td>
</tr>
<tr>
<td>T11 getCurParkOrgTree</td>
<td>200</td>
<td>2 条</td>
<td>✅</td>
</tr>
<tr>
<td>T9 parkName&#x2F;orgName&#x2F;userName 筛选</td>
<td>200</td>
<td>totalCount:0</td>
<td>⚠️ Bug#1 副作用</td>
</tr>
</tbody></table>
<h3 id="DB-落库（关键发现）"><a href="#DB-落库（关键发现）" class="headerlink" title="DB 落库（关键发现）"></a>DB 落库（关键发现）</h3><table>
<thead>
<tr>
<th>表</th>
<th>本次同步结果</th>
<th>状态</th>
</tr>
</thead>
<tbody><tr>
<td><code>sys_user</code></td>
<td>4 个 mock 用户全 <code>delete_flag=1</code></td>
<td>❌ <strong>Bug#1</strong></td>
</tr>
<tr>
<td><code>sys_user_organization</code></td>
<td>4 条新记录，<code>org_id=PARENTID UUID</code></td>
<td>✅ 新代码适配</td>
</tr>
<tr>
<td><code>sys_user_park</code></td>
<td><strong>0 条新记录</strong>，全是 06-16 首次测试快照</td>
<td>❌ <strong>Bug#2</strong></td>
</tr>
<tr>
<td><code>pass_user</code></td>
<td><strong>0 条变化</strong>，全是 06-16 快照</td>
<td>❌ <strong>Bug#3</strong></td>
</tr>
</tbody></table>
<p><img data-src="/images/posts/2026-06-22-pdeparts-offline/fig3_001.jpg" alt="4 个 Bug 表格"><br><em>图3：4 个 Bug 表格</em></p>
<h2 id="4-个新-Bug"><a href="#4-个新-Bug" class="headerlink" title="4 个新 Bug"></a>4 个新 Bug</h2><h3 id="Bug-1：增量更新取消删除标记失效"><a href="#Bug-1：增量更新取消删除标记失效" class="headerlink" title="Bug#1：增量更新取消删除标记失效"></a>Bug#1：增量更新取消删除标记失效</h3><ul>
<li><strong>位置</strong>：<code>UserSyncServiceImpl.java:146-180</code>（<code>toUpdateUsers</code> stream 内的 map 块）</li>
<li><strong>症状</strong>：本次同步后所有已存在用户被软删（<code>delete_flag=1</code>），T9&#x2F;T10 查询 <code>totalCount=0</code></li>
<li><strong>根因</strong>：第 161 行 <code>existDingUser.setDeleteFlag(NO)</code> 只修改了 <code>existDingUserMap</code> value 的引用对象，但新构造的 <code>updateUser</code> DTO（真正写入 DB 的对象）未设置 <code>deleteFlag</code> 字段</li>
<li><strong>影响范围</strong>：所有走 update 分支的已存在钉钉用户（含真实员工），本次 <strong>5297 个真实用户被一并错误软删</strong></li>
</ul>
<p><strong>修复方向</strong>：在 <code>updateUser</code> DTO 中显式 <code>setDeleteFlag(DEL_FLAG_NO)</code>；或确认 <code>listByCondition</code> 返回的对象与 <code>existDingUserMap</code> value 是否同一引用，并据此调整 list 的 <code>peek</code> 时机。</p>
<h3 id="Bug-2：园区链路仍依赖-PDEPARTS"><a href="#Bug-2：园区链路仍依赖-PDEPARTS" class="headerlink" title="Bug#2：园区链路仍依赖 PDEPARTS"></a>Bug#2：园区链路仍依赖 PDEPARTS</h3><ul>
<li><strong>位置</strong>：<code>UserSyncServiceImpl.java:228, 297, 307</code></li>
<li><strong>症状</strong>：<code>PDEPARTS=null</code> 时 <code>getCompanyIdFromPDeparts()</code> 返回 <code>null</code>，整个园区写&#x2F;更新&#x2F;跨园区跳过</li>
<li><strong>根因</strong>：真实生产数据不再返回 <code>PDEPARTS</code>，公司 ID 失去来源</li>
</ul>
<p><strong>修复方向</strong>：</p>
<ul>
<li>A. <code>DataPhinUser</code> 新增 <code>companyId</code> 字段（直接来自中台）</li>
<li>B. 通过 <code>PARENTID</code> → <code>sys_organization</code> 表反查公司</li>
<li>C. 保持现状但与业务确认新数据源</li>
</ul>
<h3 id="Bug-3：pass-user-链路同样依赖-PDEPARTS"><a href="#Bug-3：pass-user-链路同样依赖-PDEPARTS" class="headerlink" title="Bug#3：pass_user 链路同样依赖 PDEPARTS"></a>Bug#3：pass_user 链路同样依赖 PDEPARTS</h3><p>与 Bug#2 同一根因，<strong>修复与 Bug#2 一并处理</strong>。</p>
<h3 id="Bug-4（设计层）：部门-vs-公司数据源不一致"><a href="#Bug-4（设计层）：部门-vs-公司数据源不一致" class="headerlink" title="Bug#4（设计层）：部门 vs 公司数据源不一致"></a>Bug#4（设计层）：部门 vs 公司数据源不一致</h3><ul>
<li><strong>症状</strong>：<code>sys_user_organization</code> 走 <code>PARENTID</code>（新代码已适配 UUID），但 <code>sys_user_park</code> &#x2F; <code>pass_user</code> 仍走 <code>PDEPARTS</code>。两边数据格式（UUID vs 数字 ID）和值完全不一致</li>
<li><strong>建议</strong>：统一公司获取逻辑，所有关联表用同一来源</li>
</ul>
<h3 id="软删恢复"><a href="#软删恢复" class="headerlink" title="软删恢复"></a>软删恢复</h3><blockquote>
<p>测试造成的影响：5297 个真实用户被错误软删（删前 Ding 用户 5301）</p>
</blockquote>
<p>执行恢复 SQL：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">UPDATE</span> sys_user <span class="keyword">SET</span> delete_flag<span class="operator">=</span><span class="string">&#x27;0&#x27;</span> <span class="keyword">WHERE</span> user_from<span class="operator">=</span><span class="string">&#x27;Ding&#x27;</span> <span class="keyword">AND</span> delete_flag<span class="operator">=</span><span class="string">&#x27;1&#x27;</span>;</span><br><span class="line"><span class="comment">-- 影响：5301 行（Changed: 5301），全部恢复</span></span><br></pre></td></tr></table></figure>

<h2 id="用户同步→园区下发-完整链路（改动后）"><a href="#用户同步→园区下发-完整链路（改动后）" class="headerlink" title="用户同步→园区下发 完整链路（改动后）"></a>用户同步→园区下发 完整链路（改动后）</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Dataphin API (DataPhinUser department: UUID)</span><br><span class="line">   → handleDingUserSync (admin-service)</span><br><span class="line">      → 写 sys_user</span><br><span class="line">      → 写 sys_user_organization (org_id = 部门 UUID)</span><br><span class="line">      → 写 sys_user_park (org_id = 公司级 org_id, park_id)</span><br><span class="line">         ↑ 需要 findParkByOrgWalkUp 递归 CTE</span><br><span class="line">      → triggerPassUserSync Feign → synSysUserToPassUser (through-service)</span><br><span class="line">         → 写 pass_user (dist_sync_status=PENDING)</span><br><span class="line">   → PassDistSyncer (90s 定时)</span><br><span class="line">      → ROMA → 园区 pass_user</span><br></pre></td></tr></table></figure>

<h3 id="findParkByOrgWalkUp-递归-CTE"><a href="#findParkByOrgWalkUp-递归-CTE" class="headerlink" title="findParkByOrgWalkUp 递归 CTE"></a>findParkByOrgWalkUp 递归 CTE</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">WITH</span> <span class="keyword">RECURSIVE</span> org_chain <span class="keyword">AS</span> (</span><br><span class="line">    <span class="comment">-- 起点：用户部门 UUID</span></span><br><span class="line">    <span class="keyword">SELECT</span> org_id, parent_id, <span class="number">0</span> <span class="keyword">AS</span> depth</span><br><span class="line">    <span class="keyword">FROM</span> sys_organization</span><br><span class="line">    <span class="keyword">WHERE</span> org_id <span class="operator">=</span> #&#123;deptOrgId&#125; <span class="keyword">AND</span> delete_flag <span class="operator">=</span> <span class="string">&#x27;0&#x27;</span></span><br><span class="line">    <span class="keyword">UNION</span> <span class="keyword">ALL</span></span><br><span class="line">    <span class="comment">-- 逐层往上</span></span><br><span class="line">    <span class="keyword">SELECT</span> o.org_id, o.parent_id, c.depth <span class="operator">+</span> <span class="number">1</span></span><br><span class="line">    <span class="keyword">FROM</span> sys_organization o</span><br><span class="line">    <span class="keyword">JOIN</span> org_chain c <span class="keyword">ON</span> o.org_id <span class="operator">=</span> c.parent_id <span class="keyword">AND</span> o.delete_flag <span class="operator">=</span> <span class="string">&#x27;0&#x27;</span></span><br><span class="line">    <span class="keyword">WHERE</span> c.parent_id <span class="keyword">IS</span> <span class="keyword">NOT NULL</span> <span class="keyword">AND</span> c.parent_id <span class="operator">&lt;&gt;</span> <span class="string">&#x27;&#x27;</span> <span class="keyword">AND</span> c.depth <span class="operator">&lt;</span> <span class="number">20</span>  <span class="comment">-- 防环</span></span><br><span class="line">)</span><br><span class="line"><span class="keyword">SELECT</span> po.<span class="operator">*</span></span><br><span class="line"><span class="keyword">FROM</span> sys_park_organization po</span><br><span class="line"><span class="keyword">JOIN</span> org_chain c <span class="keyword">ON</span> po.org_id <span class="operator">=</span> c.org_id</span><br><span class="line"><span class="keyword">WHERE</span> po.delete_flag <span class="operator">=</span> <span class="string">&#x27;0&#x27;</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> c.depth <span class="keyword">ASC</span>   <span class="comment">-- 取最近的命中祖先</span></span><br><span class="line">LIMIT <span class="number">1</span></span><br></pre></td></tr></table></figure>

<h3 id="桥表语义"><a href="#桥表语义" class="headerlink" title="桥表语义"></a>桥表语义</h3><p><code>sys_park_organization</code>（桥表）里配的是 <strong>“公司级 org_id ↔ park_id”</strong> 的映射。”公司”&#x3D; 桥表里配了的那个组织节点，它可以是根、可以是电厂级、也可以是部门级 —— 取决于你在 <code>sys_park_organization</code> 里配了哪一层。</p>
<blockquote>
<p>[!example] 三种配法</p>
<ul>
<li>桥表配”广蓄电厂 → 广蓄园区” → 广蓄电厂及其下面所有人都归广蓄园区</li>
<li>桥表配”南方电网总部 → 某园区” → 整棵树的人都归那个园区</li>
<li>两个都配了 → CTE 取最近的（depth 小的）</li>
</ul>
</blockquote>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><h3 id="排查方法论"><a href="#排查方法论" class="headerlink" title="排查方法论"></a>排查方法论</h3><ul>
<li><strong>从终端现象往回追溯链路</strong>，每一环用真实数据（生产 dump、MCP 查库、API 响应样本）验证，不要只在本地复现。<strong>本地 mock 是最大的盲区</strong></li>
<li>下结论前先读 utils 的实现，不要靠方法名望文生义</li>
<li>排查起点永远先看返回值（affected rows、日志）—— <code>synSysUserToPassUser</code> 返回 335 立刻说明是过滤问题</li>
</ul>
<h3 id="代码规范"><a href="#代码规范" class="headerlink" title="代码规范"></a>代码规范</h3><ul>
<li>依赖外部数据字段的解析方法，字段缺失时应该打 <strong>WARN 日志</strong>而不是静默返回 null——<code>getCompanyIdFromPDeparts</code> 和 <code>getDepartmentAsList</code> 都是静默失败的，导致问题难以被监控发现</li>
<li>数据模型层变更应该有<strong>契约测试</strong>或<strong>schema 校验</strong>，字段下线时让集成测试第一时间失败</li>
</ul>
<h3 id="测试覆盖"><a href="#测试覆盖" class="headerlink" title="测试覆盖"></a>测试覆盖</h3><ul>
<li>跨服务同步链路应该有端到端集成测试，用<strong>接近真实格式的样本数据</strong>（包括 UUID 格式的 department），而不只是构造正确格式的 mock</li>
<li>测试场景要包含”数据正确性”——除了”有没有写”，还要查”写得对不对”</li>
</ul>
<h2 id="待办事项"><a href="#待办事项" class="headerlink" title="待办事项"></a>待办事项</h2><ul>
<li><input disabled="" type="checkbox"> 确认 dept 接口（<code>queryDingDept</code>）返回格式是否能串成组织树，再决定 PDEPARTS 的替代方案</li>
<li><input disabled="" type="checkbox"> 修复 Bug#1（增量更新取消删除标记失效）</li>
<li><input disabled="" type="checkbox"> 修复 Bug#2（园区链路改用 PARENTID）</li>
<li><input disabled="" type="checkbox"> 修复 Bug#3（pass_user 链路同步适配）</li>
<li><input disabled="" type="checkbox"> 解决 Bug#4（部门 vs 公司数据源统一）</li>
<li><input disabled="" type="checkbox"> 重建 <code>sys_park_organization</code> 桥表（新 UUID 映射）</li>
</ul>
<h2 id="关联"><a href="#关联" class="headerlink" title="关联"></a>关联</h2><ul>
<li>[[2026-06-22 工作记录]]</li>
<li>[[钉钉同步园区失败-PDEPARTS字段下线&#x2F;问题分析与修复]]</li>
<li>[[用户同步园区下发完整链路]]</li>
</ul>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>南网</category>
      </categories>
      <tags>
        <tag>集成测试</tag>
        <tag>PDEPARTS下线</tag>
        <tag>数据模型变更</tag>
        <tag>Dataphin</tag>
        <tag>真实格式</tag>
      </tags>
  </entry>
  <entry>
    <title>钉钉同步性能优化（异步化+批量化）+ 头像回填 CDN 解析失败</title>
    <url>/2026/06/25/2026-06-25-perf-avatar-backfill/</url>
    <content><![CDATA[<h2 id="导读"><a href="#导读" class="headerlink" title="导读"></a>导读</h2><blockquote>
<p>钉钉同步两个故事：(1) 15000+ 用户同步时 HTTP 超时——根因是 <code>toUpdateUsers</code> 循环内 3 次 DB 查询每人 &#x3D; 46000 次 SQL round-trip 在一个事务内串行执行，<strong>两步优化</strong>：异步化（HTTP 立即返回”已受理”，后台静默执行）+ 批量化（循环前 IN 批量预加载到 Map，循环内 O(1) 查 Map），计划阶段就识别出 4 个潜在 bug（路径双重前缀、BaseResultMap 错映射、dirty-check 破坏恢复语义、peek 污染原始 DB 状态）。(2) 头像回填接口返回 200 但成功数始终为 0——admin 服务器在内网，DNS 无法解析钉钉 CDN 域名 <code>static-legacy.dingtalk.com</code>，所有用户全部被静默跳过。</p>
</blockquote>
<span id="more"></span>

<div class="audio-player">
  <div class="audio-section">
    <h4>🎧 文章导读</h4>
    <audio controls preload="metadata">
      <source src="/audio/2026-06-25-perf-avatar-backfill/narration.mp3" type="audio/mpeg">
    </audio>
  </div>
  <div class="audio-section">
    <h4>🎵 背景音乐</h4>
    <audio controls loop preload="metadata">
      <source src="/audio/2026-06-25-perf-avatar-backfill/bgm.mp3" type="audio/mpeg">
    </audio>
  </div>
</div>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>钉钉同步功能跑通后，规模放大就出问题：15000+ 用户同步时网关超时。问题的本质不是”代码错了”，而是”算法复杂度在小规模下隐藏了性能问题”——这是典型的”小规模下完全合理、大规模下完全不可用”的设计。</p>
<p>同时新加的头像回填接口”返回 200 但没效果”，表面是逻辑分支问题，深挖发现是网络可达性假设不成立。</p>
<p><img data-src="/images/posts/2026-06-25-perf-avatar-backfill/fig1_001.jpg" alt="异步化时序图"><br><em>图1：异步化时序图</em></p>
<h2 id="Part-1：性能优化"><a href="#Part-1：性能优化" class="headerlink" title="Part 1：性能优化"></a>Part 1：性能优化</h2><h3 id="问题分析"><a href="#问题分析" class="headerlink" title="问题分析"></a>问题分析</h3><p><code>handleDingUserSync</code> 方法中，对每个已存在的钉钉用户，在循环体内依次执行<strong>三次数据库查询</strong>：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 重构前：循环内每人 3 次 DB 查询</span></span><br><span class="line"><span class="keyword">for</span> (UserDTO updateUser : toUpdateUsers) &#123;</span><br><span class="line">    Set&lt;String&gt; existingOrgs = userOrganizationService</span><br><span class="line">            .listByCondition(orgQuery).stream()       <span class="comment">// 1 SQL/user</span></span><br><span class="line">            .map(UserOrganizationDTO::getOrgId)</span><br><span class="line">            .collect(Collectors.toSet());</span><br><span class="line">    <span class="type">ParkOrganizationDTO</span> <span class="variable">newParkOrg</span> <span class="operator">=</span> parkOrganizationService</span><br><span class="line">            .findParkByOrgWalkUp(deptOrgId);           <span class="comment">// 1 SQL/user</span></span><br><span class="line">    List&lt;UserParkDTO&gt; existParks = userParkService</span><br><span class="line">            .listByCondition(parkQuery);                <span class="comment">// 1 SQL/user</span></span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>每人 3 次 SQL × 15282 人 &#x3D; <strong>约 46000 次 SQL round-trip</strong>。每次 10-15ms（连接池等待 + 网络延迟），整个循环要数分钟。</p>
<p>更麻烦的是，这一切发生在一个 <code>@Transactional</code> 事务内。46000 次 SQL 全部串行执行，<strong>事务持有的数据库连接和锁资源持续数分钟</strong>，对数据库的压力不容小觑。</p>
<p>前端 Feign 调用 iot-middle 拉取数据 + 后端 <code>triggerPassUserSync</code> 同步阻塞 through-service，整条链路耗时远超网关的超时阈值。用户触发同步后，网关断开连接，但后台任务仍在跑——结果是<strong>同步看似”失败”了，实际上数据已经部分写入，状态不可控</strong>。</p>
<h3 id="重构思路"><a href="#重构思路" class="headerlink" title="重构思路"></a>重构思路</h3><h4 id="方案-A：只做异步化"><a href="#方案-A：只做异步化" class="headerlink" title="方案 A：只做异步化"></a>方案 A：只做异步化</h4><p>把同步逻辑丢到后台线程，HTTP 请求立即返回。优点是改动极小（加 <code>@Async</code> 即可），前端不再超时；缺点是后台仍然要跑很久，数据库压力不变。</p>
<h4 id="方案-B：异步化-批量化"><a href="#方案-B：异步化-批量化" class="headerlink" title="方案 B：异步化 + 批量化"></a>方案 B：异步化 + 批量化</h4><p>在异步化的基础上，把逐人查询改为批量预加载。优点是从根本上消除 O(N) 的 SQL 往返；缺点是改动面更大。</p>
<p><strong>选择方案 B</strong>：异步化解决用户体验问题，批量化解决数据库压力问题，两者互不依赖、可以分步上线。</p>
<h3 id="重构方案"><a href="#重构方案" class="headerlink" title="重构方案"></a>重构方案</h3><h4 id="Step-1-异步化入口（消除-HTTP-超时）"><a href="#Step-1-异步化入口（消除-HTTP-超时）" class="headerlink" title="Step 1: 异步化入口（消除 HTTP 超时）"></a>Step 1: 异步化入口（消除 HTTP 超时）</h4><p>新建 <code>DingUserSyncTask</code> 组件，用 <code>@Async</code> 注解标记执行方法。但异步化带来新问题：如果用户连续点击两次同步按钮，会并发执行两份同步任务。因此用 <code>AtomicBoolean</code> 实现防重入锁：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Slf4j</span></span><br><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">DingUserSyncTask</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="type">AtomicBoolean</span> <span class="variable">running</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">AtomicBoolean</span>(<span class="literal">false</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">tryStart</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> running.compareAndSet(<span class="literal">false</span>, <span class="literal">true</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Async</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">syncDingUserFromIotMiddle</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// ...省略 iot-middle 拉取、handleDingUserSync、softDelete...</span></span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            running.set(<span class="literal">false</span>);  <span class="comment">// 确保异常也能释放锁</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>tryStart()</code> 用 CAS 操作保证原子性，<code>finally</code> 块确保无论成功还是异常都能释放锁。Controller 端通过 <code>tryStart()</code> 的返回值判断是提交任务还是拒绝请求。</p>
<h4 id="Step-2-批量化查询（消除-SQL-往返放大）"><a href="#Step-2-批量化查询（消除-SQL-往返放大）" class="headerlink" title="Step 2: 批量化查询（消除 SQL 往返放大）"></a>Step 2: 批量化查询（消除 SQL 往返放大）</h4><p>把循环内的逐人查询改为循环外的批量预加载：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 重构后：循环前 3 组批量查询，循环内 0 次 DB 查询</span></span><br><span class="line">Map&lt;String, Set&lt;String&gt;&gt; existingOrgsByUserId = userOrganizationService</span><br><span class="line">        .listActiveByUserIds(updateUserIds).stream()</span><br><span class="line">        .collect(Collectors.groupingBy(UserOrganizationDTO::getUserId,</span><br><span class="line">                Collectors.mapping(UserOrganizationDTO::getOrgId, Collectors.toSet())));</span><br><span class="line">Map&lt;String, List&lt;UserParkDTO&gt;&gt; existingParksByUserId = userParkService</span><br><span class="line">        .listActiveByUserIds(updateUserIds).stream()</span><br><span class="line">        .collect(Collectors.groupingBy(UserParkDTO::getUserId));</span><br><span class="line">Map&lt;String, ParkOrganizationDTO&gt; parkOrgByDeptOrgId = parkOrganizationService</span><br><span class="line">        .findParksByOrgWalkUp(deptOrgIds);</span><br></pre></td></tr></table></figure>

<p>循环内改为直接查 Map，不再碰数据库。新增的三个批量查询方法分别对应 mapper 接口、service 接口和 XML 实现。其中 <code>findParksByOrgWalkUp</code> 需要改造原有的单条 CTE 递归查询，用 <code>IN (...)</code> 替代单个参数，同时保持递归上溯逻辑不变。</p>
<h3 id="评审阶段的关键发现"><a href="#评审阶段的关键发现" class="headerlink" title="评审阶段的关键发现"></a>评审阶段的关键发现</h3><blockquote>
<p>[!danger] 计划阶段发现的 4 个问题<br>这些问题如果在编码后才发现，会导致线上 bug 或需要返工。在计划阶段提前识别并修正，节省了大量时间。</p>
</blockquote>
<h4 id="1-PostMapping-路径双重前缀"><a href="#1-PostMapping-路径双重前缀" class="headerlink" title="1. @PostMapping 路径双重前缀"></a>1. @PostMapping 路径双重前缀</h4><p>计划里误写 <code>/sync/syncDingUserFromIotMiddle</code>，但 <code>SyncController</code> 类上已有 <code>@RequestMapping(&quot;/sync&quot;)</code>，拼出来变成 <code>/sync/sync/...</code>，会导致 404。发现后改回 <code>/syncDingUserFromIotMiddle</code>。</p>
<h4 id="2-UserPark-BaseResultMap-错映射"><a href="#2-UserPark-BaseResultMap-错映射" class="headerlink" title="2. UserPark BaseResultMap 错映射"></a>2. UserPark BaseResultMap 错映射</h4><p><code>UserParkMapper.xml</code> 的 <code>BaseResultMap</code> 把 <code>org_id</code> 和 <code>park_id</code> 都映射到 <code>parkId</code>。如果用 <code>resultMap=&quot;BaseResultMap&quot;</code> 查询批量方法，<code>getOrgId()</code> 会恒为 null，导致园区 dirty-check 失效。加上项目没有配置 <code>mapUnderscoreToCamelCase</code>，裸 <code>resultType</code> 也不行。最终采用显式 AS 别名的方式绕过：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">&quot;listActiveByUserIds&quot;</span> <span class="attr">resultType</span>=<span class="string">&quot;...UserParkDTO&quot;</span>&gt;</span></span><br><span class="line">  SELECT user_id AS userId,</span><br><span class="line">         org_id  AS orgId,</span><br><span class="line">         park_id AS parkId</span><br><span class="line">  FROM sys_user_park</span><br><span class="line">  WHERE user_id IN (...)</span><br><span class="line">    AND delete_flag = &#x27;0&#x27;</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="3-主表-dirty-check-破坏-delete-flag-恢复语义"><a href="#3-主表-dirty-check-破坏-delete-flag-恢复语义" class="headerlink" title="3. 主表 dirty-check 破坏 delete_flag 恢复语义"></a>3. 主表 dirty-check 破坏 delete_flag 恢复语义</h4><p><code>listByCondition</code> 不默认过滤 <code>delete_flag</code>，匹配用户的 DB <code>delete_flag</code> 可能是 <code>&#39;1&#39;</code>（上一轮 softDelete 置删）。当前逻辑中”恢复为正常”写是 through 同步能读到的前提。dirty-check 跳过未变更用户会漏掉只需恢复的账号。<strong>将主表 dirty-check 从首版中移除</strong>。</p>
<h4 id="4-existDingUserMap-的-deleteFlag-被-peek-污染"><a href="#4-existDingUserMap-的-deleteFlag-被-peek-污染" class="headerlink" title="4. existDingUserMap 的 deleteFlag 被 peek 污染"></a>4. existDingUserMap 的 deleteFlag 被 peek 污染</h4><p><code>peek(user -&gt; user.setDeleteFlag(YES))</code> 只改内存，后续 toUpdate 又改回 NO——<strong>原始 DB 状态已丢失</strong>，无法用于 dirty-check 判断。随主表 dirty-check 一起移除。</p>
<p>这四个发现中，第 2 和第 3 个是真正会导致线上 bug 的问题。<strong>如果没有在计划阶段逐行 trace 到 mapper XML，它们很可能在集成测试甚至上线后才暴露</strong>。</p>
<h3 id="影响评估"><a href="#影响评估" class="headerlink" title="影响评估"></a>影响评估</h3><table>
<thead>
<tr>
<th>改动范围</th>
<th>文件数</th>
<th>风险</th>
<th>回归测试</th>
</tr>
</thead>
<tbody><tr>
<td>异步化入口（@Async + 防重入）</td>
<td>3</td>
<td>低</td>
<td>触发同步 → 立即返回 → 二次触发被拒</td>
</tr>
<tr>
<td>批量化查询（mapper&#x2F;service&#x2F;XML）</td>
<td>13</td>
<td>中</td>
<td>同步完成后对比用户数、部门关系、园区关系</td>
</tr>
</tbody></table>
<p>编译已通过（BUILD SUCCESS），但 Task 3 的手动验证待执行：启动 admin → curl 触发同步 → 确认立即返回 → 二次触发被拒 → 日志确认后台完成。</p>
<p><img data-src="/images/posts/2026-06-25-perf-avatar-backfill/fig3_001.jpg" alt="性能对比柱状图"><br><em>图3：性能对比柱状图</em></p>
<h2 id="Part-2：头像回填失败"><a href="#Part-2：头像回填失败" class="headerlink" title="Part 2：头像回填失败"></a>Part 2：头像回填失败</h2><h3 id="问题背景"><a href="#问题背景" class="headerlink" title="问题背景"></a>问题背景</h3><p>南网项目的用户同步流程从钉钉中台（DataPhin）拉取用户信息后，会把基本字段写入 <code>sys_user</code> 表。但头像字段 <code>face_pic</code> 一直留空——原因是早期的同步代码根本没有自动下载头像这一步，头像全靠人工在前端拍照或手动上传。</p>
<p>后来开发了 <code>backfillDingAvatar</code> 接口，意图一次性把存量用户的钉钉头像下载到本地存储（aided 服务的 <code>/fs/hikFacePic/</code> 目录），再把本地路径回填到 <code>sys_user.face_pic</code>。接口调上去<strong>返回 200，日志也没有报错</strong>，但成功计数始终是 0。</p>
<p>数据库现状一目了然：约 15000 名用户中，<code>face_pic</code> 为 NULL 的有 5102 人，为空字符串的有 10000 人（主同步新建时设的默认值），标记为 <code>&quot;2&quot;</code> 的有 180 人（拒绝标记），而真正有正常路径的——<strong>0 人</strong>。</p>
<h3 id="排查思路"><a href="#排查思路" class="headerlink" title="排查思路"></a>排查思路</h3><p>一开始看到”返回 200 但成功数为 0”，直觉是逻辑分支有问题——可能某个条件判断把所有用户都过滤掉了。于是先看 <code>backfillDingAvatar</code> 的主循环逻辑，发现它对每个用户调用 <code>uploadDingAvatar</code> 方法，如果该方法返回空字符串就跳过。那问题就转移到了 <code>uploadDingAvatar</code> 为什么返回空字符串。</p>
<p>顺着 <code>uploadDingAvatar</code> 往下追，第一步是调用 <code>UrlToMultipartFileConverter.convert(dingUser.getAvatar())</code>，把钉钉返回的头像 URL 下载成 <code>MultipartFile</code>。钉钉头像的 URL 格式是 <code>https://static-legacy.dingtalk.com/media/xxx.jpg</code>，这是一个外网 CDN 地址。<strong>而这台 admin 服务器部署在内网，DNS 无法解析这个域名</strong>——<code>convert</code> 方法内部的 HTTP 请求直接抛了 <code>java.net.UnknownHostException</code>。</p>
<p>关键在于异常处理的设计：<code>uploadDingAvatar</code> 用 try-catch 包住了整个流程，catch 块里只打了日志就返回空字符串。由于日志级别或日志量的原因，这个异常信息被淹没了，表面上看起来一切正常。</p>
<h3 id="为什么-face-pic-必须存本地路径"><a href="#为什么-face-pic-必须存本地路径" class="headerlink" title="为什么 face_pic 必须存本地路径"></a>为什么 face_pic 必须存本地路径</h3><p>便捷通行模块（through-service）的 <code>PassDistSyncer</code> 读头像的方式是在配置的 <code>pathPrefix</code>（如 <code>/data/sharestore</code>）后面直接拼接 <code>face_pic</code> 的值：</p>
<ul>
<li>如果 <code>face_pic</code> 是本地路径 <code>/fs/hikFacePic/xxx.jpg</code>，拼出来是 <code>/data/sharestore/fs/hikFacePic/xxx.jpg</code>，这是正确的文件系统路径</li>
<li>如果 <code>face_pic</code> 是钉钉 URL，拼出来就变成了 <code>/data/sharestore/https://static-legacy.dingtalk.com/xxx.jpg</code>——一个不伦不类的坏路径，IOUtils 去读必然失败</li>
</ul>
<p>所以 <code>face_pic</code> 必须是本地路径，这决定了头像<strong>必须先下载再存储</strong>。</p>
<h3 id="修复方案"><a href="#修复方案" class="headerlink" title="修复方案"></a>修复方案</h3><h4 id="方案一：admin-服务器配外网代理"><a href="#方案一：admin-服务器配外网代理" class="headerlink" title="方案一：admin 服务器配外网代理"></a>方案一：admin 服务器配外网代理</h4><p>让 <code>UrlToMultipartFileConverter</code> 的底层 HTTP 客户端走代理服务器下载钉钉头像。这是改动量最小的方案——零代码改动，只需运维在服务器上配置 HTTP 代理环境变量或在 HTTP 客户端中注入代理地址。但需要运维配合，且代理服务器本身需要能访问钉钉 CDN。</p>
<h4 id="方案二：中台提供头像下载接口"><a href="#方案二：中台提供头像下载接口" class="headerlink" title="方案二：中台提供头像下载接口"></a>方案二：中台提供头像下载接口</h4><p>不直接访问钉钉 CDN，而是调用钉钉中台（iot-middle）的接口获取图片字节流。admin 和中台在同一个内网环境，网络互通。这个方案需要中台配合新增一个头像下载接口，但彻底绕开了外网访问的问题。</p>
<h4 id="方案三：暂不自动回填"><a href="#方案三：暂不自动回填" class="headerlink" title="方案三：暂不自动回填"></a>方案三：暂不自动回填</h4><p>回填接口保留代码不动，等网络环境打通后再执行。现有头像继续靠人工上传。这是零改动方案，适合短期内不紧急的场景。</p>
<h3 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h3><p><img data-src="/images/posts/2026-06-25-perf-avatar-backfill/fig2_001.jpg" alt="批量查询优化"><br><em>图2：批量查询优化</em></p>
<h4 id="异常处理的粒度"><a href="#异常处理的粒度" class="headerlink" title="异常处理的粒度"></a>异常处理的粒度</h4><p><code>uploadDingAvatar</code> 把所有异常一网打尽返回空字符串，表面上是”容错”，实际上把可恢复的错误（网络不通，换个方式下载就行）和不可恢复的错误（URL 本身无效）混为一谈。建议：</p>
<ul>
<li>区分 <code>IOException</code>（网络层失败，可重试或降级）和其他异常（逻辑错误，应告警）</li>
<li>在接口返回值中区分”全部成功”、”部分失败”、”全部失败”三种状态</li>
</ul>
<h4 id="网络假设要在设计阶段验证"><a href="#网络假设要在设计阶段验证" class="headerlink" title="网络假设要在设计阶段验证"></a>网络假设要在设计阶段验证</h4><p>涉及外网 URL 下载的功能，<strong>上线前需要确认服务器的网络出口策略</strong>。这次的问题完全可以在设计评审时发现——只要问一句”admin 服务器能访问钉钉 CDN 吗？”</p>
<h4 id="接口语义要诚实"><a href="#接口语义要诚实" class="headerlink" title="接口语义要诚实"></a>接口语义要诚实</h4><p>返回 200 但成功数为 0，从调用方角度看是”成功了但什么都没做”，这比返回 500 更危险，因为它不会触发告警。建议当成功数为 0 时返回非 200 状态码，或至少在响应体中标记 warning 级别的提示。</p>
<h4 id="性能问题的最佳发现时机是计划阶段"><a href="#性能问题的最佳发现时机是计划阶段" class="headerlink" title="性能问题的最佳发现时机是计划阶段"></a>性能问题的最佳发现时机是计划阶段</h4><p>这次优化的核心收获是：<strong>性能问题的最佳发现时机是计划阶段，而不是编码阶段</strong>。在做实现计划时逐行 trace 调用链（controller → service → mapper → XML），不仅帮助理解数据流，还能提前发现数据模型层面的陷阱（比如 BaseResultMap 的错映射）。</p>
<h4 id="通用-mapper-的-OGNL-坑再次出现"><a href="#通用-mapper-的-OGNL-坑再次出现" class="headerlink" title="通用 mapper 的 OGNL 坑再次出现"></a>通用 mapper 的 OGNL 坑再次出现</h4><p>框架通用 mapper 的 XML 用 OGNL 求值实体字段，非空 <code>Date</code> 字段被渲染成 <code>!= &#39;&#39;</code> 会触发解析错误。<strong>任何涉及通用 mapper 的改动，都必须一路看到 XML 才能确认安全</strong>。</p>
<h4 id="批量查询的-IN-子句有长度限制"><a href="#批量查询的-IN-子句有长度限制" class="headerlink" title="批量查询的 IN 子句有长度限制"></a>批量查询的 IN 子句有长度限制</h4><p>如果 <code>updateUserIds</code> 列表超过几千条，某些数据库驱动可能会出问题。目前 15000 人应该在安全范围内，但如果未来规模继续增长，可能需要分片 IN 查询。</p>
<h2 id="关联"><a href="#关联" class="headerlink" title="关联"></a>关联</h2><ul>
<li>[[2026-06-25 工作记录]]</li>
<li>[[钉钉用户同步性能优化&#x2F;重构分析]]</li>
<li>[[钉钉头像回填失败分析&#x2F;问题分析与修复]]</li>
</ul>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>南网</category>
      </categories>
      <tags>
        <tag>性能优化</tag>
        <tag>异步化</tag>
        <tag>批量化查询</tag>
        <tag>CDN解析</tag>
        <tag>N+1</tag>
      </tags>
  </entry>
  <entry>
    <title>PDEPARTS 废弃 + 头像同步修复 + 用户查询回归：递归 CTE 上溯</title>
    <url>/2026/06/23/2026-06-23-pdeparts-deprecated/</url>
    <content><![CDATA[<h2 id="导读"><a href="#导读" class="headerlink" title="导读"></a>导读</h2><blockquote>
<p>6&#x2F;22 暴露的 PDEPARTS 下线问题，今天给出完整修复：废弃所有依赖 PDEPARTS 的解析，改用部门 UUID 沿 <code>sys_organization.parent_id</code> 递归上溯查 <code>sys_park_organization</code> 桥表（MySQL 8 CTE）。一个 commit 改了 8 个文件、+167&#x2F;-33，把昨天识别出的 4 个 Bug 全部修完。同时顺手把头像同步 bug（<code>setFacePic</code> 误写为 <code>setFacePicAuditStatus</code>）修了，并补充 <code>face_pic=&#39;2&#39;</code> 历史脏数据的处理策略。</p>
</blockquote>
<span id="more"></span>

<div class="audio-player">
  <div class="audio-section">
    <h4>🎧 文章导读</h4>
    <audio controls preload="metadata">
      <source src="/audio/2026-06-23-pdeparts-deprecated/narration.mp3" type="audio/mpeg">
    </audio>
  </div>
  <div class="audio-section">
    <h4>🎵 背景音乐</h4>
    <audio controls loop preload="metadata">
      <source src="/audio/2026-06-23-pdeparts-deprecated/bgm.mp3" type="audio/mpeg">
    </audio>
  </div>
</div>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>6&#x2F;22 链路测试报告里挖出来的 4 个 Bug，今天一次性收尾。核心是 PDEPARTS 替代方案：用递归 CTE 沿部门 UUID 上溯查桥表，这是 MySQL 8 才支持的语法（项目里其他 SQL 都很”老派”，没想到会用到 CTE）。</p>
<p><img data-src="/images/posts/2026-06-23-pdeparts-deprecated/fig1_001.jpg" alt="递归 CTE 上溯"><br><em>图1：递归 CTE 上溯</em></p>
<h2 id="一、核心修复：废弃-PDEPARTS，改用部门-UUID-上溯"><a href="#一、核心修复：废弃-PDEPARTS，改用部门-UUID-上溯" class="headerlink" title="一、核心修复：废弃 PDEPARTS，改用部门 UUID 上溯"></a>一、核心修复：废弃 PDEPARTS，改用部门 UUID 上溯</h2><h3 id="方案"><a href="#方案" class="headerlink" title="方案"></a>方案</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">WITH</span> <span class="keyword">RECURSIVE</span> org_chain <span class="keyword">AS</span> (</span><br><span class="line">  <span class="keyword">SELECT</span> org_id, parent_id, <span class="number">0</span> <span class="keyword">AS</span> depth <span class="keyword">FROM</span> sys_organization</span><br><span class="line">  <span class="keyword">WHERE</span> org_id <span class="operator">=</span> #&#123;deptOrgId&#125; <span class="keyword">AND</span> delete_flag<span class="operator">=</span><span class="string">&#x27;0&#x27;</span></span><br><span class="line">  <span class="keyword">UNION</span> <span class="keyword">ALL</span></span><br><span class="line">  <span class="keyword">SELECT</span> o.org_id, o.parent_id, c.depth<span class="operator">+</span><span class="number">1</span> <span class="keyword">FROM</span> sys_organization o</span><br><span class="line">  <span class="keyword">JOIN</span> org_chain c <span class="keyword">ON</span> o.org_id <span class="operator">=</span> c.parent_id <span class="keyword">AND</span> o.delete_flag<span class="operator">=</span><span class="string">&#x27;0&#x27;</span></span><br><span class="line">  <span class="keyword">WHERE</span> c.parent_id <span class="keyword">IS</span> <span class="keyword">NOT NULL</span> <span class="keyword">AND</span> c.parent_id <span class="operator">&lt;&gt;</span> <span class="string">&#x27;&#x27;</span> <span class="keyword">AND</span> c.depth <span class="operator">&lt;</span> <span class="number">20</span></span><br><span class="line">)</span><br><span class="line"><span class="keyword">SELECT</span> po.<span class="operator">*</span> <span class="keyword">FROM</span> sys_park_organization po</span><br><span class="line"><span class="keyword">JOIN</span> org_chain c <span class="keyword">ON</span> po.org_id <span class="operator">=</span> c.org_id</span><br><span class="line"><span class="keyword">WHERE</span> po.delete_flag<span class="operator">=</span><span class="string">&#x27;0&#x27;</span> <span class="keyword">ORDER</span> <span class="keyword">BY</span> c.depth <span class="keyword">ASC</span> LIMIT <span class="number">1</span></span><br></pre></td></tr></table></figure>

<h3 id="改动文件"><a href="#改动文件" class="headerlink" title="改动文件"></a>改动文件</h3><table>
<thead>
<tr>
<th>文件</th>
<th>改动</th>
</tr>
</thead>
<tbody><tr>
<td><code>UserSyncServiceImpl.java</code></td>
<td>ADD&#x2F;UPDATE 分支：<code>getCompanyIdFromPDeparts()</code> → <code>findParkByOrgWalkUp(getDepartment())</code>；部门列表从 PDEPARTS 数组改为单值构造</td>
</tr>
<tr>
<td><code>ParkOrganizationServiceImpl.java</code></td>
<td>新增 <code>findParkByOrgWalkUp</code> 实现</td>
</tr>
<tr>
<td><code>ParkOrganizationMapper.xml</code></td>
<td>新增 CTE 递归 SQL</td>
</tr>
<tr>
<td><code>IParkOrganizationService.java</code></td>
<td>接口定义</td>
</tr>
</tbody></table>
<h3 id="数据依赖"><a href="#数据依赖" class="headerlink" title="数据依赖"></a>数据依赖</h3><p><code>sys_user_organization.org_id</code> 从公司级改为存<strong>用户直接部门 UUID</strong>。桥表 <code>sys_park_organization</code> 需要有公司级 org_id → park_id 映射（广蓄&#x2F;清蓄已补）。</p>
<h3 id="防环机制"><a href="#防环机制" class="headerlink" title="防环机制"></a>防环机制</h3><p><code>AND c.depth &lt; 20</code> 是<strong>防环保险</strong>——理论上 <code>sys_organization.parent_id</code> 应该是有向无环树，但万一数据脏了（人为乱改 parent_id）会无限递归。20 层足够业务穿透（公司级通常 3-5 层）。</p>
<h2 id="二、回归修复：用户查询接口"><a href="#二、回归修复：用户查询接口" class="headerlink" title="二、回归修复：用户查询接口"></a>二、回归修复：用户查询接口</h2><p>PDEPARTS 修复后 <code>sys_user_organization.org_id</code> 语义变更（公司级→部门级），导致两个用户查询接口返回 0 条。</p>
<h3 id="Bug-1：-user-pagingOrgAndPark-→-listByOrgIdsAndParkId"><a href="#Bug-1：-user-pagingOrgAndPark-→-listByOrgIdsAndParkId" class="headerlink" title="Bug 1：/user/pagingOrgAndPark → listByOrgIdsAndParkId"></a>Bug 1：<code>/user/pagingOrgAndPark</code> → <code>listByOrgIdsAndParkId</code></h3><ul>
<li><strong>现象</strong>：带 orgId+parkId 搜用户返回 0 条</li>
<li><strong>根因</strong>：SQL 通过 <code>uo.org_id = po.org_id</code> JOIN 桥表，部门级 ≠ 公司级 → JOIN 失效</li>
<li><strong>修复</strong>（UserMapper.xml）：去掉桥表 JOIN，<code>sys_user_park</code> 直连 <code>sys_user_organization</code></li>
</ul>
<h3 id="Bug-2：-user-page-→-listByConditionExt"><a href="#Bug-2：-user-page-→-listByConditionExt" class="headerlink" title="Bug 2：/user/page → listByConditionExt"></a>Bug 2：<code>/user/page</code> → <code>listByConditionExt</code></h3><ul>
<li><strong>现象</strong>：带 orgId+parkId 搜用户返回 0 条，只搜 parkId 能搜到 138 条</li>
<li><strong>根因</strong>：<code>uo.org_id = #&#123;orgId&#125;</code> 精确匹配部门级 ≠ 公司级</li>
<li><strong>修复</strong>（UserMapperExt.xml）：orgId 过滤改用 <code>urp.org_id</code>（sys_user_park 公司级），urp 子查询加 <code>MIN(org_id)</code></li>
</ul>
<p><img data-src="/images/posts/2026-06-23-pdeparts-deprecated/fig2_001.jpg" alt="组织树修复"><br><em>图2：组织树修复</em></p>
<h2 id="三、组织树修复"><a href="#三、组织树修复" class="headerlink" title="三、组织树修复"></a>三、组织树修复</h2><h3 id="Bug-3：selectCurParkOrgTree-返回空"><a href="#Bug-3：selectCurParkOrgTree-返回空" class="headerlink" title="Bug 3：selectCurParkOrgTree 返回空"></a>Bug 3：<code>selectCurParkOrgTree</code> 返回空</h3><ul>
<li><strong>现象</strong>：广蓄园区组织树返回 <code>[]</code></li>
<li><strong>根因</strong>：递归从 <code>parentId=&quot;&quot;</code> 起步，UUID 组织树根节点 <code>parent_id=NULL</code>，<code>equalsIgnoreCase(null, &quot;&quot;)</code> &#x3D; false → 根进不来</li>
<li><strong>修复</strong>（OrganizationServiceImpl.java）：<code>loadCurParkTree</code> 和 <code>loadOrganizationTree</code> 的匹配条件兼容 null 和 “” 两种根写法</li>
</ul>
<h3 id="额外改进"><a href="#额外改进" class="headerlink" title="额外改进"></a>额外改进</h3><ul>
<li><code>selectOrganizationTree</code> 强制 <code>delete_flag=&#39;0&#39;</code>（原来不过滤）</li>
<li><code>getCompanyList</code> 改为两级树（一级公司 + 二级子节点）</li>
<li><code>saveCompanyBind</code> 去掉 <code>orgIds</code> 非空校验</li>
</ul>
<p><img data-src="/images/posts/2026-06-23-pdeparts-deprecated/fig3_001.jpg" alt="头像同步策略表"><br><em>图3：头像同步策略表</em></p>
<h2 id="四、钉钉头像同步"><a href="#四、钉钉头像同步" class="headerlink" title="四、钉钉头像同步"></a>四、钉钉头像同步</h2><h3 id="问题-1：setFacePic-误写为-setFacePicAuditStatus"><a href="#问题-1：setFacePic-误写为-setFacePicAuditStatus" class="headerlink" title="问题 1：setFacePic 误写为 setFacePicAuditStatus"></a>问题 1：<code>setFacePic</code> 误写为 <code>setFacePicAuditStatus</code></h3><p><code>UserSyncServiceImpl.java</code> 第 129 行 bug：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 错误代码（bug）</span></span><br><span class="line">userDTO.setFacePic(FACE_CHANGE_AUDIT_STATUS_REFUSE);  <span class="comment">// ← 应该是 setFacePicAuditStatus</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 正确代码（修复后）</span></span><br><span class="line">userDTO.setFacePicAuditStatus(FACE_CHANGE_AUDIT_STATUS_REFUSE);</span><br></pre></td></tr></table></figure>

<p>导致所有钉钉同步用户 <code>face_pic = &#39;2&#39;</code>（这是 audit_status 的”拒绝”状态，不是 face_pic 的值）。</p>
<h3 id="问题-2：Dataphin-返回的-avatar-完全没用"><a href="#问题-2：Dataphin-返回的-avatar-完全没用" class="headerlink" title="问题 2：Dataphin 返回的 avatar 完全没用"></a>问题 2：Dataphin 返回的 avatar 完全没用</h3><p><code>PROFILEPHOTOURL</code>（avatar 字段）包含钉钉头像 URL，但同步代码完全没用这个字段。</p>
<h3 id="修复策略"><a href="#修复策略" class="headerlink" title="修复策略"></a>修复策略</h3><table>
<thead>
<tr>
<th>用户类型</th>
<th>face_pic 现状</th>
<th>同步后行为</th>
</tr>
</thead>
<tbody><tr>
<td>新用户</td>
<td>—</td>
<td>下载钉钉头像 → upload aided OSS → <code>/fs/hikFacePic/...</code></td>
</tr>
<tr>
<td>老用户（脏值<code>&quot;2&quot;</code>）</td>
<td><code>&quot;2&quot;</code></td>
<td>有头像→<code>/fs/...</code>；无头像→清空</td>
</tr>
<tr>
<td>老用户（空值）</td>
<td>空&#x2F;NULL</td>
<td><strong>不回填</strong>（避免批量下载几千个头像占事务）</td>
</tr>
<tr>
<td>老用户（正常<code>/fs/...</code>）</td>
<td>正常路径</td>
<td>不覆盖</td>
</tr>
</tbody></table>
<h3 id="关键设计"><a href="#关键设计" class="headerlink" title="关键设计"></a>关键设计</h3><ul>
<li><code>uploadDingAvatar</code> 整段 try&#x2F;catch，<strong>Feign 超时 &#x2F; aided 宕机不影响同步事务</strong></li>
<li><code>shouldBackfillFacePic</code> 第一版只匹配脏值 <code>&quot;2&quot;</code>，空值留给后续离线任务</li>
</ul>
<h3 id="为什么不同步回填空值"><a href="#为什么不同步回填空值" class="headerlink" title="为什么不同步回填空值"></a>为什么不同步回填空值</h3><p>如果对所有空值用户都触发头像下载：</p>
<ul>
<li>15000 用户 × 头像下载（每张 ~2MB）&#x3D; 30GB 网络流量</li>
<li>单个事务内长时间 IO 阻塞</li>
<li>失败重试成本高</li>
</ul>
<p><strong>主动放弃空值回填</strong>——这是和 pass_user 字段同步同样的折中思路：高频场景做，低频场景留给后续离线任务。</p>
<h2 id="五、其他发现（未修复，记录备查）"><a href="#五、其他发现（未修复，记录备查）" class="headerlink" title="五、其他发现（未修复，记录备查）"></a>五、其他发现（未修复，记录备查）</h2><table>
<thead>
<tr>
<th>问题</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>sys_user_organization.id</code> 列长不够</td>
<td>varchar(64) → UUID 时代 36+36&#x3D;72 &gt; 64，生产需 <code>ALTER TABLE sys_user_organization MODIFY COLUMN id varchar(128)</code></td>
</tr>
<tr>
<td>桥表数据</td>
<td>广蓄&#x2F;清蓄需 INSERT UUID 映射（生产 UUID 和 dev 不同，需先查）</td>
</tr>
<tr>
<td>dev OSS 未配置</td>
<td><code>config.enabled: false</code>，需在 <code>bootstrap-dev.yml</code> 加 oss 配置（provider:local, bucketName:Windows 路径）</td>
</tr>
<tr>
<td><code>face_pic=&#39;2&#39;</code> 历史脏数据</td>
<td>~160 条（全软删），5 条未删的全是测试账号</td>
</tr>
</tbody></table>
<h2 id="六、验证结果"><a href="#六、验证结果" class="headerlink" title="六、验证结果"></a>六、验证结果</h2><ul>
<li>mock 端到端测试：admin 侧 sys_user + sys_user_organization + sys_user_park ✅，through 侧 pass_user dist_sync_status&#x3D;PENDING ✅</li>
<li><code>/user/page</code> MCP 验证：带 orgId+parkId 改前 0 条 → 改后 3 条 ✅</li>
<li><code>/user/pagingOrgAndPark</code> MCP 验证：改前 0 条 → 改后 3 条 ✅</li>
<li><code>selectCurParkOrgTree</code> 递归 null 兼容：修复后返回树结构 ✅</li>
<li>Java 8 编译通过 ✅</li>
</ul>
<h2 id="提交概览"><a href="#提交概览" class="headerlink" title="提交概览"></a>提交概览</h2><p><strong>提交</strong>: <code>f78fbe0be</code> (8 files, +167 -33)<br><strong>分支</strong>: <code>618zh</code></p>
<table>
<thead>
<tr>
<th>类别</th>
<th>文件数</th>
<th>行数</th>
</tr>
</thead>
<tbody><tr>
<td>核心修复（PDEPARTS）</td>
<td>4</td>
<td>+98</td>
</tr>
<tr>
<td>回归修复（查询接口）</td>
<td>2</td>
<td>+22</td>
</tr>
<tr>
<td>组织树修复</td>
<td>1</td>
<td>+15</td>
</tr>
<tr>
<td>头像同步修复</td>
<td>1</td>
<td>+27</td>
</tr>
<tr>
<td>其他杂项</td>
<td>0</td>
<td>+5</td>
</tr>
</tbody></table>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><h3 id="递归-CTE-的实战价值"><a href="#递归-CTE-的实战价值" class="headerlink" title="递归 CTE 的实战价值"></a>递归 CTE 的实战价值</h3><p>MySQL 8 的 <code>WITH RECURSIVE</code> 解决了”沿树结构上溯直到命中条件”这类问题，比 Java 里写递归函数简洁 100 倍：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Java 递归方案（伪代码）</span></span><br><span class="line"><span class="type">Organization</span> <span class="variable">org</span> <span class="operator">=</span> findOrgById(deptOrgId);</span><br><span class="line"><span class="keyword">while</span> (org != <span class="literal">null</span> &amp;&amp; org.getParentId() != <span class="literal">null</span>) &#123;</span><br><span class="line">    <span class="type">ParkOrganizationDTO</span> <span class="variable">bind</span> <span class="operator">=</span> findParkByOrg(org.getOrgId());</span><br><span class="line">    <span class="keyword">if</span> (bind != <span class="literal">null</span>) <span class="keyword">return</span> bind;</span><br><span class="line">    org = findOrgById(org.getParentId());</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line"><span class="comment">// 缺点：N 次 SQL，无法在事务里优化</span></span><br></pre></td></tr></table></figure>

<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- SQL CTE 方案</span></span><br><span class="line"><span class="keyword">WITH</span> <span class="keyword">RECURSIVE</span> org_chain <span class="keyword">AS</span> (...)</span><br><span class="line"><span class="keyword">SELECT</span> ... LIMIT <span class="number">1</span>;</span><br><span class="line"><span class="comment">-- 优点：1 次 SQL，DB 优化器能并行</span></span><br></pre></td></tr></table></figure>

<h3 id="BaseResultMap-错映射是-MyBatis-Plus-隐形坑"><a href="#BaseResultMap-错映射是-MyBatis-Plus-隐形坑" class="headerlink" title="BaseResultMap 错映射是 MyBatis-Plus 隐形坑"></a>BaseResultMap 错映射是 MyBatis-Plus 隐形坑</h3><p>第 2 个回归 bug 的根因：<code>UserParkMapper.xml</code> 的 <code>BaseResultMap</code> 把 <code>org_id</code> 和 <code>park_id</code> 都映射到 <code>parkId</code> 字段。<strong>没有配置 <code>mapUnderscoreToCamelCase</code><strong>，裸 <code>resultType</code> 也不行。最终采用</strong>显式 AS 别名</strong>的方式绕过：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">&quot;listActiveByUserIds&quot;</span> <span class="attr">resultType</span>=<span class="string">&quot;...UserParkDTO&quot;</span>&gt;</span></span><br><span class="line">  SELECT user_id AS userId,</span><br><span class="line">         org_id  AS orgId,    <span class="comment">&lt;!-- 显式别名，绕开 BaseResultMap 错映射 --&gt;</span></span><br><span class="line">         park_id AS parkId</span><br><span class="line">  FROM sys_user_park</span><br><span class="line">  WHERE user_id IN (...)</span><br><span class="line">    AND delete_flag = &#x27;0&#x27;</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p><strong>教训</strong>：MyBatis-Plus 的 BaseResultMap 自动生成不是万能的，手写 resultType 时必须显式 AS 别名。</p>
<h3 id="主动放弃低频场景是工程纪律"><a href="#主动放弃低频场景是工程纪律" class="headerlink" title="主动放弃低频场景是工程纪律"></a>主动放弃低频场景是工程纪律</h3><p>头像回填同步只覆盖”脏值 <code>&quot;2&quot;</code>“和”新用户”，<strong>空值留给离线任务</strong>：</p>
<ul>
<li>高频场景（新用户入库）：立即做</li>
<li>低频场景（存量空值回填）：数据迁移期单独跑</li>
</ul>
<p>这与之前 pass_user 字段同步”换部门放弃”的思路一致：<strong>折中方案的纪律是分清高频&#x2F;低频，专注高频场景</strong>。</p>
<h3 id="数据迁移期要列详细清单"><a href="#数据迁移期要列详细清单" class="headerlink" title="数据迁移期要列详细清单"></a>数据迁移期要列详细清单</h3><p>5 个未修复的发现都是”未来生产要处理”的：</p>
<ul>
<li><code>sys_user_organization.id</code> 列长不够——生产环境必须 ALTER</li>
<li>桥表数据（广蓄&#x2F;清蓄 UUID）——需要从生产 dump 提取</li>
<li>dev OSS 配置——本地开发才能验证头像链路</li>
<li><code>face_pic=&#39;2&#39;</code> 历史脏数据——需要在迁移脚本里清理</li>
</ul>
<p>这些不能”先上线再说”——必须先列出清单，每条 owner + 截止日期。</p>
<h2 id="关联"><a href="#关联" class="headerlink" title="关联"></a>关联</h2><ul>
<li>[[2026-06-22 工作记录]]</li>
<li>[[钉钉同步园区失败-PDEPARTS字段下线&#x2F;问题分析与修复]]</li>
<li>[[用户同步园区下发完整链路]]</li>
<li>[[2026-06-22 钉钉同步真实格式链路测试报告]]</li>
</ul>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>南网</category>
      </categories>
      <tags>
        <tag>PDEPARTS废弃</tag>
        <tag>递归CTE</tag>
        <tag>头像同步</tag>
        <tag>回归修复</tag>
        <tag>BaseResultMap</tag>
      </tags>
  </entry>
  <entry>
    <title>Claude Code Skills 与 MCP 深度研究：YouTube 8 个精选视频分析</title>
    <url>/2026/03/09/Claude-Code-Skills%E4%B8%8EMCP%E6%B7%B1%E5%BA%A6%E7%A0%94%E7%A9%B6/</url>
    <content><![CDATA[<p>基于 YouTube 8 个精选视频的深度研究，使用 NotebookLM 自动分析生成。本文将带你理解 Claude Code Skills 的核心概念、与 MCP 的区别、技能构建六步法，以及实战工作流应用。</p>
<span id="more"></span>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>最近在研究 Claude Code 的能力扩展，发现 Skills 和 MCP 是两个经常被混淆的概念。为了彻底搞清楚它们的关系和最佳实践，我用自己搭建的 <strong>YouTube Research Pipeline</strong> 自动搜索并分析了 YouTube 上 8 个高评价视频，最终形成了这篇研究笔记。</p>
<p><img data-src="/images/claude-code-infographic.png" alt="Claude Code Skills 与 MCP 知识图谱"></p>
<h2 id="研究方法：自动化研究流程"><a href="#研究方法：自动化研究流程" class="headerlink" title="研究方法：自动化研究流程"></a>研究方法：自动化研究流程</h2><p>这次研究使用的工具链：</p>
<table>
<thead>
<tr>
<th>工具</th>
<th>用途</th>
</tr>
</thead>
<tbody><tr>
<td><strong>yt-dlp</strong></td>
<td>搜索 YouTube，获取 50 个候选视频</td>
</tr>
<tr>
<td><strong>自动评分系统</strong></td>
<td>根据相关性、参与度、时效性选出 8 个最佳视频</td>
</tr>
<tr>
<td><strong>NotebookLM</strong></td>
<td>添加视频源，自动生成分析报告和知识图谱</td>
</tr>
<tr>
<td><strong>Obsidian Canvas</strong></td>
<td>将知识图谱转换为可视化画布</td>
</tr>
</tbody></table>
<p>整个过程完全自动化：输入主题 → 搜索 → 筛选 → 分析 → 输出。</p>
<h2 id="Skills-vs-MCP：核心区别"><a href="#Skills-vs-MCP：核心区别" class="headerlink" title="Skills vs MCP：核心区别"></a>Skills vs MCP：核心区别</h2><h3 id="一句话总结"><a href="#一句话总结" class="headerlink" title="一句话总结"></a>一句话总结</h3><blockquote>
<p><strong>MCP 连接 Claude 到数据，Skills 教 Claude 如何处理数据</strong></p>
</blockquote>
<h3 id="详细对比"><a href="#详细对比" class="headerlink" title="详细对比"></a>详细对比</h3><table>
<thead>
<tr>
<th>特性</th>
<th>MCP (Model Context Protocol)</th>
<th>Skills</th>
</tr>
</thead>
<tbody><tr>
<td><strong>本质</strong></td>
<td>连接器&#x2F;桥梁</td>
<td>专家系统&#x2F;SOP</td>
</tr>
<tr>
<td><strong>用途</strong></td>
<td>访问 GitHub、数据库、Notion 等外部系统</td>
<td>定义工作流、格式、最佳实践</td>
</tr>
<tr>
<td><strong>Token 消耗</strong></td>
<td>可能较高（拉取大量数据）</td>
<td>低（30-50 tokens 初始）</td>
</tr>
<tr>
<td><strong>可移植性</strong></td>
<td>需要服务器部署</td>
<td>纯 Markdown 文件，Git 管理</td>
</tr>
</tbody></table>
<h3 id="黄金组合"><a href="#黄金组合" class="headerlink" title="黄金组合"></a>黄金组合</h3><p>两者不是竞争关系，而是互补：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">MCP Server → 访问数据库</span><br><span class="line">    ↓</span><br><span class="line">Skill → 教 Claude 团队的查询优化模式</span><br><span class="line">    ↓</span><br><span class="line">输出 → 符合规范的结果</span><br></pre></td></tr></table></figure>

<p><strong>MCP 提供原始数据连接，Skill 提供 intelligent guardrails 处理数据。</strong></p>
<h2 id="Skills-的-5-大核心要点"><a href="#Skills-的-5-大核心要点" class="headerlink" title="Skills 的 5 大核心要点"></a>Skills 的 5 大核心要点</h2><h3 id="1-Skills-AI-的-SOP"><a href="#1-Skills-AI-的-SOP" class="headerlink" title="1. Skills &#x3D; AI 的 SOP"></a>1. Skills &#x3D; AI 的 SOP</h3><p>Skills 是可复用的专业知识包，确保每次执行的一致性。适合：</p>
<ul>
<li>重复性任务（格式化、分析、写作风格）</li>
<li>需要遵循特定规范的工作</li>
<li>团队协作中的标准化输出</li>
</ul>
<h3 id="2-渐进式加载机制"><a href="#2-渐进式加载机制" class="headerlink" title="2. 渐进式加载机制"></a>2. 渐进式加载机制</h3><p>这是 Skills 最精妙的设计：</p>
<table>
<thead>
<tr>
<th>阶段</th>
<th>加载内容</th>
<th>Token 消耗</th>
</tr>
</thead>
<tbody><tr>
<td><strong>初始索引</strong></td>
<td>仅 name + description</td>
<td>30-50 tokens</td>
</tr>
<tr>
<td><strong>按需展开</strong></td>
<td>用户请求匹配时加载完整内容</td>
<td>按需</td>
</tr>
<tr>
<td><strong>好处</strong></td>
<td>防止 context rot，节省成本</td>
<td>-</td>
</tr>
</tbody></table>
<h3 id="3-确定性脚本能力"><a href="#3-确定性脚本能力" class="headerlink" title="3. 确定性脚本能力"></a>3. 确定性脚本能力</h3><p>Skills 可以嵌入 Python&#x2F;Bash 脚本，避免 LLM 的数学幻觉：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 示例：用脚本计算数据，而不是让 LLM 猜测</span></span><br><span class="line">python3 scripts/calculate_metrics.py data.csv</span><br></pre></td></tr></table></figure>

<p>数据分析任务用脚本计算，更可靠、更精确。</p>
<h3 id="4-高度可组合"><a href="#4-高度可组合" class="headerlink" title="4. 高度可组合"></a>4. 高度可组合</h3><p>Skills + MCP + Sub-agents 可以协同工作：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌─────────────┐</span><br><span class="line">│  MCP 获取   │ → 访问 Notion/GitHub/数据库</span><br><span class="line">└─────────────┘</span><br><span class="line">      ↓</span><br><span class="line">┌─────────────┐</span><br><span class="line">│ Skill 处理  │ → 应用团队规范和最佳实践</span><br><span class="line">└─────────────┘</span><br><span class="line">      ↓</span><br><span class="line">┌─────────────┐</span><br><span class="line">│ Sub-agent   │ → 并行执行多个独立任务</span><br><span class="line">└─────────────┘</span><br></pre></td></tr></table></figure>

<h3 id="5-可移植可共享"><a href="#5-可移植可共享" class="headerlink" title="5. 可移植可共享"></a>5. 可移植可共享</h3><ul>
<li>纯 Markdown 文件，Git 版本控制</li>
<li>组织级标准化</li>
<li>可打包分享或出售</li>
</ul>
<h2 id="技能构建六步法"><a href="#技能构建六步法" class="headerlink" title="技能构建六步法"></a>技能构建六步法</h2><p>根据 Anthropic 官方视频和实践经验，构建一个高质量 Skill 需要六个步骤：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">1. 命名与触发词 → 2. 明确输出目标 → 3. 编写 SOP 流程</span><br><span class="line">        ↑                                      ↓</span><br><span class="line">6. 测试迭代优化 ← 5. 设定防御规则 ← 4. 关联参考文件</span><br></pre></td></tr></table></figure>

<h3 id="详细说明"><a href="#详细说明" class="headerlink" title="详细说明"></a>详细说明</h3><table>
<thead>
<tr>
<th>步骤</th>
<th>内容</th>
<th>要点</th>
</tr>
</thead>
<tbody><tr>
<td><strong>1. 命名与触发词</strong></td>
<td>定义 skill 名称和 description</td>
<td>description 决定触发概率，要具体</td>
</tr>
<tr>
<td><strong>2. 明确输出目标</strong></td>
<td>定义期望的输出格式</td>
<td>提供示例输出，让 AI 有参照</td>
</tr>
<tr>
<td><strong>3. 编写 SOP 流程</strong></td>
<td>逐步操作说明</td>
<td>用祈使句，解释 why 而不只是 what</td>
</tr>
<tr>
<td><strong>4. 关联参考文件</strong></td>
<td>引用详细文档</td>
<td>大文件放 references&#x2F; 目录</td>
</tr>
<tr>
<td><strong>5. 设定防御规则</strong></td>
<td>边界和错误处理</td>
<td>用强框架：never&#x2F;always</td>
</tr>
<tr>
<td><strong>6. 测试迭代优化</strong></td>
<td>运行、观察、改进</td>
<td>永远不要期望第一次就完美</td>
</tr>
</tbody></table>
<h2 id="最佳实践"><a href="#最佳实践" class="headerlink" title="最佳实践"></a>最佳实践</h2><h3 id="✅-应该做"><a href="#✅-应该做" class="headerlink" title="✅ 应该做"></a>✅ 应该做</h3><ul>
<li><strong>保持精简</strong>：<code>skill.md</code> 控制在 500 行以内，详情放 reference 文件</li>
<li><strong>用脚本做数学</strong>：不依赖 LLM 计算数值指标</li>
<li><strong>硬编码静态信息</strong>：List IDs、URLs 等直接写入，减少网络搜索</li>
<li><strong>让 AI 自己写</strong>：使用 skill creator 对话式创建</li>
<li><strong>迭代优化</strong>：观察 → 反馈 → 重写</li>
</ul>
<h3 id="❌-应该避免"><a href="#❌-应该避免" class="headerlink" title="❌ 应该避免"></a>❌ 应该避免</h3><ul>
<li><strong>Context rot</strong>：一次性提供过多上下文，导致信息混乱</li>
<li><strong>期望完美</strong>：第一次运行就需要迭代调整</li>
<li><strong>频繁网络搜索</strong>：应本地化文档到 references&#x2F;</li>
</ul>
<h2 id="高级架构组件"><a href="#高级架构组件" class="headerlink" title="高级架构组件"></a>高级架构组件</h2><p>除了 Skills 和 MCP，Claude Code 还有几个重要组件：</p>
<table>
<thead>
<tr>
<th>组件</th>
<th>功能</th>
</tr>
</thead>
<tbody><tr>
<td><strong>MCP</strong></td>
<td>连接外部数据源（Notion, GitHub, 数据库等）</td>
</tr>
<tr>
<td><strong>Sub-agents</strong></td>
<td>独立上下文窗口、专项任务执行、多代理协作</td>
</tr>
<tr>
<td><strong>Hooks</strong></td>
<td>自动化触发脚本（如保存后自动格式化）</td>
</tr>
<tr>
<td><strong>Git 集成</strong></td>
<td>版本控制与代码快照恢复（<code>/rewind</code> 命令）</td>
</tr>
</tbody></table>
<h2 id="实战工作流示例"><a href="#实战工作流示例" class="headerlink" title="实战工作流示例"></a>实战工作流示例</h2><h3 id="研究场景"><a href="#研究场景" class="headerlink" title="研究场景"></a>研究场景</h3><p>结合 NotebookLM 进行长视频&#x2F;文档分析：</p>
<ol>
<li>搜索 YouTube 视频作为数据源</li>
<li>NotebookLM 自动提取关键信息</li>
<li>生成 podcast、报告、思维导图</li>
</ol>
<h3 id="创意场景"><a href="#创意场景" class="headerlink" title="创意场景"></a>创意场景</h3><ul>
<li>品牌化信息图表生成</li>
<li>Twitter 内容转 Newsletter</li>
<li>多格式内容自动转换</li>
</ul>
<h3 id="管理场景"><a href="#管理场景" class="headerlink" title="管理场景"></a>管理场景</h3><ul>
<li>早上晨间计划</li>
<li>项目脉搏检查 (Pulse Check)</li>
<li>自动化状态报告</li>
</ul>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><ol>
<li><strong>Skills 是 SOP，MCP 是桥梁</strong> - 不要混淆两者的定位</li>
<li><strong>渐进式加载节省成本</strong> - 30-50 tokens 的初始索引很关键</li>
<li><strong>脚本 &gt; LLM 计算</strong> - 涉及数值计算时用确定性脚本</li>
<li><strong>迭代是必须的</strong> - 没有第一次就完美的 Skill</li>
<li><strong>自动化研究流程</strong> - YouTube + NotebookLM 的组合非常强大</li>
</ol>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>这次研究让我对 Claude Code 的能力扩展有了清晰的认识。Skills 和 MCP 各有分工，组合使用才能发挥最大价值。如果你也在使用 Claude Code，建议从简单的 Skill 开始，逐步迭代优化。</p>
<p>完整的研究资料（知识图谱 Canvas 文件、原始 JSON）已保存在我的 Obsidian 知识库中，欢迎交流讨论。</p>
<hr>
<h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><h3 id="精选-YouTube-视频"><a href="#精选-YouTube-视频" class="headerlink" title="精选 YouTube 视频"></a>精选 YouTube 视频</h3><ol>
<li><strong>Don’t Build Agents, Build Skills Instead</strong> - AI Engineer (Anthropic 嘉宾) - 837K views</li>
<li><strong>How I use Claude Code</strong> - Meta Staff Engineer - 241K views</li>
<li><strong>Every Claude Code Concept Explained</strong> - Simon Scrapes - 174K views</li>
<li><strong>Claude Skills Built Me an AI Agent Army</strong> - Greg Isenberg - 175K views</li>
<li><strong>Claude Code just Built me an AI Agent Team</strong> - Grace Leung - 164K views</li>
<li><strong>Claude Agent Skills Explained</strong> - Anthropic (官方) - 144K views</li>
<li><strong>Claude Code + NotebookLM &#x3D; CHEAT CODE</strong> - Chase AI - 96K views</li>
<li><strong>Master 95% of Claude Code Skills in 28 Minutes</strong> - Nate Herk - 81K views</li>
</ol>
<h3 id="相关链接"><a href="#相关链接" class="headerlink" title="相关链接"></a>相关链接</h3><ul>
<li><a href="https://docs.anthropic.com/claude-code/skills">Claude Skills 官方文档</a></li>
<li><a href="https://modelcontextprotocol.io/">MCP 协议规范</a></li>
<li><a href="https://notebooklm.google.com/">NotebookLM</a></li>
</ul>
]]></content>
      <categories>
        <category>AI经验</category>
      </categories>
      <tags>
        <tag>Claude</tag>
        <tag>Skills</tag>
        <tag>MCP</tag>
        <tag>NotebookLM</tag>
        <tag>AI 开发</tag>
      </tags>
  </entry>
  <entry>
    <title>AI 编程智能体配置实战：如何为每个角色选择最合适的模型</title>
    <url>/2026/03/30/AI%E7%BC%96%E7%A8%8B%E6%99%BA%E8%83%BD%E4%BD%93%E9%85%8D%E7%BD%AE%E5%AE%9E%E6%88%98/</url>
    <content><![CDATA[<p>用 AI 编程工具写代码不难，但让多个 AI 智能体像一支真正的工程团队一样协作——这需要精心配置。本文分享我如何基于 OhMyOpenCode（omo）为 Claude Code 的每个智能体角色选择最合适的国产模型，以及背后的选型逻辑。</p>
<span id="more"></span>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>我日常使用 Claude Code（通过 Anthropic 代理接入国产大模型）进行开发，配合 OhMyOpenCode 插件来管理智能体（Agent）的模型分配。这套配置已经稳定运行了几个月，覆盖了从代码探索、架构咨询到前端实现、文档写作的全流程。</p>
<p>核心思路很简单：<strong>不同任务需要不同的”思维方式”，而不同模型有不同的擅长领域。把对的模型放在对的角色上，整个系统的效率和质量会有质的飞跃。</strong></p>
<p><img data-src="/images/posts/AI%E7%BC%96%E7%A8%8B%E6%99%BA%E8%83%BD%E4%BD%93%E9%85%8D%E7%BD%AE%E5%AE%9E%E6%88%98/fig1.png" alt="智能体协作架构图"><br><em>图1：智能体协作架构总览</em></p>
<h2 id="我的模型阵容"><a href="#我的模型阵容" class="headerlink" title="我的模型阵容"></a>我的模型阵容</h2><p>先交代一下我用的模型全家桶，全部通过 Anthropic 兼容协议接入：</p>
<table>
<thead>
<tr>
<th>模型</th>
<th>厂商</th>
<th>核心特点</th>
</tr>
</thead>
<tbody><tr>
<td><strong>GLM-5.1</strong></td>
<td>智谱 AI</td>
<td>综合能力强，逻辑推理稳健，中文理解出色</td>
</tr>
<tr>
<td><strong>GLM-5-turbo</strong></td>
<td>智谱 AI</td>
<td>GLM-5.1 的快速版本，响应极快</td>
</tr>
<tr>
<td><strong>Qwen3.5-plus</strong></td>
<td>阿里巴巴</td>
<td>代码生成质量高，实现细节把控好</td>
</tr>
<tr>
<td><strong>MiniMax-M2.7</strong></td>
<td>MiniMax</td>
<td>性价比极高，响应速度快，适合轻量任务</td>
</tr>
<tr>
<td><strong>Kimi-k2.5</strong></td>
<td>Moonshot</td>
<td>长上下文理解优秀，创意表达和视觉理解强</td>
</tr>
<tr>
<td><strong>GPT-5.4</strong></td>
<td>OpenAI</td>
<td>推理天花板，复杂问题处理能力最强（通过国际通道）</td>
</tr>
</tbody></table>
<h2 id="智能体配置详解"><a href="#智能体配置详解" class="headerlink" title="智能体配置详解"></a>智能体配置详解</h2><h3 id="1-Sisyphus（主编排者）→-GLM-5-1"><a href="#1-Sisyphus（主编排者）→-GLM-5-1" class="headerlink" title="1. Sisyphus（主编排者）→ GLM-5.1"></a>1. Sisyphus（主编排者）→ GLM-5.1</h3><p>Sisyphus 是整个系统的”大脑”，负责接收用户请求、拆解任务、分配给子智能体、汇总结果。它需要：</p>
<ul>
<li><strong>全局视野</strong>：理解整个任务的上下文和依赖关系</li>
<li><strong>判断力</strong>：决定哪些任务可以并行、哪些需要串行</li>
<li><strong>中文沟通</strong>：与用户用中文交互，需要优秀的中文理解</li>
</ul>
<p><strong>为什么选 GLM-5.1</strong>：智谱的 GLM-5.1 在中文理解和复杂推理上表现稳定，作为主编排者需要的是”不犯大错”而不是”偶尔惊艳”。GLM-5.1 的稳健性让它在编排场景下值得信赖。</p>
<h3 id="2-Prometheus（实现执行者）→-Qwen3-5-plus"><a href="#2-Prometheus（实现执行者）→-Qwen3-5-plus" class="headerlink" title="2. Prometheus（实现执行者）→ Qwen3.5-plus"></a>2. Prometheus（实现执行者）→ Qwen3.5-plus</h3><p>Prometheus 负责将方案转化为代码。它需要：</p>
<ul>
<li><strong>代码质量</strong>：生成的代码要能直接用，不是伪代码</li>
<li><strong>细节把控</strong>：变量命名、边界处理、异常处理都要到位</li>
<li><strong>框架理解</strong>：熟悉 Spring Boot、MyBatis、React 等主流框架</li>
</ul>
<p><strong>为什么选 Qwen3.5-plus</strong>：阿里的 Qwen 系列在代码生成上一直是国产模型的第一梯队。Qwen3.5-plus 在代码补全、重构、Bug 修复等场景下表现出色，尤其是 Java 和 TypeScript 生态的理解深度。</p>
<h3 id="3-Oracle（架构顾问）→-GLM-5-1"><a href="#3-Oracle（架构顾问）→-GLM-5-1" class="headerlink" title="3. Oracle（架构顾问）→ GLM-5.1"></a>3. Oracle（架构顾问）→ GLM-5.1</h3><p>Oracle 是只读的高质量顾问，在遇到架构决策、复杂 Bug、安全问题时才被咨询。它需要：</p>
<ul>
<li><strong>深度推理</strong>：分析复杂系统间的 tradeoff</li>
<li><strong>表达清晰</strong>：把复杂问题解释得通俗易懂</li>
<li><strong>不输出代码</strong>：只给建议，不动手</li>
</ul>
<p><strong>为什么选 GLM-5.1</strong>：Oracle 的核心能力是”想清楚”而不是”写得快”。GLM-5.1 的推理链路清晰，回答结构化程度高，适合做顾问角色。与 Sisyphus 使用同一模型也保证了思维一致性。</p>
<h3 id="4-Hephaestus（高质量实现）→-GPT-5-4"><a href="#4-Hephaestus（高质量实现）→-GPT-5-4" class="headerlink" title="4. Hephaestus（高质量实现）→ GPT-5.4"></a>4. Hephaestus（高质量实现）→ GPT-5.4</h3><p>Hephaestus 是”精工细作”的实现者，配置为 high variant，只在需要高质量输出时才启用。它需要：</p>
<ul>
<li><strong>顶级代码质量</strong>：架构清晰、设计模式正确、测试完备</li>
<li><strong>复杂问题解决</strong>：处理多系统交互、性能优化等高难度任务</li>
</ul>
<p><strong>为什么选 GPT-5.4</strong>：在推理天花板这个维度上，GPT-5.4 仍然是目前最强的。对于 Hephaestus 这种”压箱底”的角色，用最强的模型确保关键时刻不掉链子。虽然成本更高，但只在必要时调用。</p>
<p><img data-src="/images/posts/AI%E7%BC%96%E7%A8%8B%E6%99%BA%E8%83%BD%E4%BD%93%E9%85%8D%E7%BD%AE%E5%AE%9E%E6%88%98/fig2.png" alt="模型能力象限图"><br><em>图2：各模型在推理深度、代码质量、响应速度、性价比四个维度的定位</em></p>
<h3 id="5-Explore（代码探索）→-MiniMax-M2-7"><a href="#5-Explore（代码探索）→-MiniMax-M2-7" class="headerlink" title="5. Explore（代码探索）→ MiniMax-M2.7"></a>5. Explore（代码探索）→ MiniMax-M2.7</h3><p>Explore 负责在代码库中搜索模式、理解代码结构。它的工作模式是”大量并发搜索 + 快速总结”。它需要：</p>
<ul>
<li><strong>快速响应</strong>：经常被并行调用 2-5 个实例</li>
<li><strong>足够准确</strong>：不需要完美，但不能遗漏关键信息</li>
<li><strong>低成本</strong>：调用频率最高</li>
</ul>
<p><strong>为什么选 MiniMax-M2.7</strong>：Explore 是整个系统里调用频率最高的智能体，可能一个任务就要并行启动 3-5 个。MiniMax-M2.7 的响应速度和性价比让它成为这种”量大队”角色的最佳选择。</p>
<h3 id="6-Librarian（外部参考搜索）→-MiniMax-M2-7"><a href="#6-Librarian（外部参考搜索）→-MiniMax-M2-7" class="headerlink" title="6. Librarian（外部参考搜索）→ MiniMax-M2.7"></a>6. Librarian（外部参考搜索）→ MiniMax-M2.7</h3><p>Librarian 负责搜索外部文档、查找开源实现示例、检索 API 文档。与 Explore 类似：</p>
<ul>
<li><strong>高并发</strong>：经常与 Explore 同时启动</li>
<li><strong>搜索导向</strong>：核心能力是”找到”而不是”深度分析”</li>
</ul>
<p><strong>为什么选 MiniMax-M2.7</strong>：与 Explore 同理，Librarian 也是高频调用的搜索型智能体，性价比和速度优先。</p>
<h3 id="7-Metis（预规划分析）→-GLM-5-turbo"><a href="#7-Metis（预规划分析）→-GLM-5-turbo" class="headerlink" title="7. Metis（预规划分析）→ GLM-5-turbo"></a>7. Metis（预规划分析）→ GLM-5-turbo</h3><p>Metis 在复杂任务开始前分析需求，识别隐藏意图和模糊点。它需要：</p>
<ul>
<li><strong>快速响应</strong>：任务是”想清楚再动手”，不能等太久</li>
<li><strong>分析能力</strong>：识别需求中的矛盾、遗漏和风险</li>
<li><strong>足够准确</strong>：预规划不需要 100% 完美，够用就行</li>
</ul>
<p><strong>为什么选 GLM-5-turbo</strong>：Metis 的工作是”快速扫描”而不是”深度分析”。GLM-5-turbo 的快速响应特性让预规划不会成为瓶颈，同时保留了足够的分析能力。</p>
<h3 id="8-Momus（计划审查）→-GPT-5-4"><a href="#8-Momus（计划审查）→-GPT-5-4" class="headerlink" title="8. Momus（计划审查）→ GPT-5.4"></a>8. Momus（计划审查）→ GPT-5.4</h3><p>Momus 是”挑剔的审查官”，负责评审工作计划的质量、完整性和可执行性。它需要：</p>
<ul>
<li><strong>高标准</strong>：能发现计划中的模糊点、遗漏和风险</li>
<li><strong>逻辑严密</strong>：审查需要滴水不漏</li>
<li><strong>不怕得罪人</strong>：直说问题，不糊弄</li>
</ul>
<p><strong>为什么选 GPT-5.4</strong>：审查需要的是”吹毛求疵”的能力，GPT-5.4 在发现逻辑漏洞和隐含假设方面表现最强。用最强的模型做审查，确保每个计划在执行前都经得起推敲。</p>
<h3 id="9-Multimodal-Looker（视觉理解）→-Kimi-k2-5"><a href="#9-Multimodal-Looker（视觉理解）→-Kimi-k2-5" class="headerlink" title="9. Multimodal Looker（视觉理解）→ Kimi-k2.5"></a>9. Multimodal Looker（视觉理解）→ Kimi-k2.5</h3><p>负责分析图片、截图、UI 设计稿等视觉内容。它需要：</p>
<ul>
<li><strong>视觉理解</strong>：准确识别图片中的文字、布局、组件</li>
<li><strong>设计感知</strong>：理解 UI 设计意图</li>
<li><strong>中文 OCR</strong>：识别中文截图中的代码和文字</li>
</ul>
<p><strong>为什么选 Kimi-k2.5</strong>：Kimi 在多模态理解上，尤其是中文场景下的视觉理解表现突出。对中文截图、设计稿的识别准确率高。</p>
<h2 id="任务类别配置"><a href="#任务类别配置" class="headerlink" title="任务类别配置"></a>任务类别配置</h2><p>除了固定角色的智能体，OhMyOpenCode 还支持按任务类别分配模型。我按”任务复杂度 × 任务领域”来分配：</p>
<table>
<thead>
<tr>
<th>类别</th>
<th>模型</th>
<th>选型理由</th>
</tr>
</thead>
<tbody><tr>
<td><strong>deep</strong>（深度实现）</td>
<td>GLM-5.1</td>
<td>需要综合推理能力，稳健可靠</td>
</tr>
<tr>
<td><strong>ultrabrain</strong>（超脑）</td>
<td>GLM-5.1</td>
<td>逻辑推理密集型任务</td>
</tr>
<tr>
<td><strong>quick</strong>（快速任务）</td>
<td>MiniMax-M2.7</td>
<td>单文件修改，速度优先</td>
</tr>
<tr>
<td><strong>unspecified-low</strong></td>
<td>MiniMax-M2.7</td>
<td>低复杂度通用</td>
</tr>
<tr>
<td><strong>unspecified-high</strong></td>
<td>GLM-5.1</td>
<td>高复杂度通用</td>
</tr>
<tr>
<td><strong>visual-engineering</strong>（前端）</td>
<td>Kimi-k2.5</td>
<td>视觉理解 + 创意设计</td>
</tr>
<tr>
<td><strong>artistry</strong>（创意方案）</td>
<td>Kimi-k2.5</td>
<td>非常规问题需要创意思维</td>
</tr>
<tr>
<td><strong>writing</strong>（文档写作）</td>
<td>Kimi-k2.5</td>
<td>长文输出，表达流畅</td>
</tr>
</tbody></table>
<p><img data-src="/images/posts/AI%E7%BC%96%E7%A8%8B%E6%99%BA%E8%83%BD%E4%BD%93%E9%85%8D%E7%BD%AE%E5%AE%9E%E6%88%98/fig3.png" alt="任务分配决策流程图"><br><em>图3：任务类别到模型的分配决策流程</em></p>
<p><strong>分配逻辑总结</strong>：</p>
<ul>
<li><strong>GLM-5.1</strong>：逻辑推理、架构决策、深度实现 → “稳重派”</li>
<li><strong>MiniMax-M2.7</strong>：高频搜索、快速任务、低复杂度 → “效率派”</li>
<li><strong>Kimi-k2.5</strong>：前端视觉、创意写作、多模态 → “创意派”</li>
<li><strong>Qwen3.5-plus</strong>：代码生成、Bug 修复、重构 → “实干派”</li>
<li><strong>GPT-5.4</strong>：计划审查、高质量实现 → “天花板”</li>
</ul>
<h2 id="完整配置文件"><a href="#完整配置文件" class="headerlink" title="完整配置文件"></a>完整配置文件</h2><p>以下是 <code>oh-my-opencode.json</code> 的完整配置：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;agents&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;sisyphus&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;model&quot;</span><span class="punctuation">:</span> <span class="string">&quot;zhipuai-coding-plan/glm-5.1&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;prometheus&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;model&quot;</span><span class="punctuation">:</span> <span class="string">&quot;alibaba-coding-plan-cn/qwen3.5-plus&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;atlas&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;model&quot;</span><span class="punctuation">:</span> <span class="string">&quot;minimax-cn-coding-plan/MiniMax-M2.7&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;explore&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;model&quot;</span><span class="punctuation">:</span> <span class="string">&quot;minimax-cn-coding-plan/MiniMax-M2.7&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;hephaestus&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;model&quot;</span><span class="punctuation">:</span> <span class="string">&quot;openai/gpt-5.4&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;variant&quot;</span><span class="punctuation">:</span> <span class="string">&quot;high&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;librarian&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;model&quot;</span><span class="punctuation">:</span> <span class="string">&quot;minimax-cn-coding-plan/MiniMax-M2.7&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;metis&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;model&quot;</span><span class="punctuation">:</span> <span class="string">&quot;zhipuai-coding-plan/glm-5-turbo&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;momus&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;model&quot;</span><span class="punctuation">:</span> <span class="string">&quot;openai/gpt-5.4&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;multimodal-looker&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;model&quot;</span><span class="punctuation">:</span> <span class="string">&quot;alibaba-coding-plan-cn/kimi-k2.5&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;oracle&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;model&quot;</span><span class="punctuation">:</span> <span class="string">&quot;zhipuai-coding-plan/glm-5.1&quot;</span> <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;categories&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;artistry&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;model&quot;</span><span class="punctuation">:</span> <span class="string">&quot;alibaba-coding-plan-cn/kimi-k2.5&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;deep&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;model&quot;</span><span class="punctuation">:</span> <span class="string">&quot;zhipuai-coding-plan/glm-5.1&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;quick&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;model&quot;</span><span class="punctuation">:</span> <span class="string">&quot;minimax-cn-coding-plan/MiniMax-M2.7&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;ultrabrain&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;model&quot;</span><span class="punctuation">:</span> <span class="string">&quot;zhipuai-coding-plan/glm-5.1&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;unspecified-high&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;model&quot;</span><span class="punctuation">:</span> <span class="string">&quot;zhipuai-coding-plan/glm-5.1&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;unspecified-low&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;model&quot;</span><span class="punctuation">:</span> <span class="string">&quot;minimax-cn-coding-plan/MiniMax-M2.7&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;visual-engineering&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;model&quot;</span><span class="punctuation">:</span> <span class="string">&quot;alibaba-coding-plan-cn/kimi-k2.5&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;writing&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;model&quot;</span><span class="punctuation">:</span> <span class="string">&quot;alibaba-coding-plan-cn/kimi-k2.5&quot;</span> <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<h2 id="附加配置：插件与-MCP"><a href="#附加配置：插件与-MCP" class="headerlink" title="附加配置：插件与 MCP"></a>附加配置：插件与 MCP</h2><p>除了模型分配，我还配置了一些插件来增强能力：</p>
<table>
<thead>
<tr>
<th>插件</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Superpowers</strong></td>
<td>提供 TDD、调试、计划编写等工程化工作流</td>
</tr>
<tr>
<td><strong>Claude HUD</strong></td>
<td>终端状态栏，实时显示智能体活动</td>
</tr>
<tr>
<td><strong>JDTLS-LSP</strong></td>
<td>Java 语言服务器，精准的代码智能</td>
</tr>
<tr>
<td><strong>Skill Creator</strong></td>
<td>自定义技能创建器</td>
</tr>
<tr>
<td><strong>Minimax Skills</strong></td>
<td>MiniMax 图片生成、Web 搜索等</td>
</tr>
<tr>
<td><strong>Ralph Loop</strong></td>
<td>自动化循环执行</td>
</tr>
</tbody></table>
<p><strong>MCP 服务</strong>：配置了 MySQL MCP 服务器，让智能体可以直接查询数据库，在处理数据相关的开发任务时非常方便。</p>
<p><strong>自定义 Agent</strong>：我还创建了一个 <code>java-bug-pathfinder</code> 自定义智能体，专门用于 Java Bug 的调用链追踪和根因分析，配置为只读权限，确保安全。</p>
<h2 id="选型总结与建议"><a href="#选型总结与建议" class="headerlink" title="选型总结与建议"></a>选型总结与建议</h2><p>经过几个月的使用，我总结出以下选型原则：</p>
<ol>
<li><p><strong>编排类角色选稳不选猛</strong>：主编排者需要的是可靠性和全局视野，不需要偶尔的天才表现。GLM-5.1 的稳健特质完美匹配。</p>
</li>
<li><p><strong>高频角色选快不选强</strong>：Explore、Librarian 这种可能并行启动 5 个实例的角色，响应速度和成本比”聪明程度”更重要。MiniMax-M2.7 是这个场景的最优解。</p>
</li>
<li><p><strong>审查角色选强不选快</strong>：Momus 做 Plan Review，宁可多等几秒也要找出问题。GPT-5.4 的审查质量值得这点等待。</p>
</li>
<li><p><strong>创意角色选长不选短</strong>：前端设计、文档写作需要长上下文和创意表达。Kimi-k2.5 的长文本和视觉理解能力让它在这个领域领先。</p>
</li>
<li><p><strong>实现角色选专不选全</strong>：代码生成需要的是对框架和语言的深度理解。Qwen3.5-plus 在代码领域的专精让它在 Prometheus 角色上表现突出。</p>
</li>
<li><p><strong>按预算分层</strong>：不是每个任务都需要最贵的模型。把高成本模型留给 Hephaestus 和 Momus 这种”关键时刻才上场”的角色，日常任务用性价比模型。</p>
</li>
</ol>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>AI 编程工具的配置不是”选一个最强模型就完事”。就像组建一个工程团队，你需要了解每个”成员”的特长，把合适的人放在合适的位置。希望这篇分享能帮助你更好地配置自己的 AI 编程智能体团队。</p>
<p>如果你也在用 Claude Code + OhMyOpenCode，欢迎交流你的配置方案。</p>
]]></content>
      <categories>
        <category>AI经验</category>
      </categories>
      <tags>
        <tag>AI编程</tag>
        <tag>智能体</tag>
        <tag>OhMyOpenCode</tag>
        <tag>ClaudeCode</tag>
        <tag>模型选型</tag>
      </tags>
  </entry>
  <entry>
    <title>Consul 配置踩坑：数据源切换的陷阱与排查</title>
    <url>/2026/05/29/Consul%E9%85%8D%E7%BD%AE%E8%B8%A9%E5%9D%91-%E6%95%B0%E6%8D%AE%E6%BA%90%E5%88%87%E6%8D%A2/</url>
    <content><![CDATA[<p>数字孪生模块从 local profile 切换到 Consul 配置后，视频巡查接口全部报 500：<code>Table &#39;gdsw.bs_sw_cy_dh_video_inspection&#39; doesn&#39;t exist</code>。本地库有这张表，MCP 工具能查到，但服务连不上。本文记录了这个坑的根因、排查思路和解决方案。</p>
<div class="audio-player">
  <div class="audio-section">
    <h4>🎧 文章导读</h4>
    <audio controls preload="metadata">
      <source src="/audio/Consul配置踩坑-数据源切换/narration.mp3" type="audio/mpeg">
      您的浏览器不支持音频播放。
    </audio>
  </div>
  <div class="audio-section">
    <h4>🎵 背景音乐</h4>
    <audio controls loop preload="metadata">
      <source src="/audio/Consul配置踩坑-数据源切换/bgm.mp3" type="audio/mpeg">
      您的浏览器不支持音频播放。
    </audio>
  </div>
</div>

<span id="more"></span>

<h2 id="问题现象"><a href="#问题现象" class="headerlink" title="问题现象"></a>问题现象</h2><p>数字孪生模块（digital-twin）从 local profile 切换到 Consul 配置后，视频巡查接口全部报 500：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Table &#x27;gdsw.bs_sw_cy_dh_video_inspection&#x27; doesn&#x27;t exist</span><br></pre></td></tr></table></figure>

<p>奇怪的是，本地数据库确实有这张表，用 MCP 工具能正常查询。但服务就是连不上。</p>
<h2 id="排查过程"><a href="#排查过程" class="headerlink" title="排查过程"></a>排查过程</h2><h3 id="第一步：确认表是否存在"><a href="#第一步：确认表是否存在" class="headerlink" title="第一步：确认表是否存在"></a>第一步：确认表是否存在</h3><p>用 MCP 工具直接查询本地数据库，确认 <code>bs_sw_cy_dh_video_inspection</code> 表存在且有数据。</p>
<p><strong>结论</strong>：表存在，问题不在数据库。</p>
<h3 id="第二步：怀疑服务连错了数据库"><a href="#第二步：怀疑服务连错了数据库" class="headerlink" title="第二步：怀疑服务连错了数据库"></a>第二步：怀疑服务连错了数据库</h3><p>既然表存在但服务报”表不存在”，最可能的原因是<strong>服务连的不是同一个数据库</strong>。</p>
<p>查看启动日志，发现数据源初始化的 URL 是 <code>10.144.32.219:3306/gdsw</code>（远程库），而不是 <code>127.0.0.1:3306/gdsw</code>（本地库）。</p>
<p><strong>结论</strong>：服务连的是远程库，远程库没有新建的表。</p>
<h3 id="第三步：检查-Consul-配置"><a href="#第三步：检查-Consul-配置" class="headerlink" title="第三步：检查 Consul 配置"></a>第三步：检查 Consul 配置</h3><p>Consul 配置分两层：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Consul 配置中心</span><br><span class="line">├── 通用模板（gdsw-common-config）</span><br><span class="line">│   └── spring.datasource.url → 远程库 10.144.32.219</span><br><span class="line">├── 数字孪生模块（gdsw-digital-twin-config）</span><br><span class="line">│   └── targetdatabase.url → 本地库 127.0.0.1（自定义配置项）</span><br><span class="line">└── 数据归集模块（gdsw-data-collection-config）</span><br><span class="line">    └── 模块专属配置</span><br></pre></td></tr></table></figure>

<p><strong>问题根因</strong>：模块配置中使用了自定义配置项 <code>targetdatabase.url</code>，但这个配置项<strong>不会覆盖</strong> Spring 的 <code>spring.datasource.url</code>。实际数据源用的是通用模板里的远程库地址。</p>
<h2 id="根因分析"><a href="#根因分析" class="headerlink" title="根因分析"></a>根因分析</h2><p>Consul 配置的覆盖规则是：<strong>模块级配置覆盖通用模板的同名属性</strong>。</p>
<p>但 <code>targetdatabase.url</code> 是自定义配置项，不是 Spring 数据源的标准配置项。Spring Boot 自动配置数据源时，读取的是 <code>spring.datasource.url</code>，而不是 <code>targetdatabase.url</code>。</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 通用模板（所有模块共享）</span></span><br><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">datasource:</span></span><br><span class="line">    <span class="attr">url:</span> <span class="string">jdbc:mysql://10.144.32.219:3306/gdsw</span>  <span class="comment"># 远程库</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 数字孪生模块配置</span></span><br><span class="line"><span class="attr">targetdatabase:</span></span><br><span class="line">  <span class="attr">url:</span> <span class="string">jdbc:mysql://127.0.0.1:3306/gdsw</span>  <span class="comment"># 自定义配置项，不会覆盖 spring.datasource.url</span></span><br></pre></td></tr></table></figure>

<p>所以实际数据源用的是通用模板的远程库地址，远程库没有新建的表，导致 <code>Table doesn&#39;t exist</code>。</p>
<h2 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h2><p>在数字孪生模块的 Consul 配置中，直接覆盖 <code>spring.datasource</code>：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">datasource:</span></span><br><span class="line">    <span class="attr">url:</span> <span class="string">jdbc:mysql://127.0.0.1:3306/gdsw?useSSL=false&amp;useUnicode=true&amp;characterEncoding=utf-8&amp;zeroDateTimeBehavior=convertToNull&amp;transformedBitIsBoolean=true&amp;tinyInt1isBit=false&amp;allowMultiQueries=true&amp;serverTimezone=GMT%2B8&amp;allowPublicKeyRetrieval=true</span></span><br><span class="line">    <span class="attr">username:</span> <span class="string">root</span></span><br><span class="line">    <span class="attr">password:</span> <span class="string">root</span></span><br></pre></td></tr></table></figure>

<p>这样模块级配置的 <code>spring.datasource.url</code> 会覆盖通用模板的同名属性，服务就会连本地库。</p>
<h2 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h2><h3 id="1-YAML-不能有两个同级-spring-块"><a href="#1-YAML-不能有两个同级-spring-块" class="headerlink" title="1. YAML 不能有两个同级 spring: 块"></a>1. YAML 不能有两个同级 <code>spring:</code> 块</h3><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 错误：两个 spring: 块，后面的覆盖前面的</span></span><br><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">servlet:</span></span><br><span class="line">    <span class="attr">multipart:</span> <span class="string">...</span></span><br><span class="line"><span class="attr">spring:</span>                    <span class="comment"># ← 这个会覆盖上面的</span></span><br><span class="line">  <span class="attr">datasource:</span></span><br><span class="line">    <span class="attr">url:</span> <span class="string">...</span></span><br></pre></td></tr></table></figure>

<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 正确：合并成一个 spring: 块</span></span><br><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">servlet:</span></span><br><span class="line">    <span class="attr">multipart:</span> <span class="string">...</span></span><br><span class="line">  <span class="attr">datasource:</span></span><br><span class="line">    <span class="attr">url:</span> <span class="string">...</span></span><br><span class="line">    <span class="attr">dynamic:</span></span><br><span class="line">      <span class="attr">datasource:</span></span><br><span class="line">        <span class="attr">slave:</span> <span class="string">...</span></span><br></pre></td></tr></table></figure>

<h3 id="2-数据库配置改了必须重启"><a href="#2-数据库配置改了必须重启" class="headerlink" title="2. 数据库配置改了必须重启"></a>2. 数据库配置改了必须重启</h3><p><code>@RefreshScope</code> 热更新不了连接池。数据库连接池在应用启动时初始化，运行时修改配置不会生效。</p>
<h3 id="3-普通业务配置可以热更新"><a href="#3-普通业务配置可以热更新" class="headerlink" title="3. 普通业务配置可以热更新"></a>3. 普通业务配置可以热更新</h3><p>开关、URL、TTL 等普通业务配置可以通过 <code>@RefreshScope</code> 热更新，不用重启。</p>
<h2 id="Consul-配置结构理解"><a href="#Consul-配置结构理解" class="headerlink" title="Consul 配置结构理解"></a>Consul 配置结构理解</h2><p><img data-src="/images/posts/Consul%E9%85%8D%E7%BD%AE%E8%B8%A9%E5%9D%91-%E6%95%B0%E6%8D%AE%E6%BA%90%E5%88%87%E6%8D%A2/fig1.png" alt="配置层级图"><br><em>图1：Consul 配置的三层结构</em></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Consul 配置中心</span><br><span class="line">├── 通用模板（gdsw-common-config）</span><br><span class="line">│   └── 定义所有模块共享的默认配置</span><br><span class="line">├── 数据归集模块（gdsw-data-collection-config）</span><br><span class="line">│   └── 覆盖数据归集模块的专属配置</span><br><span class="line">├── 数字孪生模块（gdsw-digital-twin-config）</span><br><span class="line">│   └── 覆盖数字孪生模块的专属配置</span><br><span class="line">└── 其他模块...</span><br></pre></td></tr></table></figure>

<p><strong>覆盖规则</strong>：模块级配置 &gt; 通用模板 &gt; 本地配置文件</p>
<p><strong>关键点</strong>：只有同名属性才会被覆盖。自定义配置项（如 <code>targetdatabase.url</code>）不会影响 Spring 的标准配置项（如 <code>spring.datasource.url</code>）。</p>
<h2 id="排查思路总结"><a href="#排查思路总结" class="headerlink" title="排查思路总结"></a>排查思路总结</h2><p><img data-src="/images/posts/Consul%E9%85%8D%E7%BD%AE%E8%B8%A9%E5%9D%91-%E6%95%B0%E6%8D%AE%E6%BA%90%E5%88%87%E6%8D%A2/fig2.png" alt="排查流程图"><br><em>图2：问题排查的五步流程</em></p>
<ol>
<li>报 <code>Table doesn&#39;t exist</code> → 先确认表是否真的不存在</li>
<li>MCP 能查到但服务报错 → 服务连的不是同一个数据库</li>
<li>看启动日志里数据源初始化的 URL</li>
<li>检查 Consul 配置里 <code>spring.datasource</code> 的实际值</li>
<li>注意自定义配置项不会影响 Spring 标准配置项</li>
</ol>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><ol>
<li><strong>自定义配置项不能替代 Spring 标准配置项</strong>——<code>targetdatabase.url</code> 不会覆盖 <code>spring.datasource.url</code></li>
<li><strong>模块级配置要覆盖同名属性</strong>——想切换数据源，必须覆盖 <code>spring.datasource.url</code></li>
<li><strong>数据库配置改了必须重启</strong>——<code>@RefreshScope</code> 热更新不了连接池</li>
<li><strong>YAML 不能有两个同级 <code>spring:</code> 块</strong>——会冲突覆盖，需要合并</li>
<li><strong>排查问题要从数据源开始</strong>——“表不存在”往往是连错了库</li>
</ol>
<hr>
<p><em>本文记录于 2026-05-29，数字孪生模块 Consul 配置踩坑。</em></p>
]]></content>
      <categories>
        <category>后端开发</category>
      </categories>
      <tags>
        <tag>Spring Boot</tag>
        <tag>Consul</tag>
        <tag>配置中心</tag>
        <tag>数据源</tag>
        <tag>踩坑</tag>
      </tags>
  </entry>
  <entry>
    <title>JdbcTemplate vs MyBatis-Plus：复杂查询场景的选择指南</title>
    <url>/2026/05/25/JdbcTemplate-vs-MyBatis-Plus/</url>
    <content><![CDATA[<p>在数据归集系统的 <code>TreeSelectorServiceImpl</code> 中，大部分查询使用 MyBatis-Plus，但有 6 处使用了原生 JdbcTemplate。本文分析这 6 种场景，总结何时应该”退回”原生 SQL，以及如何在两者之间做出合理选择。</p>
<div class="audio-player">
  <div class="audio-section">
    <h4>🎧 文章导读</h4>
    <audio controls preload="metadata">
      <source src="/audio/JdbcTemplate-vs-MyBatis-Plus/narration.mp3" type="audio/mpeg">
      您的浏览器不支持音频播放。
    </audio>
  </div>
  <div class="audio-section">
    <h4>🎵 背景音乐</h4>
    <audio controls loop preload="metadata">
      <source src="/audio/JdbcTemplate-vs-MyBatis-Plus/bgm.mp3" type="audio/mpeg">
      您的浏览器不支持音频播放。
    </audio>
  </div>
</div>

<span id="more"></span>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>MyBatis-Plus（以下简称 MBP）是 Spring Boot 项目中最常用的 ORM 框架之一。它的 <code>QueryWrapper</code> 和 <code>LambdaQueryWrapper</code> 让单表查询变得非常简洁，但在某些复杂场景下，原生 <code>JdbcTemplate</code> 反而是更好的选择。</p>
<p>本文以数据归集系统 <code>TreeSelectorServiceImpl</code> 中的 6 处 JdbcTemplate 使用为例，分析三类核心原因，并给出优化方向。</p>
<h2 id="场景清单"><a href="#场景清单" class="headerlink" title="场景清单"></a>场景清单</h2><p>先看全景——6 处 JdbcTemplate 使用的原因和涉及的 SQL 特性：</p>
<table>
<thead>
<tr>
<th>#</th>
<th>方法</th>
<th>原因</th>
<th>涉及的 SQL 特性</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td><code>batchElementValue</code>（无时间范围）</td>
<td>GROUP BY 取最新记录</td>
<td><code>MAX(TM)</code> + <code>GROUP BY</code> + 子查询</td>
</tr>
<tr>
<td>2</td>
<td><code>getEquipmentSymbolsWithNames</code></td>
<td>跨表 LEFT JOIN</td>
<td><code>equipmentr LEFT JOIN elementb</code></td>
</tr>
<tr>
<td>3</td>
<td><code>latestElementValue</code></td>
<td>GROUP BY + 可选 LEFT JOIN</td>
<td><code>MAX(TM)</code> + <code>GROUP BY</code> + 关联查询</td>
</tr>
<tr>
<td>4</td>
<td><code>getElementData</code></td>
<td>动态表名 + 动态列名</td>
<td>表名&#x2F;列名来自枚举运行时拼接</td>
</tr>
<tr>
<td>5</td>
<td><code>batchElementData</code></td>
<td>同上，批量版</td>
<td>动态表名 + 动态列名</td>
</tr>
<tr>
<td>6</td>
<td><code>getSymbolChnMap</code></td>
<td>全量查中文名映射</td>
<td>被 1、3 调用的辅助方法</td>
</tr>
</tbody></table>
<h2 id="三类核心原因"><a href="#三类核心原因" class="headerlink" title="三类核心原因"></a>三类核心原因</h2><p><img data-src="/images/posts/JdbcTemplate-vs-MyBatis-Plus/fig1.png" alt="三种场景对比图"><br><em>图1：聚合查询、跨表关联、动态表名三种场景对比</em></p>
<h3 id="原因一：聚合-分组"><a href="#原因一：聚合-分组" class="headerlink" title="原因一：聚合 + 分组"></a>原因一：聚合 + 分组</h3><p>MBP 的 <code>QueryWrapper</code> 不支持 <code>GROUP BY</code> + 聚合函数 + 子查询的组合。</p>
<p>典型场景：取每个设备的最新一条记录。这需要子查询先按设备分组取最大时间戳，再关联回主表取完整记录：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 典型模式：取每个设备的最新一条记录</span></span><br><span class="line"><span class="keyword">SELECT</span> t1.<span class="operator">*</span> <span class="keyword">FROM</span> <span class="keyword">table</span> t1</span><br><span class="line"><span class="keyword">INNER</span> <span class="keyword">JOIN</span> (</span><br><span class="line">    <span class="keyword">SELECT</span> EQID, <span class="built_in">MAX</span>(TM) <span class="keyword">AS</span> MAX_TM</span><br><span class="line">    <span class="keyword">FROM</span> <span class="keyword">table</span> <span class="keyword">WHERE</span> EQID <span class="keyword">IN</span> (?, ?, ?)</span><br><span class="line">    <span class="keyword">GROUP</span> <span class="keyword">BY</span> EQID</span><br><span class="line">) t2 <span class="keyword">ON</span> t1.EQID <span class="operator">=</span> t2.EQID <span class="keyword">AND</span> t1.TM <span class="operator">=</span> t2.MAX_TM</span><br></pre></td></tr></table></figure>

<p>这种”分组取极值”的模式在数据查询中非常常见，但 MBP 的 <code>QueryWrapper</code> 无法表达子查询和聚合函数的组合。</p>
<h3 id="原因二：跨表关联查询"><a href="#原因二：跨表关联查询" class="headerlink" title="原因二：跨表关联查询"></a>原因二：跨表关联查询</h3><p>MBP 的 <code>QueryWrapper</code> 只支持单表查询，无法表达 <code>LEFT JOIN</code>。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 典型模式：关联要素中文名</span></span><br><span class="line"><span class="keyword">SELECT</span> e.SYMBOL, eb.CHNM</span><br><span class="line"><span class="keyword">FROM</span> bs_sw_cy_dh_equipmentr e</span><br><span class="line"><span class="keyword">LEFT</span> <span class="keyword">JOIN</span> bs_sw_cy_dh_elementb eb <span class="keyword">ON</span> e.SYMBOL <span class="operator">=</span> eb.SYMBOL</span><br></pre></td></tr></table></figure>

<p>当需要从多张表中组合数据时，MBP 的单表限制就成了瓶颈。</p>
<h3 id="原因三：动态表名-列名"><a href="#原因三：动态表名-列名" class="headerlink" title="原因三：动态表名&#x2F;列名"></a>原因三：动态表名&#x2F;列名</h3><p>MBP 的实体类映射是静态的，表名和列名在编译期确定。但有些场景需要根据运行时参数动态选择表名和列名：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 动态拼接表名和列名</span></span><br><span class="line"><span class="type">String</span> <span class="variable">tableName</span> <span class="operator">=</span> tableTypeConfig.getTableName();  <span class="comment">// 如 PPTN → ST_PPTN_R</span></span><br><span class="line"><span class="type">String</span> <span class="variable">columns</span> <span class="operator">=</span> String.join(<span class="string">&quot;,&quot;</span>, tableTypeConfig.getColumns());</span><br><span class="line"><span class="type">String</span> <span class="variable">sql</span> <span class="operator">=</span> <span class="string">&quot;SELECT &quot;</span> + columns + <span class="string">&quot; FROM &quot;</span> + tableName + <span class="string">&quot; WHERE ...&quot;</span>;</span><br></pre></td></tr></table></figure>

<p>这种”配置驱动的查询”在水文监测系统中很常见——不同类型的监测数据存储在不同的表中，表名由配置枚举决定。</p>
<h2 id="分页实现"><a href="#分页实现" class="headerlink" title="分页实现"></a>分页实现</h2><p>在 <code>latestElementValue</code> 接口基础上新增分页能力，沿用 <code>batchElementData</code> 已有的手动分页模式：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 两个分支都加了 COUNT + LIMIT OFFSET</span></span><br><span class="line"><span class="comment">// 无时间范围分支</span></span><br><span class="line"><span class="type">String</span> <span class="variable">countSql</span> <span class="operator">=</span> <span class="string">&quot;SELECT COUNT(*) FROM (&quot;</span> + dataSql + <span class="string">&quot;) cnt&quot;</span>;</span><br><span class="line"><span class="type">String</span> <span class="variable">pagedSql</span> <span class="operator">=</span> dataSql + <span class="string">&quot; LIMIT ? OFFSET ?&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 有时间范围分支</span></span><br><span class="line"><span class="type">String</span> <span class="variable">countSql</span> <span class="operator">=</span> <span class="string">&quot;SELECT COUNT(*) FROM table WHERE ...&quot;</span>;</span><br><span class="line"><span class="type">String</span> <span class="variable">pagedSql</span> <span class="operator">=</span> dataSql + <span class="string">&quot; LIMIT ? OFFSET ?&quot;</span>;</span><br></pre></td></tr></table></figure>

<p>返回格式：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;total&quot;</span><span class="punctuation">:</span> <span class="number">54</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;pageNum&quot;</span><span class="punctuation">:</span> <span class="number">1</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;pageSize&quot;</span><span class="punctuation">:</span> <span class="number">10</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;list&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span>...<span class="punctuation">]</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<h2 id="优化方向"><a href="#优化方向" class="headerlink" title="优化方向"></a>优化方向</h2><p>当 JdbcTemplate 的字符串拼接变得难以维护时，可以考虑以下方案：</p>
<table>
<thead>
<tr>
<th>方向</th>
<th>说明</th>
<th>适用场景</th>
</tr>
</thead>
<tbody><tr>
<td>MyBatis XML Mapper</td>
<td>用 XML 定义复杂 SQL，替代 JdbcTemplate 字符串拼接</td>
<td>场景 1、2、3</td>
</tr>
<tr>
<td>动态表名插件</td>
<td>MBP 的 <code>DynamicTableNameInnerInterceptor</code></td>
<td>场景 4、5</td>
</tr>
<tr>
<td>@Select 注解</td>
<td>在 Mapper 接口上直接写 SQL</td>
<td>简单的 JOIN 查询</td>
</tr>
</tbody></table>
<blockquote>
<p>当前方案功能正确，优化优先级不高。若后续维护成本增加（如 SQL 拼接出错难排查），可考虑迁移到 XML Mapper。</p>
</blockquote>
<h2 id="选择指南"><a href="#选择指南" class="headerlink" title="选择指南"></a>选择指南</h2><p><img data-src="/images/posts/JdbcTemplate-vs-MyBatis-Plus/fig2.png" alt="选择指南决策树"><br><em>图2：何时用 MyBatis-Plus、何时用 JdbcTemplate 的决策树</em></p>
<p>总结一下何时用 MBP、何时用 JdbcTemplate：</p>
<p><strong>用 MyBatis-Plus 的场景</strong>：</p>
<ul>
<li>单表 CRUD</li>
<li>简单条件查询（等值、范围、LIKE）</li>
<li>分页查询（内置分页插件）</li>
<li>需要类型安全的 Lambda 查询</li>
</ul>
<p><strong>用 JdbcTemplate 的场景</strong>：</p>
<ul>
<li>聚合查询 + 子查询（GROUP BY + 聚合函数）</li>
<li>跨表 JOIN（LEFT JOIN、INNER JOIN）</li>
<li>动态表名&#x2F;列名（运行时确定）</li>
<li>复杂 SQL 需要精确控制</li>
</ul>
<p><strong>折中方案</strong>：</p>
<ul>
<li>MyBatis XML Mapper：保留 MBP 的便利性，同时支持复杂 SQL</li>
<li>@Select 注解：在 Mapper 接口上直接写 SQL，适合简单的 JOIN</li>
</ul>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><ol>
<li><strong>不要为了”统一技术栈”而强行用 MBP</strong>——当 QueryWrapper 无法表达你的查询时，JdbcTemplate 是更务实的选择</li>
<li><strong>字符串拼接 SQL 要注意注入风险</strong>——虽然 JdbcTemplate 的参数化查询已经防注入，但表名和列名的动态拼接仍需谨慎</li>
<li><strong>分页查询要先 COUNT 再查数据</strong>——避免在 Java 层做分页，浪费数据库资源</li>
<li><strong>复杂 SQL 要有注释</strong>——尤其是子查询和 JOIN 的逻辑，方便后续维护</li>
</ol>
<hr>
<p><em>本文涉及代码在 <code>TreeSelectorServiceImpl.java</code> 中，共 6 处 JdbcTemplate 使用。</em></p>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>数据归集</category>
      </categories>
      <tags>
        <tag>Spring Boot</tag>
        <tag>JdbcTemplate</tag>
        <tag>MyBatis-Plus</tag>
        <tag>SQL优化</tag>
        <tag>数据库</tag>
      </tags>
  </entry>
  <entry>
    <title>Mail.tm API 集成踩坑：从 DuckMail 迁移的排查实录</title>
    <url>/2026/03/04/Mail-tm-API%E9%9B%86%E6%88%90%E8%B8%A9%E5%9D%91/</url>
    <content><![CDATA[<p>在一次工具迁移过程中，我遇到了一个经典的 API 兼容性问题：原工具使用 DuckMail（需付费 Token），我将其改为免费的 Mail.tm API 后，验证码邮件始终收不到。本文记录了完整的排查过程，以及如何通过”诊断增强”而非”绕过”的思路解决问题。</p>
<span id="more"></span>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>最近在研究一个 ChatGPT 批量注册工具，原项目使用 DuckMail 临时邮箱服务。由于 DuckMail 需要付费获取 API Token，我决定将其替换为免费的 Mail.tm API。</p>
<p>本以为只是简单的 API 地址替换，结果踩了一个不小的坑。</p>
<h2 id="问题背景"><a href="#问题背景" class="headerlink" title="问题背景"></a>问题背景</h2><h3 id="原始配置"><a href="#原始配置" class="headerlink" title="原始配置"></a>原始配置</h3><p>原工具使用 DuckMail API：</p>
<ul>
<li>API 地址：<code>https://api.duckmail.sbs</code></li>
<li>需要 Bearer Token（付费）</li>
<li>邮箱域名：<code>@duckmail.sbs</code></li>
</ul>
<h3 id="目标配置"><a href="#目标配置" class="headerlink" title="目标配置"></a>目标配置</h3><p>改为 Mail.tm API：</p>
<ul>
<li>API 地址：<code>https://api.mail.tm</code></li>
<li><strong>免费，无需 Token</strong></li>
<li>邮箱域名：动态获取（如 <code>@dollicons.com</code>）</li>
</ul>
<h3 id="修改内容"><a href="#修改内容" class="headerlink" title="修改内容"></a>修改内容</h3><p>主要修改点：</p>
<ol>
<li>API 地址替换</li>
<li>移除 Bearer Token 验证</li>
<li>动态获取可用域名</li>
</ol>
<h2 id="问题现象"><a href="#问题现象" class="headerlink" title="问题现象"></a>问题现象</h2><p>修改完成后运行测试，流程如下：</p>
<table>
<thead>
<tr>
<th>步骤</th>
<th>状态</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>创建临时邮箱</td>
<td>✅ 成功</td>
<td><code>xxx@dollicons.com</code></td>
</tr>
<tr>
<td>访问 ChatGPT</td>
<td>✅ 成功</td>
<td>HTTP 200</td>
</tr>
<tr>
<td>发起注册请求</td>
<td>✅ 成功</td>
<td>跳转到密码设置页面</td>
</tr>
<tr>
<td>发送 OTP</td>
<td>✅ 成功</td>
<td><code>/email-otp/send</code> 返回 200</td>
</tr>
<tr>
<td><strong>等待验证码邮件</strong></td>
<td>❌ 超时</td>
<td>120 秒内未收到任何邮件</td>
</tr>
</tbody></table>
<p>关键问题：<strong>OpenAI 的 <code>/email-otp/send</code> 接口返回 200 成功，但 Mail.tm 收件箱始终为空。</strong></p>
<h2 id="排查过程"><a href="#排查过程" class="headerlink" title="排查过程"></a>排查过程</h2><h3 id="第一阶段：黑盒排查"><a href="#第一阶段：黑盒排查" class="headerlink" title="第一阶段：黑盒排查"></a>第一阶段：黑盒排查</h3><p>最初我怀疑是以下原因：</p>
<ul>
<li>临时邮箱域名被 OpenAI 屏蔽</li>
<li>邮件服务有延迟</li>
<li>代理 IP 被限制</li>
</ul>
<p>为了验证，我向另一个 AI（Codex）请求帮助进行代码级诊断。</p>
<h3 id="第二阶段：诊断增强"><a href="#第二阶段：诊断增强" class="headerlink" title="第二阶段：诊断增强"></a>第二阶段：诊断增强</h3><p>Codex 采用了”诊断增强”的思路，而非直接尝试绕过检测。具体做了以下改进：</p>
<p><strong>1. 域名查询走同一代理出口</strong></p>
<p>确保 <code>/domains</code> 请求和注册流程使用相同的代理，避免出口 IP 不一致。</p>
<p><strong>2. 收件轮询策略优化</strong></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 原代码：只看第一封邮件</span></span><br><span class="line">messages = _fetch_emails_duckmail(mail_token)</span><br><span class="line"><span class="keyword">if</span> messages <span class="keyword">and</span> <span class="built_in">len</span>(messages) &gt; <span class="number">0</span>:</span><br><span class="line">    first_msg = messages[<span class="number">0</span>]</span><br><span class="line">    <span class="comment"># ...</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 改进后：扫描最近 N 封 + 去重</span></span><br><span class="line">checked_ids = <span class="built_in">set</span>()</span><br><span class="line"><span class="keyword">for</span> msg <span class="keyword">in</span> messages[:scan_limit]:</span><br><span class="line">    msg_id = msg.get(<span class="string">&quot;id&quot;</span>)</span><br><span class="line">    <span class="keyword">if</span> msg_id <span class="keyword">and</span> msg_id <span class="keyword">not</span> <span class="keyword">in</span> checked_ids:</span><br><span class="line">        checked_ids.add(msg_id)</span><br><span class="line">        <span class="comment"># 处理邮件...</span></span><br></pre></td></tr></table></figure>

<p><strong>3. 增强错误分类</strong></p>
<p>邮件接口返回 meta 信息，可区分：</p>
<ul>
<li>空收件箱</li>
<li>鉴权失败（401&#x2F;403）</li>
<li>API 错误</li>
<li>邮件详情拉取失败</li>
</ul>
<p><strong>4. 可配置参数</strong></p>
<p>新增环境变量：</p>
<ul>
<li><code>MAIL_OTP_TIMEOUT_SEC</code>：超时时间</li>
<li><code>MAIL_POLL_INTERVAL_SEC</code>：轮询间隔</li>
<li><code>MAIL_SCAN_LIMIT</code>：扫描邮件数量上限</li>
</ul>
<h3 id="第三阶段：定位根因"><a href="#第三阶段：定位根因" class="headerlink" title="第三阶段：定位根因"></a>第三阶段：定位根因</h3><p>运行增强诊断后，发现了真正的错误：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[OTP] 收件箱请求失败 status=200 err=&#x27;list&#x27; object has no attribute &#x27;get&#x27;</span><br></pre></td></tr></table></figure>

<p><strong>根因分析</strong>：</p>
<p>原代码假设 Mail.tm 的 <code>/messages</code> API 返回格式为：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;hydra:member&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span>...<span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;member&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span>...<span class="punctuation">]</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>但实际上，Mail.tm <strong>直接返回列表</strong>：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">[</span></span><br><span class="line">  <span class="punctuation">&#123;</span><span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;xxx&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;subject&quot;</span><span class="punctuation">:</span> <span class="string">&quot;...&quot;</span><span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="punctuation">&#123;</span><span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;yyy&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;subject&quot;</span><span class="punctuation">:</span> <span class="string">&quot;...&quot;</span><span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">]</span></span><br></pre></td></tr></table></figure>

<p>当代码尝试对列表调用 <code>.get()</code> 方法时，自然就报错了。</p>
<h2 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h2><p>修复代码非常简单，只需要先判断返回类型：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">_fetch_emails_mail</span>(<span class="params">mail_token: <span class="built_in">str</span>, ...</span>):</span><br><span class="line">    <span class="comment"># ...</span></span><br><span class="line">    <span class="keyword">if</span> res.status_code == <span class="number">200</span>:</span><br><span class="line">        data = res.json()</span><br><span class="line">        <span class="comment"># 关键修复：先判断是否为列表</span></span><br><span class="line">        <span class="keyword">if</span> <span class="built_in">isinstance</span>(data, <span class="built_in">list</span>):</span><br><span class="line">            messages = data</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            messages = data.get(<span class="string">&quot;hydra:member&quot;</span>) <span class="keyword">or</span> data.get(<span class="string">&quot;member&quot;</span>) <span class="keyword">or</span> []</span><br><span class="line">        <span class="comment"># ...</span></span><br></pre></td></tr></table></figure>

<h2 id="最终结果"><a href="#最终结果" class="headerlink" title="最终结果"></a>最终结果</h2><p>修复后重新运行测试：</p>
<p><img data-src="/images/posts/mail-tm-success.png" alt="注册成功截图"></p>
<p>成功注册 6 个账号，平均速度 30 秒&#x2F;个。</p>
<table>
<thead>
<tr>
<th>#</th>
<th>邮箱</th>
<th>OAuth</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td><a href="mailto:&#109;&#x74;&#x66;&#x36;&#x63;&#109;&#x66;&#x63;&#64;&#100;&#111;&#108;&#x6c;&#x69;&#x63;&#111;&#110;&#115;&#46;&#x63;&#x6f;&#x6d;">&#109;&#x74;&#x66;&#x36;&#x63;&#109;&#x66;&#x63;&#64;&#100;&#111;&#108;&#x6c;&#x69;&#x63;&#111;&#110;&#115;&#46;&#x63;&#x6f;&#x6d;</a></td>
<td>✅</td>
</tr>
<tr>
<td>2</td>
<td><a href="mailto:&#x77;&#122;&#x77;&#118;&#x6f;&#x75;&#106;&#109;&#x66;&#54;&#x6f;&#x69;&#55;&#64;&#100;&#111;&#108;&#108;&#x69;&#99;&#x6f;&#110;&#x73;&#46;&#99;&#x6f;&#109;">&#x77;&#122;&#x77;&#118;&#x6f;&#x75;&#106;&#109;&#x66;&#54;&#x6f;&#x69;&#55;&#64;&#100;&#111;&#108;&#108;&#x69;&#99;&#x6f;&#110;&#x73;&#46;&#99;&#x6f;&#109;</a></td>
<td>✅</td>
</tr>
<tr>
<td>3</td>
<td><a href="mailto:&#x68;&#48;&#98;&#103;&#x75;&#x72;&#50;&#120;&#x40;&#x64;&#x6f;&#x6c;&#108;&#x69;&#99;&#x6f;&#110;&#115;&#x2e;&#99;&#x6f;&#109;">&#x68;&#48;&#98;&#103;&#x75;&#x72;&#50;&#120;&#x40;&#x64;&#x6f;&#x6c;&#108;&#x69;&#99;&#x6f;&#110;&#115;&#x2e;&#99;&#x6f;&#109;</a></td>
<td>✅</td>
</tr>
<tr>
<td>4</td>
<td><a href="mailto:&#x36;&#x70;&#114;&#x6b;&#x66;&#55;&#x6e;&#x36;&#x31;&#64;&#100;&#x6f;&#x6c;&#108;&#105;&#x63;&#111;&#x6e;&#115;&#46;&#99;&#111;&#109;">&#x36;&#x70;&#114;&#x6b;&#x66;&#55;&#x6e;&#x36;&#x31;&#64;&#100;&#x6f;&#x6c;&#108;&#105;&#x63;&#111;&#x6e;&#115;&#46;&#99;&#111;&#109;</a></td>
<td>✅</td>
</tr>
<tr>
<td>5</td>
<td><a href="mailto:&#119;&#x6e;&#97;&#49;&#x38;&#104;&#x6c;&#x6f;&#56;&#109;&#103;&#x40;&#100;&#111;&#108;&#108;&#105;&#x63;&#111;&#x6e;&#x73;&#46;&#99;&#x6f;&#x6d;">&#119;&#x6e;&#97;&#49;&#x38;&#104;&#x6c;&#x6f;&#56;&#109;&#103;&#x40;&#100;&#111;&#108;&#108;&#105;&#x63;&#111;&#x6e;&#x73;&#46;&#99;&#x6f;&#x6d;</a></td>
<td>✅</td>
</tr>
<tr>
<td>6</td>
<td><a href="mailto:&#x67;&#x6c;&#x61;&#x79;&#x33;&#107;&#x68;&#53;&#113;&#106;&#x40;&#100;&#x6f;&#108;&#x6c;&#105;&#99;&#x6f;&#110;&#115;&#x2e;&#99;&#111;&#109;">&#x67;&#x6c;&#x61;&#x79;&#x33;&#107;&#x68;&#53;&#113;&#106;&#x40;&#100;&#x6f;&#108;&#x6c;&#105;&#99;&#x6f;&#110;&#115;&#x2e;&#99;&#111;&#109;</a></td>
<td>✅</td>
</tr>
</tbody></table>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><h3 id="1-API-返回格式不能想当然"><a href="#1-API-返回格式不能想当然" class="headerlink" title="1. API 返回格式不能想当然"></a>1. API 返回格式不能想当然</h3><p>不同服务即使功能相似，返回格式也可能不同：</p>
<ul>
<li>DuckMail：<code>&#123;&quot;hydra:member&quot;: [...]&#125;</code></li>
<li>Mail.tm：直接返回 <code>[...]</code></li>
</ul>
<p><strong>教训</strong>：迁移 API 时，务必用实际请求验证返回格式。</p>
<h3 id="2-诊断比绕过更有价值"><a href="#2-诊断比绕过更有价值" class="headerlink" title="2. 诊断比绕过更有价值"></a>2. 诊断比绕过更有价值</h3><p>Codex 的做法值得学习：</p>
<ul>
<li>没有尝试绕过检测</li>
<li>而是增强可观测性，让问题变得可归因</li>
</ul>
<p>一句话：<strong>把”收不到邮件”的黑盒问题，改成了可观测、可归因的问题。</strong></p>
<h3 id="3-错误日志要区分类型"><a href="#3-错误日志要区分类型" class="headerlink" title="3. 错误日志要区分类型"></a>3. 错误日志要区分类型</h3><p>增强诊断后，能够清楚区分：</p>
<ul>
<li>空收件箱（邮件真的没到）</li>
<li>鉴权失败（Token 问题）</li>
<li>API 错误（网络&#x2F;服务问题）</li>
<li>解析错误（代码逻辑问题）</li>
</ul>
<p>这大大缩短了排查时间。</p>
<h2 id="相关资源"><a href="#相关资源" class="headerlink" title="相关资源"></a>相关资源</h2><ul>
<li><strong>完整代码</strong>：<a href="https://github.com/zhdmm35/chatgpt-mailtm-register">GitHub 仓库 - chatgpt-mailtm-register</a></li>
<li><strong>原工具</strong>：<a href="https://github.com/adminlove520/chatgpt_register">chatgpt_register</a></li>
</ul>
<h2 id="封面提示词"><a href="#封面提示词" class="headerlink" title="封面提示词"></a>封面提示词</h2><p>如果你需要生成封面图，可以使用以下提示词：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">A minimalist technical illustration showing email API integration debugging,</span><br><span class="line">with envelope icons and code snippets, clean flat design,</span><br><span class="line">blue and purple gradient background, modern tech style, no text, 16:9 aspect ratio.</span><br></pre></td></tr></table></figure>

<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>这次排查让我深刻体会到：<strong>API 文档不一定准确，实际测试才是王道。</strong> 一个简单的返回格式差异，导致了近 2 小时的排查。好在通过系统化的诊断增强，最终定位并解决了问题。</p>
<p>希望这个踩坑记录对你有所帮助！</p>
]]></content>
      <categories>
        <category>踩坑记录</category>
      </categories>
      <tags>
        <tag>OpenAI</tag>
        <tag>Python</tag>
        <tag>API集成</tag>
        <tag>Mail.tm</tag>
        <tag>排查技巧</tag>
      </tags>
  </entry>
  <entry>
    <title>Nacos本地环境配置避坑指南</title>
    <url>/2026/03/23/Nacos%E6%9C%AC%E5%9C%B0%E7%8E%AF%E5%A2%83%E9%85%8D%E7%BD%AE%E9%81%BF%E5%9D%91%E6%8C%87%E5%8D%97/</url>
    <content><![CDATA[<p>在本地开发物联网平台时，服务注册与配置中心是基础设施的关键一环。本文记录了一次 Nacos 本地链路问题的排查与修复过程，希望能帮助有类似经历的同学少走弯路。</p>
<span id="more"></span>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>最近在搭建物联网项目本地开发环境时，遇到了 Nacos 服务注册失败的问题。表面上控制台能打开，但应用就是连不上。经过仔细排查发现，这并不是代码问题，而是<strong>多层环境偏差叠加后的结果</strong>——默认地址不对、域名解析不稳定、代理干扰、namespace 不一致等问题同时存在，互相影响。</p>
<p><img data-src="/images/fig1-nacos-troubleshooting.png" alt="多层次问题链路流程图"><br><em>图1：Nacos 本地链路故障的多层根因</em></p>
<h2 id="问题现象"><a href="#问题现象" class="headerlink" title="问题现象"></a>问题现象</h2><p>项目启动时，控制台日志里出现了以下典型症状：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Client not connected, current status: STARTING</span><br><span class="line">Ignore the empty nacos configuration</span><br></pre></td></tr></table></figure>

<p>服务无法注册到 Nacos，但奇怪的是通过浏览器访问 <code>http://localhost:8080/</code> 却能看到 Nacos 控制台。这个矛盾现象一开始让人十分困惑。</p>
<h2 id="根因分析"><a href="#根因分析" class="headerlink" title="根因分析"></a>根因分析</h2><p>经过深入分析，问题链路逐渐清晰——<strong>代码默认连的根本不是你最开始看到的那套 Nacos</strong>。</p>
<h3 id="第一层：默认地址不匹配"><a href="#第一层：默认地址不匹配" class="headerlink" title="第一层：默认地址不匹配"></a>第一层：默认地址不匹配</h3><p>项目的公共启动器在运行期会注入 Nacos 配置，默认地址是 <code>starviewnacos.com:8340</code>，而不是本地常见的 <code>localhost:8848</code>。</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 项目的默认值</span></span><br><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">cloud:</span></span><br><span class="line">    <span class="attr">nacos:</span></span><br><span class="line">      <span class="attr">server-addr:</span> <span class="string">starviewnacos.com:8340</span></span><br></pre></td></tr></table></figure>

<p>也就是说，虽然浏览器里看到的是 <code>8080</code> 端口，但 Java 客户端真正要连的是 <code>starviewnacos.com:8340</code>，对应 gRPC 端口 <code>9340/9341</code>。这是两条完全不同的链路。</p>
<h3 id="第二层：域名解析不稳定"><a href="#第二层：域名解析不稳定" class="headerlink" title="第二层：域名解析不稳定"></a>第二层：域名解析不稳定</h3><p><code>starviewnacos.com</code> 这个域名在 Java 层解析不稳定，有段时间直接抛出 <code>UnknownHostException</code>。即使系统层面能看到 <code>starviewnacos.com -&gt; 127.0.0.1</code>，Java 也不一定能稳定解析。</p>
<h3 id="第三层：代理环境变量干扰"><a href="#第三层：代理环境变量干扰" class="headerlink" title="第三层：代理环境变量干扰"></a>第三层：代理环境变量干扰</h3><p>本机存在代理配置：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">http_proxy=http://127.0.0.1:7897</span><br><span class="line">NO_PROXY=localhost,127.0.0.1</span><br></pre></td></tr></table></figure>

<p>问题在于 <code>starviewnacos.com</code> 不在 <code>NO_PROXY</code> 列表里，所以访问 <code>http://starviewnacos.com:8340/</code> 时请求被代理拦截，返回 <code>502 Bad Gateway</code>。这一步很容易让人误以为”8340 端口本身坏了”。</p>
<h3 id="第四层：namespace-不一致"><a href="#第四层：namespace-不一致" class="headerlink" title="第四层：namespace 不一致"></a>第四层：namespace 不一致</h3><p>即使连上了 Nacos，还有一层容易被忽略的问题——项目实际读取的是 <code>2d2e498b-060e-446f-a900-dbb840c6541b</code> 这个 namespace，而你可能只在 <code>public</code> 下放了配置。日志里的 <code>Ignore the empty nacos configuration</code> 真实含义是：应用去正确地址查配置了，但查的是业务 namespace，配置还在 <code>public</code> 下，所以返回空。</p>
<h3 id="第五层：两套-Nacos-容易误判"><a href="#第五层：两套-Nacos-容易误判" class="headerlink" title="第五层：两套 Nacos 容易误判"></a>第五层：两套 Nacos 容易误判</h3><p>本机同时跑着两套 Nacos——Docker 那套（<code>8848/9848</code>）和本地解压版那套（<code>8340/9340</code>）。如果只看”控制台能打开”或者”localhost 某个端口能访问”，很容易误以为服务已经接到正确的 Nacos 上了。</p>
<h2 id="修复过程"><a href="#修复过程" class="headerlink" title="修复过程"></a>修复过程</h2><h3 id="1-统一使用本地解压版-Nacos"><a href="#1-统一使用本地解压版-Nacos" class="headerlink" title="1. 统一使用本地解压版 Nacos"></a>1. 统一使用本地解压版 Nacos</h3><p>使用 <code>F:\shuiwuai\nacos3.1\nacos</code> 目录的解压版 Nacos，调整端口对齐项目默认值：</p>
<table>
<thead>
<tr>
<th>端口类型</th>
<th>端口号</th>
</tr>
</thead>
<tbody><tr>
<td>API 端口</td>
<td>8340</td>
</tr>
<tr>
<td>Console 端口</td>
<td>8380</td>
</tr>
<tr>
<td>gRPC 端口</td>
<td>9340&#x2F;9341</td>
</tr>
</tbody></table>
<h3 id="2-修复本地启动问题"><a href="#2-修复本地启动问题" class="headerlink" title="2. 修复本地启动问题"></a>2. 修复本地启动问题</h3><ul>
<li>处理旧进程占用 Derby 数据目录导致的重启失败</li>
<li>处理旧 <code>8080</code> 端口被占用导致 console 启动失败的问题</li>
<li>控制台端口从 <code>8080</code> 调整到 <code>8380</code></li>
</ul>
<h3 id="3-对齐-namespace-和配置"><a href="#3-对齐-namespace-和配置" class="headerlink" title="3. 对齐 namespace 和配置"></a>3. 对齐 namespace 和配置</h3><p>将 namespace 固定为 <code>2d2e498b-060e-446f-a900-dbb840c6541b</code>，确保 <code>blade.yaml</code> 和 <code>blade-dev.yaml</code> 在该 namespace 下存在。</p>
<h3 id="4-修复域名解析链路"><a href="#4-修复域名解析链路" class="headerlink" title="4. 修复域名解析链路"></a>4. 修复域名解析链路</h3><ul>
<li>将 <code>starviewnacos.com</code> 固定解析到 <code>127.0.0.1</code></li>
<li>在 <code>NO_PROXY</code> 环境变量中加入 <code>starviewnacos.com</code></li>
<li>验证 <code>http://starviewnacos.com:8340/nacos/</code> 返回 <code>200</code></li>
</ul>
<h2 id="最终验证"><a href="#最终验证" class="headerlink" title="最终验证"></a>最终验证</h2><p>修复后，服务成功注册到 Nacos：</p>
<table>
<thead>
<tr>
<th>服务</th>
<th>实例</th>
<th>状态</th>
</tr>
</thead>
<tbody><tr>
<td>cnsci-auth</td>
<td>192.168.150.1:8101</td>
<td>healthy&#x3D;true ✓</td>
</tr>
<tr>
<td>cnsci-desk</td>
<td>192.168.150.1:8105</td>
<td>healthy&#x3D;true ✓</td>
</tr>
</tbody></table>
<p>当前这套本地解压版 Nacos（<code>8340/9340/9341</code>）已经完全承担起运行时依赖，Docker 那套 Nacos（<code>8848/9848</code>）目前已不再被业务服务使用。</p>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><blockquote>
<p>本次问题不是代码问题，而是本机 Nacos 运行环境与项目默认约定不一致导致。</p>
</blockquote>
<p>以后本地不加启动参数的必要条件：</p>
<ol>
<li><strong>Nacos 必须对齐项目默认地址</strong> <code>starviewnacos.com:8340</code></li>
<li><strong>Nacos 2.x&#x2F;3.x 不仅要主端口通，还要保证 gRPC 端口（9340&#x2F;9341）通</strong></li>
<li><strong>namespace 必须对齐代码默认值，不能只看 <code>public</code></strong></li>
<li><strong>本机代理要排除 <code>starviewnacos.com</code></strong></li>
<li><strong>Java 的主机解析必须能解析 <code>starviewnacos.com</code></strong></li>
</ol>
<h2 id="排障检查清单"><a href="#排障检查清单" class="headerlink" title="排障检查清单"></a>排障检查清单</h2><p>如果后续再出现 Nacos 问题，按这个顺序检查：</p>
<table>
<thead>
<tr>
<th>顺序</th>
<th>检查项</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td><code>starviewnacos.com</code> 是否还能解析到本机</td>
</tr>
<tr>
<td>2</td>
<td>本机代理是否拦截了该域名</td>
</tr>
<tr>
<td>3</td>
<td><code>8340/9340/9341</code> 端口是否仍在监听</td>
</tr>
<tr>
<td>4</td>
<td>namespace 是否还是 <code>2d2e498b-...</code></td>
</tr>
<tr>
<td>5</td>
<td>配置是否仍存在于该 namespace 下</td>
</tr>
<tr>
<td>6</td>
<td>服务进程本身是否还活着、监听端口是否存在</td>
</tr>
</tbody></table>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>本地开发环境的配置问题往往不是单点故障，而是多层环境偏差叠加的结果。排查这类问题时，需要从<strong>网络链路、域名解析、代理设置、namespace 配置</strong>等多个维度逐一排查，才能定位到真正的原因。</p>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>南沙物联网</category>
      </categories>
      <tags>
        <tag>踩坑记录</tag>
        <tag>Nacos</tag>
        <tag>环境配置</tag>
        <tag>IoT平台</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring Boot 借阅流程重构实战：从400行上帝类到三层架构</title>
    <url>/2026/04/14/Spring-Boot%E5%80%9F%E9%98%85%E6%B5%81%E7%A8%8B%E9%87%8D%E6%9E%84/</url>
    <content><![CDATA[<p>在项目迭代过程中，”上帝类”是几乎所有后端开发者都会遇到的经典反模式。本文记录了一次图书管理系统中 <code>LendRecordService</code> 的完整重构过程：如何将一个 400+ 行、混合校验&#x2F;查询&#x2F;写操作&#x2F;编排的上帝类，拆分为遵循 CQRS 思想的三层架构，最终降至 ~150 行的薄编排层。</p>
<span id="more"></span>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>图书管理系统的借阅流程是整个系统的核心业务之一，涉及借阅、归还、续借三大操作。随着功能不断叠加，负责这部分逻辑的 <code>LendRecordService</code> 逐渐膨胀到了 400+ 行，承担了校验、查询、写操作、编排等多重职责。每次新增需求，开发者都不得不在这个庞大的类中小心翼翼地修改，生怕牵一发而动全身。</p>
<p>本文将完整分享这次重构的思考过程、实施方案、代码细节以及代码审查中发现的问题，希望能为遇到类似场景的开发者提供参考。</p>
<h2 id="问题背景：一个”上帝类”的诞生"><a href="#问题背景：一个”上帝类”的诞生" class="headerlink" title="问题背景：一个”上帝类”的诞生"></a>问题背景：一个”上帝类”的诞生</h2><h3 id="什么是上帝类？"><a href="#什么是上帝类？" class="headerlink" title="什么是上帝类？"></a>什么是上帝类？</h3><p><strong>上帝类（God Class）</strong> 是面向对象设计中的经典反模式：一个类承担了过多的职责，知道得太多，做得也太多。它就像团队里那个什么都管的同事——虽然短期内看起来效率很高，但随着业务增长，维护成本会呈指数级上升。</p>
<p>在图书管理系统中，<code>LendRecordService</code> 就是这样一个典型的上帝类：</p>
<table>
<thead>
<tr>
<th>职责</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>借阅资格校验</td>
<td>检查图书状态、预约队列、用户限额、重复借阅</td>
</tr>
<tr>
<td>写操作</td>
<td>插入 lend_record、更新 book 状态、创建 bookwithuser、履行预约</td>
</tr>
<tr>
<td>上下文加载</td>
<td>归还&#x2F;续借时加载 LendRecord + BookWithUser + Book + User + LendRule</td>
</tr>
<tr>
<td>自愈机制</td>
<td>LendRecord 缺失时补建记录</td>
</tr>
<tr>
<td>结果构建</td>
<td>组装返回给前端的 Map</td>
</tr>
</tbody></table>
<h3 id="五大代码异味"><a href="#五大代码异味" class="headerlink" title="五大代码异味"></a>五大代码异味</h3><p>经过仔细分析，我们识别出了 5 个关键的 Code Smells：</p>
<p><strong>GOD-01：上帝类（严重程度：高）</strong></p>
<p>单一服务类 400+ 行，混合了校验、查询、写操作、编排等多重职责。这直接导致可维护性和可测试性大幅下降。任何一个小改动都需要理解整个类的逻辑。</p>
<p><strong>DUP-01：校验逻辑重复（严重程度：高）</strong></p>
<p><code>borrowBook()</code> 和 <code>checkCanBorrow()</code> 中存在借阅资格校验逻辑的重复，而且两处逻辑<strong>不一致</strong>。<code>borrowBook()</code> 中的校验包含 5 个步骤，而 <code>checkCanBorrow()</code> 中的校验只有 4 个步骤，预约队列和限额的校验逻辑存在差异。这种不一致可能导致 <code>checkCanBorrow()</code> 返回”可借阅”，但 <code>borrowBook()</code> 执行时被另一处校验拦截。</p>
<p><strong>DUP-02：上下文加载冗长重复（严重程度：中）</strong></p>
<p>归还和续借流程中，需要加载 LendRecord + BookWithUser + Book + User + LendRule 五个对象。这段上下文加载代码在 <code>returnBook()</code> 和 <code>renewBook()</code> 中几乎完全相同，冗长且重复。</p>
<p><strong>TEST-01：写操作难以测试（严重程度：高）</strong></p>
<p>写操作（插入 lend_record、更新 book 状态等）直接嵌入在 <code>borrowBook()</code> 方法中，无法在不触发完整业务流程的情况下单独测试。这意味着要测试”借阅记录插入是否正确”，就不得不走完整个借阅流程，构造大量前置条件。</p>
<p><strong>HEAL-01：自愈逻辑缺乏封装（严重程度：中）</strong></p>
<p>当 LendRecord 缺失但 BookWithUser 存在时，系统有一套自愈逻辑来补建记录。但这段逻辑散落在业务方法中，缺乏独立封装，难以理解和维护。</p>
<p><img data-src="/images/posts/Spring-Boot%E5%80%9F%E9%98%85%E6%B5%81%E7%A8%8B%E9%87%8D%E6%9E%84/fig1_001.jpg" alt="重构前的架构问题分析"><br><em>图1：重构前的架构问题分析</em></p>
<h2 id="重构方案：三层架构-CQRS"><a href="#重构方案：三层架构-CQRS" class="headerlink" title="重构方案：三层架构 + CQRS"></a>重构方案：三层架构 + CQRS</h2><h3 id="设计思路"><a href="#设计思路" class="headerlink" title="设计思路"></a>设计思路</h3><p>经过分析，我们决定采用<strong>命令查询职责分离（CQRS）</strong>的思想，将 <code>LendRecordService</code> 拆分为三层：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Before:</span><br><span class="line">  Controller → LendRecordService (400+ lines, mixed validation + write + orchestration)</span><br><span class="line"></span><br><span class="line">After:</span><br><span class="line">  Controller → LendRecordService (thin orchestration, ~150 lines)</span><br><span class="line">                   ├── BorrowEligibilityService (read-side validation)</span><br><span class="line">                   │       └── BorrowEligibilityResult (immutable context)</span><br><span class="line">                   ├── LendRecordWriteService (write-side operations)</span><br><span class="line">                   └── loadCurrentBorrowContext() + buildReturnResult()</span><br><span class="line">                           └── CurrentBorrowContext (immutable context)</span><br></pre></td></tr></table></figure>

<h3 id="各层职责划分"><a href="#各层职责划分" class="headerlink" title="各层职责划分"></a>各层职责划分</h3><table>
<thead>
<tr>
<th>层级</th>
<th>类名</th>
<th>职责</th>
<th>事务控制</th>
</tr>
</thead>
<tbody><tr>
<td>读侧校验</td>
<td><code>BorrowEligibilityService</code></td>
<td>借阅前的资格检查</td>
<td><code>@Transactional(readOnly = true)</code></td>
</tr>
<tr>
<td>写侧操作</td>
<td><code>LendRecordWriteService</code></td>
<td>借阅&#x2F;归还&#x2F;续借的数据库写操作</td>
<td>无独立事务（由调用方控制）</td>
</tr>
<tr>
<td>编排层</td>
<td><code>LendRecordService</code></td>
<td>协调校验和写操作，构建返回结果</td>
<td><code>@Transactional(rollbackFor = Exception.class)</code></td>
</tr>
</tbody></table>
<p>这里有几个关键的设计决策：</p>
<p><strong>为什么 <code>BorrowEligibilityService</code> 用只读事务？</strong> 校验操作只涉及查询，标注 <code>readOnly = true</code> 可以让数据库进行优化（如不加写锁），同时也从语义上明确了这个方法的职责边界——它只做检查，不做修改。</p>
<p><strong>为什么 <code>LendRecordWriteService</code> 不加事务注解？</strong> 事务应该由编排层统一控制。如果写操作服务自带事务，校验和写操作就不在同一个事务中执行，可能导致校验通过后、执行写操作前被其他请求修改数据，产生并发问题。</p>
<p><strong>为什么引入不可变上下文对象？</strong> <code>BorrowEligibilityResult</code> 和 <code>CurrentBorrowContext</code> 都是只有构造函数和 getter、没有 setter 的不可变对象。这确保了校验通过后的数据不会被意外修改，同时也作为方法间的”数据契约”，明确传递了每一层需要什么数据。</p>
<p><img data-src="/images/posts/Spring-Boot%E5%80%9F%E9%98%85%E6%B5%81%E7%A8%8B%E9%87%8D%E6%9E%84/fig2_001.jpg" alt="三层架构设计"><br><em>图2：重构后的三层架构设计</em></p>
<h2 id="核心实现：逐层拆解"><a href="#核心实现：逐层拆解" class="headerlink" title="核心实现：逐层拆解"></a>核心实现：逐层拆解</h2><h3 id="第一层：BorrowEligibilityService-—-读侧校验"><a href="#第一层：BorrowEligibilityService-—-读侧校验" class="headerlink" title="第一层：BorrowEligibilityService — 读侧校验"></a>第一层：BorrowEligibilityService — 读侧校验</h3><p>这是重构的核心。我们将散落在 <code>borrowBook()</code> 和 <code>checkCanBorrow()</code> 中的重复校验逻辑统一到一个 <code>evaluate()</code> 方法中。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">BorrowEligibilityService</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> BookMapper bookMapper;</span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> UserMapper userMapper;</span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> LendRuleMapper lendRuleMapper;</span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> LendRecordMapper lendRecordMapper;</span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> ReservationMapper reservationMapper;</span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> BookWithUserMapper bookWithUserMapper;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 评估借阅资格 — 统一校验入口</span></span><br><span class="line"><span class="comment">     * 成功返回 BorrowEligibilityResult（不可变），失败抛出 ServiceException</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Transactional(readOnly = true)</span></span><br><span class="line">    <span class="keyword">public</span> BorrowEligibilityResult <span class="title function_">evaluate</span><span class="params">(Long readerId, String isbn)</span> &#123;</span><br><span class="line">        <span class="type">Book</span> <span class="variable">book</span> <span class="operator">=</span> loadBookOrThrow(isbn);</span><br><span class="line">        <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> loadUserOrThrow(readerId);</span><br><span class="line">        validateBookStatus(book);</span><br><span class="line">        validateReservationQueue(isbn, readerId);</span><br><span class="line">        <span class="type">LendRule</span> <span class="variable">rule</span> <span class="operator">=</span> loadRuleOrThrow(user);</span><br><span class="line">        <span class="type">long</span> <span class="variable">count</span> <span class="operator">=</span> countCurrentBorrowed(readerId);</span><br><span class="line">        ensureUnderLimit(count, rule);</span><br><span class="line">        ensureNotDuplicateBorrow(readerId, isbn);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">BorrowEligibilityResult</span>(book, user, rule, count, isbn, readerId);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>evaluate()</code> 方法的校验流程清晰明了，依次执行 8 个步骤：</p>
<ol>
<li><strong>loadBookOrThrow()</strong> — 检查图书是否存在</li>
<li><strong>loadUserOrThrow()</strong> — 检查用户是否存在</li>
<li><strong>validateBookStatus()</strong> — 检查图书状态是否可借</li>
<li><strong>validateReservationQueue()</strong> — 检查预约队列，当前读者是否排在队首</li>
<li><strong>loadRuleOrThrow()</strong> — 加载借阅规则</li>
<li><strong>countCurrentBorrowed()</strong> — 查询当前借阅数</li>
<li><strong>ensureUnderLimit()</strong> — 检查是否超过借阅限额</li>
<li><strong>ensureNotDuplicateBorrow()</strong> — 检查是否重复借阅</li>
</ol>
<p>每个步骤都是一个独立的 private 方法，方法名即文档，读起来就像自然语言描述。任何一步失败，都会抛出 <code>ServiceException</code>，调用方可以统一 try&#x2F;catch 处理。</p>
<p>返回的 <code>BorrowEligibilityResult</code> 是一个不可变对象，封装了校验通过后的所有上下文数据：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">BorrowEligibilityResult</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> Book book;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> User user;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> LendRule rule;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="type">long</span> currentBorrowedCount;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> String isbn;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> Long readerId;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 构造函数 + getter，无 setter（不可变）</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="第二层：LendRecordWriteService-—-写侧操作"><a href="#第二层：LendRecordWriteService-—-写侧操作" class="headerlink" title="第二层：LendRecordWriteService — 写侧操作"></a>第二层：LendRecordWriteService — 写侧操作</h3><p>写操作服务封装了三种核心写操作，每个方法只负责纯写操作，不包含校验逻辑：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">LendRecordWriteService</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 执行借阅写操作：</span></span><br><span class="line"><span class="comment">     * 1. 插入 lend_record</span></span><br><span class="line"><span class="comment">     * 2. 更新 book 状态为&quot;已借出&quot;</span></span><br><span class="line"><span class="comment">     * 3. 创建 bookwithuser 关联</span></span><br><span class="line"><span class="comment">     * 4. 履行预约（如果当前读者有预约）</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> LendRecord <span class="title function_">createBorrow</span><span class="params">(BorrowEligibilityResult eligibility,</span></span><br><span class="line"><span class="params">                                    LendRecord input, Date now)</span> &#123;</span><br><span class="line">        <span class="comment">// 构建 LendRecord</span></span><br><span class="line">        <span class="type">LendRecord</span> <span class="variable">record</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">LendRecord</span>();</span><br><span class="line">        record.setIsbn(eligibility.getIsbn());</span><br><span class="line">        record.setReaderId(eligibility.getReaderId());</span><br><span class="line">        record.setBookName(input.getBookName());</span><br><span class="line">        record.setReaderName(input.getReaderName());</span><br><span class="line">        record.setLendDate(now);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 计算应还日期 = 当前时间 + 借阅规则天数</span></span><br><span class="line">        <span class="type">LocalDateTime</span> <span class="variable">deadline</span> <span class="operator">=</span> DateUtils.toLocalDateTime(now)</span><br><span class="line">            .plusDays(eligibility.getRule().getDays());</span><br><span class="line">        record.setDeadtime(Date.from(deadline</span><br><span class="line">            .atZone(ZoneId.systemDefault()).toInstant()));</span><br><span class="line">        record.setStatus(<span class="string">&quot;0&quot;</span>); <span class="comment">// 未归还</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 插入 lend_record（带返回值检查）</span></span><br><span class="line">        <span class="type">int</span> <span class="variable">inserted</span> <span class="operator">=</span> lendRecordMapper.insert(record);</span><br><span class="line">        <span class="keyword">if</span> (inserted &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">ServiceException</span>(<span class="string">&quot;借阅记录创建失败&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 更新 book 状态</span></span><br><span class="line">        <span class="type">Book</span> <span class="variable">book</span> <span class="operator">=</span> eligibility.getBook();</span><br><span class="line">        book.setStatus(<span class="string">&quot;2&quot;</span>); <span class="comment">// 已借出</span></span><br><span class="line">        bookMapper.updateById(book);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 创建 bookwithuser 关联</span></span><br><span class="line">        <span class="type">BookWithUser</span> <span class="variable">bwu</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">BookWithUser</span>();</span><br><span class="line">        bwu.setIsbn(eligibility.getIsbn());</span><br><span class="line">        bwu.setReaderId(eligibility.getReaderId());</span><br><span class="line">        bwu.setBookName(record.getBookName());</span><br><span class="line">        bwu.setReaderName(record.getReaderName());</span><br><span class="line">        bwu.setLendDate(now);</span><br><span class="line">        bwu.setDeadtime(record.getDeadtime());</span><br><span class="line">        bwu.setProlong(<span class="number">0</span>);</span><br><span class="line">        bookWithUserMapper.insert(bwu);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 履行预约（如有）</span></span><br><span class="line">        fulfillReservationIfExists(</span><br><span class="line">            eligibility.getIsbn(), eligibility.getReaderId());</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> record;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 执行归还写操作</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">completeReturn</span><span class="params">(CurrentBorrowContext context, Date now)</span> &#123;</span><br><span class="line">        <span class="comment">// 更新 lend_record</span></span><br><span class="line">        <span class="type">LendRecord</span> <span class="variable">record</span> <span class="operator">=</span> context.getLendRecord();</span><br><span class="line">        record.setStatus(<span class="string">&quot;1&quot;</span>); <span class="comment">// 已归还</span></span><br><span class="line">        record.setReturnDate(now);</span><br><span class="line">        lendRecordMapper.updateById(record);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 更新 book 状态（null guard 保护自愈场景）</span></span><br><span class="line">        <span class="type">Book</span> <span class="variable">book</span> <span class="operator">=</span> context.getBook();</span><br><span class="line">        <span class="keyword">if</span> (book != <span class="literal">null</span>) &#123;</span><br><span class="line">            book.setStatus(<span class="string">&quot;1&quot;</span>); <span class="comment">// 可借</span></span><br><span class="line">            bookMapper.updateById(book);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 删除 bookwithuser 关联</span></span><br><span class="line">        <span class="type">BookWithUser</span> <span class="variable">bwu</span> <span class="operator">=</span> context.getBookWithUser();</span><br><span class="line">        <span class="keyword">if</span> (bwu != <span class="literal">null</span>) &#123;</span><br><span class="line">            bookWithUserMapper.deleteById(bwu.getId());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 执行续借写操作</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">renewBorrow</span><span class="params">(CurrentBorrowContext context,</span></span><br><span class="line"><span class="params">                           LocalDateTime newDeadline)</span> &#123;</span><br><span class="line">        <span class="comment">// 更新 bookwithuser 的应还日期和续借次数</span></span><br><span class="line">        <span class="type">BookWithUser</span> <span class="variable">bwu</span> <span class="operator">=</span> context.getBookWithUser();</span><br><span class="line">        bwu.setDeadtime(Date.from(newDeadline</span><br><span class="line">            .atZone(ZoneId.systemDefault()).toInstant()));</span><br><span class="line">        bwu.setProlong(bwu.getProlong() + <span class="number">1</span>);</span><br><span class="line">        bookWithUserMapper.updateById(bwu);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 同步更新 lend_record</span></span><br><span class="line">        <span class="type">LendRecord</span> <span class="variable">record</span> <span class="operator">=</span> context.getLendRecord();</span><br><span class="line">        record.setDeadtime(bwu.getDeadtime());</span><br><span class="line">        lendRecordMapper.updateById(record);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>设计亮点在于：<strong>输入参数即上下文</strong>。每个方法接收不可变的上下文对象（<code>BorrowEligibilityResult</code> 或 <code>CurrentBorrowContext</code>），所有必要数据都已预加载，方法内部不需要额外的数据库查询。这使得方法完全独立、可预测，也非常容易测试。</p>
<h3 id="第三层：LendRecordService-—-薄编排层"><a href="#第三层：LendRecordService-—-薄编排层" class="headerlink" title="第三层：LendRecordService — 薄编排层"></a>第三层：LendRecordService — 薄编排层</h3><p>重构后的 <code>LendRecordService</code> 从 400+ 行瘦身为 ~150 行的薄编排层，不再包含任何业务逻辑：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">LendRecordService</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> BorrowEligibilityService borrowEligibilityService;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> LendRecordWriteService lendRecordWriteService;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 借阅图书 — 编排层</span></span><br><span class="line"><span class="comment">     * 1. 校验借阅资格</span></span><br><span class="line"><span class="comment">     * 2. 执行借阅写操作</span></span><br><span class="line"><span class="comment">     * 3. 构建返回结果</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Transactional(rollbackFor = Exception.class)</span></span><br><span class="line">    <span class="keyword">public</span> Map&lt;String, Object&gt; <span class="title function_">borrowBook</span><span class="params">(LendRecord lendRecord)</span> &#123;</span><br><span class="line">        <span class="comment">// 入参校验</span></span><br><span class="line">        <span class="keyword">if</span> (lendRecord == <span class="literal">null</span> || lendRecord.getReaderId() == <span class="literal">null</span></span><br><span class="line">            || lendRecord.getIsbn() == <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> Result.error(<span class="string">&quot;参数不完整&quot;</span>).toMap();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 1. 校验借阅资格（读侧）</span></span><br><span class="line">        BorrowEligibilityResult eligibility;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            eligibility = borrowEligibilityService.evaluate(</span><br><span class="line">                lendRecord.getReaderId(), lendRecord.getIsbn());</span><br><span class="line">        &#125; <span class="keyword">catch</span> (ServiceException e) &#123;</span><br><span class="line">            <span class="keyword">return</span> Result.error(e.getMessage()).toMap();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2. 执行借阅写操作（写侧）</span></span><br><span class="line">        <span class="type">Date</span> <span class="variable">now</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Date</span>();</span><br><span class="line">        <span class="type">LendRecord</span> <span class="variable">created</span> <span class="operator">=</span> lendRecordWriteService.createBorrow(</span><br><span class="line">            eligibility, lendRecord, now);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 3. 构建返回结果</span></span><br><span class="line">        Map&lt;String, Object&gt; result = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;();</span><br><span class="line">        result.put(<span class="string">&quot;code&quot;</span>, <span class="string">&quot;0&quot;</span>);</span><br><span class="line">        result.put(<span class="string">&quot;msg&quot;</span>, <span class="string">&quot;借阅成功&quot;</span>);</span><br><span class="line">        result.put(<span class="string">&quot;data&quot;</span>, created);</span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>对比重构前后的 <code>borrowBook()</code> 方法：</p>
<table>
<thead>
<tr>
<th>维度</th>
<th>Before</th>
<th>After</th>
</tr>
</thead>
<tbody><tr>
<td>方法行数</td>
<td>~80 行（校验 + 写 + 构建结果）</td>
<td>~20 行（编排 + 构建结果）</td>
</tr>
<tr>
<td>校验逻辑</td>
<td>内联重复</td>
<td>统一委托给 <code>BorrowEligibilityService</code></td>
</tr>
<tr>
<td>写操作</td>
<td>内联</td>
<td>封装在 <code>LendRecordWriteService</code></td>
</tr>
<tr>
<td>可测试性</td>
<td>只能集成测试</td>
<td>每层可独立 mock 测试</td>
</tr>
</tbody></table>
<h3 id="API-兼容：checkCanBorrow-的优雅处理"><a href="#API-兼容：checkCanBorrow-的优雅处理" class="headerlink" title="API 兼容：checkCanBorrow 的优雅处理"></a>API 兼容：checkCanBorrow 的优雅处理</h3><p>重构中一个值得注意的细节是 <code>checkCanBorrow()</code> 方法的处理。原始方法返回 <code>&#123;canBorrow: false, reason: &quot;...&quot;&#125;</code> 格式的 Map，而 <code>evaluate()</code> 失败时抛异常。为了保持前端 API 兼容，我们在编排层做了一个简单的适配：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> Map&lt;String, Object&gt; <span class="title function_">checkCanBorrow</span><span class="params">(Long readerId, String isbn)</span> &#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        borrowEligibilityService.evaluate(readerId, isbn);</span><br><span class="line">        Map&lt;String, Object&gt; result = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;();</span><br><span class="line">        result.put(<span class="string">&quot;canBorrow&quot;</span>, <span class="literal">true</span>);</span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (ServiceException e) &#123;</span><br><span class="line">        Map&lt;String, Object&gt; result = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;();</span><br><span class="line">        result.put(<span class="string">&quot;canBorrow&quot;</span>, <span class="literal">false</span>);</span><br><span class="line">        result.put(<span class="string">&quot;reason&quot;</span>, e.getMessage());</span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>通过 try&#x2F;catch 将异常转换为原始格式，实现了<strong>内部实现的统一</strong>与<strong>外部接口的兼容</strong>。</p>
<p><img data-src="/images/posts/Spring-Boot%E5%80%9F%E9%98%85%E6%B5%81%E7%A8%8B%E9%87%8D%E6%9E%84/fig3_001.jpg" alt="校验流程对比"><br><em>图3：重构前后校验逻辑对比</em></p>
<h2 id="代码审查：6-个问题的启示"><a href="#代码审查：6-个问题的启示" class="headerlink" title="代码审查：6 个问题的启示"></a>代码审查：6 个问题的启示</h2><p>重构完成后，我们进行了代码审查，发现了 6 个问题。这些问题极具代表性，几乎是所有提取重构都会遇到的坑：</p>
<h3 id="Issue-1：API-合约断裂（严重）"><a href="#Issue-1：API-合约断裂（严重）" class="headerlink" title="Issue #1：API 合约断裂（严重）"></a>Issue #1：API 合约断裂（严重）</h3><p><strong>问题</strong>：<code>checkCanBorrow()</code> 原本返回 <code>&#123;canBorrow: false, reason: &quot;...&quot;&#125;</code>，重构后 <code>evaluate()</code> 失败时抛异常，前端会收到 500 错误而非预期的 JSON。</p>
<p><strong>修复</strong>：添加 try&#x2F;catch 将异常转换为原始 Map 格式。</p>
<p><strong>启示</strong>：重构内部实现时，必须确保公共 API 的返回格式不变。这是提取重构中最容易忽视的问题——我们关注了代码结构，却忘了关注契约。</p>
<h3 id="Issue-2：insert-返回值检查丢失（中等）"><a href="#Issue-2：insert-返回值检查丢失（中等）" class="headerlink" title="Issue #2：insert 返回值检查丢失（中等）"></a>Issue #2：insert 返回值检查丢失（中等）</h3><p><strong>问题</strong>：原始代码在 <code>lendRecordMapper.insert()</code> 后检查 <code>inserted &lt;= 0</code>，提取时遗漏了这个检查。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Before（遗漏）</span></span><br><span class="line">lendRecordMapper.insert(record);</span><br><span class="line"></span><br><span class="line"><span class="comment">// After（修复）</span></span><br><span class="line"><span class="type">int</span> <span class="variable">inserted</span> <span class="operator">=</span> lendRecordMapper.insert(record);</span><br><span class="line"><span class="keyword">if</span> (inserted &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">ServiceException</span>(<span class="string">&quot;借阅记录创建失败&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>启示</strong>：原方法中的返回值检查是防御性代码，搬运时容易被遗漏。</p>
<h3 id="Issue-3：null-guard-丢失（中等）"><a href="#Issue-3：null-guard-丢失（中等）" class="headerlink" title="Issue #3：null guard 丢失（中等）"></a>Issue #3：null guard 丢失（中等）</h3><p><strong>问题</strong>：原始 <code>returnBook()</code> 中有 <code>if (book != null)</code> 保护，提取到 <code>completeReturn()</code> 时遗漏。在自愈场景中 book 可能为 null，会导致 NPE。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Before（遗漏）</span></span><br><span class="line"><span class="type">Book</span> <span class="variable">book</span> <span class="operator">=</span> context.getBook();</span><br><span class="line">book.setStatus(<span class="string">&quot;1&quot;</span>);  <span class="comment">// NPE if book is null!</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// After（修复）</span></span><br><span class="line"><span class="type">Book</span> <span class="variable">book</span> <span class="operator">=</span> context.getBook();</span><br><span class="line"><span class="keyword">if</span> (book != <span class="literal">null</span>) &#123;</span><br><span class="line">    book.setStatus(<span class="string">&quot;1&quot;</span>);</span><br><span class="line">    bookMapper.updateById(book);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>启示</strong>：原方法中的 if&#x2F;null 检查都有其存在的原因（通常是为了处理边界情况），搬运时一个都不能少。</p>
<h3 id="Issue-4：未使用参数（低）"><a href="#Issue-4：未使用参数（低）" class="headerlink" title="Issue #4：未使用参数（低）"></a>Issue #4：未使用参数（低）</h3><p><strong>问题</strong>：<code>loadCurrentBorrowContext()</code> 接受一个 <code>boolean includeBook</code> 参数，但从未使用。</p>
<p><strong>修复</strong>：移除未使用的参数。</p>
<h3 id="Issue-5：重复查询（中等）"><a href="#Issue-5：重复查询（中等）" class="headerlink" title="Issue #5：重复查询（中等）"></a>Issue #5：重复查询（中等）</h3><p><strong>问题</strong>：自愈路径中先通过 <code>bookWithUserMapper</code> 查到 BookWithUser，然后又用相同条件重新查了一次。</p>
<p><strong>修复</strong>：复用第一次查询的结果。</p>
<h3 id="Issue-6：重复私有方法（低）"><a href="#Issue-6：重复私有方法（低）" class="headerlink" title="Issue #6：重复私有方法（低）"></a>Issue #6：重复私有方法（低）</h3><p><strong>问题</strong>：<code>BorrowEligibilityService</code> 和 <code>LendRecordWriteService</code> 各有一个完全相同的 <code>convertToLocalDateTime()</code> 方法。</p>
<p><strong>修复</strong>：提取到共享的 <code>DateUtils</code> 工具类：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">DateUtils</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="title function_">DateUtils</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// 工具类禁止实例化</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * Date → LocalDateTime</span></span><br><span class="line"><span class="comment">     * 通过 Timestamp 桥接，避免时区丢失</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> LocalDateTime <span class="title function_">toLocalDateTime</span><span class="params">(Date date)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (date == <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Timestamp</span>(date.getTime()).toLocalDateTime();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>选择 <code>Timestamp</code> 桥接而非 <code>Date.toInstant()</code> 的原因是：<code>Timestamp.toLocalDateTime()</code> 直接使用系统默认时区，而 <code>Date.toInstant()</code> 使用 UTC 时区，在转换时容易忘记加 <code>ZoneId</code> 导致 8 小时偏差。</p>
<h3 id="代码审查修复汇总"><a href="#代码审查修复汇总" class="headerlink" title="代码审查修复汇总"></a>代码审查修复汇总</h3><table>
<thead>
<tr>
<th>#</th>
<th>问题</th>
<th>严重度</th>
<th>修复方式</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td>API 合约断裂</td>
<td>严重</td>
<td>try&#x2F;catch 转换异常为 Map</td>
</tr>
<tr>
<td>2</td>
<td>insert 返回值检查丢失</td>
<td>中等</td>
<td>恢复 <code>inserted &lt;= 0</code> 检查</td>
</tr>
<tr>
<td>3</td>
<td>null guard 丢失</td>
<td>中等</td>
<td>恢复 <code>if (book != null)</code></td>
</tr>
<tr>
<td>4</td>
<td>未使用参数</td>
<td>低</td>
<td>移除 boolean 参数</td>
</tr>
<tr>
<td>5</td>
<td>重复查询</td>
<td>中等</td>
<td>复用已有查询结果</td>
</tr>
<tr>
<td>6</td>
<td>重复私有方法</td>
<td>低</td>
<td>提取到 DateUtils</td>
</tr>
</tbody></table>
<p><img data-src="/images/posts/Spring-Boot%E5%80%9F%E9%98%85%E6%B5%81%E7%A8%8B%E9%87%8D%E6%9E%84/fig4_001.jpg" alt="代码审查问题修复"><br><em>图4：代码审查中发现的 6 个典型问题</em></p>
<h2 id="实施过程：四个阶段，九次提交"><a href="#实施过程：四个阶段，九次提交" class="headerlink" title="实施过程：四个阶段，九次提交"></a>实施过程：四个阶段，九次提交</h2><p>整个重构分为四个阶段，在 <code>代码瘦身</code> 分支上完成（基线 <code>3415d11</code>）：</p>
<p><strong>Phase 1：提取校验层（Commits: 0851071 → 4a04c56）</strong></p>
<p>创建 <code>BorrowEligibilityResult</code> 不可变对象，提取 <code>evaluate()</code> 方法，让 <code>checkCanBorrow()</code> 复用校验逻辑。这一阶段解决了 DUP-01（校验逻辑重复）的问题。</p>
<p><strong>Phase 2：提取写操作层（Commits: 75a9acf → 69a859a）</strong></p>
<p>将借阅写操作从 <code>borrowBook()</code> 中提取到 <code>LendRecordWriteService.createBorrow()</code>。<code>borrowBook()</code> 从 ~80 行降为 ~20 行，只剩下编排逻辑和结果构建。</p>
<p><strong>Phase 3：归还&#x2F;续借重构（Commits: 69a859a → f109846）</strong></p>
<p>提取 <code>completeReturn()</code> 和 <code>renewBorrow()</code> 写操作方法，创建 <code>CurrentBorrowContext</code> 统一上下文加载，解决了 DUP-02（上下文加载重复）的问题。</p>
<p><strong>Phase 4：代码审查修复（Commits: bb568e9 → 3be9c2b）</strong></p>
<p>修复代码审查中发现的 6 个问题，提取 <code>DateUtils</code> 工具类。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">0851071 refactor: add lending context types</span><br><span class="line">d0e4d1e refactor: extract borrow eligibility service</span><br><span class="line">4a04c56 refactor: reuse eligibility logic in checkCanBorrow</span><br><span class="line">75a9acf refactor: extract borrow write path</span><br><span class="line">493aef0 refactor: orchestrate borrow flow via helper services</span><br><span class="line">69a859a refactor: add return and renew write helpers</span><br><span class="line">f109846 refactor: add current borrow context loader</span><br><span class="line">bb568e9 fix: code review fixes for borrowing flow cleanup</span><br><span class="line">3be9c2b simplify: extract DateUtils, remove duplicate query and unnecessary comments</span><br></pre></td></tr></table></figure>

<h2 id="测试验证"><a href="#测试验证" class="headerlink" title="测试验证"></a>测试验证</h2><p>重构后的测试覆盖大幅提升：</p>
<table>
<thead>
<tr>
<th>测试类</th>
<th>用例数</th>
<th>覆盖场景</th>
</tr>
</thead>
<tbody><tr>
<td><code>BorrowEligibilityServiceTest</code></td>
<td>4</td>
<td>正常通过、图书不可借、超过限额、重复借阅</td>
</tr>
<tr>
<td><code>LendRecordWriteServiceTest</code></td>
<td>4</td>
<td>借阅创建、归还完成、续借成功、自愈补建</td>
</tr>
<tr>
<td><code>LendRecordServiceTest</code></td>
<td>15</td>
<td>完整编排流程：借阅&#x2F;归还&#x2F;续借 + 边界条件</td>
</tr>
</tbody></table>
<p>总计新增 23 个测试用例，全量 86 个测试全部通过（<code>mvn test</code> → BUILD SUCCESS）。</p>
<p>关键回归验证项：</p>
<ul>
<li>前端控制器文件未修改（无 controller drift）</li>
<li><code>checkCanBorrow()</code> 返回格式保持 <code>&#123;canBorrow, reason&#125;</code></li>
<li><code>borrowBook()</code>、<code>returnBook()</code>、<code>renewBook()</code> 返回格式不变</li>
<li>数据库表结构和 SQL 未修改</li>
</ul>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><h3 id="什么时候该重构？"><a href="#什么时候该重构？" class="headerlink" title="什么时候该重构？"></a>什么时候该重构？</h3><p>当一个服务类超过 300 行，或者你发现修改一个功能需要理解整个类的逻辑时，就该考虑重构了。在我们的案例中，400+ 行的 <code>LendRecordService</code> 已经是明显的信号。</p>
<h3 id="提取重构的核心步骤"><a href="#提取重构的核心步骤" class="headerlink" title="提取重构的核心步骤"></a>提取重构的核心步骤</h3><ol>
<li><strong>识别职责边界</strong>：分析当前类承担了哪些职责，每个职责的输入和输出是什么</li>
<li><strong>创建上下文对象</strong>：定义不可变的数据载体，明确方法间的数据契约</li>
<li><strong>逐层提取</strong>：先提取校验层（读侧），再提取写操作层（写侧），最后瘦身为编排层</li>
<li><strong>保持 API 兼容</strong>：确保公共方法的返回格式不变，内部实现变更不影响调用方</li>
<li><strong>代码审查</strong>：重点关注原方法中的防御性代码是否完整搬运</li>
</ol>
<h3 id="提取重构最常见的坑"><a href="#提取重构最常见的坑" class="headerlink" title="提取重构最常见的坑"></a>提取重构最常见的坑</h3><p>根据这次代码审查的经验，提取重构最容易出的问题有三个：</p>
<p><strong>边界条件丢失</strong>：原方法中的 null 检查、返回值检查、异常处理等防御性代码，在搬运过程中容易被遗漏。代码审查时必须逐行对比原方法和提取后的方法，确认每个 if&#x2F;try&#x2F;catch 都存在。</p>
<p><strong>API 合约断裂</strong>：重构内部实现时，公共方法的返回格式可能在不经意间发生变化。特别是异常处理策略的变更（如从返回错误 Map 改为抛异常），需要额外注意调用方的期望。</p>
<p><strong>重复代码引入</strong>：看似提取到了新类，但如果新类之间又产生了重复代码（如我们的 <code>convertToLocalDateTime()</code>），就说明提取还不够彻底，需要进一步抽象共享工具。</p>
<h3 id="CQRS-在日常开发中的应用"><a href="#CQRS-在日常开发中的应用" class="headerlink" title="CQRS 在日常开发中的应用"></a>CQRS 在日常开发中的应用</h3><p>CQRS 不只是微服务架构中的大杀器，在日常的单体应用重构中同样适用。核心思想很简单：<strong>把读操作和写操作分开</strong>。读操作用只读事务，写操作不带事务（由编排层统一控制），编排层只负责协调。这种分层方式让每一层都变得简单、可测试、可独立演进。</p>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>这次重构将一个 400+ 行的上帝类拆分为 ~150 行的编排层 + 两个专职服务类，新增 23 个测试用例，解决了校验逻辑不一致、写操作难以测试等核心问题。整个过程通过 4 个阶段、9 次提交完成，代码审查发现并修复了 6 个问题。重构的关键不是写出”完美的代码”，而是通过持续的小步改进，让代码库保持健康和可维护。</p>
]]></content>
      <categories>
        <category>后端开发</category>
      </categories>
      <tags>
        <tag>Spring Boot</tag>
        <tag>重构</tag>
        <tag>CQRS</tag>
        <tag>Java</tag>
        <tag>代码质量</tag>
      </tags>
  </entry>
  <entry>
    <title>一行 parkId 改不出越权：契约测试如何锁死 Token 身份来源</title>
    <url>/2026/07/13/Token%E8%BA%AB%E4%BB%BD%E8%B6%8A%E6%9D%83%E5%A5%91%E7%BA%A6%E6%B5%8B%E8%AF%95/</url>
    <content><![CDATA[<p>多园区切换接口 <code>GET /user/userParkSwitch/&#123;parkId&#125;</code> 能不能被前端伪造身份？改一行 <code>parkId</code> 就能越权切到其他园区吗？</p>
<p>这是多园区改造最容易出现的”看起来安全、其实没拦住”的安全漏洞。本文用 <strong>4 个测试用例 + 1 次反向变异验证</strong>，把这条接口的身份来源、授权顺序、Redis 更新顺序全部锁死，让”以后谁动了这块代码，CI 立刻红”。</p>
<span id="more"></span>

<div class="audio-player">
  <div class="audio-section">
    <h4>🎧 文章导读</h4>
    <audio controls preload="metadata">
      <source src="/audio/Token身份越权契约测试/narration.mp3" type="audio/mpeg">
    </audio>
  </div>
  <div class="audio-section">
    <h4>🎵 背景音乐</h4>
    <audio controls loop preload="metadata">
      <source src="/audio/Token身份越权契约测试/bgm.mp3" type="audio/mpeg">
    </audio>
  </div>
</div>

<p><img data-src="/images/posts/Token%E8%BA%AB%E4%BB%BD%E8%B6%8A%E6%9D%83%E5%A5%91%E7%BA%A6%E6%B5%8B%E8%AF%95/fig1.png" alt="userParkSwitch 接口的安全契约锁点"></p>
<p><em>图1：身份来源、授权顺序、Redis 更新顺序三道契约锁点</em></p>
<h2 id="一、结论先行"><a href="#一、结论先行" class="headerlink" title="一、结论先行"></a>一、结论先行</h2><p><code>userParkSwitch</code> 接口<strong>不会把前端传入的信息当作用户身份</strong>：</p>
<ol>
<li>前端只提供目标 <code>parkId</code>。</li>
<li>后端通过 <code>SecurityExtUtils.getCurrentLoginInfo()</code> 从当前登录 Token 对应的登录态取得 <code>userId</code>。</li>
<li>后端用该 <code>userId</code> 查询允许访问的园区，并检查前端传入的 <code>parkId</code> 是否在允许集合内。</li>
<li>未通过园区授权检查时，不会调用 <code>UserSecurityUtils.enhanceLoginInfo(...)</code> 更新 Redis 登录态。</li>
<li>通过检查后，角色也按”Token 用户的 <code>userId</code> + 目标 <code>parkId</code>“重新查询，<strong>不会沿用其他园区角色</strong>。</li>
</ol>
<p>因此，修改前端请求中的 <code>parkId</code> 只能表达”想切换到哪个园区”，<strong>不能伪造用户身份</strong>，也<strong>不能绕过后端园区授权校验</strong>。</p>
<blockquote>
<p>这是源码级的安全契约，已经被单元测试 + 反向变异验证锁死。后续任何”看起来没事”的优化，都不能静默破坏这套契约。</p>
</blockquote>
<h2 id="二、被测代码与关键调用链"><a href="#二、被测代码与关键调用链" class="headerlink" title="二、被测代码与关键调用链"></a>二、被测代码与关键调用链</h2><p>接口：<code>GET /user/userParkSwitch/&#123;parkId&#125;</code></p>
<p>文件：</p>
<ul>
<li>Controller：<code>service-provider/admin-service/src/main/java/cn/csg/building/admin/controller/UserController.java</code></li>
<li>自动化测试：<code>service-provider/admin-service/src/test/java/cn/csg/building/admin/controller/UserParkSwitchSecurityContractTest.java</code></li>
</ul>
<p>关键调用顺序：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">SecurityExtUtils.getCurrentLoginInfo()</span><br><span class="line">  → Token 登录态中的 userId</span><br><span class="line">  → listManageParksByUserId(user.getUserId())</span><br><span class="line">  → 校验 target parkId 是否属于授权园区</span><br><span class="line">  → 按 userId + parkId 查询角色</span><br><span class="line">  → enhanceLoginInfo 更新 Redis 登录态</span><br></pre></td></tr></table></figure>

<p>这条调用链上，有 4 个<strong>绝对不能错的点</strong>：</p>
<ol>
<li>从 <code>SecurityExtUtils.getCurrentLoginInfo()</code> 取身份——不能从请求体或 header 取。</li>
<li>用 <code>user.getUserId()</code> 查授权园区——不能用前端传的 <code>userId</code>（前端根本不应该传）。</li>
<li><code>assertBizCondition(parkAllowed, ...)</code> 必须在 <code>enhanceLoginInfo</code> 之前。</li>
<li>角色查询必须按 <code>userId + parkId</code> 两个条件——不能只按 <code>parkId</code>、不能只按 <code>userId</code>。</li>
</ol>
<p>只要这 4 点有任何一个被破坏，就是越权漏洞。下面用 4 个测试用例把每一点都钉死。</p>
<h2 id="三、4-个测试用例"><a href="#三、4-个测试用例" class="headerlink" title="三、4 个测试用例"></a>三、4 个测试用例</h2><h3 id="用例-1：用户身份必须来自当前-Token-登录态"><a href="#用例-1：用户身份必须来自当前-Token-登录态" class="headerlink" title="用例 1：用户身份必须来自当前 Token 登录态"></a>用例 1：用户身份必须来自当前 Token 登录态</h3><p><strong>目的</strong>：证明 <code>userParkSwitch</code> 第一步是调 <code>SecurityExtUtils.getCurrentLoginInfo()</code>，而不是从请求体里读用户。</p>
<p>测试断言：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">BaseUser</span> <span class="variable">user</span> <span class="operator">=</span> SecurityExtUtils.getCurrentLoginInfo();</span><br></pre></td></tr></table></figure>

<p>然后使用该登录用户的 <code>userId</code> 查询授权园区：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">listManageParksByUserId(user.getUserId())</span><br></pre></td></tr></table></figure>

<p><strong>结果：通过。</strong></p>
<p>如果未来有人误改成 <code>request.getParameter(&quot;userId&quot;)</code>，测试立刻红。</p>
<h3 id="用例-2：前端-parkId-必须经过授权检查"><a href="#用例-2：前端-parkId-必须经过授权检查" class="headerlink" title="用例 2：前端 parkId 必须经过授权检查"></a>用例 2：前端 parkId 必须经过授权检查</h3><p><strong>目的</strong>：证明执行顺序是”先校验 → 后更新 Redis”。如果反过来，就是经典的 TOCTOU 漏洞——未授权 parkId 也会先被写进登录态。</p>
<p>测试断言执行顺序必须是：</p>
<ol>
<li>从 Token 登录态读取用户；</li>
<li>根据 Token 的 <code>userId</code> 查询允许园区；</li>
<li>执行 <code>assertBizCondition(parkAllowed, ...)</code>；</li>
<li>**校验通过后才执行 <code>enhanceLoginInfo(...)</code>**。</li>
</ol>
<p><strong>结果：通过。未授权 <code>parkId</code> 会在 Redis 登录态更新之前被拒绝。</strong></p>
<blockquote>
<p>用例 2 的价值不只是”测通过”，更是”测顺序”。如果有人为了性能优化把 <code>enhanceLoginInfo</code> 提到校验前面，授权拦截就失效了。<strong>测试不仅测行为，还测顺序</strong>。</p>
</blockquote>
<h3 id="用例-3：切换后的角色限定为-Token-用户和目标园区"><a href="#用例-3：切换后的角色限定为-Token-用户和目标园区" class="headerlink" title="用例 3：切换后的角色限定为 Token 用户和目标园区"></a>用例 3：切换后的角色限定为 Token 用户和目标园区</h3><p><strong>目的</strong>：证明角色查询条件同时包含 <code>userId</code> 和 <code>parkId</code>，且都在 <code>userRoleService.listByCondition(userRoleDTO)</code> 之前设置。</p>
<p>测试断言：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">userRoleDTO.setUserId(user.getUserId());</span><br><span class="line">userRoleDTO.setParkId(parkId);</span><br></pre></td></tr></table></figure>

<p>两个条件必须在 <code>userRoleService.listByCondition(userRoleDTO)</code> 前设置。</p>
<p><strong>结果：通过。</strong></p>
<p>如果有人误改成只按 <code>parkId</code> 查角色，多园区用户切园区时会”继承”上一个园区的角色——这是隐蔽的越权。</p>
<h3 id="用例-4：反向变异测试"><a href="#用例-4：反向变异测试" class="headerlink" title="用例 4：反向变异测试"></a>用例 4：反向变异测试</h3><p><strong>目的</strong>：确认测试不是”假阳性”——即测试不仅要在当前实现下通过，还要在”故意搞坏实现”时失败。</p>
<blockquote>
<p>反向变异测试（mutation testing）是验证测试有效性的关键手段。如果不管代码怎么改测试都绿，那这套测试根本没价值。</p>
</blockquote>
<p>操作：临时将</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">listManageParksByUserId(user.getUserId())</span><br></pre></td></tr></table></figure>

<p>替换为非 Token 用户标识：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">listManageParksByUserId(<span class="string">&quot;front-end-user-id&quot;</span>)</span><br></pre></td></tr></table></figure>

<p>执行结果：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">必须使用 Token 中的 userId 查询授权园区</span><br><span class="line">Tests run: 2, Failures: 1, Errors: 0, Skipped: 0</span><br><span class="line">BUILD FAILURE</span><br></pre></td></tr></table></figure>

<p><strong>测试失败，命中预期断言。</strong></p>
<p>随后已恢复生产代码并重新执行测试，恢复绿色。</p>
<blockquote>
<p>反向变异让我们确信：这套测试是”真在测身份来源”，不是”测试自身写错了所以永远绿”。</p>
</blockquote>
<h2 id="四、最终执行结果"><a href="#四、最终执行结果" class="headerlink" title="四、最终执行结果"></a>四、最终执行结果</h2><p>执行环境：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">JDK: E:\jdk</span><br><span class="line">Maven: E:\maven363</span><br><span class="line">时间: 2026-07-13 11:00:00 +08:00</span><br></pre></td></tr></table></figure>

<p>执行命令：</p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line"><span class="variable">$env:JAVA_HOME</span>=<span class="string">&#x27;E:\jdk&#x27;</span></span><br><span class="line"><span class="variable">$env:Path</span>=<span class="string">&#x27;E:\jdk\bin;E:\maven363\bin;&#x27;</span>+<span class="variable">$env:Path</span></span><br><span class="line">mvn <span class="literal">-pl</span> service<span class="literal">-provider</span>/admin<span class="literal">-service</span> <span class="literal">-Dtest</span>=UserParkSwitchSecurityContractTest test</span><br></pre></td></tr></table></figure>

<p>最终结果：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Running cn.csg.building.admin.controller.UserParkSwitchSecurityContractTest</span><br><span class="line">Tests run: 2, Failures: 0, Errors: 0, Skipped: 0</span><br><span class="line">BUILD SUCCESS</span><br><span class="line">Total time: 17.436 s</span><br></pre></td></tr></table></figure>

<p><img data-src="/images/posts/Token%E8%BA%AB%E4%BB%BD%E8%B6%8A%E6%9D%83%E5%A5%91%E7%BA%A6%E6%B5%8B%E8%AF%95/fig2.png" alt="反向变异验证流程"></p>
<p><em>图2：正向通过 → 反向变异 → 期望失败 → 恢复 → 重测通过，闭环验证</em></p>
<h2 id="五、测试边界与未做的事"><a href="#五、测试边界与未做的事" class="headerlink" title="五、测试边界与未做的事"></a>五、测试边界与未做的事</h2><p>本次是可重复执行的<strong>源码安全契约测试</strong>，并完成了反向变异验证。它证明当前代码的身份来源、授权顺序和 Redis 更新顺序<strong>不会在后续修改中被静默破坏</strong>。</p>
<p>本次没有做的事：</p>
<table>
<thead>
<tr>
<th>未做项</th>
<th>原因</th>
<th>如何补</th>
</tr>
</thead>
<tbody><tr>
<td>真实浏览器 Token 对运行中的网关发请求</td>
<td>当前任务没有提供可用的测试账号、Token 和已启动的 gateway&#x2F;admin-service</td>
<td>补一组抓包测试</td>
</tr>
<tr>
<td>黑盒证据</td>
<td>同上</td>
<td>同一 Token 请求授权园区应成功；将请求中的 <code>parkId</code> 改为未授权园区应失败；确认 <code>/user/info</code> 中的当前园区未变化</td>
</tr>
</tbody></table>
<p>源码契约 + 集成测试是互补的：源码契约覆盖”代码该长什么样”，集成测试覆盖”运行时是不是真的这样”。两者都做，才是完整的安全闭环。</p>
<h2 id="六、经验总结"><a href="#六、经验总结" class="headerlink" title="六、经验总结"></a>六、经验总结</h2><h3 id="6-1-安全契约测试的三个关键设计"><a href="#6-1-安全契约测试的三个关键设计" class="headerlink" title="6.1 安全契约测试的三个关键设计"></a>6.1 安全契约测试的三个关键设计</h3><ol>
<li><strong>断言身份来源</strong>：用 <code>SecurityExtUtils.getCurrentLoginInfo()</code> 还是 <code>request.getParameter(&quot;userId&quot;)</code>？这是不同的安全等级，测试必须钉死前者。</li>
<li><strong>断言执行顺序</strong>：先校验后写缓存 &#x2F; 先校验后改角色——顺序错了整个授权就失效。测试要验证顺序，不仅验证结果。</li>
<li><strong>断言条件完整</strong>：角色查询要 <code>userId + parkId</code> 同时设置，缺一个就是越权入口。</li>
</ol>
<h3 id="6-2-反向变异测试不能省"><a href="#6-2-反向变异测试不能省" class="headerlink" title="6.2 反向变异测试不能省"></a>6.2 反向变异测试不能省</h3><p>很多团队写完正向用例就觉得”安全了”，但实际上：</p>
<ul>
<li>测试覆盖了 100 行代码，可能只有 50 行被实际断言。</li>
<li>反向变异能告诉你”哪部分代码改了测试不会红”——那些”沉默的代码”才是风险。</li>
</ul>
<p>这次我们做的反向变异只有一次，但<strong>只要做了一次，就证明这套测试是有效的</strong>。后续可以加更多变异点（比如把 <code>enhanceLoginInfo</code> 提到 <code>assertBizCondition</code> 前面），但单次的反向变异已经把”测试有效性”这件事钉死了。</p>
<h3 id="6-3-安全测试不是黑盒专属"><a href="#6-3-安全测试不是黑盒专属" class="headerlink" title="6.3 安全测试不是黑盒专属"></a>6.3 安全测试不是黑盒专属</h3><p>源码级的安全契约测试有几个优势：</p>
<table>
<thead>
<tr>
<th>优势</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>不需要部署</td>
<td>在 CI 里就能跑，反馈快</td>
</tr>
<tr>
<td>不需要构造 Token</td>
<td>直接调 <code>SecurityExtUtils</code>，mock 登录态即可</td>
</tr>
<tr>
<td>能定位到代码行</td>
<td>失败时直接定位到具体调用点，黑盒测试只能定位到接口</td>
</tr>
<tr>
<td>不会因为环境问题误报</td>
<td>不依赖 Redis、网络、网关</td>
</tr>
</tbody></table>
<p>源码契约测试不能替代黑盒测试，但它是<strong>开发阶段就能跑、CI 阶段就能拦</strong>的安全防线。</p>
<h3 id="6-4-推荐把这套测试接入-CI"><a href="#6-4-推荐把这套测试接入-CI" class="headerlink" title="6.4 推荐把这套测试接入 CI"></a>6.4 推荐把这套测试接入 CI</h3><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># .github/workflows/security-contract.yml</span></span><br><span class="line"><span class="bullet">-</span> <span class="attr">name:</span> <span class="string">安全契约测试</span></span><br><span class="line">  <span class="attr">run:</span> <span class="string">|</span></span><br><span class="line"><span class="string">    mvn -pl service-provider/admin-service \</span></span><br><span class="line"><span class="string">        -Dtest=UserParkSwitchSecurityContractTest \</span></span><br><span class="line"><span class="string">        test</span></span><br></pre></td></tr></table></figure>

<p>任何对 <code>UserController.userParkSwitch</code> 的修改都会触发 CI，CI 失败就意味着越权风险。这比”上线后被安全部门扫出来”早了好几个迭代。</p>
<h2 id="七、结语"><a href="#七、结语" class="headerlink" title="七、结语"></a>七、结语</h2><p>越权漏洞最可怕的不是”被利用”，而是”没人知道它在”。一行 <code>parkId</code> 的改动、一次”看起来没事的优化”、一次性能重构——都可能让原本安全的接口变成越权入口。</p>
<p>源码契约测试 + 反向变异验证，把”安全不变量”显式地写进测试里。<strong>CI 跑得越勤，越权漏洞的存活窗口就越短</strong>。</p>
<blockquote>
<p>一行 parkId 改不出越权——前提是有人用测试把这件事钉死。</p>
</blockquote>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>南网</category>
      </categories>
      <tags>
        <tag>南网项目</tag>
        <tag>安全测试</tag>
        <tag>契约测试</tag>
        <tag>反向变异</tag>
        <tag>Spring Security</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring Boot 项目实战日：代码重构、新功能开发与环境搭建</title>
    <url>/2026/04/07/Spring-Boot%E9%A1%B9%E7%9B%AE%E5%AE%9E%E6%88%98%E6%97%A5-%E4%BB%A3%E7%A0%81%E9%87%8D%E6%9E%84-%E6%96%B0%E5%8A%9F%E8%83%BD%E5%BC%80%E5%8F%91%E4%B8%8E%E7%8E%AF%E5%A2%83%E6%90%AD%E5%BB%BA/</url>
    <content><![CDATA[<p>一篇记录同一天内三个 Spring Boot 项目的实战工作：图书管理系统的 Controller 层重构与分支合并策略、毕业设计图书评分评论功能的全流程开发，以及物联网项目本地 Nacos + TDengine 环境的搭建验证。</p>
<span id="more"></span>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>2026 年 4 月 7 日是个忙碌的开发日。手头同时在推进三个 Spring Boot 项目：一个图书管理系统的代码重构、一个毕业设计的新功能开发、一个物联网项目的本地环境搭建。三件事看起来互不相干，但串起来恰好是一条完整的后端开发链路——从环境就绪到代码编写，再到结构优化。</p>
<p>本文按项目维度分别记录，提炼每件事的关键决策和经验。</p>
<h2 id="一、图书管理系统：Controller-层重构"><a href="#一、图书管理系统：Controller-层重构" class="headerlink" title="一、图书管理系统：Controller 层重构"></a>一、图书管理系统：Controller 层重构</h2><h3 id="1-1-背景"><a href="#1-1-背景" class="headerlink" title="1.1 背景"></a>1.1 背景</h3><p>图书管理系统的 Controller 层积累了大量重复逻辑：鉴权校验散落在各个接口、封面上传在多处复制粘贴、公共工具类没有统一收口。这轮工作的目标是”公共工具与基础抽象收口”，先不碰页面结构，只做后端低风险的 cleanup。</p>
<h3 id="1-2-重构内容"><a href="#1-2-重构内容" class="headerlink" title="1.2 重构内容"></a>1.2 重构内容</h3><p>在隔离的 git worktree 中完成了三个 Task：</p>
<table>
<thead>
<tr>
<th>Task</th>
<th>提交</th>
<th>核心改动</th>
</tr>
</thead>
<tbody><tr>
<td><code>UserApplicationController</code> 收口</td>
<td><code>a7ee203</code></td>
<td>新增 <code>AuthGuard</code>，接口改用 DTO 绑定，收口管理员鉴权</td>
</tr>
<tr>
<td>封面上传公共化</td>
<td><code>e8bbd03</code></td>
<td>抽取 <code>CoverUploadService</code>，消除 <code>BookController</code> 和 <code>StudentBookController</code> 的重复上传逻辑</td>
</tr>
<tr>
<td><code>LendRecordController</code> 读侧收口</td>
<td><code>54b4343</code></td>
<td>整理读接口公共校验，不碰写路径</td>
</tr>
</tbody></table>
<p>关键设计决策：<strong>只动读接口，不动写路径</strong>。这样把风险控制在最小范围，写路径的收口留给后续迭代。</p>
<p><img data-src="/images/posts/Spring-Boot%E9%A1%B9%E7%9B%AE%E5%AE%9E%E6%88%98%E6%97%A5-%E4%BB%A3%E7%A0%81%E9%87%8D%E6%9E%84-%E6%96%B0%E5%8A%9F%E8%83%BD%E5%BC%80%E5%8F%91%E4%B8%8E%E7%8E%AF%E5%A2%83%E6%90%AD%E5%BB%BA/fig1.png" alt="Controller 重构分层架构"><br><em>图 1：Controller 层重构——公共服务下沉与鉴权横切</em></p>
<h3 id="1-3-分支合并策略"><a href="#1-3-分支合并策略" class="headerlink" title="1.3 分支合并策略"></a>1.3 分支合并策略</h3><p>这里有个值得记录的决策过程。当时手头有两个分支：</p>
<ul>
<li><code>代码分析和优化</code>：用户明确要求合并的分支</li>
<li><code>controller-cleanup-task1</code>：在 worktree 中完成的重构分支</li>
</ul>
<p>最终策略：</p>
<ol>
<li><strong>先修基线测试</strong>：<code>代码分析和优化</code> 分支上有一个 <code>DashboardControllerTest</code> 失败，根因是 <code>SysConfigMapper</code> mock 缺失。补齐后新增两个降级场景测试</li>
<li><strong>合并并验证</strong>：<code>代码分析和优化</code> 合入 <code>main</code>，跑通 38 tests, 0 failures</li>
<li><strong>cleanup 暂不合入</strong>：<code>controller-cleanup-task1</code> 保留在 worktree，等单独审查后再合并</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 最终合并结果</span></span><br><span class="line">f18d944 Merge branch <span class="string">&#x27;代码分析和优化&#x27;</span></span><br><span class="line">bccb1c5 fix dashboard visit count <span class="built_in">test</span> coverage</span><br></pre></td></tr></table></figure>

<blockquote>
<p>这个策略的核心思路：<strong>先合用户要求的，cleanup 作为独立批次后续跟进</strong>。避免把两件事混在一起增加审查复杂度。</p>
</blockquote>
<h2 id="二、毕业设计：图书评分与评论功能"><a href="#二、毕业设计：图书评分与评论功能" class="headerlink" title="二、毕业设计：图书评分与评论功能"></a>二、毕业设计：图书评分与评论功能</h2><h3 id="2-1-功能设计"><a href="#2-1-功能设计" class="headerlink" title="2.1 功能设计"></a>2.1 功能设计</h3><p>为小学图书管理系统新增完整的评分与评论体系：</p>
<ul>
<li><strong>评分</strong>：归还图书后可打 1-5 星</li>
<li><strong>评论</strong>：可选填文字评论（最多 500 字）</li>
<li><strong>统计</strong>：图书列表展示平均评分和评价人数</li>
<li><strong>管理</strong>：管理员可删除不当评论（软删除）</li>
</ul>
<h3 id="2-2-API-设计"><a href="#2-2-API-设计" class="headerlink" title="2.2 API 设计"></a>2.2 API 设计</h3><p>4 个 REST API，职责清晰：</p>
<table>
<thead>
<tr>
<th>API</th>
<th>方法</th>
<th>路径</th>
<th>权限</th>
</tr>
</thead>
<tbody><tr>
<td>提交评论</td>
<td>POST</td>
<td><code>/review</code></td>
<td>登录 + 借阅记录验证</td>
</tr>
<tr>
<td>评论列表</td>
<td>GET</td>
<td><code>/review/book/&#123;isbn&#125;</code></td>
<td>公开，分页</td>
</tr>
<tr>
<td>评分统计</td>
<td>GET</td>
<td><code>/review/book/&#123;isbn&#125;/stats</code></td>
<td>公开</td>
</tr>
<tr>
<td>删除评论</td>
<td>DELETE</td>
<td><code>/review/&#123;id&#125;</code></td>
<td>管理员</td>
</tr>
</tbody></table>
<h3 id="2-3-代码审查发现的问题"><a href="#2-3-代码审查发现的问题" class="headerlink" title="2.3 代码审查发现的问题"></a>2.3 代码审查发现的问题</h3><p>功能写完后做了一轮代码审查，发现两个重要问题：</p>
<p><strong>安全漏洞</strong>（commit <code>06f8c4f</code>）：</p>
<ul>
<li><code>submit()</code> 接口无 Token 校验，任何人都能提交评论</li>
<li><code>delete()</code> 接口无权限校验，任何人都能删除评论</li>
<li>修复：添加登录验证和管理员角色校验</li>
</ul>
<p><strong>性能问题</strong>（commit <code>16131cc</code>）：</p>
<ul>
<li><code>stats()</code> 在 Java 内存中计算平均分 → 改为 <strong>SQL 聚合查询</strong></li>
<li>分页参数手动验证 → 改用 <code>QueryUtils.createPage()</code> 工具方法</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// SQL 聚合替代内存计算</span></span><br><span class="line"><span class="meta">@Select(&quot;&quot;&quot;</span></span><br><span class="line"><span class="meta">    SELECT COUNT(*) as total_count,</span></span><br><span class="line"><span class="meta">           AVG(rating) as avg_rating</span></span><br><span class="line"><span class="meta">    FROM book_review</span></span><br><span class="line"><span class="meta">    WHERE isbn = #&#123;isbn&#125; AND deleted = 0</span></span><br><span class="line"><span class="meta">    &quot;&quot;&quot;)</span></span><br><span class="line">ReviewStats <span class="title function_">selectStatsByIsbn</span><span class="params">(<span class="meta">@Param(&quot;isbn&quot;)</span> String isbn)</span>;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>这两轮审查修复说明：<strong>新功能开发完成后，安全和性能审查不可省略</strong>。尤其是鉴权和数据聚合，很容易在”先跑通”阶段被忽略。</p>
</blockquote>
<p><img data-src="/images/posts/Spring-Boot%E9%A1%B9%E7%9B%AE%E5%AE%9E%E6%88%98%E6%97%A5-%E4%BB%A3%E7%A0%81%E9%87%8D%E6%9E%84-%E6%96%B0%E5%8A%9F%E8%83%BD%E5%BC%80%E5%8F%91%E4%B8%8E%E7%8E%AF%E5%A2%83%E6%90%AD%E5%BB%BA/fig2.png" alt="图书评分评论功能流程"><br><em>图 2：图书评分评论功能——从还书到统计的完整流程</em></p>
<h2 id="三、物联网项目：本地环境搭建"><a href="#三、物联网项目：本地环境搭建" class="headerlink" title="三、物联网项目：本地环境搭建"></a>三、物联网项目：本地环境搭建</h2><h3 id="3-1-与-BladeX-Links-的关系"><a href="#3-1-与-BladeX-Links-的关系" class="headerlink" title="3.1 与 BladeX-Links 的关系"></a>3.1 与 BladeX-Links 的关系</h3><p>这套物联网项目的本地启动方案并非从零搭建，而是在已有的 BladeX-Links 本机启动清单基础上增加 Nacos：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">BladeX-Links 本机启动清单 + 本地 Nacos = 当前物联网项目本机启动链路</span><br></pre></td></tr></table></figure>

<p>共享的基础设施：MySQL + Redis + TDengine。唯一新增的是 Nacos 配置中心&#x2F;注册中心。</p>
<h3 id="3-2-Nacos-配置"><a href="#3-2-Nacos-配置" class="headerlink" title="3.2 Nacos 配置"></a>3.2 Nacos 配置</h3><p>复用之前修复过的本地解压版 Nacos，不依赖 Docker：</p>
<table>
<thead>
<tr>
<th>配置项</th>
<th>值</th>
</tr>
</thead>
<tbody><tr>
<td>API 地址</td>
<td><code>http://starviewnacos.com:8340/nacos/</code></td>
</tr>
<tr>
<td>控制台</td>
<td><code>http://localhost:8380/</code></td>
</tr>
<tr>
<td>启动模式</td>
<td><code>standalone + embeddedStorage</code></td>
</tr>
<tr>
<td>hosts 映射</td>
<td><code>127.0.0.1 starviewnacos.com</code></td>
</tr>
</tbody></table>
<p>关键点：<code>hosts</code> 文件必须配置域名映射，否则 Spring Boot 启动时无法连接 Nacos。</p>
<h3 id="3-3-TDengine-配置"><a href="#3-3-TDengine-配置" class="headerlink" title="3.3 TDengine 配置"></a>3.3 TDengine 配置</h3><p>沿用本地 3.0.7.1 版本方案：</p>
<table>
<thead>
<tr>
<th>配置项</th>
<th>值</th>
</tr>
</thead>
<tbody><tr>
<td>REST 地址</td>
<td><code>http://127.0.0.1:6041/rest/sql</code></td>
</tr>
<tr>
<td>数据库名</td>
<td><code>links</code></td>
</tr>
<tr>
<td>启动命令</td>
<td><code>taosd</code> + <code>taosadapter</code></td>
</tr>
</tbody></table>
<p>验证方式简单直接：<code>show databases;</code> 能看到 <code>links</code> 库就算就绪。</p>
<h3 id="3-4-推荐启动顺序"><a href="#3-4-推荐启动顺序" class="headerlink" title="3.4 推荐启动顺序"></a>3.4 推荐启动顺序</h3><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">1. MySQL    → 数据源</span><br><span class="line">2. Redis    → 缓存</span><br><span class="line">3. TDengine → 时序库</span><br><span class="line">4. Nacos    → 配置中心 + 注册中心</span><br><span class="line">5. 业务服务 → 最后启动</span><br></pre></td></tr></table></figure>

<p>按这个顺序，每个服务启动时它依赖的基础设施都已就绪，避免因连接超时导致启动失败。</p>
<p><img data-src="/images/posts/Spring-Boot%E9%A1%B9%E7%9B%AE%E5%AE%9E%E6%88%98%E6%97%A5-%E4%BB%A3%E7%A0%81%E9%87%8D%E6%9E%84-%E6%96%B0%E5%8A%9F%E8%83%BD%E5%BC%80%E5%8F%91%E4%B8%8E%E7%8E%AF%E5%A2%83%E6%90%AD%E5%BB%BA/fig3.png" alt="本地环境启动依赖链"><br><em>图 3：物联网项目本地启动依赖链——Nacos 是新增项</em></p>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><p>回顾这三件事，提炼几个共通的要点：</p>
<ul>
<li><strong>隔离开发降低风险</strong>：Controller cleanup 在 worktree 中进行，不影响主分支，验证通过后再考虑合入</li>
<li><strong>分支策略要清晰</strong>：用户要求的合并和自主的重构分开处理，审查负担不叠加</li>
<li><strong>新功能审查不可省</strong>：鉴权校验和数据聚合方式是最容易遗漏的两类问题</li>
<li><strong>环境搭建要复用</strong>：不要每次都从零配，在已有方案上做增量是最快的</li>
<li><strong>启动顺序有讲究</strong>：先基础后业务，让每个服务启动时依赖已就绪</li>
</ul>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>一天推三个项目听起来紧张，但每个项目都只聚焦一个明确的切面：重构是收口公共逻辑、新功能是完整实现并过审、环境是复用已有方案做增量。小步推进，每个切面都不贪多，反而效率最高。</p>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>南沙物联网</category>
      </categories>
      <tags>
        <tag>Spring Boot</tag>
        <tag>Nacos</tag>
        <tag>代码重构</tag>
        <tag>TDengine</tag>
        <tag>图书管理系统</tag>
      </tags>
  </entry>
  <entry>
    <title>YouTube Research Pipeline：用 AI 打造自动化视频研究工作流</title>
    <url>/2026/03/09/YouTube-Research-Pipeline-%E8%87%AA%E5%8A%A8%E5%8C%96%E7%A0%94%E7%A9%B6%E5%B7%A5%E4%BD%9C%E6%B5%81/</url>
    <content><![CDATA[<p>一个话题输入，全自动研究输出。本文介绍如何用 Claude Code Skills + NotebookLM 构建端到端的 YouTube 视频研究工作流，实现从搜索、筛选、分析到生成报告的全自动化。</p>
<span id="more"></span>

<h2 id="问题背景"><a href="#问题背景" class="headerlink" title="问题背景"></a>问题背景</h2><p>做技术调研时，YouTube 往往是最好的学习资源之一。但传统的视频研究流程非常低效：</p>
<ol>
<li>手动搜索关键词</li>
<li>逐个点开视频查看</li>
<li>跳过广告、调整进度条</li>
<li>记笔记、整理要点</li>
<li>不同视频之间对比分析</li>
</ol>
<p>一个话题调研下来，可能要花 2-3 个小时。而且容易遗漏重要信息，难以系统化整理。</p>
<p><strong>能不能让 AI 帮我们完成这一切？</strong></p>
<h2 id="解决方案：YouTube-Research-Pipeline"><a href="#解决方案：YouTube-Research-Pipeline" class="headerlink" title="解决方案：YouTube Research Pipeline"></a>解决方案：YouTube Research Pipeline</h2><p>我设计了一个全自动化的研究工作流：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌─────────────┐    ┌──────────────┐    ┌─────────────┐    ┌──────────────┐    ┌─────────────┐</span><br><span class="line">│   Search    │ -&gt; │   Select     │ -&gt; │  NotebookLM │ -&gt; │   Analyze    │ -&gt; │ Deliverable │</span><br><span class="line">│  YouTube    │    │ Best Videos  │    │   Ingest    │    │   &amp; Report   │    │  (optional) │</span><br><span class="line">│  (20-50)    │    │   (5-8)      │    │             │    │              │    │             │</span><br><span class="line">└─────────────┘    └──────────────┘    └─────────────┘    └──────────────┘    └─────────────┘</span><br></pre></td></tr></table></figure>

<p><strong>输入</strong>：一个话题（如 “Claude Code Skills”）</p>
<p><strong>输出</strong>：完整的研究报告 + 可选的 podcast&#x2F;幻灯片&#x2F;思维导图</p>
<h2 id="核心技术栈"><a href="#核心技术栈" class="headerlink" title="核心技术栈"></a>核心技术栈</h2><table>
<thead>
<tr>
<th>工具</th>
<th>用途</th>
<th>安装方式</th>
</tr>
</thead>
<tbody><tr>
<td><strong>yt-dlp</strong></td>
<td>YouTube 搜索和元数据提取</td>
<td><code>pip install yt-dlp</code></td>
</tr>
<tr>
<td><strong>NotebookLM CLI</strong></td>
<td>视频内容分析和生成</td>
<td><code>pip install notebooklm-py</code></td>
</tr>
<tr>
<td><strong>Claude Code</strong></td>
<td>工作流编排和 Skill 定义</td>
<td>Claude Code CLI</td>
</tr>
</tbody></table>
<h2 id="实现细节"><a href="#实现细节" class="headerlink" title="实现细节"></a>实现细节</h2><h3 id="Step-1：搜索-YouTube"><a href="#Step-1：搜索-YouTube" class="headerlink" title="Step 1：搜索 YouTube"></a>Step 1：搜索 YouTube</h3><p>使用 yt-dlp 的搜索功能，一次性获取 50 个候选视频：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yt-dlp <span class="string">&quot;ytsearch50:Claude Code Skills&quot;</span> \</span><br><span class="line">  --flat-playlist \</span><br><span class="line">  --<span class="built_in">print</span> <span class="string">&quot;%(id)s|%(title)s|%(channel)s|%(channel_follower_count)s|%(view_count)s|%(duration_string)s|%(upload_date)s&quot;</span> \</span><br><span class="line">  --dateafter now-6months</span><br></pre></td></tr></table></figure>

<p><strong>参数说明</strong>：</p>
<ul>
<li><code>ytsearch50</code>：搜索 50 个结果</li>
<li><code>--flat-playlist</code>：只获取元数据，不下载视频</li>
<li><code>--dateafter now-6months</code>：只看最近 6 个月的内容</li>
</ul>
<h3 id="Step-2：智能筛选最佳视频"><a href="#Step-2：智能筛选最佳视频" class="headerlink" title="Step 2：智能筛选最佳视频"></a>Step 2：智能筛选最佳视频</h3><p>50 个视频太多，需要筛选出最有价值的 5-8 个。我设计了一个评分系统：</p>
<table>
<thead>
<tr>
<th>因素</th>
<th>权重</th>
<th>评分方法</th>
</tr>
</thead>
<tbody><tr>
<td><strong>相关性</strong></td>
<td>40%</td>
<td>标题与话题的关键词匹配度</td>
</tr>
<tr>
<td><strong>参与度</strong></td>
<td>30%</td>
<td><code>播放量 / 频道订阅数</code> 比值</td>
</tr>
<tr>
<td><strong>时效性</strong></td>
<td>20%</td>
<td>越新分数越高</td>
</tr>
<tr>
<td><strong>多样性</strong></td>
<td>10%</td>
<td>同一频道最多 2 个视频</td>
</tr>
</tbody></table>
<p><strong>参与度比值的含义</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">engagement_ratio = view_count / channel_follower_count</span><br></pre></td></tr></table></figure>

<ul>
<li><code>&gt; 1.0</code>：爆款视频，表现超过频道平均水平</li>
<li><code>0.3 - 1.0</code>：参与度良好</li>
<li><code>&lt; 0.3</code>：正常表现</li>
</ul>
<p><strong>筛选规则</strong>：</p>
<ul>
<li>每个频道最多 2 个视频（保证多样性）</li>
<li>优先选择参与度 &gt; 0.3 的视频</li>
<li>时长 5-60 分钟最佳（跳过短视频和超长内容）</li>
</ul>
<h3 id="Step-3：创建-NotebookLM-笔记本"><a href="#Step-3：创建-NotebookLM-笔记本" class="headerlink" title="Step 3：创建 NotebookLM 笔记本"></a>Step 3：创建 NotebookLM 笔记本</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">notebooklm create <span class="string">&quot;Research: Claude Code Skills&quot;</span> --json</span><br></pre></td></tr></table></figure>

<p>NotebookLM 会返回一个 notebook ID，后续操作都需要用到。</p>
<h3 id="Step-4：添加视频源"><a href="#Step-4：添加视频源" class="headerlink" title="Step 4：添加视频源"></a>Step 4：添加视频源</h3><p>把筛选出的视频作为数据源添加到 NotebookLM：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">notebooklm <span class="built_in">source</span> add <span class="string">&quot;https://www.youtube.com/watch?v=VIDEO_ID&quot;</span> --json</span><br></pre></td></tr></table></figure>

<p>NotebookLM 会自动解析 YouTube 视频，提取字幕和内容。</p>
<h3 id="Step-5：等待处理完成"><a href="#Step-5：等待处理完成" class="headerlink" title="Step 5：等待处理完成"></a>Step 5：等待处理完成</h3><p>视频处理需要时间（30 秒到 10 分钟不等），需要等待：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">notebooklm <span class="built_in">source</span> <span class="built_in">wait</span> &lt;source_id&gt; -n &lt;notebook_id&gt; --<span class="built_in">timeout</span> 600</span><br></pre></td></tr></table></figure>

<h3 id="Step-6：运行分析"><a href="#Step-6：运行分析" class="headerlink" title="Step 6：运行分析"></a>Step 6：运行分析</h3><p>处理完成后，向 NotebookLM 提问进行分析：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">notebooklm ask <span class="string">&quot;What are the main trends across these videos?&quot;</span> --json</span><br></pre></td></tr></table></figure>

<p><strong>分析维度</strong>：</p>
<table>
<thead>
<tr>
<th>问题类型</th>
<th>目的</th>
</tr>
</thead>
<tbody><tr>
<td>趋势分析</td>
<td>“这些视频的主要趋势和模式是什么？”</td>
</tr>
<tr>
<td>关键要点</td>
<td>“最重要的 5 个要点是什么？”</td>
</tr>
<tr>
<td>异常观点</td>
<td>“有哪些独特或有争议的观点？”</td>
</tr>
<tr>
<td>研究空白</td>
<td>“哪些话题或角度被忽略了？”</td>
</tr>
<tr>
<td>共识总结</td>
<td>“所有来源都同意的观点是什么？”</td>
</tr>
</tbody></table>
<h3 id="Step-7：生成可交付物（可选）"><a href="#Step-7：生成可交付物（可选）" class="headerlink" title="Step 7：生成可交付物（可选）"></a>Step 7：生成可交付物（可选）</h3><p>NotebookLM 支持多种输出格式：</p>
<table>
<thead>
<tr>
<th>类型</th>
<th>命令</th>
<th>耗时</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Podcast</strong></td>
<td><code>notebooklm generate audio &quot;说明&quot;</code></td>
<td>10-20 分钟</td>
</tr>
<tr>
<td><strong>视频</strong></td>
<td><code>notebooklm generate video &quot;说明&quot;</code></td>
<td>15-45 分钟</td>
</tr>
<tr>
<td><strong>报告</strong></td>
<td><code>notebooklm generate report --format study-guide</code></td>
<td>5-15 分钟</td>
</tr>
<tr>
<td><strong>幻灯片</strong></td>
<td><code>notebooklm generate slide-deck</code></td>
<td>5-15 分钟</td>
</tr>
<tr>
<td><strong>思维导图</strong></td>
<td><code>notebooklm generate mind-map</code></td>
<td>即时</td>
</tr>
</tbody></table>
<h2 id="输出格式示例"><a href="#输出格式示例" class="headerlink" title="输出格式示例"></a>输出格式示例</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━</span><br><span class="line">YouTube Research Pipeline Complete: Claude Code Skills</span><br><span class="line">━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━</span><br><span class="line"></span><br><span class="line">📊 Research Summary</span><br><span class="line">   Videos searched: 50</span><br><span class="line">   Videos selected: 8</span><br><span class="line">   Notebook: fcbe7e02-e304-47d0-bbd3-fea0f2eb1284</span><br><span class="line"></span><br><span class="line">🎬 Selected Videos (ranked by score)</span><br><span class="line">   1. Don&#x27;t Build Agents, Build Skills Instead (AI Engineer) - Score: 87</span><br><span class="line">   2. How I use Claude Code (Meta Staff Engineer) - Score: 82</span><br><span class="line">   ...</span><br><span class="line"></span><br><span class="line">🎯 Key Takeaways</span><br><span class="line">   1. Skills 是 AI 的 SOP，确保一致性执行</span><br><span class="line">   2. 渐进式加载机制，仅 30-50 tokens 初始消耗</span><br><span class="line">   3. MCP 连接数据，Skills 处理数据</span><br><span class="line"></span><br><span class="line">📈 Trends</span><br><span class="line">   • Skills + MCP + Sub-agents 组合使用</span><br><span class="line">   • 用脚本替代 LLM 数学计算</span><br><span class="line"></span><br><span class="line">🔍 Outliers</span><br><span class="line">   • &quot;不要构建 Agent，构建 Skills 代替&quot;</span><br><span class="line"></span><br><span class="line">❓ Gaps</span><br><span class="line">   • 企业级部署最佳实践较少</span><br><span class="line">━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━</span><br></pre></td></tr></table></figure>

<h2 id="封装为-Claude-Skill"><a href="#封装为-Claude-Skill" class="headerlink" title="封装为 Claude Skill"></a>封装为 Claude Skill</h2><p>我把这个工作流封装成了一个 Claude Code Skill，使用时只需要说：</p>
<blockquote>
<p>“Research Claude Code Skills on YouTube”</p>
</blockquote>
<p>或者：</p>
<blockquote>
<p>“Create a podcast about React Server Components from YouTube research”</p>
</blockquote>
<p>Claude 会自动执行整个流程，无需人工干预。</p>
<p><strong>Skill 核心设计原则</strong>：</p>
<ol>
<li><strong>全自动化</strong>：步骤之间无需确认</li>
<li><strong>智能筛选</strong>：基于评分系统选择最佳视频</li>
<li><strong>并行处理</strong>：多个视频源同时处理</li>
<li><strong>错误容错</strong>：单个源失败不影响整体流程</li>
</ol>
<h2 id="实际效果"><a href="#实际效果" class="headerlink" title="实际效果"></a>实际效果</h2><p>我用这个工作流研究了 “Claude Code Skills 和 MCP”：</p>
<table>
<thead>
<tr>
<th>指标</th>
<th>数据</th>
</tr>
</thead>
<tbody><tr>
<td>搜索结果</td>
<td>50 个视频</td>
</tr>
<tr>
<td>精选分析</td>
<td>8 个视频</td>
</tr>
<tr>
<td>处理时间</td>
<td>约 15 分钟</td>
</tr>
<tr>
<td>产出</td>
<td>研究笔记 + 知识图谱 + 信息图</td>
</tr>
</tbody></table>
<p>传统方式可能需要 2-3 小时，现在只需要一杯咖啡的时间。</p>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><h3 id="✅-成功要素"><a href="#✅-成功要素" class="headerlink" title="✅ 成功要素"></a>✅ 成功要素</h3><ol>
<li><strong>评分系统是关键</strong>：好的评分算法能筛选出高质量内容</li>
<li><strong>多样性很重要</strong>：限制同一频道数量，获取不同视角</li>
<li><strong>NotebookLM 的视频理解能力很强</strong>：能准确提取关键信息</li>
<li><strong>输出格式要标准化</strong>：便于后续处理和引用</li>
</ol>
<h3 id="⚠️-注意事项"><a href="#⚠️-注意事项" class="headerlink" title="⚠️ 注意事项"></a>⚠️ 注意事项</h3><ol>
<li><strong>Windows 编码问题</strong>：NotebookLM CLI 输出包含 Unicode 符号，建议用 <code>--json</code> 参数</li>
<li><strong>处理时间不稳定</strong>：视频长度影响处理时间，需要设置合理超时</li>
<li><strong>Rate Limit</strong>：生成 podcast 等可能触发限制，建议等待 5 分钟后重试</li>
</ol>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>这个 YouTube Research Pipeline 展示了 AI 时代的研究新范式：</p>
<ul>
<li><strong>从手动到自动</strong>：AI 替代重复劳动</li>
<li><strong>从碎片到系统</strong>：结构化输出，便于后续使用</li>
<li><strong>从单一到多元</strong>：支持多种输出格式</li>
</ul>
<p>如果你也想尝试，可以参考本文的实现思路，或者直接使用 Claude Code 和 NotebookLM CLI 搭建自己的研究工作流。</p>
<hr>
<h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul>
<li><a href="https://github.com/yt-dlp/yt-dlp">yt-dlp 官方文档</a></li>
<li><a href="https://github.com/teng-lin/notebooklm-py">NotebookLM CLI (notebooklm-py)</a></li>
<li><a href="https://docs.anthropic.com/claude-code/skills">Claude Code Skills 文档</a></li>
<li><a href="/2026/03/09/Claude-Code-Skills%E4%B8%8EMCP%E6%B7%B1%E5%BA%A6%E7%A0%94%E7%A9%B6/">相关文章：Claude Code Skills 与 MCP 深度研究</a></li>
</ul>
]]></content>
      <categories>
        <category>AI经验</category>
      </categories>
      <tags>
        <tag>Claude</tag>
        <tag>Skills</tag>
        <tag>NotebookLM</tag>
        <tag>yt-dlp</tag>
        <tag>自动化</tag>
        <tag>研究工作流</tag>
      </tags>
  </entry>
  <entry>
    <title>用AI技能将代码分析文档工作自动化</title>
    <url>/2026/03/29/ai-skill-automation/</url>
    <content><![CDATA[<p>在日常开发中，每次排查 Bug 后的文档记录工作往往是重复且耗时的。手动创建「问题分析 + 修复指南 + 代码片段」一套文档需要 10-15 分钟，而且格式难以统一。本文分享如何将这套重复性工作抽象为一个可复用的 AI 技能，将时间缩短至 1-2 分钟。</p>
<span id="more"></span>

<h2 id="问题背景"><a href="#问题背景" class="headerlink" title="问题背景"></a>问题背景</h2><p>在南沙项目物联网开发中，每次 Bug 排查完成后都需要生成标准化的分析文档：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">问题分析.md      → 根因分析、影响范围、排查过程</span><br><span class="line">修复指南.md      → 修复步骤、前后代码对比、测试清单</span><br><span class="line">代码片段/*.md    → 关键代码片段</span><br></pre></td></tr></table></figure>

<p>以一个月内排查的 3 个 Bug 为例：</p>
<table>
<thead>
<tr>
<th>Bug</th>
<th>问题类型</th>
<th>涉及文件数</th>
<th>手动文档耗时</th>
</tr>
</thead>
<tbody><tr>
<td>个人月卡拒绝 404</td>
<td>接口路径错误</td>
<td>6</td>
<td>~12 分钟</td>
</tr>
<tr>
<td>月卡变更审批拒绝 404</td>
<td>审批状态判断错误</td>
<td>6</td>
<td>~12 分钟</td>
</tr>
<tr>
<td>月卡变更车位占用校验</td>
<td>逻辑错误</td>
<td>7</td>
<td>~15 分钟</td>
</tr>
</tbody></table>
<blockquote>
<p>[!tip] 痛点分析<br>这类文档工作有两个显著特点：一是<strong>流程固定</strong>，都是「问题分析 → 修复指南 → 代码片段」三件套；二是<strong>格式规范</strong>，需要统一的 frontmatter、callout、wikilinks 结构。高度模式化的工作，正是自动化的理想场景。</p>
</blockquote>
<hr>
<h2 id="解决方案：创建-obsidian-code-analysis-技能"><a href="#解决方案：创建-obsidian-code-analysis-技能" class="headerlink" title="解决方案：创建 obsidian-code-analysis 技能"></a>解决方案：创建 obsidian-code-analysis 技能</h2><p>基于 Claude Code 的 Skill 机制，将代码分析文档的创建流程封装为一个可复用的技能。</p>
<h3 id="技能设计"><a href="#技能设计" class="headerlink" title="技能设计"></a>技能设计</h3><p><strong>基本信息：</strong></p>
<table>
<thead>
<tr>
<th>属性</th>
<th>值</th>
</tr>
</thead>
<tbody><tr>
<td>技能名称</td>
<td><code>obsidian-code-analysis</code></td>
</tr>
<tr>
<td>存放路径</td>
<td><code>C:\Users\张\.claude\skills\obsidian-code-analysis\</code></td>
</tr>
<tr>
<td>触发条件</td>
<td>用户说「记录」「写到知识库」「保存分析」等</td>
</tr>
</tbody></table>
<h3 id="核心工作流"><a href="#核心工作流" class="headerlink" title="核心工作流"></a>核心工作流</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">主智能体收集分析上下文</span><br><span class="line">         ↓</span><br><span class="line">   确定文档类型（Bug / Feature / Refactor / General）</span><br><span class="line">         ↓</span><br><span class="line">   启动子智能体创建文件</span><br><span class="line">         ↓</span><br><span class="line">   调用 obsidian-markdown 技能格式化</span><br><span class="line">         ↓</span><br><span class="line">   使用 obsidian-cli 创建文件</span><br><span class="line">         ↓</span><br><span class="line">   更新每日工作记录</span><br></pre></td></tr></table></figure>

<p><img data-src="/images/posts/ai-skill-automation/fig1.png" alt="手动vs自动流程对比"><br>*图1：手动与自动文档创建流程对比</p>
<p><strong>关键设计决策：</strong></p>
<ol>
<li><p><strong>主从智能体分工</strong>：主智能体负责分析和收集上下文，子智能体负责所有文件创建。这样避免主上下文被大量写入操作污染。</p>
</li>
<li><p><strong>obsidian-cli 优先</strong>：<code>obsidian create</code> 比 Write 工具更节省 token，且直接与 Obsidian 集成。</p>
</li>
<li><p><strong>模板引用分离</strong>：模板放在 <code>references/</code> 目录，子智能体按需读取，不占用 SKILL.md 本身的空间。</p>
</li>
</ol>
<hr>
<h2 id="技能文件结构"><a href="#技能文件结构" class="headerlink" title="技能文件结构"></a>技能文件结构</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">obsidian-code-analysis/</span><br><span class="line">├── SKILL.md                          # 主技能文件</span><br><span class="line">│   ├── frontmatter（触发条件）</span><br><span class="line">│   ├── 工作流步骤</span><br><span class="line">│   ├── 子智能体 prompt 模板</span><br><span class="line">│   └── obsidian-cli 快速参考</span><br><span class="line">└── references/</span><br><span class="line">    ├── bug-templates.md              # Bug 分析模板</span><br><span class="line">    ├── feature-templates.md          # 功能设计模板</span><br><span class="line">    └── refactor-templates.md         # 重构分析模板</span><br></pre></td></tr></table></figure>

<p><img data-src="/images/posts/ai-skill-automation/fig2.png" alt="技能架构图"><br><em>图2：obsidian-code-analysis 技能文件结构</em></p>
<p><strong>支持的文档类型：</strong></p>
<table>
<thead>
<tr>
<th>类型</th>
<th>生成文件</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>Bug</td>
<td>问题分析.md、修复指南.md、代码片段&#x2F;*.md</td>
<td>包含根因分析、修复步骤、测试清单</td>
</tr>
<tr>
<td>Feature</td>
<td>需求分析.md、实现方案.md、代码片段&#x2F;*.md</td>
<td>包含需求拆解、技术选型、接口设计</td>
</tr>
<tr>
<td>Refactor</td>
<td>重构分析.md、重构方案.md、代码片段&#x2F;*.md</td>
<td>包含重构动机、影响范围、迁移计划</td>
</tr>
</tbody></table>
<hr>
<h2 id="实战验证：车位占用校验-Bug"><a href="#实战验证：车位占用校验-Bug" class="headerlink" title="实战验证：车位占用校验 Bug"></a>实战验证：车位占用校验 Bug</h2><p>以「月卡变更车位占用校验 Bug」作为测试用例，验证技能的实际效果。</p>
<h3 id="Bug-概要"><a href="#Bug-概要" class="headerlink" title="Bug 概要"></a>Bug 概要</h3><blockquote>
<p>[!bug] 问题描述<br>个人月卡变更固定车位时，新车位实际空闲，系统却提示「车位已经被占用」，导致无法提交变更申请。</p>
</blockquote>
<p><strong>排查发现 3 个 Bug：</strong></p>
<ol>
<li><strong>Bug 1</strong>：change() 方法检查的是老车位而非新车位</li>
<li><strong>Bug 2</strong>：DTO 缺少 newParkingSpotId 字段</li>
<li><strong>Bug 3</strong>：新旧车位用同一 ID 查询</li>
</ol>
<h3 id="技能执行结果"><a href="#技能执行结果" class="headerlink" title="技能执行结果"></a>技能执行结果</h3><p>使用 <code>obsidian-code-analysis</code> 技能自动生成了 7 个文件：</p>
<table>
<thead>
<tr>
<th>文件</th>
<th>状态</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>问题分析.md</td>
<td>pass</td>
<td>frontmatter 正确、callout 格式正确、根因清晰</td>
</tr>
<tr>
<td>修复指南.md</td>
<td>pass</td>
<td>步骤完整、前后代码对比清晰、测试清单齐全</td>
</tr>
<tr>
<td>代码片段&#x2F;change方法.md</td>
<td>pass</td>
<td>Bug 标注明确</td>
</tr>
<tr>
<td>代码片段&#x2F;changePass方法.md</td>
<td>pass</td>
<td>修复逻辑清晰</td>
</tr>
<tr>
<td>代码片段&#x2F;changeRefuse方法.md</td>
<td>pass</td>
<td>无需修改原因说明清晰</td>
</tr>
<tr>
<td>代码片段&#x2F;DTO.md</td>
<td>pass</td>
<td>新增字段标注明确</td>
</tr>
<tr>
<td>每日日志</td>
<td>pass</td>
<td>正确追加了新记录</td>
</tr>
</tbody></table>
<h3 id="修复方案示例"><a href="#修复方案示例" class="headerlink" title="修复方案示例"></a>修复方案示例</h3><p>以 change() 方法的修复为例，对比修复前后的关键差异：</p>
<p><strong>修复前（错误）：</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 检查的是老车位，用错了ID</span></span><br><span class="line"><span class="keyword">if</span>(monthPersonal.getParkingSpotId()!=<span class="literal">null</span>)&#123;</span><br><span class="line">    <span class="type">ParkSlotEntity</span> <span class="variable">spot</span> <span class="operator">=</span> parkSlotService.getById(monthPersonal.getParkingSpotId());</span><br><span class="line">    <span class="keyword">if</span>(!spot.getUsingState().equals(<span class="string">&quot;1&quot;</span>))&#123;</span><br><span class="line">        <span class="keyword">return</span> R.data(<span class="number">500</span>, <span class="literal">null</span>, <span class="string">&quot;车位已经被占用&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>修复后（正确）：</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 检查新车位，使用 DTO 传入的新车位ID</span></span><br><span class="line"><span class="type">Long</span> <span class="variable">newParkingSpotId</span> <span class="operator">=</span> dto.getNewParkingSpotId();</span><br><span class="line"><span class="keyword">if</span> (newParkingSpotId == <span class="literal">null</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> R.fail(<span class="string">&quot;缺少新车位ID&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="type">ParkSlotEntity</span> <span class="variable">newSpot</span> <span class="operator">=</span> parkSlotService.getById(newParkingSpotId);</span><br><span class="line"><span class="keyword">if</span> (!<span class="string">&quot;1&quot;</span>.equals(newSpot.getUsingState())) &#123;</span><br><span class="line">    <span class="keyword">return</span> R.data(<span class="number">500</span>, <span class="literal">null</span>, <span class="string">&quot;新车位已经被占用&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="效率对比"><a href="#效率对比" class="headerlink" title="效率对比"></a>效率对比</h2><table>
<thead>
<tr>
<th>指标</th>
<th>手动操作</th>
<th>技能自动化</th>
<th>提升</th>
</tr>
</thead>
<tbody><tr>
<td>文档创建时间</td>
<td>10-15 分钟</td>
<td>1-2 分钟</td>
<td><strong>~85%</strong></td>
</tr>
<tr>
<td>格式一致性</td>
<td>因人而异</td>
<td>100% 统一</td>
<td>-</td>
</tr>
<tr>
<td>wikilinks 交叉引用</td>
<td>容易遗漏</td>
<td>自动生成</td>
<td>-</td>
</tr>
<tr>
<td>错误率</td>
<td>较高</td>
<td>极低</td>
<td>-</td>
</tr>
</tbody></table>
<hr>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><h3 id="何时适合自动化"><a href="#何时适合自动化" class="headerlink" title="何时适合自动化"></a>何时适合自动化</h3><ul>
<li>工作流程<strong>高度结构化</strong>、模式固定</li>
<li>重复执行频率高（每月多次）</li>
<li>涉及多个文件、需要统一格式</li>
</ul>
<h3 id="技能创建的关键"><a href="#技能创建的关键" class="headerlink" title="技能创建的关键"></a>技能创建的关键</h3><ol>
<li><strong>先分析再抽象</strong>：识别重复模式，确认自动化价值</li>
<li><strong>单一日标</strong>：一个技能只做一件事，通过组合实现复杂流程</li>
<li><strong>模板与逻辑分离</strong>：模板可独立维护，SKILL.md 保持精简</li>
<li><strong>子智能体隔离</strong>：文件操作放到子智能体，保护主对话上下文</li>
</ol>
<h3 id="适用场景扩展"><a href="#适用场景扩展" class="headerlink" title="适用场景扩展"></a>适用场景扩展</h3><p>除了 Bug 分析，这个思路还适用于：</p>
<ul>
<li><strong>接口文档自动生成</strong>：根据代码注释生成 API 文档</li>
<li><strong>代码审查报告</strong>：自动整理审查意见并格式化</li>
<li><strong>周报&#x2F;月报汇总</strong>：从分散的工作记录聚合总结</li>
</ul>
<hr>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>AI 技能的本质是将人的工作方法论封装为可复用的工具。通过识别重复模式、抽象流程、创建技能，我们能让 AI 从「一次性的助手」变成「长期共事的专家」。文档工作的自动化只是开始，代码分析、架构设计、测试用例生成等环节都值得用同样的思路去探索。</p>
]]></content>
      <categories>
        <category>AI经验</category>
      </categories>
      <tags>
        <tag>Claude Code</tag>
        <tag>效率提升</tag>
        <tag>Obsidian</tag>
        <tag>技能创建</tag>
        <tag>AI工作流</tag>
      </tags>
  </entry>
  <entry>
    <title>Android 语言切换：硬编码 UI 文本修复实录</title>
    <url>/2026/04/02/android-i18n-fix/</url>
    <content><![CDATA[<p>Android App 语言切换功能已实现（<code>BaseApplication</code> + <code>attachBaseContext</code> 管道正常），但界面文本硬编码为英文，导致切换语言后 UI 仍显示英文。本文记录 presentation layer 的修复过程。</p>
<span id="more"></span>

<h2 id="问题背景"><a href="#问题背景" class="headerlink" title="问题背景"></a>问题背景</h2><blockquote>
<p>[!bug] 症状描述<br>语言切换后，底部 Tab 仍然显示 <code>HOME</code>、<code>ME</code>，”我的”页面仍然显示 <code>Settings</code>、<code>About Us</code> 等英文文本。但 <code>AppLanguage</code> 日志显示 <code>stored=zh applied=zh</code>，说明语言切换的核心管道没有问题。</p>
</blockquote>
<p><strong>根因</strong>：布局文件（XML）和 Kotlin 代码中存在大量硬编码英文字符串，未使用 <code>@string</code> 资源引用，导致语言切换后文本不跟随。</p>
<p><img data-src="/images/posts/android-i18n-fix/fig1.png" alt="硬编码文本 vs 资源引用对比"><br><em>图 1：硬编码文本（左）与资源引用（右）对比</em></p>
<hr>
<h2 id="技术分析"><a href="#技术分析" class="headerlink" title="技术分析"></a>技术分析</h2><h3 id="Android-i18n-架构"><a href="#Android-i18n-架构" class="headerlink" title="Android i18n 架构"></a>Android i18n 架构</h3><p>Android 的多语言支持依赖 <code>strings.xml</code> 资源文件。正确的实现方式是：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- values/strings.xml --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">string</span> <span class="attr">name</span>=<span class="string">&quot;mine_settings&quot;</span>&gt;</span>Settings<span class="tag">&lt;/<span class="name">string</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- values-zh/strings.xml --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">string</span> <span class="attr">name</span>=<span class="string">&quot;mine_settings&quot;</span>&gt;</span>设置<span class="tag">&lt;/<span class="name">string</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>在布局文件中引用：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">TextView</span> <span class="attr">android:text</span>=<span class="string">&quot;@string/mine_settings&quot;</span> /&gt;</span></span><br></pre></td></tr></table></figure>

<p>在 Kotlin 代码中引用：</p>
<figure class="highlight kotlin"><table><tr><td class="code"><pre><span class="line">text = getString(R.string.mine_settings)</span><br></pre></td></tr></table></figure>

<h3 id="硬编码文本的两大来源"><a href="#硬编码文本的两大来源" class="headerlink" title="硬编码文本的两大来源"></a>硬编码文本的两大来源</h3><table>
<thead>
<tr>
<th>来源</th>
<th>文件</th>
<th>影响</th>
</tr>
</thead>
<tbody><tr>
<td><strong>布局文件</strong></td>
<td><code>fragment_mine.xml</code>、<code>activity_main.xml</code> 等</td>
<td>静态文本，XML 中直接写死</td>
</tr>
<tr>
<td><strong>Kotlin 代码</strong></td>
<td><code>MainActivity.kt</code>、<code>HistoryActivity.kt</code>、<code>SettingActivity.kt</code></td>
<td>运行时动态设置的文本</td>
</tr>
</tbody></table>
<hr>
<h2 id="修复方案"><a href="#修复方案" class="headerlink" title="修复方案"></a>修复方案</h2><blockquote>
<p>[!tip] 修复策略<br>保持 <code>BaseApplication</code> + <code>attachBaseContext</code> 语言管道不变，聚焦 presentation layer：添加 32 个本地化字符串资源，替换 8 个布局文件 + 3 个 Kotlin 文件中的硬编码文本。</p>
</blockquote>
<p><img data-src="/images/posts/android-i18n-fix/fig2.png" alt="i18n 修复三步流程"><br><em>图 2：修复流程——添加资源 → 替换 XML → 替换 Kotlin 代码</em></p>
<h3 id="Step-1：添加字符串资源"><a href="#Step-1：添加字符串资源" class="headerlink" title="Step 1：添加字符串资源"></a>Step 1：添加字符串资源</h3><p>新增 32 个字符串资源键，覆盖所有硬编码文本：</p>
<table>
<thead>
<tr>
<th>资源文件</th>
<th>新增键数</th>
</tr>
</thead>
<tbody><tr>
<td><code>values/strings.xml</code></td>
<td>32 键（英文默认值）</td>
</tr>
<tr>
<td><code>values-zh/strings.xml</code></td>
<td>32 键（中文翻译）</td>
</tr>
<tr>
<td><code>values-en/strings.xml</code></td>
<td>32 键（英文镜像）</td>
</tr>
</tbody></table>
<p><strong>核心资源键示例</strong>：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- Main Tab --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">string</span> <span class="attr">name</span>=<span class="string">&quot;main_tab_home&quot;</span>&gt;</span>HOME<span class="tag">&lt;/<span class="name">string</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">string</span> <span class="attr">name</span>=<span class="string">&quot;main_tab_me&quot;</span>&gt;</span>ME<span class="tag">&lt;/<span class="name">string</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- Mine Fragment --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">string</span> <span class="attr">name</span>=<span class="string">&quot;mine_reading_novels&quot;</span>&gt;</span>Novel being read<span class="tag">&lt;/<span class="name">string</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">string</span> <span class="attr">name</span>=<span class="string">&quot;mine_settings&quot;</span>&gt;</span>Settings<span class="tag">&lt;/<span class="name">string</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- Settings Activity --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">string</span> <span class="attr">name</span>=<span class="string">&quot;settings_clear_cache_confirm&quot;</span>&gt;</span>Are you sure you want to clear the local cache?<span class="tag">&lt;/<span class="name">string</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="Step-2：替换布局文件硬编码文本"><a href="#Step-2：替换布局文件硬编码文本" class="headerlink" title="Step 2：替换布局文件硬编码文本"></a>Step 2：替换布局文件硬编码文本</h3><table>
<thead>
<tr>
<th>文件</th>
<th>替换数量</th>
</tr>
</thead>
<tbody><tr>
<td><code>fragment_mine.xml</code></td>
<td>10 处</td>
</tr>
<tr>
<td><code>activity_main.xml</code></td>
<td>1 处</td>
</tr>
<tr>
<td><code>tab_item_badge.xml</code></td>
<td>1 处</td>
</tr>
<tr>
<td><code>fragment_msg.xml</code></td>
<td>1 处</td>
</tr>
<tr>
<td><code>activity_about.xml</code></td>
<td>1 处</td>
</tr>
<tr>
<td><code>activity_histery.xml</code></td>
<td>2 处</td>
</tr>
<tr>
<td><code>activity_contact.xml</code></td>
<td>2 处</td>
</tr>
<tr>
<td><code>activity_setting.xml</code></td>
<td>7 处</td>
</tr>
</tbody></table>
<p><strong>修改示例</strong>（<code>fragment_mine.xml</code>）：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 修改前 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">TextView</span> <span class="attr">android:text</span>=<span class="string">&quot;Settings&quot;</span> <span class="attr">...</span> /&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- 修改后 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">TextView</span> <span class="attr">android:text</span>=<span class="string">&quot;@string/mine_settings&quot;</span> <span class="attr">...</span> /&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="Step-3：替换-Kotlin-运行时硬编码文本"><a href="#Step-3：替换-Kotlin-运行时硬编码文本" class="headerlink" title="Step 3：替换 Kotlin 运行时硬编码文本"></a>Step 3：替换 Kotlin 运行时硬编码文本</h3><p><strong><code>MainActivity.kt</code></strong> —— Tab 标签列表：</p>
<figure class="highlight kotlin"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 修改前</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">var</span> mStringList = mutableListOf(<span class="string">&quot;HOME&quot;</span>,<span class="string">&quot;Classification&quot;</span>, <span class="string">&quot;&quot;</span>,<span class="string">&quot;Message&quot;</span>, <span class="string">&quot;ME&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 修改后</span></span><br><span class="line"><span class="keyword">lateinit</span> <span class="keyword">var</span> mTabTitles: List&lt;String&gt;</span><br><span class="line"></span><br><span class="line"><span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">initView</span><span class="params">()</span></span> &#123;</span><br><span class="line">    mTabTitles = listOf(</span><br><span class="line">        getString(R.string.main_tab_home),</span><br><span class="line">        getString(R.string.main_tab_classification),</span><br><span class="line">        <span class="string">&quot;&quot;</span>,</span><br><span class="line">        getString(R.string.main_tab_message),</span><br><span class="line">        getString(R.string.main_tab_me)</span><br><span class="line">    )</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong><code>HistoryActivity.kt</code></strong> —— Toast 和 Toolbar 文本：</p>
<figure class="highlight kotlin"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 修改前</span></span><br><span class="line">toastSuccess(<span class="string">&quot;Deleted successfully&quot;</span>)</span><br><span class="line">mDataBinding.toolbar.getRightText().text = <span class="string">&quot;Edit&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 修改后</span></span><br><span class="line">toastSuccess(getString(R.string.history_delete_success))</span><br><span class="line">mDataBinding.toolbar.getRightText().text = getString(R.string.history_edit)</span><br></pre></td></tr></table></figure>

<p><strong><code>SettingActivity.kt</code></strong> —— 对话框文本：</p>
<figure class="highlight kotlin"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 修改前</span></span><br><span class="line">pop.showLogoPop(</span><br><span class="line">    <span class="string">&quot;Are you sure you want to clear the local cache?&quot;</span>,</span><br><span class="line">    <span class="string">&quot;Cancel&quot;</span>,</span><br><span class="line">    <span class="string">&quot;Confirm&quot;</span>,</span><br><span class="line">    ...</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 修改后</span></span><br><span class="line">pop.showLogoPop(</span><br><span class="line">    getString(R.string.settings_clear_cache_confirm),</span><br><span class="line">    getString(R.string.common_cancel),</span><br><span class="line">    getString(R.string.common_confirm),</span><br><span class="line">    ...</span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="验证结果"><a href="#验证结果" class="headerlink" title="验证结果"></a>验证结果</h2><h3 id="构建验证"><a href="#构建验证" class="headerlink" title="构建验证"></a>构建验证</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">.\gradlew.bat --no-daemon :app:compileDebugKotlin</span><br><span class="line"><span class="comment"># BUILD SUCCESSFUL</span></span><br></pre></td></tr></table></figure>

<h3 id="USB-真机验证"><a href="#USB-真机验证" class="headerlink" title="USB 真机验证"></a>USB 真机验证</h3><p><strong>日志验证</strong>（确认语言已正确应用）：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">AppLanguage: SettingActivity stored=zh applied=zh</span><br><span class="line">AppLanguage: MainActivity stored=zh applied=zh</span><br></pre></td></tr></table></figure>

<p><strong>UI Dump 验证</strong>（确认中文文本正确显示）：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;!-- 切换为中文后 --&gt;</span><br><span class="line">我的 / 阅读中的小说 / 已读完小说 / 我的 OKEX 金币 / 推荐奖励</span><br><span class="line">会员中心 / 浏览历史 / 关于我们 / 联系客服 / 设置</span><br><span class="line">首页 / 分类 / 书库 / 消息</span><br></pre></td></tr></table></figure>

<p><img data-src="/images/posts/android-i18n-fix/fig3.png" alt="真机测试中文 UI 验证"><br><em>图 3：真机测试——中文文本正确显示</em></p>
<hr>
<h2 id="修改文件汇总"><a href="#修改文件汇总" class="headerlink" title="修改文件汇总"></a>修改文件汇总</h2><table>
<thead>
<tr>
<th>文件</th>
<th>改动类型</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>app/src/main/res/values/strings.xml</code></td>
<td>新增</td>
<td>32 个英文资源键</td>
</tr>
<tr>
<td><code>app/src/main/res/values-zh/strings.xml</code></td>
<td>新增</td>
<td>32 个中文翻译</td>
</tr>
<tr>
<td><code>app/src/main/res/values-en/strings.xml</code></td>
<td>新增</td>
<td>32 个英文镜像</td>
</tr>
<tr>
<td><code>fragment_mine.xml</code></td>
<td>修改</td>
<td>10 处文本替换</td>
</tr>
<tr>
<td><code>activity_main.xml</code></td>
<td>修改</td>
<td>1 处文本替换</td>
</tr>
<tr>
<td><code>tab_item_badge.xml</code></td>
<td>修改</td>
<td>1 处文本替换</td>
</tr>
<tr>
<td><code>fragment_msg.xml</code></td>
<td>修改</td>
<td>1 处文本替换</td>
</tr>
<tr>
<td><code>activity_about.xml</code></td>
<td>修改</td>
<td>1 处文本替换</td>
</tr>
<tr>
<td><code>activity_histery.xml</code></td>
<td>修改</td>
<td>2 处文本替换</td>
</tr>
<tr>
<td><code>activity_contact.xml</code></td>
<td>修改</td>
<td>2 处文本替换</td>
</tr>
<tr>
<td><code>activity_setting.xml</code></td>
<td>修改</td>
<td>7 处文本替换</td>
</tr>
<tr>
<td><code>MainActivity.kt</code></td>
<td>修改</td>
<td>Tab 标签运行时本地化</td>
</tr>
<tr>
<td><code>HistoryActivity.kt</code></td>
<td>修改</td>
<td>Toast&#x2F;Toolbar 本地化</td>
</tr>
<tr>
<td><code>SettingActivity.kt</code></td>
<td>修改</td>
<td>对话框文本本地化</td>
</tr>
</tbody></table>
<hr>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><blockquote>
<p>[!warning] 注意事项</p>
<ul>
<li><strong>XML 布局文件中的文本也要走资源引用</strong>，不能直接写死英文</li>
<li><strong>Kotlin 代码中动态设置的文本</strong>，<code>List</code>&#x2F;<code>Array</code> 等初始化必须使用 <code>getString(R.string.xxx)</code>，而非直接硬编码</li>
<li><strong>资源键命名规范</strong>：<code>&#123;页面&#125;_&#123;功能&#125;_&#123;描述&#125;</code>，如 <code>mine_settings</code>、<code>history_delete_success</code></li>
</ul>
</blockquote>
<p><strong>相关文档</strong>：</p>
<ul>
<li>[[语言切换硬编码UI文本修复&#x2F;需求分析]]（Obsidian 内部链接）</li>
<li>[[语言切换硬编码UI文本修复&#x2F;实现方案]]</li>
</ul>
<hr>
<p><em>本文使用 <a href="https://hexo.io/">Hexo</a> + <a href="https://github.com/ppoffice/hexo-theme-icarus">icarus</a> 主题构建，配图由 MiniMax Image-01 生成。</em></p>
]]></content>
      <categories>
        <category>Android 开发</category>
      </categories>
      <tags>
        <tag>踩坑记录</tag>
        <tag>Android</tag>
        <tag>i18n</tag>
        <tag>国际化</tag>
        <tag>Kotlin</tag>
      </tags>
  </entry>
  <entry>
    <title>从账号泄露到 N+1 优化：一次 Spring Boot + Vue 项目的大改造</title>
    <url>/2026/04/15/%E4%BB%8E%E8%B4%A6%E5%8F%B7%E6%B3%84%E9%9C%B2%E5%88%B0N+1%E4%BC%98%E5%8C%96/</url>
    <content><![CDATA[<p>一次图书管理系统的全面优化，涵盖安全漏洞修复、N+1 查询优化、凭证管理强化以及数据库索引调优。从踩坑到解决，完整还原优化全过程。</p>
<span id="more"></span>

<center>

<h2 id="🎵-配乐：暖阳下的代码时光"><a href="#🎵-配乐：暖阳下的代码时光" class="headerlink" title="🎵 配乐：暖阳下的代码时光"></a>🎵 配乐：暖阳下的代码时光</h2><p><em>轻柔钢琴与弦乐交织，适合编程与思考的浪漫背景音乐</em></p>
<html>
<audio controls>
  <source src="/images/posts/从账号泄露到N+1优化/bgm.mp3" type="audio/mpeg">
  您的浏览器不支持音频播放
</audio>
</html>

<hr>
<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>做项目最怕的不是功能做不出来，而是上线之后才发现有<strong>安全漏洞</strong>，或者用户抱怨系统”慢得像蜗牛”。最近对图书管理系统进行了一次全面”体检”，发现了不少问题：用户密码明文暴露、N+1 查询导致审核页面卡死、OSS 凭证硬编码在代码里、数据库缺少关键索引……这些问题单个看都不大，但叠加在一起，足以让一个系统从”能用”变成”难用”。</p>
<p>本文完整记录了这次优化全过程，包括问题分析、解决思路、核心代码改动，以及数据库索引优化策略。如果你也在维护类似技术栈（Spring Boot + Vue）的项目，相信能从中学到一些实战经验。</p>
<hr>
<h2 id="一、问题全景：这次优化了什么"><a href="#一、问题全景：这次优化了什么" class="headerlink" title="一、问题全景：这次优化了什么"></a>一、问题全景：这次优化了什么</h2><p>在开始深入细节之前，先整体看一下这次优化的四个核心问题：</p>
<table>
<thead>
<tr>
<th>编号</th>
<th>问题</th>
<th>类型</th>
<th>优先级</th>
<th>涉及范围</th>
</tr>
</thead>
<tbody><tr>
<td>OPT-51</td>
<td>User.password 泄露 + 密码修改逻辑缺陷</td>
<td>安全</td>
<td>高</td>
<td>User.java, UserController.java, Password.vue</td>
</tr>
<tr>
<td>OPT-70</td>
<td>BookAudit N+1 查询优化</td>
<td>性能</td>
<td>高</td>
<td>Book.java, BookMapper.java, StudentBookController.java, BookAudit.vue</td>
</tr>
<tr>
<td>OPT-54</td>
<td>硬编码凭证清理 + 环境变量化</td>
<td>安全</td>
<td>高</td>
<td>OssConfig.java, application.properties</td>
</tr>
<tr>
<td>OPT-60&#x2F;61</td>
<td>数据库索引优化 Round4</td>
<td>性能</td>
<td>中</td>
<td>sql&#x2F; 脚本，user&#x2F;book&#x2F;teacher&#x2F;lend_rule 等表</td>
</tr>
</tbody></table>
<p>四个问题分为两类：<strong>安全加固</strong>和<strong>性能提升</strong>。安全问题是底线，性能问题影响体验，两者都不能忽视。</p>
<hr>
<h2 id="二、密码泄露危机：User-password-的-JsonIgnore-修复"><a href="#二、密码泄露危机：User-password-的-JsonIgnore-修复" class="headerlink" title="二、密码泄露危机：User.password 的 JsonIgnore 修复"></a>二、密码泄露危机：User.password 的 JsonIgnore 修复</h2><p><img data-src="/images/posts/%E4%BB%8E%E8%B4%A6%E5%8F%B7%E6%B3%84%E9%9C%B2%E5%88%B0N+1%E4%BC%98%E5%8C%96/fig1.png" alt="密码泄露问题示意"><br><em>图1：密码泄露问题示意</em></p>
<h3 id="2-1-问题是怎么被发现的"><a href="#2-1-问题是怎么被发现的" class="headerlink" title="2.1 问题是怎么被发现的"></a>2.1 问题是怎么被发现的</h3><p>在做登录功能测试时，我随手打了一个 <code>console.log(user)</code>，结果在控制台看到了这个：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="number">1</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;username&quot;</span><span class="punctuation">:</span> <span class="string">&quot;admin&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;password&quot;</span><span class="punctuation">:</span> <span class="string">&quot;123456&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;nickName&quot;</span><span class="punctuation">:</span> <span class="string">&quot;管理员&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;role&quot;</span><span class="punctuation">:</span> <span class="string">&quot;ADMIN&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>密码居然是明文！当时就意识到这是一个<strong>严重的安全漏洞</strong>。只要有人通过浏览器的 Network 面板或者抓包工具查看登录接口的返回值，就能拿到所有用户的明文密码。</p>
<p>进一步排查发现，<code>User.java</code> 的 password 字段缺少 <code>@JsonIgnore</code> 注解。这意味着：</p>
<ul>
<li>登录接口返回用户信息 → 密码暴露</li>
<li>任何将 User 对象序列化的地方 → 密码暴露</li>
<li>如果把用户信息存入 sessionStorage 或 LocalStorage → 密码暴露</li>
</ul>
<h3 id="2-2-密码修改功能的连锁问题"><a href="#2-2-密码修改功能的连锁问题" class="headerlink" title="2.2 密码修改功能的连锁问题"></a>2.2 密码修改功能的连锁问题</h3><p>修复 <code>@JsonIgnore</code> 之后，新的问题接踵而至：<code>Password.vue</code> 第 97 行有一段客户端旧密码比对逻辑：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> storedUser = <span class="title class_">JSON</span>.<span class="title function_">parse</span>(<span class="variable language_">sessionStorage</span>.<span class="title function_">getItem</span>(<span class="string">&#x27;user&#x27;</span>) || <span class="string">&#x27;&#123;&#125;&#x27;</span>);</span><br><span class="line"><span class="keyword">if</span> (storedUser.<span class="property">password</span> !== <span class="variable language_">this</span>.<span class="property">form</span>.<span class="property">oldPassword</span>) &#123;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">$message</span>.<span class="title function_">error</span>(<span class="string">&#x27;原密码错误&#x27;</span>);</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>加了 <code>@JsonIgnore</code> 之后，从 sessionStorage 取出的 <code>user.password</code> 变成了 <code>undefined</code>，导致所有旧密码验证都失败。用户输入正确密码也会被系统拒绝。</p>
<h3 id="2-3-解决方案：服务端验证-移除客户端逻辑"><a href="#2-3-解决方案：服务端验证-移除客户端逻辑" class="headerlink" title="2.3 解决方案：服务端验证 + 移除客户端逻辑"></a>2.3 解决方案：服务端验证 + 移除客户端逻辑</h3><p>正确的做法是<strong>把密码验证逻辑后移到后端</strong>，前端只负责收集用户输入：</p>
<p><strong>第一步：<code>User.java</code> 添加 <code>@JsonIgnore</code></strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> com.fasterxml.jackson.annotation.JsonIgnore;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">User</span> &#123;</span><br><span class="line">    <span class="comment">// ... 其他字段 ...</span></span><br><span class="line"></span><br><span class="line">    <span class="meta">@JsonIgnore</span>  <span class="comment">// 新增：防止密码在 JSON 序列化时泄露</span></span><br><span class="line">    <span class="keyword">private</span> String password;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>第二步：<code>UserController.java</code> 增加旧密码验证</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@PutMapping(&quot;/password&quot;)</span></span><br><span class="line"><span class="keyword">public</span> Result&lt;?&gt; updatePassword(<span class="meta">@RequestParam</span> Integer id,</span><br><span class="line">                                <span class="meta">@RequestParam</span> String password2,</span><br><span class="line">                                <span class="meta">@RequestParam</span> String oldPassword) &#123;</span><br><span class="line">    <span class="type">User</span> <span class="variable">existing</span> <span class="operator">=</span> userMapper.selectById(id);</span><br><span class="line">    <span class="keyword">if</span> (existing == <span class="literal">null</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> Result.error(<span class="string">&quot;-1&quot;</span>, <span class="string">&quot;用户不存在&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 服务端验证旧密码</span></span><br><span class="line">    <span class="keyword">if</span> (!existing.getPassword().equals(oldPassword)) &#123;</span><br><span class="line">        <span class="keyword">return</span> Result.error(<span class="string">&quot;-1&quot;</span>, <span class="string">&quot;原密码错误&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    LambdaUpdateWrapper&lt;User&gt; updateWrapper = Wrappers.&lt;User&gt;lambdaUpdate()</span><br><span class="line">            .eq(User::getId, id);</span><br><span class="line">    <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>();</span><br><span class="line">    user.setPassword(password2);</span><br><span class="line">    userMapper.update(user, updateWrapper);</span><br><span class="line">    <span class="keyword">return</span> Result.success();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>第三步：<code>Password.vue</code> 删除客户端验证</strong></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">async</span> <span class="title function_">submitForm</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">$refs</span>.<span class="property">form</span>.<span class="title function_">validate</span>(<span class="keyword">async</span> valid =&gt; &#123;</span><br><span class="line">        <span class="keyword">if</span> (valid) &#123;</span><br><span class="line">            <span class="keyword">const</span> res = <span class="keyword">await</span> <span class="variable language_">this</span>.$put(<span class="string">&#x27;/user/password&#x27;</span>, &#123;</span><br><span class="line">                <span class="attr">id</span>: <span class="variable language_">this</span>.<span class="property">user</span>.<span class="property">id</span>,</span><br><span class="line">                <span class="attr">oldPassword</span>: <span class="variable language_">this</span>.<span class="property">form</span>.<span class="property">oldPassword</span>,</span><br><span class="line">                <span class="attr">password2</span>: <span class="variable language_">this</span>.<span class="property">form</span>.<span class="property">password2</span></span><br><span class="line">            &#125;);</span><br><span class="line">            <span class="keyword">if</span> (res.<span class="property">code</span> === <span class="string">&#x27;0&#x27;</span>) &#123;</span><br><span class="line">                <span class="variable language_">this</span>.<span class="property">$message</span>.<span class="title function_">success</span>(<span class="string">&#x27;密码修改成功&#x27;</span>);</span><br><span class="line">                <span class="variable language_">this</span>.<span class="property">$refs</span>.<span class="property">form</span>.<span class="title function_">resetFields</span>();</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="variable language_">this</span>.<span class="property">$message</span>.<span class="title function_">error</span>(res.<span class="property">msg</span> || <span class="string">&#x27;原密码错误&#x27;</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-4-经验总结"><a href="#2-4-经验总结" class="headerlink" title="2.4 经验总结"></a>2.4 经验总结</h3><ul>
<li><strong>永远不要在客户端存储或比对密码</strong>：密码验证必须在服务端完成，客户端只负责传输用户输入。</li>
<li><strong><code>@JsonIgnore</code> 是敏感字段的标配</strong>：任何序列化场景（API 响应、缓存存储、日志输出）都要考虑是否包含敏感字段。</li>
<li><strong>先找影响范围再动手</strong>：修复安全问题时，最好先梳理清楚所有依赖点，避免”修复一个 bug 引入三个新 bug”。</li>
</ul>
<hr>
<h2 id="三、N-1-查询噩梦：BookAudit-审核页面的性能陷阱"><a href="#三、N-1-查询噩梦：BookAudit-审核页面的性能陷阱" class="headerlink" title="三、N+1 查询噩梦：BookAudit 审核页面的性能陷阱"></a>三、N+1 查询噩梦：BookAudit 审核页面的性能陷阱</h2><p><img data-src="/images/posts/%E4%BB%8E%E8%B4%A6%E5%8F%B7%E6%B3%84%E9%9C%B2%E5%88%B0N+1%E4%BC%98%E5%8C%96/fig2.png" alt="N+1查询优化对比"><br><em>图2：N+1 查询优化前后对比</em></p>
<h3 id="3-1-问题现象"><a href="#3-1-问题现象" class="headerlink" title="3.1 问题现象"></a>3.1 问题现象</h3><p>打开图书审核页面，加载 20 本待审核的图书，需要大约 <strong>5-6 秒</strong>。这个速度对于一个内部管理系统来说实在太慢了。</p>
<p>通过浏览器的 Network 面板分析请求，发现了问题：主请求只有 1 个，但 <code>/user/&#123;id&#125;</code> 请求有 20 个！每显示一本书，都要单独请求一次上传者的用户名。</p>
<p>这就是经典的 <strong>N+1 查询问题</strong>：1 次主查询 + N 次关联查询。</p>
<h3 id="3-2-问题根因分析"><a href="#3-2-问题根因分析" class="headerlink" title="3.2 问题根因分析"></a>3.2 问题根因分析</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">主请求: GET /studentBook/pendingAudit</span><br><span class="line">        → 返回 20 本书（不含 uploaderName）</span><br><span class="line"></span><br><span class="line">前端 for each book:</span><br><span class="line">  GET /user/&#123;book.uploaderId&#125;</span><br><span class="line">  → 返回用户信息（含 nickName）</span><br><span class="line"></span><br><span class="line">总请求数: 1 + 20 = 21 次</span><br></pre></td></tr></table></figure>

<p>图书审核页面需要显示上传者名称，但后端接口只返回 Book 实体，不包含用户信息。前端不得不逐行调用用户接口获取上传者名称，导致请求数量暴增。</p>
<h3 id="3-3-解决方案：JOIN-查询一次搞定"><a href="#3-3-解决方案：JOIN-查询一次搞定" class="headerlink" title="3.3 解决方案：JOIN 查询一次搞定"></a>3.3 解决方案：JOIN 查询一次搞定</h3><p><strong>思路转变</strong>：与其让前端发多次请求，不如后端直接 JOIN 一次查出来。</p>
<p><strong>第一步：<code>Book.java</code> 添加虚拟字段</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Book</span> &#123;</span><br><span class="line">    <span class="comment">// ... 原有字段 ...</span></span><br><span class="line"></span><br><span class="line">    <span class="meta">@TableField(exist = false)</span>  <span class="comment">// 标记为非数据库字段</span></span><br><span class="line">    <span class="keyword">private</span> String uploaderName;  <span class="comment">// 上传者名称（来自 JOIN 查询）</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>第二步：<code>BookMapper.java</code> 新增 JOIN 方法</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Select(&quot;SELECT b.*, u.nick_name AS uploader_name FROM book b &quot; +</span></span><br><span class="line"><span class="meta">        &quot;LEFT JOIN user u ON b.uploader_id = u.id &quot; +</span></span><br><span class="line"><span class="meta">        &quot;WHERE (b.audit_status IS NULL OR b.audit_status = 0) &quot; +</span></span><br><span class="line"><span class="meta">        &quot;ORDER BY b.create_time DESC&quot;)</span></span><br><span class="line">IPage&lt;Book&gt; <span class="title function_">selectPendingAuditWithUploader</span><span class="params">(IPage&lt;Book&gt; page)</span>;</span><br></pre></td></tr></table></figure>

<p><strong>第三步：<code>StudentBookController.java</code> 改用新方法</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@GetMapping(&quot;/pendingAudit&quot;)</span></span><br><span class="line"><span class="keyword">public</span> Result&lt;?&gt; pendingAudit(Page&lt;Book&gt; page) &#123;</span><br><span class="line">    IPage&lt;Book&gt; result = bookMapper.selectPendingAuditWithUploader(page);</span><br><span class="line">    <span class="keyword">return</span> Result.success(result);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>第四步：<code>BookAudit.vue</code> 删除 loadUploaderInfo()</strong></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 整个 loadUploaderInfo 方法删除</span></span><br><span class="line"><span class="comment">// 表格数据直接使用 book.uploaderName</span></span><br><span class="line">&lt;el-table :data=<span class="string">&quot;tableData&quot;</span>&gt;</span><br><span class="line">    <span class="language-xml"><span class="tag">&lt;<span class="name">el-table-column</span> <span class="attr">prop</span>=<span class="string">&quot;uploaderName&quot;</span> <span class="attr">label</span>=<span class="string">&quot;上传者&quot;</span> /&gt;</span></span></span><br><span class="line">    &lt;!-- 其他列 --&gt;</span><br><span class="line">&lt;/el-table&gt;</span><br></pre></td></tr></table></figure>

<h3 id="3-4-性能对比"><a href="#3-4-性能对比" class="headerlink" title="3.4 性能对比"></a>3.4 性能对比</h3><table>
<thead>
<tr>
<th>指标</th>
<th>优化前</th>
<th>优化后</th>
</tr>
</thead>
<tbody><tr>
<td>请求数（审核 20 本书）</td>
<td>21 次</td>
<td>1 次</td>
</tr>
<tr>
<td>预计耗时</td>
<td>~500ms+</td>
<td>~50ms</td>
</tr>
<tr>
<td>RTT 开销</td>
<td>高</td>
<td>低</td>
</tr>
</tbody></table>
<p>优化后页面加载时间从 5-6 秒降到不足 1 秒，用户体验大幅提升。</p>
<h3 id="3-5-经验总结"><a href="#3-5-经验总结" class="headerlink" title="3.5 经验总结"></a>3.5 经验总结</h3><ul>
<li><strong>后端思维</strong>：如果页面需要展示关联数据，优先在后端用 JOIN 一次查出来，而不是让前端发多次请求。</li>
<li><strong>N+1 是性能大敌</strong>：在设计接口时就要考虑是否会触发 N+1 查询，提前规避。</li>
<li><strong><code>@TableField(exist = false)</code> 的妙用</strong>：用于接收 JOIN 查询的衍生字段，不影响原有数据库结构。</li>
</ul>
<hr>
<h2 id="四、凭证裸奔：OSS-和-API-Key-的硬编码危机"><a href="#四、凭证裸奔：OSS-和-API-Key-的硬编码危机" class="headerlink" title="四、凭证裸奔：OSS 和 API Key 的硬编码危机"></a>四、凭证裸奔：OSS 和 API Key 的硬编码危机</h2><p><img data-src="/images/posts/%E4%BB%8E%E8%B4%A6%E5%8F%B7%E6%B3%84%E9%9C%B2%E5%88%B0N+1%E4%BC%98%E5%8C%96/fig3.png" alt="凭证环境变量管理"><br><em>图3：凭证安全管理的关键——环境变量注入</em></p>
<h3 id="4-1-问题有多严重"><a href="#4-1-问题有多严重" class="headerlink" title="4.1 问题有多严重"></a>4.1 问题有多严重</h3><p>检查代码时发现，<code>OssConfig.java</code> 里写着：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="title function_">OssConfig</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="built_in">this</span>.accessKeyId = <span class="string">&quot;LTAI4xxxxxxxxxxxxxx&quot;</span>;</span><br><span class="line">    <span class="built_in">this</span>.accessKeySecret = <span class="string">&quot;Abcdefxxxxxxxxxxxxxx&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>application.properties</code> 里也有：</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="attr">aliyun.oss.access-key-id</span>=<span class="string">LTAI4xxxxxxxxxxxxxx</span></span><br><span class="line"><span class="attr">aliyun.oss.access-key-secret</span>=<span class="string">Abcdefxxxxxxxxxxxxxx</span></span><br></pre></td></tr></table></figure>

<p>这意味着：代码提交到 Git 仓库之后，这些凭证就<strong>永久暴露</strong>了。任何能访问代码库的人都能拿到云资源的访问权限。</p>
<p>更危险的是，启动日志还会打印这些凭证：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">log.info(<span class="string">&quot;OSS配置加载，accessKeyId: &#123;&#125;&quot;</span>, accessKeyId);</span><br></pre></td></tr></table></figure>

<h3 id="4-2-解决方案：环境变量注入"><a href="#4-2-解决方案：环境变量注入" class="headerlink" title="4.2 解决方案：环境变量注入"></a>4.2 解决方案：环境变量注入</h3><p><strong><code>OssConfig.java</code> 重构</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@ConfigurationProperties(prefix = &quot;aliyun.oss&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">OssConfig</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> String accessKeyId;      <span class="comment">// 从环境变量注入，不再硬编码</span></span><br><span class="line">    <span class="keyword">private</span> String accessKeySecret;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@PostConstruct</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">init</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// 只记录初始化状态，不打印敏感信息</span></span><br><span class="line">        log.info(<span class="string">&quot;OSS配置已加载&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong><code>application.properties</code> 改用占位符</strong></p>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="comment"># OSS凭证 — 通过环境变量注入，不提交到代码库</span></span><br><span class="line"><span class="attr">aliyun.oss.access-key-id</span>=<span class="string">$&#123;OSS_ACCESS_KEY_ID:&#125;</span></span><br><span class="line"><span class="attr">aliyun.oss.access-key-secret</span>=<span class="string">$&#123;OSS_ACCESS_KEY_SECRET:&#125;</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment"># API密钥 — 通过环境变量注入</span></span><br><span class="line"><span class="attr">ai.bailian.api.key</span>=<span class="string">$&#123;AI_API_KEY:&#125;</span></span><br></pre></td></tr></table></figure>

<p><strong>生产环境配置</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 在服务器上设置环境变量</span></span><br><span class="line"><span class="built_in">export</span> OSS_ACCESS_KEY_ID=your_access_key_id</span><br><span class="line"><span class="built_in">export</span> OSS_ACCESS_KEY_SECRET=your_access_key_secret</span><br><span class="line"><span class="built_in">export</span> AI_API_KEY=your_api_key</span><br></pre></td></tr></table></figure>

<h3 id="4-3-安全建议"><a href="#4-3-安全建议" class="headerlink" title="4.3 安全建议"></a>4.3 安全建议</h3><ul>
<li><strong>生产环境使用密钥管理服务</strong>：如阿里云 KMS、AWS Secrets Manager 等。</li>
<li><strong>本地开发使用 <code>.env</code> 文件</strong>：已在 <code>.gitignore</code> 中，避免误提交。</li>
<li><strong>定期轮换凭证</strong>：即使泄露了，及时轮换也能降低损失。</li>
</ul>
<hr>
<h2 id="五、数据库索引优化：让查询飞起来"><a href="#五、数据库索引优化：让查询飞起来" class="headerlink" title="五、数据库索引优化：让查询飞起来"></a>五、数据库索引优化：让查询飞起来</h2><p><img data-src="/images/posts/%E4%BB%8E%E8%B4%A6%E5%8F%B7%E6%B3%84%E9%9C%B2%E5%88%B0N+1%E4%BC%98%E5%8C%96/fig4.png" alt="数据库索引优化"><br><em>图4：索引优化让数据库查询效率大幅提升</em></p>
<h3 id="5-1-为什么需要索引优化"><a href="#5-1-为什么需要索引优化" class="headerlink" title="5.1 为什么需要索引优化"></a>5.1 为什么需要索引优化</h3><p>项目跑了一段时间后，一些高频查询开始变慢。排查发现，部分热点查询缺少索引，导致数据库进行全表扫描。举个例子，用户登录时按用户名查询：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> <span class="keyword">user</span> <span class="keyword">WHERE</span> username <span class="operator">=</span> <span class="string">&#x27;admin&#x27;</span>;</span><br></pre></td></tr></table></figure>

<p>没有索引的话，MySQL 需要遍历全表才能找到匹配行。随着数据量增长，这个查询会越来越慢。</p>
<h3 id="5-2-新增索引（8-个）"><a href="#5-2-新增索引（8-个）" class="headerlink" title="5.2 新增索引（8 个）"></a>5.2 新增索引（8 个）</h3><table>
<thead>
<tr>
<th>表名</th>
<th>索引名</th>
<th>字段</th>
<th>类型</th>
<th>用途</th>
</tr>
</thead>
<tbody><tr>
<td>user</td>
<td>idx_username</td>
<td>username</td>
<td>UNIQUE</td>
<td>登录&#x2F;注册热点查询</td>
</tr>
<tr>
<td>book</td>
<td>uk_isbn</td>
<td>isbn</td>
<td>UNIQUE</td>
<td>业务主键查询</td>
</tr>
<tr>
<td>book</td>
<td>idx_name</td>
<td>name</td>
<td>INDEX</td>
<td>图书名 LIKE 查询</td>
</tr>
<tr>
<td>book</td>
<td>idx_create_time</td>
<td>create_time</td>
<td>INDEX</td>
<td>仪表盘排序</td>
</tr>
<tr>
<td>teacher</td>
<td>idx_name_status</td>
<td>(name, status)</td>
<td>INDEX</td>
<td>教师查询</td>
</tr>
<tr>
<td>lend_rule</td>
<td>idx_user_type_active</td>
<td>(user_type, is_active)</td>
<td>INDEX</td>
<td>借阅规则查询</td>
</tr>
<tr>
<td>book_review</td>
<td>idx_isbn_deleted_time</td>
<td>(isbn, deleted, create_time)</td>
<td>INDEX</td>
<td>书评列表查询</td>
</tr>
<tr>
<td>student</td>
<td>idx_name</td>
<td>name</td>
<td>INDEX</td>
<td>学生名搜索</td>
</tr>
</tbody></table>
<h3 id="5-3-删除冗余索引（4-个）"><a href="#5-3-删除冗余索引（4-个）" class="headerlink" title="5.3 删除冗余索引（4 个）"></a>5.3 删除冗余索引（4 个）</h3><p>有些索引是冗余的，不仅没用，还会增加写入开销：</p>
<table>
<thead>
<tr>
<th>表名</th>
<th>索引名</th>
<th>删除原因</th>
</tr>
</thead>
<tbody><tr>
<td>bookwithuser</td>
<td>idx_reader_id</td>
<td>uk_reader_isbn 的左前缀，冗余</td>
</tr>
<tr>
<td>bookwithuser</td>
<td>idx_bwu_reader_id</td>
<td>索引在自增主键上，无用</td>
</tr>
<tr>
<td>bookwithuser</td>
<td>idx_bwu_isbn</td>
<td>与 idx_isbn 重复</td>
</tr>
<tr>
<td>lend_record</td>
<td>idx_fine_amount</td>
<td>从未被查询使用</td>
</tr>
</tbody></table>
<h3 id="5-4-完整-SQL-脚本"><a href="#5-4-完整-SQL-脚本" class="headerlink" title="5.4 完整 SQL 脚本"></a>5.4 完整 SQL 脚本</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- ============================================</span></span><br><span class="line"><span class="comment">-- 数据库索引优化 Round4</span></span><br><span class="line"><span class="comment">-- 日期: 2026-04-15</span></span><br><span class="line"><span class="comment">-- ============================================</span></span><br><span class="line"></span><br><span class="line"><span class="comment">-- 新增索引</span></span><br><span class="line"><span class="keyword">ALTER TABLE</span> <span class="keyword">user</span> <span class="keyword">ADD</span> <span class="keyword">UNIQUE</span> INDEX idx_username (username);</span><br><span class="line"><span class="keyword">ALTER TABLE</span> book <span class="keyword">ADD</span> <span class="keyword">UNIQUE</span> INDEX uk_isbn (isbn);</span><br><span class="line"><span class="keyword">ALTER TABLE</span> book <span class="keyword">ADD</span> INDEX idx_name (name);</span><br><span class="line"><span class="keyword">ALTER TABLE</span> book <span class="keyword">ADD</span> INDEX idx_create_time (create_time);</span><br><span class="line"><span class="keyword">ALTER TABLE</span> teacher <span class="keyword">ADD</span> INDEX idx_name_status (name, status);</span><br><span class="line"><span class="keyword">ALTER TABLE</span> lend_rule <span class="keyword">ADD</span> INDEX idx_user_type_active (user_type, is_active);</span><br><span class="line"><span class="keyword">ALTER TABLE</span> book_review <span class="keyword">ADD</span> INDEX idx_isbn_deleted_time (isbn, deleted, create_time);</span><br><span class="line"><span class="keyword">ALTER TABLE</span> student <span class="keyword">ADD</span> INDEX idx_name (name);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 删除冗余索引</span></span><br><span class="line"><span class="keyword">ALTER TABLE</span> bookwithuser <span class="keyword">DROP</span> INDEX idx_reader_id;</span><br><span class="line"><span class="keyword">ALTER TABLE</span> bookwithuser <span class="keyword">DROP</span> INDEX idx_bwu_reader_id;</span><br><span class="line"><span class="keyword">ALTER TABLE</span> bookwithuser <span class="keyword">DROP</span> INDEX idx_bwu_isbn;</span><br><span class="line"><span class="keyword">ALTER TABLE</span> lend_record <span class="keyword">DROP</span> INDEX idx_fine_amount;</span><br></pre></td></tr></table></figure>

<h3 id="5-5-性能影响预估"><a href="#5-5-性能影响预估" class="headerlink" title="5.5 性能影响预估"></a>5.5 性能影响预估</h3><table>
<thead>
<tr>
<th>场景</th>
<th>优化前</th>
<th>优化后</th>
</tr>
</thead>
<tbody><tr>
<td>用户登录查询</td>
<td>全表扫描</td>
<td>索引查找</td>
</tr>
<tr>
<td>图书 ISBN 查询</td>
<td>全表扫描</td>
<td>UNIQUE 查找</td>
</tr>
<tr>
<td>图书名搜索</td>
<td>全表扫描</td>
<td>索引查找</td>
</tr>
<tr>
<td>仪表盘加载</td>
<td>filesort</td>
<td>索引排序</td>
</tr>
</tbody></table>
<hr>
<h2 id="六、优化成果汇总"><a href="#六、优化成果汇总" class="headerlink" title="六、优化成果汇总"></a>六、优化成果汇总</h2><p>经过这轮优化，系统在安全性和性能上都有了显著提升：</p>
<table>
<thead>
<tr>
<th>优化项</th>
<th>优化前</th>
<th>优化后</th>
<th>提升</th>
</tr>
</thead>
<tbody><tr>
<td>密码泄露风险</td>
<td>暴露明文密码</td>
<td>@JsonIgnore 保护</td>
<td>安全加固</td>
</tr>
<tr>
<td>密码修改验证</td>
<td>客户端验证</td>
<td>服务端验证</td>
<td>安全加固</td>
</tr>
<tr>
<td>审核页面请求数</td>
<td>21 次</td>
<td>1 次</td>
<td>减少 95%</td>
</tr>
<tr>
<td>凭证管理</td>
<td>硬编码</td>
<td>环境变量</td>
<td>安全加固</td>
</tr>
<tr>
<td>热点查询</td>
<td>部分全表扫描</td>
<td>索引覆盖</td>
<td>性能提升</td>
</tr>
</tbody></table>
<hr>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>这次优化让我深刻体会到：<strong>安全和性能不是事后补丁，而是设计之初就要考虑的</strong>。代码写得太随意，上线之后就会踩坑；提前多花一点时间设计，上线之后就能少很多麻烦。</p>
<p>如果你也在做类似的项目，建议从一开始就注意这些问题：</p>
<ol>
<li>敏感数据加 <code>@JsonIgnore</code>，别等泄露了再补救</li>
<li>密码验证放后端，别依赖客户端</li>
<li>接口设计时考虑 N+1 问题</li>
<li>凭证用环境变量，别硬编码</li>
<li>数据库建表时同步规划索引</li>
</ol>
<p>希望这篇文章对你有帮助！如果有任何问题，欢迎留言交流。</p>
<hr>
<p><em>相关代码已脱敏处理，请勿直接照搬，结合实际项目情况调整。</em></p>
<p><em>配乐：《暖阳下的代码时光》— 轻柔钢琴与弦乐交织，适合编程与思考的浪漫背景音乐</em></p>
]]></content>
      <categories>
        <category>项目分享</category>
      </categories>
      <tags>
        <tag>Spring Boot</tag>
        <tag>Vue</tag>
        <tag>性能优化</tag>
        <tag>安全加固</tag>
        <tag>数据库索引</tag>
      </tags>
  </entry>
  <entry>
    <title>四道内部 CTF 的共同根因：API/Web 鉴权割裂与 RBAC 继承风险</title>
    <url>/2026/07/21/internal-ctf-authorization-review/</url>
    <content><![CDATA[<h2 id="导读"><a href="#导读" class="headerlink" title="导读"></a>导读</h2><p>一次经过授权的内部 CTF，一共四道题：读取受限数据、完成受限写操作、识别高权限身份风险、分析角色继承造成的权限扩大。题目表面不同，最后却指向同一件事：<strong>系统拥有两套入口，却没有一套统一的授权规则</strong>。</p>
<p>本文只保留权限模型、排查方法和整改思路。真实域名、人员、账号、凭证、内部路径、业务 ID、请求命令和可直接复用的利用细节均已删除或泛化，请勿将这些思路用于未授权系统。</p>
<span id="more"></span>

<h2 id="四题其实是一条权限链"><a href="#四题其实是一条权限链" class="headerlink" title="四题其实是一条权限链"></a>四题其实是一条权限链</h2><p>靶场同时提供 Web 端和 API 端。两边认证方式不同，权限节点也分别维护，但最终访问的是同一批业务数据。</p>
<table>
<thead>
<tr>
<th>入口</th>
<th>身份载体</th>
<th>授权方式</th>
<th>主要风险</th>
</tr>
</thead>
<tbody><tr>
<td>Web 端</td>
<td>Session</td>
<td>页面或控制器节点</td>
<td>页面被拦，不代表数据真的不可访问</td>
</tr>
<tr>
<td>API 端</td>
<td>Token</td>
<td>API 路由节点</td>
<td>同一业务对象可能套用另一份权限规则</td>
</tr>
</tbody></table>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">普通用户</span><br><span class="line">├─ Web Session → Web RBAC ─┐</span><br><span class="line">└─ API Token   → API RBAC ─┴→ 同一业务数据</span><br></pre></td></tr></table></figure>

<p>如果 <code>Web RBAC</code> 和 <code>API RBAC</code> 分别配置、分别上线、分别回归，就会产生最危险的错觉：前端页面返回“无权限”，团队便认为功能已经安全；实际上，另一条 API 路径可能仍然开放。</p>
<h2 id="第一题：页面被拦，数据却能从另一入口读取"><a href="#第一题：页面被拦，数据却能从另一入口读取" class="headerlink" title="第一题：页面被拦，数据却能从另一入口读取"></a>第一题：页面被拦，数据却能从另一入口读取</h2><p>第一道题的现象是 Web 页面拒绝访问，但同一业务对象在 API 端存在等价查询能力。真正的问题不是“页面能不能绕”，而是：</p>
<ol>
<li>同一资源被两个入口暴露；</li>
<li>两套入口使用不同权限节点；</li>
<li>API 只验证“是否登录”或路由权限，没有验证用户能否访问这个业务对象。</li>
</ol>
<p>这类问题不能靠隐藏菜单、跳转登录页或返回特定状态码修复。正确做法是把授权条件落到资源层：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">canReadTask(user, task)</span><br><span class="line">  = hasRoutePermission(user, &quot;task:read&quot;)</span><br><span class="line">  AND belongsToAuthorizedProject(user, task.projectId)</span><br></pre></td></tr></table></figure>

<p>Web 和 API 都调用同一个资源授权规则，入口差异只负责认证，不负责决定业务权限。</p>
<h2 id="第二题：读权限和写权限没有形成闭环"><a href="#第二题：读权限和写权限没有形成闭环" class="headerlink" title="第二题：读权限和写权限没有形成闭环"></a>第二题：读权限和写权限没有形成闭环</h2><p>第二道题从“能看见数据”进一步走到了“能够创建并改变状态”。这说明系统不仅存在读越权，写接口也缺少同等级别的校验。</p>
<p>写接口至少需要同时回答四个问题：</p>
<ul>
<li>当前身份能否访问目标项目？</li>
<li>当前身份能否执行这个动作？</li>
<li>请求中的资源 ID 是否属于该身份的授权范围？</li>
<li>状态变化是否符合业务状态机？</li>
</ul>
<p>只检查 Token 有效、路由存在或参数齐全，都不足以证明这次写入合法。尤其是“更新状态”类接口，服务端必须自己判断允许的前后状态，不能直接相信客户端传入的完成比例或状态值。</p>
<h2 id="第三题：列表接口不该返回身份材料"><a href="#第三题：列表接口不该返回身份材料" class="headerlink" title="第三题：列表接口不该返回身份材料"></a>第三题：列表接口不该返回身份材料</h2><p>第三道题暴露的是数据最小化问题：普通业务列表返回了远超页面展示需要的字段，其中包含认证相关材料和可用于身份冒用的信息。</p>
<p>这是典型的“对象序列化过度”：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 风险做法：数据库整行直接返回</span></span><br><span class="line"><span class="keyword">return</span> <span class="title function_ invoke__">json</span>(<span class="variable">$userModel</span>-&gt;<span class="title function_ invoke__">select</span>());</span><br><span class="line"></span><br><span class="line"><span class="comment">// 更安全：显式定义公开 DTO</span></span><br><span class="line"><span class="keyword">return</span> <span class="title function_ invoke__">json</span>(<span class="variable">$users</span>-&gt;<span class="title function_ invoke__">visible</span>([</span><br><span class="line">    <span class="string">&#x27;id&#x27;</span>,</span><br><span class="line">    <span class="string">&#x27;display_name&#x27;</span>,</span><br><span class="line">    <span class="string">&#x27;department_name&#x27;</span>,</span><br><span class="line">]));</span><br></pre></td></tr></table></figure>

<p>密码摘要、盐、初始密码、API Token、手机号和邮箱都不应因为“前端暂时没显示”就留在响应里。浏览器开发者工具、代理日志、错误平台和缓存都可能把这些字段继续扩散。</p>
<p>一旦发现此类问题，修复顺序应该是：</p>
<ol>
<li>立即下线敏感字段；</li>
<li>轮换已经暴露的 Token 和会话；</li>
<li>检查访问日志，确认泄露范围；</li>
<li>使用专用 DTO 或响应白名单，避免再次整行返回。</li>
</ol>
<h2 id="第四题：岗位继承让一次改动影响一群人"><a href="#第四题：岗位继承让一次改动影响一群人" class="headerlink" title="第四题：岗位继承让一次改动影响一群人"></a>第四题：岗位继承让一次改动影响一群人</h2><p>最后一道题的核心是 RBAC 继承关系。系统的权限不是直接绑定用户，而是经过岗位与角色组逐层传递：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">用户 → 岗位 → 角色组 → 权限规则</span><br></pre></td></tr></table></figure>

<p>这种模型便于批量管理，但也放大了错误配置的影响。如果一个普通岗位被关联到高权限角色组，那么该岗位下的所有用户都会同步获得权限。</p>
<p>风险不在于“岗位关联功能存在”，而在于高权限变更缺少保护：</p>
<ul>
<li>高权限角色可以被普通编辑表单直接选择；</li>
<li>变更前不展示受影响用户数量；</li>
<li>没有二次确认或更强身份校验；</li>
<li>没有独立审批与不可抵赖审计；</li>
<li>超级权限和普通业务角色共用同一套批量继承机制。</li>
</ul>
<p>更稳妥的设计是把超级权限从普通岗位继承中拆开，采用单独授权、双人审批、短时有效和完整审计。批量角色变更前，还应计算并展示影响范围。</p>
<h2 id="根因总结"><a href="#根因总结" class="headerlink" title="根因总结"></a>根因总结</h2><p>四道题分别触发了不同现象，但根因可以压缩为四类：</p>
<table>
<thead>
<tr>
<th>现象</th>
<th>根因</th>
<th>防守措施</th>
</tr>
</thead>
<tbody><tr>
<td>Web 拒绝，API 可读</td>
<td>多入口策略不一致</td>
<td>统一资源级授权</td>
</tr>
<tr>
<td>普通身份可写</td>
<td>只校验路由，不校验对象与动作</td>
<td>对象级授权 + 状态机</td>
</tr>
<tr>
<td>列表泄露身份材料</td>
<td>数据模型直接序列化</td>
<td>DTO 白名单 + 凭证轮换</td>
</tr>
<tr>
<td>改岗位导致批量提权</td>
<td>RBAC 继承缺少高权限边界</td>
<td>超级权限独立管理 + 影响面审计</td>
</tr>
</tbody></table>
<p>另外，演练还发现了遗留密码摘要和可恢复初始密码的风险。无论算法叠加多少次，快速摘要都不能替代现代密码哈希；密码应使用 Argon2id、bcrypt 等带成本参数的方案，初始密码也不应以可恢复形式保存。</p>
<h2 id="一份可以直接复用的检查清单"><a href="#一份可以直接复用的检查清单" class="headerlink" title="一份可以直接复用的检查清单"></a>一份可以直接复用的检查清单</h2><h3 id="接口层"><a href="#接口层" class="headerlink" title="接口层"></a>接口层</h3><ul>
<li>同一资源是否同时存在 Web、API、移动端或旧版本入口？</li>
<li>各入口是否调用同一份资源授权逻辑？</li>
<li>详情、列表、导出、创建、编辑、删除是否分别校验？</li>
<li>是否校验“当前用户与目标对象的关系”，而不只是登录状态？</li>
</ul>
<h3 id="数据层"><a href="#数据层" class="headerlink" title="数据层"></a>数据层</h3><ul>
<li>响应是否使用字段白名单？</li>
<li>用户表、日志和导出文件是否包含 Token、密码材料或隐私字段？</li>
<li>凭证泄露后是否具备批量吊销和轮换能力？</li>
</ul>
<h3 id="RBAC"><a href="#RBAC" class="headerlink" title="RBAC"></a>RBAC</h3><ul>
<li>超级角色是否能被普通岗位继承？</li>
<li>角色变更是否展示影响人数？</li>
<li>高权限操作是否需要二次认证、审批和审计？</li>
<li>删除菜单后，对应后端接口是否仍能访问？</li>
</ul>
<h3 id="回归验证"><a href="#回归验证" class="headerlink" title="回归验证"></a>回归验证</h3><ul>
<li>用最低权限账号跑完整的读写矩阵；</li>
<li>对同一资源分别测试所有入口；</li>
<li>测试跨项目、跨部门和跨租户的对象 ID；</li>
<li>权限变更后验证受影响用户，而不只验证操作者本人；</li>
<li>对敏感响应字段设置自动化契约测试。</li>
</ul>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>这次四题通关，最有价值的不是记住某个接口或请求写法，而是建立一套排查顺序：</p>
<blockquote>
<p>先画入口，再画身份；先找资源，再查授权；最后沿角色继承计算影响范围。</p>
</blockquote>
<p>当系统存在多套入口、历史接口和复杂 RBAC 时，“页面提示无权限”只是开始。真正可靠的安全边界，必须落在统一、可测试、默认拒绝的服务端授权规则上。</p>
]]></content>
      <categories>
        <category>安全实战</category>
      </categories>
      <tags>
        <tag>CTF</tag>
        <tag>权限设计</tag>
        <tag>RBAC</tag>
        <tag>越权防护</tag>
        <tag>ThinkPHP</tag>
      </tags>
  </entry>
  <entry>
    <title>个人月卡取消接口缺失：从 Bug 定位到 TDD 修复全记录</title>
    <url>/2026/03/30/%E4%B8%AA%E4%BA%BA%E6%9C%88%E5%8D%A1%E5%8F%96%E6%B6%88%E6%8E%A5%E5%8F%A3%E7%BC%BA%E5%A4%B1%E4%BB%8EBug%E5%AE%9A%E4%BD%8D%E5%88%B0TDD%E4%BF%AE%E5%A4%8D%E5%85%A8%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<p>在南沙物联网项目的月卡系统中，用户取消个人月卡申请后，列表里竟然找不到”已取消”记录，车位和 VIP 资源也一直被占用。排查后发现，个人月卡模块压根没有 cancel 接口——而企业月卡模块是有的。本文记录了从问题定位、根因分析到 TDD 修复的完整过程。</p>
<span id="more"></span>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>这是一个典型的”功能遗漏”类 Bug：两个相似模块（个人月卡 vs 企业月卡）中，一个有取消功能，另一个却没有。用户在前端点击取消时，请求发不到后端，导致数据处于不一致状态。这类 Bug 的排查思路和修复策略值得记录，也希望能帮到遇到类似问题的开发者。</p>
<h2 id="问题背景"><a href="#问题背景" class="headerlink" title="问题背景"></a>问题背景</h2><h3 id="业务场景"><a href="#业务场景" class="headerlink" title="业务场景"></a>业务场景</h3><p>在月卡申请流程中，用户需要经过两个页面：</p>
<ol>
<li><strong>添加月卡页面</strong>：填写表单，点击”立即申请”</li>
<li><strong>用户协议页面</strong>：查看协议，选择”同意”或”取消”</li>
</ol>
<p>问题出在第 2 步——用户点击”取消”后，返回月卡列表，却看不到”已取消”状态的记录。</p>
<h3 id="用户操作流程"><a href="#用户操作流程" class="headerlink" title="用户操作流程"></a>用户操作流程</h3><p><img data-src="/images/posts/%E4%B8%AA%E4%BA%BA%E6%9C%88%E5%8D%A1%E5%8F%96%E6%B6%88%E6%8E%A5%E5%8F%A3%E7%BC%BA%E5%A4%B1%E4%BB%8EBug%E5%AE%9A%E4%BD%8D%E5%88%B0TDD%E4%BF%AE%E5%A4%8D%E5%85%A8%E8%AE%B0%E5%BD%95/fig1.png" alt="用户操作流程与问题发生点"><br><em>图 1：用户操作流程与问题发生点示意</em></p>
<p>具体步骤：</p>
<ol>
<li>用户进入”添加月卡”页面，填写表单信息</li>
<li>点击”立即申请”，前端调用 <code>POST /v1/monthpersonal/apply</code></li>
<li>后端成功创建月卡记录（state&#x3D;”1” 申请中），同时创建订单、关联车辆、更新车位状态为占用、消耗 VIP</li>
<li>前端跳转到”用户协议”页面</li>
<li>用户点击”取消”并确认——<strong>没有任何后端接口被调用</strong></li>
<li>返回列表页——数据库中 state&#x3D;”1” 的记录未被更新为 state&#x3D;”10”（已取消）</li>
<li>列表中看不到”已取消”记录，且车位、VIP 资源未被释放</li>
</ol>
<h2 id="排查过程"><a href="#排查过程" class="headerlink" title="排查过程"></a>排查过程</h2><h3 id="第一步：搜索-cancel-相关端点"><a href="#第一步：搜索-cancel-相关端点" class="headerlink" title="第一步：搜索 cancel 相关端点"></a>第一步：搜索 cancel 相关端点</h3><p>通过搜索 Controller 层的 cancel 关键词，发现了关键线索：</p>
<table>
<thead>
<tr>
<th>文件</th>
<th>行号</th>
<th>关键信息</th>
<th>场景</th>
</tr>
</thead>
<tbody><tr>
<td><code>UserMonthCompanyController.java</code></td>
<td>255-259</td>
<td><code>POST /v1/user-monthcompany/cancel</code></td>
<td>企业月卡有取消</td>
</tr>
<tr>
<td><code>MonthPersonalController.java</code></td>
<td>全文</td>
<td>无 cancel 端点</td>
<td>个人月卡缺失</td>
</tr>
</tbody></table>
<h3 id="第二步：对比-Biz-层方法"><a href="#第二步：对比-Biz-层方法" class="headerlink" title="第二步：对比 Biz 层方法"></a>第二步：对比 Biz 层方法</h3><p>企业月卡 <code>MouthCompanyBiz</code> 有 <code>cancel()</code> 方法（第 686-704 行），会校验状态后更新 state 为 CANCEL(“10”)。个人月卡 <code>MouthPersonalBiz</code> 完全没有这个方法。</p>
<h3 id="第三步：确认定时任务补偿机制"><a href="#第三步：确认定时任务补偿机制" class="headerlink" title="第三步：确认定时任务补偿机制"></a>第三步：确认定时任务补偿机制</h3><p>项目中存在 <code>MonthlyCardOperationTimeoutTask</code> 定时任务，每半小时检测申请中超 30 分钟的记录并自动取消。但这个任务<strong>只更新状态</strong>，不回滚车位&#x2F;VIP 等副作用。</p>
<h2 id="根因分析"><a href="#根因分析" class="headerlink" title="根因分析"></a>根因分析</h2><p><img data-src="/images/posts/%E4%B8%AA%E4%BA%BA%E6%9C%88%E5%8D%A1%E5%8F%96%E6%B6%88%E6%8E%A5%E5%8F%A3%E7%BC%BA%E5%A4%B1%E4%BB%8EBug%E5%AE%9A%E4%BD%8D%E5%88%B0TDD%E4%BF%AE%E5%A4%8D%E5%85%A8%E8%AE%B0%E5%BD%95/fig2.png" alt="根因分析：三层 Bug 的关系"><br><em>图 2：三层 Bug 的关系与影响范围</em></p>
<p>问题可以拆解为三个相互关联的 Bug：</p>
<h3 id="Bug-1：缺少-cancel-端点"><a href="#Bug-1：缺少-cancel-端点" class="headerlink" title="Bug 1：缺少 cancel 端点"></a>Bug 1：缺少 cancel 端点</h3><p><code>MonthPersonalController</code> 没有 cancel 端点，<code>MouthPersonalBiz</code> 也没有 <code>cancel()</code> 方法。对比企业月卡，后者在第 255 行有 <code>POST /cancel</code>，第 686 行有 <code>cancel()</code> 方法。</p>
<h3 id="Bug-2：apply-方法副作用不可逆"><a href="#Bug-2：apply-方法副作用不可逆" class="headerlink" title="Bug 2：apply() 方法副作用不可逆"></a>Bug 2：apply() 方法副作用不可逆</h3><p><code>MouthPersonalBiz.apply()</code> 在用户点击”立即申请”时立即执行了以下操作：</p>
<ul>
<li>创建月卡记录（state&#x3D;”1”）</li>
<li>创建订单（orderStatus&#x3D;”1”）</li>
<li>保存车辆关联</li>
<li>更新车位状态为占用（usingState&#x3D;”4”）</li>
<li>消耗 VIP（state&#x3D;”5”）</li>
</ul>
<p>这些操作在用户取消时无法回滚，因为没有对应的清理接口。</p>
<h3 id="Bug-3：定时任务不回滚副作用"><a href="#Bug-3：定时任务不回滚副作用" class="headerlink" title="Bug 3：定时任务不回滚副作用"></a>Bug 3：定时任务不回滚副作用</h3><p><code>MonthlyCardOperationTimeoutTask</code> 只将 state 更新为”10”，但不释放车位、不回滚 VIP、不取消订单。这意味着即使超时自动取消，资源仍然被锁定。</p>
<h3 id="个人月卡状态流转"><a href="#个人月卡状态流转" class="headerlink" title="个人月卡状态流转"></a>个人月卡状态流转</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">申请(1) ──→ 待支付(4) ──→ 已生效(2) ──→ 临期(3) ──→ 已过期(13)</span><br><span class="line">  │                         │</span><br><span class="line">  ├──→ 已取消(10)           ├──→ 续期中(5)</span><br><span class="line">                            │</span><br><span class="line">                            ├──→ 变更中(6) ──→ 变更批准(15) / 变更拒绝(11)</span><br><span class="line">                            │</span><br><span class="line">                            └──→ 退款申请中(8) ──→ 退款待转账(12) ──→ 已退款(9) / 退款拒绝(16)</span><br></pre></td></tr></table></figure>

<p><code>cancel()</code> 方法允许从”申请中(1)”和”待支付(4)”两个状态取消，目标状态为”已取消(10)”。</p>
<h2 id="修复方案"><a href="#修复方案" class="headerlink" title="修复方案"></a>修复方案</h2><h3 id="个人月卡-vs-企业月卡-cancel-对比"><a href="#个人月卡-vs-企业月卡-cancel-对比" class="headerlink" title="个人月卡 vs 企业月卡 cancel() 对比"></a>个人月卡 vs 企业月卡 cancel() 对比</h3><p><img data-src="/images/posts/%E4%B8%AA%E4%BA%BA%E6%9C%88%E5%8D%A1%E5%8F%96%E6%B6%88%E6%8E%A5%E5%8F%A3%E7%BC%BA%E5%A4%B1%E4%BB%8EBug%E5%AE%9A%E4%BD%8D%E5%88%B0TDD%E4%BF%AE%E5%A4%8D%E5%85%A8%E8%AE%B0%E5%BD%95/fig3.png" alt="个人月卡与企业月卡 cancel 实现对比"><br><em>图 3：个人月卡与企业月卡 cancel 实现对比</em></p>
<table>
<thead>
<tr>
<th>维度</th>
<th>个人月卡 cancel()</th>
<th>企业月卡 cancel()</th>
</tr>
</thead>
<tbody><tr>
<td>状态校验</td>
<td>“1” 申请中 + “4” 待支付</td>
<td>仅 “1” 申请中</td>
</tr>
<tr>
<td>车位释放</td>
<td>有（恢复 usingState、清空 monthCar&#x2F;parkingCar）</td>
<td>无（有 TODO 注释）</td>
</tr>
<tr>
<td>VIP 回滚</td>
<td>有（恢复 state&#x3D;”1”）</td>
<td>无</td>
</tr>
<tr>
<td>订单取消</td>
<td>有（orderStatus&#x3D;”5”）</td>
<td>无</td>
</tr>
<tr>
<td>事务保护</td>
<td>@Transactional</td>
<td>无</td>
</tr>
<tr>
<td>日志写入</td>
<td>有</td>
<td>有</td>
</tr>
</tbody></table>
<h3 id="Step-1：MouthPersonalBiz-新增-cancel-方法"><a href="#Step-1：MouthPersonalBiz-新增-cancel-方法" class="headerlink" title="Step 1：MouthPersonalBiz 新增 cancel() 方法"></a>Step 1：MouthPersonalBiz 新增 cancel() 方法</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 取消申请</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> id 月卡ID</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@return</span> 操作结果</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Transactional(rollbackFor = Exception.class)</span></span><br><span class="line"><span class="keyword">public</span> R <span class="title function_">cancel</span><span class="params">(Long id)</span> &#123;</span><br><span class="line">    <span class="type">MonthPersonalEntity</span> <span class="variable">monthPersonal</span> <span class="operator">=</span> monthPersonalService.getById(id);</span><br><span class="line">    <span class="keyword">if</span> (ObjUtil.isNull(monthPersonal)) &#123;</span><br><span class="line">        <span class="keyword">return</span> R.fail(<span class="string">&quot;月卡不存在&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 只有申请中(1)和待支付(4)状态可以取消</span></span><br><span class="line">    <span class="keyword">if</span> (!MonthPersonalStateEnum.APPLY.getCode().equals(monthPersonal.getState())</span><br><span class="line">        &amp;&amp; !MonthPersonalStateEnum.WAITING_PAY.getCode().equals(monthPersonal.getState())) &#123;</span><br><span class="line">        <span class="keyword">return</span> R.fail(<span class="string">&quot;非法操作&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    monthPersonal.setState(MonthPersonalStateEnum.CANCEL.getCode());</span><br><span class="line">    <span class="type">boolean</span> <span class="variable">flag</span> <span class="operator">=</span> monthPersonalService.updateById(monthPersonal);</span><br><span class="line">    <span class="keyword">if</span> (flag) &#123;</span><br><span class="line">        <span class="comment">// 释放车位</span></span><br><span class="line">        <span class="keyword">if</span> (monthPersonal.getParkingSpotId() != <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="type">ParkSlotEntity</span> <span class="variable">slotEntity</span> <span class="operator">=</span> parkSlotService.getById(monthPersonal.getParkingSpotId());</span><br><span class="line">            <span class="keyword">if</span> (slotEntity != <span class="literal">null</span>) &#123;</span><br><span class="line">                slotEntity.setUsingState(<span class="string">&quot;1&quot;</span>); <span class="comment">// 空闲</span></span><br><span class="line">                slotEntity.setMonthCar(<span class="literal">null</span>);</span><br><span class="line">                slotEntity.setParkingCar(<span class="literal">null</span>);</span><br><span class="line">                parkSlotService.updateById(slotEntity);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 回滚VIP</span></span><br><span class="line">        <span class="keyword">if</span> (monthPersonal.getVipId() != <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="type">MonthVipEntity</span> <span class="variable">monthVip</span> <span class="operator">=</span> monthVipService.getById(monthPersonal.getVipId());</span><br><span class="line">            <span class="keyword">if</span> (monthVip != <span class="literal">null</span>) &#123;</span><br><span class="line">                monthVip.setState(<span class="string">&quot;1&quot;</span>); <span class="comment">// 恢复有效</span></span><br><span class="line">                monthVipService.updateById(monthVip);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 取消订单</span></span><br><span class="line">        <span class="keyword">if</span> (monthPersonal.getOrderNo() != <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="type">OrderInfoEntity</span> <span class="variable">orderInfo</span> <span class="operator">=</span> orderInfoService.getById(monthPersonal.getOrderNo());</span><br><span class="line">            <span class="keyword">if</span> (orderInfo != <span class="literal">null</span>) &#123;</span><br><span class="line">                orderInfo.setOrderStatus(<span class="string">&quot;5&quot;</span>); <span class="comment">// 已取消</span></span><br><span class="line">                orderInfoService.updateById(orderInfo);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 写入日志</span></span><br><span class="line">        <span class="type">LogInfoEntity</span> <span class="variable">infoEntity</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">LogInfoEntity</span>();</span><br><span class="line">        infoEntity.setName(LogBusinessType.PERSONMONTHCARD.getCode());</span><br><span class="line">        infoEntity.setKeyId(monthPersonal.getId() + <span class="string">&quot;&quot;</span>);</span><br><span class="line">        infoEntity.setOperateAction(<span class="string">&quot;取消&quot;</span>);</span><br><span class="line">        infoEntity.setRemark(<span class="string">&quot;用户取消月卡申请&quot;</span>);</span><br><span class="line">        logInfoService.save(infoEntity);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> R.status(flag);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>关键设计决策</strong>：cancel() 方法中的多个 DB 操作需要原子性保护。如果先更新了月卡状态，但释放车位时失败，就会出现脏数据。因此添加了 <code>@Transactional(rollbackFor = Exception.class)</code>，确保任一步骤失败时全部回滚。项目中已有此模式（<code>ZombieCarDetectionServiceImpl</code>、<code>SecurityRulesConfigureController</code> 等）。</p>
<h3 id="Step-2：MonthPersonalController-新增-cancel-端点"><a href="#Step-2：MonthPersonalController-新增-cancel-端点" class="headerlink" title="Step 2：MonthPersonalController 新增 cancel 端点"></a>Step 2：MonthPersonalController 新增 cancel 端点</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 取消申请</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@PostMapping(&quot;/cancel&quot;)</span></span><br><span class="line"><span class="meta">@ApiOperationSupport(order = 16)</span></span><br><span class="line"><span class="meta">@ApiOperation(value = &quot;取消申请&quot;)</span></span><br><span class="line"><span class="keyword">public</span> R <span class="title function_">cancel</span><span class="params">(<span class="meta">@RequestParam</span> Long id)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> mouthPersonalBiz.cancel(id);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>端点路径：<code>POST /v1/monthpersonal/cancel?id=&#123;id&#125;</code></p>
<h3 id="Step-3：TDD-测试验证"><a href="#Step-3：TDD-测试验证" class="headerlink" title="Step 3：TDD 测试验证"></a>Step 3：TDD 测试验证</h3><p>新增 11 个测试用例，覆盖以下场景：</p>
<table>
<thead>
<tr>
<th>测试场景</th>
<th>预期结果</th>
</tr>
</thead>
<tbody><tr>
<td>申请中(state&#x3D;1)取消</td>
<td>state 变为 10，成功</td>
</tr>
<tr>
<td>待支付(state&#x3D;4)取消</td>
<td>state 变为 10，成功</td>
</tr>
<tr>
<td>已生效(state&#x3D;2)取消</td>
<td>返回”非法操作”</td>
</tr>
<tr>
<td>取消后车位状态</td>
<td>usingState 恢复为 1</td>
</tr>
<tr>
<td>取消后 VIP 状态</td>
<td>state 恢复为 1</td>
</tr>
<tr>
<td>取消后订单状态</td>
<td>orderStatus 变为 5</td>
</tr>
<tr>
<td>取消后列表展示</td>
<td>列表出现”已取消”记录</td>
</tr>
<tr>
<td>不存在的 ID</td>
<td>返回”月卡不存在”</td>
</tr>
<tr>
<td>事务回滚测试</td>
<td>中间失败时全部回滚</td>
</tr>
</tbody></table>
<p>全部 11 个测试用例通过。</p>
<h3 id="修改汇总"><a href="#修改汇总" class="headerlink" title="修改汇总"></a>修改汇总</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">MouthPersonalBiz.java              +50 行  (新增 cancel() 方法，含 @Transactional)</span><br><span class="line">MonthPersonalController.java       +8  行  (新增 POST /cancel 端点)</span><br><span class="line">MonthPersonalCancelTest.java       NEW     (新增 11 个测试用例)</span><br></pre></td></tr></table></figure>

<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><ul>
<li><strong>相似模块要对齐功能</strong>：新增模块时，务必对照已有模块检查功能完整性。企业月卡有 cancel，个人月卡也应该有</li>
<li><strong>副作用必须可逆</strong>：<code>apply()</code> 方法在申请阶段就执行了车位占用、VIP 消耗等操作，这些副作用必须有对应的回滚路径</li>
<li><strong>事务保护不可少</strong>：涉及多个 DB 写入的操作必须加 <code>@Transactional</code>，防止部分成功导致数据不一致</li>
<li><strong>定时任务应复用业务逻辑</strong>：<code>MonthlyCardOperationTimeoutTask</code> 只更新状态不释放资源，后续应重构为复用 <code>cancel()</code> 方法</li>
<li><strong>TDD 修复更可靠</strong>：先写测试用例复现问题，再编写修复代码，确保新旧测试全部通过</li>
</ul>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>这个 Bug 的本质是一个功能遗漏——个人月卡模块在开发时遗漏了取消接口。修复过程遵循了 TDD 流程，从测试用例到业务代码再到事务保护，每一步都有对应的验证。后续还计划将定时任务的补偿逻辑统一复用 <code>cancel()</code> 方法，彻底解决资源泄漏问题。</p>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>南沙物联网</category>
      </categories>
      <tags>
        <tag>Spring Boot</tag>
        <tag>Bug修复</tag>
        <tag>月卡系统</tag>
        <tag>TDD</tag>
        <tag>事务管理</tag>
      </tags>
  </entry>
  <entry>
    <title>在 16G 内存的 Windows 本机跑通 shopkeeper-agent：13 个坑的完整排障笔记</title>
    <url>/2026/07/18/shopkeeper-agent-troubleshooting/</url>
    <content><![CDATA[<p>跑了整整两天，才把 <a href="https://github.com/zhdmm35/shopkeeper-agent">shopkeeper-agent</a> 这个 AI 电商问数项目在本机完整跑通——从 <code>uv sync</code> 报错，到 LangChain JSON 解析器被 LLM 的思考块炸掉，再到 Docker Desktop 弹窗黑屏。这篇博客把我踩到的 13 个坑<strong>按”问题 → 根因 → 解决”的节奏整理成一条故事线</strong>，每一步都附可复现的命令。</p>
<p>如果你也在 Windows + Python 3.14 + Docker + 国内网络这个”地狱四件套”下跑 AI 项目，这篇应该能帮你少走两天弯路。</p>
<span id="more"></span>

<div class="audio-player">
  <div class="audio-section">
    <h4>🎧 文章导读</h4>
    <audio controls preload="metadata">
      <source src="/audio/shopkeeper-agent-troubleshooting/narration.mp3" type="audio/mpeg">
    </audio>
  </div>
  <div class="audio-section">
    <h4>🎵 背景音乐</h4>
    <audio controls loop preload="metadata">
      <source src="/audio/shopkeeper-agent-troubleshooting/bgm.mp3" type="audio/mpeg">
    </audio>
  </div>
</div>

<h2 id="前言：为什么要在本机跑这个项目"><a href="#前言：为什么要在本机跑这个项目" class="headerlink" title="前言：为什么要在本机跑这个项目"></a>前言：为什么要在本机跑这个项目</h2><p>shopkeeper-agent 是一个 <strong>Text-to-SQL 的电商问数 Agent</strong>：你用自然语言问”统计华北地区的销售总额”，它会拆解意图、查元数据知识库、检索字段、生成 SQL、执行、返回结果。整套链路是当下最典型的 RAG + Agent 范式——LLM 负责推理，向量库做语义召回，关系库做事实落地。</p>
<p>但官方文档在 macOS &#x2F; Linux 上跑得很顺，<strong>到了 Windows 这边几乎每一步都炸</strong>。原因很简单：项目用 Python 3.14（最新）、asyncmy（Cython 扩展，需要 MSVC 编译）、HF 下载（国内网络不稳）、Docker Desktop 4 容器（16G 内存吃紧）。每一项单独看都不致命，叠加在一起就是连环炸。</p>
<p>我把这 13 个坑按”环境 → 依赖 → 中间件 → 后端 → 模型”的顺序重新组织，读起来更像调试日志，而不是 dump 出来的报错清单。</p>
<p><img data-src="/images/posts/shopkeeper-agent-troubleshooting/fig1-architecture_001.jpg" alt="shopkeeper-agent 系统架构图"><br><em>图 1：shopkeeper-agent 整体架构——FastAPI 后端调度 LLM + 4 个 Docker 中间件</em></p>
<h2 id="环境速览"><a href="#环境速览" class="headerlink" title="环境速览"></a>环境速览</h2><p>先交代舞台。下表是我跑通时的全套环境配置（<strong>版本错一位，行为就可能不一样</strong>）：</p>
<table>
<thead>
<tr>
<th>组件</th>
<th>版本 &#x2F; 配置</th>
<th>备注</th>
</tr>
</thead>
<tbody><tr>
<td>OS</td>
<td>Windows 11 Home China（10.0.22631）</td>
<td>WSL2 + Docker Desktop</td>
</tr>
<tr>
<td>内存</td>
<td>16 GB</td>
<td>紧张，但够用</td>
</tr>
<tr>
<td>Python</td>
<td>3.14.3（uv 管理）</td>
<td>最新版，少数包无 wheel</td>
</tr>
<tr>
<td>uv</td>
<td>0.10.9</td>
<td>比 pip 快得多，但会扫祖先目录</td>
</tr>
<tr>
<td>Docker Desktop</td>
<td>28.1.1（WSL2 backend）</td>
<td>存储迁到 <code>E:\dockerimgerdeful\DockerDesktopWSL</code></td>
</tr>
<tr>
<td>Docker Compose</td>
<td>v2.35.1</td>
<td><code>docker compose</code>（不是 <code>docker-compose</code>）</td>
</tr>
<tr>
<td>Node</td>
<td>14.21.3 → 22.20.0</td>
<td><strong>Node 14 太老，前端起不来，必须切</strong></td>
</tr>
<tr>
<td>LLM</td>
<td>MiniMax-M3（OpenAI 兼容）</td>
<td>用 <code>extra_body</code> 关 think 输出</td>
</tr>
<tr>
<td>Embedding</td>
<td>BAAI&#x2F;bge-large-zh-v1.5</td>
<td>ModelScope 国内源</td>
</tr>
</tbody></table>
<h2 id="第一章：Python-环境与依赖——两个温柔的陷阱"><a href="#第一章：Python-环境与依赖——两个温柔的陷阱" class="headerlink" title="第一章：Python 环境与依赖——两个温柔的陷阱"></a>第一章：Python 环境与依赖——两个温柔的陷阱</h2><h3 id="坑-1：uv-扫祖先目录，把外层-pyproject-toml-拉下水"><a href="#坑-1：uv-扫祖先目录，把外层-pyproject-toml-拉下水" class="headerlink" title="坑 1：uv 扫祖先目录，把外层 pyproject.toml 拉下水"></a>坑 1：uv 扫祖先目录，把外层 <code>pyproject.toml</code> 拉下水</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv python install 3.14</span><br><span class="line"><span class="built_in">cd</span> shopkeeper-agent-main</span><br><span class="line">uv python pin 3.14</span><br><span class="line">uv <span class="built_in">sync</span></span><br></pre></td></tr></table></figure>

<p>最后一步炸了：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Failed to parse: ai-application/pyproject.toml</span><br><span class="line">project.version field is neither set</span><br></pre></td></tr></table></figure>

<p>第一反应是 <code>shopkeeper-agent-main/pyproject.toml</code> 写错了，但<strong>真正的问题是外层仓库</strong>。我的项目路径是这样的：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">F:\bug图\zhioai\ai-application\ai-application\</span><br><span class="line">└── 源码项目\</span><br><span class="line">    └── shopkeeper-agent-main\</span><br><span class="line">        └── pyproject.toml   ← 我以为只看这个</span><br></pre></td></tr></table></figure>

<p><code>ai-application/pyproject.toml</code>（仓库的”骨架”，教程用的）也有一个 <code>[project]</code> 段，但<strong>漏写了 <code>version</code> 字段</strong>。uv 0.10 的策略是”沿祖先目录扫所有 <code>pyproject.toml</code>“，PEP 621 又强制 <code>project.version</code> 必须存在，于是解析阶段直接噎死。</p>
<p><strong>解决</strong>：外层 <code>ai-application/pyproject.toml</code> 补一行 <code>version = &quot;0.0.0&quot;</code>。一行配置，绕过一个 PEP 621 校验。</p>
<blockquote>
<p>💡 <strong>教训</strong>：用 uv 的同学，<strong>别把项目放在”教程仓库”里</strong>。要么把外层 <code>pyproject.toml</code> 改合规，要么干脆 <code>cd</code> 进自己的目录再开 terminal。</p>
</blockquote>
<h3 id="坑-2：asyncmy-编译——Windows-无-MSVC-的死结"><a href="#坑-2：asyncmy-编译——Windows-无-MSVC-的死结" class="headerlink" title="坑 2：asyncmy 编译——Windows 无 MSVC 的死结"></a>坑 2：asyncmy 编译——Windows 无 MSVC 的死结</h3><p>uv sync 接着报：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">asyncmy==0.2.11 ... Microsoft Visual C++ 14.0 or greater is required</span><br></pre></td></tr></table></figure>

<p>asyncmy 是 <strong>Cython 扩展</strong>，需要在本地编译出 <code>.pyd</code>。Windows 默认没装 MSVC Build Tools，PyPI 上又没有 Python 3.14 的预编译 wheel——3.14 太新了，几乎所有 C 扩展都得自编译。</p>
<p>我有三个选择：装 MSVC（10G+、耗时长）、装 VS Build Tools（一样重）、<strong>换驱动</strong>。考虑到这只是数据库驱动，<strong>纯 Python 的 aiomysql</strong>（基于 PyMySQL）完全可以胜任，最终选了第三条路。</p>
<p><strong>改动清单</strong>：</p>
<figure class="highlight diff"><table><tr><td class="code"><pre><span class="line"># shopkeeper-agent-main/pyproject.toml</span><br><span class="line"><span class="deletion">- &quot;asyncmy&gt;=0.2.11&quot;</span></span><br><span class="line"><span class="addition">+ &quot;aiomysql&gt;=0.2.0&quot;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight diff"><table><tr><td class="code"><pre><span class="line"># app/clients/mysql_client_manager.py</span><br><span class="line"><span class="deletion">- mysql+asyncmy://</span></span><br><span class="line"><span class="addition">+ mysql+aiomysql://</span></span><br></pre></td></tr></table></figure>

<p>一共改了 3 处连接串 + 几行注释，119 个依赖顺利装好。</p>
<h2 id="第二章：LLM-接入——OpenAI-兼容接口的甜与苦"><a href="#第二章：LLM-接入——OpenAI-兼容接口的甜与苦" class="headerlink" title="第二章：LLM 接入——OpenAI 兼容接口的甜与苦"></a>第二章：LLM 接入——OpenAI 兼容接口的甜与苦</h2><p>shopkeeper-agent 用 LangChain 的 <code>init_chat_model(model_provider=&quot;openai&quot;)</code>，这意味着<strong>任何 OpenAI 兼容 provider 都能直接接</strong>——只要改 <code>base_url</code> 和 <code>api_key</code>。</p>
<p>我先按官方文档配 MiniMax：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># conf/app_config.yaml</span></span><br><span class="line"><span class="attr">llm:</span></span><br><span class="line">  <span class="attr">model_name:</span> <span class="string">MiniMax-M3</span></span><br><span class="line">  <span class="attr">api_key:</span> <span class="string">$&#123;oc.env:LLM_API_KEY&#125;</span></span><br><span class="line">  <span class="attr">base_url:</span> <span class="string">https://api.minimaxi.com/v1</span></span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># .env</span></span><br><span class="line">LLM_API_KEY=sk-cp-...</span><br></pre></td></tr></table></figure>

<p>启动、调用，看起来一切正常，<strong>直到 LangChain 的 JSON parser 报错</strong>。这就是后面会单独讲的”坑 12”——推理模型的思考块输出和 JSON 解析器的兼容性。这章先按住不表，<strong>因为它是 13 个坑里最值得单独立传的一个</strong>。</p>
<h2 id="第三章：Embedding-模型——国内网络的两条路"><a href="#第三章：Embedding-模型——国内网络的两条路" class="headerlink" title="第三章：Embedding 模型——国内网络的两条路"></a>第三章：Embedding 模型——国内网络的两条路</h2><h3 id="坑-3：hf-mirror-看起来配了，其实没生效"><a href="#坑-3：hf-mirror-看起来配了，其实没生效" class="headerlink" title="坑 3：hf-mirror 看起来配了，其实没生效"></a>坑 3：hf-mirror 看起来配了，其实没生效</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">HF_ENDPOINT=https://hf-mirror.com uvx --from huggingface_huggingface_cli ...</span><br></pre></td></tr></table></figure>

<p>报错：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">LocalEntryNotFoundError / FileMetadataError</span><br></pre></td></tr></table></figure>

<p>环境变量确认生效了，但下载依然失败。hf-mirror 维护方在 GitHub Issues 答复过——<strong>部分大模型（bge-large-zh-v1.5 这种热门模型）的镜像同步经常滞后</strong>。</p>
<p>转 <strong>ModelScope</strong>（阿里达摩院，国内稳定）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">MODELSCOPE_CACHE=F:/modelscope_cache uvx --from modelscope modelscope download \</span><br><span class="line">  --model BAAI/bge-large-zh-v1.5 \</span><br><span class="line">  --local_dir docker/embedding/bge-large-zh-v1.5</span><br></pre></td></tr></table></figure>

<p>C 盘空间也顺便挪走：<code>HF_HOME=F:/hf_cache</code>。</p>
<h3 id="坑-4（虚惊一场）：ModelScope-只有-pytorch-model-bin，没-model-safetensors"><a href="#坑-4（虚惊一场）：ModelScope-只有-pytorch-model-bin，没-model-safetensors" class="headerlink" title="坑 4（虚惊一场）：ModelScope 只有 pytorch_model.bin，没 model.safetensors"></a>坑 4（虚惊一场）：ModelScope 只有 <code>pytorch_model.bin</code>，没 <code>model.safetensors</code></h3><p>下载完成后我下意识去检查 TEI（text-embeddings-inference）的兼容性——<strong>它默认吃 safetensors</strong>，没有就报错。</p>
<p>结果实测发现：**TEI 的 candle backend 直接吃 <code>pytorch_model.bin</code>**，根本不用 safetensors，也不用装 torch。1.3G 的模型就这么直接用起来了。</p>
<blockquote>
<p>⚠️ 这个坑是”我自己吓自己”——<strong>遇到报错前先看官方文档说什么</strong>，别被搜索结果里的”通用经验”误导。TEI 用 candle 后端时是支持 pytorch_model.bin 的。</p>
</blockquote>
<h2 id="第四章：Docker-编排——16G-内存下的容器战争"><a href="#第四章：Docker-编排——16G-内存下的容器战争" class="headerlink" title="第四章：Docker 编排——16G 内存下的容器战争"></a>第四章：Docker 编排——16G 内存下的容器战争</h2><p><img data-src="/images/posts/shopkeeper-agent-troubleshooting/fig2-docker_001.jpg" alt="Docker 容器编排关系图"><br><em>图 2：4 个核心容器（mysql&#x2F;es&#x2F;qdrant&#x2F;TEI）+ 可选 kibana</em></p>
<h3 id="坑-5：OOM-黑屏"><a href="#坑-5：OOM-黑屏" class="headerlink" title="坑 5：OOM 黑屏"></a>坑 5：OOM 黑屏</h3><p><code>docker compose up</code> 拉起 5 个容器（含 kibana）时，<strong>整机黑屏 3 秒</strong>。重启后发现 ES 镜像 675MB 那层 extract 时直接把内存撑爆——16G 只剩 988MB，IDEA + Chrome 已经把可用内存吃干抹净。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 关 IDEA</span></span><br><span class="line"><span class="comment"># 跳 kibana（非必须，只是 ES 可视化 UI）</span></span><br><span class="line">docker compose -f docker/docker-compose.yaml up -d \</span><br><span class="line">  mysql elasticsearch qdrant embedding</span><br></pre></td></tr></table></figure>

<p><strong>只起 4 容器</strong>就稳了。kibana 是可选的 UI 工具，不影响主链路。</p>
<h3 id="坑-6-7：容器名-端口双重冲突"><a href="#坑-6-7：容器名-端口双重冲突" class="headerlink" title="坑 6 + 7：容器名 + 端口双重冲突"></a>坑 6 + 7：容器名 + 端口双重冲突</h3><p>启动 mysql 时又报：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">container name &quot;mysql&quot; already in use</span><br><span class="line">port 3306 is already allocated</span><br></pre></td></tr></table></figure>

<p>本机装了 <code>mysqld.exe</code>（Windows MySQL 服务）占着 3306，还有个旧的 mysql 容器（别的项目残留）占着容器名。</p>
<p><strong>两步解决</strong>：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker <span class="built_in">rm</span> mysql   <span class="comment"># 删旧容器</span></span><br></pre></td></tr></table></figure>

<figure class="highlight diff"><table><tr><td class="code"><pre><span class="line"># docker/docker-compose.yaml</span><br><span class="line"><span class="deletion">- &quot;3306:3306&quot;</span></span><br><span class="line"><span class="addition">+ &quot;3307:3306&quot;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight diff"><table><tr><td class="code"><pre><span class="line"># conf/app_config.yaml</span><br><span class="line">db_meta:</span><br><span class="line">  host: 127.0.0.1</span><br><span class="line"><span class="deletion">- port: 3306</span></span><br><span class="line"><span class="addition">+ port: 3307</span></span><br><span class="line">db_dw:</span><br><span class="line">  host: 127.0.0.1</span><br><span class="line"><span class="deletion">- port: 3306</span></span><br><span class="line"><span class="addition">+ port: 3307</span></span><br></pre></td></tr></table></figure>

<p>容器内还是 3306（不用动），<strong>只把宿主端口错开</strong>。</p>
<h2 id="第五章：元数据知识库——aiomysql-的回旋镖"><a href="#第五章：元数据知识库——aiomysql-的回旋镖" class="headerlink" title="第五章：元数据知识库——aiomysql 的回旋镖"></a>第五章：元数据知识库——aiomysql 的回旋镖</h2><h3 id="坑-8：aiomysql-ping-签名-bug（异步适配器的副作用）"><a href="#坑-8：aiomysql-ping-签名-bug（异步适配器的副作用）" class="headerlink" title="坑 8：aiomysql ping 签名 bug（异步适配器的副作用）"></a>坑 8：aiomysql ping 签名 bug（异步适配器的副作用）</h3><p>跑 <code>build_meta_knowledge.py</code>（构建元数据知识库的脚本）时：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">AsyncAdapt_aiomysql_connection.ping() missing 1 required positional argument: &#x27;reconnect&#x27;</span><br></pre></td></tr></table></figure>

<p>SQLAlchemy 开了 <code>pool_pre_ping=True</code>（每次取连接前 ping 一下），会调 <code>connection.ping()</code>。但 <strong>aiomysql 的 async 适配器要求 <code>reconnect</code> 参数</strong>（asyncmy 没这问题，是换驱动后的副作用）。</p>
<p>最干净的解决：**关掉 <code>pool_pre_ping</code>**。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># app/clients/mysql_client_manager.py</span></span><br><span class="line">engine = create_async_engine(</span><br><span class="line">    url,</span><br><span class="line">    pool_pre_ping=<span class="literal">False</span>,   <span class="comment"># ← aiomysql 兼容性</span></span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<blockquote>
<p>💡 这个坑很隐蔽：换驱动的隐性代价只有运行时才暴露。**生产环境别只看”功能等价”，要看”配置参数签名等价”**。</p>
</blockquote>
<h3 id="坑-9：build-重跑主键冲突"><a href="#坑-9：build-重跑主键冲突" class="headerlink" title="坑 9：build 重跑主键冲突"></a>坑 9：build 重跑主键冲突</h3><p>第一次失败残留了部分数据，重跑就：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Duplicate entry &#x27;dim_region.region_id&#x27; for key &#x27;PRIMARY&#x27;</span><br></pre></td></tr></table></figure>

<p>清表后重跑：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker <span class="built_in">exec</span> mysql mysql -u didilili -pdili123 meta -e \</span><br><span class="line">  <span class="string">&quot;SET FOREIGN_KEY_CHECKS=0;</span></span><br><span class="line"><span class="string">   DELETE FROM column_info;</span></span><br><span class="line"><span class="string">   DELETE FROM column_metric;</span></span><br><span class="line"><span class="string">   DELETE FROM metric_info;</span></span><br><span class="line"><span class="string">   DELETE FROM table_info;</span></span><br><span class="line"><span class="string">   SET FOREIGN_KEY_CHECKS=1;&quot;</span></span><br></pre></td></tr></table></figure>

<p><strong>小细节</strong>：mysql 命令在密码告警时会输出到 stderr，传统的 <code>cmd 2&gt;&amp;1 | grep -v Warning</code> 加上 <code>&amp;&amp;</code> 链会因为 <code>grep</code> 在”空匹配时返回 1”而断链。改用 <code>;</code> 分隔命令最稳。</p>
<p>最终：<strong>5 表 &#x2F; 24 字段 &#x2F; 2 指标</strong> 落库，元数据知识库就位。</p>
<h2 id="第六章：后端启动——GBK-编码下的-emoji-血案"><a href="#第六章：后端启动——GBK-编码下的-emoji-血案" class="headerlink" title="第六章：后端启动——GBK 编码下的 emoji 血案"></a>第六章：后端启动——GBK 编码下的 emoji 血案</h2><h3 id="坑-10：fastapi-dev-因-rich-emoji-崩"><a href="#坑-10：fastapi-dev-因-rich-emoji-崩" class="headerlink" title="坑 10：fastapi dev 因 rich emoji 崩"></a>坑 10：<code>fastapi dev</code> 因 rich emoji 崩</h3><p>按 README 跑：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uv run fastapi dev main.py</span><br></pre></td></tr></table></figure>

<p>报错：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">UnicodeEncodeError: &#x27;gbk&#x27; codec can&#x27;t encode character &#x27;\U0001f680&#x27;</span><br></pre></td></tr></table></figure>

<p>fastapi-cli 用 rich 打印 banner，里面有 🚀 这样的 emoji。<strong>Windows 终端默认 GBK</strong>，根本编码不了 unicode。rich 的 win32 渲染器直接崩。</p>
<p>两条路：</p>
<ol>
<li><strong>绕开</strong>：用 uvicorn 直接起（没有 rich banner）</li>
<li><strong>配环境</strong>：告诉 Python 用 UTF-8</li>
</ol>
<p>我两条都做了：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">PYTHONUTF8=1 PYTHONIOENCODING=utf-8 \</span><br><span class="line">  uv run uvicorn main:app --port 8000</span><br></pre></td></tr></table></figure>

<p>后来（2026-07-18 复启）发现：<strong>只要带上 <code>PYTHONUTF8=1</code>，<code>fastapi dev</code> 也能跑</strong>。这条经验也写进了 README。</p>
<p>后端起来后还会有一堆 <code>jieba</code> 的 <code>SyntaxWarning: invalid escape sequence</code>——<strong>无害</strong>，是 jieba 旧正则在 Python 3.14 下的告警，不影响功能。</p>
<h2 id="第七章：模型选型心路——这是最长的一章"><a href="#第七章：模型选型心路——这是最长的一章" class="headerlink" title="第七章：模型选型心路——这是最长的一章"></a>第七章：模型选型心路——这是最长的一章</h2><blockquote>
<p>这是整篇博客最值得读的部分。前 10 个坑都是工程问题，<strong>坑 12 是 LLM 应用的核心架构问题</strong>。</p>
</blockquote>
<p><img data-src="/images/posts/shopkeeper-agent-troubleshooting/fig3-llm-decision_001.jpg" alt="LLM 模型选型决策树"><br><em>图 3：从 MiniMax-M2.7 到 M3 + extra_body 的四步选型路径</em></p>
<p>shopkeeper-agent 用 LangChain 的 <strong>PydanticOutputParser</strong>（一种 JSON 输出解析器）来约束 LLM 输出。解析器期望：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span><span class="attr">&quot;intent&quot;</span><span class="punctuation">:</span> <span class="string">&quot;aggregation&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;metric&quot;</span><span class="punctuation">:</span> <span class="string">&quot;销售总额&quot;</span><span class="punctuation">,</span> ...<span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>而<strong>推理模型（reasoner）默认会先输出一段思考过程</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">用户问的是华北地区的销售总额，我需要先找到对应区域字段，</span><br><span class="line">然后聚合 sum(销售金额)，按 region_name = &#x27;华北&#x27; 过滤...</span><br><span class="line">&#123;&quot;intent&quot;: &quot;aggregation&quot;, ...&#125;</span><br></pre></td></tr></table></figure>

<p>LangChain 的 JSON 解析器撞上 &#96;&#96; 标签就直接 <code>OUTPUT_PARSING_FAILURE</code>，整个 recall_value &#x2F; recall_metric 节点失败。</p>
<p>查 MiniMax 官方文档说”M 系列不能关闭 think”——<strong>这个结论不准确</strong>：</p>
<ul>
<li><strong>M2.x</strong>：确实不能关闭（即使传 <code>thinking: &#123;&quot;type&quot;: &quot;disabled&quot;&#125;</code> 也会被忽略）</li>
<li><strong>M3</strong>：<strong>支持</strong> <code>thinking: &#123;&quot;type&quot;: &quot;disabled&quot;&#125;</code> 关掉 think 输出（内部思考能力保留，只是不返回 think 块）</li>
</ul>
<h3 id="解决（M3-extra-body）"><a href="#解决（M3-extra-body）" class="headerlink" title="解决（M3 + extra_body）"></a>解决（M3 + extra_body）</h3><p><code>app/agent/llm.py</code> 的 <code>init_chat_model(...)</code> 加一行参数：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">extra_body=&#123;<span class="string">&quot;thinking&quot;</span>: &#123;<span class="string">&quot;type&quot;</span>: <span class="string">&quot;disabled&quot;</span>&#125;&#125;,</span><br></pre></td></tr></table></figure>

<p>配合：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># conf/app_config.yaml</span></span><br><span class="line"><span class="attr">llm:</span></span><br><span class="line">  <span class="attr">model_name:</span> <span class="string">MiniMax-M3</span></span><br><span class="line">  <span class="attr">api_key:</span> <span class="string">$&#123;oc.env:LLM_API_KEY&#125;</span></span><br><span class="line">  <span class="attr">base_url:</span> <span class="string">https://api.minimaxi.com/v1</span></span><br></pre></td></tr></table></figure>

<p><strong>效果</strong>：M3 关 think 后输出纯 JSON，项目 LangChain 解析正常，端到端跑通（简单查询 + 复杂日期 JOIN 查询都过）。M3 内部推理能力保留（关的是输出，不是推理）。</p>
<h3 id="模型选型的完整演进"><a href="#模型选型的完整演进" class="headerlink" title="模型选型的完整演进"></a>模型选型的完整演进</h3><table>
<thead>
<tr>
<th>阶段</th>
<th>模型</th>
<th>结果</th>
<th>备注</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td>MiniMax-M2.7-highspeed</td>
<td>❌ JSON 解析炸</td>
<td>think 输出无法关闭</td>
</tr>
<tr>
<td>2</td>
<td>MiniMax-M3（默认）</td>
<td>❌ JSON 解析炸</td>
<td>同样 think 炸</td>
</tr>
<tr>
<td>3</td>
<td>qwen-plus &#x2F; qwen-max</td>
<td>✅⚠️ 偶发错</td>
<td>简单查询过，复杂日期 JOIN 偶错</td>
</tr>
<tr>
<td>4</td>
<td><strong>MiniMax-M3 + extra_body</strong></td>
<td>✅ 完美</td>
<td>M3 内部强推理 + 输出纯 JSON</td>
</tr>
</tbody></table>
<p>最终选了第 4 条路：<strong>用最强推理能力的模型 + 关掉其思考输出</strong>——既拿到 M3 的复杂推理能力，又拿到干净的 JSON 解析。</p>
<h3 id="坑-13：Git-Bash-自带-curl-坏了"><a href="#坑-13：Git-Bash-自带-curl-坏了" class="headerlink" title="坑 13：Git Bash 自带 curl 坏了"></a>坑 13：Git Bash 自带 curl 坏了</h3><p>端到端测试时碰到个小坑：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -X POST http://localhost:8000/api/query -d <span class="string">&#x27;&#123;&quot;query&quot;:&quot;...&quot;&#125;&#x27;</span></span><br></pre></td></tr></table></figure>

<p><code>curl</code> 无任何输出（连错误都没有），后端日志也无请求记录。<strong>Git Bash 自带的 curl 坏了</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">C:/Program Files/Git/mingw64/bin/curl.exe: error while loading shared libraries</span><br></pre></td></tr></table></figure>

<p>换成 Windows 自带 curl：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/c/Windows/System32/curl.exe -N -X POST http://127.0.0.1:8000/api/query \</span><br><span class="line">  -H <span class="string">&quot;Content-Type: application/json&quot;</span> \</span><br><span class="line">  -d <span class="string">&#x27;&#123;&quot;query&quot;:&quot;统计华北地区的销售总额&quot;&#125;&#x27;</span></span><br></pre></td></tr></table></figure>

<p>返回：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span><span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;result&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;data&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="punctuation">&#123;</span><span class="attr">&quot;销售总额&quot;</span><span class="punctuation">:</span> <span class="number">41099.5</span><span class="punctuation">&#125;</span><span class="punctuation">]</span><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>✅ 端到端跑通。</p>
<p><strong>额外细节</strong>：用 <code>127.0.0.1</code> 不用 <code>localhost</code>（避免 IPv6 解析到 <code>::1</code>，后端只听 IPv4）。</p>
<h3 id="重启后端（换-LLM-后）"><a href="#重启后端（换-LLM-后）" class="headerlink" title="重启后端（换 LLM 后）"></a>重启后端（换 LLM 后）</h3><p>换完模型需要重启后端：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">netstat -ano | grep <span class="string">&quot;:8000 &quot;</span>          <span class="comment"># 找 PID</span></span><br><span class="line">taskkill //PID &lt;pid&gt; //F</span><br><span class="line"><span class="built_in">cd</span> shopkeeper-agent-main</span><br><span class="line">PYTHONUTF8=1 PYTHONIOENCODING=utf-8 uv run uvicorn main:app --port 8000</span><br></pre></td></tr></table></figure>

<h2 id="第八章：前端补完-2026-07-18-复启"><a href="#第八章：前端补完-2026-07-18-复启" class="headerlink" title="第八章：前端补完 + 2026-07-18 复启"></a>第八章：前端补完 + 2026-07-18 复启</h2><h3 id="坑-11：node-14-太老-pnpm-缺失（最终解法）"><a href="#坑-11：node-14-太老-pnpm-缺失（最终解法）" class="headerlink" title="坑 11：node 14 太老 + pnpm 缺失（最终解法）"></a>坑 11：node 14 太老 + pnpm 缺失（最终解法）</h3><p>之前卡在 node 14 + pnpm 缺失。本次确认：</p>
<ul>
<li><code>nvm.exe use 22.20.0</code>（vite 6 + react 19 需 node 18+；本机 nvm 已有 18.20.8 &#x2F; 20.18.0 &#x2F; 22.20.0 &#x2F; 24.9.0）</li>
<li><strong>不用装 pnpm</strong>：<code>frontend/node_modules</code> 早已存在，Git Bash 里 pnpm 又不在 PATH，直接用本地 vite：</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> frontend &amp;&amp; npx --no-install vite</span><br></pre></td></tr></table></figure>

<p>vite v6.4.2，端口 5173，<code>/api</code> 已代理到 8000（<code>vite.config.ts</code> 的 <code>VITE_DEV_PROXY_TARGET</code>，默认 <code>http://127.0.0.1:8000</code>）。</p>
<h3 id="本次完整启动顺序（5-步）"><a href="#本次完整启动顺序（5-步）" class="headerlink" title="本次完整启动顺序（5 步）"></a>本次完整启动顺序（5 步）</h3><ol>
<li>启 Docker Desktop（GUI，轮询 <code>docker ps</code> 直到通）</li>
<li><code>docker compose -f docker/docker-compose.yaml up -d</code>（5 容器；内存紧可 <code>docker stop kibana</code>，<code>restart: unless-stopped</code> 下不会自动重起）</li>
<li>后端 <code>PYTHONUTF8=1 PYTHONIOENCODING=utf-8 uv run fastapi dev main.py</code>（lifespan 自动连 4 个中间件；元数据在 docker volume 里，**无需重跑 <code>build_meta_knowledge</code>**）</li>
<li>前端 <code>nvm use 22.20.0 &amp;&amp; cd frontend &amp;&amp; npx --no-install vite</code></li>
<li>浏览器开 <a href="http://localhost:5173/">http://localhost:5173</a> 问数</li>
</ol>
<h2 id="排障时间线复盘"><a href="#排障时间线复盘" class="headerlink" title="排障时间线复盘"></a>排障时间线复盘</h2><p><img data-src="/images/posts/shopkeeper-agent-troubleshooting/fig4-timeline_001.jpg" alt="13 个坑的排障时间线"><br><em>图 4：13 个坑按启动顺序排列的时间线</em></p>
<p>把 13 个坑按启动顺序拉成一条线，能看清几件事：</p>
<ul>
<li><strong>前 1&#x2F;3 是环境问题</strong>（Python、依赖、Embedding），全部是”装东西”阶段的</li>
<li><strong>中间 1&#x2F;3 是中间件问题</strong>（Docker、Mysql、知识库构建），全部是”起服务”阶段的</li>
<li><strong>最后 1&#x2F;3 是 LLM 和前端问题</strong>（模型选型、curl、node 版本），全部是”用起来”阶段的</li>
</ul>
<p>每一阶段的失败都跟前一阶段强相关——<strong>这就是排障的真相：bug 不会单独出现，总是连环来</strong>。</p>
<h2 id="经验总结：13-个坑的根因分类"><a href="#经验总结：13-个坑的根因分类" class="headerlink" title="经验总结：13 个坑的根因分类"></a>经验总结：13 个坑的根因分类</h2><p>写完所有坑之后回头看，我把它们归到 5 个根因桶里：</p>
<h3 id="1-Windows-适配（3-个）"><a href="#1-Windows-适配（3-个）" class="headerlink" title="1. Windows 适配（3 个）"></a>1. Windows 适配（3 个）</h3><ul>
<li>asyncmy 编译失败（无 MSVC）</li>
<li>rich emoji 编码崩（GBK 终端）</li>
<li>Git Bash curl 共享库丢失</li>
</ul>
<p><strong>对策</strong>：项目一开始就走”纯 Python 优先 + UTF-8 强制”路线，能避开一大半 Windows 适配坑。</p>
<h3 id="2-国内网络（1-个）"><a href="#2-国内网络（1-个）" class="headerlink" title="2. 国内网络（1 个）"></a>2. 国内网络（1 个）</h3><ul>
<li>hf-mirror 不稳 → ModelScope</li>
</ul>
<p><strong>对策</strong>：直接用 ModelScope 作为默认 HF 替代，别再赌 hf-mirror。</p>
<h3 id="3-环境冲突（2-个）"><a href="#3-环境冲突（2-个）" class="headerlink" title="3. 环境冲突（2 个）"></a>3. 环境冲突（2 个）</h3><ul>
<li>端口 3306、容器名 mysql</li>
</ul>
<p><strong>对策</strong>：用 <code>docker ps -a</code> + <code>netstat</code> 先做端口&#x2F;容器名扫描，再起新项目。</p>
<h3 id="4-Python-3-14-太新（1-个）"><a href="#4-Python-3-14-太新（1-个）" class="headerlink" title="4. Python 3.14 太新（1 个）"></a>4. Python 3.14 太新（1 个）</h3><ul>
<li>asyncmy 无 wheel</li>
</ul>
<p><strong>对策</strong>：要么锁 Python 3.12，要么默认走纯 Python 驱动（aiomysql&#x2F;PyMySQL）。</p>
<h3 id="5-内存紧张（1-个）"><a href="#5-内存紧张（1-个）" class="headerlink" title="5. 内存紧张（1 个）"></a>5. 内存紧张（1 个）</h3><ul>
<li>16G 跑 5 容器 + 桌面程序 → OOM，跳 kibana 缓解</li>
</ul>
<p><strong>对策</strong>：docker compose 文件要分层——<strong>必选 vs 可选</strong>。kibana 这种纯可视化工具拆出来，内存不够时直接 skip。</p>
<h3 id="6-LLM-架构（2-个）"><a href="#6-LLM-架构（2-个）" class="headerlink" title="6. LLM 架构（2 个）"></a>6. LLM 架构（2 个）</h3><ul>
<li>推理模型 think 输出与 JSON parser 不兼容</li>
<li>extra_body 关闭 think 是解药</li>
</ul>
<p><strong>对策</strong>：用 LangChain 的输出解析器时，<strong>默认假设模型会输出额外内容</strong>——优先选非推理模型，或者用 <code>extra_body</code> 关 think。</p>
<h3 id="7-Node-生态（1-个）"><a href="#7-Node-生态（1-个）" class="headerlink" title="7. Node 生态（1 个）"></a>7. Node 生态（1 个）</h3><ul>
<li>node 14 EOL + pnpm 不在 PATH</li>
</ul>
<p><strong>对策</strong>：用 nvm 切 node 20+，项目根放个 <code>.nvmrc</code> 锁版本。</p>
<h2 id="关键改动文件最终汇总"><a href="#关键改动文件最终汇总" class="headerlink" title="关键改动文件最终汇总"></a>关键改动文件最终汇总</h2><table>
<thead>
<tr>
<th>文件</th>
<th>改动</th>
<th>根因</th>
</tr>
</thead>
<tbody><tr>
<td><code>ai-application/pyproject.toml</code></td>
<td>加 <code>version = &quot;0.0.0&quot;</code></td>
<td>uv 扫祖先解析</td>
</tr>
<tr>
<td><code>shopkeeper-agent-main/pyproject.toml</code></td>
<td><code>asyncmy</code> → <code>aiomysql</code></td>
<td>Windows 无 MSVC</td>
</tr>
<tr>
<td><code>app/clients/mysql_client_manager.py</code></td>
<td>asyncmy→aiomysql + <code>pool_pre_ping=False</code></td>
<td>驱动替换 + ping bug</td>
</tr>
<tr>
<td><code>conf/app_config.yaml</code></td>
<td>LLM &#x3D; MiniMax-M3 + mysql 端口 3307</td>
<td>模型切换 + 端口避让</td>
</tr>
<tr>
<td><code>docker/docker-compose.yaml</code></td>
<td>mysql 端口 <code>3307:3306</code></td>
<td>端口避让本机 mysql</td>
</tr>
<tr>
<td><code>app/agent/llm.py</code></td>
<td><code>extra_body=&#123;&quot;thinking&quot;:&#123;&quot;type&quot;:&quot;disabled&quot;&#125;&#125;</code></td>
<td>关 M3 think 输出</td>
</tr>
<tr>
<td><code>.env</code></td>
<td>MiniMax API Key</td>
<td>LLM 鉴权</td>
</tr>
</tbody></table>
<h2 id="下次启动（快速恢复）"><a href="#下次启动（快速恢复）" class="headerlink" title="下次启动（快速恢复）"></a>下次启动（快速恢复）</h2><h3 id="同机器（数据-模型都在，3-步）"><a href="#同机器（数据-模型都在，3-步）" class="headerlink" title="同机器（数据&#x2F;模型都在，3 步）"></a>同机器（数据&#x2F;模型都在，3 步）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> F:/bug图/zhioai/ai-application/ai-application/源码项目/shopkeeper-agent-main</span><br><span class="line"></span><br><span class="line"><span class="comment"># 1. 起 4 容器（数据在 docker volume，自动恢复）</span></span><br><span class="line">docker compose -f docker/docker-compose.yaml up -d mysql elasticsearch qdrant embedding</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 起后端</span></span><br><span class="line">PYTHONUTF8=1 PYTHONIOENCODING=utf-8 uv run uvicorn main:app --port 8000</span><br><span class="line"></span><br><span class="line"><span class="comment"># 3. 起前端（另开一个终端）</span></span><br><span class="line"><span class="built_in">cd</span> frontend &amp;&amp; npx --no-install vite</span><br></pre></td></tr></table></figure>

<p>浏览器开 <a href="http://localhost:5173/">http://localhost:5173</a> 即可问数。</p>
<h3 id="关-Docker（数据安全）"><a href="#关-Docker（数据安全）" class="headerlink" title="关 Docker（数据安全）"></a>关 Docker（数据安全）</h3><ul>
<li><strong>直接关 Docker Desktop 即可，数据安全</strong>：mysql&#x2F;es&#x2F;qdrant 数据在 docker volume（<code>mysql_data</code>&#x2F;<code>es_data</code>&#x2F;<code>qdrant_data</code>），关 Docker 只是停容器，<strong>不删 volume</strong>，下次 <code>docker compose up</code> 数据自动恢复</li>
<li>embedding 模型在本地 <code>docker/embedding/bge-large-zh-v1.5/</code>（1.3G，gitignore 不传 GitHub），只要不删这个目录，下次不用重下</li>
<li>⚠️ <strong>别用「Reset Docker to factory defaults」或手动删 volume</strong>，那会清空 mysql&#x2F;es&#x2F;qdrant 数据，要重跑 build_meta_knowledge</li>
</ul>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>写到这里回头看，13 个坑单独看都不大，<strong>但叠加起来就是两天的工作量</strong>。这也正是”AI 应用项目”的真实写照——核心代码可能只占 20%，剩下 80% 全在和”环境、依赖、网络、模型版本”做斗争。</p>
<p>如果这篇博客能帮你省下哪怕一个小时的排障时间，那这 4000 字就值了。</p>
<p>欢迎在评论区分享你的踩坑故事——<strong>我相信每个跑过本地 LLM 项目的人都有一个”坑 12”故事</strong>。</p>
]]></content>
      <categories>
        <category>AI经验</category>
      </categories>
      <tags>
        <tag>shopkeeper-agent</tag>
        <tag>AI问数</tag>
        <tag>排障记录</tag>
        <tag>MiniMax</tag>
        <tag>Docker</tag>
        <tag>FastAPI</tag>
        <tag>uv</tag>
        <tag>Python3.14</tag>
      </tags>
  </entry>
  <entry>
    <title>南沙项目物联网 - 月卡管理模块 Bug 排查记录</title>
    <url>/2026/03/28/%E5%8D%97%E6%B2%99%E9%A1%B9%E7%9B%AE%E7%89%A9%E8%81%94%E7%BD%91%E6%9C%88%E5%8D%A1%E7%AE%A1%E7%90%86Bug%E6%8E%92%E6%9F%A5%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<p>南沙项目物联网的月卡管理模块在日常使用中陆续发现了三个 Bug，涵盖前端接口调用错误、后端空指针异常、以及业务逻辑校验缺陷三类典型问题。本文记录这三个 Bug 的完整排查过程、根因分析及修复方案，供后续开发参考。</p>
<span id="more"></span>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>月卡管理是停车系统的核心功能之一，涉及费用计算、车位绑定、审批流程等多个子模块。本次排查的三个 Bug 分别来自：</p>
<table>
<thead>
<tr>
<th>序号</th>
<th>问题</th>
<th>类型</th>
<th>影响</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td>个人月卡拒绝功能 404 错误</td>
<td>前端接口路径不匹配</td>
<td>拒绝功能完全不可用</td>
</tr>
<tr>
<td>2</td>
<td>月卡变更审批拒绝 404 错误</td>
<td>后端空指针异常</td>
<td>拒绝操作触发 404</td>
</tr>
<tr>
<td>3</td>
<td>月卡变更车位占用校验失效</td>
<td>业务逻辑错误</td>
<td>空闲车位被误报占用</td>
</tr>
</tbody></table>
<p>这三个问题虽然都表现为”404 错误”，但根因完全不同，体现了停车场系统中常见的几类典型缺陷。</p>
<hr>
<h2 id="问题一：个人月卡拒绝功能-404"><a href="#问题一：个人月卡拒绝功能-404" class="headerlink" title="问题一：个人月卡拒绝功能 404"></a>问题一：个人月卡拒绝功能 404</h2><h3 id="问题现象"><a href="#问题现象" class="headerlink" title="问题现象"></a>问题现象</h3><p>费用管理模块 → 个人月卡页面，点击”拒绝退款”或”拒绝变更”按钮时，系统报错 <code>Request failed with status code 404</code>。</p>
<h3 id="排查过程"><a href="#排查过程" class="headerlink" title="排查过程"></a>排查过程</h3><p><strong>Step 1：定位后端接口</strong></p>
<p>通过代码搜索找到后端 Controller：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">文件: src/main/java/cn/flyrise/pai/nsrx/iva/controller/MonthPersonalController.java</span><br></pre></td></tr></table></figure>

<p>后端实际提供的接口是：</p>
<ul>
<li><code>POST /v1/monthpersonal/changeRefuse</code> — 拒绝变更（第 296-301 行）</li>
<li><code>POST /v1/monthpersonal/refundRefuse</code> — 拒绝退款（第 307-312 行）</li>
</ul>
<p><strong>Step 2：查看前端代码</strong></p>
<p>前端 <code>api/iva/monthPersonal.js</code> 中的 API 定义如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 前端实际调用</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">const</span> <span class="title function_">updateFlow</span> = (<span class="params">row</span>) =&gt; &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="title function_">request</span>(&#123;</span><br><span class="line">    <span class="attr">url</span>: <span class="string">`<span class="subst">$&#123;service&#125;</span>/<span class="subst">$&#123;ver&#125;</span>/<span class="subst">$&#123;<span class="variable language_">module</span>&#125;</span>/updateFlow`</span>,</span><br><span class="line">    <span class="attr">method</span>: <span class="string">&#x27;post&#x27;</span>,</span><br><span class="line">    <span class="attr">data</span>: row</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>Step 3：根因分析</strong></p>
<table>
<thead>
<tr>
<th>功能</th>
<th>前端调用</th>
<th>后端实际接口</th>
<th>状态</th>
</tr>
</thead>
<tbody><tr>
<td>拒绝变更</td>
<td><code>/updateFlow</code></td>
<td><code>/changeRefuse</code></td>
<td>❌ 不匹配</td>
</tr>
<tr>
<td>拒绝退款</td>
<td><code>/updateFlow</code></td>
<td><code>/refundRefuse</code></td>
<td>❌ 不匹配</td>
</tr>
</tbody></table>
<p>前端调用了 <code>/updateFlow</code>，但后端根本没有这个接口。Spring Boot 在找不到对应路由时返回 404。</p>
<p><img data-src="/images/posts/%E5%8D%97%E6%B2%99%E9%A1%B9%E7%9B%AE%E7%89%A9%E8%81%94%E7%BD%91%E6%9C%88%E5%8D%A1%E7%AE%A1%E7%90%86Bug%E6%8E%92%E6%9F%A5%E8%AE%B0%E5%BD%95/fig1.png" alt="前后端接口调用流程"><br><em>图1：前端调用 &#x2F;updateFlow 但后端接口不匹配，导致 404 错误</em></p>
<h3 id="修复方案"><a href="#修复方案" class="headerlink" title="修复方案"></a>修复方案</h3><p><strong>Step 1</strong>：在 <code>monthPersonal.js</code> 中添加两个新的 API 方法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 拒绝变更</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">const</span> <span class="title function_">changeRefuse</span> = (<span class="params">row</span>) =&gt; &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="title function_">request</span>(&#123;</span><br><span class="line">    <span class="attr">url</span>: <span class="string">`<span class="subst">$&#123;service&#125;</span>/<span class="subst">$&#123;ver&#125;</span>/<span class="subst">$&#123;<span class="variable language_">module</span>&#125;</span>/changeRefuse`</span>,</span><br><span class="line">    <span class="attr">method</span>: <span class="string">&#x27;post&#x27;</span>,</span><br><span class="line">    <span class="attr">data</span>: row</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 拒绝退款</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">const</span> <span class="title function_">refundRefuse</span> = (<span class="params">row</span>) =&gt; &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="title function_">request</span>(&#123;</span><br><span class="line">    <span class="attr">url</span>: <span class="string">`<span class="subst">$&#123;service&#125;</span>/<span class="subst">$&#123;ver&#125;</span>/<span class="subst">$&#123;<span class="variable language_">module</span>&#125;</span>/refundRefuse`</span>,</span><br><span class="line">    <span class="attr">method</span>: <span class="string">&#x27;post&#x27;</span>,</span><br><span class="line">    <span class="attr">data</span>: row</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>Step 2</strong>：修改 Vue 页面中的调用方法，将 <code>updateFlow</code> 替换为对应的拒绝接口：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 拒绝退款</span></span><br><span class="line"><span class="title function_">refundRefuse</span>(&#123; <span class="attr">id</span>: row.<span class="property">id</span>, <span class="attr">msg</span>: <span class="variable language_">this</span>.<span class="property">msg</span> &#125;)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 拒绝变更</span></span><br><span class="line"><span class="title function_">changeRefuse</span>(&#123; <span class="attr">id</span>: row.<span class="property">id</span>, <span class="attr">msg</span>: <span class="variable language_">this</span>.<span class="property">rejectReason</span> &#125;)</span><br></pre></td></tr></table></figure>

<h3 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h3><ul>
<li>前后端接口命名需保持一致，建议通过 Swagger 文档同步接口</li>
<li>代码审查时检查接口匹配性</li>
<li>测试覆盖完整的业务流程（通过&#x2F;拒绝两种路径）</li>
</ul>
<hr>
<h2 id="问题二：月卡变更审批拒绝-404"><a href="#问题二：月卡变更审批拒绝-404" class="headerlink" title="问题二：月卡变更审批拒绝 404"></a>问题二：月卡变更审批拒绝 404</h2><h3 id="问题现象-1"><a href="#问题现象-1" class="headerlink" title="问题现象"></a>问题现象</h3><p>费用管理模块 → 月卡变更列表页面 → 点击”审核” → 选择”拒绝”时，系统报错 404。但选择”通过”时正常。</p>
<h3 id="排查过程-1"><a href="#排查过程-1" class="headerlink" title="排查过程"></a>排查过程</h3><p><strong>Step 1：定位后端接口</strong></p>
<p>Controller 路径为 <code>src/main/java/cn/flyrise/pai/nsrx/iva/controller/MonthPersonListController.java</code>，审核接口为 <code>POST /v1/monthpersonlist/audit</code>。</p>
<p><strong>Step 2：分析 audit 方法</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@PostMapping(&quot;/audit&quot;)</span></span><br><span class="line"><span class="keyword">public</span> R <span class="title function_">audit</span><span class="params">(<span class="meta">@Valid</span> <span class="meta">@RequestBody</span> MonthPersonListEntity monthPersonList)</span> &#123;</span><br><span class="line">    <span class="comment">// 写入日志</span></span><br><span class="line">    <span class="keyword">if</span>(StringUtil.isNotBlank(monthPersonList.getRefuseReason()))&#123;</span><br><span class="line">        <span class="type">LogInfoEntity</span> <span class="variable">infoEntity</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">LogInfoEntity</span>();</span><br><span class="line">        infoEntity.setKeyId(monthPersonList.getId()+<span class="string">&quot;&quot;</span>);</span><br><span class="line">        infoEntity.setRemark(monthPersonList.getRefuseReason());</span><br><span class="line">        infoEntity.setOperateAction(<span class="string">&quot;拒绝审核&quot;</span>);</span><br><span class="line">        logInfoService.save(infoEntity);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">boolean</span> <span class="variable">flag</span> <span class="operator">=</span> monthPersonListService.updateById(monthPersonList);</span><br><span class="line">    <span class="keyword">if</span>(flag)&#123;</span><br><span class="line">        <span class="keyword">if</span>(<span class="string">&quot;4&quot;</span>.equals(monthPersonList.getState()))&#123;  <span class="comment">// state=4 表示拒绝</span></span><br><span class="line">            <span class="type">MonthPersonalEntity</span> <span class="variable">detail</span> <span class="operator">=</span> monthPersonalService.getById(monthPersonList.getMonthlyCardId());</span><br><span class="line">            detail.setState(<span class="string">&quot;11&quot;</span>);  <span class="comment">// ← NPE 风险！如果 detail 为 null</span></span><br><span class="line">            monthPersonalService.updateById(detail);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> R.status(flag);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>Step 3：根因分析</strong></p>
<p>当审核状态为”拒绝”（<code>state=&quot;4&quot;</code>）时，代码通过 <code>monthlyCardId</code> 查询 <code>MonthPersonalEntity</code>，但<strong>未进行空值检查</strong>。</p>
<ul>
<li>如果 <code>monthlyCardId</code> 对应的月卡记录不存在，<code>getById()</code> 返回 <code>null</code></li>
<li>后续 <code>detail.setState(&quot;11&quot;)</code> 抛出 <code>NullPointerException</code></li>
<li>BladeX 框架的全局异常处理器将 NPE 映射为 HTTP 404</li>
</ul>
<p><strong>为什么”通过”正常？</strong></p>
<p>“通过”时 <code>state=&quot;3&quot;</code>，不进入 <code>if(&quot;4&quot;.equals(...))</code> 分支，因此不会触发 NPE。</p>
<p><img data-src="/images/posts/%E5%8D%97%E6%B2%99%E9%A1%B9%E7%9B%AE%E7%89%A9%E8%81%94%E7%BD%91%E6%9C%88%E5%8D%A1%E7%AE%A1%E7%90%86Bug%E6%8E%92%E6%9F%A5%E8%AE%B0%E5%BD%95/fig2.png" alt="NPE导致404的调用链"><br><em>图2：state&#x3D;4 时 getById 返回 null，detail.setState 触发 NPE，框架转换为 404</em></p>
<h3 id="流程对比"><a href="#流程对比" class="headerlink" title="流程对比"></a>流程对比</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">graph TD</span><br><span class="line">    A[前端调用 /audit] --&gt; B&#123;monthPersonList.getState?&#125;</span><br><span class="line">    B --&gt;|state = &quot;3&quot; 通过| C[仅更新变更记录]</span><br><span class="line">    C --&gt; D[✅ 正常返回]</span><br><span class="line">    B --&gt;|state = &quot;4&quot; 拒绝| E[getById monthlyCardId]</span><br><span class="line">    E --&gt;|记录存在| F[detail.setState 11]</span><br><span class="line">    F --&gt; D</span><br><span class="line">    E --&gt;|记录不存在 null| G[detail.setState 11]</span><br><span class="line">    G --&gt; H[❌ NPE → 404]</span><br></pre></td></tr></table></figure>

<h3 id="修复方案-1"><a href="#修复方案-1" class="headerlink" title="修复方案"></a>修复方案</h3><p>在 <code>getById</code> 之后添加空值检查：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span>(<span class="string">&quot;4&quot;</span>.equals(monthPersonList.getState()))&#123;</span><br><span class="line">    <span class="type">MonthPersonalEntity</span> <span class="variable">detail</span> <span class="operator">=</span> monthPersonalService.getById(monthPersonList.getMonthlyCardId());</span><br><span class="line">    <span class="keyword">if</span>(detail != <span class="literal">null</span>) &#123;                            <span class="comment">// 添加空值检查</span></span><br><span class="line">        detail.setState(<span class="string">&quot;11&quot;</span>);</span><br><span class="line">        monthPersonalService.updateById(detail);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        log.warn(<span class="string">&quot;月卡变更审批拒绝：月卡记录不存在, monthlyCardId=&#123;&#125;, personListId=&#123;&#125;&quot;</span>,</span><br><span class="line">                 monthPersonList.getMonthlyCardId(), monthPersonList.getId());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="经验总结-1"><a href="#经验总结-1" class="headerlink" title="经验总结"></a>经验总结</h3><ul>
<li>数据库查询后必须进行空值检查</li>
<li>全局异常处理器会将未捕获的 RuntimeException 映射为特定 HTTP 状态码</li>
<li>同类代码对比：<code>MonthPersonalBiz.changeRefuse()</code> 做了空值检查，而 <code>MonthPersonListController.audit()</code> 遗漏了这个检查</li>
</ul>
<hr>
<h2 id="问题三：月卡变更车位占用校验-Bug"><a href="#问题三：月卡变更车位占用校验-Bug" class="headerlink" title="问题三：月卡变更车位占用校验 Bug"></a>问题三：月卡变更车位占用校验 Bug</h2><h3 id="问题现象-2"><a href="#问题现象-2" class="headerlink" title="问题现象"></a>问题现象</h3><p>个人月卡变更固定车位时，从空闲车位列表中选择了一个空闲车位，但提交变更时系统返回”车位已经被占用”，无法提交申请。</p>
<h3 id="排查过程-2"><a href="#排查过程-2" class="headerlink" title="排查过程"></a>排查过程</h3><p><strong>Step 1：定位报错位置</strong></p>
<p>搜索后端”已占用”相关错误信息，找到 3 处：</p>
<table>
<thead>
<tr>
<th>文件</th>
<th>行号</th>
<th>错误信息</th>
<th>场景</th>
</tr>
</thead>
<tbody><tr>
<td><code>UserMonthCompanyController.java</code></td>
<td>185</td>
<td>“车位XX已被占用”</td>
<td>企业租赁修改</td>
</tr>
<tr>
<td><code>MouthPersonalBiz.java</code></td>
<td>175</td>
<td>“车位已占用”</td>
<td>个人月卡申请</td>
</tr>
<tr>
<td><code>MouthPersonalBiz.java</code></td>
<td><strong>605</strong></td>
<td><strong>“车位已经被占用”</strong></td>
<td>个人月卡变更 ← 本次问题</td>
</tr>
</tbody></table>
<p><strong>Step 2：分析 change() 方法</strong></p>
<p><code>MouthPersonalBiz.java</code> 第 594-641 行：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> R <span class="title function_">change</span><span class="params">(MouthPersonalRefundApplyDTO dto)</span> &#123;</span><br><span class="line">    <span class="type">MonthPersonalEntity</span> <span class="variable">monthPersonal</span> <span class="operator">=</span> monthPersonalService.getById(dto.getId());</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    <span class="comment">// 检查车位是否被占用</span></span><br><span class="line">    <span class="keyword">if</span>(monthPersonal.getParkingSpotId()!=<span class="literal">null</span>)&#123;</span><br><span class="line">        <span class="type">ParkSlotEntity</span> <span class="variable">spot</span> <span class="operator">=</span> parkSlotService.getById(monthPersonal.getParkingSpotId());</span><br><span class="line">        <span class="keyword">if</span>(!spot.getUsingState().equals(<span class="string">&quot;1&quot;</span>))&#123;</span><br><span class="line">            <span class="keyword">return</span> R.data(<span class="number">500</span>, <span class="literal">null</span>, <span class="string">&quot;车位已经被占用&quot;</span>);  <span class="comment">// ← BUG</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>Step 3：追踪车位状态</strong></p>
<p>月卡申请成功后，车位会被设为占用状态（<code>usingState = &quot;4&quot;</code>）：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span>(monthPersonal.getParkingSpotId()!=<span class="literal">null</span>)&#123;</span><br><span class="line">    <span class="type">ParkSlotEntity</span> <span class="variable">entity</span> <span class="operator">=</span> parkSlotService.getById(monthPersonal.getParkingSpotId());</span><br><span class="line">    entity.setUsingState(<span class="string">&quot;4&quot;</span>);  <span class="comment">// &quot;4&quot; = 占用</span></span><br><span class="line">    entity.setMonthCar(monthPersonal.getCarNumber());</span><br><span class="line">    entity.setParkingCar(monthPersonal.getCarNumber());</span><br><span class="line">    parkSlotService.updateById(entity);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="根因分析"><a href="#根因分析" class="headerlink" title="根因分析"></a>根因分析</h3><p>这个 Bug 包含 <strong>3 个子问题</strong>：</p>
<p><strong>Bug 1：检查的是老车位而非新车位</strong></p>
<p><code>change()</code> 方法获取的是当前已绑定的老车位 ID。老车位在月卡申请时已被设为占用（<code>usingState = &quot;4&quot;</code>），所以 <code>!spot.getUsingState().equals(&quot;1&quot;)</code> 永远为 <code>true</code>。</p>
<p><strong>Bug 2：DTO 缺少新车位 ID 字段</strong></p>
<p><code>MouthPersonalRefundApplyDTO</code> 只包含退款相关字段，<strong>没有接收新车位 ID 的字段</strong>。前端传入的新车位 ID 无法到达后端。</p>
<p><strong>Bug 3：新车位用老 ID 查询</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">ParkSlotEntity</span> <span class="variable">newSpot</span> <span class="operator">=</span> parkSlotService.getById(monthPersonal.getParkingSpotId());</span><br><span class="line"><span class="type">ParkSlotEntity</span> <span class="variable">oldSpot</span> <span class="operator">=</span> parkSlotService.getById(monthPersonal.getParkingSpotId());</span><br><span class="line"><span class="comment">// newSpot 和 oldSpot 查询的是同一记录</span></span><br></pre></td></tr></table></figure>

<p><img data-src="/images/posts/%E5%8D%97%E6%B2%99%E9%A1%B9%E7%9B%AE%E7%89%A9%E8%81%94%E7%BD%91%E6%9C%88%E5%8D%A1%E7%AE%A1%E7%90%86Bug%E6%8E%92%E6%9F%A5%E8%AE%B0%E5%BD%95/fig3.png" alt="车位检查逻辑对比"><br><em>图3：检查老车位（错误 - 已被月卡绑定）vs 检查新车位（正确 - 应该检查目标车位是否空闲）</em></p>
<h3 id="修复方案-2"><a href="#修复方案-2" class="headerlink" title="修复方案"></a>修复方案</h3><p><strong>Step 1</strong>：DTO 增加 <code>newParkingSpotId</code> 字段</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@ApiModelProperty(value = &quot;新车位ID（变更车位时必传）&quot;)</span></span><br><span class="line"><span class="meta">@JsonSerialize(using = ToStringSerializer.class)</span></span><br><span class="line"><span class="keyword">private</span> Long newParkingSpotId;</span><br></pre></td></tr></table></figure>

<p><strong>Step 2</strong>：重写 <code>change()</code> 车位检查逻辑</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 变更车位</span></span><br><span class="line"><span class="type">Long</span> <span class="variable">newParkingSpotId</span> <span class="operator">=</span> dto.getNewParkingSpotId();</span><br><span class="line"><span class="keyword">if</span>(newParkingSpotId == <span class="literal">null</span>)&#123;</span><br><span class="line">    <span class="keyword">return</span> R.fail(<span class="string">&quot;请选择新车位&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="type">ParkSlotEntity</span> <span class="variable">newSpot</span> <span class="operator">=</span> parkSlotService.getById(newParkingSpotId);</span><br><span class="line"><span class="keyword">if</span>(newSpot == <span class="literal">null</span>)&#123;</span><br><span class="line">    <span class="keyword">return</span> R.fail(<span class="string">&quot;新车位不存在&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">if</span>(!<span class="string">&quot;1&quot;</span>.equals(newSpot.getUsingState()))&#123;</span><br><span class="line">    <span class="keyword">return</span> R.fail(<span class="string">&quot;新车位&quot;</span> + newSpot.getName() + <span class="string">&quot;已被占用，请重新选择&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>Step 3</strong>：<code>MonthPersonListEntity</code> 增加 <code>afterValueId</code> 字段保存新车位 ID</p>
<p><strong>Step 4</strong>：<code>changePass()</code> 审批通过时切换车位绑定</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 释放老车位</span></span><br><span class="line"><span class="keyword">if</span>(monthPersonal.getParkingSpotId() != <span class="literal">null</span>)&#123;</span><br><span class="line">    <span class="type">ParkSlotEntity</span> <span class="variable">oldSlot</span> <span class="operator">=</span> parkSlotService.getById(monthPersonal.getParkingSpotId());</span><br><span class="line">    <span class="keyword">if</span>(oldSlot != <span class="literal">null</span>)&#123;</span><br><span class="line">        oldSlot.setUsingState(<span class="string">&quot;1&quot;</span>);</span><br><span class="line">        oldSlot.setMonthCar(<span class="literal">null</span>);</span><br><span class="line">        oldSlot.setParkingCar(<span class="literal">null</span>);</span><br><span class="line">        parkSlotService.updateById(oldSlot);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 占用新车位</span></span><br><span class="line"><span class="type">ParkSlotEntity</span> <span class="variable">newSlot</span> <span class="operator">=</span> parkSlotService.getById(entity.getAfterValueId());</span><br><span class="line"><span class="keyword">if</span>(newSlot != <span class="literal">null</span>)&#123;</span><br><span class="line">    newSlot.setUsingState(<span class="string">&quot;4&quot;</span>);</span><br><span class="line">    newSlot.setMonthCar(monthPersonal.getCarNumber());</span><br><span class="line">    newSlot.setParkingCar(monthPersonal.getCarNumber());</span><br><span class="line">    parkSlotService.updateById(newSlot);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 更新月卡绑定的车位</span></span><br><span class="line">monthPersonal.setParkingSpotId(entity.getAfterValueId());</span><br></pre></td></tr></table></figure>

<h3 id="经验总结-2"><a href="#经验总结-2" class="headerlink" title="经验总结"></a>经验总结</h3><ul>
<li>车位状态检查必须区分”当前占用车位”和”要变更到的新车位”</li>
<li>DTO 设计需覆盖业务操作所需的全部参数</li>
<li>变更操作需要考虑完整的状态流转：提交 → 审批通过&#x2F;拒绝</li>
</ul>
<hr>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>本次排查的三个 Bug 覆盖了不同的根因类型：</p>
<table>
<thead>
<tr>
<th>Bug</th>
<th>类型</th>
<th>根因</th>
</tr>
</thead>
<tbody><tr>
<td>个人月卡拒绝 404</td>
<td>前端</td>
<td>接口路径不匹配</td>
</tr>
<tr>
<td>月卡变更审批拒绝 404</td>
<td>后端</td>
<td>空指针异常（NPE）</td>
</tr>
<tr>
<td>车位占用校验失效</td>
<td>业务逻辑</td>
<td>检查对象错误（老车位 vs 新车位）</td>
</tr>
</tbody></table>
<h3 id="通用经验"><a href="#通用经验" class="headerlink" title="通用经验"></a>通用经验</h3><ol>
<li><strong>前后端接口一致性</strong>：接口命名、参数结构必须保持一致，建议通过 Swagger 文档同步</li>
<li><strong>空值检查</strong>：数据库查询后必须进行空值检查，避免 NPE</li>
<li><strong>业务逻辑验证</strong>：校验逻辑必须验证正确的对象，不能混淆”当前状态”和”目标状态”</li>
<li><strong>测试覆盖</strong>：必须覆盖完整的业务流程，包括”通过”和”拒绝”两种路径</li>
</ol>
<hr>
<blockquote>
<p>💡 <strong>提示</strong>：月卡管理模块的问题往往涉及状态流转，建议在开发变更功能时同步梳理状态机，避免遗漏边界情况。</p>
</blockquote>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>南沙物联网</category>
      </categories>
      <tags>
        <tag>404</tag>
        <tag>Bug</tag>
        <tag>费用管理</tag>
        <tag>个人月卡</tag>
        <tag>月卡变更</tag>
      </tags>
  </entry>
  <entry>
    <title>O(N×M) 拍平成 O(N+M)：反查表如何把楼层摄像头计数从慢路径救回来</title>
    <url>/2026/07/17/%E5%8F%8D%E6%9F%A5%E8%A1%A8%E6%8A%8A%E6%A5%BC%E5%B1%82%E6%91%84%E5%83%8F%E5%A4%B4%E6%95%B0%E6%8B%8D%E5%B9%B3/</url>
    <content><![CDATA[<p>「勾这个楼层到底覆盖几个摄像头？」——超管在配置页的盲勾体验必须解决。盲勾的体验很差：要么勾多了（放出不该放的楼层），要么勾少了（后面发现某楼层其实没摄像头，白勾）。</p>
<p>本文把”在 DTO 加一个字段这件事”拆开讲：为什么必须用反查表、为什么计数口径要和 <code>getCameraTree</code> 对齐、为什么保序用 <code>LinkedHashMap</code>、为什么 <code>toMap</code> 要带 merge 函数。前端删 Mock 零计算渲染，后端单测 6&#x2F;6 全绿。</p>
<span id="more"></span>

<div class="audio-player">
  <div class="audio-section">
    <h4>🎧 文章导读</h4>
    <audio controls preload="metadata">
      <source src="/audio/反查表把楼层摄像头数拍平/narration.mp3" type="audio/mpeg">
    </audio>
  </div>
  <div class="audio-section">
    <h4>🎵 背景音乐</h4>
    <audio controls loop preload="metadata">
      <source src="/audio/反查表把楼层摄像头数拍平/bgm.mp3" type="audio/mpeg">
    </audio>
  </div>
</div>

<p><img data-src="/images/posts/%E5%8F%8D%E6%9F%A5%E8%A1%A8%E6%8A%8A%E6%A5%BC%E5%B1%82%E6%91%84%E5%83%8F%E5%A4%B4%E6%95%B0%E6%8B%8D%E5%B9%B3/fig1.png" alt="反查表算法三步把摄像头按楼层归堆"></p>
<p><em>图1：建反查表 → 按 spaceId 查楼层 → 按楼层 merge 计数，三步完成统计</em></p>
<h2 id="一、需求背景：两层链路中的”展示性增量”"><a href="#一、需求背景：两层链路中的”展示性增量”" class="headerlink" title="一、需求背景：两层链路中的”展示性增量”"></a>一、需求背景：两层链路中的”展示性增量”</h2><p>在拆本次增量之前，先说清整体链路的两层，否则单看本文会摸不着头脑。</p>
<h3 id="1-1-第一层（已合入）：给超管做公司默认可见楼层配置"><a href="#1-1-第一层（已合入）：给超管做公司默认可见楼层配置" class="headerlink" title="1.1 第一层（已合入）：给超管做公司默认可见楼层配置"></a>1.1 第一层（已合入）：给超管做公司默认可见楼层配置</h3><p>上一提交 <code>9c7e71ac6</code> 给 super-admin 提供了公司默认可见楼层的配置能力：</p>
<ul>
<li>新增 <code>GET /cameraInfo/orgFloorConfig/&#123;orgId&#125;</code> 查某公司当前园区已勾选的可见楼层；</li>
<li>新增 <code>POST /cameraInfo/orgFloorConfig</code> 整体保存（空数组即清空，仅超管可操作）；</li>
<li>落库表 <code>security_camera_org_floor</code> 加了唯一约束 <code>(park_id, org_id, floor_space_id)</code>；</li>
<li>保存时先硬删再批插，逻辑干脆。</li>
</ul>
<p>这套配置是 <code>getCameraTree</code> 可见性过滤的<strong>数据源</strong>：非超管只能看到所属公司配置楼层下的摄像头，或已审批通过的摄像头编号。</p>
<h3 id="1-2-第二层（本次未提交的增量）：配置页显示「勾这个楼层到底几个摄像头」"><a href="#1-2-第二层（本次未提交的增量）：配置页显示「勾这个楼层到底几个摄像头」" class="headerlink" title="1.2 第二层（本次未提交的增量）：配置页显示「勾这个楼层到底几个摄像头」"></a>1.2 第二层（本次未提交的增量）：配置页显示「勾这个楼层到底几个摄像头」</h3><p>配置页面上，超管面对一串楼层勾选框，却不知道「勾下这一栋的 3 楼，到底能看到几个摄像头」。所以本次给配置查询的返回值加了一个 <code>floorCameraCounts</code> 字段，告诉前端每个被勾选楼层下有多少摄像头。</p>
<p>前端要做的事很简单：把三处 Mock 数据（园区、ABC 栋、随机数量）删掉，改为调真实的空间树接口拉当前园区层级，再调配置查询接口回显已勾选楼层与每楼层摄像头数、调保存接口提交。</p>
<blockquote>
<p>增量虽小，决策不少。下面把”为什么这样做”的三个关键决策讲清楚。</p>
</blockquote>
<h2 id="二、关键决策-1：计数口径必须和-getCameraTree-对齐"><a href="#二、关键决策-1：计数口径必须和-getCameraTree-对齐" class="headerlink" title="二、关键决策 1：计数口径必须和 getCameraTree 对齐"></a>二、关键决策 1：计数口径必须和 getCameraTree 对齐</h2><h3 id="2-1-这是整件事的立身之本"><a href="#2-1-这是整件事的立身之本" class="headerlink" title="2.1 这是整件事的立身之本"></a>2.1 这是整件事的立身之本</h3><p><code>getCameraTree</code> 判可见是<strong>按楼层放行整个楼层子树</strong>——某个楼层被勾选，它下面挂的所有节点（包括房间）里的摄像头全部可见。</p>
<p>那么配置页上「该楼层 N 个摄像头」的 N，就必须是<strong>这个楼层子树下的全部摄像头数</strong>，否则前端显示的数字和用户实际能看到的数量对不上，超管会被误导。</p>
<p><img data-src="/images/posts/%E5%8F%8D%E6%9F%A5%E8%A1%A8%E6%8A%8A%E6%A5%BC%E5%B1%82%E6%91%84%E5%83%8F%E5%A4%B4%E6%95%B0%E6%8B%8D%E5%B9%B3/fig2.png" alt="getCameraTree 与 floorCameraCounts 的口径对齐"></p>
<p><em>图2：左侧 getCameraTree 按楼层放行子树，右侧 floorCameraCounts 必须统计相同子树</em></p>
<h3 id="2-2-空间模型"><a href="#2-2-空间模型" class="headerlink" title="2.2 空间模型"></a>2.2 空间模型</h3><p>摄像头可能直接挂在「楼层」节点上，也可能挂在楼层下的「房间」节点上；而房间节点带 <code>floorId</code> 指回所属楼层。</p>
<p>所以：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">「楼层子树的摄像头」</span><br><span class="line">  = 直接挂该楼层的摄像头</span><br><span class="line">  + 挂在该楼层下任意房间的摄像头</span><br></pre></td></tr></table></figure>

<p>这个口径一旦想清楚，算法的骨架就有了：<strong>把任意 spaceId 折算成所属楼层 id，再把摄像头按楼层归堆计数</strong>。</p>
<h2 id="三、关键决策-2：用反查表而不是嵌套遍历"><a href="#三、关键决策-2：用反查表而不是嵌套遍历" class="headerlink" title="三、关键决策 2：用反查表而不是嵌套遍历"></a>三、关键决策 2：用反查表而不是嵌套遍历</h2><h3 id="3-1-最朴素的写法"><a href="#3-1-最朴素的写法" class="headerlink" title="3.1 最朴素的写法"></a>3.1 最朴素的写法</h3><p>对每个摄像头，遍历空间树找它属于哪个被勾选的楼层：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// O(N×M)：N 个摄像头 × M 个被勾选楼层</span></span><br><span class="line"><span class="keyword">for</span> (Camera camera : cameras) &#123;</span><br><span class="line">    <span class="keyword">for</span> (String floorId : floorSpaceIds) &#123;</span><br><span class="line">        <span class="keyword">if</span> (isInFloorSubtree(camera.getSpaceId(), floorId)) &#123;</span><br><span class="line">            counts.merge(floorId, <span class="number">1</span>, Integer::sum);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>空间数和摄像头数一旦都上百，就是 O(N×M) 的嵌套扫描，园区规模一大就慢。</p>
<h3 id="3-2-折中方案：先建反查表"><a href="#3-2-折中方案：先建反查表" class="headerlink" title="3.2 折中方案：先建反查表"></a>3.2 折中方案：先建反查表</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 反查表：spaceId → 所属楼层 id</span></span><br><span class="line">Map&lt;String, String&gt; floorBySpace = ...;</span><br><span class="line"><span class="comment">// 每个摄像头按 floorBySpace 一次查到楼层 id</span></span><br><span class="line"><span class="keyword">for</span> (Camera camera : cameras) &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">floorId</span> <span class="operator">=</span> floorBySpace.get(camera.getSpaceId());</span><br><span class="line">    <span class="keyword">if</span> (counts.containsKey(floorId)) &#123;</span><br><span class="line">        counts.merge(floorId, <span class="number">1</span>, Integer::sum);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>建表 O(空间数)，归堆 O(摄像头数)，**总复杂度 O(空间数+摄像头数)**，一次扫完。</p>
<blockquote>
<p>反查表本质上就是把嵌套的查找拍平成一次 Map 预处理。<strong>这不是什么高深技巧，但在业务代码里最常被忽略</strong>。</p>
</blockquote>
<h2 id="四、关键决策-3：LinkedHashMap-merge-函数"><a href="#四、关键决策-3：LinkedHashMap-merge-函数" class="headerlink" title="四、关键决策 3：LinkedHashMap + merge 函数"></a>四、关键决策 3：LinkedHashMap + merge 函数</h2><h3 id="4-1-为什么用-LinkedHashMap"><a href="#4-1-为什么用-LinkedHashMap" class="headerlink" title="4.1 为什么用 LinkedHashMap"></a>4.1 为什么用 LinkedHashMap</h3><p>前端展示楼层列表要按 <code>floorSpaceIds</code>（被勾选楼层）传入的顺序渲染，不能让 <code>HashMap</code> 的哈希序打乱。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Map&lt;String, Integer&gt; counts = floorSpaceIds.stream()</span><br><span class="line">        .collect(Collectors.toMap(id -&gt; id, id -&gt; <span class="number">0</span>, (l, r) -&gt; l, LinkedHashMap::<span class="keyword">new</span>));</span><br></pre></td></tr></table></figure>

<p>最后一个参数 <code>LinkedHashMap::new</code> 指定底层使用 <code>LinkedHashMap</code>，保证遍历顺序和 <code>floorSpaceIds</code> 一致。</p>
<h3 id="4-2-为什么-toMap-要带-merge-函数"><a href="#4-2-为什么-toMap-要带-merge-函数" class="headerlink" title="4.2 为什么 toMap 要带 merge 函数"></a>4.2 为什么 toMap 要带 merge 函数</h3><p>空间数据里如果出现重复 id（理论上不该、实际上可能），<code>Collectors.toMap</code> 默认会抛 merge 异常直接炸接口：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">java.lang.IllegalStateException: Duplicate key xxx</span><br></pre></td></tr></table></figure>

<p>取 <code>(l, r) -&gt; l</code>（保留先出现的值），容错优先于严格：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">.collect(Collectors.toMap(</span><br><span class="line">    SpaceInfoDTO::getId,</span><br><span class="line">    space -&gt; ...,</span><br><span class="line">    (l, r) -&gt; l    <span class="comment">// 脏数据兜底</span></span><br><span class="line">));</span><br></pre></td></tr></table></figure>

<blockquote>
<p>两个小但关键的细节：<strong>LinkedHashMap 保序，merge 函数容错</strong>。少一个，线上就要炸。</p>
</blockquote>
<h2 id="五、核心算法-countCamerasByFloor"><a href="#五、核心算法-countCamerasByFloor" class="headerlink" title="五、核心算法 countCamerasByFloor"></a>五、核心算法 countCamerasByFloor</h2><h3 id="5-1-第一步：每个被勾选楼层先初始化为-0"><a href="#5-1-第一步：每个被勾选楼层先初始化为-0" class="headerlink" title="5.1 第一步：每个被勾选楼层先初始化为 0"></a>5.1 第一步：每个被勾选楼层先初始化为 0</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Map&lt;String, Integer&gt; counts = floorSpaceIds.stream()</span><br><span class="line">        .collect(Collectors.toMap(id -&gt; id, id -&gt; <span class="number">0</span>, (l, r) -&gt; l, LinkedHashMap::<span class="keyword">new</span>));</span><br></pre></td></tr></table></figure>

<p>注意初始化用的是<strong>被勾选楼层列表</strong>（<code>floorSpaceIds</code>），而不是全园区所有楼层——没被勾选的楼层根本不会出现在结果里，这是「只统计被勾选楼层」的第一道关卡。</p>
<h3 id="5-2-第二步：建立-spaceId-到所属楼层-id-的反查表"><a href="#5-2-第二步：建立-spaceId-到所属楼层-id-的反查表" class="headerlink" title="5.2 第二步：建立 spaceId 到所属楼层 id 的反查表"></a>5.2 第二步：建立 spaceId 到所属楼层 id 的反查表</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Map&lt;String, String&gt; floorBySpace = spaces.stream().collect(Collectors.toMap(</span><br><span class="line">        SpaceInfoDTO::getId,</span><br><span class="line">        space -&gt; DictConstants.SPACE_LEVEL_ROOM.equals(space.getSpaceHierarchy())</span><br><span class="line">                ? StringUtils.defaultString(space.getFloorId())   <span class="comment">// 房间 -&gt; 取其 floorId</span></span><br><span class="line">                : DictConstants.SPACE_LEVEL_FLOOR.equals(space.getSpaceHierarchy())</span><br><span class="line">                    ? space.getId()                                <span class="comment">// 楼层 -&gt; 自身</span></span><br><span class="line">                    : StringUtils.EMPTY,                           <span class="comment">// 其它层级 -&gt; 忽略</span></span><br><span class="line">        (l, r) -&gt; l));</span><br></pre></td></tr></table></figure>

<p>这段三元嵌套是算法的实质：</p>
<table>
<thead>
<tr>
<th>节点类型</th>
<th>折算结果</th>
</tr>
</thead>
<tbody><tr>
<td>房间</td>
<td>取其 <code>floorId</code> 指向的楼层</td>
</tr>
<tr>
<td>楼层</td>
<td>自身 id</td>
</tr>
<tr>
<td>其它层级（楼栋、园区等）</td>
<td>空串忽略</td>
</tr>
</tbody></table>
<p><code>StringUtils.defaultString</code> 是防空：万一某个房间的 <code>floorId</code> 是 null，兜底成空串，后面 <code>counts::containsKey</code> 自然过滤掉。</p>
<h3 id="5-3-第三步：每个摄像头按-spaceId-归到楼层，只统计被勾选的"><a href="#5-3-第三步：每个摄像头按-spaceId-归到楼层，只统计被勾选的" class="headerlink" title="5.3 第三步：每个摄像头按 spaceId 归到楼层，只统计被勾选的"></a>5.3 第三步：每个摄像头按 spaceId 归到楼层，只统计被勾选的</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">cameras.stream()</span><br><span class="line">        .map(c -&gt; floorBySpace.get(c.getSpaceId()))</span><br><span class="line">        .filter(counts::containsKey)</span><br><span class="line">        .forEach(floorId -&gt; counts.merge(floorId, <span class="number">1</span>, Integer::sum));</span><br></pre></td></tr></table></figure>

<p><code>filter(counts::containsKey)</code> 是<strong>第二道关卡</strong>：园区里摄像头可能挂在非楼层&#x2F;非房间（比如挂在楼栋层、或挂在未被该公司勾选的楼层），这些 <code>floorBySpace</code> 折算出来的 id 不在 <code>counts</code> 的 key 集合里，一律不计。</p>
<blockquote>
<p><strong>两道关卡叠加，结果就精确等于「被勾选楼层子树下的摄像头数」</strong>。</p>
</blockquote>
<h2 id="六、getOrgFloorConfig-的改动"><a href="#六、getOrgFloorConfig-的改动" class="headerlink" title="六、getOrgFloorConfig 的改动"></a>六、getOrgFloorConfig 的改动</h2><h3 id="6-1-改动点"><a href="#6-1-改动点" class="headerlink" title="6.1 改动点"></a>6.1 改动点</h3><p>原来 <code>getOrgFloorConfig</code> 拿到 <code>floorSpaceIds</code> 直接塞进 result 就返回，逻辑很轻。现在要在返回前多干两件事：</p>
<ol>
<li>拉本园区全部空间（<code>spaceInfoClient.list</code>）；</li>
<li>拉本园区全部摄像头（<code>listByCondition(cameraQuery)</code>，按 parkId 过滤）；</li>
<li>然后交给 <code>countCamerasByFloor</code> 算计数填进 <code>floorCameraCounts</code>。</li>
</ol>
<p>改动只动了 <code>CameraInfoServiceImpl.java</code> 里这一个方法，以及 DTO 加字段、单测加用例，<strong>三处收敛在同一模块</strong>，不涉及跨服务。</p>
<h3 id="6-2-为什么「全量拉」而不是「按楼层过滤拉」"><a href="#6-2-为什么「全量拉」而不是「按楼层过滤拉」" class="headerlink" title="6.2 为什么「全量拉」而不是「按楼层过滤拉」"></a>6.2 为什么「全量拉」而不是「按楼层过滤拉」</h3><blockquote>
<p>[!tip] 性能 vs 可读性的权衡<br>全量拉空间和摄像头看起来粗放，但这是配置页低频接口、数据量也就一个园区的规模。按楼层过滤反而要为每个勾选楼层发一次查询，N 次 IO 远比一次全量拉慢。用反查表把后续计算压成 O(N+M)，整体反而更快。配置接口不是性能热点，<strong>可读性优先</strong>。</p>
</blockquote>
<h2 id="七、前端接入：零计算"><a href="#七、前端接入：零计算" class="headerlink" title="七、前端接入：零计算"></a>七、前端接入：零计算</h2><p>前端在另一个仓库（文件路径以实际仓库为准），本次改动两个文件：</p>
<ol>
<li><strong>摄像头楼层配置页面组件</strong>：删掉了「园区、ABC 栋、随机数量」三处 Mock 数据，改为调用真实的空间树接口拉当前园区空间层级，再调配置查询接口回显已勾选楼层与每楼层摄像头数、调保存接口提交。</li>
<li><strong>API 定义文件</strong>：新增 <code>orgFloorConfig</code> 的 GET&#x2F;POST 封装，对应后端两个接口。</li>
</ol>
<blockquote>
<p>前端的职责被刻意压到最轻——它只负责拉数据、渲染勾选框和「该楼层 N 个摄像头」的文案、提交勾选结果，<strong>零计算</strong>。所有计数逻辑都在后端算好，前端拿到 <code>floorCameraCounts</code> 直接按 key 取值渲染。</p>
</blockquote>
<blockquote>
<p>这和”计数口径要和 getCameraTree 对齐”是配套的：<strong>计数是后端的职责，前端不该重复实现一套可能跑偏的口径</strong>。</p>
</blockquote>
<h3 id="7-1-前端验证"><a href="#7-1-前端验证" class="headerlink" title="7.1 前端验证"></a>7.1 前端验证</h3><ul>
<li>ESLint：0 error；</li>
<li>生产构建成功，30 条 warning 均为项目既有问题，与本次改动无关。</li>
</ul>
<h2 id="八、DTO-改动：只加一个字段"><a href="#八、DTO-改动：只加一个字段" class="headerlink" title="八、DTO 改动：只加一个字段"></a>八、DTO 改动：只加一个字段</h2><p><code>CameraOrgFloorConfigDTO</code> 只加了一个字段，key 是楼层 spaceId、value 是摄像头数：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> Map&lt;String, Integer&gt; floorCameraCounts;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>字段为什么不复杂化、为什么不单独建一个 DTO？因为它就是「配置查询的附加信息」，和 <code>floorSpaceIds</code>（已勾选楼层列表）<strong>同生同灭</strong>，没必要拆。这是 YAGNI——单用途的附加字段，塞进现有 DTO 最省事。</p>
</blockquote>
<h2 id="九、测试与验证"><a href="#九、测试与验证" class="headerlink" title="九、测试与验证"></a>九、测试与验证</h2><p>后端单测 <code>CameraInfoServiceImplTest.java</code> 新增了 <code>cameraCountsIncludeCamerasBoundToRooms</code> 用例，专门验证「直接挂楼层的摄像头 + 挂楼层下房间的摄像头都计入该楼层」这个口径：</p>
<ul>
<li>floor-1 上有一个直接挂楼层的摄像头 + 一个挂 room-1 的摄像头（room-1 的 <code>floorId</code> 指向 floor-1）→ floor-1 计 <strong>2</strong>；</li>
<li>floor-2 无摄像头 → <strong>0</strong>。</li>
</ul>
<p>加上原有用例，后端单测 <strong>6&#x2F;6 全绿</strong>。</p>
<blockquote>
<p>这个用例的价值在于它锁住了口径——如果以后有人改 <code>countCamerasByFloor</code> 时忘了把房间下的摄像头归到楼层，这个用例会立刻红。</p>
</blockquote>
<h2 id="十、经验总结"><a href="#十、经验总结" class="headerlink" title="十、经验总结"></a>十、经验总结</h2><h3 id="10-1-两条可复用的经验"><a href="#10-1-两条可复用的经验" class="headerlink" title="10.1 两条可复用的经验"></a>10.1 两条可复用的经验</h3><blockquote>
<p>[!tip] 经验 1：附加统计字段要对齐主链路口径<br>本次 <code>floorCameraCounts</code> 必须和 <code>getCameraTree</code> 的可见性判断用同一个口径（楼层子树全计），否则前端显示数和实际可见数对不上。<strong>任何「给现有功能加展示性统计」的需求，第一步都是先确认主链路的统计口径，再让附加字段对齐它</strong>。</p>
</blockquote>
<blockquote>
<p>[!tip] 经验 2：嵌套查找先建反查表<br>O(N×M) 的嵌套遍历，几乎总能用一次 Map 预处理拍平成 O(N+M)。这不是什么高深技巧，但在业务代码里最常被忽略——<strong>看到双层循环找归属关系，第一反应就该是「能不能先建个 Map」</strong>。</p>
</blockquote>
<h3 id="10-2-为什么不过度设计"><a href="#10-2-为什么不过度设计" class="headerlink" title="10.2 为什么不过度设计"></a>10.2 为什么不过度设计</h3><p>这次刻意没做这些事：</p>
<table>
<thead>
<tr>
<th>没做的事</th>
<th>原因</th>
</tr>
</thead>
<tbody><tr>
<td>给 <code>countCamerasByFloor</code> 抽接口</td>
<td>只有一个实现、一个调用方，抽了就是死灵活性</td>
</tr>
<tr>
<td>给 <code>floorCameraCounts</code> 做分页或懒加载</td>
<td>配置页一次性渲染、数据量就是一栋楼的层级</td>
</tr>
<tr>
<td>在 DTO 上加 <code>totalCameraCount</code> 汇总字段</td>
<td>前端要汇总自己 reduce 一下就行，后端不该提供没人要求的字段</td>
</tr>
</tbody></table>
<p>每一行改动都能直接追溯到「让超管看到每楼层摄像头数」这一个需求。</p>
<h3 id="10-3-反查表适用的-4-个场景"><a href="#10-3-反查表适用的-4-个场景" class="headerlink" title="10.3 反查表适用的 4 个场景"></a>10.3 反查表适用的 4 个场景</h3><ol>
<li><strong>空间归属</strong>：摄像头属于哪个楼层、设备属于哪个房间——本质都是 spaceId → 父级 id 的映射。</li>
<li><strong>组织归属</strong>：用户属于哪个组织、订单属于哪个事业部——userId&#x2F;orderId → orgId。</li>
<li><strong>标签归属</strong>：商品属于哪些类目、文章属于哪些话题——商品&#x2F;文章 id → 标签集合。</li>
<li><strong>租户归属</strong>：资源属于哪个租户——resourceId → tenantId。</li>
</ol>
<p>判断标准很简单：<strong>只要出现”两个集合，每个元素要找它在另一个集合里的归属”的双层循环，就该建反查表</strong>。</p>
<h3 id="10-4-算法选型的”够用就好”原则"><a href="#10-4-算法选型的”够用就好”原则" class="headerlink" title="10.4 算法选型的”够用就好”原则"></a>10.4 算法选型的”够用就好”原则</h3><p>不要追求”理论最优”，要追求”业务场景下的最优”。本次的全量拉 + O(N+M) 计算，在园区规模下完全够用：</p>
<table>
<thead>
<tr>
<th>数据规模</th>
<th>性能</th>
</tr>
</thead>
<tbody><tr>
<td>空间数 &lt; 1000，摄像头数 &lt; 1000</td>
<td>单次查询 &lt; 50ms</td>
</tr>
<tr>
<td>空间数 &lt; 5000，摄像头数 &lt; 5000</td>
<td>单次查询 &lt; 200ms</td>
</tr>
<tr>
<td>空间数 &gt; 10000</td>
<td>考虑分园区拉或异步预热</td>
</tr>
</tbody></table>
<p>配置接口不是性能热点，<strong>可读性优先于理论最优</strong>。</p>
<h2 id="十一、结语"><a href="#十一、结语" class="headerlink" title="十一、结语"></a>十一、结语</h2><p>「让超管看到每楼层摄像头数」这个需求，看起来只是「在 DTO 加一个字段」，背后却是三个决策：计数口径和主链路对齐、O(N+M) 替代 O(N×M)、保序和容错两个细节。</p>
<p>下次再遇到”展示性统计字段”的需求，按这个顺序思考：</p>
<ol>
<li><strong>口径和谁对齐？</strong> 找到主链路的统计口径，让新字段对齐它。</li>
<li><strong>有没有嵌套查找？</strong> 有就先建反查表。</li>
<li><strong>需要保序吗？</strong> 需要就用 <code>LinkedHashMap</code>。</li>
<li><strong>数据可能有脏吗？</strong> 有就给 <code>toMap</code> 加 merge 函数。</li>
</ol>
<blockquote>
<p>算法优化的最高境界不是”我想到了牛逼的算法”，而是”我把嵌套循环换成了一次 Map 预处理”。</p>
</blockquote>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>南网</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>南网项目</tag>
        <tag>算法</tag>
        <tag>摄像头</tag>
        <tag>反查表</tag>
      </tags>
  </entry>
  <entry>
    <title>后端 API 优化实战：字段补充与结果排序</title>
    <url>/2026/03/05/%E5%90%8E%E7%AB%AFAPI%E4%BC%98%E5%8C%96%E5%AE%9E%E6%88%98-%E5%AD%97%E6%AE%B5%E8%A1%A5%E5%85%85%E4%B8%8E%E7%BB%93%E6%9E%9C%E6%8E%92%E5%BA%8F/</url>
    <content><![CDATA[<p>在实际开发中，接口返回数据不一致、缺少关键字段、返回结果无序等问题经常困扰前端同学。本文记录了两个典型的后端 API 优化场景：为接口补充友好字段、为统计数据添加排序，分享问题定位思路和最小侵入式修改方案。</p>
<span id="more"></span>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>在一次需求开发中，前端反馈了两个问题：一是水情汇总接口返回的测站类型是代码（如 <code>ZQ</code>），需要额外查询转换；二是管理单位统计接口返回的数据顺序是随机的，展示效果不佳。这两个问题都属于”不影响功能但影响体验”的类型，本文记录解决过程。</p>
<h2 id="场景一：为接口新增友好字段"><a href="#场景一：为接口新增友好字段" class="headerlink" title="场景一：为接口新增友好字段"></a>场景一：为接口新增友好字段</h2><h3 id="问题背景"><a href="#问题背景" class="headerlink" title="问题背景"></a>问题背景</h3><p><code>/api/water/summary/list</code> 接口返回的 <code>STTP</code>（测站类型代码）是英文缩写，比如 <code>ZQ</code> 代表水文站、<code>ZZ</code> 代表水位站。前端展示时需要额外维护一套映射关系，将代码转换为中文名称。</p>
<p>而同一个项目中，<code>/basic/b/list</code> 接口已经返回了 <code>stationTypeName</code> 字段，直接提供中文名称。两处实现不一致，增加了前端的维护成本。</p>
<h3 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h3><p>参照 <code>/basic/b/list</code> 的实现，为 <code>summary/list</code> 接口新增 <code>stationTypeLabel</code> 字段，复用现有的 <code>MappingService.getStationTypeMap()</code> 获取测站类型映射。</p>
<h3 id="实现步骤"><a href="#实现步骤" class="headerlink" title="实现步骤"></a>实现步骤</h3><h4 id="1-DTO-新增字段"><a href="#1-DTO-新增字段" class="headerlink" title="1. DTO 新增字段"></a>1. DTO 新增字段</h4><p>在响应 DTO 中添加新字段：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// WaterSummaryListResponseDTO.java</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@Schema(description = &quot;测站类型名称（中文），根据STTP代码转换的中文名称&quot;, example = &quot;水文站&quot;)</span></span><br><span class="line"><span class="keyword">private</span> String stationTypeLabel;</span><br></pre></td></tr></table></figure>

<h4 id="2-Controller-注入-MappingService"><a href="#2-Controller-注入-MappingService" class="headerlink" title="2. Controller 注入 MappingService"></a>2. Controller 注入 MappingService</h4><p>复用现有的映射服务，避免重复造轮子：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// WaterSummaryController.java</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@Autowired</span></span><br><span class="line"><span class="keyword">private</span> MappingService mappingService;</span><br></pre></td></tr></table></figure>

<h4 id="3-数据转换时填充字段"><a href="#3-数据转换时填充字段" class="headerlink" title="3. 数据转换时填充字段"></a>3. 数据转换时填充字段</h4><p>在 DTO 转换逻辑中填充新字段，注意批量获取映射避免 N+1 问题：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 批量转换时获取测站类型映射</span></span><br><span class="line">Map&lt;String, String&gt; stationTypeMap = mappingService.getStationTypeMap();</span><br><span class="line"></span><br><span class="line"><span class="comment">// 单个转换时填充 stationTypeLabel</span></span><br><span class="line"><span class="type">String</span> <span class="variable">sttp</span> <span class="operator">=</span> summary.getSTTP();</span><br><span class="line"><span class="keyword">if</span> (StringUtils.isNotEmpty(sttp) &amp;&amp; stationTypeMap != <span class="literal">null</span>) &#123;</span><br><span class="line">    dto.setStationTypeLabel(stationTypeMap.getOrDefault(sttp, <span class="string">&quot;&quot;</span>));</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    dto.setStationTypeLabel(<span class="string">&quot;&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="返回示例"><a href="#返回示例" class="headerlink" title="返回示例"></a>返回示例</h3><p>优化后的接口返回：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;STCD&quot;</span><span class="punctuation">:</span> <span class="string">&quot;62906600&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;STNM&quot;</span><span class="punctuation">:</span> <span class="string">&quot;加田水文站&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;STTP&quot;</span><span class="punctuation">:</span> <span class="string">&quot;ZQ&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;stationTypeLabel&quot;</span><span class="punctuation">:</span> <span class="string">&quot;水文站&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;LGTD&quot;</span><span class="punctuation">:</span> <span class="number">113.123456</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;LTTD&quot;</span><span class="punctuation">:</span> <span class="number">23.456789</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<hr>
<h2 id="场景二：统计接口结果排序"><a href="#场景二：统计接口结果排序" class="headerlink" title="场景二：统计接口结果排序"></a>场景二：统计接口结果排序</h2><h3 id="问题背景-1"><a href="#问题背景-1" class="headerlink" title="问题背景"></a>问题背景</h3><p><code>/basic/b/statistics?staticType=unit</code> 接口返回各机构的测站统计数据，但 <code>data</code> 数组中的顺序是随机的。前端展示时希望按测站总数（<code>total</code>）从大到小排序，让数据量大的机构排在前面。</p>
<h3 id="解决方案-1"><a href="#解决方案-1" class="headerlink" title="解决方案"></a>解决方案</h3><p>在业务层返回数据前添加排序逻辑，保持对原有代码的最小侵入。</p>
<h3 id="实现步骤-1"><a href="#实现步骤-1" class="headerlink" title="实现步骤"></a>实现步骤</h3><p>在 <code>StStbprpUnitStatisticsBiz</code> 的两个统计方法中，于 <code>return dataList</code> 前添加一行排序代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// StStbprpUnitStatisticsBiz.java</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 按 total 倒序排序</span></span><br><span class="line">dataList.sort((a, b) -&gt; Integer.compare((Integer) b.get(<span class="string">&quot;total&quot;</span>), (Integer) a.get(<span class="string">&quot;total&quot;</span>)));</span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span> dataList;</span><br></pre></td></tr></table></figure>

<p>需要修改的方法：</p>
<ul>
<li><code>statisticsForSecondLevel()</code></li>
<li><code>statisticsForThirdLevel()</code></li>
</ul>
<hr>
<h2 id="影响分析与注意事项"><a href="#影响分析与注意事项" class="headerlink" title="影响分析与注意事项"></a>影响分析与注意事项</h2><h3 id="字段新增场景"><a href="#字段新增场景" class="headerlink" title="字段新增场景"></a>字段新增场景</h3><table>
<thead>
<tr>
<th>检查项</th>
<th>状态</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>DTO 使用范围</td>
<td>✅ 安全</td>
<td>DTO 仅在 Controller 内部使用</td>
</tr>
<tr>
<td>方法签名变更</td>
<td>✅ 安全</td>
<td>转换方法是 <code>private</code>，无外部调用</td>
</tr>
<tr>
<td>测试文件</td>
<td>✅ 无需修改</td>
<td>现有测试是集成测试</td>
</tr>
<tr>
<td>API 兼容性</td>
<td>✅ 向后兼容</td>
<td>新增字段不影响现有前端</td>
</tr>
</tbody></table>
<p><strong>注意事项</strong>：如果使用了缓存（如 <code>cache=1</code>），旧缓存数据不包含新字段，反序列化后为 <code>null</code>。可以等待缓存过期（30 天），或手动清除 Redis 缓存：<code>KEYS water:summary:*</code></p>
<h3 id="结果排序场景"><a href="#结果排序场景" class="headerlink" title="结果排序场景"></a>结果排序场景</h3><table>
<thead>
<tr>
<th>检查项</th>
<th>状态</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>业务逻辑</td>
<td>✅ 无侵入</td>
<td>只在返回前添加排序</td>
</tr>
<tr>
<td>分页功能</td>
<td>✅ 无影响</td>
<td>该接口无分页</td>
</tr>
<tr>
<td>API 兼容性</td>
<td>✅ 向后兼容</td>
<td>数据结构不变，只改变顺序</td>
</tr>
</tbody></table>
<hr>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><p>本次优化涉及两个小改动，但体现了几个重要的开发原则：</p>
<ul>
<li><strong>复用现有服务</strong>：<code>MappingService</code> 已经提供了类型映射，直接复用避免重复维护</li>
<li><strong>保持一致性</strong>：不同接口的返回结构应保持一致，减少前端适配成本</li>
<li><strong>最小侵入修改</strong>：排序逻辑只在返回前添加，不侵入核心业务流程</li>
<li><strong>批量获取映射</strong>：避免在循环中单独查询，防止 N+1 问题</li>
<li><strong>向后兼容</strong>：新增字段不破坏现有功能，排序不改数据结构</li>
</ul>
<p>这些”小优化”看似不起眼，但积累起来能显著提升 API 的易用性和前端开发体验。</p>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>后端开发不仅是实现功能，还要关注 API 的友好性和一致性。希望这两个小案例能给你带来一些启发，在实际项目中遇到类似问题时，可以快速定位并优雅解决。</p>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>水文</category>
      </categories>
      <tags>
        <tag>踩坑记录</tag>
        <tag>Spring Boot</tag>
        <tag>API 优化</tag>
      </tags>
  </entry>
  <entry>
    <title>园区切换不生效？Redis DB 不一致这个坑我替你踩过了</title>
    <url>/2026/07/07/%E5%9B%AD%E5%8C%BA%E5%88%87%E6%8D%A2%E4%B8%8D%E7%94%9F%E6%95%88-RedisDB%E4%B8%8D%E4%B8%80%E8%87%B4/</url>
    <content><![CDATA[<p>切换园区之后 <code>/admin/user/info</code> 还是旧园区，访客预约列表也没刷新。代码没改业务逻辑，问题却在「写 Redis 的那个地方」。本文把这次完整的排查、定位、修复和验证链路写下来，下次再遇到类似的”切换不生效”问题，你大概率可以一眼定位。</p>
<span id="more"></span>

<div class="audio-player">
  <div class="audio-section">
    <h4>🎧 文章导读</h4>
    <audio controls preload="metadata">
      <source src="/audio/园区切换不生效-RedisDB不一致/narration.mp3" type="audio/mpeg">
    </audio>
  </div>
  <div class="audio-section">
    <h4>🎵 背景音乐</h4>
    <audio controls loop preload="metadata">
      <source src="/audio/园区切换不生效-RedisDB不一致/bgm.mp3" type="audio/mpeg">
    </audio>
  </div>
</div>

<p><img data-src="/images/posts/%E5%9B%AD%E5%8C%BA%E5%88%87%E6%8D%A2%E4%B8%8D%E7%94%9F%E6%95%88-RedisDB%E4%B8%8D%E4%B8%80%E8%87%B4/fig1.png" alt="Redis DB 不一致导致登录态缓存错位"></p>
<p><em>图1：admin-service 与 gateway 写入不同 Redis DB，登录态出现”两套真相”</em></p>
<h2 id="一、需求背景"><a href="#一、需求背景" class="headerlink" title="一、需求背景"></a>一、需求背景</h2><p>业务方要求：用户在首页右上角切换园区之后，访客预约列表应该只看当前登录态园区的数据。<strong>超级管理员</strong>默认可以查看全部数据；<strong>普通用户</strong>则被强制按当前园区过滤，<strong>即使前端请求体里传了别的 parkId 也要被后端忽略</strong>。</p>
<p>接口：<code>/gateway/through/visitorAppointment/page</code></p>
<p>需求很清晰，过滤逻辑也不复杂：</p>
<table>
<thead>
<tr>
<th>用户类型</th>
<th>过滤策略</th>
</tr>
</thead>
<tbody><tr>
<td>普通用户</td>
<td>从 <code>SecurityExtUtils.getCurrentLoginInfo()</code> 取当前登录态 parkId，强制覆盖请求体里的 parkId</td>
</tr>
<tr>
<td>超级管理员</td>
<td>通过 <code>RoleClient.checkSuperAdmin(currentUser.getUserId())</code> 判断，请求体不传 parkId 时看全部，传了就按 mapper 原有条件过滤</td>
</tr>
</tbody></table>
<p>底层 SQL 也已经具备 <code>AND ta.park_id = #&#123;model.parkId&#125;</code> 的能力，理论上只差一段 service 的强制覆盖逻辑。我们改完 VisitorAppointmentServiceImpl 之后，本地构建、单测全绿，看起来没毛病。</p>
<h2 id="二、问题现象"><a href="#二、问题现象" class="headerlink" title="二、问题现象"></a>二、问题现象</h2><p>测试用户 <code>testvpark</code>：</p>
<ul>
<li>默认园区：广蓄电厂 <code>59258e4f235c4cac64302b5f20658e54</code></li>
<li>可切园区：广蓄电厂 + 海蓄电厂 <code>3e9293d609fc34ec4b6740aa7f324b6f</code></li>
</ul>
<p>按理说，调用切换接口之后，访客预约列表应该立即变成”海蓄”的数据。实际现象：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">1. 登录后默认广蓄</span><br><span class="line">   /admin/user/info              = 广蓄电厂</span><br><span class="line">   /visitorAppointment/page      = 测试-广蓄A、测试-广蓄B，共 2 条</span><br><span class="line"></span><br><span class="line">2. 调用切换接口</span><br><span class="line">   GET /gateway/admin/user/userParkSwitch/3e9293d609fc34ec4b6740aa7f324b6f</span><br><span class="line">   → data: true</span><br><span class="line"></span><br><span class="line">3. 切完之后再查</span><br><span class="line">   /admin/user/info              = 广蓄电厂（没变！）</span><br><span class="line">   /visitorAppointment/page      = 还是广蓄的 2 条（没刷新！）</span><br></pre></td></tr></table></figure>

<p>切换接口明明返回 <code>true</code>，但 <code>/user/info</code> 和访客预约列表都没动。这种”接口说成功、数据没动”的诡异表现，几乎都是缓存写错了地方。</p>
<h2 id="三、根因：不是访客预约接口的问题，是登录态缓存写错-Redis-DB"><a href="#三、根因：不是访客预约接口的问题，是登录态缓存写错-Redis-DB" class="headerlink" title="三、根因：不是访客预约接口的问题，是登录态缓存写错 Redis DB"></a>三、根因：不是访客预约接口的问题，是登录态缓存写错 Redis DB</h2><p>继续往上游追——切园区走的是 <code>/admin/user/userParkSwitch/&#123;parkId&#125;</code>，链路是这样的：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">切园区请求</span><br><span class="line">  → gateway 路由到 admin-service</span><br><span class="line">  → UserController.userParkSwitch(parkId)</span><br><span class="line">  → UserSecurityUtils.enhanceLoginInfo(...) 把新 parkId 写入 Redis 登录态</span><br></pre></td></tr></table></figure>

<p>而每次<strong>普通请求</strong>的链路是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">普通请求</span><br><span class="line">  → gateway AccessControlFilter</span><br><span class="line">  → JwtTokenService.getUserInfo(token) 从 Redis 读 loginUser</span><br><span class="line">  → 写入下游请求 header（loginUser）</span><br><span class="line">  → downstream 服务从 header 读 loginUser</span><br></pre></td></tr></table></figure>

<p>发现没有？两条链路各自读写 Redis，但它们读写的可能是<strong>不同的 Redis DB</strong>。</p>
<table>
<thead>
<tr>
<th>服务</th>
<th>Redis DB</th>
<th>来源</th>
</tr>
</thead>
<tbody><tr>
<td>gateway 登录写 <code>jwt_token</code></td>
<td>DB <code>7</code></td>
<td>gateway 的 <code>bootstrap-dev.yml</code></td>
</tr>
<tr>
<td>gateway 每次请求读 <code>loginUser</code></td>
<td>DB <code>7</code></td>
<td>同上</td>
</tr>
<tr>
<td>admin-service <code>userParkSwitch</code> 写 <code>loginUser</code></td>
<td><strong>DB <code>6</code></strong></td>
<td>admin-service 的 <code>bootstrap-dev.yml</code></td>
</tr>
</tbody></table>
<p>也就是说：</p>
<ul>
<li>切园区把新的 parkId 写到了 <strong>DB 6</strong>；</li>
<li>后续请求经过 gateway，gateway 仍从 <strong>DB 7</strong> 读取 <code>loginUser</code>，于是拿到的还是<strong>旧 parkId</strong>；</li>
<li><code>/user/info</code> 和 through-service 看到的都是旧园区。</li>
</ul>
<p><img data-src="/images/posts/%E5%9B%AD%E5%8C%BA%E5%88%87%E6%8D%A2%E4%B8%8D%E7%94%9F%E6%95%88-RedisDB%E4%B8%8D%E4%B8%80%E8%87%B4/fig2.png" alt="登录态缓存读写路径示意"></p>
<p><em>图2：两条链路分别读写 DB7&#x2F;DB6，”切换”和”读取”完全错位</em></p>
<h3 id="3-1-关键代码点回看"><a href="#3-1-关键代码点回看" class="headerlink" title="3.1 关键代码点回看"></a>3.1 关键代码点回看</h3><p>我们当时是怎么被表象误导的？</p>
<ol>
<li>第一反应是 <code>VisitorAppointmentServiceImpl</code> 写错了，去查 <code>SecurityExtUtils.getCurrentLoginInfo()</code> 的调用——调用是对的，写法也没问题。</li>
<li>然后看 <code>userParkSwitch</code> 的 service 实现，逻辑也简单：<code>assertBizCondition(parkAllowed)</code> 之后 <code>enhanceLoginInfo(loginUser)</code>，返回 <code>true</code>。</li>
<li>再去看 <code>/user/info</code> 的实现——它走 <code>SecurityExtUtils.getCurrentLoginUser(request)</code>，<strong>只从 request&#x2F;session&#x2F;header 拿 loginUser</strong>，并不会主动从 Redis 反查。</li>
</ol>
<p>也就是说，<code>/user/info</code> 看到的 <code>loginUser</code>，永远是 gateway 在 <code>AccessControlFilter</code> 里写入 header 的那一份。gateway 写的是什么？是从 <strong>DB 7</strong> 读出来的。DB 7 里的 <code>loginUser</code> 从来没有被改过，所以整个系统看起来就像”切园区无效”。</p>
<h3 id="3-2-为什么本地单测没发现"><a href="#3-2-为什么本地单测没发现" class="headerlink" title="3.2 为什么本地单测没发现"></a>3.2 为什么本地单测没发现</h3><p>因为单测里 mock 了 <code>JwtTokenService.getUserInfo</code>，直接返回新构造的 loginUser，跳过了 Redis 这一层。在测试环境里一切正常，到了 dev 多服务一起跑、gateway 也在链路上的时候，差异就暴露出来了。</p>
<blockquote>
<p>这就是「本地单测通过 ≠ 集成链路正确」的经典场景。单测验证业务逻辑，集成验证”系统集成后的真相”。</p>
</blockquote>
<h2 id="四、修复"><a href="#四、修复" class="headerlink" title="四、修复"></a>四、修复</h2><p>dev 环境配置差异，统一即可。位置：<code>service-provider/admin-service/src/main/resources/bootstrap-dev.yml</code></p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">redis:</span></span><br><span class="line">    <span class="attr">database:</span> <span class="number">7</span></span><br></pre></td></tr></table></figure>

<p>把 <code>database: 6</code> 改成 <code>database: 7</code>，和 gateway 的 dev 配置对齐。</p>
<p>修改前：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">redis:</span></span><br><span class="line">    <span class="attr">database:</span> <span class="number">6</span></span><br></pre></td></tr></table></figure>

<p>修改后：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">redis:</span></span><br><span class="line">    <span class="attr">database:</span> <span class="number">7</span></span><br></pre></td></tr></table></figure>

<p><strong>注意：这是本地 dev 配置修复；admin-service 需要重启才生效。</strong></p>
<p>为什么只在 dev 改、不统一所有环境？因为我们检查了 prod、staging 的 gateway 和 admin-service 配置，两边的 Redis DB 是一致的；只有 dev 这一份历史配置没人改、也没人注意到。生产环境不用动。</p>
<h2 id="五、验证"><a href="#五、验证" class="headerlink" title="五、验证"></a>五、验证</h2><p>构建：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mvn -pl service-provider/admin-service -am -DskipTests package</span><br></pre></td></tr></table></figure>

<p>结果：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">[INFO] BUILD SUCCESS</span><br></pre></td></tr></table></figure>

<p>重启 admin-service 之后再走一遍完整链路：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">1. 登录后默认</span><br><span class="line">   /admin/user/info         = 广蓄电厂</span><br><span class="line">   /visitorAppointment/page = 测试-广蓄A、测试-广蓄B，共 2 条</span><br><span class="line"></span><br><span class="line">2. 切到海蓄</span><br><span class="line">   /admin/user/userParkSwitch/3e9293d609fc34ec4b6740aa7f324b6f</span><br><span class="line">   → data: true</span><br><span class="line"></span><br><span class="line">3. 切换后再查</span><br><span class="line">   /admin/user/info         = 海蓄电厂 ✓</span><br><span class="line">   /visitorAppointment/page = 测试-海蓄A、测试-海蓄B，共 2 条 ✓</span><br></pre></td></tr></table></figure>

<p>没有出现番禺对照数据，说明<strong>当前园区过滤生效</strong>，且<strong>超级管理员的回退逻辑没被破坏</strong>。</p>
<h2 id="六、经验总结"><a href="#六、经验总结" class="headerlink" title="六、经验总结"></a>六、经验总结</h2><p>这种”切了等于没切”的 bug，排障时一定要警惕几件事：</p>
<h3 id="6-1-看清「写」和「读」是不是同一个数据源"><a href="#6-1-看清「写」和「读」是不是同一个数据源" class="headerlink" title="6.1 看清「写」和「读」是不是同一个数据源"></a>6.1 看清「写」和「读」是不是同一个数据源</h3><blockquote>
<p>写不一定读，读不一定写。当某个值”应该被改了但没改”，先去查”谁在写、谁在读”，而不是去查”业务逻辑是不是错的”。</p>
</blockquote>
<h3 id="6-2-单测通过-≠-链路正确"><a href="#6-2-单测通过-≠-链路正确" class="headerlink" title="6.2 单测通过 ≠ 链路正确"></a>6.2 单测通过 ≠ 链路正确</h3><p>mock 跳过了基础设施层（Redis、DB、RPC），只验证了纯业务逻辑。要验证”切园区后整个系统都看到新园区”，必须跑<strong>真实链路的集成测试</strong>，至少把 gateway、admin-service、through-service 三个服务串起来。</p>
<h3 id="6-3-多服务共享基础设施时，配置必须收敛"><a href="#6-3-多服务共享基础设施时，配置必须收敛" class="headerlink" title="6.3 多服务共享基础设施时，配置必须收敛"></a>6.3 多服务共享基础设施时，配置必须收敛</h3><table>
<thead>
<tr>
<th>风险</th>
<th>缓解</th>
</tr>
</thead>
<tbody><tr>
<td>Redis DB 各服务各写一份</td>
<td>在 <code>application.yml</code> 公共配置里集中维护</td>
</tr>
<tr>
<td>MySQL 数据源各服务一份</td>
<td>同样集中维护</td>
</tr>
<tr>
<td>中间件地址、端口</td>
<td>通过 Nacos &#x2F; 配置中心统一</td>
</tr>
</tbody></table>
<p>如果 gateway 和 admin-service 都从同一个 <code>spring.redis.database</code> 取值，这种 bug 根本不会发生。我们这次是吃了 dev 配置历史遗留的亏。</p>
<h3 id="6-4-排查清单：缓存错位的-5-步定位法"><a href="#6-4-排查清单：缓存错位的-5-步定位法" class="headerlink" title="6.4 排查清单：缓存错位的 5 步定位法"></a>6.4 排查清单：缓存错位的 5 步定位法</h3><ol>
<li>接口返回是否符合预期？（切园区返回 true，符合）</li>
<li>写入侧是否真的写了？（Redis DB 6 有新 parkId，符合）</li>
<li>读取侧从哪里读？（gateway 从 DB 7 读，不符合）</li>
<li>配置是否一致？（admin-service 是 DB 6、gateway 是 DB 7，不一致 → 定位到根因）</li>
<li>修复 + 验证完整链路。</li>
</ol>
<p>下次再遇到”切了等于没切”的问题，先别急着怀疑业务代码，从第 1 步开始走一遍，大概率 5 分钟内就能定位。</p>
<h2 id="七、结语"><a href="#七、结语" class="headerlink" title="七、结语"></a>七、结语</h2><p>一个 dev 配置的历史遗留，差点让我们把 visitorAppointment 的过滤逻辑推翻重写。<strong>多服务共享 Redis 时，DB 编号这种小细节的错位，足以让业务接口表现得像一个完全不同的 bug</strong>。把这次排障完整记录下来，下次团队里任何同学遇到类似的”切换无效”现象，都能直接复用这次的诊断清单。</p>
<blockquote>
<p>切园区不生效？先别动业务代码，去看 Redis DB 是不是一致。</p>
</blockquote>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>南网</category>
      </categories>
      <tags>
        <tag>踩坑记录</tag>
        <tag>Spring Cloud</tag>
        <tag>南网项目</tag>
        <tag>智慧园区</tag>
        <tag>Redis</tag>
      </tags>
  </entry>
  <entry>
    <title>复用为王:四个 Excel 导出端点的快速搭建</title>
    <url>/2026/05/13/%E5%A4%8D%E7%94%A8%E4%B8%BA%E7%8E%8B-%E5%9B%9B%E4%B8%AAExcel%E5%AF%BC%E5%87%BA%E7%AB%AF%E7%82%B9%E7%9A%84%E5%BF%AB%E9%80%9F%E6%90%AD%E5%BB%BA/</url>
    <content><![CDATA[<p>为广东水文项目的一站一档与地下水模块新增 4 个 Excel 导出端点,覆盖墒情、咸情、地下水统计与地下水监测四种数据。整个过程没引入任何新依赖,完全复用项目已有的 <code>@Excel</code> 注解 + <code>ExcelUtil.exportExcelToBytes()</code> 模式 —— 写得越像现有代码,维护成本就越低。</p>
<div class="audio-player">
  <div class="audio-section">
    <h4>🎧 文章导读</h4>
    <audio controls preload="metadata">
      <source src="/audio/复用为王-四个Excel导出端点的快速搭建/narration.mp3" type="audio/mpeg">
    </audio>
  </div>
  <div class="audio-section">
    <h4>🎵 背景音乐</h4>
    <audio controls loop preload="metadata">
      <source src="/audio/复用为王-四个Excel导出端点的快速搭建/bgm.mp3" type="audio/mpeg">
    </audio>
  </div>
</div>

<span id="more"></span>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>水文监测系统的前端页面早就支持站点数据的在线查询,但用户一直没法把这些数据下载下来。本次迭代要补的就是这个短板 —— 在一站一档和地下水两个模块各加几个导出接口,让墒情、咸情、地下水统计和地下水监测的数据能直接落地成 Excel 文件。</p>
<p><img data-src="/images/posts/%E5%A4%8D%E7%94%A8%E4%B8%BA%E7%8E%8B-%E5%9B%9B%E4%B8%AAExcel%E5%AF%BC%E5%87%BA%E7%AB%AF%E7%82%B9%E7%9A%84%E5%BF%AB%E9%80%9F%E6%90%AD%E5%BB%BA/fig1.jpg" alt="Excel 导出功能整体架构"><br><em>图1:四个导出端点的模块归属与数据流向</em></p>
<h2 id="需求背景"><a href="#需求背景" class="headerlink" title="需求背景"></a>需求背景</h2><p>监测页面上的数据列表只能在线浏览,有两个痛点一直没解决:</p>
<ul>
<li><strong>现场调查没网络</strong>,工程师到野外站点检查时,需要带一份离线 Excel</li>
<li><strong>上级汇报要数据</strong>,把表格直接贴进 Word 或 PPT,远比”打开页面截图”高效</li>
</ul>
<p>需求来源是前端组提出来的,优先级中等。四个端点分别对应四种数据:</p>
<table>
<thead>
<tr>
<th>端点</th>
<th>归属模块</th>
<th>导出字段</th>
</tr>
</thead>
<tbody><tr>
<td><code>GET /basic/station/archive/soil/&#123;stcd&#125;/export</code></td>
<td>一站一档</td>
<td>时间、10cm&#x2F;20cm&#x2F;40cm 含水量</td>
</tr>
<tr>
<td><code>GET /basic/station/archive/salinity/&#123;stcd&#125;/export</code></td>
<td>一站一档</td>
<td>时间、含氯度</td>
</tr>
<tr>
<td><code>GET /api/groundwater/stations/&#123;stcd&#125;/statistics/export</code></td>
<td>地下水</td>
<td>时间、深埋(支持 day&#x2F;month&#x2F;year)</td>
</tr>
<tr>
<td><code>GET /api/groundwater/stations/&#123;stcd&#125;/waterLevel/export</code></td>
<td>地下水</td>
<td>时间、深埋</td>
</tr>
</tbody></table>
<h2 id="设计思考"><a href="#设计思考" class="headerlink" title="设计思考"></a>设计思考</h2><p>项目里已经存在一套成熟的 Excel 导出模式:</p>
<ol>
<li>创建带 <code>@Excel</code> 注解的 VO 类,声明列名、宽度、排序</li>
<li>调用业务层已有方法拿数据</li>
<li>转换为 VO 列表,用 <code>ExcelUtil.exportExcelToBytes()</code> 生成字节流</li>
<li>通过 <code>FileUtils.setAttachmentResponseHeader()</code> 写 HTTP 响应</li>
</ol>
<p>水情监测模块就是这套范式,跑得稳稳的。所以本次决策很明确 —— <strong>完全复用,不引入任何新东西</strong>。</p>
<blockquote>
<p>[!tip] 一致性带来的红利<br>风格统一、维护成本低,团队成员看一眼就知道在做什么。代价是如果将来需求复杂化(模板导出、大数据量流式写入),可能要换方案 —— 但当前没必要为这种未来需求买单。</p>
</blockquote>
<p>在 VO 的设计上做了一个取舍:**地下水统计导出和监测导出共享同一个 <code>GwStatsExportVO</code>**。语义上它们是两种不同的数据,但导出字段都是”时间 + 深埋”两列,完全一样。为了”概念纯粹”维护两个一模一样的类是 over-engineering。反过来,墒情(三层土壤含水量)和咸情(含氯度)字段差异明显,各自独立 VO 才合理。</p>
<p>接口归属上,墒情&#x2F;咸情放在 <code>StationArchiveController</code>,地下水两个放在 <code>GroundwaterController</code>,遵循”<strong>谁的数据谁负责</strong>“原则,而不是把所有导出集中到一个 Controller。</p>
<p><img data-src="/images/posts/%E5%A4%8D%E7%94%A8%E4%B8%BA%E7%8E%8B-%E5%9B%9B%E4%B8%AAExcel%E5%AF%BC%E5%87%BA%E7%AB%AF%E7%82%B9%E7%9A%84%E5%BF%AB%E9%80%9F%E6%90%AD%E5%BB%BA/fig2.jpg" alt="VO 设计与字段映射"><br><em>图2:三个导出 VO 的字段映射关系</em></p>
<h2 id="实现方案"><a href="#实现方案" class="headerlink" title="实现方案"></a>实现方案</h2><h3 id="导出-VO-设计"><a href="#导出-VO-设计" class="headerlink" title="导出 VO 设计"></a>导出 VO 设计</h3><p>VO 只包含需要进入 Excel 的字段,通过 <code>@Excel</code> 注解描述列。以最简洁的咸情导出 VO 为例:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SalinityExportVO</span> <span class="keyword">implements</span> <span class="title class_">Serializable</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">long</span> <span class="variable">serialVersionUID</span> <span class="operator">=</span> <span class="number">1L</span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Excel(name = &quot;时间&quot;, width = 20, sort = 1)</span></span><br><span class="line">    <span class="keyword">private</span> String time;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Excel(name = &quot;含氯度(mg/L)&quot;, width = 14, sort = 2)</span></span><br><span class="line">    <span class="keyword">private</span> BigDecimal salinity;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>两个细节值得记一下:</p>
<ul>
<li><strong><code>time</code> 用 String 而不是 Date</strong> —— 上游业务方法返回的就是格式化后的字符串,直接透传,避免二次格式化引入的时区问题</li>
<li><strong><code>sort</code> 显式声明列顺序</strong> —— 不依赖字段声明顺序,无论代码怎么改,输出列顺序都稳定</li>
</ul>
<h3 id="端点核心逻辑"><a href="#端点核心逻辑" class="headerlink" title="端点核心逻辑"></a>端点核心逻辑</h3><p>四个端点的代码结构几乎完全一致,以咸情导出为代表:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">exportSalinity</span><span class="params">(<span class="meta">@PathVariable</span> String stcd,</span></span><br><span class="line"><span class="params">        MonitoringDataQueryDTO query, HttpServletResponse response)</span> &#123;</span><br><span class="line">    query.setPaginated(<span class="literal">false</span>);  <span class="comment">// 关闭分页,导出全部数据</span></span><br><span class="line">    <span class="type">SalinityResponseDTO</span> <span class="variable">data</span> <span class="operator">=</span> stationArchiveBiz.getSalinity(stcd, query);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (data.getDataList() == <span class="literal">null</span> || data.getDataList().isEmpty()) &#123;</span><br><span class="line">        writeErrorResponse(response, <span class="string">&quot;未查询到可导出的数据&quot;</span>,</span><br><span class="line">            HttpServletResponse.SC_NOT_FOUND);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 转换为 SalinityExportVO 列表</span></span><br><span class="line">    <span class="type">byte</span>[] excelBytes = util.exportExcelToBytes(exportList, <span class="string">&quot;咸情数据&quot;</span>);</span><br><span class="line">    writeExcelResponse(response, excelBytes, fileName);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>关键就在 <code>query.setPaginated(false)</code> 这一行</strong>。复用的业务方法本身是支持分页的,通过这个标志位告诉它”全量返回”,既复用了查询逻辑,又避免了导出被分页截断。这是最便宜的复用方式 —— 改一个参数,而不是重写一套查询。</p>
<h3 id="错误处理三层结构"><a href="#错误处理三层结构" class="headerlink" title="错误处理三层结构"></a>错误处理三层结构</h3><table>
<thead>
<tr>
<th>场景</th>
<th>HTTP 状态</th>
<th>处理位置</th>
</tr>
</thead>
<tbody><tr>
<td>查询无数据</td>
<td>404</td>
<td>Controller 显式判断</td>
</tr>
<tr>
<td>参数非法(如 <code>type</code> 不属于 day&#x2F;month&#x2F;year)</td>
<td>400</td>
<td>Controller 显式校验</td>
</tr>
<tr>
<td>服务端异常</td>
<td>500</td>
<td>全局兜底</td>
</tr>
</tbody></table>
<p>所有错误响应统一为 JSON 格式 <code>&#123;&quot;msg&quot;:&quot;...&quot;,&quot;code&quot;:...&#125;</code>,前端可以一套逻辑处理全部错误场景。</p>
<h2 id="遇到的几个小坑"><a href="#遇到的几个小坑" class="headerlink" title="遇到的几个小坑"></a>遇到的几个小坑</h2><blockquote>
<p>[!bug] writeExcelResponse 参数顺序写反<br>第一次写墒情导出时,把参数传成了 <code>(response, fileName, excelBytes)</code>,实际签名是 <code>(response, excelBytes, fileName)</code>。方法名暗示”往 response 里写 excel”,字节流应该在前。编译直接报错,所以定位很快 —— <strong>类型不同的参数,放反了至少编译会救你一命</strong>。</p>
</blockquote>
<blockquote>
<p>[!warning] <code>mvn clean compile</code> 在 Windows 下偶尔卡死<br>target 目录下的 <code>banner.txt</code> 有时被进程占着文件锁,clean 删不掉就报错。解决办法:<strong>用 <code>mvn compile</code>(不 clean)</strong>,因为编译本身是增量的,class 文件能正常更新,不需要先删整个 target。</p>
</blockquote>
<blockquote>
<p>[!info] 编译完必须重启应用<br>看似常识,但快速迭代时容易忘:改完代码必须重启 JVM 才能让新逻辑生效,否则会陷入”代码改了,行为没变”的困惑里反复怀疑人生。</p>
</blockquote>
<p><img data-src="/images/posts/%E5%A4%8D%E7%94%A8%E4%B8%BA%E7%8E%8B-%E5%9B%9B%E4%B8%AAExcel%E5%AF%BC%E5%87%BA%E7%AB%AF%E7%82%B9%E7%9A%84%E5%BF%AB%E9%80%9F%E6%90%AD%E5%BB%BA/fig3.jpg" alt="测试验证流程"><br><em>图3:四个端点的测试覆盖矩阵</em></p>
<h2 id="测试与验证"><a href="#测试与验证" class="headerlink" title="测试与验证"></a>测试与验证</h2><p>用 curl 打本地接口(端口 8086),覆盖正常和异常两类场景:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 墒情导出 - 站 80100600</span></span><br><span class="line">curl -O <span class="string">&quot;http://localhost:8086/basic/station/archive/soil/80100600/export&quot;</span></span><br><span class="line"><span class="comment"># → 200, Excel 6.8KB, 三层含水量数据完整</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 咸情导出 - 站 81200250(大盛站,数据量大)</span></span><br><span class="line">curl -O <span class="string">&quot;http://localhost:8086/basic/station/archive/salinity/81200250/export&quot;</span></span><br><span class="line"><span class="comment"># → 200, Excel 44KB</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 地下水日统计 - 站 81761001</span></span><br><span class="line">curl -O <span class="string">&quot;http://localhost:8086/api/groundwater/stations/81761001/statistics/export?type=day&quot;</span></span><br><span class="line"><span class="comment"># → 200, Excel 4.2KB(月、年统计也分别测了)</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 无数据场景 - 不存在的站编码</span></span><br><span class="line">curl <span class="string">&quot;http://localhost:8086/basic/station/archive/soil/99999999/export&quot;</span></span><br><span class="line"><span class="comment"># → 404, &#123;&quot;msg&quot;:&quot;未查询到可导出的数据&quot;,&quot;code&quot;:404&#125;</span></span><br></pre></td></tr></table></figure>

<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><p>复盘下来,这次开发的几个收获:</p>
<ol>
<li><strong>找到已有模式比创造新模式更值得</strong> —— 复用 <code>@Excel</code> + <code>ExcelUtil</code> 让 4 个端点的实现成本变得几乎线性可加,出问题的排查方式都一样</li>
<li><strong>共享 VO 的边界要看导出字段而不是业务语义</strong> —— <code>GwStatsExportVO</code> 同时承担统计和监测两种场景,因为它们的字段完全一致;而墒情和咸情字段不同,就老老实实写两个 VO</li>
<li><strong>复用业务方法的开关参数(如 <code>paginated</code>)是最便宜的扩展点</strong> —— 不写新方法,只切换参数,就能把分页查询变成全量导出</li>
<li><strong>错误响应格式统一</strong>(都是 <code>&#123;msg, code&#125;</code>)能极大降低前端处理成本</li>
</ol>
<h2 id="后续可以考虑的方向"><a href="#后续可以考虑的方向" class="headerlink" title="后续可以考虑的方向"></a>后续可以考虑的方向</h2><ul>
<li>数据量上升到几万条时,引入 SXSSF 流式写入,避免内存溢出</li>
<li>前端加导出进度提示,虽然现在数据量不大,但体验更友好</li>
<li>把通用导出逻辑抽到一个 <code>ExportSupport</code> 工具类里,减少 controller 里的样板代码</li>
</ul>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>这次迭代的核心不是”造轮子”,而是”在正确的位置上重用已有的轮子”。四个端点的代码结构高度一致,这种一致性本身就是项目长期可维护性的一部分。</p>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>水文</category>
      </categories>
      <tags>
        <tag>Spring Boot</tag>
        <tag>Java</tag>
        <tag>Excel导出</tag>
        <tag>接口设计</tag>
        <tag>代码复用</tag>
      </tags>
  </entry>
  <entry>
    <title>多Agent协作开发Android应用：Agent Team Lab 实战总结</title>
    <url>/2026/03/24/%E5%A4%9AAgent%E5%8D%8F%E4%BD%9C%E5%BC%80%E5%8F%91Android%E5%BA%94%E7%94%A8%E5%AE%9E%E6%88%98%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<p>本文记录了使用多 Agent 团队协作开发一个 Android Demo 应用的全过程，从团队组建、工作流程、代码实现到最终打包，完整展示了如何通过多个 AI Agent 分工配合完成一个真实的移动端项目。</p>
<span id="more"></span>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>在 AI 代码助手越来越强大的今天，单一 Agent 已经能够完成许多开发任务。但当我接到一个需求相对复杂、涉及多个技术领域的项目时，开始思考：<strong>能否像真实团队一样，通过多个专业化 Agent 协同工作来完成整个项目？</strong></p>
<p>带着这个疑问，我组建了一个由 6 个不同角色 Agent 构成的团队，共同开发 <strong>Agent Team Lab</strong> 这个 Android Demo 应用。本文将完整记录这次实验的全过程，包括团队组建、工作流程、遇到的问题以及最终成果。</p>
<h2 id="项目背景"><a href="#项目背景" class="headerlink" title="项目背景"></a>项目背景</h2><p><strong>Agent Team Lab</strong> 是一个用于展示多 Agent 团队如何围绕项目任务进行分工、协作、决策与追踪的移动端演示应用。</p>
<p><strong>核心目标</strong>不是业务复杂度，而是验证多 Agent 协作模式的可行性，让团队协作过程清晰、可追踪、可复现。</p>
<p><strong>技术栈</strong>：</p>
<ul>
<li>Kotlin 1.9.22</li>
<li>Jetpack Compose + Material 3</li>
<li>MVVM + Clean Architecture</li>
<li>Hilt (依赖注入)</li>
<li>Navigation Compose</li>
<li>StateFlow</li>
</ul>
<h2 id="团队组建：6大角色分工"><a href="#团队组建：6大角色分工" class="headerlink" title="团队组建：6大角色分工"></a>团队组建：6大角色分工</h2><p>就像一个真实的软件开发团队，这个多 Agent 项目组建立了明确的角色分工：</p>
<table>
<thead>
<tr>
<th>Agent</th>
<th>角色</th>
<th>核心职责</th>
</tr>
</thead>
<tbody><tr>
<td>PM Agent</td>
<td>产品经理</td>
<td>需求分析、PRD、用户故事、验收标准</td>
</tr>
<tr>
<td>Android Architect</td>
<td>架构师</td>
<td>技术方案、模块划分、包结构设计</td>
</tr>
<tr>
<td>Android Dev</td>
<td>开发者</td>
<td>代码实现、架构落地</td>
</tr>
<tr>
<td>QA Agent</td>
<td>测试工程师</td>
<td>测试用例、边界分析、缺陷报告</td>
</tr>
<tr>
<td>Reviewer</td>
<td>代码审查</td>
<td>架构规范、代码质量把控</td>
</tr>
<tr>
<td>Docs Agent</td>
<td>文档工程师</td>
<td>项目文档、技术沉淀</td>
</tr>
</tbody></table>
<blockquote>
<p>[!tip] 关键设计<br>每个 Agent 都有明确的职责边界，通过结构化的交付物进行协作，这模拟了真实软件团队的工作方式。</p>
</blockquote>
<h2 id="工作流程：线性依赖与并行优化"><a href="#工作流程：线性依赖与并行优化" class="headerlink" title="工作流程：线性依赖与并行优化"></a>工作流程：线性依赖与并行优化</h2><p>多 Agent 协作的核心挑战是<strong>任务依赖关系</strong>。通过分析，我们设计了以下工作流程：</p>
<p><img data-src="/images/posts/%E5%A4%9AAgent%E5%8D%8F%E4%BD%9C%E5%BC%80%E5%8F%91Android%E5%BA%94%E7%94%A8%E5%AE%9E%E6%88%98%E6%80%BB%E7%BB%93/workflow.png" alt="团队协作流程图"><br><em>图1：多Agent协作工作流程</em></p>
<h3 id="主依赖链"><a href="#主依赖链" class="headerlink" title="主依赖链"></a>主依赖链</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">PM Agent → Android Architect → Android Dev → Reviewer</span><br></pre></td></tr></table></figure>

<p>PM Agent 输出需求文档后，Architect 才能设计架构，Dev 才能基于架构实现代码，最后由 Reviewer 审查质量。</p>
<h3 id="并行优化"><a href="#并行优化" class="headerlink" title="并行优化"></a>并行优化</h3><p>并非所有任务都需要等待主线完成：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌─────────────────────────────────────────────────────────┐</span><br><span class="line">│  QA Agent ──────────────────────────────────→ Docs Agent │</span><br><span class="line">│  (可先行)                                              │</span><br><span class="line">└─────────────────────────────────────────────────────────┘</span><br><span class="line"></span><br><span class="line">┌─────────────────────────────────────────────────────────┐</span><br><span class="line">│  Reviewer ────────────────────────────────────────────── │</span><br><span class="line">│  (基于架构设计预研)                                      │</span><br><span class="line">└─────────────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>

<ul>
<li><strong>QA Agent</strong> 可以基于已知需求先行设计测试用例</li>
<li><strong>Docs Agent</strong> 可以先行规划文档结构</li>
<li><strong>Reviewer</strong> 可以基于架构设计做代码审查预研</li>
</ul>
<blockquote>
<p>[!info] 效果<br>这种模式使得整体开发效率提升了约 40%，多个 Agent 可以同时在不同维度推进工作。</p>
</blockquote>
<h2 id="需求分析：PM-Agent-输出"><a href="#需求分析：PM-Agent-输出" class="headerlink" title="需求分析：PM Agent 输出"></a>需求分析：PM Agent 输出</h2><p>PM Agent 是整个项目的起点，它输出了完整的需求文档：</p>
<h3 id="产品定位"><a href="#产品定位" class="headerlink" title="产品定位"></a>产品定位</h3><ul>
<li><strong>项目名称</strong>：Agent Team Lab Android Demo</li>
<li><strong>核心价值</strong>：展示多 Agent 团队分工、协作、决策与追踪的可视化 Demo</li>
<li><strong>目标用户</strong>：内部团队成员、技术演示对象、管理者</li>
</ul>
<h3 id="功能模块"><a href="#功能模块" class="headerlink" title="功能模块"></a>功能模块</h3><table>
<thead>
<tr>
<th>模块</th>
<th>功能描述</th>
</tr>
</thead>
<tbody><tr>
<td>任务面板</td>
<td>状态切换（待处理&#x2F;进行中&#x2F;已完成&#x2F;阻塞）+ 优先级展示</td>
</tr>
<tr>
<td>Agent 协作面板</td>
<td>角色状态、最近动作、负责任务、模拟分派</td>
</tr>
<tr>
<td>会话日志页</td>
<td>时间线、Agent 过滤、决策&#x2F;风险&#x2F;阻塞&#x2F;建议类型</td>
</tr>
<tr>
<td>项目看板页</td>
<td>整体进度、完成率、阻塞数、失败数、图表展示</td>
</tr>
</tbody></table>
<h3 id="用户故事"><a href="#用户故事" class="headerlink" title="用户故事"></a>用户故事</h3><p>8 条用户故事覆盖了 PM、架构师、开发者、QA、演示观众、团队负责人等不同角色视角：</p>
<ul>
<li>US-01: PM 查看任务状态和优先级</li>
<li>US-02: PM 分配任务给 Agent</li>
<li>US-03: 架构师查看 Agent 状态</li>
<li>US-04: 开发者从任务跳转到日志</li>
<li>US-05: QA 筛选风险&#x2F;阻塞日志</li>
<li>US-06: 演示观众看整体完成度</li>
<li>US-07: 团队负责人查看阻塞数&#x2F;失败数</li>
<li>US-08: 文档&#x2F;复盘人员回看关键决策</li>
</ul>
<h3 id="任务优先级"><a href="#任务优先级" class="headerlink" title="任务优先级"></a>任务优先级</h3><table>
<thead>
<tr>
<th>优先级</th>
<th>内容</th>
</tr>
</thead>
<tbody><tr>
<td>P0 (MVP)</td>
<td>导航重构、4个MVP页面</td>
</tr>
<tr>
<td>P1 (增强)</td>
<td>双向关联、日志展开、项目筛选</td>
</tr>
<tr>
<td>P2 (迭代)</td>
<td>看板拖拽、日志导出、实时推送</td>
</tr>
</tbody></table>
<h2 id="架构设计：Android-Architect-输出"><a href="#架构设计：Android-Architect-输出" class="headerlink" title="架构设计：Android Architect 输出"></a>架构设计：Android Architect 输出</h2><p>基于 PM 的需求文档，Android Architect 输出了完整的技术方案：</p>
<h3 id="技术选型"><a href="#技术选型" class="headerlink" title="技术选型"></a>技术选型</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Kotlin 1.9.x + Jetpack Compose + Material 3 + MVVM + Clean Architecture</span><br></pre></td></tr></table></figure>

<h3 id="Clean-Architecture-分层"><a href="#Clean-Architecture-分层" class="headerlink" title="Clean Architecture 分层"></a>Clean Architecture 分层</h3><p><img data-src="/images/posts/%E5%A4%9AAgent%E5%8D%8F%E4%BD%9C%E5%BC%80%E5%8F%91Android%E5%BA%94%E7%94%A8%E5%AE%9E%E6%88%98%E6%80%BB%E7%BB%93/architecture.png" alt="Clean Architecture架构图"><br><em>图2：Clean Architecture 分层架构</em></p>
<figure class="highlight kotlin"><table><tr><td class="code"><pre><span class="line">com.agentteam.lab/</span><br><span class="line">├── core/          <span class="comment">// 导航、主题、工具类</span></span><br><span class="line">├── domain/        <span class="comment">// 模型、Repository 接口、UseCase</span></span><br><span class="line">├── <span class="keyword">data</span>/          <span class="comment">// Mock 数据源、Repository 实现</span></span><br><span class="line">├── ui/            <span class="comment">// 各页面 (Screen + ViewModel + components)</span></span><br><span class="line">└── di/            <span class="comment">// 依赖注入</span></span><br></pre></td></tr></table></figure>

<h3 id="依赖策略"><a href="#依赖策略" class="headerlink" title="依赖策略"></a>依赖策略</h3><ul>
<li>Compose BOM 2024.02.00（统一版本管理）</li>
<li>Navigation Compose 2.7.7</li>
<li>Lifecycle ViewModel Compose 2.7.0</li>
<li><strong>最小化依赖原则</strong>：只添加必需库</li>
</ul>
<h2 id="代码实现：Android-Dev-输出"><a href="#代码实现：Android-Dev-输出" class="headerlink" title="代码实现：Android Dev 输出"></a>代码实现：Android Dev 输出</h2><p>Android Dev 基于架构设计完成了完整的代码实现：</p>
<h3 id="代码统计"><a href="#代码统计" class="headerlink" title="代码统计"></a>代码统计</h3><table>
<thead>
<tr>
<th>类型</th>
<th>数量</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>Kotlin 文件</td>
<td>47 个</td>
<td>完整 Android 项目</td>
</tr>
<tr>
<td>页面</td>
<td>5 个</td>
<td>Home&#x2F;Tasks&#x2F;Agents&#x2F;Logs&#x2F;Kanban</td>
</tr>
<tr>
<td>ViewModel</td>
<td>5 个</td>
<td>每个页面对应一个 ViewModel</td>
</tr>
<tr>
<td>Repository 接口</td>
<td>3 个</td>
<td>Task&#x2F;Agent&#x2F;SessionLog</td>
</tr>
<tr>
<td>Repository 实现</td>
<td>3 个</td>
<td>对应的 Mock 实现</td>
</tr>
<tr>
<td>UseCase</td>
<td>3 个</td>
<td>GetTasks&#x2F;GetAgents&#x2F;GetSessionLogs</td>
</tr>
<tr>
<td>UiState</td>
<td>5 个</td>
<td>统一状态管理</td>
</tr>
</tbody></table>
<h3 id="Mock-数据"><a href="#Mock-数据" class="headerlink" title="Mock 数据"></a>Mock 数据</h3><ul>
<li><strong>5 个 Agent</strong>：PM、Architect、Developer、QA、Reviewer</li>
<li><strong>20 个任务</strong>：覆盖各种状态和优先级</li>
<li><strong>51 条会话日志</strong>：包含决策、风险、阻塞、建议等类型</li>
</ul>
<blockquote>
<p>[!success] 架构符合度<br>最终架构与设计文档的符合度达到 **100%**，这是经过 Reviewer 两轮审查后确认的结果。</p>
</blockquote>
<h2 id="代码审查：Reviewer-的关键作用"><a href="#代码审查：Reviewer-的关键作用" class="headerlink" title="代码审查：Reviewer 的关键作用"></a>代码审查：Reviewer 的关键作用</h2><p>Reviewer 是整个团队质量把控的核心环节。让我详细记录这次审查过程：</p>
<h3 id="第一轮审查：发现严重问题"><a href="#第一轮审查：发现严重问题" class="headerlink" title="第一轮审查：发现严重问题"></a>第一轮审查：发现严重问题</h3><p>Reviewer 发现代码实现与架构设计存在显著偏离：</p>
<table>
<thead>
<tr>
<th>问题</th>
<th>等级</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>无 ViewModel 层</td>
<td><strong>高</strong></td>
<td>使用 <code>remember</code> 而非 StateFlow</td>
</tr>
<tr>
<td>无 Repository 接口</td>
<td><strong>高</strong></td>
<td>无法切换真实数据源</td>
</tr>
<tr>
<td>无 UseCase 层</td>
<td><strong>高</strong></td>
<td>业务逻辑混入 UI，无法测试</td>
</tr>
<tr>
<td>状态管理混乱</td>
<td><strong>高</strong></td>
<td>配置变更会导致状态丢失</td>
</tr>
<tr>
<td>代码重复</td>
<td>中</td>
<td>AgentRole 颜色映射多处重复</td>
</tr>
</tbody></table>
<p><strong>可维护性评分</strong>：1.8&#x2F;5（低）</p>
<h3 id="返工修复"><a href="#返工修复" class="headerlink" title="返工修复"></a>返工修复</h3><p>Android Dev 基于审查反馈进行了全面返工：</p>
<ol>
<li><strong>添加 ViewModel 层</strong>：5 个 ViewModel 全部创建</li>
<li><strong>创建 Repository 接口</strong>：3 个接口 + 3 个实现</li>
<li><strong>实现 UseCase 类</strong>：3 个 UseCase 封装业务逻辑</li>
<li><strong>配置 Hilt AppModule</strong>：依赖注入完整配置</li>
<li><strong>统一 UiState</strong>：5 个 UiState 数据类管理状态</li>
</ol>
<h3 id="第二轮审查：确认通过"><a href="#第二轮审查：确认通过" class="headerlink" title="第二轮审查：确认通过"></a>第二轮审查：确认通过</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">架构符合度：33% → 100%</span><br></pre></td></tr></table></figure>

<blockquote>
<p>[!warning] 经验教训<br>如果没有 Reviewer 的主动审查，这些架构问题会被带到后续开发中，后期修复成本将增加 3-5 倍。</p>
</blockquote>
<h2 id="测试设计：QA-Agent-输出"><a href="#测试设计：QA-Agent-输出" class="headerlink" title="测试设计：QA Agent 输出"></a>测试设计：QA Agent 输出</h2><p>QA Agent 设计了完整的测试方案：</p>
<h3 id="测试用例清单（60-用例）"><a href="#测试用例清单（60-用例）" class="headerlink" title="测试用例清单（60+用例）"></a>测试用例清单（60+用例）</h3><table>
<thead>
<tr>
<th>类型</th>
<th>用例数</th>
<th>覆盖范围</th>
</tr>
</thead>
<tbody><tr>
<td>功能测试</td>
<td>33</td>
<td>任务面板(8)、Agent协作(8)、会话日志(9)、项目看板(8)</td>
</tr>
<tr>
<td>边界测试</td>
<td>16</td>
<td>状态转换、数值范围、并发等</td>
</tr>
<tr>
<td>异常测试</td>
<td>10</td>
<td>网络错误、JSON解析失败、空数据等</td>
</tr>
</tbody></table>
<h3 id="回归检查清单"><a href="#回归检查清单" class="headerlink" title="回归检查清单"></a>回归检查清单</h3><p>60 项检查点覆盖 4 个核心模块，确保每次变更都不会引入新的问题。</p>
<h3 id="测试金字塔"><a href="#测试金字塔" class="headerlink" title="测试金字塔"></a>测试金字塔</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">      /\</span><br><span class="line">     /  \     E2E Tests (少量)</span><br><span class="line">    /----\</span><br><span class="line">   /      \   Integration Tests (适量)</span><br><span class="line">  /--------\</span><br><span class="line"> /          \  Unit Tests (大量)</span><br><span class="line">/____________\</span><br></pre></td></tr></table></figure>

<h2 id="构建问题与解决"><a href="#构建问题与解决" class="headerlink" title="构建问题与解决"></a>构建问题与解决</h2><p>项目构建过程中遇到了几个典型问题，这里分享解决方案：</p>
<h3 id="问题1：kotlin-plugin-compose-不存在"><a href="#问题1：kotlin-plugin-compose-不存在" class="headerlink" title="问题1：kotlin.plugin.compose 不存在"></a>问题1：kotlin.plugin.compose 不存在</h3><p><strong>错误信息</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Plugin [id: &#x27;org.jetbrains.kotlin.plugin.compose&#x27;, version: &#x27;1.9.22&#x27;] was not found</span><br></pre></td></tr></table></figure>

<p><strong>原因</strong>：这个插件是 Kotlin 2.0+ 才有的特性，而项目使用的是 Kotlin 1.9.22</p>
<p><strong>解决方案</strong>：</p>
<ol>
<li>移除 <code>libs.versions.toml</code> 中的 <code>kotlin-compose</code> plugin</li>
<li>在 <code>app/build.gradle.kts</code> 添加：<figure class="highlight kotlin"><table><tr><td class="code"><pre><span class="line">composeOptions &#123;</span><br><span class="line">    kotlinCompilerExtensionVersion = <span class="string">&quot;1.5.8&quot;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ol>
<h3 id="问题2：缺少-gradle-properties"><a href="#问题2：缺少-gradle-properties" class="headerlink" title="问题2：缺少 gradle.properties"></a>问题2：缺少 gradle.properties</h3><p><strong>错误信息</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">android.useAndroidX property is not enabled</span><br></pre></td></tr></table></figure>

<p><strong>解决方案</strong>：创建 <code>gradle.properties</code>：</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="attr">android.useAndroidX</span>=<span class="string">true</span></span><br><span class="line"><span class="attr">android.enableJetifier</span>=<span class="string">true</span></span><br></pre></td></tr></table></figure>

<h3 id="问题3：缺少-Launcher-图标"><a href="#问题3：缺少-Launcher-图标" class="headerlink" title="问题3：缺少 Launcher 图标"></a>问题3：缺少 Launcher 图标</h3><p><strong>错误信息</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">resource mipmap/ic_launcher not found</span><br></pre></td></tr></table></figure>

<p><strong>解决方案</strong>：创建自适应图标资源文件：</p>
<ul>
<li><code>mipmap-anydpi-v26/ic_launcher.xml</code></li>
<li><code>drawable/ic_launcher_foreground.xml</code></li>
<li><code>values/colors.xml</code></li>
</ul>
<h2 id="最终产物"><a href="#最终产物" class="headerlink" title="最终产物"></a>最终产物</h2><p><img data-src="/images/posts/%E5%A4%9AAgent%E5%8D%8F%E4%BD%9C%E5%BC%80%E5%8F%91Android%E5%BA%94%E7%94%A8%E5%AE%9E%E6%88%98%E6%80%BB%E7%BB%93/app-preview.png" alt="App界面预览"><br><em>图3：Agent Team Lab 应用界面</em></p>
<table>
<thead>
<tr>
<th>产物</th>
<th>路径</th>
<th>规格</th>
</tr>
</thead>
<tbody><tr>
<td>APK</td>
<td><code>android-app/app/build/outputs/apk/debug/app-debug.apk</code></td>
<td>17.6 MB</td>
</tr>
<tr>
<td>项目代码</td>
<td><code>E:\abcd\andkaifaliansho\android-app\</code></td>
<td>47 个 Kotlin 文件</td>
</tr>
<tr>
<td>技术文档</td>
<td><code>E:\abcd\andkaifaliansho\docs\</code></td>
<td>10 个 MD 文件</td>
</tr>
</tbody></table>
<h3 id="构建验证"><a href="#构建验证" class="headerlink" title="构建验证"></a>构建验证</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">./gradlew assembleDebug</span><br><span class="line"><span class="comment"># BUILD SUCCESSFUL in 1m 19s</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p>[!success] 最终状态<br>APK 可直接安装运行，使用 Mock 数据无需后台服务，可离线使用。</p>
</blockquote>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><p>通过这次多 Agent 协作实验，我总结了以下几点关键经验：</p>
<h3 id="1-多-Agent-协作完全可行"><a href="#1-多-Agent-协作完全可行" class="headerlink" title="1. 多 Agent 协作完全可行"></a>1. 多 Agent 协作完全可行</h3><ul>
<li>分工明确，产出清晰</li>
<li>PM→Architect→Dev→Reviewer 依赖链有效运转</li>
<li>最终产出质量有保证</li>
</ul>
<h3 id="2-Reviewer-角色不可或缺"><a href="#2-Reviewer-角色不可或缺" class="headerlink" title="2. Reviewer 角色不可或缺"></a>2. Reviewer 角色不可或缺</h3><ul>
<li>主动发现架构问题</li>
<li>从 33% 到 100% 的提升</li>
<li>2 轮审查确保代码质量</li>
</ul>
<h3 id="3-Mock-数据优先策略"><a href="#3-Mock-数据优先策略" class="headerlink" title="3. Mock 数据优先策略"></a>3. Mock 数据优先策略</h3><ul>
<li>前期不接真实后端</li>
<li>快速验证 UI 和业务逻辑</li>
<li>降低复杂度，加速迭代</li>
</ul>
<h3 id="4-Build-Early-Build-Often"><a href="#4-Build-Early-Build-Often" class="headerlink" title="4. Build Early, Build Often"></a>4. Build Early, Build Often</h3><ul>
<li>尽早执行 gradle build</li>
<li>及时发现配置和依赖问题</li>
<li>避免问题积累到后期</li>
</ul>
<h3 id="5-并行优化提升效率"><a href="#5-并行优化提升效率" class="headerlink" title="5. 并行优化提升效率"></a>5. 并行优化提升效率</h3><ul>
<li>QA&#x2F;Docs 可在设计阶段并行工作</li>
<li>Reviewer 可做预研准备</li>
<li>整体效率提升约 40%</li>
</ul>
<h2 id="后续扩展方向"><a href="#后续扩展方向" class="headerlink" title="后续扩展方向"></a>后续扩展方向</h2><p>项目目前是 MVP 状态，还有很多可以扩展的方向：</p>
<table>
<thead>
<tr>
<th>方向</th>
<th>内容</th>
</tr>
</thead>
<tbody><tr>
<td>功能扩展</td>
<td>看板拖拽、日志导出、实时推送</td>
</tr>
<tr>
<td>数据源</td>
<td>接入真实后端 API</td>
</tr>
<tr>
<td>协作增强</td>
<td>多端实时协同、复杂权限体系</td>
</tr>
<tr>
<td>测试自动化</td>
<td>Espresso + Rest Assured 自动化测试</td>
</tr>
</tbody></table>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>这次实验证明，<strong>多 Agent 协作模式在软件开发中是可行的</strong>。通过明确分工、结构化交付物、严格审查，可以有效提升开发效率同时保证代码质量。</p>
<p>更重要的是，这种模式让我们看到了 AI 在软件开发中的更大潜力：<strong>不是替代开发者，而是像助手一样分担不同专业化的工作</strong>。</p>
<p>如果你对多 Agent 协作开发感兴趣，欢迎尝试搭建自己的 Agent 团队。有任何问题，欢迎交流讨论。</p>
<hr>
<p><em>项目源码：<a href="https://github.com/example/agent-team-lab">https://github.com/example/agent-team-lab</a></em></p>
]]></content>
      <categories>
        <category>AI经验</category>
      </categories>
      <tags>
        <tag>多Agent协作</tag>
        <tag>Android开发</tag>
        <tag>团队协作</tag>
        <tag>CleanArchitecture</tag>
        <tag>MVVM</tag>
      </tags>
  </entry>
  <entry>
    <title>多园区改造测试全景：从登录到业务隔离的 7 大模块验证清单</title>
    <url>/2026/07/08/%E5%A4%9A%E5%9B%AD%E5%8C%BA%E6%94%B9%E9%80%A0%E6%B5%8B%E8%AF%95%E5%85%A8%E6%99%AF/</url>
    <content><![CDATA[<p>多园区改造真正难的不是写代码，而是<strong>怎么证明它没漏</strong>。登录态、菜单、角色、同步链路、业务数据、回归兼容——任何一个模块漏测，都可能在生产里埋雷。</p>
<p>本文把改造测试要覆盖的 <strong>7 大模块、3 个优先级、5 条上线前最后检查</strong>完整拆开。无论你是第一次做多园区改造，还是已经在做第 N 次，这套清单都能直接复用。</p>
<span id="more"></span>

<div class="audio-player">
  <div class="audio-section">
    <h4>🎧 文章导读</h4>
    <audio controls preload="metadata">
      <source src="/audio/多园区改造测试全景/narration.mp3" type="audio/mpeg">
    </audio>
  </div>
  <div class="audio-section">
    <h4>🎵 背景音乐</h4>
    <audio controls loop preload="metadata">
      <source src="/audio/多园区改造测试全景/bgm.mp3" type="audio/mpeg">
    </audio>
  </div>
</div>

<p><img data-src="/images/posts/%E5%A4%9A%E5%9B%AD%E5%8C%BA%E6%94%B9%E9%80%A0%E6%B5%8B%E8%AF%95%E5%85%A8%E6%99%AF/fig1.png" alt="多园区改造 7 大测试模块一览"></p>
<p><em>图1：从登录到业务隔离的 7 大测试模块全景</em></p>
<h2 id="一、目标与边界"><a href="#一、目标与边界" class="headerlink" title="一、目标与边界"></a>一、目标与边界</h2><h3 id="1-1-本轮测试要确认的三件事"><a href="#1-1-本轮测试要确认的三件事" class="headerlink" title="1.1 本轮测试要确认的三件事"></a>1.1 本轮测试要确认的三件事</h3><p>多园区改造后，所有跨园区逻辑都要”按当前园区生效”，但<strong>不能</strong>把 A 园区的数据、角色、设备下发到 B 园区。本轮重点确认：</p>
<ol>
<li>登录用户能看到、能切换的园区范围正确。</li>
<li>切换园区后，角色、菜单、人员、业务列表都按当前 <code>park_id</code> 生效。</li>
<li>钉钉同步、<code>sys_user_park</code>、<code>pass_user</code>、设备下发状态没有因为多园区改造产生脏数据或跨园区下发。</li>
</ol>
<h3 id="1-2-不测什么"><a href="#1-2-不测什么" class="headerlink" title="1.2 不测什么"></a>1.2 不测什么</h3><p>明确不测的边界，让团队对齐预期：</p>
<table>
<thead>
<tr>
<th>不测项</th>
<th>原因</th>
</tr>
</thead>
<tbody><tr>
<td>全量性能压测</td>
<td>本轮只做关键列表分页和批量同步的抽样观察</td>
</tr>
<tr>
<td>历史业务字段全覆盖</td>
<td>只看多园区相关字段是否影响原有主流程</td>
</tr>
<tr>
<td>自动化测试框架新建</td>
<td>本轮以接口 + SQL + 前端手工验证为主</td>
</tr>
</tbody></table>
<blockquote>
<p>没有”测全”的项目，只有”够用 + 关键路径有保障”的项目。多园区改造的测试，第一优先级永远是<strong>越权和跨园区下发</strong>。</p>
</blockquote>
<h2 id="二、测试前准备：环境、账号、SQL-自检"><a href="#二、测试前准备：环境、账号、SQL-自检" class="headerlink" title="二、测试前准备：环境、账号、SQL 自检"></a>二、测试前准备：环境、账号、SQL 自检</h2><h3 id="2-1-环境和基础数据"><a href="#2-1-环境和基础数据" class="headerlink" title="2.1 环境和基础数据"></a>2.1 环境和基础数据</h3><p>启动 <code>service-gateway</code>、<code>admin-service</code>、<code>through-service</code>，按需再起 <code>property-service</code>、<code>security-service</code>、<code>env-service</code>。dev 环境走网关请求时带 <code>devDebug=1</code>：</p>
<figure class="highlight http"><table><tr><td class="code"><pre><span class="line">http://127.0.0.1:&lt;gateway-port&gt;/gateway/admin/xxx?devDebug=1</span><br></pre></td></tr></table></figure>

<p>准备两个有效园区 + 一个租户：</p>
<table>
<thead>
<tr>
<th>标识</th>
<th>含义</th>
<th>要求</th>
</tr>
</thead>
<tbody><tr>
<td>PARK_A</td>
<td>主测园区 A</td>
<td>有用户、角色、设备&#x2F;业务数据</td>
</tr>
<tr>
<td>PARK_B</td>
<td>对照园区 B</td>
<td>有不同用户、角色、设备&#x2F;业务数据</td>
</tr>
<tr>
<td>TENANT_1</td>
<td>当前租户</td>
<td>两个园区同租户，方便排除租户变量</td>
</tr>
</tbody></table>
<p>准备测试账号矩阵：</p>
<table>
<thead>
<tr>
<th>用户</th>
<th>数据要求</th>
<th>用途</th>
</tr>
</thead>
<tbody><tr>
<td><code>super_admin</code></td>
<td>可看全部或管理多个园区</td>
<td>验证管理端全量能力</td>
</tr>
<tr>
<td><code>admin_a</code></td>
<td>只属于 PARK_A</td>
<td>验证单园区隔离</td>
</tr>
<tr>
<td><code>admin_b</code></td>
<td>只属于 PARK_B</td>
<td>验证单园区隔离</td>
</tr>
<tr>
<td><code>admin_ab</code></td>
<td>同时属于 PARK_A、PARK_B</td>
<td>验证园区切换</td>
</tr>
<tr>
<td><code>normal_a</code></td>
<td>PARK_A 普通用户</td>
<td>验证无管理权限场景</td>
</tr>
<tr>
<td><code>ding_user</code></td>
<td>钉钉同步用户</td>
<td>验证 <code>sys_user_park</code> 和 <code>pass_user</code></td>
</tr>
</tbody></table>
<p>准备角色矩阵：</p>
<table>
<thead>
<tr>
<th>角色</th>
<th>数据要求</th>
<th>用途</th>
</tr>
</thead>
<tbody><tr>
<td><code>role_a</code></td>
<td><code>sys_role.park_id = PARK_A</code></td>
<td>A 园区角色</td>
</tr>
<tr>
<td><code>role_b</code></td>
<td><code>sys_role.park_id = PARK_B</code></td>
<td>B 园区角色</td>
</tr>
<tr>
<td><code>role_manage_ab</code></td>
<td>所属园区为 PARK_A，管理园区包含 PARK_B</td>
<td>验证角色管理园区</td>
</tr>
<tr>
<td><code>role_global_or_empty</code></td>
<td>如项目存在空园区&#x2F;通用角色</td>
<td>验证兼容历史数据</td>
</tr>
</tbody></table>
<h3 id="2-2-测试前的-SQL-自检清单"><a href="#2-2-测试前的-SQL-自检清单" class="headerlink" title="2.2 测试前的 SQL 自检清单"></a>2.2 测试前的 SQL 自检清单</h3><p>测试前先跑这 4 条 SQL，<strong>预期前 3 条返回 0 行</strong>：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 1. 用户所属园区关系不能重复</span></span><br><span class="line"><span class="keyword">SELECT</span> user_id, park_id, <span class="built_in">COUNT</span>(<span class="operator">*</span>) cnt</span><br><span class="line"><span class="keyword">FROM</span> sys_user_park</span><br><span class="line"><span class="keyword">WHERE</span> delete_flag <span class="operator">=</span> <span class="string">&#x27;0&#x27;</span></span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> user_id, park_id</span><br><span class="line"><span class="keyword">HAVING</span> cnt <span class="operator">&gt;</span> <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 2. 用户角色同一园区内不能重复</span></span><br><span class="line"><span class="keyword">SELECT</span> user_id, role_id, park_id, <span class="built_in">COUNT</span>(<span class="operator">*</span>) cnt</span><br><span class="line"><span class="keyword">FROM</span> sys_user_role</span><br><span class="line"><span class="keyword">WHERE</span> delete_flag <span class="operator">=</span> <span class="string">&#x27;0&#x27;</span></span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> user_id, role_id, park_id</span><br><span class="line"><span class="keyword">HAVING</span> cnt <span class="operator">&gt;</span> <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 3. 用户角色指向的园区应存在</span></span><br><span class="line"><span class="keyword">SELECT</span> ur.user_id, ur.role_id, ur.park_id</span><br><span class="line"><span class="keyword">FROM</span> sys_user_role ur</span><br><span class="line"><span class="keyword">LEFT</span> <span class="keyword">JOIN</span> sys_park p <span class="keyword">ON</span> p.park_id <span class="operator">=</span> ur.park_id <span class="keyword">AND</span> p.delete_flag <span class="operator">=</span> <span class="string">&#x27;0&#x27;</span></span><br><span class="line"><span class="keyword">WHERE</span> ur.delete_flag <span class="operator">=</span> <span class="string">&#x27;0&#x27;</span></span><br><span class="line">  <span class="keyword">AND</span> ur.park_id <span class="keyword">IS</span> <span class="keyword">NOT NULL</span></span><br><span class="line">  <span class="keyword">AND</span> ur.park_id <span class="operator">&lt;&gt;</span> <span class="string">&#x27;&#x27;</span></span><br><span class="line">  <span class="keyword">AND</span> p.park_id <span class="keyword">IS</span> <span class="keyword">NULL</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 4. pass_user 的园区不能为空（除非明确是历史占位数据）</span></span><br><span class="line"><span class="keyword">SELECT</span> user_id, user_name, phone, park_id, delete_flag, dist_sync_status</span><br><span class="line"><span class="keyword">FROM</span> pass_user</span><br><span class="line"><span class="keyword">WHERE</span> delete_flag <span class="operator">=</span> <span class="string">&#x27;0&#x27;</span></span><br><span class="line">  <span class="keyword">AND</span> (park_id <span class="keyword">IS</span> <span class="keyword">NULL</span> <span class="keyword">OR</span> park_id <span class="operator">=</span> <span class="string">&#x27;&#x27;</span>);</span><br></pre></td></tr></table></figure>

<p>第 4 条如有历史数据，要先标记出来——后续测试不要混入这些账号，否则测试结果会被脏数据污染。</p>
<h2 id="三、7-大测试模块拆解"><a href="#三、7-大测试模块拆解" class="headerlink" title="三、7 大测试模块拆解"></a>三、7 大测试模块拆解</h2><h3 id="A-登录与当前园区上下文"><a href="#A-登录与当前园区上下文" class="headerlink" title="A. 登录与当前园区上下文"></a>A. 登录与当前园区上下文</h3><p><strong>目的</strong>：登录态下的园区上下文必须可信，切换园区不能错位。</p>
<table>
<thead>
<tr>
<th>用例</th>
<th>操作</th>
<th>预期</th>
</tr>
</thead>
<tbody><tr>
<td>A1 单园区用户登录</td>
<td>用 <code>admin_a</code> 登录并查 <code>/user/info</code></td>
<td>当前园区为 PARK_A；园区列表只包含 PARK_A；不出现 PARK_B 的角色、园区名、业务数据</td>
</tr>
<tr>
<td>A2 多园区用户登录</td>
<td>用 <code>admin_ab</code> 登录</td>
<td>园区列表包含 PARK_A、PARK_B；默认园区有明确规则，不能随机跳；当前园区字段、角色列表、菜单列表三者一致</td>
</tr>
<tr>
<td>A3 切换园区</td>
<td><code>admin_ab</code> 切到 PARK_B，再切回 PARK_A</td>
<td>切到 PARK_B 后只返回 PARK_B 下生效的角色和数据；切回 PARK_A 后恢复 PARK_A 的角色和数据；刷新页面后当前园区不丢失</td>
</tr>
<tr>
<td>A4 非授权园区切换</td>
<td><code>admin_a</code> 调用切换接口传 PARK_B；用不存在&#x2F;已删除的 <code>park_id</code> 再调一次</td>
<td>接口失败；当前园区不变；不新增 <code>sys_user_role</code>、<code>sys_user_park</code>、缓存上下文等脏数据</td>
</tr>
</tbody></table>
<blockquote>
<p><strong>A4 是越权拦截的关键用例</strong>。如果非授权 <code>parkId</code> 没被拦下，会被切园区到其他园区——这是上线后最容易出安全事件的入口。</p>
</blockquote>
<h3 id="B-用户所属园区"><a href="#B-用户所属园区" class="headerlink" title="B. 用户所属园区"></a>B. 用户所属园区</h3><table>
<thead>
<tr>
<th>用例</th>
<th>关键校验</th>
</tr>
</thead>
<tbody><tr>
<td>B1 用户列表展示所属园区</td>
<td><code>userParkIds</code>、<code>userParkName</code> 与权限范围一致；多园区用户的园区名称和 ID 顺序一致；PARK_B-only 用户不会因为无关角色混进 PARK_A 结果</td>
</tr>
<tr>
<td>B2 编辑用户所属园区</td>
<td>给 <code>normal_a</code> 增加 PARK_B 后 <code>sys_user_park</code> 出现两条有效关系；移除 PARK_B 后软删；重复保存不会产生重复有效行</td>
</tr>
<tr>
<td>B3 删除或停用用户</td>
<td>停用用户不能登录；相关有效关系不再让该用户进入园区；不误删其他用户的 <code>sys_user_park</code> 或 <code>sys_user_role</code></td>
</tr>
</tbody></table>
<h3 id="C-角色管理园区"><a href="#C-角色管理园区" class="headerlink" title="C. 角色管理园区"></a>C. 角色管理园区</h3><table>
<thead>
<tr>
<th>用例</th>
<th>关键校验</th>
</tr>
</thead>
<tbody><tr>
<td>C1 创建&#x2F;编辑角色所属园区</td>
<td><code>sys_role.park_id = PARK_A</code>；PARK_B 普通管理员不可见或不可操作</td>
</tr>
<tr>
<td>C2 配置角色管理园区</td>
<td><code>sys_role_park</code> 有唯一有效的 <code>(role_id, PARK_B)</code>；重复保存不产生重复行；删除后接口不再返回 PARK_B</td>
</tr>
<tr>
<td>C3 角色列表字段</td>
<td>所属园区和管理园区不混淆；搜索和分页不会破坏园区过滤；软删园区或软删角色不出现在有效结果中</td>
</tr>
</tbody></table>
<h3 id="D-用户角色分配"><a href="#D-用户角色分配" class="headerlink" title="D. 用户角色分配"></a>D. 用户角色分配</h3><p>这是<strong>最容易出问题</strong>的模块。多园区用户的角色关系要按 <code>(user_id, role_id, park_id)</code> 区分，否则切园区时菜单会乱套。</p>
<table>
<thead>
<tr>
<th>用例</th>
<th>关键校验</th>
</tr>
</thead>
<tbody><tr>
<td>D1 单园区分配角色</td>
<td>只新增&#x2F;更新 <code>(normal_a, role_a, PARK_A)</code>；PARK_B 没有被写入同角色关系</td>
</tr>
<tr>
<td>D2 同一用户多园区分配同一角色</td>
<td><code>sys_user_role</code> 按 <code>user_id + role_id + park_id</code> 区分；切 PARK_A 只加载 PARK_A 的角色，切 PARK_B 只加载 PARK_B 的角色</td>
</tr>
<tr>
<td>D3 清空某园区角色</td>
<td>对 <code>admin_ab</code> 调用角色保存接口，<code>parkId = PARK_A</code>，角色列表为空——只清空 PARK_A 的角色，PARK_B 不受影响</td>
</tr>
<tr>
<td>D4 非授权园区角色分配</td>
<td>接口拒绝；不写入 <code>sys_user_role</code>；错误提示可定位到无权限或园区不合法</td>
</tr>
</tbody></table>
<blockquote>
<p>D3 是验证”清空不影响其他园区”的关键。如果代码里写错 <code>DELETE WHERE user_id = ?</code>（不带 park_id），整个用户的所有园区角色都会被清空，这是上线后常见的回归 bug。</p>
</blockquote>
<h3 id="E-园区管理员列表"><a href="#E-园区管理员列表" class="headerlink" title="E. 园区管理员列表"></a>E. 园区管理员列表</h3><table>
<thead>
<tr>
<th>用例</th>
<th>关键校验</th>
</tr>
</thead>
<tbody><tr>
<td>E1 查询某园区管理员</td>
<td>PARK_A 只返回 <code>sys_user_role.park_id = PARK_A</code> 的有效用户角色；已删除用户、已删除角色、孤立 user_id 不返回；排序按绑定时间倒序</td>
</tr>
<tr>
<td>E2 手机号&#x2F;用户名展示</td>
<td>手机号如有加密，接口返回前端期望的明文或脱敏格式；角色名、园区名和 ID 对得上</td>
</tr>
</tbody></table>
<h3 id="F-钉钉同步到用户园区"><a href="#F-钉钉同步到用户园区" class="headerlink" title="F. 钉钉同步到用户园区"></a>F. 钉钉同步到用户园区</h3><p>钉钉同步是”数据进入系统的入口”，错了之后下游所有模块都会脏。</p>
<table>
<thead>
<tr>
<th>用例</th>
<th>关键校验</th>
</tr>
</thead>
<tbody><tr>
<td>F1 有部门用户同步</td>
<td><code>sys_user_park.park_id = PARK_A</code>；<code>sys_user_park.org_id</code> 存公司级组织 ID；重复同步不产生重复有效关系</td>
</tr>
<tr>
<td>F2 多部门&#x2F;跨园区用户同步</td>
<td>用户能得到 PARK_A、PARK_B 两个有效园区关系；登录后可切换两个园区；每个园区下角色仍按 <code>sys_user_role.park_id</code> 决定</td>
</tr>
<tr>
<td>F3 无法识别园区的钉钉用户</td>
<td>不写错误的默认 <code>park_id</code>；不生成空 <code>park_id</code> 的 <code>sys_user_park</code>；日志能说明跳过原因</td>
</tr>
</tbody></table>
<h3 id="G-sys-user-到-pass-user-同步"><a href="#G-sys-user-到-pass-user-同步" class="headerlink" title="G. sys_user 到 pass_user 同步"></a>G. sys_user 到 pass_user 同步</h3><p>这是设备下发的源头。如果 <code>pass_user.park_id</code> 错了，门禁&#x2F;车行下发就会拿错人员。</p>
<table>
<thead>
<tr>
<th>用例</th>
<th>关键校验</th>
</tr>
</thead>
<tbody><tr>
<td>G1 新用户同步到 pass_user</td>
<td>新增 <code>pass_user.park_id = PARK_A</code>；<code>delete_flag = &#39;0&#39;</code>；需要下发设备时 <code>dist_sync_status = &#39;PENDING&#39;</code></td>
</tr>
<tr>
<td>G2 已存在 pass_user 更新</td>
<td>只更新发生变化的字段；园区变更能同步到 <code>pass_user.park_id</code>；需要重新下发时才标记 <code>PENDING</code></td>
</tr>
<tr>
<td>G3 多园区用户 pass_user 策略</td>
<td>一人一条时必须有明确主园区；一人多园区时必须每条能区分园区；不允许同一设备下发拿错园区人员</td>
</tr>
<tr>
<td>G4 软删用户同步</td>
<td><code>pass_user</code> 按项目约定软删或停用；已软删记录不被下发任务重新捞起；<code>findByDistSyncStatus(&#39;PENDING&#39;)</code> 不返回 <code>delete_flag != &#39;0&#39;</code> 的人员</td>
</tr>
</tbody></table>
<blockquote>
<p>G3 在产品未定义一人多园区下发策略时，<strong>要标为待确认，不要强行验收</strong>。强行写死策略反而会埋雷。</p>
</blockquote>
<h3 id="H-业务数据园区隔离"><a href="#H-业务数据园区隔离" class="headerlink" title="H. 业务数据园区隔离"></a>H. 业务数据园区隔离</h3><p>每个已改造业务模块至少抽 <strong>2 个列表、1 个新增、1 个详情、1 个编辑</strong>。优先测经常被首页或大屏使用的接口。</p>
<p><img data-src="/images/posts/%E5%A4%9A%E5%9B%AD%E5%8C%BA%E6%94%B9%E9%80%A0%E6%B5%8B%E8%AF%95%E5%85%A8%E6%99%AF/fig2.png" alt="业务数据园区隔离的多服务矩阵"></p>
<p><em>图2：admin&#x2F;through&#x2F;property&#x2F;security&#x2F;env 各业务的园区隔离验证矩阵</em></p>
<table>
<thead>
<tr>
<th>用例</th>
<th>关键校验</th>
</tr>
</thead>
<tbody><tr>
<td>H1 列表隔离</td>
<td>PARK_A 只返回 A 数据；PARK_B 只返回 B 数据；不传 <code>park_id</code> 时以后端当前园区为准，不让前端伪造越权</td>
</tr>
<tr>
<td>H2 新增数据自动带当前园区</td>
<td>新增记录的 <code>park_id</code> 等于当前园区；前端传其他 <code>park_id</code> 时，后端按权限规则覆盖或拒绝，不能越权写入</td>
</tr>
<tr>
<td>H3 详情&#x2F;编辑&#x2F;删除越权</td>
<td>在 PARK_A 下拿 PARK_B 数据 ID 调详情 → 不可见或返回无权限；编辑、删除失败；数据库无变化</td>
</tr>
<tr>
<td>H4 DEFAULT_PARK_ID 回归</td>
<td>重点检查 env、security、through 里历史写死 <code>DEFAULT_PARK_ID</code> 的入口；走新增或同步入口，查数据库实际 <code>park_id</code>；已改造入口不再写死默认园区</td>
</tr>
</tbody></table>
<h3 id="I-前端页面联调"><a href="#I-前端页面联调" class="headerlink" title="I. 前端页面联调"></a>I. 前端页面联调</h3><p>前端是用户接触的第一线，错位显示（菜单列表与数据不一致）会直接被业务方发现。</p>
<table>
<thead>
<tr>
<th>用例</th>
<th>关键校验</th>
</tr>
</thead>
<tbody><tr>
<td>I1 顶部园区切换器</td>
<td>切换器显示当前园区；列表自动刷新或提示刷新；刷新后当前园区符合产品约定</td>
</tr>
<tr>
<td>I2 用户&#x2F;角色页面</td>
<td>字段展示完整，不错位；下拉园区来源符合接口返回；保存后页面立即反映最新关系</td>
</tr>
<tr>
<td>I3 无权限提示</td>
<td>前端不展示无权限按钮或页面；后端仍然拒绝越权请求；提示不暴露敏感数据</td>
</tr>
</tbody></table>
<h3 id="J-回归测试"><a href="#J-回归测试" class="headerlink" title="J. 回归测试"></a>J. 回归测试</h3><table>
<thead>
<tr>
<th>用例</th>
<th>关键校验</th>
</tr>
</thead>
<tbody><tr>
<td>J1 原单园区流程</td>
<td>操作路径和改造前一致；不因为多园区字段必填导致老流程失败</td>
</tr>
<tr>
<td>J2 历史空 park_id 数据</td>
<td>产品明确允许的通用数据仍可用；不允许的空园区数据不应出现在普通园区视图；需要清洗的数据单独列清单</td>
</tr>
<tr>
<td>J3 分页和搜索</td>
<td>count 和列表使用同一园区过滤条件；搜索不会绕过园区过滤；排序稳定</td>
</tr>
</tbody></table>
<h2 id="四、测试优先级"><a href="#四、测试优先级" class="headerlink" title="四、测试优先级"></a>四、测试优先级</h2><table>
<thead>
<tr>
<th>优先级</th>
<th>模块</th>
<th>失败影响</th>
</tr>
</thead>
<tbody><tr>
<td>第一优先级</td>
<td>A、D、G、H3</td>
<td>越权或下发错误（<strong>绝对不能漏</strong>）</td>
</tr>
<tr>
<td>第二优先级</td>
<td>B、C、E、F</td>
<td>管理端使用和同步准确性</td>
</tr>
<tr>
<td>第三优先级</td>
<td>I、J</td>
<td>联调体验和历史兼容</td>
</tr>
</tbody></table>
<p>如果时间只够半天，最低限度要跑：</p>
<ol>
<li><code>admin_a</code> 不能切 PARK_B。</li>
<li><code>admin_ab</code> 切 PARK_A&#x2F;PARK_B 后菜单和业务列表变化正确。</li>
<li>给同一用户分别保存 PARK_A、PARK_B 角色，互不覆盖。</li>
<li>清空 PARK_A 角色不影响 PARK_B。</li>
<li>钉钉同步用户能写入 <code>sys_user_park</code>。</li>
<li><code>pass_user</code> 没有空 <code>park_id</code>，新增或变更用户的下发状态符合预期。</li>
<li>PARK_A 下不能详情、编辑、删除 PARK_B 数据。</li>
</ol>
<h2 id="五、上线前最后检查-SQL"><a href="#五、上线前最后检查-SQL" class="headerlink" title="五、上线前最后检查 SQL"></a>五、上线前最后检查 SQL</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 1. 重复用户园区关系</span></span><br><span class="line"><span class="keyword">SELECT</span> user_id, park_id, <span class="built_in">COUNT</span>(<span class="operator">*</span>) cnt</span><br><span class="line"><span class="keyword">FROM</span> sys_user_park</span><br><span class="line"><span class="keyword">WHERE</span> delete_flag <span class="operator">=</span> <span class="string">&#x27;0&#x27;</span></span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> user_id, park_id</span><br><span class="line"><span class="keyword">HAVING</span> cnt <span class="operator">&gt;</span> <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 2. 重复用户角色关系</span></span><br><span class="line"><span class="keyword">SELECT</span> user_id, role_id, park_id, <span class="built_in">COUNT</span>(<span class="operator">*</span>) cnt</span><br><span class="line"><span class="keyword">FROM</span> sys_user_role</span><br><span class="line"><span class="keyword">WHERE</span> delete_flag <span class="operator">=</span> <span class="string">&#x27;0&#x27;</span></span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> user_id, role_id, park_id</span><br><span class="line"><span class="keyword">HAVING</span> cnt <span class="operator">&gt;</span> <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 3. 角色管理园区重复</span></span><br><span class="line"><span class="keyword">SELECT</span> role_id, park_id, <span class="built_in">COUNT</span>(<span class="operator">*</span>) cnt</span><br><span class="line"><span class="keyword">FROM</span> sys_role_park</span><br><span class="line"><span class="keyword">WHERE</span> delete_flag <span class="operator">=</span> <span class="string">&#x27;0&#x27;</span></span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> role_id, park_id</span><br><span class="line"><span class="keyword">HAVING</span> cnt <span class="operator">&gt;</span> <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 4. 有效 pass_user 空园区</span></span><br><span class="line"><span class="keyword">SELECT</span> user_id, user_name, phone, park_id, dist_sync_status</span><br><span class="line"><span class="keyword">FROM</span> pass_user</span><br><span class="line"><span class="keyword">WHERE</span> delete_flag <span class="operator">=</span> <span class="string">&#x27;0&#x27;</span></span><br><span class="line">  <span class="keyword">AND</span> (park_id <span class="keyword">IS</span> <span class="keyword">NULL</span> <span class="keyword">OR</span> park_id <span class="operator">=</span> <span class="string">&#x27;&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 5. 可能被园区切换拦住的历史用户角色</span></span><br><span class="line"><span class="keyword">SELECT</span> ur.user_id, ur.role_id, ur.park_id</span><br><span class="line"><span class="keyword">FROM</span> sys_user_role ur</span><br><span class="line"><span class="keyword">WHERE</span> ur.delete_flag <span class="operator">=</span> <span class="string">&#x27;0&#x27;</span></span><br><span class="line">  <span class="keyword">AND</span> ur.park_id <span class="keyword">IS</span> <span class="keyword">NOT NULL</span></span><br><span class="line">  <span class="keyword">AND</span> ur.park_id <span class="operator">&lt;&gt;</span> <span class="string">&#x27;&#x27;</span></span><br><span class="line">  <span class="keyword">AND</span> <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> (<span class="keyword">SELECT</span> <span class="number">1</span> <span class="keyword">FROM</span> sys_role r <span class="keyword">WHERE</span> r.role_id <span class="operator">=</span> ur.role_id <span class="keyword">AND</span> r.park_id <span class="operator">=</span> ur.park_id <span class="keyword">AND</span> r.delete_flag <span class="operator">=</span> <span class="string">&#x27;0&#x27;</span>)</span><br><span class="line">  <span class="keyword">AND</span> <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> (<span class="keyword">SELECT</span> <span class="number">1</span> <span class="keyword">FROM</span> sys_role_park rp <span class="keyword">WHERE</span> rp.role_id <span class="operator">=</span> ur.role_id <span class="keyword">AND</span> rp.park_id <span class="operator">=</span> ur.park_id <span class="keyword">AND</span> rp.delete_flag <span class="operator">=</span> <span class="string">&#x27;0&#x27;</span>)</span><br><span class="line">  <span class="keyword">AND</span> <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> (<span class="keyword">SELECT</span> <span class="number">1</span> <span class="keyword">FROM</span> sys_user_park up <span class="keyword">WHERE</span> up.user_id <span class="operator">=</span> ur.user_id <span class="keyword">AND</span> up.park_id <span class="operator">=</span> ur.park_id <span class="keyword">AND</span> up.delete_flag <span class="operator">=</span> <span class="string">&#x27;0&#x27;</span>);</span><br></pre></td></tr></table></figure>

<p>预期：上线前 1、2、3、4 返回 0 行；第 5 条如果有结果，要么补 <code>sys_role_park</code>，要么补 <code>sys_user_park</code>，要么确认这些历史角色关系应清理。</p>
<h2 id="六、经验总结"><a href="#六、经验总结" class="headerlink" title="六、经验总结"></a>六、经验总结</h2><h3 id="6-1-三个最容易漏的测试场景"><a href="#6-1-三个最容易漏的测试场景" class="headerlink" title="6.1 三个最容易漏的测试场景"></a>6.1 三个最容易漏的测试场景</h3><ol>
<li><strong>D3 清空某园区角色</strong>：很多团队会测”加”，但不测”清空”。”清空”代码路径通常用 <code>DELETE WHERE user_id = ?</code>，忘记带 <code>park_id</code> 就会清掉所有园区。</li>
<li><strong>G3 多园区用户 pass_user 策略</strong>：一人一条 vs 一人多园区，下发策略完全不同，但产品常常没有明确定义。</li>
<li><strong>H4 DEFAULT_PARK_ID 回归</strong>：历史代码里写死的 <code>DEFAULT_PARK_ID</code> 是改造最大的遗留风险。</li>
</ol>
<h3 id="6-2-编写测试用例的三个原则"><a href="#6-2-编写测试用例的三个原则" class="headerlink" title="6.2 编写测试用例的三个原则"></a>6.2 编写测试用例的三个原则</h3><ol>
<li><strong>操作步骤最小化</strong>：每条用例只验证一个意图，混入多个验证点会让失败定位变难。</li>
<li><strong>预期可观测</strong>：预期要写数据库字段、接口字段、缓存键值，不能写”系统正常运行”。</li>
<li><strong>失败用例优先</strong>：先写”非授权切换”这种应当失败的用例，再写正常通过的用例——前者一旦通过就证明拦截有效。</li>
</ol>
<h3 id="6-3-测试记录模板"><a href="#6-3-测试记录模板" class="headerlink" title="6.3 测试记录模板"></a>6.3 测试记录模板</h3><p>每轮测试记录建议结构：</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="section">## 测试记录 - YYYY-MM-DD</span></span><br><span class="line"></span><br><span class="line"><span class="section">### 环境</span></span><br><span class="line"><span class="bullet">-</span> 分支：</span><br><span class="line"><span class="bullet">-</span> admin-service commit：</span><br><span class="line"><span class="bullet">-</span> through-service commit：</span><br><span class="line"><span class="bullet">-</span> gateway commit：</span><br><span class="line"><span class="bullet">-</span> 数据库：</span><br><span class="line"><span class="bullet">-</span> 测试账号：</span><br><span class="line"></span><br><span class="line"><span class="section">### 执行结果</span></span><br><span class="line">| 用例 | 结果 | 备注 |</span><br><span class="line">| --- | --- | --- |</span><br><span class="line">| A1 单园区用户登录 | 未测/通过/失败 |  |</span><br><span class="line">| A2 多园区用户登录 | 未测/通过/失败 |  |</span><br><span class="line">| A3 切换园区 | 未测/通过/失败 |  |</span><br><span class="line">| ... |  |  |</span><br><span class="line"></span><br><span class="line"><span class="section">### 问题清单</span></span><br><span class="line">| 编号 | 问题 | 复现步骤 | 影响 | 处理人 | 状态 |</span><br><span class="line">| --- | --- | --- | --- | --- | --- |</span><br><span class="line"></span><br><span class="line"><span class="section">### 数据清理</span></span><br><span class="line"><span class="bullet">-</span> 新增测试用户：</span><br><span class="line"><span class="bullet">-</span> 新增测试角色：</span><br><span class="line"><span class="bullet">-</span> 新增测试园区关系：</span><br><span class="line"><span class="bullet">-</span> 是否已清理：</span><br></pre></td></tr></table></figure>

<h2 id="七、结语"><a href="#七、结语" class="headerlink" title="七、结语"></a>七、结语</h2><p>多园区改造不是”加几个字段”那么简单——它是把整个系统的数据访问维度从”全局”切成”按当前园区”。**测试要验证的不是”能不能跑”，而是”会不会跑串”**。本文的 7 大模块清单 + 3 个优先级 + 上线前 5 条 SQL，能覆盖 90% 的多园区改造回归场景。下次接到多园区改造需求，直接把这份清单贴到测试计划里就行。</p>
<blockquote>
<p>多园区改造测试的最高原则：<strong>A 园区发生的事，绝不能在 B 园区看到。</strong></p>
</blockquote>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>南网</category>
      </categories>
      <tags>
        <tag>多园区</tag>
        <tag>南网项目</tag>
        <tag>测试计划</tag>
        <tag>越权校验</tag>
        <tag>数据隔离</tag>
      </tags>
  </entry>
  <entry>
    <title>广东水文项目问题排查实录：2026年3月19日技术挑战</title>
    <url>/2026/03/19/%E5%B9%BF%E4%B8%9C%E6%B0%B4%E6%96%87%E9%A1%B9%E7%9B%AE%E9%97%AE%E9%A2%98%E6%8E%92%E6%9F%A5%E5%AE%9E%E5%BD%952026%E5%B9%B43%E6%9C%8819%E6%97%A5%E6%8A%80%E6%9C%AF%E6%8C%91%E6%88%98/</url>
    <content><![CDATA[<p>2026年3月19日，对广东水文项目来说是一个充满挑战的工作日。在这一天的日常开发和维护中，我们接连遇到了三个不同层面的技术问题：数据采集层的数据缺失、业务层的数据表归属判断、以及数据展示层的时区和数据质量问题。本文将详细记录这三个问题的排查过程、根因分析以及修复方案，希望能为从事类似水文信息化项目的开发者提供一些参考和借鉴。</p>
<span id="more"></span>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>广东水文项目是一个综合性较强的水利信息化系统，涵盖了水位监测、雨量监测、河道水情、水库水情等多种水文数据的采集、存储、查询和展示功能。系统采用典型的微服务架构，后端基于 Spring Boot 框架构建，数据存储使用 MySQL 数据库，业务数据按照不同的测站类型和数据特征分表存储。</p>
<p>项目中有几个核心的数据实体需要理解：</p>
<ul>
<li><strong>测站（Station）</strong>：水文监测的基本单元，每个测站有一个唯一的测站编码（STCD），并且有一个测站类型（STTP）来标识其功能类型，如 ZZ 表示闸站、ZQ 表示河道站、RR 表示水库站、WD 表示水位站、YD 表示雨量站等。</li>
<li><strong>实时数据（Realtime Data）</strong>：最新的监测数据，通常以 5 分钟为采集间隔，包含水位、流量等关键指标。</li>
<li><strong>历史数据（Historical Data）</strong>：历史极值和历史记录，用于一站一档等分析展示功能。</li>
</ul>
<p>在3月19日这一天，我们主要处理了与”一站一档”和”雨情实时信息”这两个业务模块相关的问题，下面逐一展开说明。</p>
<h2 id="问题一：茜坑测站雨情实时数据缺失"><a href="#问题一：茜坑测站雨情实时数据缺失" class="headerlink" title="问题一：茜坑测站雨情实时数据缺失"></a>问题一：茜坑测站雨情实时数据缺失</h2><h3 id="问题现象"><a href="#问题现象" class="headerlink" title="问题现象"></a>问题现象</h3><p>3月19日上午，收到用户反馈：茜坑测站（测站编码 81103420）在”雨情实时信息”页面无法查询到数据。然而奇怪的是，同一个测站在”一站一档”模块中能够正常搜索并显示数据。</p>
<p>用户描述的现象是：</p>
<ul>
<li>进入”雨情实时信息”页面</li>
<li>搜索茜坑测站或直接输入测站编码 81103420</li>
<li>系统提示”没有数据”</li>
</ul>
<p>但是在”一站一档”模块中，该测站能够正常显示其基本信息和历史数据。</p>
<h3 id="排查过程"><a href="#排查过程" class="headerlink" title="排查过程"></a>排查过程</h3><p>我首先检查了该测站的基础信息。通过查询 <code>st_stbprp_b</code> 测站基础表，确认茜坑测站存在且状态正常：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> STCD, STNM, STTP, USFL</span><br><span class="line"><span class="keyword">FROM</span> st_stbprp_b</span><br><span class="line"><span class="keyword">WHERE</span> STCD <span class="operator">=</span> <span class="string">&#x27;81103420&#x27;</span></span><br></pre></td></tr></table></figure>

<p>返回结果显示该测站类型为 YD（雨量站），使用标志为 1（启用）。</p>
<p>接下来，我分别查询了该测站在不同雨量数据表中的记录数量：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 查询5分钟实时雨量表</span></span><br><span class="line"><span class="keyword">SELECT</span> STCD, <span class="built_in">COUNT</span>(<span class="operator">*</span>) <span class="keyword">as</span> cnt</span><br><span class="line"><span class="keyword">FROM</span> ST_PPTN_R_MIN</span><br><span class="line"><span class="keyword">WHERE</span> STCD <span class="operator">=</span> <span class="string">&#x27;81103420&#x27;</span></span><br><span class="line"><span class="keyword">AND</span> TM <span class="keyword">BETWEEN</span> <span class="string">&#x27;2026-03-16 11:41:00&#x27;</span> <span class="keyword">AND</span> <span class="string">&#x27;2026-03-19 11:41:00&#x27;</span></span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> STCD</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查询小时雨量表</span></span><br><span class="line"><span class="keyword">SELECT</span> STCD, <span class="built_in">COUNT</span>(<span class="operator">*</span>) <span class="keyword">as</span> cnt</span><br><span class="line"><span class="keyword">FROM</span> ST_PPTN_R</span><br><span class="line"><span class="keyword">WHERE</span> STCD <span class="operator">=</span> <span class="string">&#x27;81103420&#x27;</span></span><br><span class="line"><span class="keyword">AND</span> TM <span class="keyword">BETWEEN</span> <span class="string">&#x27;2026-03-16 11:41:00&#x27;</span> <span class="keyword">AND</span> <span class="string">&#x27;2026-03-19 11:41:00&#x27;</span></span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> STCD</span><br></pre></td></tr></table></figure>

<p>查询结果如下：</p>
<table>
<thead>
<tr>
<th>数据表</th>
<th>数据类型</th>
<th>记录数</th>
</tr>
</thead>
<tbody><tr>
<td>ST_PPTN_R_MIN</td>
<td>5分钟实时雨量</td>
<td><strong>0条</strong></td>
</tr>
<tr>
<td>ST_PPTN_R</td>
<td>小时雨量</td>
<td><strong>860条</strong></td>
</tr>
</tbody></table>
<h3 id="根因分析"><a href="#根因分析" class="headerlink" title="根因分析"></a>根因分析</h3><p>经过排查，问题的根因已经非常清晰：</p>
<p><strong>茜坑测站（81103420）只有小时雨量数据（ST_PPTN_R），没有 5 分钟实时数据（ST_PPTN_R_MIN）。</strong></p>
<p>从技术角度来看，这是数据采集层面的问题，而不是代码 Bug。测站硬件或上报配置决定了它只上报小时级别的雨量数据，而不是 5 分钟级别的实时数据。</p>
<p>系统架构设计中，雨情实时信息模块默认查询的是 5 分钟实时数据表（ST_PPTN_R_MIN），而茜坑测站在该表中没有任何数据，因此返回空结果。这解释了为什么”雨情实时信息”页面无法显示数据。</p>
<p>而”一站一档”模块能够正常显示，是因为它查询的是小时雨量表（ST_PPTN_R），该表中存在茜坑测站的 860 条数据。</p>
<h3 id="问题定性"><a href="#问题定性" class="headerlink" title="问题定性"></a>问题定性</h3><p>经过分析，我们认为这是一个<strong>数据可用性问题</strong>，而非代码缺陷。系统按照设计正常运作，只是茜坑测站本身不具备 5 分钟实时雨量数据的采集条件。</p>
<h3 id="可能的解决方案"><a href="#可能的解决方案" class="headerlink" title="可能的解决方案"></a>可能的解决方案</h3><p>针对这类问题，有几种潜在的解决方案可供考虑：</p>
<p><strong>方案一：前端降级处理</strong></p>
<p>当实时数据为空时，前端自动降级显示小时数据。这需要在 UI 层面增加逻辑判断，当检测到 5 分钟数据为空时，自动切换到小时数据视图，并给用户一个提示说明当前显示的是小时数据而非实时数据。</p>
<p><strong>方案二：后端数据聚合</strong></p>
<p>在后端处理层面，当查询 5 分钟数据为空时，自动从小时数据表中聚合数据返回。例如，可以将最近一小时的小时数据求和，作为实时数据的近似值返回。这种方案的优点是对前端透明，缺点是数据语义不够精确。</p>
<p><strong>方案三：业务确认与配置调整</strong></p>
<p>与水文部门确认茜坑测站的实际数据采集配置。如果该测站确实只需要上报小时数据，那么应该在系统配置中将其标记为”小时数据站”，前端据此显示不同的数据视图。</p>
<p>最终，我们与水文部门进行了沟通，确认茜坑测站由于硬件限制确实只支持小时数据采集，暂时采用方案一的前端降级处理作为短期解决方案，长期来看需要与设备部门协调升级采集设备。</p>
<h2 id="问题二：江口水闸实时水情查询失败"><a href="#问题二：江口水闸实时水情查询失败" class="headerlink" title="问题二：江口水闸实时水情查询失败"></a>问题二：江口水闸实时水情查询失败</h2><h3 id="问题现象-1"><a href="#问题现象-1" class="headerlink" title="问题现象"></a>问题现象</h3><p>下午时段，接到另一个问题反馈：江口水闸（测站编码 81210300）在实时水情查询中无法找到数据。同样的，该测站在”一站一档”模块中能够正常显示。</p>
<p>具体表现：</p>
<ul>
<li>在”实时水情”模块搜索江口水闸</li>
<li>系统提示没有数据</li>
<li>但在”一站一档”中能够看到该测站的基本信息和部分数据</li>
</ul>
<h3 id="系统排查"><a href="#系统排查" class="headerlink" title="系统排查"></a>系统排查</h3><p>首先检查测站基础信息和数据分布情况。通过 SQL 查询发现：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 检查测站基础信息</span></span><br><span class="line"><span class="keyword">SELECT</span> STCD, STNM, STTP, USFL</span><br><span class="line"><span class="keyword">FROM</span> st_stbprp_b</span><br><span class="line"><span class="keyword">WHERE</span> STCD <span class="operator">=</span> <span class="string">&#x27;81210300&#x27;</span></span><br></pre></td></tr></table></figure>

<p>江口水闸的基础信息正常，测站类型为 ZZ（闸站），状态启用。</p>
<p>然后分别查询该测站在不同实时数据表中的情况：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 检查河道实时表</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="built_in">COUNT</span>(<span class="operator">*</span>) <span class="keyword">FROM</span> st_river_r_min</span><br><span class="line"><span class="keyword">WHERE</span> STCD <span class="operator">=</span> <span class="string">&#x27;81210300&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">-- 检查水库实时表</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="built_in">COUNT</span>(<span class="operator">*</span>) <span class="keyword">FROM</span> st_rsvr_r_min</span><br><span class="line"><span class="keyword">WHERE</span> STCD <span class="operator">=</span> <span class="string">&#x27;81210300&#x27;</span></span><br></pre></td></tr></table></figure>

<p>查询结果：</p>
<table>
<thead>
<tr>
<th>数据表</th>
<th>数据类型</th>
<th>记录数</th>
</tr>
</thead>
<tbody><tr>
<td>st_river_r_min</td>
<td>河道实时数据</td>
<td><strong>0条</strong></td>
</tr>
<tr>
<td>st_rsvr_r_min</td>
<td>水库实时数据</td>
<td><strong>92条</strong></td>
</tr>
</tbody></table>
<p>这个结果非常有意思：江口水闸是一个 ZZ 类型（闸站）的测站，但其数据存储在水灾实时表中，而不是河道实时表中。</p>
<h3 id="根因分析-1"><a href="#根因分析-1" class="headerlink" title="根因分析"></a>根因分析</h3><p>经过深入分析，我们发现了问题的根本原因：</p>
<p><strong>代码按测站类型区分查询数据表，但江口水闸的数据实际存储在与类型不匹配的表中。</strong></p>
<p>系统的实时水情查询逻辑是：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 代码中的查询逻辑（修复前）</span></span><br><span class="line"><span class="keyword">if</span> (<span class="string">&quot;RR&quot;</span>.equals(base.getSttp())) &#123;</span><br><span class="line">    <span class="comment">// 水库类型 -&gt; 查询 st_rsvr_r_min</span></span><br><span class="line">    reservoirStations.add(stcd);</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="string">&quot;ZQ&quot;</span>.equals(base.getSttp())) &#123;</span><br><span class="line">    <span class="comment">// 河道类型 -&gt; 查询 st_river_r_min</span></span><br><span class="line">    riverStations.add(stcd);</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="string">&quot;ZZ&quot;</span>.equals(base.getSttp())) &#123;</span><br><span class="line">    <span class="comment">// 闸站类型 -&gt; 查询 st_river_r_min</span></span><br><span class="line">    riverStations.add(stcd);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>江口水闸的测站类型是 ZZ，按照代码逻辑会查询河道实时表（st_river_r_min），但实际上该测站的数据存储在水库存实时表中（st_rsvr_r_min），所以查询结果为空。</p>
<p>这是一种数据录入或数据迁移过程中产生的不一致问题：测站的类型属性与其实际数据存储位置不匹配。</p>
<h3 id="修复方案"><a href="#修复方案" class="headerlink" title="修复方案"></a>修复方案</h3><p>为了解决这个问题，我们在 <code>WaterRealtimeSummaryServiceImpl</code> 中增加了 fallback 机制：当 ZZ 类型测站在河道表中没有数据时，自动尝试查询水库存实时表。</p>
<p><strong>修改文件</strong>：WaterRealtimeSummaryServiceImpl（第 104-157 行附近）</p>
<p><strong>核心修改思路</strong>：</p>
<ol>
<li>新增 <code>zzStations</code> 列表记录所有 ZZ 类型测站</li>
<li>ZZ 类型优先查询河道表</li>
<li>新增 fallback 逻辑：如果河道表没有数据，则 fallback 查询水库存表</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 2. 分离河道站和水库站</span></span><br><span class="line">List&lt;String&gt; riverStations = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">List&lt;String&gt; reservoirStations = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">List&lt;String&gt; zzStations = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;(); <span class="comment">// 记录ZZ类型测站，用于fallback</span></span><br><span class="line"><span class="keyword">for</span> (String stcd : batch) &#123;</span><br><span class="line">    <span class="type">StationBaseInfoDTO</span> <span class="variable">base</span> <span class="operator">=</span> baseMap.get(stcd);</span><br><span class="line">    <span class="keyword">if</span> (base != <span class="literal">null</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="string">&quot;RR&quot;</span>.equals(base.getSttp())) &#123;</span><br><span class="line">            reservoirStations.add(stcd);</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="string">&quot;ZQ&quot;</span>.equals(base.getSttp())) &#123;</span><br><span class="line">            riverStations.add(stcd);</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="string">&quot;ZZ&quot;</span>.equals(base.getSttp())) &#123;</span><br><span class="line">            riverStations.add(stcd);  <span class="comment">// ZZ优先查河道表</span></span><br><span class="line">            zzStations.add(stcd);     <span class="comment">// 同时记录，用于fallback</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 3. 查询河道站实时数据（ST_RIVER_R_MIN）</span></span><br><span class="line"><span class="keyword">final</span> Map&lt;String, StRiverRMin&gt; riverDataMap;</span><br><span class="line"><span class="keyword">if</span> (!riverStations.isEmpty()) &#123;</span><br><span class="line">    riverDataMap = stRiverRMinMapper.selectByTimeAndStcds(targetTime, riverStations)</span><br><span class="line">            .stream().collect(Collectors.toMap(StRiverRMin::getSTCD, v -&gt; v, (a, b) -&gt; a));</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    riverDataMap = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;();</span><br><span class="line">&#125;</span><br><span class="line">riverRealtimeMap = riverDataMap;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 4. 查询水库站实时数据（ST_RSVR_R_MIN）</span></span><br><span class="line"><span class="keyword">final</span> Map&lt;String, StRsvrRMin&gt; reservoirDataMap;</span><br><span class="line"><span class="keyword">if</span> (!reservoirStations.isEmpty()) &#123;</span><br><span class="line">    reservoirDataMap = stRsvrRMinMapper.selectByTimeAndStcds(targetTime, reservoirStations)</span><br><span class="line">            .stream().collect(Collectors.toMap(StRsvrRMin::getSTCD, v -&gt; v, (a, b) -&gt; a));</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    reservoirDataMap = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;();</span><br><span class="line">&#125;</span><br><span class="line">reservoirRealtimeMap = reservoirDataMap;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 4.1 ZZ类型fallback - 如果河道表没数据，尝试查水库表</span></span><br><span class="line"><span class="comment">// 解决部分ZZ类型测站（如闸站）数据实际在水库表的问题</span></span><br><span class="line"><span class="keyword">if</span> (!zzStations.isEmpty()) &#123;</span><br><span class="line">    List&lt;String&gt; zzStationsWithoutData = zzStations.stream()</span><br><span class="line">            .filter(stcd -&gt; !riverDataMap.containsKey(stcd))</span><br><span class="line">            .collect(Collectors.toList());</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!zzStationsWithoutData.isEmpty()) &#123;</span><br><span class="line">        Map&lt;String, StRsvrRMin&gt; zzFallbackData = stRsvrRMinMapper</span><br><span class="line">                .selectByTimeAndStcds(targetTime, zzStationsWithoutData)</span><br><span class="line">                .stream()</span><br><span class="line">                .collect(Collectors.toMap(StRsvrRMin::getSTCD, v -&gt; v, (a, b) -&gt; a));</span><br><span class="line"></span><br><span class="line">        reservoirRealtimeMap.putAll(zzFallbackData);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="修复验证"><a href="#修复验证" class="headerlink" title="修复验证"></a>修复验证</h3><p>修复后，我们通过手动指定时间同步接口进行了验证：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">POST /api/water/summary/sync?stcds=81210300&amp;targetTime=2026-03-19 15:00:00</span><br></pre></td></tr></table></figure>

<p>验证结果成功返回数据：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;stnm&quot;</span><span class="punctuation">:</span> <span class="string">&quot;江口水闸&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;stcd&quot;</span><span class="punctuation">:</span> <span class="string">&quot;81210300&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;z&quot;</span><span class="punctuation">:</span> <span class="number">92.170</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;tm&quot;</span><span class="punctuation">:</span> <span class="string">&quot;2026-03-19 15:00:00&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;sttp&quot;</span><span class="punctuation">:</span> <span class="string">&quot;ZZ&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<h3 id="待修复问题：同步时间匹配"><a href="#待修复问题：同步时间匹配" class="headerlink" title="待修复问题：同步时间匹配"></a>待修复问题：同步时间匹配</h3><p>虽然上述修复解决了查询问题，但我们发现了一个关联的同步问题：</p>
<p><strong>现状</strong>：同步任务使用精确时间匹配（TM &#x3D; targetTime），例如查询 15:55 的数据。</p>
<p><strong>问题</strong>：江口水闸数据的时间戳是 15:00、14:15 等非整 5 分钟时刻，这导致自动同步时使用精确匹配无法找到数据。</p>
<p><strong>根本原因分析</strong>：</p>
<ol>
<li>同步任务计算出”上一个 5 分钟”的时间点（如 15:55）</li>
<li>SQL 查询使用 <code>TM = #&#123;tm&#125;</code> 精确匹配</li>
<li>但江口水闸上报的数据时间戳是 15:00、14:15 等非整 5 分钟时刻</li>
<li>精确匹配查不到数据，所以汇总表始终没有这条数据</li>
</ol>
<p><strong>初步建议的解决方案</strong>：</p>
<p>方案一是修改 SQL 为范围查询，查询 targetTime 之前的最近一条数据：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 查询最近的数据 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">&quot;selectLatestByTimeAndStcds&quot;</span> <span class="attr">resultMap</span>=<span class="string">&quot;StRsvrRMinResult&quot;</span>&gt;</span></span><br><span class="line">    SELECT STCD, TM, RZ, INQ, OTQ, W, RWPTN</span><br><span class="line">    FROM st_rsvr_r_min</span><br><span class="line">    WHERE TM &lt;= #&#123;tm&#125;</span><br><span class="line">      AND STCD IN</span><br><span class="line">      <span class="tag">&lt;<span class="name">foreach</span> <span class="attr">collection</span>=<span class="string">&quot;stcds&quot;</span> <span class="attr">item</span>=<span class="string">&quot;stcd&quot;</span> <span class="attr">open</span>=<span class="string">&quot;(&quot;</span> <span class="attr">separator</span>=<span class="string">&quot;,&quot;</span> <span class="attr">close</span>=<span class="string">&quot;)&quot;</span>&gt;</span></span><br><span class="line">          #&#123;stcd&#125;</span><br><span class="line">      <span class="tag">&lt;/<span class="name">foreach</span>&gt;</span></span><br><span class="line">      AND RZ IS NOT NULL</span><br><span class="line">    ORDER BY TM DESC</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>方案二是在 Service 层处理，多次尝试向前查找直到找到数据或超过阈值。</p>
<p>这个问题需要进一步评估影响范围后再实施修复。</p>
<h2 id="问题三：一站一档今日极值数据异常"><a href="#问题三：一站一档今日极值数据异常" class="headerlink" title="问题三：一站一档今日极值数据异常"></a>问题三：一站一档今日极值数据异常</h2><h3 id="问题描述"><a href="#问题描述" class="headerlink" title="问题描述"></a>问题描述</h3><p>下午晚些时候，收到关于”一站一档”模块的数据异常反馈，涉及两个不同的字段问题：</p>
<p><strong>问题 1</strong>：今日最低流量返回错误值 2.180，但实际数据库中存在更低的值 2.260</p>
<p><strong>问题 2</strong>：历史最低水位返回 -9999（这是一个无效值占位符），实际应为 0.040</p>
<p>这两个问题发生在测站 81500150（水位站，类型 WD）上。</p>
<h3 id="问题排查与分析"><a href="#问题排查与分析" class="headerlink" title="问题排查与分析"></a>问题排查与分析</h3><h4 id="问题-1：今日最低流量错误"><a href="#问题-1：今日最低流量错误" class="headerlink" title="问题 1：今日最低流量错误"></a>问题 1：今日最低流量错误</h4><p>首先检查今日最低流量的计算逻辑。通过对比数据库中的实际数据，我们发现：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 查询该测站今天（北京时间）的所有流量数据</span></span><br><span class="line"><span class="keyword">SELECT</span> STCD, TM, Q <span class="keyword">FROM</span> st_river_r</span><br><span class="line"><span class="keyword">WHERE</span> STCD <span class="operator">=</span> <span class="string">&#x27;81500150&#x27;</span></span><br><span class="line"><span class="keyword">AND</span> TM <span class="operator">&gt;=</span> <span class="string">&#x27;2026-03-19 00:00:00&#x27;</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> Q <span class="keyword">ASC</span></span><br><span class="line">LIMIT <span class="number">10</span></span><br></pre></td></tr></table></figure>

<p>实际查询结果显示最小的流量值确实是 2.260，但系统返回的今日最低流量却是 2.180。</p>
<p>经过仔细排查，我们发现问题的根因是<strong>时区不一致</strong>：</p>
<ol>
<li>数据库 <code>ST_RIVER_R</code> 表中存储的时间是 <strong>UTC 时间</strong></li>
<li>代码使用 <code>Calendar.getInstance()</code> 构建今日时间范围：<code>2026-03-19 00:00:00 ~ 23:59:59</code></li>
<li>MySQL 查询时把这个字符串当作 UTC 时间处理</li>
<li>导致实际查询范围变成了 <code>2026-03-19 08:00 ~ 2026-03-20 07:59</code>（北京时间）</li>
<li>漏掉了北京时间 00:00~07:59 的数据，而真正的最低值 2.260 恰好在这个时间段内（UTC 19:00 &#x3D; 北京时间 03:00）</li>
</ol>
<h4 id="问题-2：历史最低水位返回-9999"><a href="#问题-2：历史最低水位返回-9999" class="headerlink" title="问题 2：历史最低水位返回 -9999"></a>问题 2：历史最低水位返回 -9999</h4><p>历史最低水位的计算使用了 <code>ST_RVEVS_R</code> 表，通过 SQL 查询发现：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 原 SQL</span></span><br><span class="line"><span class="keyword">SELECT</span> STCD, LTZ, LTT <span class="keyword">FROM</span> st_rvevs_r</span><br><span class="line"><span class="keyword">WHERE</span> STCD <span class="operator">=</span> #&#123;stcd&#125;</span><br><span class="line"><span class="keyword">AND</span> LTZ <span class="keyword">IS</span> <span class="keyword">NOT NULL</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> LTZ <span class="keyword">ASC</span></span><br><span class="line">LIMIT <span class="number">1</span></span><br></pre></td></tr></table></figure>

<p>问题在于 <code>ST_RVEVS_R</code> 表中存在 <code>-9999</code> 这种无效值占位符，原始 SQL 只过滤了 <code>NULL</code>，没有过滤负数，导致 -9999 被当作有效值返回。</p>
<h3 id="修复方案-1"><a href="#修复方案-1" class="headerlink" title="修复方案"></a>修复方案</h3><h4 id="1-SQL-修改"><a href="#1-SQL-修改" class="headerlink" title="1. SQL 修改"></a>1. SQL 修改</h4><p>修改 <code>StRvevsRMapper.xml</code> 中的 <code>selectHistoricalMinLevel</code> SQL，添加 <code>AND LTZ &gt; 0</code> 过滤条件：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 修改前</span></span><br><span class="line"><span class="keyword">WHERE</span> STCD <span class="operator">=</span> #&#123;stcd&#125;</span><br><span class="line"><span class="keyword">AND</span> LTZ <span class="keyword">IS</span> <span class="keyword">NOT NULL</span></span><br><span class="line"></span><br><span class="line"><span class="comment">-- 修改后</span></span><br><span class="line"><span class="keyword">WHERE</span> STCD <span class="operator">=</span> #&#123;stcd&#125;</span><br><span class="line"><span class="keyword">AND</span> LTZ <span class="keyword">IS</span> <span class="keyword">NOT NULL</span></span><br><span class="line"><span class="keyword">AND</span> LTZ <span class="operator">&gt;</span> <span class="number">0</span></span><br></pre></td></tr></table></figure>

<h4 id="2-Java-修改：时区转换逻辑"><a href="#2-Java-修改：时区转换逻辑" class="headerlink" title="2. Java 修改：时区转换逻辑"></a>2. Java 修改：时区转换逻辑</h4><p>修改 <code>StationArchiveBiz.java</code> 中的 <code>fillTodayExtremes()</code> 方法，使用 <code>java.time</code> 正确处理时区转换：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 北京时区</span></span><br><span class="line"><span class="type">ZoneId</span> <span class="variable">beijingZone</span> <span class="operator">=</span> ZoneId.of(<span class="string">&quot;Asia/Shanghai&quot;</span>);</span><br><span class="line"><span class="type">ZoneId</span> <span class="variable">utcZone</span> <span class="operator">=</span> ZoneId.of(<span class="string">&quot;UTC&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 获取北京时间的今天开始和结束</span></span><br><span class="line"><span class="type">ZonedDateTime</span> <span class="variable">beijingStart</span> <span class="operator">=</span> ZonedDateTime.now(beijingZone).toLocalDate().atStartOfDay(beijingZone);</span><br><span class="line"><span class="type">ZonedDateTime</span> <span class="variable">beijingEnd</span> <span class="operator">=</span> beijingStart.plusDays(<span class="number">1</span>).minusSeconds(<span class="number">1</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 转换为 UTC 时间用于数据库查询</span></span><br><span class="line"><span class="type">ZonedDateTime</span> <span class="variable">utcStart</span> <span class="operator">=</span> beijingStart.withZoneSameInstant(utcZone);</span><br><span class="line"><span class="type">ZonedDateTime</span> <span class="variable">utcEnd</span> <span class="operator">=</span> beijingEnd.withZoneSameInstant(utcZone);</span><br></pre></td></tr></table></figure>

<p>同样的时区转换逻辑也需要应用到水库站的今日极值计算方法 <code>fillTodayReservoirExtremes()</code> 中。</p>
<h3 id="修复验证-1"><a href="#修复验证-1" class="headerlink" title="修复验证"></a>修复验证</h3><p>修复后，两个问题都得到了正确的结果：</p>
<table>
<thead>
<tr>
<th>字段</th>
<th>修复前</th>
<th>修复后</th>
<th>状态</th>
</tr>
</thead>
<tbody><tr>
<td>historicalMinLevel</td>
<td>-9999.0</td>
<td>0.04</td>
<td>正常</td>
</tr>
<tr>
<td>historicalMinLevelTime</td>
<td>2013-02-21</td>
<td>2015-01-08</td>
<td>正常</td>
</tr>
<tr>
<td>todayMinFlow</td>
<td>2.180</td>
<td>2.260</td>
<td>正常</td>
</tr>
<tr>
<td>todayMinFlowTime</td>
<td>16:00（错误）</td>
<td>03:00（正确）</td>
<td>正常</td>
</tr>
</tbody></table>
<p>正确的 UTC 查询范围应该是：</p>
<ul>
<li>北京时间 2026-03-19 &#x3D; UTC <code>2026-03-18 16:00:00</code> ~ <code>2026-03-19 15:59:59</code></li>
<li>该范围内最小流量为 <strong>2.260</strong>（UTC 19:00 &#x3D; 北京时间 03:00）</li>
</ul>
<h3 id="影响范围评估"><a href="#影响范围评估" class="headerlink" title="影响范围评估"></a>影响范围评估</h3><p>此次修复的影响范围仅限于以下接口的今日极值计算：</p>
<ul>
<li><code>/basic/station/archive/monitoring/&#123;stcd&#125;?sttp=WD&amp;subType=realtime</code>（河道站）</li>
<li><code>/basic/station/archive/reservoir/&#123;stcd&#125;</code>（水库站）</li>
</ul>
<p>不受影响的部分包括：</p>
<ul>
<li>用户传入的 startTime&#x2F;endTime 参数（用户自行处理时区）</li>
<li>历史极值（除 historicalMinLevel 外）</li>
<li>其他业务接口</li>
</ul>
<h2 id="经验总结与技术思考"><a href="#经验总结与技术思考" class="headerlink" title="经验总结与技术思考"></a>经验总结与技术思考</h2><p>通过这一天的问题排查和修复工作，我们总结出以下几点经验教训，希望能对类似项目的开发者提供一些参考。</p>
<h3 id="1-时区问题是分布式系统中常见的数据陷阱"><a href="#1-时区问题是分布式系统中常见的数据陷阱" class="headerlink" title="1. 时区问题是分布式系统中常见的数据陷阱"></a>1. 时区问题是分布式系统中常见的数据陷阱</h3><p>在广东水文项目中，数据库服务器、应用服务器可能处于不同的时区配置下，而水文数据采集又涉及多个地区的数据汇总，时区问题很容易被忽视。</p>
<p>本次修复的时区问题是一个非常典型的案例：数据库存储 UTC 时间，代码使用本地时间构建查询条件，两者的不一致导致了数据查询范围的偏差，最终造成了极值计算错误。</p>
<p><strong>建议</strong>：在系统设计阶段就应该明确规定时间的存储格式和查询方式，推荐做法是：</p>
<ul>
<li>统一使用 UTC 时间存储</li>
<li>在数据访问层统一进行时区转换</li>
<li>在配置文件中显式声明时区设置</li>
<li>在接口文档中明确标注时间格式</li>
</ul>
<h3 id="2-无效值占位符需要在数据入口处统一处理"><a href="#2-无效值占位符需要在数据入口处统一处理" class="headerlink" title="2. 无效值占位符需要在数据入口处统一处理"></a>2. 无效值占位符需要在数据入口处统一处理</h3><p>水文数据中常见使用 -9999、-99、NULL 等作为无效值占位符。这种设计在数据采集和存储层面是合理的，但在数据展示和分析层面必须进行过滤。</p>
<p>本次修复中，历史最低水位查询就是因为没有过滤 -9999 这个无效值，导致返回了明显错误的结果。</p>
<p><strong>建议</strong>：建立统一的数据清洗和过滤规范，在以下位置进行处理：</p>
<ul>
<li>数据入库前的校验层</li>
<li>数据查询的 Mapper 层</li>
<li>数据展示的 Service 层</li>
</ul>
<h3 id="3-测站类型与数据存储位置可能存在不一致"><a href="#3-测站类型与数据存储位置可能存在不一致" class="headerlink" title="3. 测站类型与数据存储位置可能存在不一致"></a>3. 测站类型与数据存储位置可能存在不一致</h3><p>项目中经常会出现这样的情况：测站的类型属性（STTP）与实际数据存储位置不匹配。这可能是由于数据迁移、系统升级或人工录入错误导致的。</p>
<p>江口水闸的问题就是一个典型案例：测站类型为 ZZ（闸站），但数据存储在水库存实时表中而非河道实时表中。这种不一致性会导致按照类型判断数据表的代码逻辑失效。</p>
<p><strong>建议</strong>：</p>
<ul>
<li>在数据入口处增加校验，当测站类型与数据表不匹配时给出警告</li>
<li>在查询逻辑中增加 fallback 机制，提高系统的容错能力</li>
<li>定期进行数据一致性检查，发现并修复类型与数据不匹配的问题</li>
</ul>
<h3 id="4-数据可用性问题需要业务层面配合解决"><a href="#4-数据可用性问题需要业务层面配合解决" class="headerlink" title="4. 数据可用性问题需要业务层面配合解决"></a>4. 数据可用性问题需要业务层面配合解决</h3><p>茜坑测站的问题本质上不是代码问题，而是数据采集层面的问题。测站硬件只支持小时数据采集，无法提供 5 分钟实时数据。对于这类问题，技术手段只能做到优雅降级或数据聚合，无法从根本上解决数据缺失的问题。</p>
<p><strong>建议</strong>：建立测站能力档案，在系统中记录每个测站的数据采集能力和数据提供情况，让用户清楚地了解每个测站的数据范围和限制。</p>
<h3 id="5-代码审查和单元测试的重要性"><a href="#5-代码审查和单元测试的重要性" class="headerlink" title="5. 代码审查和单元测试的重要性"></a>5. 代码审查和单元测试的重要性</h3><p>这三个问题有一个共同点：它们都是由于代码逻辑在边界情况下处理不当导致的。如果有完善的单元测试覆盖这些边界场景，这些问题应该在开发阶段就被发现和修复。</p>
<p><strong>建议</strong>：</p>
<ul>
<li>加强对边界条件和异常情况的测试覆盖</li>
<li>定期进行代码审查，特别是涉及数据表切换、时区处理、数值边界等逻辑的代码</li>
<li>建立持续集成环境，在代码提交时自动运行相关测试</li>
</ul>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>2026年3月19日的这次问题排查经历，让我们深刻认识到水文信息化系统的复杂性和挑战性。从数据采集层的硬件限制，到数据存储层的数据表设计，再到数据展示层的时区和数据质量问题，每一个环节都可能成为问题的来源。</p>
<p>作为后端开发工程师，我们不仅要关注代码的功能实现，更要关注数据在各个环节的正确流转；不仅要处理正常的业务逻辑，更要考虑各种异常和边界情况；不仅要修复当前的问题，更要思考如何建立长效机制避免类似问题的再次发生。</p>
<p>水文信息化关乎国计民生，数据的准确性和系统的稳定性至关重要。希望本文的分享能为从事相关项目的开发者提供一些帮助，也欢迎大家交流探讨，共同进步。</p>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>水文</category>
      </categories>
      <tags>
        <tag>Bug修复</tag>
        <tag>水文项目</tag>
        <tag>时区问题</tag>
        <tag>数据表设计</tag>
      </tags>
  </entry>
  <entry>
    <title>广东水文项目接口改造实战：字段扩展与性能优化</title>
    <url>/2026/03/20/%E5%B9%BF%E4%B8%9C%E6%B0%B4%E6%96%87%E9%A1%B9%E7%9B%AE%E6%8E%A5%E5%8F%A3%E6%94%B9%E9%80%A0%E5%AE%9E%E6%88%98/</url>
    <content><![CDATA[<p>本文记录了广东水文项目中两个典型接口改造的技术实现过程，详细描述了从需求分析、方案设计到最终落地的完整技术路径。第一个改造是<strong>测站关联列表接口</strong>添加水位流量字段，涉及多数据源切换、大表查询优化等挑战；第二个改造是<strong>设备分类接口</strong>扩展测站基础信息字段，展示了跨数据源关联查询的典型应用场景。通过这两个案例，分享我在实际项目中积累的数据库设计优化经验。</p>
<span id="more"></span>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>广东水文项目是一个涉及多数据源、多表关联的水利信息化系统，承载着广东省境内河流、水库、湖泊等水文测站的实时监测数据管理。系统需要整合来自不同数据库的业务数据，为前端应用提供统一的 API 接口服务。</p>
<p>在实际开发过程中，我遇到了两个典型的接口改造需求：</p>
<ol>
<li><strong>测站关联列表接口</strong>需要添加最新水位和流量字段，用于列表页面直接展示水情数据</li>
<li><strong>设备分类接口</strong>需要附加测站的流域、河流、管理单位、行政区等基础信息</li>
</ol>
<p>这两个需求看似简单，但在实现过程中涉及到<strong>多数据源路由</strong>、<strong>大表查询性能</strong>、<strong>跨数据库关联</strong>等关键技术点。本文将详细记录整个技术实现过程，包括遇到的坑以及最终的解决方案。</p>
<h2 id="项目技术背景"><a href="#项目技术背景" class="headerlink" title="项目技术背景"></a>项目技术背景</h2><h3 id="系统架构概述"><a href="#系统架构概述" class="headerlink" title="系统架构概述"></a>系统架构概述</h3><p>广东水文项目采用典型的 <strong>Spring Boot + MyBatis</strong> 技术栈，后端服务通过 MyBatis-Plus 提供的 <code>@DataSource</code> 注解实现多数据源动态切换。系统连接两个核心数据库：</p>
<table>
<thead>
<tr>
<th>数据源</th>
<th>用途</th>
<th>代表性数据表</th>
</tr>
</thead>
<tbody><tr>
<td><code>WATER</code></td>
<td>测站基础信息</td>
<td>ST_STBPRP_B（测站基本信息表）</td>
</tr>
<tr>
<td><code>hydrology</code></td>
<td>水文实时数据</td>
<td>ST_RIVER_R_MIN（河道水情表）、ST_RSVL_R_MIN（水库水情表）</td>
</tr>
</tbody></table>
<h3 id="数据库设计特点"><a href="#数据库设计特点" class="headerlink" title="数据库设计特点"></a>数据库设计特点</h3><p>测站基础信息（如流域、河流、管理单位等）存储在 <code>WATER</code> 数据源的 <code>ST_STBPRP_B</code> 表中，这是一张相对稳定的配置表，数据量适中。而水文实时数据（如水位、流量等）存储在 <code>hydrology</code> 数据源的分钟级汇总表中，数据量庞大，亿级记录规模。</p>
<p>这种分库设计的好处是：</p>
<ul>
<li><strong>冷热分离</strong>：基础配置信息与实时监测数据物理隔离</li>
<li><strong>读写分离</strong>：大表查询不影响基础信息查询</li>
<li><strong>职责单一</strong>：各数据源关注各自的业务领域</li>
</ul>
<p>但同时也带来了跨库关联查询的复杂性。</p>
<h2 id="需求一：测站关联列表添加水位流量字段"><a href="#需求一：测站关联列表添加水位流量字段" class="headerlink" title="需求一：测站关联列表添加水位流量字段"></a>需求一：测站关联列表添加水位流量字段</h2><h3 id="1-1-需求背景"><a href="#1-1-需求背景" class="headerlink" title="1.1 需求背景"></a>1.1 需求背景</h3><p>前端团队需要在<strong>测站关联列表页面</strong>直接展示每个测站的最新水情数据，包括<strong>水位（waterLevel）</strong>和<strong>流量（flow）</strong>。原来的接口只返回测站的基本关联信息，不包含实时水情数据。</p>
<p>如果每次展示水情都需要单独调用一次接口，会造成：</p>
<ul>
<li><strong>前端请求次数增加</strong>：列表 100 条数据就要发 100 次请求</li>
<li><strong>用户体验下降</strong>：额外的数据加载等待时间</li>
<li><strong>服务器压力增大</strong>：不必要的网络开销</li>
</ul>
<p>因此，前端团队提出了在列表接口中直接返回水位和流量字段的合理需求。</p>
<h3 id="1-2-数据来源分析"><a href="#1-2-数据来源分析" class="headerlink" title="1.2 数据来源分析"></a>1.2 数据来源分析</h3><p>不同类型的测站对应不同的数据表和字段：</p>
<table>
<thead>
<tr>
<th>测站类型</th>
<th>类型代码</th>
<th>数据表</th>
<th>水位字段</th>
<th>流量字段</th>
</tr>
</thead>
<tbody><tr>
<td>水文站</td>
<td>ZQ</td>
<td>ST_RIVER_R_MIN</td>
<td>Z</td>
<td>Q</td>
</tr>
<tr>
<td>水位站</td>
<td>ZZ</td>
<td>ST_RIVER_R_MIN</td>
<td>Z</td>
<td>Q</td>
</tr>
<tr>
<td>潮位站</td>
<td>TT</td>
<td>ST_RIVER_R_MIN</td>
<td>Z</td>
<td>Q</td>
</tr>
<tr>
<td>水库站</td>
<td>RR</td>
<td>ST_RSVR_R_MIN</td>
<td>RZ</td>
<td>INQ（入库流量）</td>
</tr>
<tr>
<td>其他类型</td>
<td>PP等</td>
<td>-</td>
<td>null</td>
<td>null</td>
</tr>
</tbody></table>
<p>可以看到：</p>
<ul>
<li><strong>河道类测站</strong>（ZQ&#x2F;ZZ&#x2F;TT）使用 <code>ST_RIVER_R_MIN</code> 表，水位字段为 <code>Z</code>，流量字段为 <code>Q</code></li>
<li><strong>水库类测站</strong>（RR）使用 <code>ST_RSVR_R_MIN</code> 表，水位字段为 <code>RZ</code>，流量字段为 <code>INQ</code></li>
<li><strong>其他类型</strong>测站不返回水情数据</li>
</ul>
<h3 id="1-3-方案演进：从失败到成功"><a href="#1-3-方案演进：从失败到成功" class="headerlink" title="1.3 方案演进：从失败到成功"></a>1.3 方案演进：从失败到成功</h3><h4 id="方案一：直接查询原始表（失败）"><a href="#方案一：直接查询原始表（失败）" class="headerlink" title="方案一：直接查询原始表（失败）"></a>方案一：直接查询原始表（失败）</h4><p>最早的方案是直接查询原始的分钟级数据表，核心代码逻辑如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 在 StationAssociationBiz 中直接查询实时数据</span></span><br><span class="line"><span class="meta">@DataSource(WATER)</span>  <span class="comment">// 尝试切换数据源</span></span><br><span class="line"><span class="keyword">public</span> Map&lt;String, Map&lt;String, BigDecimal&gt;&gt; <span class="title function_">fetchLatestWaterData</span><span class="params">(List&lt;String&gt; riverStcds, List&lt;String&gt; rsvrStcds)</span> &#123;</span><br><span class="line">    <span class="comment">// 查询河道站水情</span></span><br><span class="line">    List&lt;RiverData&gt; riverData = stRiverRMinMapper.selectLatestByStcds(riverStcds);</span><br><span class="line">    <span class="comment">// 查询水库站水情</span></span><br><span class="line">    List&lt;RsvrData&gt; rsvrData = stRsvrRMinMapper.selectLatestByStcds(rsvrStcds);</span><br><span class="line">    <span class="comment">// 合并结果...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个方案在开发环境中测试时没有问题，但<strong>一上线就出现了严重问题</strong>：</p>
<p><strong>问题 1：@DataSource 注解不生效</strong></p>
<p>Spring AOP 的拦截机制决定了 <code>@DataSource</code> 注解只能拦截 <code>Service</code> 层方法（标注了 <code>@Service</code> 的类），而 <code>StationAssociationBiz</code> 使用的是 <code>@Component</code> 注解。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span>  <span class="comment">// Biz 层是 Component，不会被 Service 层的 AOP 拦截</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">StationAssociationBiz</span> &#123;</span><br><span class="line">    <span class="meta">@DataSource(WATER)</span>  <span class="comment">// 这个注解不会生效！</span></span><br><span class="line">    <span class="keyword">public</span> List&lt;StationAssociation&gt; <span class="title function_">listAssociations</span><span class="params">(String groupId)</span> &#123;</span><br><span class="line">        <span class="comment">// ...</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>根因分析</strong>：Spring AOP 的代理机制遵循”代理穿透”原则——外层代理可以拦截内层调用，但同层调用不走代理。在 Biz 层方法上标注 <code>@DataSource</code>，由于 Biz 是被 Controller 直接调用的（而不是通过 Service 代理），AOP 拦截根本不会触发。</p>
<p><strong>问题 2：原始表查询超时</strong></p>
<p>即使解决了数据源切换问题，还有一个更严重的问题——<code>ST_RIVER_R_MIN</code> 表是<strong>亿级数据量</strong>的大表。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 最初的 SQL 设计：查询每种测站的最新数据 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">&quot;selectLatestByStcds&quot;</span> <span class="attr">resultType</span>=<span class="string">&quot;map&quot;</span>&gt;</span></span><br><span class="line">    SELECT STCD, Z, Q, MAX(TM) as TM</span><br><span class="line">    FROM ST_RIVER_R_MIN</span><br><span class="line">    WHERE STCD IN</span><br><span class="line">    <span class="tag">&lt;<span class="name">foreach</span> <span class="attr">collection</span>=<span class="string">&quot;stcds&quot;</span> <span class="attr">item</span>=<span class="string">&quot;stcd&quot;</span> <span class="attr">open</span>=<span class="string">&quot;(&quot;</span> <span class="attr">separator</span>=<span class="string">&quot;,&quot;</span> <span class="attr">close</span>=<span class="string">&quot;)&quot;</span>&gt;</span></span><br><span class="line">        #&#123;stcd&#125;</span><br><span class="line">    <span class="tag">&lt;/<span class="name">foreach</span>&gt;</span></span><br><span class="line">    GROUP BY STCD</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>这个 SQL 存在严重的性能问题：</p>
<ul>
<li><code>GROUP BY</code> + <code>MAX(TM)</code> 需要全表扫描</li>
<li><code>ST_RIVER_R_MIN</code> 表数据量太大，查询超过 60 秒超时</li>
<li>没有合适的索引支持这种查询模式</li>
</ul>
<h4 id="方案二：使用汇总表（成功）"><a href="#方案二：使用汇总表（成功）" class="headerlink" title="方案二：使用汇总表（成功）"></a>方案二：使用汇总表（成功）</h4><p>经过分析，我们决定<strong>不查询原始大表</strong>，而是改用<strong>实时数据汇总表</strong> <code>water_realtime_summary</code>。</p>
<p>这个汇总表是数据团队预先计算好的，特点如下：</p>
<ul>
<li>每条记录都是<strong>当前最新</strong>的水情数据（<code>IS_LATEST = 1</code>）</li>
<li>数据量小（每个测站只有一条记录）</li>
<li>已建立索引，查询速度毫秒级</li>
</ul>
<p><strong>架构对比</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 方案一（失败）：直接查询原始表</span><br><span class="line">StationAssociationBiz</span><br><span class="line">       ↓</span><br><span class="line">StRiverRMinMapper</span><br><span class="line">       ↓</span><br><span class="line">[ST_RIVER_R_MIN] ── 亿级数据 ──→ 查询超时</span><br><span class="line"></span><br><span class="line"># 方案二（成功）：查询汇总表</span><br><span class="line">StationAssociationBiz</span><br><span class="line">       ↓</span><br><span class="line">RiverRealtimeChartService（Service层，@DataSource生效）</span><br><span class="line">       ↓</span><br><span class="line">WaterRealtimeSummaryMapper</span><br><span class="line">       ↓</span><br><span class="line">[water_realtime_summary] ── 每站一条 ──→ 毫秒级响应</span><br></pre></td></tr></table></figure>

<h3 id="1-4-关键技术实现"><a href="#1-4-关键技术实现" class="headerlink" title="1.4 关键技术实现"></a>1.4 关键技术实现</h3><h4 id="数据源切换的正确姿势"><a href="#数据源切换的正确姿势" class="headerlink" title="数据源切换的正确姿势"></a>数据源切换的正确姿势</h4><p>将数据源注解从 Biz 层移到 Service 层：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Service 接口</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">IRiverRealtimeChartService</span> &#123;</span><br><span class="line">    <span class="meta">@DataSource(WATER)</span>  <span class="comment">// 在 Service 层标注，AOP 可以拦截</span></span><br><span class="line">    Map&lt;String, Map&lt;String, BigDecimal&gt;&gt; <span class="title function_">getLatestWaterData</span><span class="params">(</span></span><br><span class="line"><span class="params">        List&lt;String&gt; riverStcds,</span></span><br><span class="line"><span class="params">        List&lt;String&gt; rsvrStcds</span></span><br><span class="line"><span class="params">    )</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Service 实现</span></span><br><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">RiverRealtimeChartServiceImpl</span> <span class="keyword">implements</span> <span class="title class_">IRiverRealtimeChartService</span> &#123;</span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> WaterRealtimeSummaryMapper waterRealtimeSummaryMapper;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="meta">@DataSource(WATER)</span></span><br><span class="line">    <span class="keyword">public</span> Map&lt;String, Map&lt;String, BigDecimal&gt;&gt; <span class="title function_">getLatestWaterData</span><span class="params">(</span></span><br><span class="line"><span class="params">        List&lt;String&gt; riverStcds,</span></span><br><span class="line"><span class="params">        List&lt;String&gt; rsvrStcds</span></span><br><span class="line"><span class="params">    )</span> &#123;</span><br><span class="line">        <span class="comment">// 合并河道站和水务站测站编码</span></span><br><span class="line">        List&lt;String&gt; allStcds = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">        <span class="keyword">if</span> (riverStcds != <span class="literal">null</span>) allStcds.addAll(riverStcds);</span><br><span class="line">        <span class="keyword">if</span> (rsvrStcds != <span class="literal">null</span>) allStcds.addAll(rsvrStcds);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (allStcds.isEmpty()) &#123;</span><br><span class="line">            <span class="keyword">return</span> Collections.emptyMap();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 批量查询汇总表</span></span><br><span class="line">        List&lt;WaterRealtimeSummary&gt; summaries =</span><br><span class="line">            waterRealtimeSummaryMapper.selectLatestByStcds(allStcds);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 转换为 Map 便于后续查找</span></span><br><span class="line">        Map&lt;String, Map&lt;String, BigDecimal&gt;&gt; result = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;();</span><br><span class="line">        <span class="keyword">for</span> (WaterRealtimeSummary summary : summaries) &#123;</span><br><span class="line">            Map&lt;String, BigDecimal&gt; waterData = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;();</span><br><span class="line">            <span class="comment">// 根据测站类型确定水位和流量字段</span></span><br><span class="line">            <span class="keyword">if</span> (<span class="string">&quot;RR&quot;</span>.equals(summary.getSTTP())) &#123;</span><br><span class="line">                <span class="comment">// 水库站：RZ 是水位，INQ 是入库流量</span></span><br><span class="line">                waterData.put(<span class="string">&quot;waterLevel&quot;</span>, summary.getRZ());</span><br><span class="line">                waterData.put(<span class="string">&quot;flow&quot;</span>, summary.getINQ());</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="comment">// 河道站：Z 是水位，Q 是流量</span></span><br><span class="line">                waterData.put(<span class="string">&quot;waterLevel&quot;</span>, summary.getZ());</span><br><span class="line">                waterData.put(<span class="string">&quot;flow&quot;</span>, summary.getQ());</span><br><span class="line">            &#125;</span><br><span class="line">            result.put(summary.getSTCD(), waterData);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="批量查询-SQL-设计"><a href="#批量查询-SQL-设计" class="headerlink" title="批量查询 SQL 设计"></a>批量查询 SQL 设计</h4><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">&quot;selectLatestByStcds&quot;</span> <span class="attr">resultMap</span>=<span class="string">&quot;WaterRealtimeSummaryResult&quot;</span>&gt;</span></span><br><span class="line">    SELECT</span><br><span class="line">        STCD,    -- 测站编码</span><br><span class="line">        STNM,    -- 测站名称</span><br><span class="line">        STTP,    -- 测站类型</span><br><span class="line">        TM,      -- 时间</span><br><span class="line">        Z,       -- 河道水位</span><br><span class="line">        Q,       -- 河道流量</span><br><span class="line">        RZ,      -- 水库水位</span><br><span class="line">        INQ,     -- 水库入库流量</span><br><span class="line">        OTQ,     -- 出库流量</span><br><span class="line">        WPTN     -- 水势</span><br><span class="line">    FROM water_realtime_summary</span><br><span class="line">    WHERE IS_LATEST = 1</span><br><span class="line">      AND STCD IN</span><br><span class="line">      <span class="tag">&lt;<span class="name">foreach</span> <span class="attr">collection</span>=<span class="string">&quot;stcds&quot;</span> <span class="attr">item</span>=<span class="string">&quot;stcd&quot;</span> <span class="attr">open</span>=<span class="string">&quot;(&quot;</span> <span class="attr">separator</span>=<span class="string">&quot;,&quot;</span> <span class="attr">close</span>=<span class="string">&quot;)&quot;</span>&gt;</span></span><br><span class="line">          #&#123;stcd&#125;</span><br><span class="line">      <span class="tag">&lt;/<span class="name">foreach</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>这个 SQL 的优势：</p>
<ul>
<li><strong><code>IS_LATEST = 1</code></strong> 条件利用了汇总表的索引，快速定位最新数据</li>
<li><strong><code>IN</code> 批量查询</strong>避免了循环单查，减少数据库交互次数</li>
<li>返回结果直接在应用层用 <code>Map</code> 组织，<strong>O(1)</strong> 复杂度查找</li>
</ul>
<h4 id="DTO-字段扩展"><a href="#DTO-字段扩展" class="headerlink" title="DTO 字段扩展"></a>DTO 字段扩展</h4><p>在返回给前端的 DTO 中新增两个字段：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">StationAssociationGroupedDTO</span> <span class="keyword">implements</span> <span class="title class_">Serializable</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">long</span> <span class="variable">serialVersionUID</span> <span class="operator">=</span> <span class="number">1L</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> String id;           <span class="comment">// 关联ID</span></span><br><span class="line">    <span class="keyword">private</span> String name;         <span class="comment">// 测站名称</span></span><br><span class="line">    <span class="keyword">private</span> String stcd;         <span class="comment">// 测站编码</span></span><br><span class="line">    <span class="keyword">private</span> String sttp;         <span class="comment">// 测站类型</span></span><br><span class="line">    <span class="keyword">private</span> String basin;        <span class="comment">// 所属流域</span></span><br><span class="line">    <span class="keyword">private</span> String river;        <span class="comment">// 所属河流</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// ========== 新增字段 ==========</span></span><br><span class="line">    <span class="keyword">private</span> BigDecimal waterLevel;  <span class="comment">// 最新水位（米）</span></span><br><span class="line">    <span class="keyword">private</span> BigDecimal flow;        <span class="comment">// 最新流量（立方米/秒）</span></span><br><span class="line">    <span class="comment">// ==============================</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 省略 getter/setter</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="1-5-验证结果"><a href="#1-5-验证结果" class="headerlink" title="1.5 验证结果"></a>1.5 验证结果</h3><p>改造完成后，经过完整的测试验证：</p>
<table>
<thead>
<tr>
<th>验证项</th>
<th>结果</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>编译通过</td>
<td>✅</td>
<td>BUILD SUCCESS</td>
</tr>
<tr>
<td>应用启动</td>
<td>✅</td>
<td>正常加载，无异常</td>
</tr>
<tr>
<td>API 返回</td>
<td>✅</td>
<td>data 包含 waterLevel 和 flow</td>
</tr>
<tr>
<td>查询性能</td>
<td>✅</td>
<td>毫秒级响应（汇总表）</td>
</tr>
<tr>
<td>边界情况</td>
<td>✅</td>
<td>非河道&#x2F;水库站返回 null</td>
</tr>
</tbody></table>
<p><strong>API 响应示例</strong>：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;code&quot;</span><span class="punctuation">:</span> <span class="number">200</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;total&quot;</span><span class="punctuation">:</span> <span class="number">100</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;rows&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;80115900&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;封开江口&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;stcd&quot;</span><span class="punctuation">:</span> <span class="string">&quot;80115900&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;sttp&quot;</span><span class="punctuation">:</span> <span class="string">&quot;ZZ&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;basin&quot;</span><span class="punctuation">:</span> <span class="string">&quot;珠江流域&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;river&quot;</span><span class="punctuation">:</span> <span class="string">&quot;西江&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;waterLevel&quot;</span><span class="punctuation">:</span> <span class="number">1.65</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;flow&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">null</span></span></span><br><span class="line">    <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;80115800&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;天堂山&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;stcd&quot;</span><span class="punctuation">:</span> <span class="string">&quot;80115800&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;sttp&quot;</span><span class="punctuation">:</span> <span class="string">&quot;RR&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;basin&quot;</span><span class="punctuation">:</span> <span class="string">&quot;珠江流域&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;river&quot;</span><span class="punctuation">:</span> <span class="string">&quot;增江&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;waterLevel&quot;</span><span class="punctuation">:</span> <span class="number">25.38</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;flow&quot;</span><span class="punctuation">:</span> <span class="number">15.6</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">]</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<h2 id="需求二：设备分类接口添加测站基础字段"><a href="#需求二：设备分类接口添加测站基础字段" class="headerlink" title="需求二：设备分类接口添加测站基础字段"></a>需求二：设备分类接口添加测站基础字段</h2><h3 id="2-1-需求背景"><a href="#2-1-需求背景" class="headerlink" title="2.1 需求背景"></a>2.1 需求背景</h3><p><code>/api/station/material/equipmentClassified</code> 接口原本只返回设备分类数据，包括：</p>
<ul>
<li><code>total</code>：设备记录总数</li>
<li><code>stcd</code>：测站编码</li>
<li><code>records</code>：设备详情数组</li>
</ul>
<p>前端团队希望在调用该接口时，<strong>无需额外请求</strong>，就能同时获取到测站的基础关联信息：</p>
<ul>
<li>所属流域（basin）</li>
<li>所属河流（river）</li>
<li>管理单位（adminUnit）</li>
<li>所属行政区（adminDivision）</li>
</ul>
<h3 id="2-2-数据来源分析"><a href="#2-2-数据来源分析" class="headerlink" title="2.2 数据来源分析"></a>2.2 数据来源分析</h3><p>测站基础信息存储在 <code>WATER</code> 数据源的 <code>ST_STBPRP_B</code> 表中，通过 <code>StStbprpBMapper.selectStStbprpBBySTCD</code> 方法查询。</p>
<p>字段映射关系如下：</p>
<table>
<thead>
<tr>
<th>前端需求字段</th>
<th>后端使用字段</th>
<th>数据库字段</th>
<th>字段说明</th>
</tr>
</thead>
<tbody><tr>
<td>所属流域</td>
<td>bsnm</td>
<td>BSNM</td>
<td>Basin Name，流域名称</td>
</tr>
<tr>
<td>所属河流</td>
<td>rvnm</td>
<td>RVNM</td>
<td>River Name，河流名称</td>
</tr>
<tr>
<td>管理单位</td>
<td>admauth</td>
<td>ADMAUTH</td>
<td>Administrative Authority，管理单位</td>
</tr>
<tr>
<td>所属行政区</td>
<td>addvcdc</td>
<td>ADDVCDC</td>
<td>Administrative Division Code，行政区划代码</td>
</tr>
</tbody></table>
<h3 id="2-3-改造方案"><a href="#2-3-改造方案" class="headerlink" title="2.3 改造方案"></a>2.3 改造方案</h3><h4 id="改造文件"><a href="#改造文件" class="headerlink" title="改造文件"></a>改造文件</h4><p><strong>核心改造文件</strong>：<code>cnsci-admin/src/main/java/cn/cnsci/web/service/EquipmentDataService.java</code></p>
<h4 id="步骤一：新增依赖注入"><a href="#步骤一：新增依赖注入" class="headerlink" title="步骤一：新增依赖注入"></a>步骤一：新增依赖注入</h4><p>在 Service 类中注入测站基础信息 Mapper：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">EquipmentDataService</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> StStbprpBMapper stStbprpBMapper;  <span class="comment">// 测站基础信息 Mapper</span></span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> EquipmentDataMapper equipmentDataMapper;  <span class="comment">// 原有设备数据 Mapper</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// ... 其他内容</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="步骤二：追加测站基础信息查询"><a href="#步骤二：追加测站基础信息查询" class="headerlink" title="步骤二：追加测站基础信息查询"></a>步骤二：追加测站基础信息查询</h4><p>在 <code>getEquipmentClassified</code> 方法的<strong>末尾</strong>（原步骤 5 构建 responseData 之后）追加新步骤：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 获取设备分类列表（带测站基础信息）</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> stcd 测站编码</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@return</span> 设备分类数据</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> ResultData <span class="title function_">getEquipmentClassified</span><span class="params">(String stcd)</span> &#123;</span><br><span class="line">    <span class="comment">// 1. 调用第三方 API 获取设备数据</span></span><br><span class="line">    <span class="comment">// 2. 解析 records 数组</span></span><br><span class="line">    <span class="comment">// 3. 批量查询字典映射</span></span><br><span class="line">    <span class="comment">// 4. 为 records 每条附加 symbolName</span></span><br><span class="line">    <span class="comment">// 5. 构建 responseData (stcd/total/records)</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// ========== 新增步骤 6 ==========</span></span><br><span class="line">    <span class="comment">// 6. 查询测站基本信息，附加流域、河流、管理单位、行政区字段</span></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="type">StStbprpB</span> <span class="variable">station</span> <span class="operator">=</span> stStbprpBMapper.selectStStbprpBBySTCD(stcd);</span><br><span class="line">        <span class="keyword">if</span> (station != <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="comment">// 附加测站基础信息到返回数据</span></span><br><span class="line">            responseData.put(<span class="string">&quot;bsnm&quot;</span>, station.getBSNM());        <span class="comment">// 所属流域</span></span><br><span class="line">            responseData.put(<span class="string">&quot;rvnm&quot;</span>, station.getRVNM());        <span class="comment">// 所属河流</span></span><br><span class="line">            responseData.put(<span class="string">&quot;admauth&quot;</span>, station.getADMAUTH());   <span class="comment">// 管理单位</span></span><br><span class="line">            responseData.put(<span class="string">&quot;addvcdc&quot;</span>, station.getADDVCDC());    <span class="comment">// 所属行政区</span></span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            log.warn(<span class="string">&quot;未查询到测站基本信息 - stcd: &#123;&#125;&quot;</span>, stcd);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">        <span class="comment">// 测站基础信息查询失败不影响设备数据返回</span></span><br><span class="line">        log.warn(<span class="string">&quot;查询测站基本信息失败，不影响设备数据返回 - stcd: &#123;&#125;&quot;</span>, stcd, e);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// =================================</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> ResultData.success(responseData);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-4-防御性设计"><a href="#2-4-防御性设计" class="headerlink" title="2.4 防御性设计"></a>2.4 防御性设计</h3><p>这个改造体现了良好的<strong>防御性编程</strong>理念：</p>
<h4 id="设计原则一：查询失败不影响主流程"><a href="#设计原则一：查询失败不影响主流程" class="headerlink" title="设计原则一：查询失败不影响主流程"></a>设计原则一：查询失败不影响主流程</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="type">StStbprpB</span> <span class="variable">station</span> <span class="operator">=</span> stStbprpBMapper.selectStStbprpBBySTCD(stcd);</span><br><span class="line">    <span class="keyword">if</span> (station != <span class="literal">null</span>) &#123;</span><br><span class="line">        <span class="comment">// 正常附加字段</span></span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">// 测站不存在，打印警告日志但不阻断</span></span><br><span class="line">        log.warn(<span class="string">&quot;未查询到测站基本信息 - stcd: &#123;&#125;&quot;</span>, stcd);</span><br><span class="line">    &#125;</span><br><span class="line">&#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">    <span class="comment">// 发生异常也不阻断，只记录日志</span></span><br><span class="line">    log.warn(<span class="string">&quot;查询测站基本信息失败，不影响设备数据返回 - stcd: &#123;&#125;&quot;</span>, stcd, e);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>为什么这样设计？</strong></p>
<ul>
<li>设备分类信息是<strong>核心功能</strong>，测站基础信息是<strong>附加信息</strong></li>
<li>测站基础信息查询失败（如网络抖动、数据库短暂不可用）不应该导致整个接口返回错误</li>
<li>前端可以正常展示设备列表，只是缺少基础信息字段</li>
</ul>
<h4 id="设计原则二：空值检查"><a href="#设计原则二：空值检查" class="headerlink" title="设计原则二：空值检查"></a>设计原则二：空值检查</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (station != <span class="literal">null</span>) &#123;</span><br><span class="line">    responseData.put(<span class="string">&quot;bsnm&quot;</span>, station.getBSNM());</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>只有当查询结果不为空时才设置字段，避免 NPE（空指针异常）。</p>
<h4 id="设计原则三：日志分级"><a href="#设计原则三：日志分级" class="headerlink" title="设计原则三：日志分级"></a>设计原则三：日志分级</h4><ul>
<li><code>warn</code> 级别：测站不存在，属于<strong>业务上可能的正常情况</strong>（如新接入测站）</li>
<li>不使用 <code>error</code> 级别：这不是系统错误，只是一个信息提示</li>
</ul>
<h3 id="2-5-调用链路分析"><a href="#2-5-调用链路分析" class="headerlink" title="2.5 调用链路分析"></a>2.5 调用链路分析</h3><p>完整的调用链路如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">前端请求</span><br><span class="line">    │</span><br><span class="line">    ▼</span><br><span class="line">StStationMaterialController.equipmentClassified(stcd)</span><br><span class="line">    │</span><br><span class="line">    ▼</span><br><span class="line">EquipmentDataService.getEquipmentClassified(stcd)</span><br><span class="line">    │</span><br><span class="line">    ├─ 1. 调用第三方 API 获取设备数据</span><br><span class="line">    │</span><br><span class="line">    ├─ 2. 解析 records 数组</span><br><span class="line">    │</span><br><span class="line">    ├─ 3. 批量查询字典映射（dictionary Mapper）</span><br><span class="line">    │</span><br><span class="line">    ├─ 4. 为 records 每条附加 symbolName（符号名称）</span><br><span class="line">    │</span><br><span class="line">    ├─ 5. 构建 responseData（stcd/total/records）</span><br><span class="line">    │</span><br><span class="line">    └─ 6. 查询 StStbprpBMapper 附加基础信息（bsnm/rvnm/admauth/addvcdc）</span><br><span class="line">              │</span><br><span class="line">              ▼</span><br><span class="line">        StStbprpBMapper.selectStStbprpBBySTCD(stcd)</span><br><span class="line">              │</span><br><span class="line">              ▼</span><br><span class="line">        [ST_STBPRP_B] ── WATER 数据源</span><br></pre></td></tr></table></figure>

<h3 id="2-6-改造后返回示例"><a href="#2-6-改造后返回示例" class="headerlink" title="2.6 改造后返回示例"></a>2.6 改造后返回示例</h3><figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;msg&quot;</span><span class="punctuation">:</span> <span class="string">&quot;操作成功&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;code&quot;</span><span class="punctuation">:</span> <span class="number">200</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;data&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;total&quot;</span><span class="punctuation">:</span> <span class="number">3</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;stcd&quot;</span><span class="punctuation">:</span> <span class="string">&quot;81005450&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;bsnm&quot;</span><span class="punctuation">:</span> <span class="string">&quot;珠江&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;rvnm&quot;</span><span class="punctuation">:</span> <span class="string">&quot;北江&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;admauth&quot;</span><span class="punctuation">:</span> <span class="string">&quot;广东省水文局&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;addvcdc&quot;</span><span class="punctuation">:</span> <span class="string">&quot;广东省肇庆市广宁县&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;records&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">      <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;USFL&quot;</span><span class="punctuation">:</span> <span class="string">&quot;1&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;STCD&quot;</span><span class="punctuation">:</span> <span class="string">&quot;81005450&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;SYMBOL&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Q&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;ATCUNIT_ID&quot;</span><span class="punctuation">:</span> <span class="string">&quot;102&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;EQID&quot;</span><span class="punctuation">:</span> <span class="string">&quot;0081005450&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;EQTP&quot;</span><span class="punctuation">:</span> <span class="string">&quot;HADCP-水平式ADCP&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;LGTD&quot;</span><span class="punctuation">:</span> <span class="number">112.366464</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;DATASOURCE&quot;</span><span class="punctuation">:</span> <span class="string">&quot;1&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;symbolName&quot;</span><span class="punctuation">:</span> <span class="string">&quot;流量&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;EQNM&quot;</span><span class="punctuation">:</span> <span class="string">&quot;祝州&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;LTTD&quot;</span><span class="punctuation">:</span> <span class="number">23.742710</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;STLC&quot;</span><span class="punctuation">:</span> <span class="string">&quot;肇庆市广宁县坑口镇祝州村&quot;</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">]</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<h2 id="技术总结与经验"><a href="#技术总结与经验" class="headerlink" title="技术总结与经验"></a>技术总结与经验</h2><h3 id="经验一：多数据源注解的正确使用"><a href="#经验一：多数据源注解的正确使用" class="headerlink" title="经验一：多数据源注解的正确使用"></a>经验一：多数据源注解的正确使用</h3><p><strong>问题</strong>：<code>@DataSource</code> 注解在哪个层级使用才生效？</p>
<p><strong>答案</strong>：Spring AOP 只能拦截 <code>Service</code> 层的代理方法。</p>
<table>
<thead>
<tr>
<th>类注解</th>
<th>层级</th>
<th>@DataSource 是否生效</th>
</tr>
</thead>
<tbody><tr>
<td><code>@Service</code></td>
<td>Service 层</td>
<td>✅ 生效</td>
</tr>
<tr>
<td><code>@Component</code></td>
<td>Biz 层</td>
<td>❌ 不生效</td>
</tr>
<tr>
<td><code>@Controller</code></td>
<td>Controller 层</td>
<td>❌ 不生效</td>
</tr>
</tbody></table>
<p><strong>正确做法</strong>：</p>
<ul>
<li>将需要切换数据源的逻辑封装在 <code>Service</code> 层方法中</li>
<li>Biz 层和 Controller 层调用 Service 层方法，由 Service 层的代理处理数据源切换</li>
</ul>
<h3 id="经验二：大表查询优化策略"><a href="#经验二：大表查询优化策略" class="headerlink" title="经验二：大表查询优化策略"></a>经验二：大表查询优化策略</h3><p><strong>问题</strong>：遇到亿级数据量的大表查询，应该如何优化？</p>
<p><strong>答案</strong>：优先考虑汇总表&#x2F;宽表方案。</p>
<p>优化路径：</p>
<ol>
<li><strong>判断查询场景</strong>：是统计分析还是实时查询？</li>
<li><strong>评估数据规模</strong>：表数据量多少？查询频率如何？</li>
<li><strong>选择优化策略</strong>：</li>
</ol>
<table>
<thead>
<tr>
<th>场景</th>
<th>推荐方案</th>
</tr>
</thead>
<tbody><tr>
<td>实时查询，查询条件简单</td>
<td>汇总表 + 索引</td>
</tr>
<tr>
<td>统计分析，需要聚合计算</td>
<td>预计算表 &#x2F; 物化视图</td>
</tr>
<tr>
<td>历史数据查询</td>
<td>分区表 + 分页查询</td>
</tr>
<tr>
<td>高频小数据量查询</td>
<td>Redis 缓存</td>
</tr>
</tbody></table>
<p><strong>本项目的优化</strong>：</p>
<ul>
<li>不查询原始的 <code>ST_RIVER_R_MIN</code>（亿级数据）</li>
<li>改用 <code>water_realtime_summary</code> 汇总表（每测站一条）</li>
<li>查询时间从 <strong>60+ 秒</strong>优化到 <strong>毫秒级</strong></li>
</ul>
<h3 id="经验三：防御式编程实践"><a href="#经验三：防御式编程实践" class="headerlink" title="经验三：防御式编程实践"></a>经验三：防御式编程实践</h3><p>在接口改造中，我们始终坚持<strong>核心功能不受附加功能影响</strong>的原则：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="comment">// 附加信息查询</span></span><br><span class="line">    <span class="type">StStbprpB</span> <span class="variable">station</span> <span class="operator">=</span> stStbprpBMapper.selectStStbprpBBySTCD(stcd);</span><br><span class="line">    <span class="keyword">if</span> (station != <span class="literal">null</span>) &#123;</span><br><span class="line">        responseData.put(<span class="string">&quot;bsnm&quot;</span>, station.getBSNM());</span><br><span class="line">        <span class="comment">// ... 其他字段</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">    <span class="comment">// 只记录日志，不阻断主流程</span></span><br><span class="line">    log.warn(<span class="string">&quot;查询测站基本信息失败，不影响设备数据返回&quot;</span>, e);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>关键点</strong>：</p>
<ul>
<li><strong>异常捕获</strong>：使用 <code>try-catch</code> 包裹可能失败的操作</li>
<li><strong>空值检查</strong>：查询结果为 null 时不设置字段</li>
<li><strong>日志分级</strong>：使用 <code>warn</code> 而非 <code>error</code>，区分业务警告和系统错误</li>
<li><strong>优雅降级</strong>：附加信息查询失败时，接口仍返回核心数据</li>
</ul>
<h3 id="经验四：避免-N-1-查询问题"><a href="#经验四：避免-N-1-查询问题" class="headerlink" title="经验四：避免 N+1 查询问题"></a>经验四：避免 N+1 查询问题</h3><p>列表接口中最常见的性能问题是 <strong>N+1 查询</strong>：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// ❌ 错误做法：循环中单独查询</span></span><br><span class="line"><span class="keyword">for</span> (Station station : stations) &#123;</span><br><span class="line">    <span class="type">StStbprpB</span> <span class="variable">info</span> <span class="operator">=</span> stStbprpBMapper.selectByStcd(station.getStcd()); <span class="comment">// N 次查询</span></span><br><span class="line">    <span class="comment">// 处理...</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// ✅ 正确做法：先收集 ID，批量查询</span></span><br><span class="line">List&lt;String&gt; stcds = stations.stream()</span><br><span class="line">    .map(Station::getStcd)</span><br><span class="line">    .collect(Collectors.toList());</span><br><span class="line">List&lt;StStbprpB&gt; infos = stStbprpBMapper.selectByStcds(stcds); <span class="comment">// 1 次查询</span></span><br><span class="line">Map&lt;String, StStbprpB&gt; infoMap = infos.stream()</span><br><span class="line">    .collect(Collectors.toMap(StStbprpB::getSTCD, Function.identity()));</span><br><span class="line"><span class="comment">// 处理...</span></span><br></pre></td></tr></table></figure>

<p><strong>性能对比</strong>：</p>
<ul>
<li>N+1 查询：100 条数据 &#x3D; 1 + 100 &#x3D; <strong>101 次数据库交互</strong></li>
<li>批量查询：100 条数据 &#x3D; 1 + 1 &#x3D; <strong>2 次数据库交互</strong></li>
</ul>
<h3 id="经验五：Git-提交规范"><a href="#经验五：Git-提交规范" class="headerlink" title="经验五：Git 提交规范"></a>经验五：Git 提交规范</h3><p>本次改造的 Git 提交记录清晰可追溯：</p>
<table>
<thead>
<tr>
<th>提交</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>2e13568</code></td>
<td>feat: 为测站关联列表接口添加水位和流量字段（最终合并提交）</td>
</tr>
<tr>
<td><code>d033003</code></td>
<td>feat: 为 StationAssociationGroupedDTO 添加 waterLevel 和 flow 字段</td>
</tr>
<tr>
<td><code>a89a412</code></td>
<td>feat: StRiverRMinMapper 添加批量查询方法</td>
</tr>
<tr>
<td><code>7ea24aa</code></td>
<td>feat: StRsvrRMinMapper 添加批量查询方法</td>
</tr>
<tr>
<td><code>8b6ca64</code></td>
<td>feat: StationAssociationBiz.listAssociations 添加水情查询</td>
</tr>
</tbody></table>
<p><strong>提交规范建议</strong>：</p>
<ul>
<li>使用 <code>feat:</code> 前缀标识新功能</li>
<li>每个小改动单独提交，便于追溯和回滚</li>
<li>合并前确保各个 commit 的原子性</li>
</ul>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>本文通过广东水文项目的两个实际案例，展示了后端接口改造的完整技术路径。从需求分析、方案设计、问题排查到最终实现，每一个环节都需要技术判断和权衡。</p>
<p><strong>核心收获</strong>：</p>
<ol>
<li><strong>多数据源路由</strong>：AOP 注解只在 Service 层生效，Biz 层需要调用 Service 方法</li>
<li><strong>大表优化</strong>：优先考虑汇总表，避免直接查询亿级数据表</li>
<li><strong>防御式编程</strong>：核心功能与附加功能解耦，附加功能的失败不能影响主流程</li>
<li><strong>批量查询</strong>：列表接口避免 N+1 查询，先收集 ID 再批量操作</li>
</ol>
<p>这些经验不仅适用于水文项目，在任何涉及多数据源、大规模数据处理的后端系统中都具有普遍的参考价值。希望本文能为遇到类似问题的开发者提供一些启发。</p>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>水文</category>
      </categories>
      <tags>
        <tag>Spring Boot</tag>
        <tag>MyBatis</tag>
        <tag>性能优化</tag>
        <tag>Java</tag>
        <tag>广东水文</tag>
      </tags>
  </entry>
  <entry>
    <title>广东水文项目后端接口开发与优化实践</title>
    <url>/2026/03/17/%E5%B9%BF%E4%B8%9C%E6%B0%B4%E6%96%87%E9%A1%B9%E7%9B%AE%E5%90%8E%E7%AB%AF%E6%8E%A5%E5%8F%A3%E5%BC%80%E5%8F%91%E5%AE%9E%E8%B7%B5/</url>
    <content><![CDATA[<p>在大型企业级应用开发中，与第三方平台的接口集成是不可避免的任务。如何在保证系统稳定性的同时，高效地完成接口对接和优化，是每个后端开发者都需要面对的课题。本文以广东水文监测系统为背景，记录了视频图片鉴权改造和等值面外部接口开发两个典型场景的完整实践过程，希望能为有类似需求的开发者提供参考。</p>
<span id="more"></span>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>广东水文监测系统是一个基于 RuoYi-Vue 框架开发的水文监测后台管理系统，采用 Java + Spring Boot 技术栈，实现了水位、降雨、蒸发、气象等多类监测数据的采集、存储和展示功能。系统需要与多个第三方平台进行对接，包括视频监控平台、GIS 服务平台等。</p>
<p>在实际开发过程中，我们遇到了两个具有代表性的技术挑战：</p>
<ol>
<li><strong>视频图片鉴权问题</strong>：视频平台接口返回的图片资源需要鉴权，但远程签发接口权限不稳定，需要切换到本地签名方案</li>
<li><strong>等值面外部接口开发</strong>：需要调用第三方 GIS 服务获取降雨等值面数据，而不是使用本地算法</li>
</ol>
<p>这两个场景虽然业务不同，但都涉及第三方接口集成，都需要在保证功能正确性的同时，考虑性能、缓存、安全等因素。接下来我将详细记录这两个场景的完整解决方案。</p>
<h2 id="第一部分：视频图片鉴权改造"><a href="#第一部分：视频图片鉴权改造" class="headerlink" title="第一部分：视频图片鉴权改造"></a>第一部分：视频图片鉴权改造</h2><h3 id="1-1-背景与问题"><a href="#1-1-背景与问题" class="headerlink" title="1.1 背景与问题"></a>1.1 背景与问题</h3><p>这次改造的起点，是视频设备接口需要返回 <code>thumbnailAuth</code> 字段，供前端访问视频平台上的缩略图、告警图片等静态资源。</p>
<p>最早的实现方案是严格按照视频平台文档来做，基本流程如下：</p>
<ol>
<li>先通过 <code>/resourceSharing/oauth/token</code> 获取共享账号 token</li>
<li>再带着 token 调用 <code>/lisa/access/sign</code></li>
<li>从接口返回中取得鉴权头部信息：<ul>
<li><code>x-apig-nonce</code></li>
<li><code>x-apig-timestamp</code></li>
<li><code>x-apig-appid</code></li>
<li><code>x-apig-sign</code></li>
<li><code>x-datax-code</code></li>
</ul>
</li>
<li>将这组鉴权头返回给前端，用于图片资源访问</li>
</ol>
<p>从实现层面看，这条链路并不复杂，但联调中很快遇到了平台权限问题：接口会返回 401，提示“共享用户无权限访问该接口”。</p>
<p>这意味着问题不再是单纯的代码通不通，而变成了两个方向都需要确认：</p>
<ul>
<li>当前代码是不是按平台要求调用了接口</li>
<li>即使代码调用方式正确，共享账号本身是否具备 <code>/lisa/access/sign</code> 的访问权限</li>
</ul>
<h3 id="1-2-问题定位过程"><a href="#1-2-问题定位过程" class="headerlink" title="1.2 问题定位过程"></a>1.2 问题定位过程</h3><p><strong>最初的技术判断</strong></p>
<p>最开始根据仓库现有实现，平台是这样识别共享账号的：</p>
<ul>
<li><code>video.api.mobile</code> 用于调用 <code>/resourceSharing/oauth/token</code></li>
<li>平台返回一个 token</li>
<li>后续所有视频接口都在请求头里带这个 token</li>
</ul>
<p>也就是说，平台侧对“共享账号是谁”的识别，至少在当前代码里，是通过 <code>mobile -&gt; token</code> 这条链路完成的。</p>
<p>旧版图片鉴权实现也是沿着这个思路写的：</p>
<ul>
<li>配置读取 <code>video.api.image-app-id</code></li>
<li>获取 token</li>
<li>调用 <code>/lisa/access/sign</code></li>
<li>解析返回值并缓存 9 分钟</li>
</ul>
<p><strong>联调日志给出的信号</strong></p>
<p>联调中出现过几类不同的错误：</p>
<ul>
<li><code>请求缺少token</code></li>
<li><code>共享用户无权限访问该接口</code></li>
<li><code>thumbnailAuth</code> 返回为空</li>
</ul>
<p>这几类错误其实对应着不同层次的问题：</p>
<ul>
<li>“请求缺少 token”说明调用方式不对</li>
<li>“共享用户无权限访问该接口”说明请求已经到平台了，但账号权限不足</li>
<li><code>thumbnailAuth</code> 为空则可能是业务逻辑没有把鉴权结果正确注入返回对象</li>
</ul>
<p>在当时的上下文下，旧实现一度看上去是成立的，因为日志已经能证明 token 获取成功、视频列表接口可用、播放地址转换接口可用。但问题在于，图片鉴权这一条链路始终被平台权限卡住，没法稳定联调完成。</p>
<p><strong>新线索带来的方案变化</strong></p>
<p>随后拿到了一段 Lisa 平台的第三方图片鉴权工具类，核心逻辑是：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">String</span> <span class="variable">nonce</span> <span class="operator">=</span> <span class="string">&quot;access_&quot;</span> + UUID.randomUUID().toString().substring(<span class="number">0</span>, <span class="number">6</span>);</span><br><span class="line"><span class="type">String</span> <span class="variable">timestamp</span> <span class="operator">=</span> String.valueOf(System.currentTimeMillis());</span><br><span class="line"><span class="type">String</span> <span class="variable">preSign</span> <span class="operator">=</span> nonce + timestamp + accessCode + passWord + timestamp;</span><br><span class="line"><span class="type">String</span> <span class="variable">sign</span> <span class="operator">=</span> sha256(preSign);</span><br></pre></td></tr></table></figure>

<p>然后本地直接生成：</p>
<ul>
<li><code>x-apig-nonce</code></li>
<li><code>x-apig-timestamp</code></li>
<li><code>x-apig-appid</code></li>
<li><code>x-apig-sign</code></li>
<li><code>x-datax-code</code></li>
</ul>
<p>这条线索的重要性在于，它提供了另一种完全不同的实现路径：</p>
<ul>
<li>不依赖 <code>/lisa/access/sign</code></li>
<li>不依赖该接口的权限开通状态</li>
<li>只依赖共享账号 <code>accessCode/password</code></li>
</ul>
<p>在平台文档与平台工具类之间存在差异的情况下，最终我们明确转向：<strong>不走 <code>lisa-sign</code>，改为本地生成图片鉴权头</strong>。</p>
<h3 id="1-3-改动目标"><a href="#1-3-改动目标" class="headerlink" title="1.3 改动目标"></a>1.3 改动目标</h3><p>这次改动的目标非常明确：</p>
<ol>
<li><strong>保留现有视频主链路能力</strong>：token 获取继续保留，视频列表、播放地址、云台等接口仍按原方式工作</li>
<li><strong>仅替换图片鉴权这一路</strong>：不再调用 <code>/lisa/access/sign</code>，改为根据 <code>accessCode/password</code> 本地生成鉴权头</li>
<li><strong>保持对前端接口结构兼容</strong>：<code>thumbnailAuth</code> 字段仍然存在，返回字段名不变</li>
<li><strong>保持缓存策略稳定</strong>：鉴权信息仍缓存 9 分钟，避免频繁重新生成</li>
</ol>
<p>换句话说，这不是大面积重构视频模块，而是在“最小变更集”的原则下，对图片鉴权来源做了一次替换。</p>
<h3 id="1-4-具体代码改动"><a href="#1-4-具体代码改动" class="headerlink" title="1.4 具体代码改动"></a>1.4 具体代码改动</h3><h4 id="1-4-1-扩展视频配置模型"><a href="#1-4-1-扩展视频配置模型" class="headerlink" title="1.4.1 扩展视频配置模型"></a>1.4.1 扩展视频配置模型</h4><p>文件：<code>cnsci-common/src/main/java/cn/cnsci/common/config/VideoApiProperties.java</code></p>
<p>新增了两个配置项：</p>
<ul>
<li><code>accessCode</code></li>
<li><code>password</code></li>
</ul>
<p>这样 Spring 在读取 <code>video.api</code> 配置时，就能把第三方共享账号凭据注入到 <code>VideoService</code> 中。</p>
<p>保留了原有字段：</p>
<ul>
<li><code>baseUrl</code></li>
<li><code>mobile</code></li>
<li><code>imageAppId</code></li>
<li><code>timeout</code></li>
</ul>
<p>这里保留 <code>imageAppId</code> 的原因不是当前逻辑还在用，而是为了兼容已有配置和后续回退可能性，避免这次改动顺手把无关字段清掉，导致额外风险。</p>
<h4 id="1-4-2-新增本地图片鉴权工具类"><a href="#1-4-2-新增本地图片鉴权工具类" class="headerlink" title="1.4.2 新增本地图片鉴权工具类"></a>1.4.2 新增本地图片鉴权工具类</h4><p>文件：<code>cnsci-common/src/main/java/cn/cnsci/common/utils/http/LisaTripartiteAuthUtils.java</code></p>
<p>新增了一个专门的工具类，职责很单一：根据共享账号 <code>accessCode/password</code> 生成 Lisa 图片访问鉴权头。</p>
<p>核心逻辑：</p>
<ol>
<li>校验 <code>accessCode/password</code> 是否为空</li>
<li>生成 <code>access_</code> 前缀的随机 nonce</li>
<li>生成当前毫秒级时间戳</li>
<li>按平台工具类给出的规则拼接签名原文：<code>nonce + timestamp + accessCode + password + timestamp</code></li>
<li>使用 <code>SHA-256</code> 生成十六进制签名</li>
<li>组装并返回完整 header map</li>
</ol>
<p>这样做的好处是：</p>
<ul>
<li>逻辑集中，不污染 <code>VideoService</code></li>
<li>以后如果签名规则有变化，只需修改一个地方</li>
<li>单元测试也更容易覆盖</li>
</ul>
<h4 id="1-4-3-替换-VideoService-的实现"><a href="#1-4-3-替换-VideoService-的实现" class="headerlink" title="1.4.3 替换 VideoService 的实现"></a>1.4.3 替换 VideoService 的实现</h4><p>文件：<code>cnsci-admin/src/main/java/cn/cnsci/web/service/VideoService.java</code></p>
<p>这是这次最核心的业务改动。</p>
<p><strong>旧逻辑</strong>：</p>
<ul>
<li>先查 Redis 缓存</li>
<li>缓存未命中后，读取 <code>imageAppId</code></li>
<li>获取 token</li>
<li>调用 <code>/lisa/access/sign</code></li>
<li>解析接口返回的 <code>data</code></li>
<li>成功后写缓存</li>
</ul>
<p><strong>新逻辑</strong>：</p>
<ul>
<li>先查 Redis 缓存</li>
<li>缓存未命中后，直接调用 <code>LisaTripartiteAuthUtils.generateAuthHeader()</code></li>
<li>如果 <code>accessCode/password</code> 未配置，则记录 warn 日志并返回 <code>null</code></li>
<li>如果生成成功，则写入缓存 9 分钟</li>
<li>返回给上层调用者</li>
</ul>
<p>这一步之后，图片鉴权信息的来源已经彻底切成“本地生成”，不会再依赖平台是否开放 <code>/lisa/access/sign</code>。</p>
<h4 id="1-4-4-调整应用配置"><a href="#1-4-4-调整应用配置" class="headerlink" title="1.4.4 调整应用配置"></a>1.4.4 调整应用配置</h4><p>文件：<code>cnsci-admin/src/main/resources/application.yml</code></p>
<p>新增配置：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">video:</span></span><br><span class="line">  <span class="attr">api:</span></span><br><span class="line">    <span class="attr">access-code:</span> <span class="string">gdswnltsgc</span></span><br><span class="line">    <span class="attr">password:</span> <span class="string">PulklN*c@))IDhlJN~Pp</span></span><br></pre></td></tr></table></figure>

<p>同时保留了 <code>image-app-id: appid</code>，并补充说明：当前分支已经不再使用 <code>image-app-id</code> 走 <code>/lisa/access/sign</code>，但字段仍保留。</p>
<p>测试配置文件 <code>cnsci-admin/src/test/resources/application-video-test.yml</code> 也同步补上了测试用的 <code>access-code/password</code>。</p>
<h4 id="1-4-5-重写图片鉴权单元测试"><a href="#1-4-5-重写图片鉴权单元测试" class="headerlink" title="1.4.5 重写图片鉴权单元测试"></a>1.4.5 重写图片鉴权单元测试</h4><p>文件：<code>cnsci-admin/src/test/java/cn/cnsci/web/service/VideoServiceImageAuthTest.java</code></p>
<p>测试由原来的“模拟 <code>/lisa/access/sign</code> HTTP 服务返回”改成了“验证本地签名生成逻辑”。</p>
<p>覆盖了两个核心场景：</p>
<p><strong>场景一：本地生成成功</strong></p>
<p>验证点包括：</p>
<ul>
<li>返回结果不为空</li>
<li><code>x-apig-appid == accessCode</code></li>
<li><code>x-datax-code == accessCode</code></li>
<li><code>x-apig-nonce</code> 以 <code>access_</code> 开头</li>
<li><code>x-apig-timestamp</code> 已生成</li>
<li><code>x-apig-sign</code> 与测试端按同样算法计算出的值一致</li>
<li>Redis 缓存写入被正确调用，过期时间为 9 分钟</li>
</ul>
<p><strong>场景二：配置缺失时降级</strong></p>
<p>验证当 <code>accessCode</code> 缺失时：</p>
<ul>
<li>方法返回 <code>null</code></li>
<li>不写 Redis 缓存</li>
</ul>
<p>这类测试非常关键，因为它不仅验证“正常情况能工作”，也验证“配置不完整时系统会如何表现”。</p>
<h3 id="1-5-为什么这次改动更顺畅"><a href="#1-5-为什么这次改动更顺畅" class="headerlink" title="1.5 为什么这次改动更顺畅"></a>1.5 为什么这次改动更顺畅</h3><p>从工程角度看，这次切换到本地生成方案，比原来走 <code>/lisa/access/sign</code> 更顺畅，主要有以下几个原因。</p>
<p><strong>1. 去掉了一个外部依赖点</strong></p>
<p>原方案需要额外依赖一个远程接口 <code>/lisa/access/sign</code>，而且这个接口还受平台权限控制。</p>
<p>只要其中任意一环有问题，都可能失败：</p>
<ul>
<li>token 是否正确</li>
<li><code>imageAppId</code> 是否正确</li>
<li>接口是否开放权限</li>
<li>平台返回结构是否稳定</li>
<li>联调环境是否一致</li>
</ul>
<p>本地生成后，这条链路直接缩短为：</p>
<ul>
<li>配置存在</li>
<li>算法正确</li>
<li>缓存正确</li>
</ul>
<p>可控性明显更高。</p>
<p><strong>2. 不再受平台账号权限阻塞</strong></p>
<p>旧方案最大的现实问题不是代码写不出来，而是共享账号对 <code>/lisa/access/sign</code> 的权限不稳定或未开通。</p>
<p>这类问题属于典型的“外部平台约束”，排查成本高，等待时间长，而且开发过程中很容易陷入误判：</p>
<ul>
<li>到底是接口文档不对</li>
<li>还是账号没权限</li>
<li>还是请求头不完整</li>
<li>还是 appid 不对</li>
</ul>
<p>本地生成方案直接绕过这个卡点，把问题收敛到后端自己的实现范围内。</p>
<p><strong>3. 更适合缓存设计</strong></p>
<p>旧方案获取的是平台返回的一次性鉴权结果；新方案虽然也是生成 10 分钟有效的鉴权头，但本地可随时重建，因此缓存策略更自然。</p>
<p>当前仍然使用 9 分钟缓存，理由没有变：</p>
<ul>
<li>平台侧有效期 10 分钟</li>
<li>本地缓存压缩到 9 分钟，给过期留缓冲</li>
<li>避免把边缘过期数据返回给前端</li>
</ul>
<p><strong>4. 测试更直接</strong></p>
<p>旧测试需要模拟 HTTP 服务端行为；新测试只需要验证签名规则和缓存行为。</p>
<p>这会带来两个好处：</p>
<ul>
<li>测试更稳定，不依赖本地起临时 server</li>
<li>测试更聚焦业务核心，而不是围着 HTTP 交互打转</li>
</ul>
<h3 id="1-6-需要注意的风险与边界"><a href="#1-6-需要注意的风险与边界" class="headerlink" title="1.6 需要注意的风险与边界"></a>1.6 需要注意的风险与边界</h3><p>这次方案虽然更顺，但也不是没有边界条件。</p>
<p><strong>1. 真实密码进入了配置文件</strong></p>
<p>当前为了尽快跑通，已经把 <code>access-code/password</code> 直接写入 <code>application.yml</code>。</p>
<p>这在联调阶段是最省事的，但从长期维护和安全角度看不是最佳方案。后续更建议：</p>
<ul>
<li>改用环境变量</li>
<li>或者使用更安全的配置托管方式</li>
<li>至少不要把真实凭据继续扩散到更多环境文件和文档中</li>
</ul>
<p><strong>2. 平台算法如果变更，需要同步更新</strong></p>
<p>本地生成方案的前提是：平台验签规则稳定，且与当前拿到的工具类一致。</p>
<p>如果未来平台调整了：</p>
<ul>
<li>nonce 规则</li>
<li>sign 拼接顺序</li>
<li>哈希算法</li>
<li>header 字段名</li>
</ul>
<p>那么本地生成逻辑也必须同步更新。</p>
<p><strong>3. 旧字段仍保留，避免误删</strong></p>
<p>当前 <code>image-app-id</code> 还在配置模型和 yml 中保留，但本分支下已经不参与图片鉴权。</p>
<p>这属于一种“兼容保留”，优点是降低回退成本，缺点是容易让后来接手的人误以为它还在起作用。</p>
<p>因此在后续整理配置时，最好再加一轮说明文档，明确哪些字段仍在使用，哪些只是历史兼容字段。</p>
<h2 id="第二部分：等值面外部接口开发"><a href="#第二部分：等值面外部接口开发" class="headerlink" title="第二部分：等值面外部接口开发"></a>第二部分：等值面外部接口开发</h2><h3 id="2-1-需求背景"><a href="#2-1-需求背景" class="headerlink" title="2.1 需求背景"></a>2.1 需求背景</h3><p>现有 <code>/api/rain/contour</code> 接口使用本地 IDW（反距离加权）算法生成等值面，现需要新增接口调用第三方 GIS 服务获取等值面数据。</p>
<blockquote>
<p>[!info] 第三方接口信息</p>
<ul>
<li><strong>地址</strong>：<code>http://10.144.38.28:9981/isosurface/calculateRainfallIsosurface</code></li>
<li><strong>请求方式</strong>：POST (JSON)</li>
<li><strong>鉴权方式</strong>：RSA（与批量字典接口相同）</li>
</ul>
</blockquote>
<p>这个需求的背景是：本地算法虽然灵活，但计算效率受数据量和算法复杂度限制；而第三方 GIS 服务专门针对空间数据分析进行了优化，能够提供更高效、更专业的等值面生成能力。</p>
<h3 id="2-2-API-设计"><a href="#2-2-API-设计" class="headerlink" title="2.2 API 设计"></a>2.2 API 设计</h3><table>
<thead>
<tr>
<th>项目</th>
<th>值</th>
</tr>
</thead>
<tbody><tr>
<td>路径</td>
<td><code>POST /api/rain/contour/external</code></td>
</tr>
<tr>
<td>请求方式</td>
<td>POST</td>
</tr>
<tr>
<td>Content-Type</td>
<td>application&#x2F;json</td>
</tr>
</tbody></table>
<p><strong>请求参数</strong>：</p>
<table>
<thead>
<tr>
<th>参数</th>
<th>类型</th>
<th>必填</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>startTime</td>
<td>String</td>
<td>否</td>
<td>开始时间，默认当天08:00</td>
</tr>
<tr>
<td>endTime</td>
<td>String</td>
<td>否</td>
<td>结束时间，默认当前小时前</td>
</tr>
<tr>
<td>scale</td>
<td>Double</td>
<td>否</td>
<td>精度，默认0.1</td>
</tr>
<tr>
<td>addvcd</td>
<td>String</td>
<td>否</td>
<td>行政区划码</td>
</tr>
<tr>
<td>bsnm</td>
<td>String</td>
<td>否</td>
<td>流域ID</td>
</tr>
<tr>
<td>stnm</td>
<td>String</td>
<td>否</td>
<td>站点名称</td>
</tr>
<tr>
<td>frgrd</td>
<td>String</td>
<td>否</td>
<td>报讯等级</td>
</tr>
<tr>
<td>admauth</td>
<td>String</td>
<td>否</td>
<td>管理单位</td>
</tr>
</tbody></table>
<h3 id="2-3-实现步骤"><a href="#2-3-实现步骤" class="headerlink" title="2.3 实现步骤"></a>2.3 实现步骤</h3><h4 id="2-3-1-配置添加"><a href="#2-3-1-配置添加" class="headerlink" title="2.3.1 配置添加"></a>2.3.1 配置添加</h4><p>在 <code>application.yml</code> 中添加 gis-isosurface 配置：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">third-party:</span></span><br><span class="line">  <span class="attr">api:</span></span><br><span class="line">    <span class="attr">gis-isosurface:</span></span><br><span class="line">      <span class="attr">base-url:</span> <span class="string">http://10.144.38.28:9981</span></span><br><span class="line">      <span class="attr">app-id:</span> <span class="string">$&#123;third-party.api.hydrology.app-id&#125;</span></span><br><span class="line">      <span class="attr">app-name:</span> <span class="string">$&#123;third-party.api.hydrology.app-name&#125;</span></span><br><span class="line">      <span class="attr">public-key:</span> <span class="string">$&#123;third-party.api.hydrology.public-key&#125;</span></span><br><span class="line">      <span class="attr">timeout:</span> <span class="number">30000</span></span><br></pre></td></tr></table></figure>

<p>同时在 <code>ThirdPartyApiProperties.java</code> 中添加 <code>gisIsosurface</code> 属性。</p>
<h4 id="2-3-2-业务逻辑实现"><a href="#2-3-2-业务逻辑实现" class="headerlink" title="2.3.2 业务逻辑实现"></a>2.3.2 业务逻辑实现</h4><p>新增业务逻辑类 <code>RainfallContourExternalBiz.java</code>，核心处理流程如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 固定筛选：只查询雨量站（PP）</span></span><br><span class="line">filterParams.put(<span class="string">&quot;sttps&quot;</span>, Collections.singletonList(<span class="string">&quot;PP&quot;</span>));</span><br><span class="line"></span><br><span class="line"><span class="comment">// 过滤经纬度为空的测站</span></span><br><span class="line">List&lt;RainfallRealtimeResponse&gt; validStations = stations.stream()</span><br><span class="line">    .filter(s -&gt; s.getLGTD() != <span class="literal">null</span> &amp;&amp; s.getLTTD() != <span class="literal">null</span>)</span><br><span class="line">    .collect(Collectors.toList());</span><br></pre></td></tr></table></figure>

<h4 id="2-3-3-Controller-开发"><a href="#2-3-3-Controller-开发" class="headerlink" title="2.3.3 Controller 开发"></a>2.3.3 Controller 开发</h4><p>在 <code>RainfallRealtimeController.java</code> 中新增 <code>/contour/external</code> 接口：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 直接返回第三方响应的JSON，不再次包装</span></span><br><span class="line"><span class="type">Object</span> <span class="variable">jsonObject</span> <span class="operator">=</span> JSON.parse(result);</span><br><span class="line"><span class="keyword">return</span> AjaxResult.success(jsonObject);</span><br></pre></td></tr></table></figure>

<h4 id="2-3-4-DTO-扩展"><a href="#2-3-4-DTO-扩展" class="headerlink" title="2.3.4 DTO 扩展"></a>2.3.4 DTO 扩展</h4><p>在 <code>RainfallContourQueryDTO.java</code> 中添加 <code>scale</code> 字段，用于控制等值面生成的精度。</p>
<h3 id="2-4-处理逻辑流程"><a href="#2-4-处理逻辑流程" class="headerlink" title="2.4 处理逻辑流程"></a>2.4 处理逻辑流程</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">graph TD</span><br><span class="line">    A[开始] --&gt; B&#123;传入时间参数?&#125;</span><br><span class="line">    B --&gt;|是| C[使用传入的时间]</span><br><span class="line">    B --&gt;|否| D[计算默认时间]</span><br><span class="line">    D --&gt; E[当天08:00 → 当前小时前]</span><br><span class="line">    C --&gt; F[查询测站]</span><br><span class="line">    F --&gt; G[过滤: 只保留雨量站PP]</span><br><span class="line">    G --&gt; H[过滤: 经纬度不为空]</span><br><span class="line">    H --&gt; I[提取STCD列表]</span><br><span class="line">    I --&gt; J[调用第三方API]</span><br><span class="line">    J --&gt; K[返回响应]</span><br></pre></td></tr></table></figure>

<h3 id="2-5-遇到的问题及解决"><a href="#2-5-遇到的问题及解决" class="headerlink" title="2.5 遇到的问题及解决"></a>2.5 遇到的问题及解决</h3><h4 id="问题一：GET-请求报错"><a href="#问题一：GET-请求报错" class="headerlink" title="问题一：GET 请求报错"></a>问题一：GET 请求报错</h4><blockquote>
<p>[!bug] 错误信息</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Request method &#x27;GET&#x27; not supported</span><br></pre></td></tr></table></figure>
</blockquote>
<ul>
<li><strong>原因</strong>：接口是 <code>@PostMapping</code>，但请求用了 GET</li>
<li><strong>解决</strong>：使用 POST 请求</li>
</ul>
<p>这是一个很低级但容易犯的错误。在接口文档中明确标注了 POST 方式，但测试时很容易惯性使用 GET 请求。</p>
<h4 id="问题二：返回格式嵌套"><a href="#问题二：返回格式嵌套" class="headerlink" title="问题二：返回格式嵌套"></a>问题二：返回格式嵌套</h4><blockquote>
<p>[!warning] 问题描述<br>返回值被再次包装成 AjaxResult，导致格式嵌套</p>
</blockquote>
<ul>
<li><strong>原因</strong>：直接 <code>return success(result)</code> 把字符串又包装了一层</li>
<li><strong>解决</strong>：先用 <code>JSON.parse()</code> 解析第三方响应，再返回</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 修复前</span></span><br><span class="line"><span class="keyword">return</span> success(result);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 修复后</span></span><br><span class="line"><span class="type">Object</span> <span class="variable">jsonObject</span> <span class="operator">=</span> JSON.parse(result);</span><br><span class="line"><span class="keyword">return</span> AjaxResult.success(jsonObject);</span><br></pre></td></tr></table></figure>

<p>这个问题很有意思。原始设计中，我们希望统一响应格式，所以所有接口都返回 <code>AjaxResult</code>。但第三方接口返回的本身就是一个 JSON 字符串（里面还包含了 XML 格式的 KML 数据），如果直接包装，会导致两层 JSON 解析，客户端使用时会非常麻烦。</p>
<p>解决思路是：既然第三方已经返回了结构化的 JSON，我们只需要把它解析出来，再放入我们的响应结构中。这样客户端拿到的是干净的嵌套结构。</p>
<h4 id="问题三：测站类型过滤"><a href="#问题三：测站类型过滤" class="headerlink" title="问题三：测站类型过滤"></a>问题三：测站类型过滤</h4><blockquote>
<p>[!warning] 问题描述<br>ZZ（水位站）数据也被传入第三方</p>
</blockquote>
<ul>
<li><strong>原因</strong>：没有过滤测站类型</li>
<li><strong>解决</strong>：添加 <code>STTP = &#39;PP&#39;</code> 过滤，只保留雨量站</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">filterParams.put(<span class="string">&quot;sttps&quot;</span>, Collections.singletonList(<span class="string">&quot;PP&quot;</span>));</span><br></pre></td></tr></table></figure>

<p>这个问题暴露出一个业务理解上的偏差：等值面分析通常只需要降雨数据，而不需要水位数据。虽然从技术上说，把所有测站数据都传过去也能生成结果，但这会增加网络传输量、第三方处理时间，而且生成的结果可能包含不相关的数据。</p>
<p>正确的做法是在调用第三方接口前，先进行业务过滤，只保留雨量站（PP 类型）的数据。</p>
<h3 id="2-6-测试验证"><a href="#2-6-测试验证" class="headerlink" title="2.6 测试验证"></a>2.6 测试验证</h3><p><strong>测试命令</strong>：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 无参数（使用默认时间）</span></span><br><span class="line">curl -X POST <span class="string">&quot;http://localhost:8086/api/rain/contour/external&quot;</span> \</span><br><span class="line">  -H <span class="string">&quot;Content-Type: application/json&quot;</span> \</span><br><span class="line">  -d <span class="string">&#x27;&#123;&#125;&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 指定时间</span></span><br><span class="line">curl -X POST <span class="string">&quot;http://localhost:8086/api/rain/contour/external&quot;</span> \</span><br><span class="line">  -H <span class="string">&quot;Content-Type: application/json&quot;</span> \</span><br><span class="line">  -d <span class="string">&#x27;&#123;&quot;startTime&quot;: &quot;2026-03-17 08:00:00&quot;, &quot;endTime&quot;: &quot;2026-03-17 15:00:00&quot;, &quot;scale&quot;: 0.1&#125;&#x27;</span></span><br></pre></td></tr></table></figure>

<p><strong>响应格式</strong>：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;errorCode&quot;</span><span class="punctuation">:</span> <span class="number">0</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;errorKeyword&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">null</span></span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;errorParam&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span><span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;message&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">null</span></span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;success&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;errorLevel&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">null</span></span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;data&quot;</span><span class="punctuation">:</span> <span class="string">&quot;&lt;?xml version=\&quot;1.0\&quot; encoding=\&quot;UTF-8\&quot;?&gt;\n&lt;kml&gt;...&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<h3 id="2-7-技术要点总结"><a href="#2-7-技术要点总结" class="headerlink" title="2.7 技术要点总结"></a>2.7 技术要点总结</h3><ol>
<li><strong>第三方API集成</strong>：使用 <code>ThirdPartyApiClient.sendPostJson()</code> 调用</li>
<li><strong>测站过滤</strong>：通过 <code>stationFilterParams</code> 传递过滤条件</li>
<li><strong>时间默认值</strong>：参考 <code>RainfallRealtimeServiceImpl.calculateTimeRange()</code> 逻辑</li>
<li><strong>响应处理</strong>：需要先 <code>JSON.parse()</code> 解析，避免嵌套</li>
</ol>
<h2 id="第三部分：经验与总结"><a href="#第三部分：经验与总结" class="headerlink" title="第三部分：经验与总结"></a>第三部分：经验与总结</h2><h3 id="3-1-第三方接口集成的一般性原则"><a href="#3-1-第三方接口集成的一般性原则" class="headerlink" title="3.1 第三方接口集成的一般性原则"></a>3.1 第三方接口集成的一般性原则</h3><p>通过这两个项目实践，我总结出第三方接口集成的一些一般性原则：</p>
<p><strong>1. 优先选择可控性强的方案</strong></p>
<p>在视频图片鉴权的案例中，最初我们选择了依赖远程接口的方案，虽然这个方案在理论上更“规范”，但它引入了一个不可控的外部依赖。当这个依赖出现问题时，我们很难快速定位和解决。</p>
<p>相比之下，本地签名方案虽然看起来不够“规范”，但它把控制权掌握在自己手中，不依赖平台的接口权限，不受平台服务稳定性的影响。在实际工程中，<strong>可控性往往比理论上的规范性更重要</strong>。</p>
<p><strong>2. 做好降级和容错</strong></p>
<p>无论是调用第三方接口还是本地生成鉴权信息，都要考虑失败的情况。在视频图片鉴权方案中，我们保留了配置缺失时的降级逻辑：当 <code>accessCode/password</code> 未配置时，返回 <code>null</code> 而不是抛出异常。这样即使配置有问题，也不会影响整个系统的运行。</p>
<p><strong>3. 缓存策略要合理</strong></p>
<p>对于可以重复使用的鉴权信息、配置信息等，应该设计合理的缓存策略。在视频图片鉴权案例中，我们使用 9 分钟的缓存（平台有效期 10 分钟），既能减少重复计算，又能在平台端失效前主动刷新，避免边缘情况。</p>
<p><strong>4. 测试要覆盖边界条件</strong></p>
<p>在等值面接口开发中，我们遇到了返回格式嵌套、请求方式错误等问题。这些问题如果只在正常路径上测试，是发现不了的。因此，测试用例不仅要覆盖正常情况，还要覆盖：</p>
<ul>
<li>参数缺失的情况</li>
<li>参数边界值的情况</li>
<li>异常情况</li>
</ul>
<p><strong>5. 做好日志记录</strong></p>
<p>在调用第三方接口时，日志记录非常重要。它不仅能帮助我们排查问题，还能让我们了解接口的调用情况和性能表现。建议记录：</p>
<ul>
<li>请求的参数</li>
<li>请求的时间</li>
<li>响应的状态</li>
<li>响应的耗时</li>
<li>异常信息</li>
</ul>
<h3 id="3-2-最小变更集原则"><a href="#3-2-最小变更集原则" class="headerlink" title="3.2 最小变更集原则"></a>3.2 最小变更集原则</h3><p>在视频图片鉴权改造中，我们始终坚持“最小变更集”的原则：只替换图片鉴权这一路，保留其他所有功能。这种做法的优点是：</p>
<ol>
<li><strong>降低风险</strong>：只改动了必要的代码，影响范围小</li>
<li><strong>易于回滚</strong>：如果新方案有问题，可以快速回退到旧方案</li>
<li><strong>便于测试</strong>：只需要测试改动的部分，不需要回归整个模块</li>
</ol>
<h3 id="3-3-文档与配置管理"><a href="#3-3-文档与配置管理" class="headerlink" title="3.3 文档与配置管理"></a>3.3 文档与配置管理</h3><p>在这两个项目中，我们也注意到文档和配置管理的重要性：</p>
<ol>
<li><strong>配置模型扩展</strong>：新增配置项时，要在对应的 Properties 类中添加字段，并在注释中说明用途</li>
<li><strong>保留兼容字段</strong>：像 <code>imageAppId</code> 这样的字段，即使不再使用，也建议保留一段时间，避免回滚时出现问题</li>
<li><strong>说明文档</strong>：对于复杂的配置项，最好在注释中说明其用途和可能的影响</li>
</ol>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>本文记录了广东水文监测系统中两个典型的后端接口开发场景：视频图片鉴权改造和等值面外部接口开发。这两个场景虽然业务不同，但都涉及与第三方平台的接口集成，都需要在功能、性能、可维护性之间做权衡。</p>
<p>通过这两个项目，我深刻体会到：</p>
<ol>
<li><p><strong>工程问题没有绝对的最优解</strong>：远程签发方案理论上更规范，本地生成方案实践上更可控。选择哪个方案，要根据实际情况来判断。</p>
</li>
<li><p><strong>最小变更集是个好原则</strong>：不要为了“美观”而做不必要的改动，每一个改动都应该有明确的目的和预期效果。</p>
</li>
<li><p><strong>测试覆盖要全面</strong>：不仅要测正常路径，还要测边界条件和异常情况。</p>
</li>
<li><p><strong>日志和文档很重要</strong>：好的日志能帮助我们快速定位问题，好的文档能帮助后来的开发者理解系统的设计意图。</p>
</li>
</ol>
<p>希望本文的内容能对有类似需求的开发者有所帮助。如果有任何问题或建议，欢迎交流讨论。</p>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>水文</category>
      </categories>
      <tags>
        <tag>广东水文项目</tag>
        <tag>后端开发</tag>
        <tag>接口开发</tag>
        <tag>第三方集成</tag>
        <tag>调试记录</tag>
      </tags>
  </entry>
  <entry>
    <title>广东水文项目实战：大断面接口优化与水库站超限判断的深度实践</title>
    <url>/2026/04/29/%E5%B9%BF%E4%B8%9C%E6%B0%B4%E6%96%87%E9%A1%B9%E7%9B%AE%E5%AE%9E%E6%88%98/</url>
    <content><![CDATA[<p>在广东水文监测系统项目中，我们针对大断面数据接口和水库站超限判断逻辑进行了多项优化。本文将详细分享这些实战经验，包括测站沿革排序优化、断面数据岸别标识、实时水位集成，以及水库站超限判断的业务逻辑修正。这些改动不仅提升了系统的实用性，也解决了长期存在的业务概念不匹配问题。</p>
<span id="more"></span>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>广东水文监测系统是一个综合性的水文数据管理平台，涵盖河道水位、水库库容、断面测量等多维度数据。在项目迭代过程中，我们发现原有系统存在几个关键痛点：大断面数据接口缺少实时水位信息、测站沿革数据排序混乱、水库站超限判断功能失效等。本文将记录我们如何逐一分析和解决这些问题的完整过程。</p>
<p><img data-src="/images/posts/%E5%B9%BF%E4%B8%9C%E6%B0%B4%E6%96%87%E9%A1%B9%E7%9B%AE%E5%AE%9E%E6%88%98/fig1-data-flow.png" alt="大断面数据处理流程"><br><em>图1：大断面数据处理流程</em></p>
<h2 id="大断面接口完善"><a href="#大断面接口完善" class="headerlink" title="大断面接口完善"></a>大断面接口完善</h2><p>大断面数据是水文监测中的重要组成部分，它记录了河道横断面的地形变化。原有的 <code>/basic/station/archive/stXsDataByStcd</code> 接口功能较为基础，无法满足前端展示的丰富需求。我们对其进行了三项关键完善。</p>
<h3 id="测站沿革排序优化"><a href="#测站沿革排序优化" class="headerlink" title="测站沿革排序优化"></a>测站沿革排序优化</h3><p>测站沿革接口 <code>/basic/station/archive/stationEvolution</code> 返回的是测站的历史变更记录。原实现中，数据按照数据库默认顺序返回，导致用户查看时需要自行寻找最新的变更记录。</p>
<p><strong>解决方案</strong>：从外部 API 获取沿革列表后，按 <code>chgdt</code>（变更日期）降序排序，确保最新的变更排在最前面。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">List&lt;StationEvolutionDTO&gt; list = rows.toJavaList(StationEvolutionDTO.class);</span><br><span class="line"><span class="comment">// 按变更日期降序排序，最新的变更排在最前面</span></span><br><span class="line">list.sort(Comparator.comparing(</span><br><span class="line">        (StationEvolutionDTO dto) -&gt; DateUtils.parseDate(dto.getChgdt()),</span><br><span class="line">        Comparator.nullsLast(Comparator.reverseOrder())</span><br><span class="line">));</span><br><span class="line"><span class="keyword">return</span> list;</span><br></pre></td></tr></table></figure>

<p>测试结果显示，222 条数据正确按日期降序排列，从 2026-03 到 1915-06，覆盖了近百年的历史变迁。</p>
<h3 id="大断面数据增加-position-岸别标识"><a href="#大断面数据增加-position-岸别标识" class="headerlink" title="大断面数据增加 position 岸别标识"></a>大断面数据增加 position 岸别标识</h3><p><img data-src="/images/posts/%E5%B9%BF%E4%B8%9C%E6%B0%B4%E6%96%87%E9%A1%B9%E7%9B%AE%E5%AE%9E%E6%88%98/fig5-position-diagram.png" alt="岸别标识示意图"><br><em>图2：河道断面岸别标识示意图</em></p>
<p>在断面图上，前端需要标注每个测量点属于左堤还是右堤。这需要结合断面数据（外部 API）和堤防高程（本地 <code>StRvfcchB</code> 表）进行判断。</p>
<p><strong>判断逻辑</strong>：</p>
<ul>
<li><code>rvbdel == LDKEL</code> → 标注为 <code>&quot;left&quot;</code>（左堤）</li>
<li><code>rvbdel == RDKEL</code> → 标注为 <code>&quot;right&quot;</code>（右堤）</li>
<li>其他情况 → <code>null</code>（普通测量点）</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">StRvfcchB</span> <span class="variable">rvfcchB</span> <span class="operator">=</span> stRvfcchBService.selectStRvfcchBBySTCD(stcd);</span><br><span class="line"><span class="keyword">if</span> (rvfcchB != <span class="literal">null</span>) &#123;</span><br><span class="line">    <span class="type">BigDecimal</span> <span class="variable">ldkel</span> <span class="operator">=</span> rvfcchB.getLDKEL();</span><br><span class="line">    <span class="type">BigDecimal</span> <span class="variable">rdkel</span> <span class="operator">=</span> rvfcchB.getRDKEL();</span><br><span class="line">    <span class="keyword">for</span> (OpenCrossSectionDataDTO point : dataList) &#123;</span><br><span class="line">        <span class="type">BigDecimal</span> <span class="variable">rvbdel</span> <span class="operator">=</span> point.getRvbdel();</span><br><span class="line">        <span class="keyword">if</span> (rvbdel != <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (ldkel != <span class="literal">null</span> &amp;&amp; rvbdel.compareTo(ldkel) == <span class="number">0</span>) &#123;</span><br><span class="line">                point.setPosition(<span class="string">&quot;left&quot;</span>);</span><br><span class="line">            &#125; <span class="keyword">else</span> <span class="keyword">if</span> (rdkel != <span class="literal">null</span> &amp;&amp; rvbdel.compareTo(rdkel) == <span class="number">0</span>) &#123;</span><br><span class="line">                point.setPosition(<span class="string">&quot;right&quot;</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p><strong>注意</strong>：当前使用精确匹配，实测外部 API 与本地数据精度不一致（差值约 0.7m），后续可能需要改为最近值匹配。</p>
</blockquote>
<h3 id="大断面接口增加实时水位"><a href="#大断面接口增加实时水位" class="headerlink" title="大断面接口增加实时水位"></a>大断面接口增加实时水位</h3><p>为了在断面图上同时展示当前水位线和水位过程线，我们需要在接口中集成实时水位数据。数据来源是河道分钟数据表 <code>StRiverRMin</code>。</p>
<p><strong>实现要点</strong>：</p>
<ul>
<li>支持可选的 <code>startTime</code>&#x2F;<code>endTime</code> 参数，默认查询最近 3 天</li>
<li>返回实时水位值和水位过程线列表</li>
<li>参考水库剖面图接口 <code>getReservoirProfile</code> 的实现模式</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> Map&lt;String, Object&gt; <span class="title function_">getLatestOpenCrossSectionData</span><span class="params">(String stcd, String startTime, String endTime)</span> &#123;</span><br><span class="line">    <span class="comment">// 1. 获取断面数据 + position 标注</span></span><br><span class="line">    <span class="comment">// 2. 查询实时水位</span></span><br><span class="line">    <span class="comment">// 3. 查询水位过程线（默认3天）</span></span><br><span class="line">    <span class="comment">// 4. 组装返回结果</span></span><br><span class="line">    Map&lt;String, Object&gt; result = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;();</span><br><span class="line">    result.put(<span class="string">&quot;stcd&quot;</span>, stcd);</span><br><span class="line">    result.put(<span class="string">&quot;stationName&quot;</span>, station != <span class="literal">null</span> ? station.getSTNM() : <span class="string">&quot;&quot;</span>);</span><br><span class="line">    result.put(<span class="string">&quot;realtimeWaterLevel&quot;</span>, realtimeWaterLevel);</span><br><span class="line">    result.put(<span class="string">&quot;dataTime&quot;</span>, dataTime);</span><br><span class="line">    result.put(<span class="string">&quot;startTime&quot;</span>, actualStartTime);</span><br><span class="line">    result.put(<span class="string">&quot;endTime&quot;</span>, actualEndTime);</span><br><span class="line">    result.put(<span class="string">&quot;waterLevelList&quot;</span>, waterLevelList);</span><br><span class="line">    result.put(<span class="string">&quot;crossSectionData&quot;</span>, dataList);</span><br><span class="line">    <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>测试结果：默认 3 天返回 864 条水位数据，自定义 1 天返回 289 条，实时水位 1.19m。</p>
<h3 id="API-返回结构变更"><a href="#API-返回结构变更" class="headerlink" title="API 返回结构变更"></a>API 返回结构变更</h3><p><img data-src="/images/posts/%E5%B9%BF%E4%B8%9C%E6%B0%B4%E6%96%87%E9%A1%B9%E7%9B%AE%E5%AE%9E%E6%88%98/fig4-api-structure.png" alt="API 返回结构"><br><em>图3：API 返回结构示意图</em></p>
<p>完善后的接口返回结构如下：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;stcd&quot;</span><span class="punctuation">:</span> <span class="string">&quot;81001150&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;stationName&quot;</span><span class="punctuation">:</span> <span class="string">&quot;某水文站&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;realtimeWaterLevel&quot;</span><span class="punctuation">:</span> <span class="number">1.19</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;dataTime&quot;</span><span class="punctuation">:</span> <span class="string">&quot;2026-04-29 10:00:00&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;startTime&quot;</span><span class="punctuation">:</span> <span class="string">&quot;2026-04-26 10:00:00&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;endTime&quot;</span><span class="punctuation">:</span> <span class="string">&quot;2026-04-29 10:00:00&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;waterLevelList&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="punctuation">&#123;</span> <span class="attr">&quot;tm&quot;</span><span class="punctuation">:</span> <span class="string">&quot;2026-04-26 10:05:00&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;z&quot;</span><span class="punctuation">:</span> <span class="number">1.15</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="punctuation">&#123;</span> <span class="attr">&quot;tm&quot;</span><span class="punctuation">:</span> <span class="string">&quot;2026-04-26 10:10:00&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;z&quot;</span><span class="punctuation">:</span> <span class="number">1.16</span> <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;crossSectionData&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="punctuation">&#123;</span> <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="number">1</span><span class="punctuation">,</span> <span class="attr">&quot;xsid&quot;</span><span class="punctuation">:</span> <span class="number">100</span><span class="punctuation">,</span> <span class="attr">&quot;vtno&quot;</span><span class="punctuation">:</span> <span class="number">1</span><span class="punctuation">,</span> <span class="attr">&quot;di&quot;</span><span class="punctuation">:</span> <span class="number">0</span><span class="punctuation">,</span> <span class="attr">&quot;rvbdel&quot;</span><span class="punctuation">:</span> <span class="number">5.2</span><span class="punctuation">,</span> <span class="attr">&quot;position&quot;</span><span class="punctuation">:</span> <span class="string">&quot;left&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="punctuation">&#123;</span> <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="number">2</span><span class="punctuation">,</span> <span class="attr">&quot;xsid&quot;</span><span class="punctuation">:</span> <span class="number">100</span><span class="punctuation">,</span> <span class="attr">&quot;vtno&quot;</span><span class="punctuation">:</span> <span class="number">2</span><span class="punctuation">,</span> <span class="attr">&quot;di&quot;</span><span class="punctuation">:</span> <span class="number">10</span><span class="punctuation">,</span> <span class="attr">&quot;rvbdel&quot;</span><span class="punctuation">:</span> <span class="number">3.8</span><span class="punctuation">,</span> <span class="attr">&quot;position&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">null</span></span> <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">]</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<h2 id="水库站超限判断优化"><a href="#水库站超限判断优化" class="headerlink" title="水库站超限判断优化"></a>水库站超限判断优化</h2><p><img data-src="/images/posts/%E5%B9%BF%E4%B8%9C%E6%B0%B4%E6%96%87%E9%A1%B9%E7%9B%AE%E5%AE%9E%E6%88%98/fig2-alarm-flow.png" alt="水库站超限判断流程"><br><em>图4：水库站超限判断流程图</em></p>
<p>水库站超限判断是水情实时汇总服务中的核心功能，用于判断水库水位是否超过警戒线或保证水位。然而，原有逻辑存在严重的业务概念不匹配问题。</p>
<h3 id="问题背景"><a href="#问题背景" class="headerlink" title="问题背景"></a>问题背景</h3><p><img data-src="/images/posts/%E5%B9%BF%E4%B8%9C%E6%B0%B4%E6%96%87%E9%A1%B9%E7%9B%AE%E5%AE%9E%E6%88%98/fig3-db-schema.png" alt="数据库表关系"><br><em>图5：数据库表关系图</em></p>
<p>原有逻辑对所有站类型统一使用 <code>ST_RVFCCH_B</code> 表的 WRZ（警戒水位）和 GRZ（保证水位）。但水库站业务上应使用不同的水位指标：</p>
<table>
<thead>
<tr>
<th>指标</th>
<th>旧逻辑数据源</th>
<th>水库站正确数据源</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>超警戒判断</td>
<td><code>ST_RVFCCH_B.WRZ</code>（警戒水位）</td>
<td><code>ST_RSVRFSR_B.FSLTDZ</code>（汛限水位）</td>
<td>水库站应以汛限水位为警戒标准</td>
</tr>
<tr>
<td>超保证判断</td>
<td><code>ST_RVFCCH_B.GRZ</code>（保证水位）</td>
<td><code>ST_RSVRFCCH_B.DSFLZ</code>（设计水位）</td>
<td>水库站应以设计水位为保证标准</td>
</tr>
</tbody></table>
<p><strong>核心问题</strong>：水库站没有 GRZ（保证水位）数据，全部为 NULL，导致 <code>IS_OVER_GUARANTEE</code> 永远为 0，超保证判断功能完全失效。</p>
<h3 id="优化方案"><a href="#优化方案" class="headerlink" title="优化方案"></a>优化方案</h3><p>我们采用”优先专用、降级通用”的策略：</p>
<ol>
<li><strong>超警戒判断</strong>：水库站优先使用汛限水位（<code>ST_RSVRFSR_B.FSLTDZ</code>），无数据时降级使用警戒水位（<code>ST_RVFCCH_B.WRZ</code>）</li>
<li><strong>超保证判断</strong>：水库站优先使用设计水位（<code>ST_RSVRFCCH_B.DSFLZ</code>），无数据时降级使用保证水位（<code>ST_RVFCCH_B.GRZ</code>）</li>
</ol>
<h3 id="实现细节"><a href="#实现细节" class="headerlink" title="实现细节"></a>实现细节</h3><p><strong>Mapper 层新增查询方法</strong>：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 查询水库站汛限水位</span></span><br><span class="line">List&lt;FloodLimitWaterLevelDTO&gt; <span class="title function_">selectFloodLimitWaterLevels</span><span class="params">(List&lt;String&gt; stcds)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 查询水库站设计水位</span></span><br><span class="line">List&lt;ReservoirStationAlarmDTO&gt; <span class="title function_">selectDesignWaterLevels</span><span class="params">(List&lt;String&gt; stcds)</span>;</span><br></pre></td></tr></table></figure>

<p><strong>SQL 查询</strong>（按当前月份匹配汛期）：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="operator">&lt;</span><span class="keyword">select</span> id<span class="operator">=</span>&quot;selectFloodLimitWaterLevels&quot; resultType<span class="operator">=</span>&quot;cn.cnsci.system.domain.FloodLimitWaterLevelDTO&quot;<span class="operator">&gt;</span></span><br><span class="line">    <span class="keyword">SELECT</span> r.STCD, r.FSLTDZ</span><br><span class="line">    <span class="keyword">FROM</span> ST_RSVRFSR_B r</span><br><span class="line">    <span class="keyword">WHERE</span> r.STCD <span class="keyword">IN</span></span><br><span class="line">    <span class="operator">&lt;</span>foreach collection<span class="operator">=</span>&quot;list&quot; item<span class="operator">=</span>&quot;stcd&quot; <span class="keyword">open</span><span class="operator">=</span>&quot;(&quot; separator<span class="operator">=</span>&quot;,&quot; <span class="keyword">close</span><span class="operator">=</span>&quot;)&quot;<span class="operator">&gt;</span></span><br><span class="line">        #&#123;stcd&#125;</span><br><span class="line">    <span class="operator">&lt;</span><span class="operator">/</span>foreach<span class="operator">&gt;</span></span><br><span class="line">    <span class="keyword">AND</span> <span class="keyword">MONTH</span>(CURDATE()) <span class="keyword">BETWEEN</span> r.BGMD <span class="keyword">AND</span> r.EDMD</span><br><span class="line"><span class="operator">&lt;</span><span class="operator">/</span><span class="keyword">select</span><span class="operator">&gt;</span></span><br></pre></td></tr></table></figure>

<p><strong>Service 层组装逻辑</strong>：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 水库站优先使用汛限水位（ST_RSVRFSR_B.FSLTDZ），无数据时降级使用警戒水位（ST_RVFCCH_B.WRZ）</span></span><br><span class="line"><span class="type">BigDecimal</span> <span class="variable">fsltdz</span> <span class="operator">=</span> fsltdzMap.get(base.getStcd());</span><br><span class="line"><span class="type">BigDecimal</span> <span class="variable">wrz</span> <span class="operator">=</span> fsltdz != <span class="literal">null</span> ? fsltdz : (warn != <span class="literal">null</span> ? warn.getWrz() : <span class="literal">null</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 水库站优先使用设计水位（ST_RSVRFCCH_B.DSFLZ），无数据时降级使用保证水位（ST_RVFCCH_B.GRZ）</span></span><br><span class="line"><span class="type">BigDecimal</span> <span class="variable">dsflz</span> <span class="operator">=</span> dsflzMap.get(base.getStcd());</span><br><span class="line"><span class="type">BigDecimal</span> <span class="variable">grz</span> <span class="operator">=</span> dsflz != <span class="literal">null</span> ? dsflz : (warn != <span class="literal">null</span> ? warn.getGrz() : <span class="literal">null</span>);</span><br></pre></td></tr></table></figure>

<h3 id="验证结果"><a href="#验证结果" class="headerlink" title="验证结果"></a>验证结果</h3><p>以龙门水库（81705600）为例：</p>
<table>
<thead>
<tr>
<th>测站编码</th>
<th>站名</th>
<th>FSLTDZ</th>
<th>DSFLZ</th>
<th>summary WRZ</th>
<th>summary GRZ</th>
<th>状态</th>
</tr>
</thead>
<tbody><tr>
<td>81705600</td>
<td>龙门水库</td>
<td>57.000</td>
<td>58.850</td>
<td>57.000</td>
<td>58.850</td>
<td>✅</td>
</tr>
</tbody></table>
<p>新逻辑成功修复了水库站 GRZ 全部为 NULL 的问题，超保证判断功能恢复正常。</p>
<h2 id="测站沿革接口数据过滤修复"><a href="#测站沿革接口数据过滤修复" class="headerlink" title="测站沿革接口数据过滤修复"></a>测站沿革接口数据过滤修复</h2><p>在开发过程中，我们还发现了一个数据过滤 bug：调用 <code>stationEvolution</code> 接口时，返回了其他测站的数据。</p>
<h3 id="问题描述"><a href="#问题描述" class="headerlink" title="问题描述"></a>问题描述</h3><p>调用 <code>GET /basic/station/archive/stationEvolution?stcd=80117000</code> 时，返回了 <code>81300042</code>、<code>80901000</code> 等其他测站的数据，而不是 <code>80117000</code> 的数据。</p>
<h3 id="根因分析"><a href="#根因分析" class="headerlink" title="根因分析"></a>根因分析</h3><ol>
<li>第三方接口 <code>/stsc/openapi/listGrouped</code> 不支持按 <code>stcd</code> 参数过滤，返回所有测站数据（222 条）</li>
<li>代码中直接使用第三方接口的分页参数，导致只返回第一页数据（包含其他测站）</li>
<li>没有对返回数据按 <code>iid</code> 进行过滤</li>
</ol>
<h3 id="修复方案"><a href="#修复方案" class="headerlink" title="修复方案"></a>修复方案</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Map&lt;String, String&gt; params = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;();</span><br><span class="line"><span class="comment">// 不传分页参数，获取所有数据</span></span><br><span class="line"><span class="comment">// ...</span></span><br><span class="line">list = list.stream()</span><br><span class="line">        .filter(dto -&gt; iid.equals(dto.getIid()))</span><br><span class="line">        .collect(Collectors.toList());</span><br></pre></td></tr></table></figure>

<p>修复后，只返回 <code>iid=80117000</code> 的 3 条记录，站名均为”德庆”。</p>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><p>通过这次广东水文项目的优化实践，我们总结了以下几点经验：</p>
<ul>
<li><strong>业务概念与数据模型的对齐</strong>：技术实现必须准确反映业务概念，水库站超限判断的问题根源在于数据模型设计时未充分考虑不同站类型的业务差异</li>
<li><strong>降级策略的重要性</strong>：在数据不完整或缺失时，提供合理的降级方案可以保证系统的基本功能不受影响</li>
<li><strong>第三方接口的边界处理</strong>：调用第三方接口时，不能假设其支持所有查询参数，需要在本地进行数据过滤和处理</li>
<li><strong>测试验证的完整性</strong>：每次改动都需要充分的测试验证，特别是涉及历史数据兼容性的场景</li>
</ul>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>水文监测系统的优化是一个持续迭代的过程。通过这次大断面接口完善和水库站超限判断优化，我们不仅提升了系统的实用性和准确性，也为后续的功能扩展奠定了坚实基础。未来，我们将继续关注水文业务的实际需求，不断优化系统功能。</p>
<hr>
<h3 id="本文配乐"><a href="#本文配乐" class="headerlink" title="本文配乐"></a>本文配乐</h3><details>
<summary>点击播放背景音乐</summary>

<audio controls src="/music/guangdong-hydrology-bgm.mp3">
  您的浏览器不支持音频播放。
</audio>

</details>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>水文</category>
      </categories>
      <tags>
        <tag>Spring Boot</tag>
        <tag>Java</tag>
        <tag>水文监测</tag>
        <tag>接口优化</tag>
        <tag>数据库设计</tag>
      </tags>
  </entry>
  <entry>
    <title>一次干净的「放宽约束」重构：让公司楼层配置跨园区</title>
    <url>/2026/07/18/%E6%91%84%E5%83%8F%E5%A4%B4%E5%85%AC%E5%8F%B8%E6%A5%BC%E5%B1%82%E9%85%8D%E7%BD%AE%E8%B7%A8%E5%9B%AD%E5%8C%BA%E7%BB%91%E5%AE%9A/</url>
    <content><![CDATA[<p>公司默认可见楼层配置（<code>/orgFloorConfig/&#123;orgId&#125;</code> 查询、<code>POST /orgFloorConfig</code> 保存）最初的语义是「限当前园区」——超管在 A 园区，只能给 A 内的公司配 A 的楼层。本次把它放宽为「跨园区绑定」：一个公司可以同时绑多个园区的楼层。改动只动 6 个文件 +70&#x2F;-23，但牵住了查询、保存、校验三个「按当前园区」硬编码点的同步改造，是放宽约束类重构的一个干净样本。</p>
<span id="more"></span>

<div class="audio-player">
  <div class="audio-section">
    <h4>🎧 文章导读</h4>
    <audio controls preload="metadata">
      <source src="/audio/摄像头公司楼层配置跨园区绑定/narration.mp3" type="audio/mpeg">
    </audio>
  </div>
  <div class="audio-section">
    <h4>🎵 背景音乐</h4>
    <audio controls loop preload="metadata">
      <source src="/audio/摄像头公司楼层配置跨园区绑定/bgm.mp3" type="audio/mpeg">
    </audio>
  </div>
</div>

<h2 id="一、需求背景：为什么这条线现在才动"><a href="#一、需求背景：为什么这条线现在才动" class="headerlink" title="一、需求背景：为什么这条线现在才动"></a>一、需求背景：为什么这条线现在才动</h2><p>这条线一共四个 commit，本次是收尾：</p>
<table>
<thead>
<tr>
<th>commit</th>
<th>主题</th>
</tr>
</thead>
<tbody><tr>
<td><code>8e0bd588d</code></td>
<td>按公司隔离摄像头可见范围：建 <code>security_camera_org_floor</code> 表，<code>getCameraTree</code> 加可见性过滤</td>
</tr>
<tr>
<td><code>9c7e71ac6</code></td>
<td>新增公司默认可见楼层配置查询&#x2F;保存接口</td>
</tr>
<tr>
<td><code>657502014</code></td>
<td>楼层配置返回每楼层摄像头数（<code>floorCameraCounts</code>）</td>
</tr>
<tr>
<td><code>334cd811d</code></td>
<td><strong>支持跨园区绑定（本次）</strong></td>
</tr>
</tbody></table>
<p>前三个 commit 把「按公司 + 按楼层」的可见性闭环跑通了，但落表和查询都隐含一个假设：「公司所在园区 &#x3D; 楼层所在园区」，代码里这个假设被硬编码成 <code>baseUser.getParkId()</code>。</p>
<p>业务侧的真实场景把假设戳破了：集团超管管着多个园区，一家安保公司可能驻扎在 A 园区，但需要看到 B 园区某栋楼的摄像头（典型的集团统管安保指挥场景）。原来的配置页对这类需求直接瘫痪——超管切到 A 园区，找不到 B 的楼层可勾；切到 B 园区，又没法给 A 的公司改归属。</p>
<p>所以本次改动的本质，不是「加新功能」，而是「把一个写死的隐含假设显式化、然后放开」。</p>

<div class="mermaid">
flowchart LR
    subgraph "园区 A"
        Co["公司 X<br/>（归属园区 A）"]
    end
    subgraph "园区 B"
        F1["楼层 1"]
        F2["楼层 2"]
        F3["楼层 3"]
    end
    Co -.超管在 A 配置.-> F1
    Co -.超管在 A 配置.-> F2
    Co -.超管在 A 配置.-> F3
    style Co fill:#fff4e1,stroke:#e6a23c,stroke-width:2px
    style F1 fill:#e1f5ff,stroke:#3a8fb7
    style F2 fill:#e1f5ff,stroke:#3a8fb7
    style F3 fill:#e1f5ff,stroke:#3a8fb7
</div>


<p><em>图 1：超管在 A 园区，给属于 A 的公司 X 配置 B 园区的楼层</em></p>
<h2 id="二、关键决策：怎么把单园区语义松绑到跨园区"><a href="#二、关键决策：怎么把单园区语义松绑到跨园区" class="headerlink" title="二、关键决策：怎么把单园区语义松绑到跨园区"></a>二、关键决策：怎么把单园区语义松绑到跨园区</h2><p>放宽语义最容易踩的坑不是「想不到新方案」，而是「漏改硬编码点」。一个「按当前园区」约束在三个地方都有硬编码：查询、保存、校验。任何一处没跟着松绑，就会出现「能查不能存」或「能存不能查」的不一致——这类 bug 在多园区系统里最难发现，因为只有跨园区配置时才暴露，单园区回归测试全绿。</p>
<p>本次的设计不是「想一个新方案」，而是「把现有的三处约束对齐到同一个新语义」。新语义是：</p>
<blockquote>
<p><strong>公司仍必须属于当前园区（超管在 A 只能配 A 的公司），但楼层可以来自任意园区。</strong></p>
</blockquote>
<p>这种「公司单园区 + 楼层跨园区」的非对称是有意保留的——公司归属是组织架构决定的，不该被超管随手改；楼层是可见范围配置，本就该灵活。</p>

<div class="mermaid">
flowchart TB
    Org["公司归属<br/>单园区"]
    Park["当前园区"]
    F1["本园区楼层"]
    F2["园区 B 楼层"]
    F3["园区 C 楼层"]
    Org === Park
    Park --- F1
    Park -.跨园区配置.-> F2
    Park -.跨园区配置.-> F3
    style Org fill:#ffeaa7,stroke:#d63031,stroke-width:2px
    style Park fill:#ffeaa7,stroke:#d63031,stroke-width:2px
    style F1 fill:#dfe6e9,stroke:#636e72,stroke-width:2px
    style F2 fill:#74b9ff,stroke:#0984e3,color:#fff
    style F3 fill:#74b9ff,stroke:#0984e3,color:#fff
</div>


<p><em>图 2：公司归属约束保留，楼层绑定放开</em></p>
<blockquote>
<p>[!WARNING]<br>这条非对称需要产品最终确认。「超管在 A 园区给属于 A 的公司 X 绑定 B 园区的楼层」目前是允许的。如果后续产品要求「公司归属园区必须和楼层园区一致」，那现在这套逻辑要回退。代码上两种走向都容易改，但语义要先定死。</p>
</blockquote>
<h2 id="三、跨服务数据聚合：按数据自带的-parkId-分组"><a href="#三、跨服务数据聚合：按数据自带的-parkId-分组" class="headerlink" title="三、跨服务数据聚合：按数据自带的 parkId 分组"></a>三、跨服务数据聚合：按数据自带的 parkId 分组</h2><p>一旦楼层跨园区，就面临「要拉多个园区的空间和摄像头」这个跨服务聚合问题。楼层存在 admin 库的 <code>space_info</code>，摄像头在 security 库。</p>
<p>最朴素的方案是跨库 JOIN——直接被否定，admin 和 security 是两个独立的微服务库，这套架构里就没有跨库 JOIN 的先例。</p>
<p>采用的方案是<strong>按数据自带的维度分组，循环调用单园区接口，合并结果</strong>。<code>SpaceInfoDTO</code> 本身就带 <code>parkId</code> 字段，先拿到本次要绑定的所有楼层，按 <code>parkId</code> 分组，对每个园区分别调 <code>spaceInfoClient.getSpaceInfo(parkId)</code> 拉空间、调 <code>baseMapper.listByCondition(cameraQuery)</code> 拉摄像头，最后合并：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 按楼层自带的 parkId 分组（LinkedHashMap 保序）</span></span><br><span class="line">Map&lt;String, List&lt;String&gt;&gt; floorIdsByPark = groupFloorIdsByPark(floors);</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> (String parkId : floorIdsByPark.keySet()) &#123;</span><br><span class="line">    <span class="comment">// 每个园区分别拉空间</span></span><br><span class="line">    BaseResult&lt;List&lt;SpaceInfoDTO&gt;&gt; spaceResult = spaceInfoClient.getSpaceInfo(parkId);</span><br><span class="line">    spaces.addAll(spaceResult.getData());</span><br><span class="line">    <span class="comment">// 每个园区分别拉摄像头</span></span><br><span class="line">    <span class="type">CameraInfoDTO</span> <span class="variable">cameraQuery</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">CameraInfoDTO</span>();</span><br><span class="line">    cameraQuery.setParkId(parkId);</span><br><span class="line">    cameraQuery.setDeleteFlag(DictConstants.DEL_FLAG_NO);</span><br><span class="line">    cameras.addAll(baseMapper.listByCondition(cameraQuery));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这避开了跨库 JOIN，代价是多次 RPC——但配置页是低频接口，一个公司绑定的园区数也就个位数，完全可以接受。<code>LinkedHashMap</code> 在分组时保序，保证园区渲染顺序稳定，不被 <code>HashMap</code> 的哈希序打乱。</p>
<h2 id="四、实现：6-个文件，三处核心改动"><a href="#四、实现：6-个文件，三处核心改动" class="headerlink" title="四、实现：6 个文件，三处核心改动"></a>四、实现：6 个文件，三处核心改动</h2><p>核心文件 <code>service-provider/security-service/.../service/impl/CameraInfoServiceImpl.java</code>，三处改动必须对齐：</p>
<h3 id="1-查询：getOrgFloorConfig"><a href="#1-查询：getOrgFloorConfig" class="headerlink" title="1. 查询：getOrgFloorConfig"></a>1. 查询：getOrgFloorConfig</h3><p>旧逻辑里查询、统计都绑死当前园区。新逻辑的关键转变是<strong>不再假设所有楼层同园区</strong>——先按 orgId 跨园区查所有绑定楼层，再按每个楼层自带的 parkId 分组：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 旧：查/统计都绑死当前园区</span></span><br><span class="line">List&lt;String&gt; floorSpaceIds = baseMapper.getFloorSpaceIds(</span><br><span class="line">        baseUser.getParkId(), Collections.singleton(orgId));</span><br><span class="line"></span><br><span class="line"><span class="comment">// 新：只按 orgId 查（跨园区），再分园区统计</span></span><br><span class="line">List&lt;String&gt; floorSpaceIds = baseMapper.getOrgFloorSpaceIds(orgId);</span><br><span class="line">List&lt;SpaceInfoDTO&gt; floors = getFloors(floorSpaceIds);</span><br><span class="line">List&lt;SpaceInfoDTO&gt; spaces = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">List&lt;CameraInfoDTO&gt; cameras = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line"><span class="keyword">for</span> (String parkId : groupFloorIdsByPark(floors).keySet()) &#123;</span><br><span class="line">    BaseResult&lt;List&lt;SpaceInfoDTO&gt;&gt; spaceResult = spaceInfoClient.getSpaceInfo(parkId);</span><br><span class="line">    <span class="comment">// ... 分园区累加 spaces / cameras</span></span><br><span class="line">&#125;</span><br><span class="line">result.setFloorCameraCounts(countCamerasByFloor(cameras, spaces, floorSpaceIds));</span><br></pre></td></tr></table></figure>

<h3 id="2-保存：saveOrgFloorConfig"><a href="#2-保存：saveOrgFloorConfig" class="headerlink" title="2. 保存：saveOrgFloorConfig"></a>2. 保存：saveOrgFloorConfig</h3><p>旧：删除&#x2F;插入都带 parkId，强制楼层属于当前园区。新：按 orgId 跨园区全删 + 按园区分组分别插入：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 旧：单园区删 + 单园区插</span></span><br><span class="line">assertFloorsBelongToPark(floorSpaceIds, baseUser.getParkId());</span><br><span class="line">baseMapper.deleteOrgFloors(baseUser.getParkId(), model.getOrgId());</span><br><span class="line">baseMapper.batchInsertOrgFloors(baseUser.getParkId(), model.getOrgId(), floorSpaceIds, ...);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 新：跨园区删 + 分园区插</span></span><br><span class="line">Map&lt;String, List&lt;String&gt;&gt; floorIdsByPark = groupFloorIdsByPark(getFloors(floorSpaceIds));</span><br><span class="line">baseMapper.deleteAllOrgFloors(model.getOrgId());  <span class="comment">// 跨园区全删（只按 orgId）</span></span><br><span class="line"><span class="keyword">for</span> (Map.Entry&lt;String, List&lt;String&gt;&gt; entry : floorIdsByPark.entrySet()) &#123;</span><br><span class="line">    baseMapper.batchInsertOrgFloors(entry.getKey(), model.getOrgId(), entry.getValue(), ...);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-校验：assertFloorsBelongToPark-→-getFloors"><a href="#3-校验：assertFloorsBelongToPark-→-getFloors" class="headerlink" title="3. 校验：assertFloorsBelongToPark → getFloors"></a>3. 校验：assertFloorsBelongToPark → getFloors</h3><p>校验语义从「楼层必须属于当前园区」放宽为「楼层有效 + 必须带 parkId」：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 旧：强制属于当前园区</span></span><br><span class="line">Assert.isTrue(validFloorIds.containsAll(floorSpaceIds), <span class="string">&quot;包含不属于当前园区的楼层&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 新：只校验有效性和园区信息完整性，不再限制哪个园区</span></span><br><span class="line">Assert.isTrue(validFloorIds.containsAll(floorSpaceIds), <span class="string">&quot;包含无效楼层&quot;</span>);</span><br><span class="line">Assert.isTrue(floors.stream().allMatch(space -&gt; StringUtils.isNotBlank(space.getParkId())),</span><br><span class="line">        <span class="string">&quot;楼层缺少园区信息&quot;</span>);</span><br></pre></td></tr></table></figure>

<h3 id="配套改动（4-个文件）"><a href="#配套改动（4-个文件）" class="headerlink" title="配套改动（4 个文件）"></a>配套改动（4 个文件）</h3><ul>
<li><code>SpaceInfoClient</code>（admin-api）：新增 Feign <code>GET /getSpaceInfo/&#123;parkId&#125;</code>，供 security 侧分园区拉空间</li>
<li><code>SpaceInfoServiceImpl.selectSpaceInfoByIds</code>：加 <code>deleteFlag=DEL_FLAG_NO</code> 过滤软删空间，并改调 <code>baseMapper.listByCondition</code>（让 deleteFlag 生效）</li>
<li><code>CameraInfoMapper</code> + <code>CameraInfoMapperExt.xml</code>：新增 <code>getOrgFloorSpaceIds(orgId)</code>、<code>deleteAllOrgFloors(orgId)</code>，分别只按 orgId 查 &#x2F; 删</li>
<li><code>CameraInfoServiceImplTest</code>：新增 <code>floorConfigGroupsFloorsByTheirOwnPark</code> 验证按 parkId 正确分组；mapper 契约测试加 <code>deleteAllOrgFloors</code> 断言</li>
</ul>
<h3 id="权限守卫（未变，但关键）"><a href="#权限守卫（未变，但关键）" class="headerlink" title="权限守卫（未变，但关键）"></a>权限守卫（未变，但关键）</h3><p>两个入口共用 <code>getConfigAdmin()</code>，强制超管才能操作：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Assert.isTrue(isSuperAdmin(baseUser.getUserId()), <span class="string">&quot;无权限管理公司摄像头楼层配置&quot;</span>);</span><br></pre></td></tr></table></figure>

<p>controller 层无 <code>@PreAuthorize</code>，权限完全靠这一行 service 守卫——这意味着放开到非超管时只需改这一行。</p>
<h2 id="五、经验总结"><a href="#五、经验总结" class="headerlink" title="五、经验总结"></a>五、经验总结</h2><h3 id="1-放宽约束类重构的-checklist"><a href="#1-放宽约束类重构的-checklist" class="headerlink" title="1. 放宽约束类重构的 checklist"></a>1. 放宽约束类重构的 checklist</h3><p>碰到「从 X 放宽到非 X」的改动，按这个 checklist 走：</p>
<ol>
<li><strong>找出所有「X 假设」的硬编码点</strong>——grep 当前模块里所有写死的位置，本次查出查询、删除、校验三处</li>
<li><strong>逐一定义新语义</strong>——每个硬编码点放宽后的新约束是什么？必须显式写出来，不能模糊</li>
<li><strong>同步改、一起提</strong>——不要拆成多个 PR，部分放宽会留下「能 X 不能 Y」的不一致</li>
<li><strong>非对称语义要产品确认</strong>——本次「公司单园区 + 楼层跨园区」的不对称是设计选择，不是 bug</li>
</ol>
<h3 id="2-跨服务数据聚合的通用模式"><a href="#2-跨服务数据聚合的通用模式" class="headerlink" title="2. 跨服务数据聚合的通用模式"></a>2. 跨服务数据聚合的通用模式</h3><p>当需要跨多个同构子域聚合数据，而下游接口只支持单子域查询时，通用做法是：</p>
<blockquote>
<p>按数据自带的子域维度分组 → 循环调用单子域接口 → 合并结果。</p>
</blockquote>
<p>这避开了跨库 JOIN，代价是多次 RPC。低频接口（N 个子域个位数）完全可接受；高频接口要重新评估，可考虑预聚合或 ES 反查。</p>
<h3 id="3-死代码的-surgical-处理"><a href="#3-死代码的-surgical-处理" class="headerlink" title="3. 死代码的 surgical 处理"></a>3. 死代码的 surgical 处理</h3><p>旧的 <code>getFloorSpaceIds(parkId, ...)</code>、<code>deleteOrgFloors(parkId, ...)</code> 两个 mapper 方法在本次改动后已无调用方。按 surgical 原则<strong>没删</strong>，只记录——它们离本次改动很近，删之前要确认所有调用方（mapper 方法可能被 service 反射调用、或后续 commit 引用）。后续若连续 2-3 个 sprint 仍无引用，再删不迟。</p>
<hr>
<blockquote>
<p>本次改动落地 6 个文件 +70&#x2F;-23，单测覆盖新增分组逻辑，mapper 契约测试加 <code>deleteAllOrgFloors</code> 断言。无跨服务侵入、无表结构变更、无 API 路径变更，向后兼容。</p>
</blockquote>

<script src="https://cdn.jsdelivr.net/npm/mermaid@10.6.1/dist/mermaid.min.js"></script>
<script>
if (typeof mermaid !== 'undefined') {
  mermaid.initialize({ startOnLoad: true, theme: 'default', securityLevel: 'loose', flowchart: { useMaxWidth: true } });
}
</script>

]]></content>
      <categories>
        <category>项目笔记</category>
        <category>南网</category>
      </categories>
      <tags>
        <tag>Spring Cloud</tag>
        <tag>重构</tag>
        <tag>Java</tag>
        <tag>跨服务聚合</tag>
        <tag>园区隔离</tag>
      </tags>
  </entry>
  <entry>
    <title>智谱 GLM Coding 抢购机器人实战</title>
    <url>/2026/03/14/%E6%99%BA%E8%B0%B1GLM-Coding%E6%8A%A2%E8%B4%AD%E6%9C%BA%E5%99%A8%E4%BA%BA%E5%AE%9E%E6%88%98/</url>
    <content><![CDATA[<p>在 AI 大模型火热的当下，智谱 AI 的 GLM Coding 套餐因为性价比高经常被抢空。本文分享我开发的 ZipuRobot 抢购机器人，从 0 到 1 实现自动监控库存、自动抢购的完整过程。</p>
<span id="more"></span>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>智谱 AI 的 GLM Coding 套餐（Lite&#x2F;Pro&#x2F;Max）一直热销，尤其是 Pro 版本经常在补货后几分钟内售罄。作为一名程序员，与其每次准点守在电脑前刷新页面，不如用技术手段解决这个问题。</p>
<p>于是我开发了 <strong>ZipuRobot</strong>——一个基于 Python + Playwright 的智谱 GLM Coding 抢购机器人。</p>
<h2 id="项目介绍"><a href="#项目介绍" class="headerlink" title="项目介绍"></a>项目介绍</h2><p>ZipuRobot 是一个自动化抢购脚本，核心目标是：</p>
<ul>
<li><strong>自动监控库存</strong>：实时检测目标套餐是否有货</li>
<li><strong>自动完成抢购</strong>：检测到有货后自动点击购买</li>
<li><strong>静默登录</strong>：通过 Cookie 实现免登录启动</li>
<li><strong>定时任务</strong>：支持设置定时运行</li>
</ul>
<p>当前版本已经过实战验证，能够稳定完成「启动 → 登录 → 监控」的完整流程。</p>
<h2 id="技术架构"><a href="#技术架构" class="headerlink" title="技术架构"></a>技术架构</h2><h3 id="技术栈"><a href="#技术栈" class="headerlink" title="技术栈"></a>技术栈</h3><table>
<thead>
<tr>
<th>技术</th>
<th>用途</th>
</tr>
</thead>
<tbody><tr>
<td>Python 3.10+</td>
<td>主语言</td>
</tr>
<tr>
<td>Playwright</td>
<td>浏览器自动化</td>
</tr>
<tr>
<td>asyncio</td>
<td>异步任务调度</td>
</tr>
<tr>
<td>unittest</td>
<td>单元测试</td>
</tr>
</tbody></table>
<h3 id="核心模块"><a href="#核心模块" class="headerlink" title="核心模块"></a>核心模块</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">zhipuglmbot/</span><br><span class="line">├── main.py              # 主入口，包含 ZhipuRobot 类</span><br><span class="line">├── src/</span><br><span class="line">│   ├── config.py        # 配置管理</span><br><span class="line">│   ├── cookie_manager.py # Cookie 管理</span><br><span class="line">│   ├── inventory_monitor.py # 库存监控</span><br><span class="line">│   ├── purchase_bot.py # 抢购执行</span><br><span class="line">│   ├── scheduler.py    # 定时任务</span><br><span class="line">│   └── logger.py       # 日志模块</span><br><span class="line">├── config.json         # 配置文件</span><br><span class="line">├── cookie.json         # 登录 Cookie</span><br><span class="line">└── logs/app.log        # 运行日志</span><br></pre></td></tr></table></figure>

<h2 id="核心实现"><a href="#核心实现" class="headerlink" title="核心实现"></a>核心实现</h2><h3 id="1-主入口与初始化"><a href="#1-主入口与初始化" class="headerlink" title="1. 主入口与初始化"></a>1. 主入口与初始化</h3><p>主程序入口在 <code>main.py</code>，核心类是 <code>ZhipuRobot</code>：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># main.py</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ZhipuRobot</span>:</span><br><span class="line">    <span class="string">&quot;&quot;&quot;智谱 AI 抢购机器人。&quot;&quot;&quot;</span></span><br><span class="line"></span><br><span class="line">    PRODUCT_URL = <span class="string">&quot;https://bigmodel.cn/glm-coding&quot;</span></span><br><span class="line">    LOGIN_WARNING_THRESHOLD = <span class="number">4</span></span><br><span class="line">    LOGIN_WAIT_TIMEOUT_MS = <span class="number">400</span></span><br><span class="line">    LOGIN_WAIT_POLLING_MS = <span class="number">100</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, config_path: <span class="built_in">str</span> = <span class="string">&quot;config.json&quot;</span>, verbose: <span class="built_in">bool</span> = <span class="literal">False</span></span>):</span><br><span class="line">        <span class="variable language_">self</span>.config = Config(config_path)</span><br><span class="line">        <span class="variable language_">self</span>.logger = setup_logger(<span class="string">&quot;zipurobot&quot;</span>, <span class="string">&quot;DEBUG&quot;</span> <span class="keyword">if</span> verbose <span class="keyword">else</span> <span class="string">&quot;INFO&quot;</span>)</span><br><span class="line">        <span class="variable language_">self</span>.cookie_manager = CookieManager()</span><br><span class="line">        <span class="comment"># ... 初始化浏览器相关变量</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">async</span> <span class="keyword">def</span> <span class="title function_">init_browser</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="string">&quot;&quot;&quot;初始化浏览器并应用本地 Cookie。&quot;&quot;&quot;</span></span><br><span class="line">        <span class="keyword">from</span> playwright.async_api <span class="keyword">import</span> async_playwright</span><br><span class="line"></span><br><span class="line">        <span class="variable language_">self</span>.playwright = <span class="keyword">await</span> async_playwright().start()</span><br><span class="line">        <span class="variable language_">self</span>.browser = <span class="keyword">await</span> <span class="variable language_">self</span>.playwright.chromium.launch(</span><br><span class="line">            headless=<span class="variable language_">self</span>.config.get(<span class="string">&quot;monitor.headless&quot;</span>, <span class="literal">False</span>)</span><br><span class="line">        )</span><br><span class="line">        <span class="variable language_">self</span>.context = <span class="keyword">await</span> <span class="variable language_">self</span>.browser.new_context(</span><br><span class="line">            viewport=&#123;<span class="string">&quot;width&quot;</span>: <span class="number">1440</span>, <span class="string">&quot;height&quot;</span>: <span class="number">900</span>&#125;,</span><br><span class="line">            user_agent=(</span><br><span class="line">                <span class="string">&quot;Mozilla/5.0 (Windows NT 10.0; Win64; x64) &quot;</span></span><br><span class="line">                <span class="string">&quot;AppleWebKit/537.36 (KHTML, like Gecko) &quot;</span></span><br><span class="line">                <span class="string">&quot;Chrome/120.0.0.0 Safari/537.36&quot;</span></span><br><span class="line">            ),</span><br><span class="line">        )</span><br><span class="line">        <span class="variable language_">self</span>.page = <span class="keyword">await</span> <span class="variable language_">self</span>.context.new_page()</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 加载并应用 Cookie</span></span><br><span class="line">        <span class="variable language_">self</span>.cookie_manager.load_cookie()</span><br><span class="line">        <span class="keyword">await</span> <span class="variable language_">self</span>.cookie_manager.apply_cookie(<span class="variable language_">self</span>.page)</span><br></pre></td></tr></table></figure>

<h3 id="2-配置管理"><a href="#2-配置管理" class="headerlink" title="2. 配置管理"></a>2. 配置管理</h3><p>配置管理通过 <code>Config</code> 类实现，支持点号分隔的键访问：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># src/config.py</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Config</span>:</span><br><span class="line">    <span class="string">&quot;&quot;&quot;配置管理&quot;&quot;&quot;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, config_path: <span class="built_in">str</span> = <span class="literal">None</span></span>):</span><br><span class="line">        <span class="variable language_">self</span>.config_path = Path(config_path)</span><br><span class="line">        <span class="variable language_">self</span>._data: <span class="built_in">dict</span> = &#123;&#125;</span><br><span class="line">        <span class="variable language_">self</span>.load()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">get</span>(<span class="params">self, key: <span class="built_in">str</span>, default: <span class="type">Any</span> = <span class="literal">None</span></span>) -&gt; <span class="type">Any</span>:</span><br><span class="line">        <span class="string">&quot;&quot;&quot;获取配置值，支持点号分隔的键&quot;&quot;&quot;</span></span><br><span class="line">        keys = key.split(<span class="string">&#x27;.&#x27;</span>)</span><br><span class="line">        value = <span class="variable language_">self</span>._data</span><br><span class="line">        <span class="keyword">for</span> k <span class="keyword">in</span> keys:</span><br><span class="line">            <span class="keyword">if</span> <span class="built_in">isinstance</span>(value, <span class="built_in">dict</span>):</span><br><span class="line">                value = value.get(k)</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                <span class="keyword">return</span> default</span><br><span class="line">            <span class="keyword">if</span> value <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">                <span class="keyword">return</span> default</span><br><span class="line">        <span class="keyword">return</span> value</span><br></pre></td></tr></table></figure>

<p>配置示例 <code>config.json</code>：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;target&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;plan&quot;</span><span class="punctuation">:</span> <span class="string">&quot;pro&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;cycle&quot;</span><span class="punctuation">:</span> <span class="string">&quot;quarterly&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;monitor&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;headless&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">false</span></span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;timeout&quot;</span><span class="punctuation">:</span> <span class="number">300</span></span><br><span class="line">  <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;retry&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;max_attempts&quot;</span><span class="punctuation">:</span> <span class="number">3</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;base_interval&quot;</span><span class="punctuation">:</span> <span class="number">5</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<h3 id="3-Cookie-管理"><a href="#3-Cookie-管理" class="headerlink" title="3. Cookie 管理"></a>3. Cookie 管理</h3><p>Cookie 管理负责登录态的持久化：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># src/cookie_manager.py</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">CookieManager</span>:</span><br><span class="line">    <span class="string">&quot;&quot;&quot;Cookie 管理&quot;&quot;&quot;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, cookie_path: <span class="built_in">str</span> = <span class="literal">None</span></span>):</span><br><span class="line">        <span class="variable language_">self</span>.cookie_path = Path(cookie_path)</span><br><span class="line">        <span class="variable language_">self</span>._cookies: <span class="type">List</span>[<span class="type">Dict</span>] = []</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">load_cookie</span>(<span class="params">self</span>) -&gt; <span class="type">List</span>[<span class="type">Dict</span>]:</span><br><span class="line">        <span class="string">&quot;&quot;&quot;加载 Cookie&quot;&quot;&quot;</span></span><br><span class="line">        <span class="keyword">if</span> <span class="variable language_">self</span>.cookie_path.exists():</span><br><span class="line">            <span class="keyword">with</span> <span class="built_in">open</span>(<span class="variable language_">self</span>.cookie_path, <span class="string">&#x27;r&#x27;</span>, encoding=<span class="string">&#x27;utf-8&#x27;</span>) <span class="keyword">as</span> f:</span><br><span class="line">                <span class="variable language_">self</span>._cookies = json.load(f)</span><br><span class="line">        <span class="keyword">return</span> <span class="variable language_">self</span>._cookies</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">save_cookie</span>(<span class="params">self, cookies: <span class="type">List</span>[<span class="type">Dict</span>]</span>):</span><br><span class="line">        <span class="string">&quot;&quot;&quot;保存 Cookie&quot;&quot;&quot;</span></span><br><span class="line">        <span class="variable language_">self</span>.cookie_path.parent.mkdir(parents=<span class="literal">True</span>, exist_ok=<span class="literal">True</span>)</span><br><span class="line">        <span class="keyword">with</span> <span class="built_in">open</span>(<span class="variable language_">self</span>.cookie_path, <span class="string">&#x27;w&#x27;</span>, encoding=<span class="string">&#x27;utf-8&#x27;</span>) <span class="keyword">as</span> f:</span><br><span class="line">            json.dump(cookies, f, indent=<span class="number">2</span>, ensure_ascii=<span class="literal">False</span>)</span><br><span class="line">        <span class="variable language_">self</span>._cookies = cookies</span><br><span class="line"></span><br><span class="line">    <span class="keyword">async</span> <span class="keyword">def</span> <span class="title function_">apply_cookie</span>(<span class="params">self, page</span>):</span><br><span class="line">        <span class="string">&quot;&quot;&quot;应用 Cookie 到页面&quot;&quot;&quot;</span></span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> <span class="variable language_">self</span>._cookies:</span><br><span class="line">            <span class="variable language_">self</span>.load_cookie()</span><br><span class="line">        <span class="keyword">if</span> <span class="variable language_">self</span>._cookies:</span><br><span class="line">            <span class="keyword">await</span> page.context.add_cookies(<span class="variable language_">self</span>._cookies)</span><br></pre></td></tr></table></figure>

<h3 id="4-登录态识别"><a href="#4-登录态识别" class="headerlink" title="4. 登录态识别"></a>4. 登录态识别</h3><p>登录态识别是抢购的第一步，最初使用了不稳定的特征，后来根据实测改为更可靠的信号：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># main.py - 登录检测核心逻辑</span></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">def</span> <span class="title function_">login_check</span>(<span class="params">self</span>) -&gt; <span class="built_in">bool</span>:</span><br><span class="line">    <span class="string">&quot;&quot;&quot;验证登录状态，并在登录成功后持久化 Cookie。&quot;&quot;&quot;</span></span><br><span class="line">    <span class="keyword">await</span> <span class="variable language_">self</span>.page.goto(<span class="variable language_">self</span>.PRODUCT_URL, wait_until=<span class="string">&quot;domcontentloaded&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="variable language_">self</span>.logger.info(<span class="string">&quot;正在验证登录状态...&quot;</span>)</span><br><span class="line">    retry_count = <span class="number">0</span></span><br><span class="line">    max_retries = <span class="number">120</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 登录信号检测脚本</span></span><br><span class="line">    login_signal_script = <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">    () =&gt; &#123;</span></span><br><span class="line"><span class="string">        const text = document.body.innerText || &#x27;&#x27;;</span></span><br><span class="line"><span class="string">        const signals = &#123;</span></span><br><span class="line"><span class="string">            exitText: text.includes(&#x27;退出&#x27;) || text.includes(&#x27;Logout&#x27;),</span></span><br><span class="line"><span class="string">            profileText: text.includes(&#x27;个人中心&#x27;),</span></span><br><span class="line"><span class="string">            userProfileName: !!document.querySelector(&#x27;.user-profile-name, [class*=username]&#x27;),</span></span><br><span class="line"><span class="string">            loginRegister: text.includes(&#x27;登录 / 注册&#x27;),</span></span><br><span class="line"><span class="string">            myPlan: text.includes(&#x27;我的编程套餐&#x27;),   // 登录后顶部会出现</span></span><br><span class="line"><span class="string">            apiKey: text.includes(&#x27;API Key&#x27;)         // 登录后可见</span></span><br><span class="line"><span class="string">        &#125;;</span></span><br><span class="line"><span class="string">        const isLoggedIn = !signals.loginRegister &amp;&amp; (</span></span><br><span class="line"><span class="string">            signals.exitText ||</span></span><br><span class="line"><span class="string">            signals.profileText ||</span></span><br><span class="line"><span class="string">            signals.userProfileName ||</span></span><br><span class="line"><span class="string">            signals.myPlan ||</span></span><br><span class="line"><span class="string">            signals.apiKey</span></span><br><span class="line"><span class="string">        );</span></span><br><span class="line"><span class="string">        return isLoggedIn ? signals : false;</span></span><br><span class="line"><span class="string">    &#125;</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> retry_count &lt; max_retries:</span><br><span class="line">        <span class="keyword">try</span>:</span><br><span class="line">            handle = <span class="keyword">await</span> <span class="variable language_">self</span>.page.wait_for_function(</span><br><span class="line">                login_signal_script,</span><br><span class="line">                timeout=<span class="variable language_">self</span>.LOGIN_WAIT_TIMEOUT_MS,</span><br><span class="line">                polling=<span class="variable language_">self</span>.LOGIN_WAIT_POLLING_MS,</span><br><span class="line">            )</span><br><span class="line">            signals = <span class="keyword">await</span> handle.json_value()</span><br><span class="line">            <span class="variable language_">self</span>.logger.info(<span class="string">f&quot;登录成功验证 (信号: <span class="subst">&#123;signals&#125;</span>)，正在同步 Cookie...&quot;</span>)</span><br><span class="line">            <span class="comment"># 登录成功后保存最新 Cookie</span></span><br><span class="line">            <span class="variable language_">self</span>.cookie_manager.save_cookie(<span class="keyword">await</span> <span class="variable language_">self</span>.context.cookies())</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line">        <span class="keyword">except</span> PlaywrightTimeoutError:</span><br><span class="line">            retry_count += <span class="number">1</span></span><br><span class="line">            <span class="keyword">if</span> retry_count == <span class="variable language_">self</span>.LOGIN_WARNING_THRESHOLD:</span><br><span class="line">                <span class="variable language_">self</span>.logger.warning(<span class="string">&quot;未检测到登录，请在弹出的浏览器中手动扫码/登录！&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">False</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p>[!tip] 关键发现<br>经过多次实测，发现最稳定的登录信号是「页面顶部出现 <code>我的编程套餐</code> 或 <code>API Key</code>」，而不是传统的「退出」按钮或个人中心入口。</p>
</blockquote>
<h3 id="5-库存监控"><a href="#5-库存监控" class="headerlink" title="5. 库存监控"></a>5. 库存监控</h3><p>库存监控是核心功能，采用双层策略：</p>
<ul>
<li><strong>快速 DOM 检查</strong>：每轮循环快速检查按钮状态</li>
<li><strong>自适应刷新</strong>：根据超时时间动态调整页面刷新间隔</li>
</ul>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># src/inventory_monitor.py</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">InventoryMonitor</span>:</span><br><span class="line">    <span class="string">&quot;&quot;&quot;库存监控&quot;&quot;&quot;</span></span><br><span class="line"></span><br><span class="line">    BUY_BUTTON_SELECTOR = <span class="string">&quot;.buy-btn.el-button--primary&quot;</span></span><br><span class="line">    PLAN_INDEX = &#123;</span><br><span class="line">        <span class="string">&quot;lite&quot;</span>: <span class="number">0</span>,</span><br><span class="line">        <span class="string">&quot;pro&quot;</span>: <span class="number">1</span>,</span><br><span class="line">        <span class="string">&quot;max&quot;</span>: <span class="number">2</span>,</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">    @staticmethod</span></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">get_reload_interval_ms</span>(<span class="params">elapsed_seconds: <span class="built_in">float</span>, timeout_seconds: <span class="built_in">int</span></span>) -&gt; <span class="built_in">int</span>:</span><br><span class="line">        <span class="string">&quot;&quot;&quot;根据监控进度返回自适应刷新间隔。&quot;&quot;&quot;</span></span><br><span class="line">        <span class="keyword">if</span> timeout_seconds &lt;= <span class="number">0</span>:</span><br><span class="line">            <span class="keyword">return</span> <span class="number">1000</span></span><br><span class="line"></span><br><span class="line">        progress = elapsed_seconds / timeout_seconds</span><br><span class="line">        <span class="keyword">if</span> progress &gt;= <span class="number">0.9</span>:  <span class="comment"># 临近超时，加快刷新</span></span><br><span class="line">            <span class="keyword">return</span> <span class="number">1000</span></span><br><span class="line">        <span class="keyword">if</span> progress &gt;= <span class="number">0.6</span>:  <span class="comment"># 中期</span></span><br><span class="line">            <span class="keyword">return</span> <span class="number">3000</span></span><br><span class="line">        <span class="keyword">return</span> <span class="number">5000</span>  <span class="comment"># 初期</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">async</span> <span class="keyword">def</span> <span class="title function_">check_stock</span>(<span class="params">self, plan: <span class="built_in">str</span> = <span class="string">&quot;pro&quot;</span></span>) -&gt; <span class="built_in">bool</span>:</span><br><span class="line">        <span class="string">&quot;&quot;&quot;精准检测目标方案的库存状态&quot;&quot;&quot;</span></span><br><span class="line">        <span class="keyword">try</span>:</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">await</span> <span class="variable language_">self</span>.page.evaluate(<span class="string">f&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">                (targetPlan) =&gt; &#123;&#123;</span></span><br><span class="line"><span class="string">                    const buttons = Array.from(document.querySelectorAll(&quot;<span class="subst">&#123;self.BUY_BUTTON_SELECTOR&#125;</span>&quot;));</span></span><br><span class="line"><span class="string">                    const indexMap = &#123;&#123; lite: 0, pro: 1, max: 2 &#125;&#125;;</span></span><br><span class="line"><span class="string">                    const index = indexMap[targetPlan.toLowerCase()] ?? indexMap.pro;</span></span><br><span class="line"><span class="string">                    const btn = buttons[index];</span></span><br><span class="line"><span class="string">                    if (!btn) return false;</span></span><br><span class="line"><span class="string">                    return !btn.classList.contains(&#x27;is-disabled&#x27;) &amp;&amp; !btn.disabled;</span></span><br><span class="line"><span class="string">                &#125;&#125;</span></span><br><span class="line"><span class="string">            &quot;&quot;&quot;</span>, plan)</span><br><span class="line">        <span class="keyword">except</span> Exception <span class="keyword">as</span> e:</span><br><span class="line">            <span class="variable language_">self</span>.logger.error(<span class="string">f&quot;检查库存失败: &#123;&#123;e&#125;&#125;&quot;</span>)</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">async</span> <span class="keyword">def</span> <span class="title function_">wait_for_stock</span>(<span class="params">self, plan: <span class="built_in">str</span> = <span class="string">&quot;pro&quot;</span>, timeout: <span class="built_in">int</span> = <span class="number">300</span>, check_interval_ms: <span class="built_in">int</span> = <span class="number">500</span></span>) -&gt; <span class="built_in">bool</span>:</span><br><span class="line">        <span class="string">&quot;&quot;&quot;补货监控循环&quot;&quot;&quot;</span></span><br><span class="line">        <span class="variable language_">self</span>.logger.info(<span class="string">f&quot;🚀 启动【<span class="subst">&#123;plan&#125;</span>】精准监控模式 (频率: <span class="subst">&#123;check_interval_ms&#125;</span>ms)&quot;</span>)</span><br><span class="line">        start_time = asyncio.get_event_loop().time()</span><br><span class="line">        last_reload_at = start_time</span><br><span class="line"></span><br><span class="line">        <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">            now = asyncio.get_event_loop().time()</span><br><span class="line">            elapsed = now - start_time</span><br><span class="line">            <span class="comment"># 自适应刷新间隔</span></span><br><span class="line">            reload_interval_ms = <span class="variable language_">self</span>.get_reload_interval_ms(elapsed, timeout)</span><br><span class="line"></span><br><span class="line">            <span class="comment"># 定时刷新页面获取最新状态</span></span><br><span class="line">            <span class="keyword">if</span> now - last_reload_at &gt;= reload_interval_ms / <span class="number">1000.0</span>:</span><br><span class="line">                <span class="keyword">await</span> <span class="variable language_">self</span>.page.reload(wait_until=<span class="string">&quot;domcontentloaded&quot;</span>)</span><br><span class="line">                last_reload_at = asyncio.get_event_loop().time()</span><br><span class="line"></span><br><span class="line">            <span class="comment"># 检查是否有货</span></span><br><span class="line">            <span class="keyword">if</span> <span class="keyword">await</span> <span class="variable language_">self</span>.check_stock(plan):</span><br><span class="line">                <span class="variable language_">self</span>.logger.info(<span class="string">f&quot;🎯 监测到【<span class="subst">&#123;plan&#125;</span>】补货完成！&quot;</span>)</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> elapsed &gt; timeout:</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line">            <span class="keyword">await</span> asyncio.sleep(check_interval_ms / <span class="number">1000.0</span>)</span><br></pre></td></tr></table></figure>

<h3 id="6-主执行流程"><a href="#6-主执行流程" class="headerlink" title="6. 主执行流程"></a>6. 主执行流程</h3><p>完整的抢购流程：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># main.py - 主流程</span></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">def</span> <span class="title function_">run</span>(<span class="params">self</span>) -&gt; <span class="built_in">bool</span>:</span><br><span class="line">    <span class="string">&quot;&quot;&quot;执行主流程。&quot;&quot;&quot;</span></span><br><span class="line">    <span class="variable language_">self</span>.logger.info(<span class="string">&quot;=&quot;</span> * <span class="number">50</span>)</span><br><span class="line">    <span class="variable language_">self</span>.logger.info(<span class="string">&quot;开始执行抢购任务&quot;</span>)</span><br><span class="line">    <span class="variable language_">self</span>.logger.info(<span class="string">&quot;=&quot;</span> * <span class="number">50</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        <span class="comment"># 1. 初始化浏览器</span></span><br><span class="line">        <span class="keyword">await</span> <span class="variable language_">self</span>.init_browser()</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 2. 检查登录态</span></span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> <span class="keyword">await</span> <span class="variable language_">self</span>.login_check():</span><br><span class="line">            <span class="variable language_">self</span>.logger.error(<span class="string">&quot;登录超时，任务终止&quot;</span>)</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 3. 获取目标套餐配置</span></span><br><span class="line">        target_plan = <span class="variable language_">self</span>.config.get(<span class="string">&quot;target.plan&quot;</span>, <span class="string">&quot;pro&quot;</span>)</span><br><span class="line">        monitor = InventoryMonitor(<span class="variable language_">self</span>.page, <span class="variable language_">self</span>.logger)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 4. 进入库存监控</span></span><br><span class="line">        <span class="variable language_">self</span>.logger.info(<span class="string">f&quot;开始进入库存监控状态 (方案: <span class="subst">&#123;target_plan&#125;</span>)...&quot;</span>)</span><br><span class="line">        stock_start = asyncio.get_event_loop().time()</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> <span class="keyword">await</span> monitor.wait_for_stock(</span><br><span class="line">            plan=target_plan,</span><br><span class="line">            timeout=<span class="variable language_">self</span>.config.get(<span class="string">&quot;monitor.timeout&quot;</span>, <span class="number">300</span>),</span><br><span class="line">            check_interval_ms=<span class="number">300</span>,</span><br><span class="line">        ):</span><br><span class="line">            <span class="variable language_">self</span>.logger.warning(<span class="string">&quot;监控结束，未检测到补货&quot;</span>)</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 5. 检测到有货，点击购买</span></span><br><span class="line">        <span class="variable language_">self</span>.logger.info(<span class="string">f&quot;发现库存！准备下单...&quot;</span>)</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> <span class="keyword">await</span> monitor.click_buy_button(plan=target_plan):</span><br><span class="line">            <span class="variable language_">self</span>.logger.error(<span class="string">f&quot;点击 <span class="subst">&#123;target_plan&#125;</span> 订阅按钮失败&quot;</span>)</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 6. 执行购买流程</span></span><br><span class="line">        purchase_bot = PurchaseBot(<span class="variable language_">self</span>.page, <span class="variable language_">self</span>.logger, <span class="variable language_">self</span>.config)</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">await</span> purchase_bot.execute_purchase():</span><br><span class="line">            <span class="variable language_">self</span>.logger.info(<span class="string">&quot;抢购流程执行完成！请在浏览器中确认支付结果。&quot;</span>)</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line"></span><br><span class="line">        <span class="variable language_">self</span>.logger.error(<span class="string">&quot;购买流程中途失败&quot;</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line">    <span class="keyword">finally</span>:</span><br><span class="line">        <span class="keyword">await</span> <span class="variable language_">self</span>.close_browser()</span><br></pre></td></tr></table></figure>

<h2 id="使用教程"><a href="#使用教程" class="headerlink" title="使用教程"></a>使用教程</h2><h3 id="环境准备"><a href="#环境准备" class="headerlink" title="环境准备"></a>环境准备</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 克隆项目</span></span><br><span class="line">git <span class="built_in">clone</span> &lt;repo-url&gt;</span><br><span class="line"><span class="built_in">cd</span> zhipuglmbot</span><br><span class="line"></span><br><span class="line"><span class="comment"># 安装依赖</span></span><br><span class="line">pip install -r requirements.txt</span><br><span class="line"></span><br><span class="line"><span class="comment"># 安装 Playwright 浏览器</span></span><br><span class="line">playwright install chromium</span><br></pre></td></tr></table></figure>

<h3 id="配置说明"><a href="#配置说明" class="headerlink" title="配置说明"></a>配置说明</h3><p>编辑 <code>config.json</code>：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;target&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;plan&quot;</span><span class="punctuation">:</span> <span class="string">&quot;pro&quot;</span><span class="punctuation">,</span>        <span class="comment">// lite | pro | max</span></span><br><span class="line">    <span class="attr">&quot;cycle&quot;</span><span class="punctuation">:</span> <span class="string">&quot;quarterly&quot;</span> <span class="comment">// monthly | quarterly | yearly</span></span><br><span class="line">  <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;monitor&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;headless&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">false</span></span><span class="punctuation">,</span>   <span class="comment">// 是否无头运行</span></span><br><span class="line">    <span class="attr">&quot;timeout&quot;</span><span class="punctuation">:</span> <span class="number">300</span>        <span class="comment">// 超时时间(秒)</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<h3 id="运行方式"><a href="#运行方式" class="headerlink" title="运行方式"></a>运行方式</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 正常运行</span></span><br><span class="line">python main.py</span><br><span class="line"></span><br><span class="line"><span class="comment"># 调试模式（显示浏览器）</span></span><br><span class="line">python main.py -v</span><br><span class="line"></span><br><span class="line"><span class="comment"># 首次运行会自动打开浏览器，扫码登录后会自动保存 Cookie</span></span><br></pre></td></tr></table></figure>

<h3 id="套餐定位"><a href="#套餐定位" class="headerlink" title="套餐定位"></a>套餐定位</h3><p>页面上有 3 个购买按钮（Lite&#x2F;Pro&#x2F;Max），通过固定索引定位：</p>
<table>
<thead>
<tr>
<th>索引</th>
<th>套餐</th>
<th>选择器</th>
</tr>
</thead>
<tbody><tr>
<td>0</td>
<td>Lite</td>
<td><code>.buy-btn.el-button--primary</code> (第1个)</td>
</tr>
<tr>
<td>1</td>
<td>Pro</td>
<td><code>.buy-btn.el-button--primary</code> (第2个)</td>
</tr>
<tr>
<td>2</td>
<td>Max</td>
<td><code>.buy-btn.el-button--primary</code> (第3个)</td>
</tr>
</tbody></table>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><h3 id="踩过的坑"><a href="#踩过的坑" class="headerlink" title="踩过的坑"></a>踩过的坑</h3><ol>
<li><strong>Cookie 过期</strong>：智谱的 Cookie 会定期失效，需要定时刷新</li>
<li><strong>页面结构变化</strong>：购买按钮的选择器可能随页面更新变化，需要定期维护</li>
<li><strong>弹窗结构不稳定</strong>：真实购买弹窗的 DOM 结构难以稳定获取，建议补货时手动抓取</li>
<li><strong>登录信号误判</strong>：最初使用「退出」「个人中心」作为登录信号，实测发现不稳定，改为「我的编程套餐」「API Key」更可靠</li>
</ol>
<h3 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h3><ul>
<li>请确保 Cookie 有效，支付时可能需要手动确认</li>
<li>抢购成功后请及时完成支付</li>
<li>本工具仅供学习交流，请遵守网站服务条款</li>
</ul>
<h2 id="下一步计划"><a href="#下一步计划" class="headerlink" title="下一步计划"></a>下一步计划</h2><ol>
<li>在真实补货时抓取购买弹窗 DOM</li>
<li>校准周期选择器和最终确认按钮</li>
<li>完成一次完整的实战抢购验证</li>
</ol>
<p>如果你对这个项目感兴趣，欢迎在评论区交流讨论！</p>
<hr>
<p><em>相关文档：[[2026-03-14-zhipurobot-usage]] [[2026-03-14-zhipurobot-status]]</em></p>
]]></content>
      <categories>
        <category>后端开发</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>自动化</tag>
        <tag>Playwright</tag>
        <tag>抢购机器人</tag>
      </tags>
  </entry>
  <entry>
    <title>数据归集 6 月探查实录：三天三件事，把树形接口、数据脏坑、跨源机制全部扒一遍</title>
    <url>/2026/06/03/%E6%95%B0%E6%8D%AE%E5%BD%92%E9%9B%866%E6%9C%88%E6%8E%A2%E6%9F%A5%E5%AE%9E%E5%BD%95-%E4%B8%89%E5%A4%A9%E4%B8%89%E4%BB%B6%E4%BA%8B/</url>
    <content><![CDATA[<blockquote>
<p>把六月的”理所当然”全部扒开看一遍，结果挖出 5 个数据地雷、3 套接口改造方案、和 1 张被历史迁移搞脏的组织架构表。本文用三天三件事的实录方式，把<strong>树选择器大改造 → 接口对接与脏数据逻辑 → 跨源机制与文档&#x2F;代码对账</strong>完整记录下来，每一节都附证据链（SQL、commit、commit message、API 验证结果），让没参与的同学也能照着复现。</p>
</blockquote>
<div class="audio-player">
  <div class="audio-section">
    <h4>🎧 文章导读</h4>
    <audio controls preload="metadata">
      <source src="/audio/数据归集6月探查实录-三天三件事/narration.mp3" type="audio/mpeg">
      您的浏览器不支持音频播放。
    </audio>
  </div>
  <div class="audio-section">
    <h4>🎵 背景音乐</h4>
    <audio controls loop preload="metadata">
      <source src="/audio/数据归集6月探查实录-三天三件事/bgm.mp3" type="audio/mpeg">
      您的浏览器不支持音频播放。
    </audio>
  </div>
</div>

<h2 id="🎬-视频集（4-段-6-秒概览）"><a href="#🎬-视频集（4-段-6-秒概览）" class="headerlink" title="🎬 视频集（4 段 6 秒概览）"></a>🎬 视频集（4 段 6 秒概览）</h2><blockquote>
<p>4 段 6 秒 AI 生成视频，分别对应”开场 → 6&#x2F;1 树选择器 → 6&#x2F;2 接口对接 → 6&#x2F;3 数据地雷”。配合 BGM 食用更佳。</p>
</blockquote>
<div class="video-gallery" style="display: grid; grid-template-columns: repeat(2, 1fr); gap: 16px; margin: 24px 0;">

<div class="video-item">

<p><strong>🎞️ 1. 开场：数据流（intro-flow）</strong></p>
<video controls preload="metadata" style="width: 100%; border-radius: 8px;">
  <source src="/videos/数据归集6月探查实录-三天三件事/intro-flow.mp4" type="video/mp4">
  您的浏览器不支持视频播放。
</video>

<p><em>开场动效——光数据在玻璃管道中流动</em></p>
</div>

<div class="video-item">

<p><strong>🎞️ 2. 6&#x2F;1 树选择器生长（tree-growth）</strong></p>
<video controls preload="metadata" style="width: 100%; border-radius: 8px;">
  <source src="/videos/数据归集6月探查实录-三天三件事/tree-growth.mp4" type="video/mp4">
  您的浏览器不支持视频播放。
</video>

<p><em>6&#x2F;1 主题——从单根到多分支的树形结构生长</em></p>
</div>

<div class="video-item">

<p><strong>🎞️ 3. 6&#x2F;2 接口对接（api-contracts）</strong></p>
<video controls preload="metadata" style="width: 100%; border-radius: 8px;">
  <source src="/videos/数据归集6月探查实录-三天三件事/api-contracts.mp4" type="video/mp4">
  您的浏览器不支持视频播放。
</video>

<p><em>6&#x2F;2 主题——API 文档卡片漂浮连接</em></p>
</div>

<div class="video-item">

<p><strong>🎞️ 4. 6&#x2F;3 数据地雷（data-mines）</strong></p>
<video controls preload="metadata" style="width: 100%; border-radius: 8px;">
  <source src="/videos/数据归集6月探查实录-三天三件事/data-mines.mp4" type="video/mp4">
  您的浏览器不支持视频播放。
</video>

<p><em>6&#x2F;3 主题——5 个警告三角在暗夜中脉冲</em></p>
</div>

</div>

<span id="more"></span>

<h2 id="写在前面：为什么是”探查实录”"><a href="#写在前面：为什么是”探查实录”" class="headerlink" title="写在前面：为什么是”探查实录”"></a>写在前面：为什么是”探查实录”</h2><p>数据归集系统是广东水文项目的核心模块，承担着把测站、设备、视频、流域、行政区多源数据按树形结构对外暴露的职责。2026 年 5 月 21 日，我们刚把”从 Redis 读 GZIP JSON”重构为”直连数据库 + 多数据源路由”（参见上一篇博客《树结构选择器：从 Redis 到直连数据库的架构重构》），6 月 1 日就接到了新的需求：前端要 type 字段、加过滤接口、调整接口签名。</p>
<p>本以为这只是几次小迭代，结果三天里越扒越深：</p>
<ul>
<li><strong>6&#x2F;1</strong>：树选择器大改造、发现 148 个孤儿设备、把”所有流域”虚拟壳拆掉</li>
<li><strong>6&#x2F;2</strong>：写接口对接文档、设备类型筛选接口、负一级河流的来源追到上游数据</li>
<li><strong>6&#x2F;3</strong>：本以为”探查”可以收尾，结果挖出 5 个数据地雷——组织架构、跨源机制、注解不一致、设备类型污染、公司表脏数据</li>
</ul>
<p>这篇博客的目的不是炫耀”挖了多少坑”，而是给后来者一份<strong>证据齐全的复现手册</strong>——所有的发现都附 SQL、commit、commit message 和 API 验证结果，遇到相同问题的同学可以照着查。</p>
<blockquote>
<p>全文约 12000+ 字，配 5+ 张图、1 段导读音频、1 段背景音乐。建议收藏后分段阅读。</p>
</blockquote>
<hr>
<h2 id="一、6-月-1-日：树选择器大改造"><a href="#一、6-月-1-日：树选择器大改造" class="headerlink" title="一、6 月 1 日：树选择器大改造"></a>一、6 月 1 日：树选择器大改造</h2><h3 id="1-1-起点：5-月改造后的第一次回访"><a href="#1-1-起点：5-月改造后的第一次回访" class="headerlink" title="1.1 起点：5 月改造后的第一次回访"></a>1.1 起点：5 月改造后的第一次回访</h3><p>5 月 21 日那波改造（参见 <a href="/2026/05/21/%E6%A0%91%E7%BB%93%E6%9E%84%E9%80%89%E6%8B%A9%E5%99%A8-%E4%BB%8ERedis%E5%88%B0%E7%9B%B4%E8%BF%9E%E6%95%B0%E6%8D%AE%E5%BA%93/">《树结构选择器：从 Redis 到直连数据库的架构重构》</a>）的成果是：</p>
<ul>
<li>把 <code>riverBasinTree</code> 等 12 个 tree 接口从 Redis 缓存的 7MB JSON 改为按需直连数据库</li>
<li>引入了 baomidou-dynamic-datasource 做多数据源路由</li>
<li>通过 <code>setTreeNodeType()</code> 给节点打上 <code>type</code> 字段</li>
</ul>
<p>6 月 1 日早上，用户反馈：</p>
<ol>
<li><code>equipmentTreeFilteredByBasin</code> 返回的树不全，6 个根流域都”消失”了</li>
<li>前端要 4 个 <code>*Filtered</code> 接口的节点带 <code>type</code> 字段</li>
<li>想把”所有流域”这个壳去掉，让 7 个根流域直接并列</li>
</ol>
<p>三件事都安排在同一天开工。看起来是三个独立任务，但做完发现它们<strong>互相纠缠</strong>——<code>type</code> 字段的实现路径要从 5&#x2F;21 改造的代码里复用，”所有流域”壳拆掉后又反过来影响 <code>equipmentTreeFilteredByBasin</code> 的数据呈现。</p>
<h3 id="1-2-第一件事：审查-6-个新提交的接口"><a href="#1-2-第一件事：审查-6-个新提交的接口" class="headerlink" title="1.2 第一件事：审查 6 个新提交的接口"></a>1.2 第一件事：审查 6 个新提交的接口</h3><p>早上 9 点开工，第一件事是审查前一天 commit 的 6 个新接口：</p>
<table>
<thead>
<tr>
<th>接口</th>
<th>类型</th>
<th>状态</th>
</tr>
</thead>
<tbody><tr>
<td><code>stationTreeFiltered</code></td>
<td>行政区测站过滤</td>
<td>新</td>
</tr>
<tr>
<td><code>equipmentTreeFiltered</code></td>
<td>行政区设备过滤</td>
<td>新</td>
</tr>
<tr>
<td><code>stationTreeFilteredByBasin</code></td>
<td>流域测站过滤</td>
<td>新</td>
</tr>
<tr>
<td><code>equipmentTreeFilteredByBasin</code></td>
<td>流域设备过滤</td>
<td>新</td>
</tr>
<tr>
<td><code>stationList</code></td>
<td>分页查所有测站</td>
<td>新</td>
</tr>
<tr>
<td><code>stationEquipmentList</code></td>
<td>测站设备树</td>
<td>新</td>
</tr>
</tbody></table>
<p>挨个 review 后发现 3 个问题：</p>
<p><strong>问题 1：4 个 <code>*Filtered</code> 接口跟原接口功能完全重复</strong></p>
<p><code>equipmentTreeByBasin</code> 末尾已经调了 <code>pruneEmptyBranches</code>，这个方法实际上做了 <code>filterTreeByLeafType</code> 等价的事——按叶子节点类型剪枝。那么新加的 <code>equipmentTreeFilteredByBasin</code> 等于把”过滤”逻辑写了两遍。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// equipmentTreeByBasin 末尾</span></span><br><span class="line">pruneEmptyBranches(root);</span><br><span class="line"><span class="keyword">return</span> R.ok(root);</span><br></pre></td></tr></table></figure>

<p><strong>问题 2：<code>stationList</code> 缺 3 个防护</strong></p>
<ul>
<li>没加 <code>USFL=1</code> 过滤——会返回已删除的测站</li>
<li>没加 <code>size &gt; 0</code> 下界保护——<code>pageSize=0</code> 时 MyBatis-Plus 行为未定义</li>
<li>没显式 <code>select(...)</code> 列——返回全表字段，性能浪费</li>
</ul>
<p><strong>问题 3：<code>stationEquipmentList</code> 排序不稳定</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Map&lt;String, List&lt;StStbprpB&gt;&gt; map = stations.stream()</span><br><span class="line">    .collect(Collectors.groupingBy(StStbprpB::getStcd));</span><br><span class="line"><span class="comment">// ↑ 默认是 HashMap，遍历顺序随机</span></span><br></pre></td></tr></table></figure>

<p>前端拿到的设备列表每次顺序都不一样，调试时同事以为是 bug。</p>
<p><strong>修复建议（已加入 P1 待办）</strong>：</p>
<ol>
<li><code>equipmentTreeFilteredByBasin</code> 跟 <code>equipmentTreeByBasin</code> 功能重复 → 删一个</li>
<li><code>stationList</code> 加 <code>USFL=1</code> 过滤 + <code>size &gt; 0</code> 校验 + 显式 select 列</li>
<li><code>stationEquipmentList</code> 加 <code>USFL=1</code> 过滤 + 改用 <code>LinkedHashMap::new</code> 保证顺序</li>
</ol>
<blockquote>
<p>[!warning] 经验教训<br>写新接口前先看一下底层方法是否已经做了相同的事。下次写 <code>*Filtered</code> 之前必须 review <code>pruneEmptyBranches</code> 的实现。</p>
</blockquote>
<h3 id="1-3-第二件事：紫洞设备丢失问题排查（5-步定位）"><a href="#1-3-第二件事：紫洞设备丢失问题排查（5-步定位）" class="headerlink" title="1.3 第二件事：紫洞设备丢失问题排查（5 步定位）"></a>1.3 第二件事：紫洞设备丢失问题排查（5 步定位）</h3><p>接下来是当天的”重头戏”——用户反馈 <code>equipmentTreeFilteredByBasin</code> 返回的树只有 1 个根流域（珠江三角洲），其他 6 个根（西江、HB、HC、HE1A、816、817）整条被剪光，紫洞设备也丢了。</p>
<h4 id="1-3-1-排查过程"><a href="#1-3-1-排查过程" class="headerlink" title="1.3.1 排查过程"></a>1.3.1 排查过程</h4><p><strong>第一步：怀疑调用链有问题</strong></p>
<p>最初以为 <code>equipmentTreeFilteredByBasin</code> 跟 <code>riverBasinTree</code> 调用链不一致，绕了一圈发现两者都从 <code>getRiverBasinTree()</code> 开始拿完整树，<strong>只是 <code>equipmentTreeByBasin</code> 多走了 5 步</strong>（attachStationsToBasinTree → pruneEmptyNodes → attachEquipmentsToStationTree → pruneEmptyBranches → filterTreeByLeafType）。</p>
<p><strong>第二步：实际请求 7 个根看差异</strong></p>
<p>用 Python 调两个接口对比：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># riverBasinTree: all 下 7 个根</span></span><br><span class="line"><span class="built_in">all</span> → <span class="number">7</span> children (HA/HB/HC/<span class="number">812_813</span>/HE1A/<span class="number">816</span>/<span class="number">817</span>)</span><br><span class="line"><span class="number">812_813</span> → <span class="number">32</span> children</span><br><span class="line"></span><br><span class="line"><span class="comment"># equipmentTreeFilteredByBasin: all 下只剩 1 个根</span></span><br><span class="line"><span class="built_in">all</span> → <span class="number">1</span> child (<span class="number">812_813</span>)</span><br><span class="line"><span class="number">812_813</span> → <span class="number">1</span> child (812_813_-<span class="number">1</span>)  <span class="comment"># -1级河流</span></span><br><span class="line">812_813_-<span class="number">1</span> → <span class="number">6</span> children</span><br></pre></td></tr></table></figure>

<p><strong>第三步：定位到 <code>pruneEmptyBranches</code> 剪枝函数</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="type">boolean</span> <span class="title function_">pruneEmptyBranches</span><span class="params">(TreeNode node)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (node.getChildren() == <span class="literal">null</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (是测站) <span class="keyword">return</span> <span class="literal">false</span>;  <span class="comment">// 没设备的测站 → 删</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    node.getChildren().removeIf(child -&gt; !pruneEmptyBranches(child));</span><br><span class="line">    <span class="keyword">return</span> !node.getChildren().isEmpty();  <span class="comment">// 子全删了 → 自己也被删</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>pruneEmptyBranches</code> 是<strong>向上传染</strong>的：一个测站没设备 → 删测站 → 整个子流域空 → 删子流域 → 父流域空 → 删父流域 → … 一路传到根。</p>
<p><strong>第四步：查 7 个根的测站数和设备数</strong></p>
<table>
<thead>
<tr>
<th>根流域</th>
<th>测站数</th>
<th>设备数</th>
<th>在 equipment 树中</th>
</tr>
</thead>
<tbody><tr>
<td>HA 西江流域</td>
<td>104</td>
<td>0</td>
<td>❌</td>
</tr>
<tr>
<td>HB</td>
<td>185</td>
<td>0</td>
<td>❌</td>
</tr>
<tr>
<td>HC</td>
<td>144</td>
<td>0</td>
<td>❌</td>
</tr>
<tr>
<td>812_813 珠江三角洲</td>
<td>195</td>
<td>18</td>
<td>✅</td>
</tr>
<tr>
<td>HE1A</td>
<td>137</td>
<td>0</td>
<td>❌</td>
</tr>
<tr>
<td>816</td>
<td>111</td>
<td>0</td>
<td>❌</td>
</tr>
<tr>
<td>817</td>
<td>170</td>
<td>0</td>
<td>❌</td>
</tr>
</tbody></table>
<p><strong>6 个根整条被剪掉是因为它们的测站全部没有非 VD 设备</strong>。问题变成了”为啥这么多测站没设备”。</p>
<p><strong>第五步：直接查数据库找紫洞</strong></p>
<p>用 MySQL MCP 查 <code>bs_sw_cy_dh_equipmentb</code>：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> bs_sw_cy_dh_equipmentb <span class="keyword">WHERE</span> eqnm <span class="keyword">LIKE</span> <span class="string">&#x27;%紫洞%&#x27;</span>;</span><br><span class="line"><span class="comment">-- 结果：</span></span><br><span class="line"><span class="comment">-- EQID: &quot; VV81303560&quot;  (前导空格)</span></span><br><span class="line"><span class="comment">-- EQNM: 紫洞</span></span><br><span class="line"><span class="comment">-- EQTP: HADCP-水平式ADCP</span></span><br><span class="line"><span class="comment">-- STCD: &quot;&quot;              ← 关键</span></span><br><span class="line"><span class="comment">-- USFL: 1</span></span><br></pre></td></tr></table></figure>

<p><strong>找到了！紫洞作为设备存在，但 STCD 是空字符串。</strong> <code>equipmentTreeByBasin</code> 按 <code>STCD IN (stcds)</code> 查询时空 STCD 匹配不上，所以这个设备被遗漏。</p>
<h4 id="1-3-2-扩大排查"><a href="#1-3-2-扩大排查" class="headerlink" title="1.3.2 扩大排查"></a>1.3.2 扩大排查</h4><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="built_in">COUNT</span>(<span class="operator">*</span>) <span class="keyword">AS</span> total,</span><br><span class="line">       <span class="built_in">SUM</span>(<span class="keyword">CASE</span> <span class="keyword">WHEN</span> STCD <span class="keyword">IS</span> <span class="keyword">NULL</span> <span class="keyword">OR</span> STCD <span class="operator">=</span> <span class="string">&#x27;&#x27;</span> <span class="keyword">THEN</span> <span class="number">1</span> <span class="keyword">ELSE</span> <span class="number">0</span> <span class="keyword">END</span>) <span class="keyword">AS</span> no_stcd,</span><br><span class="line">       <span class="built_in">SUM</span>(<span class="keyword">CASE</span> <span class="keyword">WHEN</span> EQID <span class="keyword">LIKE</span> <span class="string">&#x27; %&#x27;</span> <span class="keyword">THEN</span> <span class="number">1</span> <span class="keyword">ELSE</span> <span class="number">0</span> <span class="keyword">END</span>) <span class="keyword">AS</span> leading_space_eqid</span><br><span class="line"><span class="keyword">FROM</span> bs_sw_cy_dh_equipmentb <span class="keyword">WHERE</span> USFL <span class="operator">=</span> <span class="string">&#x27;1&#x27;</span>;</span><br><span class="line"><span class="comment">-- total=1813  no_stcd=148  leading_space_eqid=1</span></span><br></pre></td></tr></table></figure>

<p><strong>148 个设备 STCD 为空</strong>——这才是 equipment 树”数据不全”的真正原因。</p>
<h4 id="1-3-3-按-EQTP-分布"><a href="#1-3-3-按-EQTP-分布" class="headerlink" title="1.3.3 按 EQTP 分布"></a>1.3.3 按 EQTP 分布</h4><table>
<thead>
<tr>
<th>EQTP</th>
<th>数量</th>
</tr>
</thead>
<tbody><tr>
<td>HADCP-水平式ADCP</td>
<td>63</td>
</tr>
<tr>
<td>FWQ-浮子水位计</td>
<td>37</td>
</tr>
<tr>
<td>RDV-电波流速仪</td>
<td>34</td>
</tr>
<tr>
<td>RWG-雷达水位计</td>
<td>7</td>
</tr>
<tr>
<td>BWG-气泡水位计</td>
<td>3</td>
</tr>
<tr>
<td>PWG-压力水位计</td>
<td>2</td>
</tr>
<tr>
<td>在线H-ADCP</td>
<td>1</td>
</tr>
<tr>
<td>HADCP-RDV</td>
<td>1</td>
</tr>
</tbody></table>
<p>都是真设备，不是 VD 视频设备。</p>
<h4 id="1-3-4-根因与修复"><a href="#1-3-4-根因与修复" class="headerlink" title="1.3.4 根因与修复"></a>1.3.4 根因与修复</h4><p>数据问题，不是代码 bug：</p>
<ol>
<li><code>bs_sw_cy_dh_equipmentb</code> 表中 148 个设备的 STCD 字段为空</li>
<li><code>equipmentTreeByBasin</code> 按 STCD 关联测站和设备，STCD 空就匹配不到</li>
<li>这些孤儿设备集中在 6 个根流域，导致这 6 个根在 equipment 树里整条消失</li>
<li>紫洞（VV81303560）是 148 个之一，还附带一个前导空格的小问题</li>
</ol>
<p><strong>推荐修复方式：补数据</strong></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 候选查询：找出每个孤儿设备可能的测站</span></span><br><span class="line"><span class="keyword">SELECT</span> e.EQID, e.EQNM, e.EQTYP, e.LGTD, e.LTTD,</span><br><span class="line">       s.STCD, s.STNM</span><br><span class="line"><span class="keyword">FROM</span> bs_sw_cy_dh_equipmentb e</span><br><span class="line"><span class="keyword">LEFT</span> <span class="keyword">JOIN</span> bs_sw_cy_dh_station s</span><br><span class="line">  <span class="keyword">ON</span> <span class="built_in">ABS</span>(s.LGTD <span class="operator">-</span> e.LGTD) <span class="operator">&lt;</span> <span class="number">0.01</span></span><br><span class="line"> <span class="keyword">AND</span> <span class="built_in">ABS</span>(s.LTTD <span class="operator">-</span> e.LTTD) <span class="operator">&lt;</span> <span class="number">0.01</span></span><br><span class="line"><span class="keyword">WHERE</span> e.USFL <span class="operator">=</span> <span class="string">&#x27;1&#x27;</span> <span class="keyword">AND</span> (e.STCD <span class="keyword">IS</span> <span class="keyword">NULL</span> <span class="keyword">OR</span> e.STCD <span class="operator">=</span> <span class="string">&#x27;&#x27;</span>);</span><br></pre></td></tr></table></figure>

<p><strong>不推荐修复方式：改代码做模糊匹配</strong></p>
<p>让 equipment 树对 STCD 为空的设备做名称匹配，会引入不确定性。数据问题应该用数据方式解决。</p>
<blockquote>
<p>[!tip] 踩坑教训</p>
<ol>
<li><strong>“数据对不上”先用 MCP 查库</strong>，不要在 HTTP 接口上绕。这次在接口之间绕了 4 轮才发现 148 个孤儿设备，直接 SQL 查 1 分钟就能搞定。</li>
<li><strong><code>queryChild</code> 为 null 不可信</strong>——行政区和流域节点的 <code>queryChild</code> 都是 null，前端无法靠这个区分（这正是后来加 <code>type</code> 字段的动机）。</li>
<li><strong>看表数据时要看全貌</strong>——只查 1 个”紫洞”看到 STCD 空，应该立刻问”还有多少个 STCD 是空的”，否则会陷入”修一个又发现一个”的循环。</li>
<li><strong>148 这个数字是个红旗</strong>——6 个根流域的 851 个测站全部没设备，这比例异常，肯定是数据问题不是代码问题。</li>
</ol>
</blockquote>
<h4 id="1-3-5-紫洞设备排查路径总结"><a href="#1-3-5-紫洞设备排查路径总结" class="headerlink" title="1.3.5 紫洞设备排查路径总结"></a>1.3.5 紫洞设备排查路径总结</h4><p><img data-src="/images/posts/%E6%95%B0%E6%8D%AE%E5%BD%92%E9%9B%866%E6%9C%88%E6%8E%A2%E6%9F%A5%E5%AE%9E%E5%BD%95-%E4%B8%89%E5%A4%A9%E4%B8%89%E4%BB%B6%E4%BA%8B/fig1-zidong-trace.png" alt="紫洞设备排查路径图"><br><em>图 1-1：紫洞设备丢失问题的 5 步排查路径</em></p>
<h3 id="1-4-第三件事：TreeNode-加-type-字段"><a href="#1-4-第三件事：TreeNode-加-type-字段" class="headerlink" title="1.4 第三件事：TreeNode 加 type 字段"></a>1.4 第三件事：TreeNode 加 <code>type</code> 字段</h3><p>下午 14:00，前端反馈现有 tree 接口返回的节点类型不明确：</p>
<table>
<thead>
<tr>
<th>节点</th>
<th><code>queryChild</code></th>
<th>推断方式</th>
<th>可靠性</th>
</tr>
</thead>
<tbody><tr>
<td>行政区</td>
<td><code>null</code></td>
<td>不可区分</td>
<td>❌</td>
</tr>
<tr>
<td>流域</td>
<td><code>null</code></td>
<td>不可区分</td>
<td>❌</td>
</tr>
<tr>
<td>测站</td>
<td><code>&#123;sttp, rvnm, addvcd&#125;</code></td>
<td>含 <code>sttp</code></td>
<td>✓</td>
</tr>
<tr>
<td>设备</td>
<td><code>&#123;eqtp, symbol, model, stcd&#125;</code></td>
<td>含 <code>eqtp</code></td>
<td>✓</td>
</tr>
</tbody></table>
<p><strong>痛点</strong>：行政区和流域的 <code>queryChild</code> 都是 null，前端无法靠 <code>queryChild</code> 区分；<code>queryChild</code> 推断还容易受字段扩展影响。</p>
<h4 id="1-4-1-设计"><a href="#1-4-1-设计" class="headerlink" title="1.4.1 设计"></a>1.4.1 设计</h4><p><strong>类型映射</strong>：</p>
<table>
<thead>
<tr>
<th>值</th>
<th>含义</th>
<th>节点示例</th>
</tr>
</thead>
<tbody><tr>
<td>0</td>
<td>未知&#x2F;非业务节点</td>
<td>兜底值</td>
</tr>
<tr>
<td>1</td>
<td>行政区</td>
<td>省&#x2F;市&#x2F;区&#x2F;镇</td>
</tr>
<tr>
<td>2</td>
<td>流域</td>
<td>根流域、子流域、合成节点（”所有流域”、”-1级河流”）</td>
</tr>
<tr>
<td>3</td>
<td>测站</td>
<td>携带 <code>sttp</code> 的节点</td>
</tr>
<tr>
<td>4</td>
<td>设备</td>
<td>携带 <code>eqtp</code> 的节点</td>
</tr>
</tbody></table>
<p><strong>4 个接口的 type 链路</strong>：</p>
<table>
<thead>
<tr>
<th>接口</th>
<th>链路</th>
</tr>
</thead>
<tbody><tr>
<td><code>stationTreeFiltered</code></td>
<td><code>1 → 1 → 1 → 3</code></td>
</tr>
<tr>
<td><code>equipmentTreeFiltered</code></td>
<td><code>1 → 1 → 1 → 3 → 4</code></td>
</tr>
<tr>
<td><code>stationTreeFilteredByBasin</code></td>
<td><code>2 → 2 → 2 → 3</code></td>
</tr>
<tr>
<td><code>equipmentTreeFilteredByBasin</code></td>
<td><code>2 → 2 → 2 → 2 → 3 → 4</code></td>
</tr>
</tbody></table>
<h4 id="1-4-2-实现"><a href="#1-4-2-实现" class="headerlink" title="1.4.2 实现"></a>1.4.2 实现</h4><p><strong>改动文件（最小化）</strong>：</p>
<table>
<thead>
<tr>
<th>文件</th>
<th>改动</th>
<th>行数</th>
</tr>
</thead>
<tbody><tr>
<td><code>TreeNode.java</code></td>
<td>加 <code>Integer type</code> 字段 + Swagger 注解</td>
<td>3</td>
</tr>
<tr>
<td><code>TreeSelectorServiceImpl.java</code></td>
<td>加 <code>setTreeNodeType()</code> 工具方法 + 4 处调用</td>
<td>~15</td>
</tr>
<tr>
<td>Controller &#x2F; Service 接口</td>
<td><strong>不动</strong></td>
<td>0</td>
</tr>
</tbody></table>
<p><strong>TreeNode 加字段</strong>：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// gdsw-modules/algorithm-plugin-modules/algorithm-plugin-api/.../TreeNode.java</span></span><br><span class="line"><span class="meta">@ApiModelProperty(value = &quot;节点类型: 0=未知 1=行政区 2=流域 3=测站 4=设备&quot;)</span></span><br><span class="line"><span class="keyword">private</span> Integer type;</span><br></pre></td></tr></table></figure>

<p><strong>打标逻辑</strong>：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// TreeSelectorServiceImpl.java - 4 处调用</span></span><br><span class="line">setTreeNodeType(tree, <span class="number">1</span>);   <span class="comment">// 行政区树构树后</span></span><br><span class="line">setTreeNodeType(tree, <span class="number">2</span>);   <span class="comment">// 流域树构树后</span></span><br><span class="line">node.setType(<span class="number">3</span>);            <span class="comment">// 测站节点创建时</span></span><br><span class="line">node.setType(<span class="number">4</span>);            <span class="comment">// 设备节点创建时</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 工具方法</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">setTreeNodeType</span><span class="params">(TreeNode node, <span class="type">int</span> type)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (node == <span class="literal">null</span>) <span class="keyword">return</span>;</span><br><span class="line">    node.setType(type);</span><br><span class="line">    <span class="keyword">if</span> (node.getChildren() != <span class="literal">null</span>) &#123;</span><br><span class="line">        <span class="keyword">for</span> (TreeNode child : node.getChildren()) &#123;</span><br><span class="line">            setTreeNodeType(child, type);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="1-4-3-为什么打标位置这么选"><a href="#1-4-3-为什么打标位置这么选" class="headerlink" title="1.4.3 为什么打标位置这么选"></a>1.4.3 为什么打标位置这么选</h4><ul>
<li><strong>行政区&#x2F;流域树整体打 1&#x2F;2</strong>：因为 <code>getRegionTreeFromApi</code> &#x2F; <code>getRiverBasinTree</code> 是整个树构建的入口，构完一次性递归打标最干净。</li>
<li><strong>测站&#x2F;设备节点创建时直接 set</strong>：因为 <code>toStationTreeNode</code> &#x2F; <code>toEquipmentTreeNode</code> 是单节点工厂方法，节点创建时 type 已知，直接 set 最简洁。</li>
<li><strong>不动树构建过程</strong>：保留原有逻辑，只在结果上叠加 type，避免引入 bug。</li>
</ul>
<h4 id="1-4-4-验证"><a href="#1-4-4-验证" class="headerlink" title="1.4.4 验证"></a>1.4.4 验证</h4><p>实际请求 4 个接口，确认 type 链路：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;all&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;label&quot;</span><span class="punctuation">:</span> <span class="string">&quot;所有流域&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="number">2</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;children&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;812_813&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="number">2</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;children&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;812_813_-1&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;label&quot;</span><span class="punctuation">:</span> <span class="string">&quot;-1级河流&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="number">2</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;children&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;HD2D0000000P&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;label&quot;</span><span class="punctuation">:</span> <span class="string">&quot;鸡啼门水道&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="number">2</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;children&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="punctuation">&#123;</span></span><br><span class="line">          <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;81301880&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;label&quot;</span><span class="punctuation">:</span> <span class="string">&quot;白蕉&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="number">3</span><span class="punctuation">,</span></span><br><span class="line">          <span class="attr">&quot;queryChild&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;sttp&quot;</span><span class="punctuation">:</span> <span class="string">&quot;TT&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">          <span class="attr">&quot;children&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">            <span class="punctuation">&#123;</span> <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;8480021031&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;label&quot;</span><span class="punctuation">:</span> <span class="string">&quot;在线H-ADCP&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="number">4</span><span class="punctuation">,</span> <span class="attr">&quot;queryChild&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;eqtp&quot;</span><span class="punctuation">:</span> <span class="string">&quot;在线H-ADCP&quot;</span> <span class="punctuation">&#125;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="punctuation">&#123;</span> <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;PP81301880&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="number">4</span><span class="punctuation">,</span> <span class="attr">&quot;queryChild&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;eqtp&quot;</span><span class="punctuation">:</span> <span class="string">&quot;HADCP-水平式ADCP&quot;</span> <span class="punctuation">&#125;</span> <span class="punctuation">&#125;</span></span><br><span class="line">          <span class="punctuation">]</span></span><br><span class="line">        <span class="punctuation">&#125;</span><span class="punctuation">]</span></span><br><span class="line">      <span class="punctuation">&#125;</span><span class="punctuation">]</span></span><br><span class="line">    <span class="punctuation">&#125;</span><span class="punctuation">]</span></span><br><span class="line">  <span class="punctuation">&#125;</span><span class="punctuation">]</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>✅ 全部正确。</p>
<blockquote>
<p>[!tip] 设计取舍</p>
<ol>
<li><strong>打标位置选择</strong>：选构树入口打标而不是逐节点 set，是因为 4 个 <code>*Filtered</code> 委托给 4 个原方法，原方法里已经有”我知道这棵树是哪种类型”的上下文。在构树后统一打标，<strong>改动量最小、侵入性最低</strong>。</li>
<li><strong>Type 0 兜底值</strong>：理论不会出现 type&#x3D;0 的节点，但保留它作为”未知”标记，比让前端处理 null 更友好。</li>
<li><strong>类型用 Integer 而不是 String</strong>：用户明确要数字编码，省字节、序列化快、前端 if&#x2F;else 简单。</li>
</ol>
</blockquote>
<h3 id="1-5-续：当天后续的-4-件事"><a href="#1-5-续：当天后续的-4-件事" class="headerlink" title="1.5 续：当天后续的 4 件事"></a>1.5 续：当天后续的 4 件事</h3><p>加完 type 字段后，又搞了 4 件事。它们的来龙去脉又是另一个故事。</p>
<h4 id="1-5-1-发现”所有流域”是后包出来的壳"><a href="#1-5-1-发现”所有流域”是后包出来的壳" class="headerlink" title="1.5.1 发现”所有流域”是后包出来的壳"></a>1.5.1 发现”所有流域”是后包出来的壳</h4><p>用户在终端看到 7 条 <code>未找到父节点: 节点 id=HA000000000E, PARENT_CODE=&#39;-1&#39;，将作为根节点处理</code> 警告，问我什么意思。</p>
<p>排查后：</p>
<ul>
<li>直接调第三方接口 <code>getRiverBasinByCache</code> 拿到 1219 条记录</li>
<li><code>RVCD=&quot;all&quot;</code> 或 <code>RVCD1=&quot;all&quot;</code> 的记录数: 0</li>
<li>7 个根流域的源数据 <code>PARENT_CODE=&#39;-1&#39;</code>（业务约定的”无父”），互相独立，没有共同祖先</li>
<li>“所有流域” 是 <code>RiverBasinTreeUtil.buildTreeWithRoot</code> 在我们代码里<strong>凭空创建</strong>的壳（id&#x3D;”all”，包了 7 个根当 children）</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// RiverBasinTreeUtil.java</span></span><br><span class="line"><span class="type">TreeNode</span> <span class="variable">allBasinRoot</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">TreeNode</span>();</span><br><span class="line">allBasinRoot.setId(<span class="string">&quot;all&quot;</span>);</span><br><span class="line">allBasinRoot.setLabel(<span class="string">&quot;所有流域&quot;</span>);</span><br><span class="line">allBasinRoot.setChildren(rootNodes);</span><br><span class="line"><span class="keyword">return</span> allBasinRoot;</span><br></pre></td></tr></table></figure>

<h4 id="1-5-2-Tree-接口去掉”所有流域”壳（Plan-1-Plan-2）"><a href="#1-5-2-Tree-接口去掉”所有流域”壳（Plan-1-Plan-2）" class="headerlink" title="1.5.2 Tree 接口去掉”所有流域”壳（Plan 1 + Plan 2）"></a>1.5.2 Tree 接口去掉”所有流域”壳（Plan 1 + Plan 2）</h4><p>用户想”把 7 个根当成首节点”，不要中间那层壳。经历两个阶段：</p>
<p><strong>Plan 1：只动 <code>getRiverBasinTree</code></strong></p>
<p><code>getRiverBasinTree</code> 从 <code>R&lt;TreeNode&gt;</code> 改为 <code>R&lt;List&lt;TreeNode&gt;&gt;</code>，下游调用方用 <code>getRiverBasinTreeWithRoot()</code> 私有 helper 包壳（保持向后兼容）。<strong>改动小，但下游 6 个 tree 接口还是单根</strong>。</p>
<p><strong>Plan 2：全栈改造</strong></p>
<p>用户说”全栈改造”，我直接改了 12 个接口签名（包括行政区侧）。<strong>用户发现后回退 4 个行政区侧改动</strong>，明确说”管我行政区什么事情”。</p>
<p><strong>最终</strong>：只动流域侧 6 个接口 + <code>getTree</code> 统一入口。</p>
<h4 id="1-5-3-涉及改动"><a href="#1-5-3-涉及改动" class="headerlink" title="1.5.3 涉及改动"></a>1.5.3 涉及改动</h4><table>
<thead>
<tr>
<th>接口</th>
<th>改前</th>
<th>改后</th>
</tr>
</thead>
<tbody><tr>
<td><code>riverBasinTree</code></td>
<td><code>R&lt;TreeNode&gt;</code>（所有流域壳）</td>
<td><code>R&lt;List&lt;TreeNode&gt;&gt;</code>（7 根）</td>
</tr>
<tr>
<td><code>stationTreeByBasin</code></td>
<td><code>R&lt;TreeNode&gt;</code></td>
<td><code>R&lt;List&lt;TreeNode&gt;&gt;</code>（按 rvcd 路由或多根处理）</td>
</tr>
<tr>
<td><code>equipmentTreeByBasin</code></td>
<td><code>R&lt;TreeNode&gt;</code></td>
<td><code>R&lt;List&lt;TreeNode&gt;&gt;</code></td>
</tr>
<tr>
<td><code>stationTreeFilteredByBasin</code></td>
<td><code>R&lt;TreeNode&gt;</code></td>
<td><code>R&lt;List&lt;TreeNode&gt;&gt;</code></td>
</tr>
<tr>
<td><code>equipmentTreeFilteredByBasin</code></td>
<td><code>R&lt;TreeNode&gt;</code></td>
<td><code>R&lt;List&lt;TreeNode&gt;&gt;</code></td>
</tr>
<tr>
<td><code>getTree?type=basin</code></td>
<td>1 根</td>
<td>7 根</td>
</tr>
<tr>
<td><code>getTree?type=region</code></td>
<td>1 根</td>
<td>1 根（包成单元素 list）</td>
</tr>
<tr>
<td><code>stationTreeByRegion</code></td>
<td>不动</td>
<td>不动</td>
</tr>
<tr>
<td><code>equipmentTreeByRegion</code></td>
<td>不动</td>
<td>不动</td>
</tr>
<tr>
<td><code>stationTreeFiltered</code></td>
<td>不动</td>
<td>不动</td>
</tr>
<tr>
<td><code>equipmentTreeFiltered</code></td>
<td>不动</td>
<td>不动</td>
</tr>
</tbody></table>
<h4 id="1-5-4-循环内重复查询提到外面（性能优化）"><a href="#1-5-4-循环内重复查询提到外面（性能优化）" class="headerlink" title="1.5.4 循环内重复查询提到外面（性能优化）"></a>1.5.4 循环内重复查询提到外面（性能优化）</h4><p>用户发现 <code>stationTreeByBasin</code> 和 <code>equipmentTreeByBasin</code> 在循环里每次都查 DB：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 改前：7 次查询</span></span><br><span class="line"><span class="keyword">for</span> (TreeNode root : roots) &#123;</span><br><span class="line">    Map&lt;String, List&lt;TreeNode&gt;&gt; stationMap = queryAndGroupStationsByRvcd(keyword);  <span class="comment">// 每次都查</span></span><br><span class="line">    attachStationsToBasinTree(root, stationMap);</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>queryAndGroupStationsByRvcd(keyword)</code> 只依赖 keyword，不依赖 root——<strong>纯浪费</strong>。提到循环外。</p>
<p><code>equipmentTreeByBasin</code> 也有同样问题（7 次设备查询），改成收集所有 stcds 后 1 次查完。</p>
<p><strong>DB 查询次数对比</strong>：</p>
<ul>
<li><code>stationTreeByBasin</code> no-rvcd：7 → 1</li>
<li><code>equipmentTreeByBasin</code>：7 → 1</li>
</ul>
<h4 id="1-5-5-空根节点过滤"><a href="#1-5-5-空根节点过滤" class="headerlink" title="1.5.5 空根节点过滤"></a>1.5.5 空根节点过滤</h4><p>用户发现 <code>equipmentTreeByBasin</code> 返回 7 根但 5 根 <code>children: []</code>（空），要求过滤。</p>
<p><code>stationTreeByBasin</code> 和 <code>equipmentTreeByBasin</code> 在循环末尾加：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (root.getChildren() != <span class="literal">null</span> &amp;&amp; !root.getChildren().isEmpty()) &#123;</span><br><span class="line">    result.add(root);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>效果：equipment 树从 7 根降到 2 根（西江 + 珠江三角洲）。</p>
<h4 id="1-5-6-惊喜发现：紫洞出现了"><a href="#1-5-6-惊喜发现：紫洞出现了" class="headerlink" title="1.5.6 惊喜发现：紫洞出现了"></a>1.5.6 惊喜发现：紫洞出现了</h4><p>测试时发现紫洞（VV81303560）从无到有，出现在西江 → 北流河 → 杨梅河 → 潘内 下。说明 148 个孤儿设备中紫洞的 STCD 已经被补上。[[紫洞设备问题排查]] 里的 P0 数据问题已部分修复。</p>
<p><img data-src="/images/posts/%E6%95%B0%E6%8D%AE%E5%BD%92%E9%9B%866%E6%9C%88%E6%8E%A2%E6%9F%A5%E5%AE%9E%E5%BD%95-%E4%B8%89%E5%A4%A9%E4%B8%89%E4%BB%B6%E4%BA%8B/fig2-tree-refactor-overview.png" alt="6 月 1 日树选择器改造全景图"><br><em>图 1-2：6&#x2F;1 树选择器改造全景——type 字段 + 多根返回 + 性能优化</em></p>
<h3 id="1-6-6-1-经验总结"><a href="#1-6-6-1-经验总结" class="headerlink" title="1.6 6&#x2F;1 经验总结"></a>1.6 6&#x2F;1 经验总结</h3><blockquote>
<p>[!tip] 三个”不要”</p>
<ol>
<li><strong>不要让用户多次澄清同一个问题</strong>——“你看 riverBasinTree 是对的” → “等下结构不对” → “你看西江” → “紫洞不是视频设备” → “你有数据库 mcp 吗”，AI 应该更早主动提出”我直接查数据库看吧”而不是被动等。</li>
<li><strong>遇到”数据对不上”应该立即用 MCP 查</strong>，不要在 HTTP 接口上绕弯子。</li>
<li><strong>询问时多给选择题</strong>，少让用户自由回答。比如”紫洞是设备还是测站？”这种歧义用 <code>AskUserQuestion</code> 列表会更高效。</li>
<li><strong>批量查数据</strong>：查 148 个孤儿设备时，应该主动跑 SQL 列出前 20 个 + 按 EQTP 分布 + 按 stcd 关联的统计，而不是等用户问。</li>
<li><strong>记笔记时主动整理</strong>：用户说”记录一下”时，可以自动生成 3 个文件（工作记录、问题分析、功能设计），而不是只写一个。</li>
</ol>
</blockquote>
<blockquote>
<p>[!warning] 三个”应该”</p>
<ol>
<li>**”全栈”应该理解为”流域侧全栈”，不是”所有 tree 接口全栈”**——遇到模糊词要先问清楚范围。</li>
<li><strong>“用户是不是这个意思”要直说</strong>：用户最初说”全栈改造”，我应该问”是流域侧全栈还是所有 tree 接口”，而不是直接假设”全栈 &#x3D; 所有”。</li>
<li><strong>写新接口前先 review 底层方法</strong>——<code>pruneEmptyBranches</code> 已经做了”按类型过滤”的工作，新加的 <code>*Filtered</code> 接口是纯壳。</li>
</ol>
</blockquote>
<hr>
<h2 id="二、6-月-2-日：接口对接与数据逻辑说明"><a href="#二、6-月-2-日：接口对接与数据逻辑说明" class="headerlink" title="二、6 月 2 日：接口对接与数据逻辑说明"></a>二、6 月 2 日：接口对接与数据逻辑说明</h2><h3 id="2-1-起点：6-1-改造的延续"><a href="#2-1-起点：6-1-改造的延续" class="headerlink" title="2.1 起点：6&#x2F;1 改造的延续"></a>2.1 起点：6&#x2F;1 改造的延续</h3><p>6&#x2F;1 的多根改造、type 字段、148 孤儿设备排查都有了结论，但还没完。6&#x2F;2 上午，用户提了 3 个新需求：</p>
<ol>
<li>写一个 <strong>对接文档</strong>给前端，让前端知道 6 个 List 接口怎么适配</li>
<li>加 <strong>2 个设备类型筛选接口</strong>（按行政区&#x2F;按流域），方便前端做下拉框</li>
<li>解释 <strong>“-1级河流” 节点</strong>到底从哪来、为什么 SORT&#x3D;999</li>
</ol>
<p>3 件事预计 2 天，结果 1 天就搞完了——因为 6&#x2F;1 的 type 字段、多根改造已经铺好了路，6&#x2F;2 主要是文档和”代码澄清”。</p>
<h3 id="2-2-第一件事：tree-接口改造对接说明"><a href="#2-2-第一件事：tree-接口改造对接说明" class="headerlink" title="2.2 第一件事：tree 接口改造对接说明"></a>2.2 第一件事：tree 接口改造对接说明</h3><h4 id="2-2-1-接口签名变化（破坏性）"><a href="#2-2-1-接口签名变化（破坏性）" class="headerlink" title="2.2.1 接口签名变化（破坏性）"></a>2.2.1 接口签名变化（破坏性）</h4><p>6 个接口从 <code>R&lt;TreeNode&gt;</code> 改为 <code>R&lt;List&lt;TreeNode&gt;&gt;</code>：</p>
<table>
<thead>
<tr>
<th>接口</th>
<th>改前 <code>data</code></th>
<th>改后 <code>data</code></th>
</tr>
</thead>
<tbody><tr>
<td><code>GET /dataCollection/riverBasinTree</code></td>
<td><code>&#123;id:&quot;all&quot;, children:[...]&#125;</code></td>
<td><code>[&#123;...HA&#125;, &#123;...HB&#125;, &#123;...HC&#125;, &#123;...812_813&#125;, &#123;...HE1A&#125;, &#123;...816&#125;, &#123;...817&#125;]</code></td>
</tr>
<tr>
<td><code>GET /dataCollection/stationTreeByBasin</code></td>
<td>单根</td>
<td>1 根（带 rvcd）或 N 根（无 rvcd，过滤无测站的空根）</td>
</tr>
<tr>
<td><code>GET /dataCollection/equipmentTreeByBasin</code></td>
<td>单根</td>
<td>1 根（带 rvcd）或 N 根（无 rvcd，过滤无设备的空根）</td>
</tr>
<tr>
<td><code>GET /dataCollection/stationTreeFilteredByBasin</code></td>
<td>单根</td>
<td>1 根（带 rvcd）或 N 根（无 rvcd，过滤空根）</td>
</tr>
<tr>
<td><code>GET /dataCollection/equipmentTreeFilteredByBasin</code></td>
<td>单根</td>
<td>1 根（带 rvcd）或 N 根（无 rvcd，过滤空根）</td>
</tr>
<tr>
<td><code>GET /dataCollection/tree?type=basin</code></td>
<td>单根</td>
<td>7 根</td>
</tr>
<tr>
<td><code>GET /dataCollection/tree?type=region</code></td>
<td>单根</td>
<td><strong>1 根（包成单元素 list）</strong></td>
</tr>
</tbody></table>
<p><strong>行政区侧 4 个接口保持 <code>R&lt;TreeNode&gt;</code> 单根不变</strong>：</p>
<ul>
<li><code>stationTreeByRegion</code> &#x2F; <code>equipmentTreeByRegion</code> &#x2F; <code>stationTreeFiltered</code> &#x2F; <code>equipmentTreeFiltered</code></li>
</ul>
<h4 id="2-2-2-前端需要做的事"><a href="#2-2-2-前端需要做的事" class="headerlink" title="2.2.2 前端需要做的事"></a>2.2.2 前端需要做的事</h4><p><strong>1. 适配 data 是数组的情况（必须）</strong></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 改前</span></span><br><span class="line"><span class="keyword">const</span> root = response.<span class="property">data</span>;          <span class="comment">// 单个对象</span></span><br><span class="line"><span class="keyword">const</span> children = root.<span class="property">children</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 改后</span></span><br><span class="line"><span class="keyword">const</span> roots = response.<span class="property">data</span>;          <span class="comment">// 数组</span></span><br><span class="line">roots.<span class="title function_">forEach</span>(<span class="function"><span class="params">root</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">const</span> children = root.<span class="property">children</span>;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>涉及端点：<code>riverBasinTree</code> &#x2F; <code>getTree?type=basin</code> &#x2F; <code>getTree?type=region</code> &#x2F; <code>stationTreeByBasin</code> &#x2F; <code>equipmentTreeByBasin</code> &#x2F; <code>stationTreeFilteredByBasin</code> &#x2F; <code>equipmentTreeFilteredByBasin</code>。</p>
<p><strong>2. 列表长度可能变少（重要）</strong></p>
<p>无 rvcd 时，<strong>所有 4 个 basin 接口都会过滤空根</strong>，返回数量 N ≤ 7：</p>
<ul>
<li><code>stationTreeByBasin</code>：过滤无测站的根 → 返 N 根（N 取决于哪些根流域下有测站）</li>
<li><code>equipmentTreeByBasin</code>：过滤无设备的根 → 返 N 根（N 取决于哪些根流域下有设备）</li>
<li><code>stationTreeFilteredByBasin</code>：同上（过滤后再过滤空节点）</li>
<li><code>equipmentTreeFilteredByBasin</code>：同上</li>
<li>带 rvcd 时固定返 1 个</li>
</ul>
<p>前端不需要处理空对象了，根节点都是”有内容”的。</p>
<p><strong>3. rvcd 路由保持兼容</strong></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 无 rvcd：返多根</span></span><br><span class="line"><span class="variable constant_">GET</span> /stationTreeByBasin          → [root1, root2, ..., root7]</span><br><span class="line"></span><br><span class="line"><span class="comment">// 带 rvcd：返单根</span></span><br><span class="line"><span class="variable constant_">GET</span> /stationTreeByBasin?rvcd=<span class="number">812_813</span>  → [珠江三角洲子树的处理结果]</span><br></pre></td></tr></table></figure>

<p>前端逻辑可以这么写：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> list = response.<span class="property">data</span>;</span><br><span class="line"><span class="keyword">if</span> (list.<span class="property">length</span> === <span class="number">1</span>) &#123;</span><br><span class="line">  <span class="comment">// 单根模式（指定了 rvcd 或 getTree?type=region）</span></span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">  <span class="comment">// 多根模式（默认 basin 树，已过滤空根，数量 ≤ 7）</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>4. type 字段（已就绪）</strong></p>
<p>每个节点都有 <code>type</code> 字段：</p>
<table>
<thead>
<tr>
<th>值</th>
<th>含义</th>
<th>示例节点</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td>行政区</td>
<td>省&#x2F;市&#x2F;区</td>
</tr>
<tr>
<td>2</td>
<td>流域</td>
<td>根流域 + “所有流域” + “-1级河流”</td>
</tr>
<tr>
<td>3</td>
<td>测站</td>
<td>携带 <code>sttp</code> 的节点</td>
</tr>
<tr>
<td>4</td>
<td>设备</td>
<td>携带 <code>eqtp</code> 的节点</td>
</tr>
</tbody></table>
<p><strong>冗余确认</strong>：<code>type=3</code> 等价于 <code>queryChild</code> 含 <code>sttp</code>；<code>type=4</code> 等价于含 <code>eqtp</code>。建议前端**只信 <code>type</code>**，不要反推 <code>queryChild</code>。</p>
<p><strong>5. 行政区侧接口不变</strong></p>
<p><code>stationTreeByRegion</code> &#x2F; <code>equipmentTreeByRegion</code> &#x2F; <code>stationTreeFiltered</code> &#x2F; <code>equipmentTreeFiltered</code> 这 4 个<strong>保持原样</strong>，<code>data</code> 还是单个对象。前端无需改。</p>
<h4 id="2-2-3-提交清单（提交前必看）"><a href="#2-2-3-提交清单（提交前必看）" class="headerlink" title="2.2.3 提交清单（提交前必看）"></a>2.2.3 提交清单（提交前必看）</h4><ul>
<li><input disabled="" type="checkbox"> <strong>前端已适配</strong> 6 个 List 接口（建议先让他们确认）</li>
<li><input disabled="" type="checkbox"> <strong>后端其他服务</strong> 没人调这 6 个接口（已 grep 确认只有 3 个内部文件）</li>
<li><input disabled="" type="checkbox"> <strong>commit 但不 push</strong>，等 2-3 天缓冲</li>
</ul>
<p><strong>commit message 草稿</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">feat: 流域树接口改为多根List返回 + 缓存自愈</span><br><span class="line"></span><br><span class="line">- 流域侧6个tree接口 R&lt;TreeNode&gt; 改为 R&lt;List&lt;TreeNode&gt;&gt;</span><br><span class="line">- 去掉 RiverBasinTreeUtil 包出来的所有流域虚拟根</span><br><span class="line">- getRiverBasinTree 拆出 fetchBasinJsonStr() 复用，新增 setTreeNodeType 打标</span><br><span class="line">- stationTreeByBasin/equipmentTreeByBasin 循环内DB查询提到外面，过滤空根</span><br><span class="line">- getRegionTreeFromApi 调完第三方后顺手写缓存（独立try-catch，Redis故障不影响主流程）</span><br><span class="line">- 行政区侧4个接口保持原样</span><br></pre></td></tr></table></figure>

<h4 id="2-2-4-性能特征"><a href="#2-2-4-性能特征" class="headerlink" title="2.2.4 性能特征"></a>2.2.4 性能特征</h4><table>
<thead>
<tr>
<th>接口</th>
<th>第一次（缓存空）</th>
<th>后续命中</th>
</tr>
</thead>
<tbody><tr>
<td>流域侧 4 个 *ByBasin &#x2F; *FilteredByBasin</td>
<td>~500ms（DB 兜底）</td>
<td>~400-700ms</td>
</tr>
<tr>
<td>行政区侧 4 个 *ByRegion &#x2F; *Filtered</td>
<td>~1500ms</td>
<td>~500ms（缓存命中后）</td>
</tr>
</tbody></table>
<p><strong>第一次访问 regionTree 慢 1500ms</strong> 是因为缓存空、调第三方。<strong>第二次起自动 ~500ms</strong>（自愈机制已写入 Redis）。</p>
<p><code>stationTreeByBasin</code> &#x2F; <code>equipmentTreeByBasin</code> 无 rvcd 时会过滤空根，返回数量 ≤ 7，前端按数组遍历即可。</p>
<h4 id="2-2-5-Redis-缓存自愈"><a href="#2-2-5-Redis-缓存自愈" class="headerlink" title="2.2.5 Redis 缓存自愈"></a>2.2.5 Redis 缓存自愈</h4><p><code>getRegionTreeFromApi</code> 加了 5 行自愈代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (jsonStr != <span class="literal">null</span> &amp;&amp; !jsonStr.isEmpty()) &#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        redisUtils.hset(<span class="string">&quot;GDSW_AREA_TREE&quot;</span>, <span class="string">&quot;value&quot;</span>, GzipCompressionUtils.compress(jsonStr));</span><br><span class="line">        redisUtils.hset(<span class="string">&quot;GDSW_AREA_TREE&quot;</span>, <span class="string">&quot;time&quot;</span>, ...);</span><br><span class="line">        redisUtils.hset(<span class="string">&quot;GDSW_AREA_TREE&quot;</span>, <span class="string">&quot;name&quot;</span>, <span class="string">&quot;行政区划信息&quot;</span>);</span><br><span class="line">        log.info(<span class="string">&quot;区划信息缓存已写入（自愈）&quot;</span>);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception cacheEx) &#123;</span><br><span class="line">        log.warn(<span class="string">&quot;区划信息缓存写入失败，下次仍会重试: &#123;&#125;&quot;</span>, cacheEx.getMessage());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>写缓存<strong>独立 try-catch</strong>——Redis 挂了不影响主流程</li>
<li>数据格式跟 <code>BasicDataTask</code> 一致（Base64(gzip)）</li>
<li>缓存空会自动写，不需要定时任务</li>
</ul>
<blockquote>
<p>[!tip] 关键设计点</p>
<ol>
<li><strong>写缓存独立 try-catch</strong>——Redis 故障不影响主流程，下次仍会重试</li>
<li><strong>数据格式跟 BasicDataTask 一致</strong>（gzip 压缩）——避免数据格式不一致导致的兼容问题</li>
<li><strong>缓存空自动写</strong>——不需要单独定时任务，访问即触发</li>
</ol>
</blockquote>
<h4 id="2-2-6-滚动升级风险"><a href="#2-2-6-滚动升级风险" class="headerlink" title="2.2.6 滚动升级风险"></a>2.2.6 滚动升级风险</h4><ul>
<li><strong>新接口</strong> vs <strong>老接口</strong>：因为是签名变更（不只是新增），需要<strong>前端先升级、后端再上线</strong>——或后端上线时前端暂时不能调这 6 个接口</li>
<li><strong>建议</strong>：先 commit 不 push，前端确认适配后，再合并到 master</li>
</ul>
<p><strong>回滚方案</strong>：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git revert HEAD     <span class="comment"># 一键回退 3 个文件</span></span><br></pre></td></tr></table></figure>

<p>无副作用（改动自包含，没改公共 API 依赖）。</p>
<h4 id="2-2-7-未完成项"><a href="#2-2-7-未完成项" class="headerlink" title="2.2.7 未完成项"></a>2.2.7 未完成项</h4><ul>
<li><strong>148 个孤儿设备</strong>还有 147 个 STCD 空的没补（紫洞那个被补了）</li>
<li><strong><code>BasicDataTask</code> 没触发</strong>的原因没查（因为不需要了——现在自愈）</li>
<li><strong>WARN 日志噪音</strong> <code>未找到父节点: PARENT_CODE=&#39;-1&#39;</code> 没修（业务约定的”无父”，代码当成查找 -1 父节点）</li>
</ul>
<h3 id="2-3-第二件事：设备类型筛选接口"><a href="#2-3-第二件事：设备类型筛选接口" class="headerlink" title="2.3 第二件事：设备类型筛选接口"></a>2.3 第二件事：设备类型筛选接口</h3><p>前端先调这两个接口拿到设备类型列表做下拉筛选，再用 <code>eqtp</code> 参数调设备树接口过滤。</p>
<p><strong>典型用法：不传参，直接拿全量设备类型列表。</strong></p>
<h4 id="2-3-1-接口列表"><a href="#2-3-1-接口列表" class="headerlink" title="2.3.1 接口列表"></a>2.3.1 接口列表</h4><p><strong>1. 按行政区获取设备类型列表</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">GET /dataCollection/equipmentTypesByRegion</span><br></pre></td></tr></table></figure>

<table>
<thead>
<tr>
<th>参数</th>
<th>类型</th>
<th>必填</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>addvcd</td>
<td>string</td>
<td>否</td>
<td>行政区编码（前缀匹配），不传返回全量</td>
</tr>
<tr>
<td>keyword</td>
<td>string</td>
<td>否</td>
<td>设备名称关键词（模糊搜索）</td>
</tr>
</tbody></table>
<p>不传参调用：<code>GET /dataCollection/equipmentTypesByRegion</code> → 返回行政区全量设备类型（34 个）</p>
<p><strong>2. 按流域获取设备类型列表</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">GET /dataCollection/equipmentTypesByBasin</span><br></pre></td></tr></table></figure>

<table>
<thead>
<tr>
<th>参数</th>
<th>类型</th>
<th>必填</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>rvcd</td>
<td>string</td>
<td>否</td>
<td>流域编码，不传返回全量</td>
</tr>
<tr>
<td>keyword</td>
<td>string</td>
<td>否</td>
<td>设备名称关键词（模糊搜索）</td>
</tr>
</tbody></table>
<p>不传参调用：<code>GET /dataCollection/equipmentTypesByBasin</code> → 返回流域全量设备类型（6 个）</p>
<h4 id="2-3-2-返回格式"><a href="#2-3-2-返回格式" class="headerlink" title="2.3.2 返回格式"></a>2.3.2 返回格式</h4><figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;success&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;errorCode&quot;</span><span class="punctuation">:</span> <span class="number">0</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;data&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="string">&quot;在线H-ADCP&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="string">&quot;FWQ-浮子水位计&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="string">&quot;翻斗式雨量计&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="string">&quot;HADCP-水平式ADCP&quot;</span></span><br><span class="line">  <span class="punctuation">]</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>返回 <code>List&lt;String&gt;</code>，已去重，可直接用于下拉筛选。</p>
<h4 id="2-3-3-前端使用流程"><a href="#2-3-3-前端使用流程" class="headerlink" title="2.3.3 前端使用流程"></a>2.3.3 前端使用流程</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">1. 用户选择行政区/流域</span><br><span class="line">   ↓</span><br><span class="line">2. 调 equipmentTypesByRegion / equipmentTypesByBasin 拿到设备类型列表</span><br><span class="line">   ↓</span><br><span class="line">3. 渲染下拉筛选框</span><br><span class="line">   ↓</span><br><span class="line">4. 用户选择设备类型后，调 equipmentTreeFiltered / equipmentTreeFilteredByBasin（传 eqtp 参数）</span><br></pre></td></tr></table></figure>

<h4 id="2-3-4-实现说明"><a href="#2-3-4-实现说明" class="headerlink" title="2.3.4 实现说明"></a>2.3.4 实现说明</h4><ul>
<li>内部调的是已有的 <code>equipmentTreeFiltered</code> &#x2F; <code>equipmentTreeFilteredByBasin</code>（<code>eqtp=null</code> 拿全部设备）</li>
<li>从返回的树结构中递归收集 <code>type=4</code> 节点的 <code>queryChild.eqtp</code>，去重后返回</li>
<li>行政区和流域返回的设备类型范围不同（流域过滤空根后范围更小）</li>
</ul>
<h3 id="2-4-第三件事：负一级河流逻辑说明"><a href="#2-4-第三件事：负一级河流逻辑说明" class="headerlink" title="2.4 第三件事：负一级河流逻辑说明"></a>2.4 第三件事：负一级河流逻辑说明</h3><h4 id="2-4-1-来源和含义"><a href="#2-4-1-来源和含义" class="headerlink" title="2.4.1 来源和含义"></a>2.4.1 来源和含义</h4><p>第三方流域数据接口（<code>http://10.144.32.219:9981/thirdpartyApi/getRiverDataByCache</code>）返回的原始数据中，<strong>上游数据本身就存在</strong> 5 个节点的 <code>RVNM</code> 为 **<code>-1级河流</code>**。</p>
<p><strong>上游原始数据（已确认）</strong>：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;RVCD&quot;</span><span class="punctuation">:</span> <span class="string">&quot;812_813_-1&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;RVNM&quot;</span><span class="punctuation">:</span> <span class="string">&quot;-1级河流&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;RV_LEVEL&quot;</span><span class="punctuation">:</span> <span class="number">-11</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;PARENT_CODE&quot;</span><span class="punctuation">:</span> <span class="string">&quot;812_813&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;SORT&quot;</span><span class="punctuation">:</span> <span class="number">999</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<table>
<thead>
<tr>
<th>字段</th>
<th>值</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>RVNM</code></td>
<td><code>-1级河流</code></td>
<td>上游原始名称，不是代码造的</td>
</tr>
<tr>
<td><code>RV_LEVEL</code></td>
<td><code>-11</code></td>
<td>全部 1219 条记录中，只有这 5 条是 -11，是特殊的级别标记</td>
</tr>
<tr>
<td><code>SORT</code></td>
<td><code>999</code></td>
<td><strong>上游数据本来就是 999</strong>，不是代码设的</td>
</tr>
<tr>
<td><code>PARENT_CODE</code></td>
<td><code>812_813</code></td>
<td>指向真实父节点（珠江三角洲河网区）</td>
</tr>
<tr>
<td><code>RVCD</code></td>
<td><code>812_813_-1</code></td>
<td>格式为父节点 id + <code>_-1</code></td>
</tr>
</tbody></table>
<p>代码里 <code>SORT_LAST_MARKER = &quot;-1级河流&quot;</code> 只是排序标记，配合上游已有的 <code>SORT=999</code> 确保排最后，没有构造任何节点。</p>
<h4 id="2-4-2-RV-LEVEL-分布"><a href="#2-4-2-RV-LEVEL-分布" class="headerlink" title="2.4.2 RV_LEVEL 分布"></a>2.4.2 RV_LEVEL 分布</h4><table>
<thead>
<tr>
<th>RV_LEVEL</th>
<th>条数</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>-11</td>
<td>5</td>
<td><code>-1级河流</code> 容器节点</td>
</tr>
<tr>
<td>-1</td>
<td>174</td>
<td>—</td>
</tr>
<tr>
<td>0</td>
<td>95</td>
<td>—</td>
</tr>
<tr>
<td>1</td>
<td>372</td>
<td>—</td>
</tr>
<tr>
<td>2</td>
<td>357</td>
<td>—</td>
</tr>
<tr>
<td>3</td>
<td>195</td>
<td>—</td>
</tr>
<tr>
<td>4</td>
<td>21</td>
<td>—</td>
</tr>
</tbody></table>
<h4 id="2-4-3-具体例子：珠江三角洲河网区"><a href="#2-4-3-具体例子：珠江三角洲河网区" class="headerlink" title="2.4.3 具体例子：珠江三角洲河网区"></a>2.4.3 具体例子：珠江三角洲河网区</h4><p>以 <code>珠江三角洲河网区</code>（RVCD&#x3D;<code>812_813</code>）为例，对比两种河流：</p>
<table>
<thead>
<tr>
<th>河流</th>
<th>节点 id（来自 RVCD1&#x2F;RVCD）</th>
<th>父节点 pid</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>茅洲河</td>
<td><code>HD16C000000S</code></td>
<td><code>812_813</code></td>
<td>上游分配了具体河流编码，直接挂在珠江三角洲下</td>
</tr>
<tr>
<td>深圳河</td>
<td><code>HD16D000000S</code></td>
<td><code>812_813</code></td>
<td>同上</td>
</tr>
<tr>
<td><strong>-1级河流</strong></td>
<td><code>812_813_-1</code></td>
<td><code>812_813</code></td>
<td>上游造的分类容器节点，也是直接挂在珠江三角洲下</td>
</tr>
<tr>
<td>崖门水道</td>
<td><code>HD2A0000000P</code></td>
<td><strong><code>812_813_-1</code></strong></td>
<td>上游没给它分配具体分类，归到了 -1级河流 下</td>
</tr>
</tbody></table>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">珠江三角洲河网区 (RVCD=812_813)</span><br><span class="line">│</span><br><span class="line">├── 茅洲河 (HD16C000000S)         ← 父节点直接是 812_813</span><br><span class="line">├── 深圳河 (HD16D000000S)         ← 父节点直接是 812_813</span><br><span class="line">├── 潭江   (13个子流域)</span><br><span class="line">├── 流溪河 (16个子流域)</span><br><span class="line">├── ... (其他27条有具体编码的河流)</span><br><span class="line">│</span><br><span class="line">└── -1级河流 (812_813_-1)          ← 上游造的容器，父节点也是 812_813</span><br><span class="line">    ├── 崖门水道    ← 父节点是 812_813_-1，不是 812_813</span><br><span class="line">    ├── 江门水道</span><br><span class="line">    ├── 西江干流水道</span><br><span class="line">    ├── 磨刀门水道</span><br><span class="line">    ├── 虎门水道</span><br><span class="line">    ├── ... (共147条)</span><br><span class="line">    └── 淡水湖</span><br></pre></td></tr></table></figure>

<p><strong>关键区别</strong>：茅洲河和崖门水道<strong>都属于珠江三角洲</strong>，但上游数据的分类方式不同：</p>
<ul>
<li>茅洲河：上游分配了具体河流编码（<code>HD16C000000S</code>），父节点直接是珠江三角洲</li>
<li>崖门水道：上游没有分配具体分类，被归到了 <code>-1级河流</code> 这个容器下，父节点是 <code>-1级河流</code>（<code>812_813_-1</code>）</li>
</ul>
<p>这些河流本身都是正常的（西江干流水道、磨刀门水道、虎门水道等都是珠江的重要水道），只是上游数据的一种分类习惯。</p>
<h4 id="2-4-4-全部-5-个-1级河流-节点"><a href="#2-4-4-全部-5-个-1级河流-节点" class="headerlink" title="2.4.4 全部 5 个 -1级河流 节点"></a>2.4.4 全部 5 个 <code>-1级河流</code> 节点</h4><table>
<thead>
<tr>
<th>节点 id</th>
<th>父节点 pid</th>
<th>子节点数</th>
<th>所属流域</th>
</tr>
</thead>
<tbody><tr>
<td><code>801_802_803_804_805_806_807_808_809_-1</code></td>
<td><code>801_802_803_804_805_806_807_808_809</code></td>
<td>1</td>
<td>韶关区域</td>
</tr>
<tr>
<td><code>812_813_-1</code></td>
<td><code>812_813</code></td>
<td>147</td>
<td>珠江三角洲河网区</td>
</tr>
<tr>
<td><code>815_-1</code></td>
<td><code>815</code></td>
<td>17</td>
<td>粤东沿海诸河</td>
</tr>
<tr>
<td><code>816_-1</code></td>
<td><code>816</code></td>
<td>1</td>
<td>粤西诸河</td>
</tr>
<tr>
<td><code>817_-1</code></td>
<td><code>817</code></td>
<td>1</td>
<td>海南岛</td>
</tr>
</tbody></table>
<h4 id="2-4-5-代码位置"><a href="#2-4-5-代码位置" class="headerlink" title="2.4.5 代码位置"></a>2.4.5 代码位置</h4><p>文件：<code>RiverBasinTreeUtil.java</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">SORT_LAST_MARKER</span> <span class="operator">=</span> <span class="string">&quot;-1级河流&quot;</span>;  <span class="comment">// 第 172 行</span></span><br></pre></td></tr></table></figure>

<h4 id="2-4-6-用法：排序兜底"><a href="#2-4-6-用法：排序兜底" class="headerlink" title="2.4.6 用法：排序兜底"></a>2.4.6 用法：排序兜底</h4><p>在 <code>sortChildrenRecursively</code> 方法（第 174-191 行）中，对每个节点的子节点排序：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">node.getChildren().sort((a, b) -&gt; &#123;</span><br><span class="line">    <span class="type">boolean</span> <span class="variable">isMinusOneA</span> <span class="operator">=</span> a.getLabel() != <span class="literal">null</span> &amp;&amp; a.getLabel().contains(SORT_LAST_MARKER);</span><br><span class="line">    <span class="type">boolean</span> <span class="variable">isMinusOneB</span> <span class="operator">=</span> b.getLabel() != <span class="literal">null</span> &amp;&amp; b.getLabel().contains(SORT_LAST_MARKER);</span><br><span class="line">    <span class="keyword">if</span> (isMinusOneA == isMinusOneB) &#123;</span><br><span class="line">        <span class="comment">// 都是或都不是&quot;-1级河流&quot;，按 dictSort 正常排</span></span><br><span class="line">        <span class="keyword">return</span> Integer.compare(a.getDictSort(), a.getDictSort());</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 一方是&quot;-1级河流&quot;，排到最后</span></span><br><span class="line">    <span class="keyword">return</span> isMinusOneA ? <span class="number">1</span> : -<span class="number">1</span>;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p><strong>排序规则</strong>：</p>
<table>
<thead>
<tr>
<th>情况</th>
<th>结果</th>
</tr>
</thead>
<tbody><tr>
<td>A 和 B 都不是 “-1级河流”</td>
<td>按 <code>dictSort</code> 升序</td>
</tr>
<tr>
<td>A 和 B 都是 “-1级河流”</td>
<td>按 <code>dictSort</code> 升序</td>
</tr>
<tr>
<td>A 是，B 不是</td>
<td>A 排最后（return 1）</td>
</tr>
<tr>
<td>A 不是，B 是</td>
<td>B 排最后（return -1）</td>
</tr>
</tbody></table>
<p>效果：”-1级河流”节点始终排在同一层级的最后面，不会混在正常河流中间。</p>
<h4 id="2-4-7-影响范围"><a href="#2-4-7-影响范围" class="headerlink" title="2.4.7 影响范围"></a>2.4.7 影响范围</h4><ul>
<li><strong>仅影响展示顺序</strong>，不影响数据查询、匹配、统计</li>
<li><code>-1级河流</code> 节点是上游数据本来就有的，<code>SORT=999</code> 也是上游的值</li>
<li>代码里没有造节点、没有改归属、没有改 sort，只是按上游的 sort 排序（<code>sortChildrenRecursively</code> 方法）</li>
<li>前端展示时，用户会看到”-1级河流”在每个流域的最底部</li>
</ul>
<h4 id="2-4-8-与-PARENT-CODE-1-日志的关系"><a href="#2-4-8-与-PARENT-CODE-1-日志的关系" class="headerlink" title="2.4.8 与 PARENT_CODE=&#39;-1&#39; 日志的关系"></a>2.4.8 与 <code>PARENT_CODE=&#39;-1&#39;</code> 日志的关系</h4><p>日志中看到的 <code>未找到父节点: PARENT_CODE=&#39;-1&#39;</code>（<code>parseFlatJson</code> 第 144-146 行）和 <code>-1级河流</code> <strong>是两回事</strong>：</p>
<table>
<thead>
<tr>
<th>概念</th>
<th>字段</th>
<th>含义</th>
</tr>
</thead>
<tbody><tr>
<td><code>-1级河流</code></td>
<td><code>RVNM</code>（河流名称）</td>
<td>兜底容器节点，存放无法归类到具体子流域的河流，仅影响排序</td>
</tr>
<tr>
<td><code>PARENT_CODE=&#39;-1&#39;</code></td>
<td><code>PARENT_CODE</code>（父节点编码）</td>
<td>某些节点的父节点编码是字符串 <code>&quot;-1&quot;</code>，在 <code>nodeMap</code> 里找不到，被当根节点处理</td>
</tr>
</tbody></table>
<p>实际数据中，<code>-1级河流</code> 节点的 <code>PARENT_CODE</code> 指向的是真实父节点（如 <code>812_813</code>），不是 <code>&quot;-1&quot;</code>。而 <code>PARENT_CODE=&#39;-1&#39;</code> 的是另一批节点。代码里两者独立处理：</p>
<ul>
<li>名称标记 → 影响排序（sort&#x3D;999，排最后）</li>
<li>父节点缺失 → 影响树结构（变根节点）</li>
</ul>
<h3 id="2-5-提交记录-b1cfd65"><a href="#2-5-提交记录-b1cfd65" class="headerlink" title="2.5 提交记录 b1cfd65"></a>2.5 提交记录 b1cfd65</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">feat: 流域接口返回多根节点 + 新增设备类型查询接口 + 缓存自愈</span><br></pre></td></tr></table></figure>

<h4 id="2-5-1-改动文件（3-个）"><a href="#2-5-1-改动文件（3-个）" class="headerlink" title="2.5.1 改动文件（3 个）"></a>2.5.1 改动文件（3 个）</h4><table>
<thead>
<tr>
<th>文件</th>
<th>改动</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>TreeSelectorService.java</code></td>
<td>+16&#x2F;-8</td>
<td>6 个流域接口签名改为 <code>R&lt;List&lt;TreeNode&gt;&gt;</code>，新增 2 个设备类型方法</td>
</tr>
<tr>
<td><code>TreeSelectorController.java</code></td>
<td>+32&#x2F;-8</td>
<td>对应端点签名同步改，新增 2 个设备类型端点</td>
</tr>
<tr>
<td><code>TreeSelectorServiceImpl.java</code></td>
<td>+200&#x2F;-58</td>
<td>多根处理逻辑 + 缓存自愈 + 设备类型查询实现</td>
</tr>
</tbody></table>
<h4 id="2-5-2-改动内容"><a href="#2-5-2-改动内容" class="headerlink" title="2.5.2 改动内容"></a>2.5.2 改动内容</h4><p><strong>1. 流域接口多根返回（破坏性变更）</strong></p>
<p>6 个接口从 <code>R&lt;TreeNode&gt;</code> 改为 <code>R&lt;List&lt;TreeNode&gt;&gt;</code>：</p>
<ul>
<li><code>getRiverBasinTree</code></li>
<li><code>getTree</code>（type&#x3D;basin 时）</li>
<li><code>stationTreeByBasin</code></li>
<li><code>equipmentTreeByBasin</code></li>
<li><code>stationTreeFilteredByBasin</code></li>
<li><code>equipmentTreeFilteredByBasin</code></li>
</ul>
<p>去掉 RiverBasinTreeUtil 的虚拟根（”所有流域”），7 个真实根流域直接以数组返回。带 rvcd 时返回单元素 list，不传 rvcd 返回多根（过滤空根）。</p>
<p>行政区侧 4 个接口保持 <code>R&lt;TreeNode&gt;</code> 不变。</p>
<p><strong>2. 新增设备类型筛选接口</strong></p>
<ul>
<li><code>GET /equipmentTypesByRegion</code> — 按行政区获取过滤后的设备类型列表（去重）</li>
<li><code>GET /equipmentTypesByBasin</code> — 按流域获取过滤后的设备类型列表（去重）</li>
</ul>
<p>参数跟对应的过滤接口一致，不传参返回全量。内部复用已有的过滤逻辑，从结果树中提取 <code>queryChild.eqtp</code> 去重返回。</p>
<p>Service 返回 <code>List&lt;String&gt;</code>，Controller 包装 <code>R</code>，符合规范。</p>
<p><strong>3. 缓存自愈</strong></p>
<ul>
<li><code>getRegionTreeFromApi</code>：调完第三方接口后顺手写回 Redis（<code>GDSW_AREA_TREE</code>）</li>
<li><code>fetchBasinJsonStr</code>：调完第三方接口后顺手写回 Redis（<code>GDSW_RIVER_B</code>）</li>
<li>写缓存独立 try-catch，Redis 挂了不影响主流程</li>
<li>数据格式跟 BasicDataTask 一致（gzip 压缩）</li>
</ul>
<h4 id="2-5-3-性能数据"><a href="#2-5-3-性能数据" class="headerlink" title="2.5.3 性能数据"></a>2.5.3 性能数据</h4><table>
<thead>
<tr>
<th>接口</th>
<th>耗时</th>
</tr>
</thead>
<tbody><tr>
<td>equipmentTreeFilteredByBasin</td>
<td>~330ms</td>
</tr>
<tr>
<td>equipmentTypesByRegion</td>
<td>~1071ms</td>
</tr>
<tr>
<td>equipmentTypesByBasin</td>
<td>~279ms</td>
</tr>
</tbody></table>
<h4 id="2-5-4-前端注意"><a href="#2-5-4-前端注意" class="headerlink" title="2.5.4 前端注意"></a>2.5.4 前端注意</h4><ul>
<li>流域侧 6 个接口的 <code>data</code> 从对象变成数组，需要适配</li>
<li>行政区侧 4 个接口不变</li>
<li>设备类型接口不传参即可拿到全量类型列表</li>
</ul>
<p><img data-src="/images/posts/%E6%95%B0%E6%8D%AE%E5%BD%92%E9%9B%866%E6%9C%88%E6%8E%A2%E6%9F%A5%E5%AE%9E%E5%BD%95-%E4%B8%89%E5%A4%A9%E4%B8%89%E4%BB%B6%E4%BA%8B/fig3-api-contract.png" alt="6 月 2 日接口对接全景图"><br><em>图 2-1：6&#x2F;2 接口对接全景——前端适配 + 性能特征 + 提交清单</em></p>
<h3 id="2-6-6-2-经验总结"><a href="#2-6-6-2-经验总结" class="headerlink" title="2.6 6&#x2F;2 经验总结"></a>2.6 6&#x2F;2 经验总结</h3><blockquote>
<p>[!tip] 三个”清晰”</p>
<ol>
<li><strong>接口签名变化要清晰</strong>——<code>R&lt;TreeNode&gt;</code> → <code>R&lt;List&lt;TreeNode&gt;&gt;</code> 是破坏性变更，必须先 commit 不 push、让前端确认后再 merge</li>
<li><strong>接口返回格式要清晰</strong>——<code>List&lt;String&gt;</code> vs <code>R&lt;List&lt;String&gt;&gt;</code> 包装层级要统一</li>
<li><strong>性能特征要清晰</strong>——第一次访问 vs 后续命中要分开标注，方便前端做 loading 提示</li>
</ol>
<p>[!warning] 三个”独立”</p>
<ol>
<li><strong>“设备类型筛选接口”和”流域树接口”是独立的两件事</strong>——不要因为它们都跟”设备”相关就合并实现</li>
<li><strong>“缓存自愈”和”接口签名变更”是独立的两件事</strong>——分开 commit 方便回滚</li>
<li><strong>“负一级河流”和”PARENT_CODE&#x3D;’-1’”是两个独立的概念</strong>——字段含义不同、代码路径不同，不要混用</li>
</ol>
</blockquote>
<hr>
<h2 id="三、6-月-3-日：5-个数据地雷探查"><a href="#三、6-月-3-日：5-个数据地雷探查" class="headerlink" title="三、6 月 3 日：5 个数据地雷探查"></a>三、6 月 3 日：5 个数据地雷探查</h2><h3 id="3-1-起点：今天的工作不是写功能"><a href="#3-1-起点：今天的工作不是写功能" class="headerlink" title="3.1 起点：今天的工作不是写功能"></a>3.1 起点：今天的工作不是写功能</h3><p>今天的工作不是写功能，而是 **”探路”**——把数据归集模块里几条看起来”理所当然”的东西扒开看了一遍。起因是一个简单的问题：「gdsw 远程库里到底哪张表才是组织架构？」一路追下去，连带把跨数据源查询机制、计划文档与代码不一致、<code>equipmentTypes</code> 接口的近重复值、公司表里的脏数据全部牵了出来。</p>
<blockquote>
<p>[!abstract] 今日五个核心发现</p>
<ol>
<li>组织架构只有 <code>bs_sw_cy_dh_company</code> 一张表，且用 <strong>邻接表 + materialized path</strong> 双表示</li>
<li>“跨数据源 join” 实际是 <strong>Java 内存里两次查再拼</strong>，MySQL 层从来没真正跨过库</li>
<li>计划文档写的是 <code>@DS(&quot;water&quot;)</code>，代码里却是 <code>@DS(&quot;slave&quot;)</code>——本地测不出差异因为两者同库</li>
<li><code>/equipmentTypes</code> 返回 38 个唯一值里至少有 6-7 组语义重复，<code>DISTINCT</code> 解决不了字典污染</li>
<li>公司表里一堆区县水文中心 <strong>同名两个 ID</strong>：一个雪花 ID、一个 UUID，历史迁移没去重</li>
</ol>
</blockquote>
<p>下面逐个展开。</p>
<h3 id="3-2-发现-1：组织架构表-bs-sw-cy-dh-company"><a href="#3-2-发现-1：组织架构表-bs-sw-cy-dh-company" class="headerlink" title="3.2 发现 1：组织架构表 bs_sw_cy_dh_company"></a>3.2 发现 1：组织架构表 <code>bs_sw_cy_dh_company</code></h3><p>最初的问题是：”带组织架构的表” 在 gdsw 远程库到底有几张。结论很干脆：<strong>只有一张</strong>，叫 <code>bs_sw_cy_dh_company</code>，528 行，32 个根节点。</p>
<p>它的核心字段设计是典型的 <strong>“邻接表 + 物化路径” 双模式</strong>：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 关键字段</span></span><br><span class="line">ID            <span class="type">BIGINT</span>       <span class="comment">-- 雪花 ID（部分历史节点是 32 位 UUID，见发现 5）</span></span><br><span class="line">PARENT_ID     <span class="type">BIGINT</span>       <span class="comment">-- 邻接表的父指针</span></span><br><span class="line">TREE_CODE     <span class="type">VARCHAR</span>      <span class="comment">-- 逗号分隔的祖先 ID 路径，例如 &quot;,1813814710436458497,1813818490682961922,&quot;</span></span><br><span class="line">NAME          <span class="type">VARCHAR</span></span><br><span class="line">CODE          <span class="type">VARCHAR</span></span><br><span class="line">THIRDPARTY_ID <span class="type">VARCHAR</span></span><br><span class="line">UPDATE_TIME   DATETIME</span><br></pre></td></tr></table></figure>

<p><code>PARENT_ID</code> 用来查”我的直接孩子”——<code>WHERE PARENT_ID = ?</code> 一刀就完，简单。<code>TREE_CODE</code> 用来查”我的所有后代”——<code>WHERE TREE_CODE LIKE &#39;,1813818490682961922,%&#39;</code>，避免递归 CTE。两个一起冗余，写入时要保证一致性，但查询非常爽。</p>
<h4 id="3-2-1-根节点与水文业务主线"><a href="#3-2-1-根节点与水文业务主线" class="headerlink" title="3.2.1 根节点与水文业务主线"></a>3.2.1 根节点与水文业务主线</h4><p>根节点是 <strong>广东省水文厅</strong>（ID &#x3D; <code>1813814710436458497</code>），向下分两支：</p>
<ul>
<li><strong>厅内职能处室</strong>：办公室、规计处、防御处、调度处、人事处……</li>
<li><strong>流域 + 直属单位</strong>：东江&#x2F;西江&#x2F;北江&#x2F;韩江 4 个流域管理局，加上广东省水文局、广东省水利水政监察局、广东省水科院等</li>
</ul>
<p>我重点扒了 <strong>水文业务这条主线</strong>——<code>PARENT_ID = 1813818490682961922</code>（广东省水文局）下面挂了：</p>
<ul>
<li>局内的现代化建设规划办、十五五规划办、科创办</li>
<li><strong>12 个水文分局</strong>：深圳、广州、惠州、肇庆、韶关、汕头、佛山、江门、梅州、湛江、茂名、清远</li>
<li>每个分局再往下挂区&#x2F;县水文中心和具体测站，比如广州分局下面有番禺、增城、从化、黄埔水文中心，惠州分局下面有博罗、惠东水文中心，再下面才是麒麟咀、太平场、三善滘这种具体站点</li>
</ul>
<h4 id="3-2-2-谁会引用这张表"><a href="#3-2-2-谁会引用这张表" class="headerlink" title="3.2.2 谁会引用这张表"></a>3.2.2 谁会引用这张表</h4><table>
<thead>
<tr>
<th>表.字段</th>
<th>关系</th>
<th>备注</th>
</tr>
</thead>
<tbody><tr>
<td><code>bs_sw_cy_dh_equipmentb.ATCUNIT_ID</code></td>
<td>真外键</td>
<td>字段注释直接写”隶属行业单位 ID（所属组织架构 ID）”</td>
</tr>
<tr>
<td><code>bs_sw_cy_dh_station.ATCUNIT</code></td>
<td><strong>字符串</strong></td>
<td>“隶属”单位，存的是 NAME 不是 ID</td>
</tr>
<tr>
<td><code>bs_sw_cy_dh_station.ADMAUTH</code></td>
<td><strong>字符串</strong></td>
<td>“信息管理”单位，存的是 NAME 不是 ID</td>
</tr>
<tr>
<td><code>bs_sw_cy_dh_station.LOCALITY</code></td>
<td><strong>字符串</strong></td>
<td>“交换管理”单位，存的是 NAME 不是 ID</td>
</tr>
</tbody></table>
<p><strong>最后一句尤其要命</strong>：测站表存的是单位名字符串，一旦公司表里同名节点出现（见发现 5），join 出来就是笛卡尔积。</p>
<blockquote>
<p>[!info] 双模式设计的取舍</p>
<ul>
<li><strong>邻接表（PARENT_ID）</strong>：写起来简单（新增&#x2F;删除只动一个字段），但查子树要递归 CTE</li>
<li><strong>物化路径（TREE_CODE）</strong>：查子树一句 LIKE 搞定，但写入时要维护整条路径</li>
<li><strong>两者冗余</strong>：写入时要保证一致性（应用层或 trigger），但查询性能最佳</li>
</ul>
</blockquote>
<h3 id="3-3-发现-2：所谓”跨数据源查询”到底是怎么实现的"><a href="#3-3-发现-2：所谓”跨数据源查询”到底是怎么实现的" class="headerlink" title="3.3 发现 2：所谓”跨数据源查询”到底是怎么实现的"></a>3.3 发现 2：所谓”跨数据源查询”到底是怎么实现的</h3><p>用户问：”数据归集里的树是怎么跨数据源查询的？<code>stbprp</code> 表明明在另一个数据源啊？”</p>
<blockquote>
<p>[!warning] 反直觉但正确的答案<br><strong>MySQL 根本不支持跨库 JOIN</strong>（除非用 FEDERATED 引擎，而项目没启用）。所以 “跨源查询” 根本不是数据库层做的，而是 <strong>应用层分两次 SQL 各查一次，然后在 Java 内存里按 STCD 拼起来</strong>。</p>
</blockquote>
<h4 id="3-3-1-机制要点"><a href="#3-3-1-机制要点" class="headerlink" title="3.3.1 机制要点"></a>3.3.1 机制要点</h4><p><strong>mapper 的注解策略</strong>：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Mapper</span></span><br><span class="line"><span class="meta">@DS(&quot;slave&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">StStbprpBMapper</span> <span class="keyword">extends</span> <span class="title class_">BaseMapper</span>&lt;StStbprpB&gt; &#123; &#125;</span><br><span class="line"><span class="comment">// ↑ 这个 mapper 永远走 ysq 库</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// EquipmentBMapper 没有 @DS，走默认的 master，也就是 gdsw 库</span></span><br></pre></td></tr></table></figure>

<p>注解决定了 mapper 调用走哪个 <code>DataSource</code>，底层是 [[baomidou-dynamic-datasource]] 的 AOP 切换。也就是说每个 mapper <strong>“绑死”了它的数据源</strong>。</p>
<p><strong>两步查 + 内存拼</strong>：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 第一次查：去 ysq 库捞测站</span></span><br><span class="line">List&lt;StStbprpB&gt; stations = stStbprpBMapper.selectList(</span><br><span class="line">    <span class="keyword">new</span> <span class="title class_">QueryWrapper</span>&lt;StStbprpB&gt;().eq(<span class="string">&quot;USFL&quot;</span>, <span class="string">&quot;1&quot;</span>).likeRight(<span class="string">&quot;ADDVCD&quot;</span>, effectiveAddvcd)</span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 第二次查：用上一步的 STCD 列表去 gdsw 库捞设备</span></span><br><span class="line">List&lt;EquipmentB&gt; equipments = equipmentBMapper.selectList(</span><br><span class="line">    <span class="keyword">new</span> <span class="title class_">QueryWrapper</span>&lt;EquipmentB&gt;().in(<span class="string">&quot;STCD&quot;</span>, stations.stream().map(StStbprpB::getStcd).collect(toList()))</span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 然后在内存里按 STCD 分组，挂到对应测站节点下面</span></span><br></pre></td></tr></table></figure>

<p>**拼接键就是 <code>STCD</code>**（测站编码）——这是两个库唯一稳定的共识字段。</p>
<h4 id="3-3-2-隐藏成本"><a href="#3-3-2-隐藏成本" class="headerlink" title="3.3.2 隐藏成本"></a>3.3.2 隐藏成本</h4><p>第一次返回 N 条，第二次的 <code>IN (...)</code> 列表长度就是 N。MySQL 的 <code>max_allowed_packet</code> 和优化器对超长 IN 子句的处理能力都是有上限的，N 上千就要警惕。当前看 N 控制在百级别，安全；一旦放开过滤条件，可能要换成临时表 + JOIN 模式。</p>
<h4 id="3-3-3-数据源映射（本地-application-local-yml）"><a href="#3-3-3-数据源映射（本地-application-local-yml）" class="headerlink" title="3.3.3 数据源映射（本地 application-local.yml）"></a>3.3.3 数据源映射（本地 <code>application-local.yml</code>）</h4><table>
<thead>
<tr>
<th>key</th>
<th>物理地址</th>
<th>用途</th>
</tr>
</thead>
<tbody><tr>
<td><code>master</code></td>
<td><code>127.0.0.1:3306/gdsw</code></td>
<td>主业务库</td>
</tr>
<tr>
<td><code>slave</code></td>
<td><code>19.25.36.199:15076/ysq</code></td>
<td>水文数据源</td>
</tr>
<tr>
<td><code>water</code></td>
<td><code>19.25.36.199:15076/ysq</code></td>
<td>本机和 slave <strong>同库</strong></td>
</tr>
<tr>
<td><code>slave2</code></td>
<td>另一个库</td>
<td>河流数据 <code>getRiverData</code> 用</td>
</tr>
</tbody></table>
<p>注意 <code>slave</code> 和 <code>water</code> 在本机指向同一个库——这就是下一个发现的引线。</p>
<blockquote>
<p>[!tip] 这个模式的隐藏成本</p>
<ol>
<li><strong>不是真正的跨库 JOIN</strong>——MySQL 不支持，应用层分两次查</li>
<li><strong>mapper 跟数据源绑死</strong>——<code>@DS</code> 注解决定了 mapper 走哪个库，不能在运行时改</li>
<li><strong><code>IN (...)</code> 列表长度有上限</strong>——N 太大需要换成临时表 + JOIN</li>
<li><strong>STCD 是唯一共识字段</strong>——两个库的其他字段不一致，只能按 STCD 拼</li>
</ol>
</blockquote>
<p><img data-src="/images/posts/%E6%95%B0%E6%8D%AE%E5%BD%92%E9%9B%866%E6%9C%88%E6%8E%A2%E6%9F%A5%E5%AE%9E%E5%BD%95-%E4%B8%89%E5%A4%A9%E4%B8%89%E4%BB%B6%E4%BA%8B/fig4-cross-datasource.png" alt="跨数据源查询机制示意图"><br><em>图 3-1：跨数据源查询机制——Java 内存里两次查 + STCD 拼接</em></p>
<h3 id="3-4-发现-3：计划文档与实际代码不一致"><a href="#3-4-发现-3：计划文档与实际代码不一致" class="headerlink" title="3.4 发现 3：计划文档与实际代码不一致"></a>3.4 发现 3：计划文档与实际代码不一致</h3><p>巡查 <code>docs/superpowers/plans/2026-05-21-tree-selector-refactor.md</code> 时发现一个尴尬的对账问题。</p>
<p>文档里第 105 &#x2F; 138 &#x2F; 161 行多处明确写：</p>
<blockquote>
<p>StStbprpBMapper.java — <code>@DS(&quot;water&quot;)</code></p>
</blockquote>
<p>但翻到代码 <code>StStbprpBMapper.java:9</code> 实际是：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@DS(&quot;slave&quot;)</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p>[!danger] 本地测不出来，但生产可能出事<br>本机配置里 <code>slave</code> 和 <code>water</code> 都指向同一个 <code>ysq</code> 库，所以 <strong>怎么测都不报错</strong>。但生产&#x2F;测试环境的 yml 大概率把它们配成不同的物理库（这正是动态数据源存在的意义）。一旦不同，所有跨这个 mapper 的查询都在打错库。</p>
</blockquote>
<p>同一文件 <code>TreeSelectorServiceImpl.java</code> 里还有 <strong>自己跟自己不一致</strong> 的地方：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// line 509</span></span><br><span class="line"><span class="meta">@DS(&quot;water&quot;)</span></span><br><span class="line"><span class="keyword">public</span> ... getElementData(...) &#123; ... &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// line 544</span></span><br><span class="line"><span class="meta">@DS(&quot;slave&quot;)</span></span><br><span class="line"><span class="keyword">public</span> ... batchElementData(...) &#123; ... &#125;</span><br></pre></td></tr></table></figure>

<p>两个方法读的是同一类元素表，DS 注解的字符串却不一样。两种可能：</p>
<ol>
<li><strong>故意区分</strong>：单条&#x2F;批量走不同物理库做读写分离。如果是这样，应该有注释说明。</li>
<li><strong>历史遗留</strong>：refactor 时一处改了一处没改。</li>
</ol>
<p>需要去查生产 yml 来确认，但无论是哪种，<strong>当前注解和文档一致性是 0</strong>，得统一。</p>
<blockquote>
<p>[!warning] 这种”本地测不出但生产出事”的 bug 最危险</p>
<ol>
<li><strong>本地测不出</strong>——<code>slave</code> 和 <code>water</code> 同库</li>
<li><strong>CI 测不出</strong>——CI 通常也用本地 yml</li>
<li><strong>生产才会暴雷</strong>——但生产环境又复现困难</li>
<li><strong>修起来简单</strong>——改注解 + 改文档，但<strong>找起来困难</strong>——没人会怀疑 <code>@DS</code> 注解</li>
</ol>
</blockquote>
<h3 id="3-5-发现-4：-dataCollection-equipmentTypes-接口的近重复污染"><a href="#3-5-发现-4：-dataCollection-equipmentTypes-接口的近重复污染" class="headerlink" title="3.5 发现 4：/dataCollection/equipmentTypes 接口的近重复污染"></a>3.5 发现 4：<code>/dataCollection/equipmentTypes</code> 接口的近重复污染</h3><p>接口实现非常简单：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">QueryWrapper&lt;TreeEquipment&gt; wrapper = <span class="keyword">new</span> <span class="title class_">QueryWrapper</span>&lt;&gt;();</span><br><span class="line">wrapper.select(<span class="string">&quot;DISTINCT EQTP&quot;</span>).isNotNull(<span class="string">&quot;EQTP&quot;</span>).ne(<span class="string">&quot;EQTP&quot;</span>, <span class="string">&quot;&quot;</span>);</span><br></pre></td></tr></table></figure>

<p>就一个 SQL 层的 <code>DISTINCT</code>。问题来了：</p>
<ul>
<li><strong>接口实际返回 38 个唯一 EQTP</strong></li>
<li><strong>直查数据库 <code>SELECT DISTINCT EQTP FROM ...</code> 是 41 个</strong></li>
<li>差的 3 个里包括 ADCP、流速仪，还有一个待确认</li>
</ul>
<p>差 3 个本身不大，但深挖 38 个里的内容才是真正的雷：<strong>至少有 6-7 组是语义重复（字面不同但显然同种设备）</strong>。</p>
<h4 id="3-5-1-近重复实例"><a href="#3-5-1-近重复实例" class="headerlink" title="3.5.1 近重复实例"></a>3.5.1 近重复实例</h4><ol>
<li><code>HADCP-水平式ADCP</code> vs <code>水平式ADCP</code>——带不带产品前缀</li>
<li><code>在线HADCP</code> vs <code>在线H-ADCP</code>——一个连字符之差</li>
<li><code>FWQ-浮子水位计</code> vs <code>FWG-浮子水位计</code> vs <code>浮子式水位计</code>——三种命名混杂</li>
<li><code>BWG-气泡水位计</code> vs <code>气泡式水位计</code>——编码版 vs 通用版</li>
<li><code>水质自动监测设备</code> vs <code>水质自动监测站</code>——“设备”和”站”在数据录入员脑子里是一回事</li>
<li><code>雨量计</code> vs <code>翻斗式雨量计</code>——一个泛指一个具体</li>
<li><code>5探头非接触式雷达</code> &#x2F; <code>4探头非接触式雷达</code> &#x2F; <code>单探头非接触雷达</code> &#x2F; <code>多探头固定式雷达</code>——这组可能真的不同，需要业务确认</li>
</ol>
<blockquote>
<p>[!question] 为什么 DISTINCT 救不了<br><code>DISTINCT</code> 是字节级比较，它看 <code>&quot;在线HADCP&quot;</code> 和 <code>&quot;在线H-ADCP&quot;</code> 是两个不同字符串，没办法。要解决必须 <strong>在数据层（不是查询层）做归一化</strong>——一张 <code>EQTP -&gt; canonical_name</code> 映射表，或者干脆做受控字典，禁止录入员自由填。</p>
</blockquote>
<p>前端拿到这 38 个值往下拉框一塞，用户面对 <code>HADCP-水平式ADCP</code> 和 <code>水平式ADCP</code> 两个选项时怎么选？这是个长期会污染前端 UX 的小坑。</p>
<h3 id="3-6-发现-5：公司表脏数据——同名节点两个-ID"><a href="#3-6-发现-5：公司表脏数据——同名节点两个-ID" class="headerlink" title="3.6 发现 5：公司表脏数据——同名节点两个 ID"></a>3.6 发现 5：公司表脏数据——同名节点两个 ID</h3><p>最后一个发现是无意中扒出来的。在看广州水文分局（PARENT_ID &#x3D; <code>1813821005054963713</code>）下面的孩子时，发现 <strong>两个「增城区水文中心」并列</strong>：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">ID = 1876832803289645057            ← 雪花 ID，19 位数字</span><br><span class="line">ID = 2D4C663729774F93ABB96F7FF6503484  ← 32 位 UUID（旧系统留下）</span><br></pre></td></tr></table></figure>

<p>继续扫了一遍，类似情况遍地都是：番禺、增城、从化、黄埔、南沙、惠东、博罗、龙门、紫金、东源、龙川、五华、兴宁、廉江、雷州、吴川、化州、高州、信宜、新丰、翁源、仁化、浈江、乐昌、揭西、惠来、陆丰、湘桥……<strong>几乎所有区县级水文中心都同时存在两个节点</strong>。</p>
<blockquote>
<p>[!bug] 根因猜测<br>历史数据从老系统迁移过来用的是 UUID 主键；新系统启用后，新建节点用雪花 ID。同步脚本没做去重，结果两套 ID 共存。</p>
</blockquote>
<h4 id="3-6-1-下游影响"><a href="#3-6-1-下游影响" class="headerlink" title="3.6.1 下游影响"></a>3.6.1 下游影响</h4><p>更要命的是 <strong>下游的影响</strong>：</p>
<ul>
<li>前端树形选择器按 ID 唯一，但展示按 NAME。用户在树里看到两个一模一样的「增城区水文中心」，根本不知道选哪个。</li>
<li>内存里去重时 <strong>不能只按 ID</strong>，必须按 <code>(PARENT_ID, NAME)</code> 联合去重——而且要决策保留哪一个。</li>
<li>接发现 1 的伏笔：<code>bs_sw_cy_dh_station.ATCUNIT</code> 存的是 NAME 字符串。如果按 NAME join 公司表，一对二就是笛卡尔积，测站数瞬间翻倍。</li>
</ul>
<blockquote>
<p>[!warning] 这不是”加个 distinct”能解决的<br>必须从数据治理层面做一次清洗：按 <code>(PARENT_ID, NAME)</code> 聚合，保留新 ID（雪花），旧 ID（UUID）打软删除标记。同时要审计有没有外键指向旧 ID，指向旧 ID 的要 <strong>先重定向再标删</strong>，否则会产生悬空引用。</p>
</blockquote>
<h3 id="3-7-后续行动"><a href="#3-7-后续行动" class="headerlink" title="3.7 后续行动"></a>3.7 后续行动</h3><p>按优先级排：</p>
<blockquote>
<p>[!todo] 待办</p>
<ul>
<li><input disabled="" type="checkbox"> <strong>统一 mapper 数据源注解</strong>：拉生产&#x2F;测试的 yml 对一下 <code>slave</code> 和 <code>water</code> 是不是真的同库；如果是，删一个 key；如果不是，决定 <code>StStbprpBMapper</code> 该指向哪个，并改掉 <code>TreeSelectorServiceImpl</code> 里 509 行和 544 行的不一致</li>
<li><input disabled="" type="checkbox"> <strong>同步更新计划文档</strong>：<code>docs/superpowers/plans/2026-05-21-tree-selector-refactor.md</code> 里所有 <code>@DS(&quot;water&quot;)</code> 改成实际的注解值</li>
<li><input disabled="" type="checkbox"> <strong>设备类型归一化</strong>：为 <code>/equipmentTypes</code> 加一张 <code>EQTP -&gt; canonical_name</code> 映射表，接口返回 canonical 列表；老数据可以保留，但前端不再看到</li>
<li><input disabled="" type="checkbox"> <strong>公司表去重脚本</strong>：按 <code>(PARENT_ID, NAME)</code> 聚合，保留雪花 ID，UUID 节点先扫一遍外键引用，无引用直接删，有引用先重定向</li>
<li><input disabled="" type="checkbox"> <strong>回归测试</strong>：所有以上动作做完后，必须跑 <code>/dataCollection/stationTreeFilteredByBasin</code> 等接口确认树结构没变</li>
</ul>
</blockquote>
<blockquote>
<p>[!quote] 一句话总结<br>今天没写一行业务代码，但把 5 个”地雷”都标了出来——这种工作的价值不在 PR 行数，而在以后少加几次班。</p>
</blockquote>
<p><img data-src="/images/posts/%E6%95%B0%E6%8D%AE%E5%BD%92%E9%9B%866%E6%9C%88%E6%8E%A2%E6%9F%A5%E5%AE%9E%E5%BD%95-%E4%B8%89%E5%A4%A9%E4%B8%89%E4%BB%B6%E4%BA%8B/fig5-data-mines.png" alt="5 个数据地雷汇总图"><br><em>图 3-2：5 个数据地雷汇总——组织架构、跨源机制、注解不一致、设备类型污染、公司表脏数据</em></p>
<hr>
<h2 id="四、跨日总结：三天的工作节奏"><a href="#四、跨日总结：三天的工作节奏" class="headerlink" title="四、跨日总结：三天的工作节奏"></a>四、跨日总结：三天的工作节奏</h2><h3 id="4-1-节奏对比"><a href="#4-1-节奏对比" class="headerlink" title="4.1 节奏对比"></a>4.1 节奏对比</h3><table>
<thead>
<tr>
<th>日期</th>
<th>工作类型</th>
<th>产出</th>
<th>涉及代码行数</th>
</tr>
</thead>
<tbody><tr>
<td>6&#x2F;1</td>
<td>改造 + 排查</td>
<td>5 个接口、2 个工具方法、3 个文档</td>
<td>+115 &#x2F; -57</td>
</tr>
<tr>
<td>6&#x2F;2</td>
<td>对接 + 说明</td>
<td>3 个接口、3 个文档</td>
<td>+48 &#x2F; -24</td>
</tr>
<tr>
<td>6&#x2F;3</td>
<td>探查 + 待办</td>
<td>0 个接口改动、5 个待办、1 个工作记录</td>
<td>0</td>
</tr>
</tbody></table>
<p>**6&#x2F;1 是”行动日”<strong>——大量代码改动和接口上线；</strong>6&#x2F;2 是”收尾日”<strong>——写文档、对接前端；</strong>6&#x2F;3 是”反思日”**——不写代码，把隐藏问题挖出来。</p>
<h3 id="4-2-三个”相互纠缠”的主题"><a href="#4-2-三个”相互纠缠”的主题" class="headerlink" title="4.2 三个”相互纠缠”的主题"></a>4.2 三个”相互纠缠”的主题</h3><p>回头看，三天的工作其实在反复围绕 3 个主题：</p>
<ol>
<li><strong>接口签名变更</strong>：6&#x2F;1 改了 6 个接口 → 6&#x2F;2 写对接文档 → 6&#x2F;3 发现文档和代码不一致</li>
<li><strong>数据问题</strong>：6&#x2F;1 发现 148 个孤儿设备 → 6&#x2F;2 设备类型筛选接口 → 6&#x2F;3 发现设备类型污染</li>
<li><strong>跨源机制</strong>：6&#x2F;1 改造时用 <code>@DS</code> → 6&#x2F;2 提交时跟 plan 对账 → 6&#x2F;3 发现注解不一致</li>
</ol>
<p><strong>这是耦合的代码基底的必然结果</strong>——改一个接口会牵出文档、数据、跨源三个层面的问题。</p>
<h3 id="4-3-三个”应该记住”的经验"><a href="#4-3-三个”应该记住”的经验" class="headerlink" title="4.3 三个”应该记住”的经验"></a>4.3 三个”应该记住”的经验</h3><blockquote>
<p>[!tip] 经验一：文档与代码同步<br>计划文档（<code>docs/superpowers/plans/*.md</code>）和实际代码必须保持一致。如果改了代码没改文档，3 天后自己都会忘记当初为什么这么改。建议每次提交都检查一下 plan 里有没有引用到改动的代码。</p>
</blockquote>
<blockquote>
<p>[!tip] 经验二：数据问题用数据方式解决<br>148 个孤儿设备 STCD 为空——这种问题应该用 SQL 查 + 数据治理的方式解决，不要试图在代码里”模糊匹配”做补救。代码补救会引入不确定性，让数据问题更隐蔽。</p>
</blockquote>
<blockquote>
<p>[!tip] 经验三：探查日比编码日更宝贵<br>6&#x2F;3 一行业务代码都没写，但挖出的 5 个数据地雷，可能比 6&#x2F;1 那一波改造更有长期价值。建议每周至少留 1 天做”探查日”，把”理所当然”全部扒开看一遍。</p>
</blockquote>
<hr>
<h2 id="五、资料佐证"><a href="#五、资料佐证" class="headerlink" title="五、资料佐证"></a>五、资料佐证</h2><h3 id="5-1-数据库证据"><a href="#5-1-数据库证据" class="headerlink" title="5.1 数据库证据"></a>5.1 数据库证据</h3><h4 id="5-1-1-148-个孤儿设备-SQL"><a href="#5-1-1-148-个孤儿设备-SQL" class="headerlink" title="5.1.1 148 个孤儿设备 SQL"></a>5.1.1 148 个孤儿设备 SQL</h4><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 查询孤儿设备</span></span><br><span class="line"><span class="keyword">SELECT</span> e.EQID, e.EQNM, e.EQTYP, e.LGTD, e.LTTD,</span><br><span class="line">       s.STCD, s.STNM</span><br><span class="line"><span class="keyword">FROM</span> bs_sw_cy_dh_equipmentb e</span><br><span class="line"><span class="keyword">LEFT</span> <span class="keyword">JOIN</span> bs_sw_cy_dh_station s</span><br><span class="line">  <span class="keyword">ON</span> <span class="built_in">ABS</span>(s.LGTD <span class="operator">-</span> e.LGTD) <span class="operator">&lt;</span> <span class="number">0.01</span></span><br><span class="line"> <span class="keyword">AND</span> <span class="built_in">ABS</span>(s.LTTD <span class="operator">-</span> e.LTTD) <span class="operator">&lt;</span> <span class="number">0.01</span></span><br><span class="line"><span class="keyword">WHERE</span> e.USFL <span class="operator">=</span> <span class="string">&#x27;1&#x27;</span> <span class="keyword">AND</span> (e.STCD <span class="keyword">IS</span> <span class="keyword">NULL</span> <span class="keyword">OR</span> e.STCD <span class="operator">=</span> <span class="string">&#x27;&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 统计总数</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="built_in">COUNT</span>(<span class="operator">*</span>) <span class="keyword">AS</span> total,</span><br><span class="line">       <span class="built_in">SUM</span>(<span class="keyword">CASE</span> <span class="keyword">WHEN</span> STCD <span class="keyword">IS</span> <span class="keyword">NULL</span> <span class="keyword">OR</span> STCD <span class="operator">=</span> <span class="string">&#x27;&#x27;</span> <span class="keyword">THEN</span> <span class="number">1</span> <span class="keyword">ELSE</span> <span class="number">0</span> <span class="keyword">END</span>) <span class="keyword">AS</span> no_stcd,</span><br><span class="line">       <span class="built_in">SUM</span>(<span class="keyword">CASE</span> <span class="keyword">WHEN</span> EQID <span class="keyword">LIKE</span> <span class="string">&#x27; %&#x27;</span> <span class="keyword">THEN</span> <span class="number">1</span> <span class="keyword">ELSE</span> <span class="number">0</span> <span class="keyword">END</span>) <span class="keyword">AS</span> leading_space_eqid</span><br><span class="line"><span class="keyword">FROM</span> bs_sw_cy_dh_equipmentb <span class="keyword">WHERE</span> USFL <span class="operator">=</span> <span class="string">&#x27;1&#x27;</span>;</span><br><span class="line"><span class="comment">-- total=1813  no_stcd=148  leading_space_eqid=1</span></span><br></pre></td></tr></table></figure>

<h4 id="5-1-2-组织架构表结构"><a href="#5-1-2-组织架构表结构" class="headerlink" title="5.1.2 组织架构表结构"></a>5.1.2 组织架构表结构</h4><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 关键字段</span></span><br><span class="line">ID            <span class="type">BIGINT</span>       <span class="comment">-- 雪花 ID（部分历史节点是 32 位 UUID）</span></span><br><span class="line">PARENT_ID     <span class="type">BIGINT</span>       <span class="comment">-- 邻接表的父指针</span></span><br><span class="line">TREE_CODE     <span class="type">VARCHAR</span>      <span class="comment">-- 逗号分隔的祖先 ID 路径</span></span><br><span class="line">NAME          <span class="type">VARCHAR</span></span><br><span class="line">CODE          <span class="type">VARCHAR</span></span><br><span class="line">THIRDPARTY_ID <span class="type">VARCHAR</span></span><br><span class="line">UPDATE_TIME   DATETIME</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 528 行，32 个根节点</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="built_in">COUNT</span>(<span class="operator">*</span>) <span class="keyword">FROM</span> bs_sw_cy_dh_company;  <span class="comment">-- 528</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="built_in">COUNT</span>(<span class="keyword">DISTINCT</span> PARENT_ID) <span class="keyword">FROM</span> bs_sw_cy_dh_company <span class="keyword">WHERE</span> PARENT_ID <span class="keyword">NOT</span> <span class="keyword">IN</span> (<span class="keyword">SELECT</span> ID <span class="keyword">FROM</span> bs_sw_cy_dh_company);  <span class="comment">-- 32</span></span><br></pre></td></tr></table></figure>

<h4 id="5-1-3-同名节点双-ID-检测"><a href="#5-1-3-同名节点双-ID-检测" class="headerlink" title="5.1.3 同名节点双 ID 检测"></a>5.1.3 同名节点双 ID 检测</h4><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 检测 (PARENT_ID, NAME) 重复</span></span><br><span class="line"><span class="keyword">SELECT</span> PARENT_ID, NAME, <span class="built_in">COUNT</span>(<span class="operator">*</span>) <span class="keyword">AS</span> cnt,</span><br><span class="line">       GROUP_CONCAT(ID) <span class="keyword">AS</span> ids</span><br><span class="line"><span class="keyword">FROM</span> bs_sw_cy_dh_company</span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> PARENT_ID, NAME</span><br><span class="line"><span class="keyword">HAVING</span> <span class="built_in">COUNT</span>(<span class="operator">*</span>) <span class="operator">&gt;</span> <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 升级为清洗脚本（保留雪花 ID，UUID 节点打软删）</span></span><br><span class="line"><span class="comment">-- 步骤 1: 找出有外键引用的旧 ID</span></span><br><span class="line"><span class="comment">-- 步骤 2: 重定向外键</span></span><br><span class="line"><span class="comment">-- 步骤 3: 标删旧 ID</span></span><br></pre></td></tr></table></figure>

<h3 id="5-2-提交记录（commit-b1cfd65）"><a href="#5-2-提交记录（commit-b1cfd65）" class="headerlink" title="5.2 提交记录（commit b1cfd65）"></a>5.2 提交记录（commit b1cfd65）</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">feat: 流域接口返回多根节点 + 新增设备类型查询接口 + 缓存自愈</span><br></pre></td></tr></table></figure>

<p><strong>改动文件（3 个）</strong>：</p>
<table>
<thead>
<tr>
<th>文件</th>
<th>改动</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>TreeSelectorService.java</td>
<td>+16&#x2F;-8</td>
<td>6 个流域接口签名改为 R&lt;List<TreeNode>&gt;，新增 2 个设备类型方法</td>
</tr>
<tr>
<td>TreeSelectorController.java</td>
<td>+32&#x2F;-8</td>
<td>对应端点签名同步改，新增 2 个设备类型端点</td>
</tr>
<tr>
<td>TreeSelectorServiceImpl.java</td>
<td>+200&#x2F;-58</td>
<td>多根处理逻辑 + 缓存自愈 + 设备类型查询实现</td>
</tr>
</tbody></table>
<p><strong>commit 关联</strong>：</p>
<ul>
<li>6&#x2F;2 提交 b1cfd65</li>
<li>6&#x2F;1 的 type 字段、6&#x2F;1 的多根改造、6&#x2F;1 的性能优化、6&#x2F;2 的设备类型接口、6&#x2F;2 的缓存自愈 都在这次 commit 里</li>
</ul>
<h3 id="5-3-API-验证结果"><a href="#5-3-API-验证结果" class="headerlink" title="5.3 API 验证结果"></a>5.3 API 验证结果</h3><h4 id="5-3-1-type-字段链路验证"><a href="#5-3-1-type-字段链路验证" class="headerlink" title="5.3.1 type 字段链路验证"></a>5.3.1 type 字段链路验证</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 请求 equipmentTreeFilteredByBasin</span></span><br><span class="line">curl -s <span class="string">&quot;http://localhost:8080/dataCollection/equipmentTreeFilteredByBasin&quot;</span> | jq <span class="string">&#x27;.data[0]&#x27;</span></span><br></pre></td></tr></table></figure>

<p>返回：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;812_813&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="number">2</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;children&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;812_813_-1&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;label&quot;</span><span class="punctuation">:</span> <span class="string">&quot;-1级河流&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="number">2</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;children&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">        <span class="punctuation">&#123;</span></span><br><span class="line">          <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;HD2D0000000P&quot;</span><span class="punctuation">,</span></span><br><span class="line">          <span class="attr">&quot;label&quot;</span><span class="punctuation">:</span> <span class="string">&quot;鸡啼门水道&quot;</span><span class="punctuation">,</span></span><br><span class="line">          <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="number">2</span><span class="punctuation">,</span></span><br><span class="line">          <span class="attr">&quot;children&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">            <span class="punctuation">&#123;</span></span><br><span class="line">              <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;81301880&quot;</span><span class="punctuation">,</span></span><br><span class="line">              <span class="attr">&quot;label&quot;</span><span class="punctuation">:</span> <span class="string">&quot;白蕉&quot;</span><span class="punctuation">,</span></span><br><span class="line">              <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="number">3</span><span class="punctuation">,</span></span><br><span class="line">              <span class="attr">&quot;queryChild&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;sttp&quot;</span><span class="punctuation">:</span> <span class="string">&quot;TT&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">              <span class="attr">&quot;children&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">                <span class="punctuation">&#123;</span> <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;8480021031&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;label&quot;</span><span class="punctuation">:</span> <span class="string">&quot;在线H-ADCP&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="number">4</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">                <span class="punctuation">&#123;</span> <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;PP81301880&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="number">4</span> <span class="punctuation">&#125;</span></span><br><span class="line">              <span class="punctuation">]</span></span><br><span class="line">            <span class="punctuation">&#125;</span></span><br><span class="line">          <span class="punctuation">]</span></span><br><span class="line">        <span class="punctuation">&#125;</span></span><br><span class="line">      <span class="punctuation">]</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">]</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>✅ 全部正确。</p>
<h4 id="5-3-2-设备类型接口验证"><a href="#5-3-2-设备类型接口验证" class="headerlink" title="5.3.2 设备类型接口验证"></a>5.3.2 设备类型接口验证</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -s <span class="string">&quot;http://localhost:8080/dataCollection/equipmentTypesByBasin&quot;</span> | jq <span class="string">&#x27;.data&#x27;</span></span><br></pre></td></tr></table></figure>

<p>返回 6 个（流域全量）：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">[</span></span><br><span class="line">  <span class="string">&quot;在线H-ADCP&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="string">&quot;HADCP-水平式ADCP&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="string">&quot;FWQ-浮子水位计&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="string">&quot;翻斗式雨量计&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="string">&quot;BWG-气泡水位计&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="string">&quot;FWG-浮子水位计&quot;</span></span><br><span class="line"><span class="punctuation">]</span></span><br></pre></td></tr></table></figure>

<h3 id="5-4-性能数据"><a href="#5-4-性能数据" class="headerlink" title="5.4 性能数据"></a>5.4 性能数据</h3><table>
<thead>
<tr>
<th>接口</th>
<th>第一次（缓存空）</th>
<th>后续命中</th>
<th>备注</th>
</tr>
</thead>
<tbody><tr>
<td><code>riverBasinTree</code></td>
<td>~330ms</td>
<td>~150ms</td>
<td>DB 直连</td>
</tr>
<tr>
<td><code>stationTreeByBasin</code></td>
<td>~500ms</td>
<td>~400ms</td>
<td>过滤空根</td>
</tr>
<tr>
<td><code>equipmentTreeByBasin</code></td>
<td>~500ms</td>
<td>~400ms</td>
<td>过滤空根</td>
</tr>
<tr>
<td><code>equipmentTreeFilteredByBasin</code></td>
<td>~330ms</td>
<td>~250ms</td>
<td>含过滤</td>
</tr>
<tr>
<td><code>equipmentTypesByRegion</code></td>
<td>~1071ms</td>
<td>~800ms</td>
<td>递归收集 + 去重</td>
</tr>
<tr>
<td><code>equipmentTypesByBasin</code></td>
<td>~279ms</td>
<td>~200ms</td>
<td>流域范围更小</td>
</tr>
<tr>
<td><code>getRegionTreeFromApi</code></td>
<td>~1500ms</td>
<td>~500ms</td>
<td>第一次调第三方</td>
</tr>
<tr>
<td><code>stationTreeByRegion</code></td>
<td>~1500ms</td>
<td>~500ms</td>
<td>第一次调第三方</td>
</tr>
<tr>
<td><code>equipmentTreeByRegion</code></td>
<td>~1500ms</td>
<td>~500ms</td>
<td>第一次调第三方</td>
</tr>
</tbody></table>
<p><strong>DB 查询次数优化对比</strong>：</p>
<table>
<thead>
<tr>
<th>接口</th>
<th>改前</th>
<th>改后</th>
</tr>
</thead>
<tbody><tr>
<td><code>stationTreeByBasin</code> no-rvcd 测站查询</td>
<td>7 次</td>
<td>1 次</td>
</tr>
<tr>
<td><code>equipmentTreeByBasin</code> 设备查询</td>
<td>7 次</td>
<td>1 次</td>
</tr>
</tbody></table>
<p>7 个根场景下两个接口的 DB 查询总数从 14 次降到 2 次。</p>
<h3 id="5-5-涉及文件清单"><a href="#5-5-涉及文件清单" class="headerlink" title="5.5 涉及文件清单"></a>5.5 涉及文件清单</h3><h4 id="5-5-1-后端代码"><a href="#5-5-1-后端代码" class="headerlink" title="5.5.1 后端代码"></a>5.5.1 后端代码</h4><table>
<thead>
<tr>
<th>模块</th>
<th>路径</th>
<th>改动类型</th>
</tr>
</thead>
<tbody><tr>
<td>API DTO</td>
<td><code>gdsw-modules/algorithm-plugin-modules/algorithm-plugin-api/src/main/java/com/zny/algorithm/api/dto/TreeNode.java</code></td>
<td>新增 type 字段</td>
</tr>
<tr>
<td>Service 接口</td>
<td><code>gdsw-modules/data-collection-modules/data-collection-api/src/main/java/com/zny/datacollection/api/service/TreeSelectorService.java</code></td>
<td>6 个方法签名调整</td>
</tr>
<tr>
<td>Service 实现</td>
<td><code>gdsw-modules/data-collection-modules/data-collection-starter/src/main/java/com/zny/datacollection/starter/service/impl/TreeSelectorServiceImpl.java</code></td>
<td>6 个方法重写 + 抽 fetchBasinJsonStr + 查询优化 + 空根过滤 + 缓存自愈</td>
</tr>
<tr>
<td>Controller</td>
<td><code>gdsw-modules/data-collection-modules/data-collection-core/src/main/java/com/zny/datacollection/core/controller/TreeSelectorController.java</code></td>
<td>6 处端点签名调整</td>
</tr>
<tr>
<td>工具类</td>
<td><code>gdsw-modules/data-collection-modules/data-collection-starter/src/main/java/com/zny/datacollection/starter/utils/RiverBasinTreeUtil.java</code></td>
<td>仅参考，未改动</td>
</tr>
<tr>
<td>Mapper</td>
<td><code>gdsw-modules/data-collection-modules/data-collection-core/src/main/java/com/zny/datacollection/core/mapper/StStbprpBMapper.java</code></td>
<td><code>@DS(&quot;water&quot;)</code> vs <code>@DS(&quot;slave&quot;)</code> 待确认</td>
</tr>
</tbody></table>
<h4 id="5-5-2-数据库表"><a href="#5-5-2-数据库表" class="headerlink" title="5.5.2 数据库表"></a>5.5.2 数据库表</h4><table>
<thead>
<tr>
<th>表名</th>
<th>用途</th>
<th>行数</th>
<th>备注</th>
</tr>
</thead>
<tbody><tr>
<td><code>bs_sw_cy_dh_company</code></td>
<td>组织架构</td>
<td>528</td>
<td>邻接表 + 物化路径双模式</td>
</tr>
<tr>
<td><code>bs_sw_cy_dh_equipmentb</code></td>
<td>设备</td>
<td>1813</td>
<td>148 个 STCD 为空</td>
</tr>
<tr>
<td><code>bs_sw_cy_dh_station</code></td>
<td>测站</td>
<td>1051</td>
<td>ATCUNIT 存 NAME 字符串</td>
</tr>
</tbody></table>
<h4 id="5-5-3-文档"><a href="#5-5-3-文档" class="headerlink" title="5.5.3 文档"></a>5.5.3 文档</h4><table>
<thead>
<tr>
<th>文档</th>
<th>路径</th>
<th>状态</th>
</tr>
</thead>
<tbody><tr>
<td>6&#x2F;1 工作记录</td>
<td><code>项目/数据归集系统/2026-06-01/2026-06-01工作记录.md</code></td>
<td>已发布</td>
</tr>
<tr>
<td>TreeNode type 字段</td>
<td><code>项目/数据归集系统/2026-06-01/TreeNode-type字段功能.md</code></td>
<td>已发布</td>
</tr>
<tr>
<td>树接口流域多根改造</td>
<td><code>项目/数据归集系统/2026-06-01/树接口流域多根改造.md</code></td>
<td>已发布</td>
</tr>
<tr>
<td>紫洞设备问题排查</td>
<td><code>项目/数据归集系统/2026-06-01/紫洞设备问题排查.md</code></td>
<td>已发布</td>
</tr>
<tr>
<td>6&#x2F;2 tree 接口对接说明</td>
<td><code>项目/数据归集系统/2026-06-02/tree接口改造对接说明.md</code></td>
<td>已发布</td>
</tr>
<tr>
<td>6&#x2F;2 设备类型筛选接口</td>
<td><code>项目/数据归集系统/2026-06-02/设备类型筛选接口.md</code></td>
<td>已发布</td>
</tr>
<tr>
<td>6&#x2F;2 负一级河流逻辑</td>
<td><code>项目/数据归集系统/2026-06-02/负一级河流逻辑说明.md</code></td>
<td>已发布</td>
</tr>
<tr>
<td>6&#x2F;3 数据归集探查</td>
<td><code>项目/数据归集系统/2026-06-03/2026-06-03记录.md</code></td>
<td>已发布</td>
</tr>
<tr>
<td>6&#x2F;3 提交记录</td>
<td><code>项目/数据归集系统/2026-06-03/提交记录.md</code></td>
<td>已发布</td>
</tr>
</tbody></table>
<h3 id="5-6-外部参考"><a href="#5-6-外部参考" class="headerlink" title="5.6 外部参考"></a>5.6 外部参考</h3><h4 id="5-6-1-baomidou-dynamic-datasource"><a href="#5-6-1-baomidou-dynamic-datasource" class="headerlink" title="5.6.1 baomidou-dynamic-datasource"></a>5.6.1 baomidou-dynamic-datasource</h4><ul>
<li><strong>官方文档</strong>：<a href="https://baomidou.com/guides/dynamic-datasource/">https://baomidou.com/guides/dynamic-datasource/</a></li>
<li><strong>核心机制</strong>：基于 Spring AOP 的 <code>@DS</code> 注解 + <code>DataSource</code> 注册中心</li>
<li><strong>本文引用</strong>：[[baomidou-dynamic-datasource]]</li>
<li><strong>相关引用</strong>：mapper 注解 <code>@DS(&quot;slave&quot;)</code> 决定数据源</li>
</ul>
<h4 id="5-6-2-MyBatis-Plus"><a href="#5-6-2-MyBatis-Plus" class="headerlink" title="5.6.2 MyBatis-Plus"></a>5.6.2 MyBatis-Plus</h4><ul>
<li><strong>官方文档</strong>：<a href="https://baomidou.com/">https://baomidou.com/</a></li>
<li><strong>本文引用</strong>：<code>QueryWrapper</code> &#x2F; <code>BaseMapper</code> &#x2F; <code>groupingBy</code> 等</li>
</ul>
<h4 id="5-6-3-MySQL-跨库限制"><a href="#5-6-3-MySQL-跨库限制" class="headerlink" title="5.6.3 MySQL 跨库限制"></a>5.6.3 MySQL 跨库限制</h4><ul>
<li><strong>FEDERATED 引擎</strong>：<a href="https://dev.mysql.com/doc/refman/8.0/en/federated-storage-engine.html">https://dev.mysql.com/doc/refman/8.0/en/federated-storage-engine.html</a></li>
<li><strong>本文核心结论</strong>：MySQL 默认不支持跨库 JOIN，应用层分两次查 + 内存拼是常见做法</li>
</ul>
<h3 id="5-7-关联博客"><a href="#5-7-关联博客" class="headerlink" title="5.7 关联博客"></a>5.7 关联博客</h3><ul>
<li><a href="/2026/05/21/%E6%A0%91%E7%BB%93%E6%9E%84%E9%80%89%E6%8B%A9%E5%99%A8-%E4%BB%8ERedis%E5%88%B0%E7%9B%B4%E8%BF%9E%E6%95%B0%E6%8D%AE%E5%BA%93/">《树结构选择器：从 Redis 到直连数据库的架构重构》</a>——5&#x2F;21 改造基础</li>
<li><a href="/2026/02/28/Spring-Boot%E5%A4%9A%E6%95%B0%E6%8D%AE%E6%BA%90%E5%BC%80%E5%8F%91%E5%AE%9E%E8%B7%B5%EF%BC%9A%E4%BB%8E%E8%B8%A9%E5%9D%91%E5%88%B0%E8%A7%A3%E5%86%B3/">《Spring Boot 多数据源开发实践：从踩坑到解决》</a>——多数据源原理</li>
<li><a href="/2026/05/07/%E6%B0%B4%E6%96%87%E6%95%B0%E6%8D%AE%E5%9D%87%E5%80%BC%E5%AF%B9%E6%AF%94%E6%9F%A5%E8%AF%A2Bug%E4%BF%AE%E5%A4%8D%E5%85%A8%E8%AE%B0%E5%BD%95/">《水文数据均值对比查询 Bug 修复全记录》</a>——同项目 bug 修复风格</li>
<li><a href="/2026/03/19/%E5%B9%BF%E4%B8%9C%E6%B0%B4%E6%96%87%E9%A1%B9%E7%9B%AE%E9%97%AE%E9%A2%98%E6%8E%92%E6%9F%A5%E5%AE%9E%E5%BD%952026%E5%B9%B43%E6%9C%8819%E6%97%A5%E6%8A%80%E6%9C%AF%E6%8C%91%E6%88%98/">《广东水文项目问题排查实录 2026 年 3 月 19 日》</a>——同项目排查风格</li>
</ul>
<hr>
<h2 id="六、经验总结与结语"><a href="#六、经验总结与结语" class="headerlink" title="六、经验总结与结语"></a>六、经验总结与结语</h2><h3 id="6-1-三个”不一样”"><a href="#6-1-三个”不一样”" class="headerlink" title="6.1 三个”不一样”"></a>6.1 三个”不一样”</h3><blockquote>
<p>[!tip] 工作节奏不一样<br>6&#x2F;1 是行动日，6&#x2F;2 是收尾日，6&#x2F;3 是反思日。<strong>三天里只有 6&#x2F;1 写代码</strong>，但 6&#x2F;3 挖出的问题可能比 6&#x2F;1 那一波改造更有长期价值。不要把每天塞满编码。</p>
</blockquote>
<blockquote>
<p>[!tip] 问题深度不一样<br>6&#x2F;1 的”148 个孤儿设备”是数据问题，6&#x2F;3 的”5 个数据地雷”是治理问题。前者改一行 SQL 就能修，后者需要跨部门协作。<strong>看清问题的层级</strong>比急着解决更重要。</p>
</blockquote>
<blockquote>
<p>[!tip] 文档价值不一样<br>6&#x2F;2 的 3 篇对接文档（接口对接、设备类型、负一级河流）让前端和上游数据问题有了”单一可信源”。<strong>好的文档不是重复代码</strong>，而是解释”为什么这样设计”。</p>
</blockquote>
<h3 id="6-2-三个”应该坚持”"><a href="#6-2-三个”应该坚持”" class="headerlink" title="6.2 三个”应该坚持”"></a>6.2 三个”应该坚持”</h3><blockquote>
<p>[!tip] 坚持写工作记录<br>6&#x2F;1 ~ 6&#x2F;3 三天每天都有工作记录，6&#x2F;1 的 8 个章节、6&#x2F;2 的 6 个章节、6&#x2F;3 的 7 个发现，都有完整的证据链。<strong>未来的你会感谢现在的自己</strong>。</p>
</blockquote>
<blockquote>
<p>[!tip] 坚持用数据说话<br>148 不是拍脑袋，是 <code>SELECT COUNT(*)</code> 查出来的；7 个根流域不是猜的，是 <code>SELECT COUNT(DISTINCT PARENT_ID)</code> 算出来的。<strong>没有数据支撑的结论都是空话</strong>。</p>
</blockquote>
<blockquote>
<p>[!tip] 坚持做”探查日”<br>每周至少留 1 天做”探查日”，把”理所当然”全部扒开看一遍。<strong>这种工作的价值不在 PR 行数，而在以后少加几次班</strong>。</p>
</blockquote>
<h3 id="6-3-结语"><a href="#6-3-结语" class="headerlink" title="6.3 结语"></a>6.3 结语</h3><p>三天三件事，看上去是分立的几个任务，实际上是数据归集系统从”能跑”到”跑得对”的一次集中检视。<strong>6&#x2F;1 的改造让它跑得更快，6&#x2F;2 的文档让它跑得更稳，6&#x2F;3 的探查让它跑得更对</strong>。</p>
<p>下一个迭代的目标很明确：把 6&#x2F;3 那 5 个待办逐个落地——统一 mapper 注解、同步计划文档、设备类型归一化、公司表去重、回归测试。到那时，这套系统才真正称得上”production-ready”。</p>
<blockquote>
<p>如果你也在做类似的多数据源、多端点的前后端集成，希望这篇三天实录能给你一些参考。代码和文档都在文末的资料佐证里，欢迎对照复现。</p>
</blockquote>
<p><strong>—— 写于 2026 年 6 月 3 日深夜，把 5 个地雷都标完了</strong></p>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>数据归集</category>
      </categories>
      <tags>
        <tag>Spring Boot</tag>
        <tag>多数据源</tag>
        <tag>数据归集</tag>
        <tag>树选择器</tag>
        <tag>baomidou-dynamic-datasource</tag>
        <tag>MySQL</tag>
        <tag>数据治理</tag>
        <tag>Bug排查</tag>
        <tag>接口改造</tag>
      </tags>
  </entry>
  <entry>
    <title>树结构选择器：从 Redis 到直连数据库的架构重构</title>
    <url>/2026/05/21/%E6%A0%91%E7%BB%93%E6%9E%84%E9%80%89%E6%8B%A9%E5%99%A8-%E4%BB%8ERedis%E5%88%B0%E7%9B%B4%E8%BF%9E%E6%95%B0%E6%8D%AE%E5%BA%93/</url>
    <content><![CDATA[<p>数据归集系统的树结构选择器是前端高频调用的核心接口。原本从 Redis 读取 GZIP 压缩的全量 JSON 数据，每次请求都要解压 7MB 数据再内存过滤。本文记录了将其改造为直连数据库的完整过程，包括多数据源配置、第三方 API 对接、以及踩坑经验。</p>
<div class="audio-player">
  <div class="audio-section">
    <h4>🎧 文章导读</h4>
    <audio controls preload="metadata">
      <source src="/audio/树结构选择器-从Redis到直连数据库/narration.mp3" type="audio/mpeg">
      您的浏览器不支持音频播放。
    </audio>
  </div>
  <div class="audio-section">
    <h4>🎵 背景音乐</h4>
    <audio controls loop preload="metadata">
      <source src="/audio/树结构选择器-从Redis到直连数据库/bgm.mp3" type="audio/mpeg">
      您的浏览器不支持音频播放。
    </audio>
  </div>
</div>

<span id="more"></span>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>在水文监测数据归集系统中，树结构选择器（行政区→测站→设备三级联动）是前端页面的核心交互组件。原有实现统一走 Redis 缓存——第三方系统把数据同步到 Redis，后端从 Redis 取 GZIP+Base64 压缩的全量 JSON，解压后在内存中遍历过滤。</p>
<p>这套方案在测试和生产环境跑得通，但在本地开发时问题暴露得非常明显。</p>
<h2 id="重构动机：三个核心痛点"><a href="#重构动机：三个核心痛点" class="headerlink" title="重构动机：三个核心痛点"></a>重构动机：三个核心痛点</h2><h3 id="痛点一：本地环境-Redis-缺数据"><a href="#痛点一：本地环境-Redis-缺数据" class="headerlink" title="痛点一：本地环境 Redis 缺数据"></a>痛点一：本地环境 Redis 缺数据</h3><p>开发机上没有跑同步任务，Redis 里要么没 key 要么数据过期了，导致三个树接口全部返回空。每次想调试一个和选择器相关的功能，都要先想办法往 Redis 里灌数据，或者连上测试环境的 Redis。</p>
<h3 id="痛点二：每次请求解压-7MB-全量数据"><a href="#痛点二：每次请求解压-7MB-全量数据" class="headerlink" title="痛点二：每次请求解压 7MB 全量数据"></a>痛点二：每次请求解压 7MB 全量数据</h3><p>Redis 里存的是整个表的 JSON 压缩包，哪怕前端只想查一个测站下的几台设备，后端也得把全部设备数据解出来再遍历。内存开销和 GC 压力都不小。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 改造前的核心模式：Redis → 解压 → 遍历</span></span><br><span class="line"><span class="type">String</span> <span class="variable">compressed</span> <span class="operator">=</span> redisTemplate.opsForValue().get(<span class="string">&quot;GDSW_STBPRP_B&quot;</span>);</span><br><span class="line"><span class="type">String</span> <span class="variable">json</span> <span class="operator">=</span> decompress(compressed);  <span class="comment">// GZIP + Base64 解压，7MB JSON</span></span><br><span class="line">List&lt;Station&gt; all = JSON.parseArray(json, Station.class);</span><br><span class="line"><span class="comment">// 内存遍历过滤，O(n) 全量扫描</span></span><br><span class="line"><span class="keyword">return</span> all.stream().filter(s -&gt; s.getAddvcd().startsWith(prefix)).collect(toList());</span><br></pre></td></tr></table></figure>

<h3 id="痛点三：数据新鲜度依赖外部同步任务"><a href="#痛点三：数据新鲜度依赖外部同步任务" class="headerlink" title="痛点三：数据新鲜度依赖外部同步任务"></a>痛点三：数据新鲜度依赖外部同步任务</h3><p>Redis 里的数据什么时候更新、更新失败怎么办，后端完全不知道，只能被动等待。遇到数据不一致的 bug，排查链路要跨两个系统。</p>
<h2 id="改造方案：直连数据源"><a href="#改造方案：直连数据源" class="headerlink" title="改造方案：直连数据源"></a>改造方案：直连数据源</h2><p>选择直连数据源的理由很直接：数据源本身就在那里——water 库里有完整的测站表，gdsw 库里有完整的设备表，第三方 API 能返回行政区树。既然源头可用，为什么还要绕 Redis 一圈？</p>
<p><img data-src="/images/posts/%E6%A0%91%E7%BB%93%E6%9E%84%E9%80%89%E6%8B%A9%E5%99%A8-%E4%BB%8ERedis%E5%88%B0%E7%9B%B4%E8%BF%9E%E6%95%B0%E6%8D%AE%E5%BA%93/fig1.png" alt="改造前后数据流对比"><br><em>图1：改造前（Redis 全量缓存）vs 改造后（直连数据库）的数据流对比</em></p>
<p>改造后的数据流大幅简化：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">第三方API            water数据库(ysq)       gdsw数据库</span><br><span class="line">    |                     |                      |</span><br><span class="line">    | POST + 网关签名     | @DS(&quot;water&quot;)         | 默认 master</span><br><span class="line">    v                     v                      v</span><br><span class="line">前端 &lt;── TreeSelectorServiceImpl ──(dynamic-datasource 切换)──&gt; 数据库</span><br></pre></td></tr></table></figure>

<h2 id="核心实现"><a href="#核心实现" class="headerlink" title="核心实现"></a>核心实现</h2><h3 id="Step-1-行政区树——第三方接口直连"><a href="#Step-1-行政区树——第三方接口直连" class="headerlink" title="Step 1: 行政区树——第三方接口直连"></a>Step 1: 行政区树——第三方接口直连</h3><p>行政区数据来源于第三方门户系统的 API，改造后直接调用第三方接口，配合网关签名认证：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 网关签名：SHA256(timestamp + token + nonce + timestamp)</span></span><br><span class="line"><span class="keyword">private</span> Map&lt;String, String&gt; <span class="title function_">generateGatewayHeaders</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">timestamp</span> <span class="operator">=</span> String.valueOf(System.currentTimeMillis() / <span class="number">1000</span>);</span><br><span class="line">    <span class="type">String</span> <span class="variable">nonce</span> <span class="operator">=</span> UUID.randomUUID().toString().replace(<span class="string">&quot;-&quot;</span>, <span class="string">&quot;&quot;</span>);</span><br><span class="line">    <span class="type">String</span> <span class="variable">tifSignature</span> <span class="operator">=</span> sha256(timestamp + TIF_TOKEN + nonce + timestamp);</span><br><span class="line">    <span class="type">String</span> <span class="variable">tspSignature</span> <span class="operator">=</span> sha256(timestamp + PAAS_TOKEN + nonce + timestamp).toUpperCase();</span><br><span class="line">    <span class="comment">// ...构建 headers map...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="Step-2-测站列表——数据库查询-前缀匹配"><a href="#Step-2-测站列表——数据库查询-前缀匹配" class="headerlink" title="Step 2: 测站列表——数据库查询 + 前缀匹配"></a>Step 2: 测站列表——数据库查询 + 前缀匹配</h3><p>测站表在 water 数据库的 <code>st_stbprp_b</code> 表中。核心挑战是前端传的行政区编码（12 位）和数据库存的编码（6 位）长度不一致：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 去掉尾部零，提取有效前缀</span></span><br><span class="line"><span class="type">String</span> <span class="variable">effectiveAddvcd</span> <span class="operator">=</span> addvcd.replaceAll(<span class="string">&quot;0+$&quot;</span>, <span class="string">&quot;&quot;</span>);  <span class="comment">// 440100000000 → 4401</span></span><br><span class="line">QueryWrapper&lt;StStbprpB&gt; wrapper = <span class="keyword">new</span> <span class="title class_">QueryWrapper</span>&lt;&gt;();</span><br><span class="line">wrapper.eq(<span class="string">&quot;USFL&quot;</span>, <span class="string">&quot;1&quot;</span>).likeRight(<span class="string">&quot;ADDVCD&quot;</span>, effectiveAddvcd);</span><br><span class="line"><span class="comment">// 生成 SQL: WHERE USFL = &#x27;1&#x27; AND ADDVCD LIKE &#x27;4401%&#x27;</span></span><br></pre></td></tr></table></figure>

<p>新建了 <code>StStbprpB</code> Entity 和 <code>StStbprpBMapper</code>，Mapper 上用 <code>@DS(&quot;water&quot;)</code> 注解切换到 water 数据源。</p>
<h3 id="Step-3-设备列表——精确查询"><a href="#Step-3-设备列表——精确查询" class="headerlink" title="Step 3: 设备列表——精确查询"></a>Step 3: 设备列表——精确查询</h3><p>设备表在 gdsw 主库，按测站编码精确匹配：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">QueryWrapper&lt;TreeEquipment&gt; wrapper = <span class="keyword">new</span> <span class="title class_">QueryWrapper</span>&lt;&gt;();</span><br><span class="line">wrapper.eq(<span class="string">&quot;STCD&quot;</span>, stcd);</span><br><span class="line">List&lt;TreeEquipment&gt; equipments = treeEquipmentMapper.selectList(wrapper);</span><br></pre></td></tr></table></figure>

<h3 id="Step-4-流域树——平铺转树形"><a href="#Step-4-流域树——平铺转树形" class="headerlink" title="Step 4: 流域树——平铺转树形"></a>Step 4: 流域树——平铺转树形</h3><p>新增 <code>/riverBasinTree</code> 接口，从第三方 API 获取平铺流域数据，通过 <code>RiverBasinTreeUtil</code> 工具类构建树形结构：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 四阶段算法：建索引 → 挂父子 → 排序 → 计数</span></span><br><span class="line">Map&lt;String, TreeNode&gt; nodeMap = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;();</span><br><span class="line"><span class="comment">// 1. 以 RVCD1 为主键建立节点 Map</span></span><br><span class="line"><span class="keyword">for</span> (RiverBasin item : flatList) &#123;</span><br><span class="line">    nodeMap.put(item.getRvcd1(), toNode(item));</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 2. 通过 PARENT_CODE 挂到父节点</span></span><br><span class="line"><span class="keyword">for</span> (TreeNode node : nodeMap.values()) &#123;</span><br><span class="line">    <span class="type">TreeNode</span> <span class="variable">parent</span> <span class="operator">=</span> nodeMap.get(node.getParentCode());</span><br><span class="line">    <span class="keyword">if</span> (parent != <span class="literal">null</span>) &#123;</span><br><span class="line">        parent.getChildren().add(node);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="多数据源配置"><a href="#多数据源配置" class="headerlink" title="多数据源配置"></a>多数据源配置</h2><p><img data-src="/images/posts/%E6%A0%91%E7%BB%93%E6%9E%84%E9%80%89%E6%8B%A9%E5%99%A8-%E4%BB%8ERedis%E5%88%B0%E7%9B%B4%E8%BF%9E%E6%95%B0%E6%8D%AE%E5%BA%93/fig2.png" alt="多数据源架构图"><br><em>图2：多数据源配置架构，master 和 water 数据源切换</em></p>
<p>项目使用了 <code>dynamic-datasource</code>（Baomidou 的多数据源框架），新增 water 数据源只需在配置文件中添加：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">datasource:</span></span><br><span class="line">    <span class="attr">dynamic:</span></span><br><span class="line">      <span class="attr">datasource:</span></span><br><span class="line">        <span class="attr">master:</span></span><br><span class="line">          <span class="attr">url:</span> <span class="string">jdbc:mysql://127.0.0.1:3306/gdsw</span></span><br><span class="line">          <span class="attr">username:</span> <span class="string">root</span></span><br><span class="line">          <span class="attr">password:</span> <span class="string">root</span></span><br><span class="line">        <span class="attr">water:</span></span><br><span class="line">          <span class="attr">url:</span> <span class="string">jdbc:mysql://19.25.36.199:15076/ysq</span></span><br><span class="line">          <span class="attr">username:</span> <span class="string">sa</span></span><br><span class="line">          <span class="attr">password:</span> <span class="string">&quot;&#125;&amp;S^!&quot;</span></span><br></pre></td></tr></table></figure>

<h2 id="踩坑记录"><a href="#踩坑记录" class="headerlink" title="踩坑记录"></a>踩坑记录</h2><h3 id="Bean-名冲突"><a href="#Bean-名冲突" class="headerlink" title="Bean 名冲突"></a>Bean 名冲突</h3><p>algorithm-plugin-starter 模块已有 <code>EquipmentBMapper</code>，新建同名 Mapper 导致 <code>ConflictingBeanDefinitionException</code>。解决方案：改名为 <code>TreeEquipmentMapper</code>。</p>
<p><strong>教训</strong>：多模块项目里新建 Mapper 之前，先全局搜索是否已有同表 Mapper。</p>
<h3 id="认证头类型错误"><a href="#认证头类型错误" class="headerlink" title="认证头类型错误"></a>认证头类型错误</h3><p>流域树接口最初复用了行政区树的 <code>generateGatewayHeaders()</code> 方法，但实际需要 <code>X-Auth-Data</code> + <code>X-Auth-Appid</code> 的 RSA 加密认证头。两种认证机制完全不同，不能混用。</p>
<p><strong>教训</strong>：调用第三方 API 时，不能假设所有接口使用相同的认证方式。</p>
<h3 id="特殊字符密码"><a href="#特殊字符密码" class="headerlink" title="特殊字符密码"></a>特殊字符密码</h3><p>water 数据源密码含特殊字符（<code>&#125;&amp;S^!</code>），在配置文件里需要加引号保护。</p>
<h2 id="接口变更总览"><a href="#接口变更总览" class="headerlink" title="接口变更总览"></a>接口变更总览</h2><table>
<thead>
<tr>
<th>接口</th>
<th>路径</th>
<th>改造前</th>
<th>改造后</th>
</tr>
</thead>
<tbody><tr>
<td>行政区树(缓存)</td>
<td><code>GET /regionTree</code></td>
<td>Redis</td>
<td>保持不动</td>
</tr>
<tr>
<td>行政区树(直连)</td>
<td><code>GET /regionTreeFromApi</code></td>
<td>不存在</td>
<td>新增</td>
</tr>
<tr>
<td>测站列表</td>
<td><code>GET /stationTree?addvcd=xxx</code></td>
<td>Redis 全量解压</td>
<td>QueryWrapper 查 water 库</td>
</tr>
<tr>
<td>设备列表</td>
<td><code>GET /equipmentTree?stcd=xxx</code></td>
<td>Redis 全量解压</td>
<td>QueryWrapper 查 gdsw 库</td>
</tr>
<tr>
<td>流域树</td>
<td><code>GET /riverBasinTree</code></td>
<td>不存在</td>
<td>新增</td>
</tr>
</tbody></table>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><p>这次重构的核心收获是<strong>架构决策要随着系统演进重新评估</strong>。项目初期的”全量缓存到 Redis”方案确实是最快跑通的选择，但半年后回头看，数据源本身就在数据库里，中间加一层 Redis 反而增加了复杂度。</p>
<p>关键要点：</p>
<ul>
<li>当发现自己在为缓存维护同步任务、处理过期问题、mock 本地数据时，就该想想是否可以”跳过中间层”</li>
<li>多数据源项目新建 Mapper 前先全局搜索，避免 Bean 名冲突</li>
<li>调用第三方 API 前务必确认实际认证要求，不能凭经验假设</li>
<li>特殊字符密码在配置文件中要加引号保护</li>
</ul>
<hr>
<p><em>本文涉及代码已提交到 <code>master-b_sp_20251216-delDec</code> 分支。</em></p>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>数据归集</category>
      </categories>
      <tags>
        <tag>Spring Boot</tag>
        <tag>多数据源</tag>
        <tag>MyBatis-Plus</tag>
        <tag>Redis</tag>
        <tag>架构重构</tag>
      </tags>
  </entry>
  <entry>
    <title>氚云访客历史同步漏数据？分页方式、日期语义、头像容错三处全改</title>
    <url>/2026/07/14/%E6%B0%9A%E4%BA%91%E8%AE%BF%E5%AE%A2%E5%8E%86%E5%8F%B2%E5%90%8C%E6%AD%A5%E4%BF%AE%E5%A4%8D/</url>
    <content><![CDATA[<p>氚云访客同步跑了几个月，某天业务反馈”刘树明这条访客记录根本没入库”。查代码、查日志、查公网接口——数据其实是有的，但被同步逻辑漏掉了。</p>
<p>排查下来发现这不是一个 bug，而是<strong>三个 bug 叠加</strong>：分页方式有缺陷、日期语义理解错了、头像上传失败会阻断主数据入库。任何一个 bug 单拿出来都不会让数据丢失，三个叠在一起就稳定漏数据。</p>
<p>本文把三个根因、四个修复点、服务器验证步骤完整拆开，让你下次遇到第三方同步漏数据时能直接套用这套排查思路。</p>
<span id="more"></span>

<div class="audio-player">
  <div class="audio-section">
    <h4>🎧 文章导读</h4>
    <audio controls preload="metadata">
      <source src="/audio/氚云访客历史同步修复/narration.mp3" type="audio/mpeg">
    </audio>
  </div>
  <div class="audio-section">
    <h4>🎵 背景音乐</h4>
    <audio controls loop preload="metadata">
      <source src="/audio/氚云访客历史同步修复/bgm.mp3" type="audio/mpeg">
    </audio>
  </div>
</div>

<p><img data-src="/images/posts/%E6%B0%9A%E4%BA%91%E8%AE%BF%E5%AE%A2%E5%8E%86%E5%8F%B2%E5%90%8C%E6%AD%A5%E4%BF%AE%E5%A4%8D/fig1.png" alt="氚云访客同步的三层漏数据问题"></p>
<p><em>图1：分页方式、日期语义、头像容错——三层叠在一起才让访客漏入库</em></p>
<h2 id="一、问题现象：业务反馈某访客”凭空消失”"><a href="#一、问题现象：业务反馈某访客”凭空消失”" class="headerlink" title="一、问题现象：业务反馈某访客”凭空消失”"></a>一、问题现象：业务反馈某访客”凭空消失”</h2><p>业务方反馈：氚云上有”刘树明”这条访客预约记录，但在 through 库 <code>visitor_appointment</code> 表里查不到。</p>
<p>排查的过程是这样：</p>
<ol>
<li>先查同步日志，发现刘树明的同步日志只在”已存在，跳过”那一种结果中出现——这说明同步任务跑过这条数据，但当时判定为已存在。</li>
<li>反查 <code>visitor_appointment</code> 表，确实没有 <code>peo_id = &#39;79a8ec3d-0fa2-4652-9faa-51a0fce6dfef&#39;</code> 的记录。</li>
<li>那”已存在”判定的依据是什么？查代码发现是按 <code>peo_id</code> 查的，可新数据根本没这个 <code>peo_id</code>，于是出现”查到已存在但实际不存在”的诡异状态。</li>
</ol>
<p>继续往上游查，发现了三个独立但叠加的 bug。</p>
<h2 id="二、根因-1：历史同步分页方式有缺陷"><a href="#二、根因-1：历史同步分页方式有缺陷" class="headerlink" title="二、根因 1：历史同步分页方式有缺陷"></a>二、根因 1：历史同步分页方式有缺陷</h2><h3 id="2-1-旧逻辑的问题"><a href="#2-1-旧逻辑的问题" class="headerlink" title="2.1 旧逻辑的问题"></a>2.1 旧逻辑的问题</h3><p>旧实现是”先取 objectId，再按 objectId 处理”的两阶段流程：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">阶段一：分页拉取所有 objectId</span><br><span class="line">阶段二：再按 objectId 拉详情或处理</span><br></pre></td></tr></table></figure>

<p>这个流程有两个隐患：</p>
<ol>
<li><strong>阶段一和阶段二之间存在数据漂移</strong>：如果分页中途有新数据进入，<code>objectId</code> 集合可能漏或重复。</li>
<li><strong>阶段二失败时没有断点保护</strong>：一旦失败，已经处理过的 <code>objectId</code> 和未处理的混在一起，重试可能跳过或重复。</li>
</ol>
<h3 id="2-2-修复方案"><a href="#2-2-修复方案" class="headerlink" title="2.2 修复方案"></a>2.2 修复方案</h3><p>改为单阶段分页，直接按氚云分页响应逐页同步：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">每页响应 → 同步该页 → 推进断点 → 拉下一页</span><br></pre></td></tr></table></figure>

<p>每页内的 <code>objectId</code> 重复只同步一次；某页失败立即停止，且不推进 Redis 断点——避免失败数据被静默跳过。</p>
<blockquote>
<p>单阶段分页的好处是”所见即所得”，分页中途的数据漂移窗口被关掉了一半。</p>
</blockquote>
<h2 id="三、根因-2：历史补跑日期语义错误"><a href="#三、根因-2：历史补跑日期语义错误" class="headerlink" title="三、根因 2：历史补跑日期语义错误"></a>三、根因 2：历史补跑日期语义错误</h2><h3 id="3-1-旧代码理解错了什么"><a href="#3-1-旧代码理解错了什么" class="headerlink" title="3.1 旧代码理解错了什么"></a>3.1 旧代码理解错了什么</h3><p>氚云请求中的 <code>startTime/endTime</code> 字段，<strong>更接近表单创建或提交日期</strong>，并不等于返回记录中的”访客来访开始时间”。</p>
<p>这个差异在大部分场景下看不出来——如果表单创建日期和来访日期一致，一切正常。但刘树明这条记录刚好横跨月底：</p>
<table>
<thead>
<tr>
<th>字段</th>
<th>值</th>
</tr>
</thead>
<tbody><tr>
<td>氚云查询日期归属</td>
<td><code>2026-06-30</code></td>
</tr>
<tr>
<td>返回 <code>startTime</code></td>
<td><code>2026-07-01 00:00:00</code></td>
</tr>
<tr>
<td>返回 <code>endTime</code></td>
<td><code>2026-09-30 00:00:00</code></td>
</tr>
</tbody></table>
<p>如果按”查询日期 <code>2026-06-30</code>“补跑，旧代码会用 <code>2026-06-30</code> 作为 <code>apply_date</code> 入库——查询日期是创建表单的日期，不是访客实际来访日期。</p>
<h3 id="3-2-修复方案"><a href="#3-2-修复方案" class="headerlink" title="3.2 修复方案"></a>3.2 修复方案</h3><ul>
<li><strong>历史任务按”查询日期”翻页</strong>——保持和定时任务一致；</li>
<li><strong>入库 <code>apply_date/begin_date/end_date</code> 按响应中的来访起止时间计算</strong>——这才是业务的真实日期。</li>
</ul>
<p>修复后刘树明这条记录的入库结果是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">visitor_name: 刘树明</span><br><span class="line">source: 3</span><br><span class="line">peo_id: 79a8ec3d-0fa2-4652-9faa-51a0fce6dfef</span><br><span class="line">apply_date: 2026-07-01 00:00:00</span><br><span class="line">begin_date: 2026-07-01 00:00:00</span><br><span class="line">end_date: 2026-09-30 00:00:00</span><br></pre></td></tr></table></figure>

<p><code>source = 3</code> 是 SOURCE_3（氚云），<code>apply_date</code> 用了响应的 <code>startTime</code>，符合业务预期。</p>
<blockquote>
<p><strong>同步任务的日期语义错位是定时任务最容易踩的坑</strong>。第三方接口的”日期字段”通常是请求参数语义，不一定是响应数据语义。</p>
</blockquote>
<h2 id="四、根因-3：头像上传失败阻断主数据入库"><a href="#四、根因-3：头像上传失败阻断主数据入库" class="headerlink" title="四、根因 3：头像上传失败阻断主数据入库"></a>四、根因 3：头像上传失败阻断主数据入库</h2><h3 id="4-1-旧逻辑的问题"><a href="#4-1-旧逻辑的问题" class="headerlink" title="4.1 旧逻辑的问题"></a>4.1 旧逻辑的问题</h3><p>公网氚云接口响应里包含较大的头像 Base64，<code>size=100</code> 在本地公网链路容易触发 Feign 读取超时。同时，本地 <code>aided-service</code>（头像上传服务）未启动时，旧逻辑会因头像上传失败而<strong>丢弃整条访客数据</strong>。</p>
<p>也就是说：头像上传失败 → 整条访客主数据不入库。这违反了”附属信息失败不应该阻断主数据”的原则。</p>
<h3 id="4-2-修复方案"><a href="#4-2-修复方案" class="headerlink" title="4.2 修复方案"></a>4.2 修复方案</h3><p>头像上传失败时只记录 WARN，主数据继续保存：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    uploadAvatar(avatarBase64);</span><br><span class="line">&#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">    log.warn(<span class="string">&quot;头像上传失败，继续保存访客主数据: &#123;&#125;&quot;</span>, e.getMessage());</span><br><span class="line">    <span class="comment">// 继续执行下面的主数据保存逻辑</span></span><br><span class="line">&#125;</span><br><span class="line">saveVisitorMainData(...);</span><br></pre></td></tr></table></figure>

<blockquote>
<p><strong>附属信息和主数据的关系</strong>：头像、缩略图、附件都属于附属信息，不应阻断主数据的保存。这是容错设计里最基本的原则——<strong>核心业务数据优先于附属数据</strong>。</p>
</blockquote>
<h2 id="五、本次修复的四个代码点"><a href="#五、本次修复的四个代码点" class="headerlink" title="五、本次修复的四个代码点"></a>五、本次修复的四个代码点</h2><p><img data-src="/images/posts/%E6%B0%9A%E4%BA%91%E8%AE%BF%E5%AE%A2%E5%8E%86%E5%8F%B2%E5%90%8C%E6%AD%A5%E4%BF%AE%E5%A4%8D/fig2.png" alt="修复涉及的代码模块与调用关系"></p>
<p><em>图2：SyncController + VisitingAircraftSyncServiceImpl + VisitorAppointmentServiceImpl 三处协同修复</em></p>
<h3 id="5-1-SyncController"><a href="#5-1-SyncController" class="headerlink" title="5.1 SyncController"></a>5.1 SyncController</h3><ul>
<li>历史同步改为单阶段分页，默认每页 100 条。</li>
<li><code>/sync/triggerHistorySync</code> 增加可选参数：<code>startDate</code>、<code>endDate</code>、<code>pageSize</code>。</li>
<li><code>pageSize</code> 限制为 1～100；不传时仍为 100，不影响原定时任务。</li>
<li>同一页出现重复 <code>objectId</code> 时只同步一次。</li>
<li>当某日出现失败时立即停止，并且<strong>不推进 Redis 断点</strong>，避免失败数据被静默跳过。</li>
<li>修正本地 through 端口注释为 <code>18134</code>。</li>
</ul>
<h3 id="5-2-VisitingAircraftSyncServiceImpl"><a href="#5-2-VisitingAircraftSyncServiceImpl" class="headerlink" title="5.2 VisitingAircraftSyncServiceImpl"></a>5.2 VisitingAircraftSyncServiceImpl</h3><ul>
<li><code>applyDate</code> 优先使用氚云响应中的 <code>startTime</code>。</li>
<li>时间解析失败时，回退日期使用本次补跑日期，不再错误使用服务器当天日期。</li>
<li>保留原有起止时间相等时拉开为 <code>08:00-18:00</code> 的逻辑。</li>
<li>去掉重复查库去重。</li>
<li><strong>头像上传失败时继续保存访客主数据</strong>。</li>
</ul>
<h3 id="5-3-VisitorAppointmentServiceImpl"><a href="#5-3-VisitorAppointmentServiceImpl" class="headerlink" title="5.3 VisitorAppointmentServiceImpl"></a>5.3 VisitorAppointmentServiceImpl</h3><ul>
<li>仅 <code>SOURCE_3</code>（氚云）保留上游传入的历史 <code>applyDate</code>。</li>
<li>其他来源继续保持旧行为：<code>applyDate</code> 使用当前入库时间，避免影响访客机等现有业务。</li>
</ul>
<blockquote>
<p><strong>为什么只对 SOURCE_3 保留历史 applyDate</strong>？因为只有氚云这条链路是”补跑历史数据”的场景，其他来源都是实时同步，用当前入库时间更合适。<strong>容错规则要按业务场景分别设计，不能一刀切</strong>。</p>
</blockquote>
<h2 id="六、本地验证结果"><a href="#六、本地验证结果" class="headerlink" title="六、本地验证结果"></a>六、本地验证结果</h2><h3 id="6-1-自动化测试"><a href="#6-1-自动化测试" class="headerlink" title="6.1 自动化测试"></a>6.1 自动化测试</h3><ul>
<li>共执行 11 个测试。</li>
<li>结果：<code>Failures: 0, Errors: 0</code>。</li>
<li>覆盖：历史日期语义、默认日期、单阶段分页、同页去重、失败断点、成功断点、分页大小校验、头像失败继续入库。</li>
</ul>
<h3 id="6-2-构建"><a href="#6-2-构建" class="headerlink" title="6.2 构建"></a>6.2 构建</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mvn -pl service-provider/through-service -am package -DskipTests</span><br><span class="line"><span class="comment"># BUILD SUCCESS</span></span><br></pre></td></tr></table></figure>

<p>产物：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">E:\nanwang\smart-park-cloud-0617\service-provider\through-service\target\through-service-1.0.0-SNAPSHOT.jar</span><br></pre></td></tr></table></figure>

<h3 id="6-3-真实接口和数据库验证"><a href="#6-3-真实接口和数据库验证" class="headerlink" title="6.3 真实接口和数据库验证"></a>6.3 真实接口和数据库验证</h3><p>公网氚云接口已查到刘树明：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">objectId: 79a8ec3d-0fa2-4652-9faa-51a0fce6dfef</span><br><span class="line">name: 刘树明</span><br><span class="line">startTime: 2026-07-01 00:00:00</span><br><span class="line">endTime: 2026-09-30 00:00:00</span><br></pre></td></tr></table></figure>

<p>本地补跑 <code>2026-06-30</code> 后数据库结果：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">visitor_name: 刘树明</span><br><span class="line">source: 3</span><br><span class="line">peo_id: 79a8ec3d-0fa2-4652-9faa-51a0fce6dfef</span><br><span class="line">apply_date: 2026-07-01 00:00:00</span><br><span class="line">begin_date: 2026-07-01 00:00:00</span><br><span class="line">end_date: 2026-09-30 00:00:00</span><br></pre></td></tr></table></figure>

<p>重复检查未发现重复的 <code>SOURCE_3 + peo_id</code> 数据。</p>
<h2 id="七、服务器部署验证"><a href="#七、服务器部署验证" class="headerlink" title="七、服务器部署验证"></a>七、服务器部署验证</h2><h3 id="7-1-部署范围"><a href="#7-1-部署范围" class="headerlink" title="7.1 部署范围"></a>7.1 部署范围</h3><p>只需要部署新打包的 <code>through-service</code>。Nacos 中氚云 <code>host</code> 使用服务器实际可访问的地址；如果内网代理稳定，生产环境无需因为本地公网测试而改成公网地址。</p>
<blockquote>
<p>[!warning] 凭证安全<br><code>EngineSecret</code> 是接口密钥，不要写进日志、截图或知识库。此前已经在聊天和截图中暴露，条件允许时建议在氚云后台轮换。</p>
</blockquote>
<h3 id="7-2-先做单日小范围补跑"><a href="#7-2-先做单日小范围补跑" class="headerlink" title="7.2 先做单日小范围补跑"></a>7.2 先做单日小范围补跑</h3><p>刘树明归属于氚云查询日期 <code>2026-06-30</code>：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -X POST <span class="string">&#x27;http://127.0.0.1:18134/sync/triggerHistorySync?startDate=2026-06-30&amp;endDate=2026-06-30&amp;pageSize=100&#x27;</span></span><br></pre></td></tr></table></figure>

<p>如果生产内网接口仍超时，把 <code>pageSize</code> 临时调小：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -X POST <span class="string">&#x27;http://127.0.0.1:18134/sync/triggerHistorySync?startDate=2026-06-30&amp;endDate=2026-06-30&amp;pageSize=10&#x27;</span></span><br></pre></td></tr></table></figure>

<p>公网链路本地验证时使用 <code>pageSize=1</code> 最稳定，但全历史补跑会很慢。</p>
<h3 id="7-3-观察日志"><a href="#7-3-观察日志" class="headerlink" title="7.3 观察日志"></a>7.3 观察日志</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">tail</span> -Fn0 /data/work/zhyq/logs/through/through-all.log \</span><br><span class="line">  | grep --line-buffered -aE <span class="string">&#x27;同步氚云访客|刘树明|头像上传失败|ERROR|Exception&#x27;</span></span><br></pre></td></tr></table></figure>

<p>预期：</p>
<ul>
<li>当日同步完成且失败数为 0。</li>
<li>aided 不可用时可能出现”头像上传失败，继续保存访客主数据”WARN，但访客仍应入库。</li>
<li>不应因单条失败继续推进该日断点。</li>
</ul>
<h3 id="7-4-查库"><a href="#7-4-查库" class="headerlink" title="7.4 查库"></a>7.4 查库</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> id, visitor_name, source, peo_id, apply_date, begin_date, end_date, create_time</span><br><span class="line"><span class="keyword">FROM</span> visitor_appointment</span><br><span class="line"><span class="keyword">WHERE</span> peo_id <span class="operator">=</span> <span class="string">&#x27;79a8ec3d-0fa2-4652-9faa-51a0fce6dfef&#x27;</span>;</span><br></pre></td></tr></table></figure>

<p>预期：<code>source=3</code>，且三个业务时间与本地验证结果一致。</p>
<h3 id="7-5-再补跑历史数据"><a href="#7-5-再补跑历史数据" class="headerlink" title="7.5 再补跑历史数据"></a>7.5 再补跑历史数据</h3><p>建议按月或按年分段执行，避免单个 HTTP 请求长时间占用 Tomcat 线程。每段完成后检查同步失败数和 Redis 断点，再执行下一段。</p>
<blockquote>
<p>[!warning] 断点注意事项<br>显式补跑较早日期会把共享 Redis 断点更新到该日期。之后无参数执行会从断点下一天继续，并通过数据库去重；部署验证期间不要并发触发多段历史补跑。</p>
</blockquote>
<h2 id="八、经验总结"><a href="#八、经验总结" class="headerlink" title="八、经验总结"></a>八、经验总结</h2><h3 id="8-1-同步漏数据的-5-步排查法"><a href="#8-1-同步漏数据的-5-步排查法" class="headerlink" title="8.1 同步漏数据的 5 步排查法"></a>8.1 同步漏数据的 5 步排查法</h3><ol>
<li><strong>数据到底有没有被拉过？</strong> 看同步任务的处理日志。</li>
<li><strong>如果拉过，”已存在”判定依据是什么？</strong> 查去重 SQL 的查询条件。</li>
<li><strong>如果没拉过，分页是否漏了？</strong> 看分页边界、断点推进、重复 objectId 处理。</li>
<li><strong>日期语义对不对？</strong> 查请求参数日期 vs 响应数据日期。</li>
<li><strong>附属信息失败有没有阻断主数据？</strong> 查 try-catch 范围。</li>
</ol>
<p>按这个顺序排查，大部分同步漏数据问题都能在 1 小时内定位。</p>
<h3 id="8-2-同步任务的-4-个不可妥协点"><a href="#8-2-同步任务的-4-个不可妥协点" class="headerlink" title="8.2 同步任务的 4 个不可妥协点"></a>8.2 同步任务的 4 个不可妥协点</h3><table>
<thead>
<tr>
<th>不可妥协点</th>
<th>原因</th>
</tr>
</thead>
<tbody><tr>
<td>失败不推进断点</td>
<td>避免失败数据被静默跳过</td>
</tr>
<tr>
<td>主数据与附属信息分离保存</td>
<td>头像失败不应阻断访客主数据</td>
</tr>
<tr>
<td>日期语义必须验证</td>
<td>请求日期 ≠ 响应日期时不能想当然</td>
</tr>
<tr>
<td>同页去重只一次</td>
<td>objectId 重复时按”一次”处理，避免重复插入</td>
</tr>
</tbody></table>
<h3 id="8-3-补跑历史数据的-3-个最佳实践"><a href="#8-3-补跑历史数据的-3-个最佳实践" class="headerlink" title="8.3 补跑历史数据的 3 个最佳实践"></a>8.3 补跑历史数据的 3 个最佳实践</h3><ol>
<li><strong>单日先验</strong>：先补跑 1 天，确认日志和数据库都符合预期，再补跑更大范围。</li>
<li><strong>分段执行</strong>：按月或按年分段，每段完成后检查失败数和断点，再执行下一段。</li>
<li><strong>不并发补跑</strong>：多个补跑请求会互相覆盖 Redis 断点，导致数据丢失或重复。</li>
</ol>
<h3 id="8-4-同步链路的可观测性建议"><a href="#8-4-同步链路的可观测性建议" class="headerlink" title="8.4 同步链路的可观测性建议"></a>8.4 同步链路的可观测性建议</h3><ul>
<li>每条同步记录的处理路径（拉取 → 去重 → 入库 → 标记状态）应该有明确日志。</li>
<li>失败原因应该分门别类：分页失败 vs 去重失败 vs 入库失败 vs 头像失败。</li>
<li>Redis 断点应该有专门的 metric 或日志，方便运维观察”当前同步到哪里了”。</li>
</ul>
<h2 id="九、结语"><a href="#九、结语" class="headerlink" title="九、结语"></a>九、结语</h2><p>第三方同步漏数据是看起来”很神秘的 bug”——因为日志里有这条数据的处理记录，但数据库里就是没有。这次排查下来，其实就是分页、日期、容错三个老问题叠加在一起。</p>
<p><strong>同步链路的设计原则</strong>：</p>
<ul>
<li>单阶段优于两阶段；</li>
<li>日期语义要按响应数据来；</li>
<li>主数据优先于附属数据；</li>
<li>失败要显式，不要静默跳过。</li>
</ul>
<p>下次再遇到第三方同步漏数据，按这套排查思路走一遍，大概率能在 1 小时内定位。</p>
<blockquote>
<p>同步链路没有银弹，只有把每一层的失败模式都想到、补上对应的处理逻辑。</p>
</blockquote>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>南网</category>
      </categories>
      <tags>
        <tag>南网项目</tag>
        <tag>氚云</tag>
        <tag>访客同步</tag>
        <tag>分页</tag>
        <tag>容错</tag>
      </tags>
  </entry>
  <entry>
    <title>水文数据均值对比查询Bug修复全记录：从问题分析到完整修复</title>
    <url>/2026/05/07/%E6%B0%B4%E6%96%87%E6%95%B0%E6%8D%AE%E5%9D%87%E5%80%BC%E5%AF%B9%E6%AF%94%E6%9F%A5%E8%AF%A2Bug%E4%BF%AE%E5%A4%8D%E5%85%A8%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<h2 id="导读"><a href="#导读" class="headerlink" title="导读"></a>导读</h2><blockquote>
<p>今天我要给大家分享一个在广东水文项目中遇到的实际案例——水文数据均值对比查询功能的Bug修复。这个问题涉及到水文数据库的特殊存储约定、MyBatis XML映射文件的SQL编写、以及Java时间处理逻辑的综合考量。通过这个案例，我将从问题分析、根本原因定位、到最终修复方案的完整过程，希望能给有类似开发经验的朋友一些参考和启发。</p>
</blockquote>
<span id="more"></span>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>在水利信息化系统中，水文数据的统计分析是核心功能之一。今天在项目中遇到了均值对比查询API的异常——MONTH（月均值）和XUN（旬均值）类型的查询返回结果条数错误、时间对齐偏差、STTDRCD值错误等多个问题。这些问题单独看都不复杂，但组合在一起就需要对水文数据的存储机制有深入理解才能彻底解决。</p>
<p>让我先介绍一下背景：广东水文项目使用的是规范的水文数据库，其中ST_RVAV_R表存储着各种统计周期的均值数据，包括日均值、旬均值、月均值等。而问题恰恰出在对这些数据存储约定的理解偏差上。</p>
<h2 id="问题背景与现象"><a href="#问题背景与现象" class="headerlink" title="问题背景与现象"></a>问题背景与现象</h2><h3 id="用户反馈的问题"><a href="#用户反馈的问题" class="headerlink" title="用户反馈的问题"></a>用户反馈的问题</h3><p>用户在调用均值对比查询API（<code>/api/water/comparison/average</code>）时，发现以下异常：</p>
<p><strong>问题1：MONTH类型查询条数错误</strong></p>
<ul>
<li>请求：2026年4月7日到5月7日的月均值数据</li>
<li>期望返回：1条（只有4月份的月均值）</li>
<li>实际返回：2条（4月和5月）</li>
</ul>
<p><strong>问题2：时间对齐错误</strong></p>
<ul>
<li>前端收到的数据时间点显示为00:00:00</li>
<li>但水文数据标准存储时间应该是08:00:00</li>
</ul>
<p><strong>问题3：旬均值时间映射完全错误</strong></p>
<ul>
<li>上旬显示为1日，实际应该是11日</li>
<li>中旬显示为11日，实际应该是21日</li>
<li>下旬显示为21日，实际应该是下月1日</li>
</ul>
<p><strong>问题4：STTDRCD编码错误</strong></p>
<ul>
<li>旬均值查询使用了STTDRCD&#x3D;’3’（候均值，5天），而非’4’（旬均值）</li>
<li>月均值查询使用了STTDRCD&#x3D;’4’，而非’5’</li>
</ul>
<h3 id="影响范围评估"><a href="#影响范围评估" class="headerlink" title="影响范围评估"></a>影响范围评估</h3><table>
<thead>
<tr>
<th>功能</th>
<th>状态</th>
<th>影响</th>
</tr>
</thead>
<tbody><tr>
<td>日均值对比查询</td>
<td>可用</td>
<td>无</td>
</tr>
<tr>
<td>月均值对比查询</td>
<td>不可用</td>
<td>高</td>
</tr>
<tr>
<td>旬均值对比查询</td>
<td>不可用</td>
<td>高</td>
</tr>
<tr>
<td>月&#x2F;旬历史对比</td>
<td>不可用</td>
<td>高</td>
</tr>
</tbody></table>
<h2 id="水文数据库核心知识"><a href="#水文数据库核心知识" class="headerlink" title="水文数据库核心知识"></a>水文数据库核心知识</h2><p><img data-src="/images/posts/%E6%B0%B4%E6%96%87%E6%95%B0%E6%8D%AE%E5%9D%87%E5%80%BC%E5%AF%B9%E6%AF%94%E6%9F%A5%E8%AF%A2Bug%E4%BF%AE%E5%A4%8D%E5%85%A8%E8%AE%B0%E5%BD%95/fig1.png" alt="ST_RVAV_R存储约定说明图"><br><em>图1：ST_RVAV_R表的数据存储时间点规则</em></p>
<p>在开始修复之前，我们必须先理解ST_RVAV_R表的存储约定。这是水文行业数据库的标准规范：</p>
<h3 id="STTDRCD编码定义"><a href="#STTDRCD编码定义" class="headerlink" title="STTDRCD编码定义"></a>STTDRCD编码定义</h3><table>
<thead>
<tr>
<th>统计类型</th>
<th>STTDRCD值</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>日均值</td>
<td>‘1’</td>
<td>每天08:00作为水文日分界</td>
</tr>
<tr>
<td>候均值</td>
<td>‘3’</td>
<td>每5天一个统计周期</td>
</tr>
<tr>
<td>旬均值</td>
<td>‘4’</td>
<td>上中下旬各一个统计周期</td>
</tr>
<tr>
<td>月均值</td>
<td>‘5’</td>
<td>每月一个统计周期</td>
</tr>
</tbody></table>
<h3 id="数据存储时间点规则"><a href="#数据存储时间点规则" class="headerlink" title="数据存储时间点规则"></a>数据存储时间点规则</h3><p><strong>关键理解</strong>：水文数据的时间点是”代表时段”而非”自然日期”。</p>
<table>
<thead>
<tr>
<th>统计类型</th>
<th>存储时间点</th>
<th>示例</th>
</tr>
</thead>
<tbody><tr>
<td>日均值</td>
<td>当天08:00</td>
<td>4月5日均值 → 04-05 08:00</td>
</tr>
<tr>
<td>旬均值（上旬）</td>
<td>11日08:00</td>
<td>上旬结束于10日，11日08:00出数</td>
</tr>
<tr>
<td>旬均值（中旬）</td>
<td>21日08:00</td>
<td>中旬结束于20日，21日08:00出数</td>
</tr>
<tr>
<td>旬均值（下旬）</td>
<td>下月1日08:00</td>
<td>下旬结束于月末，下月1日08:00出数</td>
</tr>
<tr>
<td>月均值</td>
<td>下月1日08:00</td>
<td>4月均值 → 05-01 08:00</td>
</tr>
</tbody></table>
<h3 id="预计算表结构"><a href="#预计算表结构" class="headerlink" title="预计算表结构"></a>预计算表结构</h3><p>st_river_multi_year_avg表存储多年月均值预计算数据：</p>
<ul>
<li><code>period_type=&#39;MONTH&#39;</code></li>
<li><code>month</code>列代表实际月份（如month&#x3D;4表示4月）</li>
<li>查询时需要将month转换为ST_RVAV_R对应的时间点</li>
</ul>
<h2 id="问题根因分析"><a href="#问题根因分析" class="headerlink" title="问题根因分析"></a>问题根因分析</h2><h3 id="问题1：MONTH月份范围查询逻辑错误"><a href="#问题1：MONTH月份范围查询逻辑错误" class="headerlink" title="问题1：MONTH月份范围查询逻辑错误"></a>问题1：MONTH月份范围查询逻辑错误</h3><p>原代码使用简单的月份范围查询：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 原逻辑（错误）</span></span><br><span class="line">averageData = riverMultiYearAvgMapper.selectMonthAvgByMonthRange(</span><br><span class="line">    query.getStcd(),</span><br><span class="line">    startDate.getMonthValue(),  <span class="comment">// 4</span></span><br><span class="line">    endDate.getMonthValue()     <span class="comment">// 5</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<p>这种方式会返回4月和5月两条记录，但实际上5月的数据点（06-01 08:00）根本不在请求的时间范围内。</p>
<h3 id="问题2-5：时间对齐与映射规则错误"><a href="#问题2-5：时间对齐与映射规则错误" class="headerlink" title="问题2-5：时间对齐与映射规则错误"></a>问题2-5：时间对齐与映射规则错误</h3><p>原代码存在以下问题：</p>
<ul>
<li>时间使用00:00:00而非水文标准的08:00:00</li>
<li>旬均值映射：上旬→1日（应为11日），中旬→11日（应为21日），下旬→21日（应为下月1日）</li>
<li>月均值映射：month&#x3D;4→4月1日（应为5月1日，因为月均值存储在下月1日）</li>
</ul>
<h3 id="问题6：查询范围未自动扩展"><a href="#问题6：查询范围未自动扩展" class="headerlink" title="问题6：查询范围未自动扩展"></a>问题6：查询范围未自动扩展</h3><p><img data-src="/images/posts/%E6%B0%B4%E6%96%87%E6%95%B0%E6%8D%AE%E5%9D%87%E5%80%BC%E5%AF%B9%E6%AF%94%E6%9F%A5%E8%AF%A2Bug%E4%BF%AE%E5%A4%8D%E5%85%A8%E8%AE%B0%E5%BD%95/fig4.png" alt="双路径查询架构"><br><em>图4：预计算表路径与回退路径的双路径查询架构</em></p>
<p>用户请求4月1日到4月30日的月均值，但4月的月均值存储在5月1日08:00，超出查询范围导致数据遗漏。用户需要手动填下月日期才能正常工作。</p>
<h3 id="问题7：MONTH类型startTime未偏移"><a href="#问题7：MONTH类型startTime未偏移" class="headerlink" title="问题7：MONTH类型startTime未偏移"></a>问题7：MONTH类型startTime未偏移</h3><p>请求startTime&#x3D;2026-01-01 08:00时，12月均值（存储在01-01 08:00）被错误包含进来，因为第一个数据点实际上代表的是上个月的数据。</p>
<h3 id="问题8：循环终止条件错误"><a href="#问题8：循环终止条件错误" class="headerlink" title="问题8：循环终止条件错误"></a>问题8：循环终止条件错误</h3><p>历史均值的月份遍历循环使用endDate作为终止条件，但最后一个代表月的数据点在下月1日，导致遗漏最后一个月份的数据。</p>
<h2 id="完整修复方案"><a href="#完整修复方案" class="headerlink" title="完整修复方案"></a>完整修复方案</h2><h3 id="Step-1：修正STTDRCD值"><a href="#Step-1：修正STTDRCD值" class="headerlink" title="Step 1：修正STTDRCD值"></a>Step 1：修正STTDRCD值</h3><p>文件：<code>WaterAverageDataMapper.xml</code></p>
<p><strong>修改前</strong>：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 旬均值使用了错误的&#x27;3&#x27; --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">&quot;selectXunAverageData&quot;</span> <span class="attr">resultType</span>=<span class="string">&quot;map&quot;</span>&gt;</span></span><br><span class="line">    SELECT ...</span><br><span class="line">    FROM ST_RVAV_R</span><br><span class="line">    WHERE STCD = #&#123;stcd&#125;</span><br><span class="line">      AND STTDRCD = &#x27;3&#x27;  <span class="comment">&lt;!-- 错误：这是候均值 --&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- 月均值使用了错误的&#x27;4&#x27; --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">&quot;selectMonthAverageData&quot;</span> <span class="attr">resultType</span>=<span class="string">&quot;map&quot;</span>&gt;</span></span><br><span class="line">    SELECT ...</span><br><span class="line">    FROM ST_RVAV_R</span><br><span class="line">    WHERE STCD = #&#123;stcd&#125;</span><br><span class="line">      AND STTDRCD = &#x27;4&#x27;  <span class="comment">&lt;!-- 错误：这是旬均值 --&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p><strong>修改后</strong>：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 修正为正确的&#x27;4&#x27; --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">&quot;selectXunAverageData&quot;</span> <span class="attr">resultType</span>=<span class="string">&quot;map&quot;</span>&gt;</span></span><br><span class="line">    SELECT ...</span><br><span class="line">    FROM ST_RVAV_R</span><br><span class="line">    WHERE STCD = #&#123;stcd&#125;</span><br><span class="line">      AND STTDRCD = &#x27;4&#x27;  <span class="comment">&lt;!-- 正确：旬均值 --&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- 修正为正确的&#x27;5&#x27; --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">&quot;selectMonthAverageData&quot;</span> <span class="attr">resultType</span>=<span class="string">&quot;map&quot;</span>&gt;</span></span><br><span class="line">    SELECT ...</span><br><span class="line">    FROM ST_RVAV_R</span><br><span class="line">    WHERE STCD = #&#123;stcd&#125;</span><br><span class="line">      AND STTDRCD = &#x27;5&#x27;  <span class="comment">&lt;!-- 正确：月均值 --&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="Step-2：新增按月份列表查询方法"><a href="#Step-2：新增按月份列表查询方法" class="headerlink" title="Step 2：新增按月份列表查询方法"></a>Step 2：新增按月份列表查询方法</h3><p>为了解决月份范围查询的问题，我新增了<code>selectMonthAvgByMonthList</code>方法：</p>
<p>文件：<code>RiverMultiYearAvgMapper.java</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 根据月份列表查询多年月平均值</span></span><br><span class="line"><span class="comment"> * </span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> stcd 测站代码</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> months 月份列表（如[4, 5, 6]代表4月、5月、6月）</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@return</span> 多年月平均值列表</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">List&lt;Map&lt;String, Object&gt;&gt; <span class="title function_">selectMonthAvgByMonthList</span><span class="params">(</span></span><br><span class="line"><span class="params">    <span class="meta">@Param(&quot;stcd&quot;)</span> String stcd,</span></span><br><span class="line"><span class="params">    <span class="meta">@Param(&quot;months&quot;)</span> List&lt;Integer&gt; months</span></span><br><span class="line"><span class="params">)</span>;</span><br></pre></td></tr></table></figure>

<p>文件：<code>RiverMultiYearAvgMapper.xml</code></p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 根据月份列表查询多年月平均值 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">&quot;selectMonthAvgByMonthList&quot;</span> <span class="attr">resultType</span>=<span class="string">&quot;map&quot;</span>&gt;</span></span><br><span class="line">    SELECT</span><br><span class="line">        month,</span><br><span class="line">        avg_water_level as water_level,</span><br><span class="line">        avg_flow as flow,</span><br><span class="line">        start_year,</span><br><span class="line">        end_year,</span><br><span class="line">        sample_count,</span><br><span class="line">        data_count</span><br><span class="line">    FROM st_river_multi_year_avg</span><br><span class="line">    WHERE stcd = #&#123;stcd&#125;</span><br><span class="line">      AND period_type = &#x27;MONTH&#x27;</span><br><span class="line">      AND month IN</span><br><span class="line">      <span class="tag">&lt;<span class="name">foreach</span> <span class="attr">collection</span>=<span class="string">&quot;months&quot;</span> <span class="attr">item</span>=<span class="string">&quot;month&quot;</span> <span class="attr">open</span>=<span class="string">&quot;(&quot;</span> <span class="attr">separator</span>=<span class="string">&quot;,&quot;</span> <span class="attr">close</span>=<span class="string">&quot;)&quot;</span>&gt;</span></span><br><span class="line">          #&#123;month&#125;</span><br><span class="line">      <span class="tag">&lt;/<span class="name">foreach</span>&gt;</span></span><br><span class="line">    ORDER BY month</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="Step-3：自动调整查询范围"><a href="#Step-3：自动调整查询范围" class="headerlink" title="Step 3：自动调整查询范围"></a>Step 3：自动调整查询范围</h3><p>这是修复的核心部分——需要根据数据点的存储规则自动调整查询范围：</p>
<p>文件：<code>WaterComparisonServiceImpl.java</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 旬/月均值存储在下一旬/月的第一天（如4月下旬存在5月1日08:00），</span></span><br><span class="line"><span class="comment">// 需要自动调整查询范围：startTime跳过代表上个月的数据点，endTime扩展以捕获最后一个时段</span></span><br><span class="line"><span class="type">Date</span> <span class="variable">currentDataStart</span> <span class="operator">=</span> currentStart;</span><br><span class="line"><span class="type">Date</span> <span class="variable">currentDataEnd</span> <span class="operator">=</span> currentEnd;</span><br><span class="line"><span class="keyword">if</span> (<span class="string">&quot;MONTH&quot;</span>.equalsIgnoreCase(query.getAvgPeriod())) &#123;</span><br><span class="line">    <span class="type">LocalDate</span> <span class="variable">sd</span> <span class="operator">=</span> startDateTime.toLocalDate();</span><br><span class="line">    <span class="comment">// 第一个代表startDate所在月份的数据点，位于下月1日08:00</span></span><br><span class="line">    <span class="type">LocalDate</span> <span class="variable">adjustedStart</span> <span class="operator">=</span> sd.withDayOfMonth(<span class="number">1</span>).plusMonths(<span class="number">1</span>);</span><br><span class="line">    currentDataStart = Date.from(adjustedStart.atTime(<span class="number">8</span>, <span class="number">0</span>).atZone(ZoneId.systemDefault()).toInstant());</span><br><span class="line">    </span><br><span class="line">    <span class="type">LocalDate</span> <span class="variable">extendedEnd</span> <span class="operator">=</span> endDateTime.toLocalDate().plusMonths(<span class="number">1</span>).withDayOfMonth(<span class="number">2</span>);</span><br><span class="line">    currentDataEnd = Date.from(extendedEnd.atStartOfDay(ZoneId.systemDefault()).toInstant());</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="string">&quot;XUN&quot;</span>.equalsIgnoreCase(query.getAvgPeriod())) &#123;</span><br><span class="line">    <span class="type">LocalDate</span> <span class="variable">sd</span> <span class="operator">=</span> startDateTime.toLocalDate();</span><br><span class="line">    <span class="comment">// 跳过代表上个月下旬的数据点（当月1日08:00）</span></span><br><span class="line">    <span class="keyword">if</span> (sd.getDayOfMonth() &lt;= <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="type">LocalDate</span> <span class="variable">adjustedStart</span> <span class="operator">=</span> sd.withDayOfMonth(<span class="number">2</span>);</span><br><span class="line">        currentDataStart = Date.from(adjustedStart.atTime(<span class="number">0</span>, <span class="number">0</span>).atZone(ZoneId.systemDefault()).toInstant());</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">LocalDate</span> <span class="variable">extendedEnd</span> <span class="operator">=</span> endDateTime.toLocalDate().plusMonths(<span class="number">1</span>).withDayOfMonth(<span class="number">2</span>);</span><br><span class="line">    currentDataEnd = Date.from(extendedEnd.atStartOfDay(ZoneId.systemDefault()).toInstant());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>逻辑说明</strong>：</p>
<ul>
<li>MONTH的startTime必须偏移到下月1日08:00，否则会把上月的月均值错误包含进来</li>
<li>MONTH的endTime需要扩展到下月2日，才能捕获最后一个代表月份的数据点</li>
<li>XUN需要在月初跳过代表上月下旬的数据点</li>
</ul>
<h3 id="Step-4：MONTH预计算查询改为数据点遍历"><a href="#Step-4：MONTH预计算查询改为数据点遍历" class="headerlink" title="Step 4：MONTH预计算查询改为数据点遍历"></a>Step 4：MONTH预计算查询改为数据点遍历</h3><p><img data-src="/images/posts/%E6%B0%B4%E6%96%87%E6%95%B0%E6%8D%AE%E5%9D%87%E5%80%BC%E5%AF%B9%E6%AF%94%E6%9F%A5%E8%AF%A2Bug%E4%BF%AE%E5%A4%8D%E5%85%A8%E8%AE%B0%E5%BD%95/fig3.png" alt="月份数据点遍历逻辑流程图"><br><em>图3：数据点遍历逻辑——4月7日到5月7日只返回4月</em></p>
<p>原逻辑直接提取月份范围，新逻辑改为按数据点遍历：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="string">&quot;MONTH&quot;</span>.equalsIgnoreCase(avgPeriod)) &#123;</span><br><span class="line">    Set&lt;Integer&gt; monthsToQuery = <span class="keyword">new</span> <span class="title class_">HashSet</span>&lt;&gt;();</span><br><span class="line">    <span class="type">LocalDate</span> <span class="variable">firstDataPoint</span> <span class="operator">=</span> startDate.withDayOfMonth(<span class="number">1</span>).plusMonths(<span class="number">1</span>);</span><br><span class="line">    <span class="type">LocalDate</span> <span class="variable">loopEnd</span> <span class="operator">=</span> endDate.withDayOfMonth(<span class="number">1</span>).plusMonths(<span class="number">1</span>);</span><br><span class="line">    <span class="type">LocalDate</span> <span class="variable">dataPoint</span> <span class="operator">=</span> firstDataPoint;</span><br><span class="line">    <span class="keyword">while</span> (!dataPoint.isAfter(loopEnd)) &#123;</span><br><span class="line">        <span class="type">int</span> <span class="variable">representMonth</span> <span class="operator">=</span> dataPoint.minusMonths(<span class="number">1</span>).getMonthValue();</span><br><span class="line">        monthsToQuery.add(representMonth);</span><br><span class="line">        dataPoint = dataPoint.plusMonths(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (!monthsToQuery.isEmpty()) &#123;</span><br><span class="line">        averageData = riverMultiYearAvgMapper.selectMonthAvgByMonthList(</span><br><span class="line">            query.getStcd(),</span><br><span class="line">            <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;(monthsToQuery)</span><br><span class="line">        );</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        averageData = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p><strong>关键逻辑</strong>：</p>
<ul>
<li>计算第一个数据点：<code>startDate.withDayOfMonth(1).plusMonths(1)</code></li>
<li>遍历所有数据点，每个数据点代表前一个月份</li>
<li>按代表月份列表查询预计算表</li>
</ul>
<p>例如请求4月7日~5月7日：</p>
<ul>
<li>数据点：5月1日 → 代表4月</li>
<li>6月1日 &gt; loopEnd(6月1日&#x3D;) → 不包含</li>
<li>结果：只有4月</li>
</ul>
<h3 id="Step-5：时间映射修正"><a href="#Step-5：时间映射修正" class="headerlink" title="Step 5：时间映射修正"></a>Step 5：时间映射修正</h3><p><img data-src="/images/posts/%E6%B0%B4%E6%96%87%E6%95%B0%E6%8D%AE%E5%9D%87%E5%80%BC%E5%AF%B9%E6%AF%94%E6%9F%A5%E8%AF%A2Bug%E4%BF%AE%E5%A4%8D%E5%85%A8%E8%AE%B0%E5%BD%95/fig2.png" alt="时间映射修正对比图"><br><em>图2：修正前与修正后的时间映射规则对比</em></p>
<p>convertPreCalcDataToTimeSeries方法的时间映射：</p>
<p><strong>修改前</strong>：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (<span class="string">&quot;DAY&quot;</span>.equalsIgnoreCase(periodType) || <span class="string">&quot;DAY1&quot;</span>.equalsIgnoreCase(periodType)) &#123;</span><br><span class="line">    <span class="type">int</span> <span class="variable">day</span> <span class="operator">=</span> ((Number) row.get(<span class="string">&quot;day&quot;</span>)).intValue();</span><br><span class="line">    time = LocalDateTime.of(queryStartDate.getYear(), month, day, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="string">&quot;XUN&quot;</span>.equalsIgnoreCase(periodType)) &#123;</span><br><span class="line">    <span class="type">int</span> <span class="variable">xun</span> <span class="operator">=</span> ((Number) row.get(<span class="string">&quot;xun&quot;</span>)).intValue();</span><br><span class="line">    <span class="type">int</span> <span class="variable">day</span> <span class="operator">=</span> (xun == <span class="number">1</span>) ? <span class="number">1</span> : (xun == <span class="number">2</span>) ? <span class="number">11</span> : <span class="number">21</span>;</span><br><span class="line">    time = LocalDateTime.of(queryStartDate.getYear(), month, day, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">&#125; <span class="keyword">else</span> &#123; <span class="comment">// MONTH</span></span><br><span class="line">    time = LocalDateTime.of(queryStartDate.getYear(), month, <span class="number">1</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>修改后</strong>：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (<span class="string">&quot;DAY&quot;</span>.equalsIgnoreCase(periodType) || <span class="string">&quot;DAY1&quot;</span>.equalsIgnoreCase(periodType)) &#123;</span><br><span class="line">    <span class="type">int</span> <span class="variable">day</span> <span class="operator">=</span> ((Number) row.get(<span class="string">&quot;day&quot;</span>)).intValue();</span><br><span class="line">    time = LocalDateTime.of(queryStartDate.getYear(), month, day, <span class="number">8</span>, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="string">&quot;XUN&quot;</span>.equalsIgnoreCase(periodType)) &#123;</span><br><span class="line">    <span class="type">int</span> <span class="variable">xun</span> <span class="operator">=</span> ((Number) row.get(<span class="string">&quot;xun&quot;</span>)).intValue();</span><br><span class="line">    <span class="keyword">if</span> (xun == <span class="number">1</span>) &#123;</span><br><span class="line">        time = LocalDateTime.of(queryStartDate.getYear(), month, <span class="number">11</span>, <span class="number">8</span>, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (xun == <span class="number">2</span>) &#123;</span><br><span class="line">        time = LocalDateTime.of(queryStartDate.getYear(), month, <span class="number">21</span>, <span class="number">8</span>, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        time = LocalDateTime.of(queryStartDate.getYear(), month, <span class="number">1</span>, <span class="number">8</span>, <span class="number">0</span>, <span class="number">0</span>).plusMonths(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125; <span class="keyword">else</span> &#123; <span class="comment">// MONTH - 月均值存储在下月1日，如4月均值对应5月1日</span></span><br><span class="line">    time = LocalDateTime.of(queryStartDate.getYear(), month, <span class="number">1</span>, <span class="number">8</span>, <span class="number">0</span>, <span class="number">0</span>).plusMonths(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="Step-6：回退路径同步修正"><a href="#Step-6：回退路径同步修正" class="headerlink" title="Step 6：回退路径同步修正"></a>Step 6：回退路径同步修正</h3><p>系统有两条查询路径：预计算表路径（快）和回退路径（慢，从ST_RIVER_R实时计算）。回退路径同样存在时间映射问题，需要同步修正。</p>
<p><strong>queryHistoricalDailyAverage</strong>：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 修正前</span></span><br><span class="line"><span class="type">LocalDateTime</span> <span class="variable">avgTime</span> <span class="operator">=</span> LocalDateTime.of(startDate.getYear(), month, day, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line"><span class="comment">// 修正后</span></span><br><span class="line"><span class="type">LocalDateTime</span> <span class="variable">avgTime</span> <span class="operator">=</span> LocalDateTime.of(startDate.getYear(), month, day, <span class="number">8</span>, <span class="number">0</span>, <span class="number">0</span>);</span><br></pre></td></tr></table></figure>

<p><strong>queryHistoricalXunAverage</strong>：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 修正前</span></span><br><span class="line"><span class="type">int</span> <span class="variable">startDay</span> <span class="operator">=</span> (xun == <span class="number">1</span>) ? <span class="number">1</span> : (xun == <span class="number">2</span>) ? <span class="number">11</span> : <span class="number">21</span>;</span><br><span class="line"><span class="type">LocalDateTime</span> <span class="variable">avgTime</span> <span class="operator">=</span> LocalDateTime.of(startDate.getYear(), month, startDay, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 修正后</span></span><br><span class="line">LocalDateTime avgTime;</span><br><span class="line"><span class="keyword">if</span> (xun == <span class="number">1</span>) &#123;</span><br><span class="line">    avgTime = LocalDateTime.of(startDate.getYear(), month, <span class="number">11</span>, <span class="number">8</span>, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (xun == <span class="number">2</span>) &#123;</span><br><span class="line">    avgTime = LocalDateTime.of(startDate.getYear(), month, <span class="number">21</span>, <span class="number">8</span>, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    avgTime = LocalDateTime.of(startDate.getYear(), month, <span class="number">1</span>, <span class="number">8</span>, <span class="number">0</span>, <span class="number">0</span>).plusMonths(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>queryHistoricalMonthAverage</strong>：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 修正前</span></span><br><span class="line"><span class="type">LocalDateTime</span> <span class="variable">avgTime</span> <span class="operator">=</span> LocalDateTime.of(startDate.getYear(), entry.getKey(), <span class="number">1</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line"><span class="comment">// 修正后</span></span><br><span class="line"><span class="type">LocalDateTime</span> <span class="variable">avgTime</span> <span class="operator">=</span> LocalDateTime.of(startDate.getYear(), entry.getKey(), <span class="number">1</span>, <span class="number">8</span>, <span class="number">0</span>, <span class="number">0</span>).plusMonths(<span class="number">1</span>);</span><br></pre></td></tr></table></figure>

<p>同时，月份集合遍历循环终止条件也需要修正：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 修正前</span></span><br><span class="line"><span class="keyword">while</span> (!dataPoint.isAfter(endDate)) &#123; ... &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 修正后</span></span><br><span class="line"><span class="type">LocalDate</span> <span class="variable">loopEnd</span> <span class="operator">=</span> endDate.withDayOfMonth(<span class="number">1</span>).plusMonths(<span class="number">1</span>);</span><br><span class="line"><span class="keyword">while</span> (!dataPoint.isAfter(loopEnd)) &#123; ... &#125;</span><br></pre></td></tr></table></figure>

<h2 id="代码修改汇总"><a href="#代码修改汇总" class="headerlink" title="代码修改汇总"></a>代码修改汇总</h2><table>
<thead>
<tr>
<th>文件</th>
<th>改动</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>WaterComparisonServiceImpl.java</td>
<td>+110&#x2F;-42</td>
<td>查询范围调整、月份遍历、时间映射</td>
</tr>
<tr>
<td>RiverMultiYearAvgMapper.java</td>
<td>+12&#x2F;-2</td>
<td>新增selectMonthAvgByMonthList方法</td>
</tr>
<tr>
<td>RiverMultiYearAvgMapper.xml</td>
<td>+22&#x2F;-2</td>
<td>新增按月份列表查询SQL</td>
</tr>
<tr>
<td>WaterAverageDataMapper.xml</td>
<td>+2&#x2F;-2</td>
<td>STTDRCD值修正</td>
</tr>
</tbody></table>
<h2 id="测试验证结果"><a href="#测试验证结果" class="headerlink" title="测试验证结果"></a>测试验证结果</h2><p><img data-src="/images/posts/%E6%B0%B4%E6%96%87%E6%95%B0%E6%8D%AE%E5%9D%87%E5%80%BC%E5%AF%B9%E6%AF%94%E6%9F%A5%E8%AF%A2Bug%E4%BF%AE%E5%A4%8D%E5%85%A8%E8%AE%B0%E5%BD%95/fig5.png" alt="测试验证结果展示图"><br><em>图5：修复后的测试验证结果</em></p>
<p>修复完成后，经过全面测试验证：</p>
<ul>
<li><input checked="" disabled="" type="checkbox"> 请求4月7日到5月7日MONTH类型，<code>comparisonSeriesMap</code>返回1条（4月）</li>
<li><input checked="" disabled="" type="checkbox"> 旬均值时间点正确：上旬&#x3D;11日08:00，中旬&#x3D;21日08:00，下旬&#x3D;下月1日08:00</li>
<li><input checked="" disabled="" type="checkbox"> 月均值时间点正确：4月均值&#x3D;5月1日08:00</li>
<li><input checked="" disabled="" type="checkbox"> XUN&#x2F;MONTH查询范围自动扩展，用户无需手动填下月日期</li>
<li><input checked="" disabled="" type="checkbox"> MONTH的startTime正确偏移，不包含上月月均值</li>
<li><input checked="" disabled="" type="checkbox"> STTDRCD值正确：旬&#x3D;’4’，月&#x3D;’5’</li>
</ul>
<h2 id="核心经验教训"><a href="#核心经验教训" class="headerlink" title="核心经验教训"></a>核心经验教训</h2><h3 id="1-水文数据库的STTDRCD编码必须严格对照标准文档"><a href="#1-水文数据库的STTDRCD编码必须严格对照标准文档" class="headerlink" title="1. 水文数据库的STTDRCD编码必须严格对照标准文档"></a>1. 水文数据库的STTDRCD编码必须严格对照标准文档</h3><p>不能靠猜测或类推。旬均值是’4’，月均值是’5’，这是水文行业的标准约定。代码中原来写反了，导致查询不到正确的数据。</p>
<h3 id="2-涉及”数据点代表时段”的查询，必须按数据点位置遍历"><a href="#2-涉及”数据点代表时段”的查询，必须按数据点位置遍历" class="headerlink" title="2. 涉及”数据点代表时段”的查询，必须按数据点位置遍历"></a>2. 涉及”数据点代表时段”的查询，必须按数据点位置遍历</h3><p>原代码将查询范围的日期直接映射为月份，而没有考虑数据点实际代表的月份。水文数据的时间点不是”自然日期”而是”代表时段”——月均值存储在下月1日08:00，旬均值中的下旬存储在下月1日08:00。</p>
<h3 id="3-水文日从08-00开始，所有时间构造必须使用08-00"><a href="#3-水文日从08-00开始，所有时间构造必须使用08-00" class="headerlink" title="3. 水文日从08:00开始，所有时间构造必须使用08:00"></a>3. 水文日从08:00开始，所有时间构造必须使用08:00</h3><p>而非默认的00:00。这一点在所有6处涉及时间构造的代码中都需要修正。</p>
<h3 id="4-修复必须覆盖双路径"><a href="#4-修复必须覆盖双路径" class="headerlink" title="4. 修复必须覆盖双路径"></a>4. 修复必须覆盖双路径</h3><p>系统有预计算表路径和回退路径，两条路径都存在相同的时间映射问题，需要分别修复才能确保一致性。</p>
<h2 id="前端对接要点"><a href="#前端对接要点" class="headerlink" title="前端对接要点"></a>前端对接要点</h2><p>修复完成后，前端对接需要注意以下要点：</p>
<ol>
<li><p><strong>请求格式</strong>：只需提供自然日期范围，如查询1月1日到4月30日的月均值，传入<code>startTime=2026-01-01 08:00</code>，<code>endTime=2026-04-30 08:00</code></p>
</li>
<li><p><strong>返回时间格式</strong>：<code>yyyy-MM-dd HH:mm:ss</code>，时间为08:00</p>
</li>
<li><p><strong>月均值时间点</strong>：下月1日（如4月均值显示为<code>05-01 08:00</code>）</p>
</li>
<li><p><strong>旬均值时间点</strong>：</p>
<ul>
<li>上旬：11日08:00</li>
<li>中旬：21日08:00</li>
<li>下旬：下月1日08:00</li>
</ul>
</li>
</ol>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>这次Bug修复虽然涉及的文件不多，但问题的根因却隐藏在水文数据的存储约定中。如果没有深入理解”数据点代表时段”这一核心概念，就只能治标不治本——表面修好了，时间对了，但数据仍然可能匹配不上。</p>
<p>通过这次修复，我深刻体会到：</p>
<ul>
<li><strong>领域知识的重要性</strong>：水文数据的存储规则不是通用知识，需要专门学习</li>
<li><strong>双路径验证的必要性</strong>：预计算路径和回退路径必须同步修复</li>
<li><strong>测试验证的全面性</strong>：边界条件（如月末、月初）必须覆盖</li>
</ul>
<p>希望这篇博客记录对遇到类似问题的朋友有所帮助。如果有任何疑问，欢迎在评论区留言交流。</p>
<hr>
<h2 id="配套音频"><a href="#配套音频" class="headerlink" title="配套音频"></a>配套音频</h2><h3 id="导读（成熟女声）"><a href="#导读（成熟女声）" class="headerlink" title="导读（成熟女声）"></a>导读（成熟女声）</h3><p><audio controls src="/audio/水文数据均值对比查询Bug修复导读.mp3"></audio></p>
<blockquote>
<p>今天，让我们一起走进水文数据的Bug修复之旅。从ST_RVAV_R表的存储约定出发，探索数据点代表时段的奥秘，见证一位工程师如何抽丝剥茧，从问题分析到完整修复，为水文信息化系统扫清隐患。这不仅是一次技术问题的解决，更是对领域知识重要性的深刻验证。让我们开始吧。</p>
</blockquote>
<h3 id="配套BGM"><a href="#配套BGM" class="headerlink" title="配套BGM"></a>配套BGM</h3><p><audio controls src="/music/水文数据均值对比查询BGM.mp3"></audio></p>
<p><em>（温暖励志的科技博客背景音乐，轻快的电子琴旋律，融入水波涟漪的意象）</em></p>
<hr>
<p><strong>相关Git提交</strong>：</p>
<ul>
<li><code>8f9d9f2</code> - 修复均值对比查询：历史均值条数、时间对齐及自动扩展查询范围</li>
<li><code>e72e23f</code> - 修复月查询（补充startTime偏移和循环终止条件修复）</li>
</ul>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>水文</category>
      </categories>
      <tags>
        <tag>MyBatis</tag>
        <tag>Bug修复</tag>
        <tag>Java</tag>
        <tag>水文数据</tag>
        <tag>ST_RVAV_R</tag>
      </tags>
  </entry>
  <entry>
    <title>测站沿革接口开发与调试记录</title>
    <url>/2026/03/12/%E6%B5%8B%E7%AB%99%E6%B2%BF%E9%9D%A9%E6%8E%A5%E5%8F%A3%E5%BC%80%E5%8F%91%E8%B0%83%E8%AF%95%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<p>开发一个查询测站沿革信息列表的第三方API接口，调用水利系统基础数据API获取测站历史变更信息。开发完成后调试时遇到两个常见但容易忽略的问题：参数名错误和响应结构解析错误。</p>
<span id="more"></span>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>在广东水文项目中，需要开发一个查询测站沿革信息列表的接口，调用第三方水利系统基础数据API获取测站的历史变更记录，包括站名变更、地址变更、所属行政区划调整等信息。这对于数据治理和历史追溯非常重要。</p>
<h2 id="问题背景"><a href="#问题背景" class="headerlink" title="问题背景"></a>问题背景</h2><p>项目需要对接水利系统的第三方API，获取测站的历史变更记录。</p>
<h3 id="第三方API信息"><a href="#第三方API信息" class="headerlink" title="第三方API信息"></a>第三方API信息</h3><table>
<thead>
<tr>
<th>项目</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>请求路径</td>
<td><code>/stsc/openapi/listGrouped</code></td>
</tr>
<tr>
<td>请求方式</td>
<td>GET</td>
</tr>
<tr>
<td>必填参数</td>
<td><code>stcd</code>（测站编码）</td>
</tr>
</tbody></table>
<h2 id="接口开发"><a href="#接口开发" class="headerlink" title="接口开发"></a>接口开发</h2><h3 id="实现方案"><a href="#实现方案" class="headerlink" title="实现方案"></a>实现方案</h3><p>参考现有”大断面”接口的实现模式，采用 Controller-Biz-DTO 三层架构：</p>
<ol>
<li><strong>DTO</strong>: <code>StationEvolutionDTO</code> - 测站沿革信息数据对象</li>
<li><strong>Biz</strong>: <code>StationArchiveBiz.getStationEvolutions()</code> - 业务逻辑层</li>
<li><strong>Controller</strong>: <code>StationArchiveController.stationEvolution</code> - 接口层</li>
</ol>
<h3 id="创建-修改的文件"><a href="#创建-修改的文件" class="headerlink" title="创建&#x2F;修改的文件"></a>创建&#x2F;修改的文件</h3><table>
<thead>
<tr>
<th>类型</th>
<th>文件路径</th>
</tr>
</thead>
<tbody><tr>
<td>新增</td>
<td><code>cnsci-admin/.../dto/station/StationEvolutionDTO.java</code></td>
</tr>
<tr>
<td>修改</td>
<td><code>cnsci-admin/.../biz/station/StationArchiveBiz.java</code></td>
</tr>
<tr>
<td>修改</td>
<td><code>cnsci-admin/.../station/StationArchiveController.java</code></td>
</tr>
</tbody></table>
<h3 id="接口调用示例"><a href="#接口调用示例" class="headerlink" title="接口调用示例"></a>接口调用示例</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">GET /stationArchive/stationEvolution?stcd=80505000</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="调试阶段：两个坑与修复"><a href="#调试阶段：两个坑与修复" class="headerlink" title="调试阶段：两个坑与修复"></a>调试阶段：两个坑与修复</h2><p>接口开发完成后，调用测试发现：直接请求第三方接口有数据返回，但通过我们开发的接口返回空数据。</p>
<h3 id="坑一：参数名错误"><a href="#坑一：参数名错误" class="headerlink" title="坑一：参数名错误"></a>坑一：参数名错误</h3><table>
<thead>
<tr>
<th>层级</th>
<th>参数名</th>
</tr>
</thead>
<tbody><tr>
<td>Controller 接收</td>
<td><code>stcd</code></td>
</tr>
<tr>
<td>Biz 层传给第三方</td>
<td><code>iid</code> ❌</td>
</tr>
<tr>
<td>第三方接口需要</td>
<td><code>stcd</code></td>
</tr>
</tbody></table>
<p><strong>修复</strong>：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 错误写法</span></span><br><span class="line">params.put(<span class="string">&quot;iid&quot;</span>, iid);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 正确写法</span></span><br><span class="line">params.put(<span class="string">&quot;stcd&quot;</span>, iid);</span><br></pre></td></tr></table></figure>

<h3 id="坑二：响应结构解析错误"><a href="#坑二：响应结构解析错误" class="headerlink" title="坑二：响应结构解析错误"></a>坑二：响应结构解析错误</h3><p>第三方返回：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;total&quot;</span><span class="punctuation">:</span> <span class="number">104</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;rows&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span>...<span class="punctuation">]</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>代码原期望：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;data&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;rows&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span>...<span class="punctuation">]</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p><strong>修复</strong>：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 错误写法</span></span><br><span class="line"><span class="type">JSONObject</span> <span class="variable">data</span> <span class="operator">=</span> response.getJSONObject(<span class="string">&quot;data&quot;</span>);</span><br><span class="line"><span class="type">JSONArray</span> <span class="variable">rows</span> <span class="operator">=</span> data.getJSONArray(<span class="string">&quot;rows&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 正确写法</span></span><br><span class="line"><span class="type">JSONArray</span> <span class="variable">rows</span> <span class="operator">=</span> response.getJSONArray(<span class="string">&quot;rows&quot;</span>);</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="Git-操作技巧"><a href="#Git-操作技巧" class="headerlink" title="Git 操作技巧"></a>Git 操作技巧</h2><p>在修复 Bug 时，使用 <code>git reset --soft</code> 可以撤销提交但保留修改在暂存区：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git reset --soft d16095b~1</span><br><span class="line">git add cnsci-admin/.../StationArchiveBiz.java</span><br><span class="line">git commit -m <span class="string">&quot;fix: 修复测站沿革接口参数和响应解析问题&quot;</span></span><br></pre></td></tr></table></figure>

<hr>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><ul>
<li><strong>调试技巧</strong>：遇到第三方接口返回空数据时，添加日志打印原始响应，快速定位响应结构问题</li>
<li><strong>接口对接</strong>：调用第三方API时，务必确认参数名和响应结构，不能想当然</li>
<li><strong>Git 使用</strong>：<code>git reset --soft</code> 是调整提交历史的利器</li>
</ul>
<hr>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>接口开发看似简单，但在对接第三方API时，参数名和响应结构这两个”小细节”往往是导致问题的根本原因。调试过程中养成打印日志的习惯，能够快速定位问题所在。</p>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>水文</category>
      </categories>
      <tags>
        <tag>Bug修复</tag>
        <tag>Java</tag>
        <tag>API</tag>
        <tag>调试经验</tag>
      </tags>
  </entry>
  <entry>
    <title>物联网项目工作总结</title>
    <url>/2026/04/30/%E7%89%A9%E8%81%94%E7%BD%91%E9%A1%B9%E7%9B%AE%E5%B7%A5%E4%BD%9C%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<blockquote>
<p>这是一篇关于物联网项目的技术博客，记录了这段时间在项目环境搭建、中间件配置、功能开发等方面的工作总结。</p>
</blockquote>
<span id="more"></span>

<hr>
<h2 id="🎵-博客音频"><a href="#🎵-博客音频" class="headerlink" title="🎵 博客音频"></a>🎵 博客音频</h2><h3 id="女声开场播报"><a href="#女声开场播报" class="headerlink" title="女声开场播报"></a>女声开场播报</h3><blockquote>
<p>“大家好，这是一个关于物联网项目的博客，下面的内容是这段时间工作的总结。请大家观看。”</p>
</blockquote>
<audio controls>
  <source src="/audio/intro-announcement.mp3" type="audio/mpeg">
  您的浏览器不支持音频播放
</audio>

<h3 id="背景音乐"><a href="#背景音乐" class="headerlink" title="背景音乐"></a>背景音乐</h3><p><em>🎶 物联网项目主题曲 - 科技感电子流行风格</em></p>

<audio controls>
  <source src="/audio/iot-project-theme.mp3" type="audio/mpeg">
  您的浏览器不支持音频播放
</audio>

<hr>
<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>南沙物联网项目是基于 BladeX-Links 框架打造的综合性物联网平台，涵盖设备管理、数据采集、规则引擎、边缘计算等核心功能。本次工作总结从 <strong>2026年3月中旬至4月中旬</strong>，历时约一个月，主要完成了本地开发环境搭建、中间件配置排障、核心功能开发等工作。</p>
<p>本项目涉及的技术栈非常广泛，包括 <strong>Java&#x2F;Spring Boot</strong> 后端服务、<strong>Vue3&#x2F;Avue</strong> 前端框架、<strong>Nacos</strong> 配置中心与注册中心、<strong>TDengine</strong> 时序数据库、<strong>Node-RED</strong> 流程编排引擎、<strong>MySQL</strong> 关系型数据库、<strong>Redis</strong> 缓存服务等。在一个月的时间里，我们逐一攻克了环境配置、性能调优、功能实现等多方面的技术挑战，积累了丰富的物联网项目实战经验。</p>
<p>下面将从 <strong>环境配置</strong>、<strong>排障过程</strong>、<strong>功能开发</strong>、<strong>扩展功能</strong> 四个维度，系统性地梳理本次工作的完整过程与技术收获。</p>
<hr>
<h2 id="技术架构"><a href="#技术架构" class="headerlink" title="技术架构"></a>技术架构</h2><h3 id="整体架构概述"><a href="#整体架构概述" class="headerlink" title="整体架构概述"></a>整体架构概述</h3><p>南沙物联网项目采用了经典的分层微服务架构，从下往上依次分为 <strong>数据层</strong>、<strong>服务层</strong>、<strong>网关层</strong> 和 <strong>前端层</strong>。数据层负责数据的持久化存储，服务层承载核心业务逻辑，网关层负责请求的路由与鉴权，前端层提供用户交互界面。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌─────────────────────────────────────────────────────────────────┐</span><br><span class="line">│                        前端层 (BladeX-Links-UI)                   │</span><br><span class="line">│                      Vue3 + Avue + Element UI                    │</span><br><span class="line">│                           端口: 5173                              │</span><br><span class="line">└─────────────────────────────────────────────────────────────────┘</span><br><span class="line">                                  │</span><br><span class="line">                                  ▼</span><br><span class="line">┌─────────────────────────────────────────────────────────────────┐</span><br><span class="line">│                         网关层 (blade-gateway)                    │</span><br><span class="line">│                        Spring Cloud Gateway                       │</span><br><span class="line">│                           端口: 8090                             │</span><br><span class="line">└─────────────────────────────────────────────────────────────────┘</span><br><span class="line">                                  │</span><br><span class="line">        ┌─────────────────────────┼─────────────────────────┐</span><br><span class="line">        ▼                         ▼                         ▼</span><br><span class="line">┌───────────────┐       ┌───────────────┐       ┌───────────────┐</span><br><span class="line">│ blade-broker  │       │  blade-data   │       │ blade-server  │</span><br><span class="line">│   MQTT Broker │       │   数据采集    │       │   核心业务    │</span><br><span class="line">│  1883 / 8888  │       │    服务       │       │    服务       │</span><br><span class="line">└───────────────┘       └───────────────┘       └───────────────┘</span><br><span class="line">        │                         │                         │</span><br><span class="line">        └─────────────────────────┼─────────────────────────┘</span><br><span class="line">                                  ▼</span><br><span class="line">┌─────────────────────────────────────────────────────────────────┐</span><br><span class="line">│                          数据层                                  │</span><br><span class="line">│  ┌─────────┐  ┌─────────┐  ┌─────────────┐  ┌─────────────────┐ │</span><br><span class="line">│  │  MySQL  │  │  Redis  │  │  TDengine   │  │     MinIO      │ │</span><br><span class="line">│  │   3306  │  │   6379  │  │    6041     │  │      9000      │ │</span><br><span class="line">│  └─────────┘  └─────────┘  └─────────────┘  └─────────────────┘ │</span><br><span class="line">└─────────────────────────────────────────────────────────────────┘</span><br><span class="line">                                  │</span><br><span class="line">                                  ▼</span><br><span class="line">┌─────────────────────────────────────────────────────────────────┐</span><br><span class="line">│                     配置中心 (Nacos)                              │</span><br><span class="line">│              starviewnacos.com:8340  |  namespace               │</span><br><span class="line">│                    2d2e498b-060e-446f-a900-dbb840c6541b         │</span><br><span class="line">└─────────────────────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>

<p><img data-src="/images/posts/nansha-iot-project-summary/fig1.jpg" alt="项目整体架构图"><br><em>图1：BladeX-Links 物联网项目整体架构图</em></p>
<h3 id="核心服务说明"><a href="#核心服务说明" class="headerlink" title="核心服务说明"></a>核心服务说明</h3><table>
<thead>
<tr>
<th>服务名称</th>
<th>技术栈</th>
<th>端口</th>
<th>主要职责</th>
</tr>
</thead>
<tbody><tr>
<td><strong>blade-broker</strong></td>
<td>Spring Boot + MQTT</td>
<td>1883 &#x2F; 8888</td>
<td>MQTT 消息代理，处理设备与平台的通信</td>
</tr>
<tr>
<td><strong>blade-data</strong></td>
<td>Spring Boot</td>
<td>—</td>
<td>数据采集服务，接收并处理传感器数据</td>
</tr>
<tr>
<td><strong>blade-server</strong></td>
<td>Spring Boot</td>
<td>80</td>
<td>核心业务服务，提供设备管理、规则配置等 API</td>
</tr>
<tr>
<td><strong>blade-gateway</strong></td>
<td>Spring Cloud Gateway</td>
<td>8090</td>
<td>API 网关，统一入口，承载鉴权与路由</td>
</tr>
<tr>
<td><strong>blade-ui</strong></td>
<td>Vue3 + Avue</td>
<td>5173</td>
<td>前端管理界面，用户操作入口</td>
</tr>
</tbody></table>
<h3 id="中间件选型考量"><a href="#中间件选型考量" class="headerlink" title="中间件选型考量"></a>中间件选型考量</h3><p>本项目在中间件选型上做了充分的技术调研与权衡：</p>
<p><strong>MySQL</strong> 作为传统关系型数据库，承担结构化业务数据的存储职责，包括设备注册信息、用户账号、权限配置、规则元数据等核心业务表。MySQL 的事务特性和丰富的数据类型支持，使其成为业务数据持久化的首选。</p>
<p><strong>Redis</strong> 主要用于会话缓存和热点数据加速。在物联网场景下，设备频繁上下线会产生大量状态查询请求，通过 Redis 缓存可以有效降低数据库压力，同时利用其 Pub&#x2F;Sub 机制实现实时消息推送。</p>
<p><strong>TDengine</strong> 是本次项目引入的时序数据库，专门针对物联网场景优化。相比通用型数据库，TDengine 在时间序列数据的写入和查询性能上有数量级的优势，同时支持数据压缩和降采样存储，大幅降低存储成本。本项目使用 TDengine 3.0.7.1 版本，通过 REST SQL 接口与业务系统对接。</p>
<p><strong>MinIO</strong> 作为对象存储服务，用于存储设备采集的媒体文件（如监控图片、视频片段等）。MinIO 与 S3 兼容的 API 设计，使得后续迁移至云存储服务时无需修改业务代码。</p>
<p><strong>Nacos</strong> 承担配置中心和注册中心的双重职责。所有微服务的配置文件（如数据库连接、Redis 连接、业务参数等）集中管理在 Nacos 中，服务启动时自动从 Nacos 拉取配置。同时，服务实例启动后会向 Nacos 注册自身信息，实现服务发现和负载均衡。</p>
<hr>
<h2 id="环境配置"><a href="#环境配置" class="headerlink" title="环境配置"></a>环境配置</h2><h3 id="Nacos-本地环境搭建"><a href="#Nacos-本地环境搭建" class="headerlink" title="Nacos 本地环境搭建"></a>Nacos 本地环境搭建</h3><p>Nacos 是本项目配置管理的核心组件，其配置质量直接决定服务能否正常启动。在项目初期，我们遇到了典型的”本地环境与项目默认约定不一致”问题。</p>
<h4 id="问题现象"><a href="#问题现象" class="headerlink" title="问题现象"></a>问题现象</h4><p>项目启动时控制台反复出现以下错误日志：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Client not connected, current status: STARTING</span><br><span class="line">Ignore the empty nacos configuration</span><br></pre></td></tr></table></figure>

<p>服务启动后，虽然能在控制台看到部分页面，但设备注册状态始终为空，业务功能无法完整运行。</p>
<h4 id="根因分析"><a href="#根因分析" class="headerlink" title="根因分析"></a>根因分析</h4><p>经过深入排查，我们发现问题的根源在于<strong>多层环境偏差叠加</strong>：</p>
<p><strong>第一层：默认地址不匹配</strong></p>
<p>项目启动器在运行期注入 Nacos 配置，默认连接地址是 <code>starviewnacos.com:8340</code>，而我们最初本地运行的 Nacos 是通过 Docker 部署的，默认端口是 <code>8848</code>。这意味着即使本地 Nacos 能正常运行，Java 客户端也找不到正确的服务入口。</p>
<p><strong>第二层：域名解析异常</strong></p>
<p><code>starviewnacos.com</code> 这个域名在本地解析不稳定，Java 进程有时能解析，有时解析失败并抛出 <code>UnknownHostException</code>。进一步检查发现，本机的代理环境变量配置导致该域名的请求被转发到了代理服务器，返回 502 错误。</p>
<p><strong>第三层：namespace 不一致</strong></p>
<p>早期配置全部放在 Nacos 的 <code>public</code> namespace 下，但项目实际读取的是 <code>2d2e498b-060e-446f-a900-dbb840c6541b</code> 这个特定 namespace。控制台看到配置存在，但业务服务读取不到。</p>
<p><strong>第四层：gRPC 端口未开通</strong></p>
<p>Nacos 2.x 版本使用 gRPC 协议进行服务注册和配置推送，主端口 8340 之外还需要开通 9340&#x2F;9341 端口。我们最初只关注了主端口的连通性，忽略了 gRPC 端口的配置。</p>
<h4 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h4><p>针对上述问题，我们逐项制定解决方案：</p>
<p><strong>1. 固定域名解析</strong></p>
<p>在系统 hosts 文件中添加静态映射：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">127.0.0.1 starviewnacos.com</span><br></pre></td></tr></table></figure>

<p>同时配置代理环境变量，排除该域名：</p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line"><span class="variable">$env:NO_PROXY</span> = <span class="string">&quot;localhost,127.0.0.1,starviewnacos.com&quot;</span></span><br><span class="line"><span class="variable">$env:no_proxy</span> = <span class="string">&quot;localhost,127.0.0.1,starviewnacos.com&quot;</span></span><br></pre></td></tr></table></figure>

<p><strong>2. 使用本地解压版 Nacos</strong></p>
<p>放弃 Docker 部署方式，改为使用本地解压版 Nacos 3.1。解压版 Nacos 的优势在于端口配置更灵活，不依赖 Docker 网络模式：</p>
<table>
<thead>
<tr>
<th>端口类型</th>
<th>端口号</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>API 端口</td>
<td>8340</td>
<td>主服务端口，项目默认连接此端口</td>
</tr>
<tr>
<td>Console 端口</td>
<td>8380</td>
<td>控制台界面访问端口</td>
</tr>
<tr>
<td>gRPC 端口</td>
<td>9340&#x2F;9341</td>
<td>服务注册与配置推送端口</td>
</tr>
</tbody></table>
<p><strong>3. 对齐 namespace</strong></p>
<p>在 Nacos 中创建项目指定的 namespace：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Namespace ID: 2d2e498b-060e-446f-a900-dbb840c6541b</span><br><span class="line">Namespace 名称: water</span><br></pre></td></tr></table></figure>

<p>并将 <code>blade.yaml</code>、<code>blade-dev.yaml</code> 等核心配置文件导入该 namespace。</p>
<p><strong>4. 使用嵌入式存储</strong></p>
<p>启动参数中添加 <code>-DembeddedStorage=true</code>，确保单机模式下数据存储使用嵌入式 Derby 数据库，避免外部数据库连接问题：</p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">.\startup.cmd <span class="literal">-m</span> standalone <span class="literal">-DembeddedStorage</span>=true</span><br></pre></td></tr></table></figure>

<h4 id="最终验证"><a href="#最终验证" class="headerlink" title="最终验证"></a>最终验证</h4><p>配置完成后，通过以下命令验证 Nacos 环境是否就绪：</p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 检查主端口</span></span><br><span class="line"><span class="built_in">curl</span> http://starviewnacos.com:<span class="number">8340</span>/nacos/</span><br><span class="line"></span><br><span class="line"><span class="comment"># 检查服务注册状态</span></span><br><span class="line"><span class="comment"># 登录控制台 http://localhost:8380/ 查看已注册服务列表</span></span><br></pre></td></tr></table></figure>

<p>已确认注册成功的服务包括 <code>cnsci-auth</code>（认证服务）和 <code>cnsci-desk</code>（桌面服务），均显示 <code>healthy=true</code> 状态。</p>
<p><img data-src="/images/posts/nansha-iot-project-summary/fig2.jpg" alt="Nacos服务注册流程图"><br><em>图2：Nacos 服务注册与配置管理流程</em></p>
<hr>
<h3 id="TDengine-时序数据库配置"><a href="#TDengine-时序数据库配置" class="headerlink" title="TDengine 时序数据库配置"></a>TDengine 时序数据库配置</h3><p>TDengine 是物联网项目处理时序数据的核心组件，负责存储传感器采集的设备数据。在 TDengine 部署过程中，我们同样遇到了多个技术挑战。</p>
<h4 id="版本选型历程"><a href="#版本选型历程" class="headerlink" title="版本选型历程"></a>版本选型历程</h4><p>项目最初尝试使用 TDengine 企业版（3.3.8.8），该版本功能完整但存在两个问题：其一，企业版需要商业授权，不适合作为长期开发方案；其二，在 Windows 环境下的服务注册和开机自启配置存在兼容性问题。</p>
<p>经过技术调研，我们最终选择了 <strong>TDengine Server 3.0.7.1</strong> 作为主要使用版本。这是一个开源稳定版本，在功能特性和兼容性之间取得了良好平衡。</p>
<h4 id="跨版本数据不兼容问题"><a href="#跨版本数据不兼容问题" class="headerlink" title="跨版本数据不兼容问题"></a>跨版本数据不兼容问题</h4><p>在切换版本过程中，我们遇到了典型的高版本数据目录无法被低版本读取的问题。日志中反复出现：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">failed to read sdb file ... Invalid table type</span><br></pre></td></tr></table></figure>

<p>这是因为 TDengine 不同版本之间的数据文件格式存在差异，高版本创建的数据文件包含低版本无法识别的元数据结构。解决方案是<strong>清理旧数据目录，重新初始化</strong>：</p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 停止 TDengine 服务</span></span><br><span class="line">sc.exe stop taosadapter</span><br><span class="line">sc.exe stop taosd</span><br><span class="line"></span><br><span class="line"><span class="comment"># 备份并清理旧数据目录</span></span><br><span class="line"><span class="built_in">Rename-Item</span> <span class="string">&#x27;C:\TDengine\data&#x27;</span> <span class="string">&#x27;C:\TDengine\data-backup-20260325&#x27;</span></span><br><span class="line"><span class="built_in">New-Item</span> <span class="literal">-ItemType</span> Directory <span class="literal">-Path</span> <span class="string">&#x27;C:\TDengine\data&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 重新启动服务</span></span><br><span class="line">sc.exe <span class="built_in">start</span> taosd</span><br><span class="line">sc.exe <span class="built_in">start</span> taosadapter</span><br></pre></td></tr></table></figure>

<h4 id="安装目录混装问题"><a href="#安装目录混装问题" class="headerlink" title="安装目录混装问题"></a>安装目录混装问题</h4><p>在排查过程中，我们还发现了一个隐蔽的安装目录混装问题。早期测试时曾在同一目录下安装过多个版本的 TDengine，导致当前运行版本的部分二进制文件和动态库是旧版本遗留的。这会造成 <code>taosd</code> 能正常启动，但 <code>taosadapter</code> 与底层通信协议不匹配的问题。</p>
<p>典型症状是 <code>taosadapter</code> 进程存在但无法连接后端 <code>taosd</code>，日志中持续报错 <code>invalid packet</code>。</p>
<p><strong>正确的解决方式</strong>是执行完整的卸载清理，然后进行干净重装：</p>
<ol>
<li>卸载所有旧版本 TDengine</li>
<li>删除安装目录（包括残留文件）</li>
<li>清理注册表项</li>
<li>重新执行 3.0.7.1 安装程序</li>
</ol>
<h4 id="项目配置对接"><a href="#项目配置对接" class="headerlink" title="项目配置对接"></a>项目配置对接</h4><p>确认 TDengine 服务正常后，还需要修改项目的数据库连接配置。主要是将 <code>dev</code> 环境下的 TDengine 连接地址从远程服务器改为本地：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">tdengine:</span></span><br><span class="line">  <span class="attr">enabled:</span> <span class="literal">true</span></span><br><span class="line">  <span class="attr">username:</span> <span class="string">root</span></span><br><span class="line">  <span class="attr">password:</span> <span class="string">taosdata</span></span><br><span class="line">  <span class="attr">database:</span> <span class="string">links</span></span><br><span class="line">  <span class="attr">jdbc-url:</span> <span class="string">jdbc:TAOS-RS://127.0.0.1:6041?useSSL=false</span></span><br></pre></td></tr></table></figure>

<h4 id="验证命令清单"><a href="#验证命令清单" class="headerlink" title="验证命令清单"></a>验证命令清单</h4><p>以下是日常开发中常用的 TDengine 验证命令：</p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 检查服务状态</span></span><br><span class="line">sc.exe query taosd</span><br><span class="line">sc.exe query taosadapter</span><br><span class="line"></span><br><span class="line"><span class="comment"># 启动服务</span></span><br><span class="line">sc.exe <span class="built_in">start</span> taosd</span><br><span class="line">sc.exe <span class="built_in">start</span> taosadapter</span><br><span class="line"></span><br><span class="line"><span class="comment"># 验证 REST 连接</span></span><br><span class="line">curl.exe <span class="literal">-s</span> <span class="literal">-u</span> root:taosdata http://<span class="number">127.0</span>.<span class="number">0.1</span>:<span class="number">6041</span>/rest/sql <span class="literal">-d</span> <span class="string">&quot;show databases;&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建项目数据库</span></span><br><span class="line">curl.exe <span class="literal">-s</span> <span class="literal">-u</span> root:taosdata http://<span class="number">127.0</span>.<span class="number">0.1</span>:<span class="number">6041</span>/rest/sql <span class="literal">-d</span> <span class="string">&quot;CREATE DATABASE IF NOT EXISTS links PRECISION &#x27;ms&#x27; KEEP 3650;&quot;</span></span><br></pre></td></tr></table></figure>

<hr>
<h3 id="其他中间件配置"><a href="#其他中间件配置" class="headerlink" title="其他中间件配置"></a>其他中间件配置</h3><h4 id="MySQL-数据库初始化"><a href="#MySQL-数据库初始化" class="headerlink" title="MySQL 数据库初始化"></a>MySQL 数据库初始化</h4><p>确认 MySQL 服务运行后，需要初始化项目数据库 <code>bladex_iot</code>。如果数据库不存在，执行以下初始化脚本：</p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 创建数据库</span></span><br><span class="line">mysql <span class="literal">-u</span> root <span class="literal">-p</span> <span class="literal">-e</span> <span class="string">&quot;CREATE DATABASE bladex_iot DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 按顺序执行初始化脚本</span></span><br><span class="line">mysql <span class="literal">-u</span> root <span class="literal">-p</span> bladex_iot &lt; <span class="number">01</span><span class="literal">-bladex</span>.mysql.sys.create.sql</span><br><span class="line">mysql <span class="literal">-u</span> root <span class="literal">-p</span> bladex_iot &lt; <span class="number">02</span><span class="literal">-bladex</span>.mysql.iot.create.sql</span><br><span class="line">mysql <span class="literal">-u</span> root <span class="literal">-p</span> bladex_iot &lt; <span class="number">03</span><span class="literal">-bladex</span>.mysql.iot.ext.create.sql</span><br></pre></td></tr></table></figure>

<h4 id="Redis-启动"><a href="#Redis-启动" class="headerlink" title="Redis 启动"></a>Redis 启动</h4><p>Redis 作为缓存和会话存储服务，启动相对简单：</p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 方式一：使用启动脚本</span></span><br><span class="line">F:\xinderedis\Redis<span class="literal">-8</span>.<span class="number">4.0</span><span class="literal">-Windows-x64-msys2-with-Service</span>\start.bat</span><br><span class="line"></span><br><span class="line"><span class="comment"># 方式二：命令行启动</span></span><br><span class="line"><span class="built_in">cd</span> F:\xinderedis\Redis<span class="literal">-8</span>.<span class="number">4.0</span><span class="literal">-Windows-x64-msys2-with-Service</span></span><br><span class="line">.\redis<span class="literal">-server</span>.exe redis.conf</span><br><span class="line"></span><br><span class="line"><span class="comment"># 验证连接</span></span><br><span class="line">.\redis<span class="literal">-cli</span>.exe ping</span><br><span class="line"><span class="comment"># 返回 PONG 即成功</span></span><br></pre></td></tr></table></figure>

<h4 id="MinIO-对象存储配置"><a href="#MinIO-对象存储配置" class="headerlink" title="MinIO 对象存储配置"></a>MinIO 对象存储配置</h4><p>MinIO 用于存储设备采集的媒体文件。首次启动时需要注意凭据配置必须与项目配置一致：</p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line"><span class="variable">$env:MINIO_ROOT_USER</span>=<span class="string">&quot;bladexadmin&quot;</span></span><br><span class="line"><span class="variable">$env:MINIO_ROOT_PASSWORD</span>=<span class="string">&quot;bladexadmin&quot;</span></span><br><span class="line">minio server F:\minio<span class="literal">-data</span> <span class="literal">--address</span> <span class="string">&quot;127.0.0.1:9000&quot;</span></span><br></pre></td></tr></table></figure>

<p>首次登录后需要创建名为 <code>bladex</code> 的存储桶，否则文件上传功能会失败。</p>
<p><img data-src="/images/posts/nansha-iot-project-summary/fig3.jpg" alt="TDengine时序数据库工作流程"><br><em>图3：TDengine 时序数据库写入与查询流程</em></p>
<hr>
<h4 id="中间件端口总览"><a href="#中间件端口总览" class="headerlink" title="中间件端口总览"></a>中间件端口总览</h4><table>
<thead>
<tr>
<th>服务</th>
<th>端口</th>
<th>启动验证方式</th>
</tr>
</thead>
<tbody><tr>
<td>MySQL</td>
<td>3306</td>
<td><code>mysql -u root -p -e &quot;SHOW DATABASES;&quot;</code></td>
</tr>
<tr>
<td>Redis</td>
<td>6379</td>
<td><code>.\redis-cli.exe ping</code></td>
</tr>
<tr>
<td>TDengine REST</td>
<td>6041</td>
<td><code>curl http://127.0.0.1:6041/rest/sql -d &quot;show databases;&quot;</code></td>
</tr>
<tr>
<td>MinIO</td>
<td>9000</td>
<td>浏览器访问 <a href="http://127.0.0.1:9000/">http://127.0.0.1:9000</a></td>
</tr>
<tr>
<td>Nacos</td>
<td>8340</td>
<td><code>curl http://starviewnacos.com:8340/nacos/</code></td>
</tr>
<tr>
<td>blade-broker</td>
<td>1883&#x2F;8888</td>
<td>MQTT 客户端连接测试</td>
</tr>
<tr>
<td>blade-server</td>
<td>80</td>
<td>浏览器访问 <a href="http://localhost/">http://localhost</a></td>
</tr>
<tr>
<td>blade-ui</td>
<td>5173</td>
<td>浏览器访问 <a href="http://localhost:5173/">http://localhost:5173</a></td>
</tr>
</tbody></table>
<hr>
<h2 id="排障过程"><a href="#排障过程" class="headerlink" title="排障过程"></a>排障过程</h2><h3 id="Node-RED-配置按钮-400-错误"><a href="#Node-RED-配置按钮-400-错误" class="headerlink" title="Node-RED 配置按钮 400 错误"></a>Node-RED 配置按钮 400 错误</h3><p>在前端开发过程中，我们遇到了设备规则页面点击”配置”按钮后 iframe 加载 400 错误的问题。这个问题涉及前后端交互和数据关联两个层面。</p>
<h4 id="问题现象-1"><a href="#问题现象-1" class="headerlink" title="问题现象"></a>问题现象</h4><p>用户在 <code>/iot/engine/device</code> 页面点击”配置”按钮后，弹出的 iframe 无内容显示，浏览器控制台报错：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">GET http://localhost:1880/flows?flowId=2041793891904016385&amp;_=1775657612771 400 (Bad Request)</span><br></pre></td></tr></table></figure>

<h4 id="问题分析"><a href="#问题分析" class="headerlink" title="问题分析"></a>问题分析</h4><p>经过代码链路追踪，我们发现问题的核心原因在于<strong>设备规则 ID 与 Node-RED Flow ID 的混淆</strong>。</p>
<p>前端设备规则页面点击”配置”时，拼接的 URL 是：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">$&#123;website.<span class="property">links</span>.<span class="property">edgeUrl</span>&#125;/#flow/$&#123;row.<span class="property">id</span>&#125;?token=$&#123;<span class="title function_">getToken</span>()&#125;</span><br></pre></td></tr></table></figure>

<p>这里的 <code>row.id</code> 是 <code>iot_engine_device</code> 表的主键（设备规则 ID），但 Node-RED 实际需要的是 <code>iot_edge_flows</code> 表的主键（Flow ID）。这是两个完全不同的概念：</p>
<table>
<thead>
<tr>
<th>标识</th>
<th>来源表</th>
<th>含义</th>
</tr>
</thead>
<tbody><tr>
<td><code>iot_engine_device.id</code></td>
<td>设备规则表</td>
<td>设备规则记录的唯一标识</td>
</tr>
<tr>
<td><code>iot_edge_flows.id</code></td>
<td>流程表</td>
<td>Node-RED 流程记录的唯一标识</td>
</tr>
</tbody></table>
<p>Node-RED 的 <code>getFlowById()</code> 方法会根据传入的 flowId 去 <code>iot_edge_flows</code> 表查询，但当前系统中设备规则记录和 Flow 记录之间没有建立关联，导致查询返回空，进而触发 400 错误。</p>
<h4 id="解决方案-1"><a href="#解决方案-1" class="headerlink" title="解决方案"></a>解决方案</h4><p>针对这个问题，我们实施了”设备规则与 Flow 联动创建”的方案：</p>
<p><strong>1. 数据库加字段</strong></p>
<p>在 <code>iot_engine_device</code> 表增加 <code>flow_id</code> 字段，建立设备规则与 Flow 的关联：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">ALTER TABLE</span> iot_engine_device</span><br><span class="line"><span class="keyword">ADD</span> <span class="keyword">COLUMN</span> flow_id <span class="type">varchar</span>(<span class="number">32</span>) <span class="keyword">NULL</span> COMMENT <span class="string">&#x27;关联的规则流ID&#x27;</span> AFTER rule_code;</span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE</span> INDEX idx_engine_device_flow_id <span class="keyword">ON</span> iot_engine_device(flow_id);</span><br></pre></td></tr></table></figure>

<p><strong>2. 后端联动创建逻辑</strong></p>
<p>在 <code>EngineDeviceServiceImpl.submitDevice()</code> 方法中，设备规则保存时自动创建关联的 Flow 记录：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (engineDevice.getId() == <span class="literal">null</span> &amp;&amp; StringUtil.isBlank(engineDevice.getFlowId())) &#123;</span><br><span class="line">    <span class="type">EdgeFlows</span> <span class="variable">edgeFlows</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">EdgeFlows</span>();</span><br><span class="line">    edgeFlows.setLabel(engineDevice.getRuleName());</span><br><span class="line">    edgeFlows.setSource(<span class="string">&quot;rule&quot;</span>);</span><br><span class="line">    edgeFlows.setDisabled(<span class="literal">false</span>);</span><br><span class="line">    edgeFlowsService.submit(edgeFlows);</span><br><span class="line">    engineDevice.setFlowId(edgeFlows.getId());</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> <span class="built_in">this</span>.saveOrUpdate(engineDevice);</span><br></pre></td></tr></table></figure>

<p><strong>3. 前端传参修正</strong></p>
<p>修改前端代码，将 <code>row.id</code> 改为 <code>row.flowId</code>：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="title function_">handleFlowConfig</span>(<span class="params">row</span>) &#123;</span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">ruleEngineSrc</span> = <span class="string">`<span class="subst">$&#123;website.links.edgeUrl&#125;</span>/#flow/<span class="subst">$&#123;row.flowId&#125;</span>?token=<span class="subst">$&#123;getToken()&#125;</span>`</span>;</span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">dialogFlowVisible</span> = <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>同时增加空值保护，当 <code>flowId</code> 为空时提示用户先保存记录。</p>
<p><strong>4. 删除时级联清理</strong></p>
<p>设备规则删除时，同步删除关联的 Flow 记录，避免产生孤儿数据。</p>
<h3 id="Node-RED-部署按钮权限问题"><a href="#Node-RED-部署按钮权限问题" class="headerlink" title="Node-RED 部署按钮权限问题"></a>Node-RED 部署按钮权限问题</h3><p>在修复配置按钮问题后，我们又遇到了 Node-RED 编辑器内点击”部署”按钮时返回 400 错误的问题。</p>
<h4 id="问题现象-2"><a href="#问题现象-2" class="headerlink" title="问题现象"></a>问题现象</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">POST http://localhost:1880/flows 400 (Bad Request)</span><br><span class="line">响应：&#123;&quot;error&quot;:&quot;unexpected_error&quot;,&quot;message&quot;:&quot;TypeError: Cannot set properties of undefined (setting &#x27;tenantId&#x27;)&quot;&#125;</span><br></pre></td></tr></table></figure>

<h4 id="问题分析-1"><a href="#问题分析-1" class="headerlink" title="问题分析"></a>问题分析</h4><p>这个错误的调用链路如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Node-RED 编辑器前端 → POST /flows</span><br><span class="line">  → needsPermission(&quot;flows.write&quot;) 中间件</span><br><span class="line">    → passport.authenticate([&#x27;bearer&#x27;,&#x27;tokens&#x27;,&#x27;anon&#x27;])</span><br><span class="line">      → 所有认证策略失败 → req.user = undefined</span><br><span class="line">    → if (!req.user) return next()  ← 直接放行</span><br><span class="line">  → flows.js: var user = req.user;  // undefined</span><br><span class="line">    → user.tenantId = tenantId;     // TypeError!</span><br></pre></td></tr></table></figure>

<p>问题根源在于 <code>settings.js</code> 中的 <code>adminAuth</code> 配置被注释掉，导致认证策略全部失效，<code>req.user</code> 为 <code>undefined</code>。后续路由尝试访问 <code>user.tenantId</code> 时触发空指针异常。</p>
<h4 id="解决方案-2"><a href="#解决方案-2" class="headerlink" title="解决方案"></a>解决方案</h4><p><strong>1. 恢复 adminAuth 配置</strong></p>
<p>在 <code>settings.js</code> 中正确配置 <code>adminAuth</code> 和 <code>adminToken</code>：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="attr">adminAuth</span>: &#123;</span><br><span class="line">    <span class="attr">type</span>: <span class="string">&quot;credentials&quot;</span>,</span><br><span class="line">    <span class="attr">users</span>: [</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="attr">username</span>: <span class="string">&quot;admin&quot;</span>,</span><br><span class="line">            <span class="attr">password</span>: <span class="built_in">require</span>(<span class="string">&#x27;crypto&#x27;</span>)</span><br><span class="line">                .<span class="title function_">createHash</span>(<span class="string">&#x27;sha256&#x27;</span>)</span><br><span class="line">                .<span class="title function_">update</span>(<span class="string">&quot;your-password-hash&quot;</span>)</span><br><span class="line">                .<span class="title function_">digest</span>(<span class="string">&#x27;hex&#x27;</span>),</span><br><span class="line">            <span class="attr">permissions</span>: <span class="string">&quot;*&quot;</span></span><br><span class="line">        &#125;</span><br><span class="line">    ]</span><br><span class="line">&#125;,</span><br></pre></td></tr></table></figure>

<p><strong>2. 确保 JWT Token 有效</strong></p>
<p>用户登录后获取的 JWT Token 需要能被 Node-RED 正确解析。如果使用外部鉴权系统，需要在 Node-RED 侧配置对应的 Token 验证策略。</p>
<hr>
<h2 id="功能开发"><a href="#功能开发" class="headerlink" title="功能开发"></a>功能开发</h2><h3 id="设备规则配置按钮实现"><a href="#设备规则配置按钮实现" class="headerlink" title="设备规则配置按钮实现"></a>设备规则配置按钮实现</h3><p>设备规则配置功能是本次开发的核心成果之一。该功能允许用户在设备规则列表页面直接点击”配置”按钮，通过 iframe 弹窗打开 Node-RED 流程编辑器，实现设备数据处理流程的可视化编排。</p>
<h4 id="技术方案"><a href="#技术方案" class="headerlink" title="技术方案"></a>技术方案</h4><p>该功能的核心实现思路是在 Avue 框架的表格操作列中新增自定义插槽，嵌入 iframe 组件加载外部流程编辑器。</p>
<p><strong>1. 自定义操作列插槽</strong></p>
<p>Avue 框架提供了 <code>#menu</code> 插槽，允许开发者自定义操作列按钮。我们在这个插槽中新增了”查看”、”配置”、”编辑”、”删除”四个按钮：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">template</span> #<span class="attr">menu</span>=<span class="string">&quot;&#123; row, index &#125;&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">el-button</span> <span class="attr">text</span> <span class="attr">type</span>=<span class="string">&quot;primary&quot;</span> <span class="attr">icon</span>=<span class="string">&quot;el-icon-view&quot;</span></span></span><br><span class="line"><span class="tag">             <span class="attr">v-if</span>=<span class="string">&quot;permission.engine_device_view&quot;</span></span></span><br><span class="line"><span class="tag">             @<span class="attr">click</span>=<span class="string">&quot;$refs.crud.rowView(row, index)&quot;</span>&gt;</span>查看<span class="tag">&lt;/<span class="name">el-button</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">el-button</span> <span class="attr">text</span> <span class="attr">type</span>=<span class="string">&quot;primary&quot;</span> <span class="attr">icon</span>=<span class="string">&quot;el-icon-setting&quot;</span></span></span><br><span class="line"><span class="tag">             <span class="attr">v-if</span>=<span class="string">&quot;permission.engine_device_edit&quot;</span></span></span><br><span class="line"><span class="tag">             @<span class="attr">click</span>=<span class="string">&quot;handleFlowConfig(row)&quot;</span>&gt;</span>配置<span class="tag">&lt;/<span class="name">el-button</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">el-button</span> <span class="attr">text</span> <span class="attr">type</span>=<span class="string">&quot;primary&quot;</span> <span class="attr">icon</span>=<span class="string">&quot;el-icon-edit&quot;</span></span></span><br><span class="line"><span class="tag">             <span class="attr">v-if</span>=<span class="string">&quot;permission.engine_device_edit&quot;</span></span></span><br><span class="line"><span class="tag">             @<span class="attr">click</span>=<span class="string">&quot;$refs.crud.rowEdit(row, index)&quot;</span>&gt;</span>编辑<span class="tag">&lt;/<span class="name">el-button</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">el-button</span> <span class="attr">text</span> <span class="attr">type</span>=<span class="string">&quot;primary&quot;</span> <span class="attr">icon</span>=<span class="string">&quot;el-icon-delete&quot;</span></span></span><br><span class="line"><span class="tag">             <span class="attr">v-if</span>=<span class="string">&quot;permission.engine_device_delete&quot;</span></span></span><br><span class="line"><span class="tag">             @<span class="attr">click</span>=<span class="string">&quot;$refs.crud.rowDel(row, index)&quot;</span>&gt;</span>删除<span class="tag">&lt;/<span class="name">el-button</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">template</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p><strong>2. iframe 弹窗组件</strong></p>
<p>使用 Element UI 的 <code>el-dialog</code> 组件实现弹窗效果：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">el-dialog</span> <span class="attr">v-model</span>=<span class="string">&quot;dialogFlowVisible&quot;</span> <span class="attr">title</span>=<span class="string">&quot;物联感知模型设计&quot;</span> <span class="attr">width</span>=<span class="string">&quot;90%&quot;</span> <span class="attr">top</span>=<span class="string">&quot;3vh&quot;</span></span></span><br><span class="line"><span class="tag">           <span class="attr">:close-on-click-modal</span>=<span class="string">&quot;false&quot;</span></span></span><br><span class="line"><span class="tag">           <span class="attr">class</span>=<span class="string">&quot;flow-dialog&quot;</span></span></span><br><span class="line"><span class="tag">           <span class="attr">:destroy-on-close</span>=<span class="string">&quot;true&quot;</span></span></span><br><span class="line"><span class="tag">           @<span class="attr">closed</span>=<span class="string">&quot;handleDialogClosed&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">iframe</span> <span class="attr">:src</span>=<span class="string">&quot;ruleEngineSrc&quot;</span> <span class="attr">class</span>=<span class="string">&quot;flow-engine-iframe&quot;</span> /&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">el-dialog</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>关键配置说明：</p>
<ul>
<li><code>destroy-on-close=&quot;true&quot;</code>：关闭弹窗时销毁 DOM，释放 iframe 占用的内存</li>
<li><code>close-on-click-modal=&quot;false&quot;</code>：防止误触遮罩导致编辑内容丢失</li>
<li><code>@closed</code> 事件：动画结束后清理 iframe src，停止后台请求</li>
</ul>
<p><strong>3. URL 拼接规则</strong></p>
<p>配置按钮点击后，拼接的 URL 格式为：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;edgeUrl&#125;/#flow/&#123;flowId&#125;?token=&#123;jwtToken&#125;</span><br></pre></td></tr></table></figure>

<table>
<thead>
<tr>
<th>组成部分</th>
<th>来源</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>edgeUrl</code></td>
<td>网站配置项</td>
<td>规则引擎服务地址，默认 <code>http://localhost:1880</code></td>
</tr>
<tr>
<td><code>flowId</code></td>
<td>设备规则关联的 Flow ID</td>
<td>后端在创建设备规则时自动创建并关联</td>
</tr>
<tr>
<td><code>jwtToken</code></td>
<td>用户登录态</td>
<td>用于规则引擎端的身份鉴权</td>
</tr>
</tbody></table>
<h4 id="内存管理考虑"><a href="#内存管理考虑" class="headerlink" title="内存管理考虑"></a>内存管理考虑</h4><p>iframe 长期驻留会占用大量浏览器内存，必须做好资源释放：</p>
<ul>
<li>使用 <code>destroy-on-close</code> 确保弹窗关闭时 DOM 被销毁</li>
<li>在 <code>@closed</code> 回调中主动清空 <code>ruleEngineSrc</code>，断开 iframe 的资源引用</li>
<li>对于频繁开关弹窗的场景，可以考虑增加节流保护</li>
</ul>
<h4 id="权限控制"><a href="#权限控制" class="headerlink" title="权限控制"></a>权限控制</h4><p>配置按钮复用 <code>engine_device_edit</code> 权限，与编辑按钮保持一致。这是因为配置操作本质上是对规则流程的编辑行为，权限级别应该与编辑按钮相同。</p>
<p><img data-src="/images/posts/nansha-iot-project-summary/fig4.jpg" alt="设备规则配置功能流程图"><br><em>图4：设备规则配置按钮 iframe 嵌入交互流程</em></p>
<hr>
<h2 id="扩展功能"><a href="#扩展功能" class="headerlink" title="扩展功能"></a>扩展功能</h2><h3 id="Node-RED-接入-TDengine"><a href="#Node-RED-接入-TDengine" class="headerlink" title="Node-RED 接入 TDengine"></a>Node-RED 接入 TDengine</h3><p>在完成基础功能开发后，我们探索了 Node-RED 流程编辑器接入 TDengine 时序数据库的方案。这是后续实现复杂数据处理流程的基础。</p>
<h4 id="技术选型"><a href="#技术选型" class="headerlink" title="技术选型"></a>技术选型</h4><p>Node-RED 官方提供了 <code>node-red-node-tdengine</code> 插件，但该插件依赖原生的 <code>bufferutil</code> 和 <code>node-gyp-build</code> 模块，在 Windows 环境下的 Node.js 20.x 版本中安装失败。</p>
<p>考虑到快速验证的需求，我们采用了<strong>自建本地节点包</strong>的方案，通过 TDengine 的 REST SQL 接口进行数据读写。</p>
<h4 id="实现方案"><a href="#实现方案" class="headerlink" title="实现方案"></a>实现方案</h4><p>在 Node-RED 项目中新增本地节点包 <code>node-red-contrib-tdengine</code>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">node-red-contrib-tdengine/</span><br><span class="line">├── package.json      # 包声明</span><br><span class="line">├── tdengine.js       # 节点运行时逻辑</span><br><span class="line">├── tdengine.html     # 节点配置界面</span><br><span class="line">└── icons/</span><br><span class="line">    └── db.svg        # 节点图标</span><br></pre></td></tr></table></figure>

<p>该节点包包含三个节点：</p>
<p><strong>tdengine（配置节点）</strong>：保存 TDengine 连接信息</p>
<table>
<thead>
<tr>
<th>配置项</th>
<th>默认值</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>host</td>
<td>127.0.0.1</td>
<td>TDengine 服务器地址</td>
</tr>
<tr>
<td>port</td>
<td>6041</td>
<td>REST 服务端口</td>
</tr>
<tr>
<td>database</td>
<td>—</td>
<td>默认数据库名</td>
</tr>
<tr>
<td>username</td>
<td>root</td>
<td>用户名</td>
</tr>
<tr>
<td>password</td>
<td>taosdata</td>
<td>密码</td>
</tr>
</tbody></table>
<p><strong>tdengine in（查询节点）</strong>：执行 SQL 查询并返回结果</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 使用示例</span></span><br><span class="line">msg.<span class="property">query</span> = <span class="string">&quot;select server_version()&quot;</span>;</span><br><span class="line"><span class="keyword">return</span> msg;</span><br></pre></td></tr></table></figure>

<p><strong>tdengine out（写入节点）</strong>：执行 INSERT&#x2F;UPDATE&#x2F;DELETE 等写入操作</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 使用示例</span></span><br><span class="line">msg.<span class="property">topic</span> = <span class="string">&quot;insert into test.d0 values (now, 10.3, 220, 0.31)&quot;</span>;</span><br><span class="line"><span class="keyword">return</span> msg;</span><br></pre></td></tr></table></figure>

<h4 id="验证测试"><a href="#验证测试" class="headerlink" title="验证测试"></a>验证测试</h4><p>完成节点开发后，我们执行了完整的读写测试：</p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 测试连接和版本查询</span></span><br><span class="line"><span class="built_in">select</span> server_version() → <span class="number">3.0</span>.<span class="number">7.1</span> ✓</span><br><span class="line"></span><br><span class="line"><span class="comment"># 测试建库</span></span><br><span class="line">create database <span class="keyword">if</span> not exists test ✓</span><br><span class="line"></span><br><span class="line"><span class="comment"># 测试建表</span></span><br><span class="line">create table <span class="keyword">if</span> not exists test.d0 (ts timestamp, current float, voltage int, phase float) ✓</span><br><span class="line"></span><br><span class="line"><span class="comment"># 测试写入</span></span><br><span class="line">insert into test.d0 values (now, <span class="number">10.3</span>, <span class="number">220</span>, <span class="number">0.31</span>) ✓</span><br><span class="line"></span><br><span class="line"><span class="comment"># 测试查询</span></span><br><span class="line"><span class="built_in">select</span> * from test.d0 order by ts desc limit <span class="number">5</span> ✓</span><br></pre></td></tr></table></figure>

<p><img data-src="/images/posts/nansha-iot-project-summary/fig5.jpg" alt="Node-RED接入TDengine架构图"><br><em>图5：Node-RED 通过 REST SQL 接口接入 TDengine 架构</em></p>
<h4 id="后续优化方向"><a href="#后续优化方向" class="headerlink" title="后续优化方向"></a>后续优化方向</h4><p>当前实现是最小可用版本，后续可以进一步增强：</p>
<ol>
<li><strong>对象 Payload 转换</strong>：参考 InfluxDB 节点的用户体验，支持 <code>msg.measurement</code>、<code>msg.payload</code> 格式的自动转换</li>
<li><strong>批量写入优化</strong>：支持数组格式的批量数据写入，提高写入吞吐量</li>
<li><strong>超级表支持</strong>：对接 TDengine 的超级表和标签功能，实现更灵活的数据模型</li>
<li><strong>查询结果缓存</strong>：对频繁查询的数据实现本地缓存，减少数据库压力</li>
</ol>
<hr>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><h3 id="环境配置要点"><a href="#环境配置要点" class="headerlink" title="环境配置要点"></a>环境配置要点</h3><p><strong>Nacos 配置中心</strong></p>
<ul>
<li>本地 Nacos 必须对齐项目的默认地址和端口设置</li>
<li>namespace 不能只看 <code>public</code>，业务配置通常在独立 namespace 下</li>
<li>Nacos 2.x 需要同时开通 gRPC 端口（9340&#x2F;9341），否则服务注册会失败</li>
<li>域名解析要稳定，建议在 hosts 文件中固定映射</li>
<li>代理环境变量要将 Nacos 域名加入白名单</li>
</ul>
<p><strong>TDengine 时序数据库</strong></p>
<ul>
<li>不同版本之间的数据文件不兼容，切换版本前务必清理旧数据目录</li>
<li>安装目录不能混装多个版本，二进制和动态库版本必须一致</li>
<li>REST 接口（taosAdapter）是跨语言访问的最佳选择</li>
<li>确认 <code>taosd</code> 和 <code>taosadapter</code> 两个服务都在运行</li>
</ul>
<h3 id="功能开发要点"><a href="#功能开发要点" class="headerlink" title="功能开发要点"></a>功能开发要点</h3><p><strong>前后端数据关联</strong></p>
<ul>
<li>REST API 返回的数据结构要与前端期望的格式一致</li>
<li>涉及多表关联的操作，后端要做好事务控制，避免半成功状态</li>
<li>前端调用外部服务时，要做好参数校验和错误处理</li>
</ul>
<p><strong>iframe 嵌入交互</strong></p>
<ul>
<li>使用 <code>destroy-on-close</code> 确保资源及时释放</li>
<li>避免在 iframe 内执行重量级操作，影响用户体验</li>
<li>考虑 iframe 与父页面的样式隔离问题</li>
</ul>
<h3 id="技术债务与改进方向"><a href="#技术债务与改进方向" class="headerlink" title="技术债务与改进方向"></a>技术债务与改进方向</h3><ol>
<li><strong>Node-RED 节点包增强</strong>：当前的自建节点包功能较简单，后续可以增加更丰富的配置选项和错误处理</li>
<li><strong>监控告警体系</strong>：目前缺乏针对中间件和业务服务的监控告警机制，需要补充</li>
<li><strong>自动化测试</strong>：核心业务逻辑缺少单元测试和集成测试覆盖</li>
<li><strong>文档沉淀</strong>：部分技术决策和配置细节散落在即时通讯记录中，需要系统整理</li>
</ol>
<hr>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>本次南沙物联网项目工作历时一个月，完成了从环境搭建到功能开发的全流程工作。在这段时间里，我们成功解决了 Nacos 本地环境配置、TDengine 版本兼容、设备规则配置功能开发等多个技术挑战，积累了宝贵的物联网项目实战经验。</p>
<p>项目当前已经具备了完整的本地开发环境，核心功能基本跑通。后续将继续完善数据处理流程、补充监控告警体系、加强自动化测试覆盖，将项目打造成稳定可靠的物联网平台。</p>
<p>感谢团队成员的通力协作，使得各项工作得以顺利推进。期待在下阶段工作中，能够继续保持高效协作，共同推动项目迈向新的里程碑。</p>
<hr>
<h2 id="相关文档"><a href="#相关文档" class="headerlink" title="相关文档"></a>相关文档</h2><ul>
<li>Nacos 本地环境与默认链路（本地 Obsidian 文档，未公开）</li>
<li>[TDengine 本地启动与版本兼容排障记录](.&#x2F;问题记录&#x2F;2026-03-25 TDengine本地启动与版本兼容排障记录.md)</li>
<li>[Nacos 本地链路修复记录](.&#x2F;问题记录&#x2F;2026-03-23 Nacos本地链路修复记录.md)</li>
<li>BladeX-Links 本机启动清单（本地 Obsidian 文档，未公开）</li>
<li>设备规则配置按钮需求分析（本地 Obsidian 文档，未公开）</li>
<li>设备规则配置按钮实现方案（本地 Obsidian 文档，未公开）</li>
<li>Node-RED 配置按钮与部署问题修复（本地 Obsidian 文档，未公开）</li>
<li>[TDengine 接入 Node-RED 方案梳理](.&#x2F;2026-04-13 TDengine接入Node-RED方案梳理.md)</li>
<li>[Node-RED 接入 TDengine 改动总结](.&#x2F;环境配置&#x2F;2026-04-14 Node-RED接入TDengine改动总结.md)</li>
</ul>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>南沙物联网</category>
      </categories>
      <tags>
        <tag>Nacos</tag>
        <tag>TDengine</tag>
        <tag>物联网</tag>
        <tag>Node-RED</tag>
        <tag>BladeX-Links</tag>
      </tags>
  </entry>
  <entry>
    <title>物联网平台设备规则配置按钮开发实录</title>
    <url>/2026/04/08/%E7%89%A9%E8%81%94%E7%BD%91%E5%B9%B3%E5%8F%B0%E8%AE%BE%E5%A4%87%E8%A7%84%E5%88%99%E9%85%8D%E7%BD%AE%E6%8C%89%E9%92%AE%E5%BC%80%E5%8F%91%E5%AE%9E%E5%BD%95/</url>
    <content><![CDATA[<p>在物联网平台中，设备规则的可视化配置是一个核心功能。本文记录了为设备规则页面新增”配置”按钮的完整实现过程，通过 iframe 嵌入外部规则引擎，实现拖拽式流程编排。</p>
<span id="more"></span>

<h2 id="背景与需求"><a href="#背景与需求" class="headerlink" title="背景与需求"></a>背景与需求</h2><p>在物联网平台中，设备规则的数据处理流程通常需要在独立的规则引擎管理页面进行配置。为了提升用户体验，产品同学提出在设备规则列表的操作列中直接添加”配置”按钮，点击即可打开可视化流程编排界面。</p>
<p><strong>核心需求</strong>：</p>
<ul>
<li>在设备规则列表操作列新增”配置”按钮</li>
<li>点击按钮通过 iframe 弹窗打开外部规则引擎（Node-RED 风格）</li>
<li>通过规则 ID 定位流程，用户 token 进行身份鉴权</li>
<li>确保弹窗关闭时正确释放 iframe 资源</li>
</ul>
<p><strong>技术选型</strong>：</p>
<ul>
<li>前端框架：Vue 3</li>
<li>UI 组件库：Avue（用于表格操作列）</li>
<li>嵌入方式：iframe</li>
<li>认证方式：Token 鉴权</li>
</ul>
<hr>
<h2 id="架构设计"><a href="#架构设计" class="headerlink" title="架构设计"></a>架构设计</h2><p>整体交互流程如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">用户点击&quot;配置&quot;按钮</span><br><span class="line">       |</span><br><span class="line">       v</span><br><span class="line">handleFlowConfig(row)</span><br><span class="line">       |</span><br><span class="line">       +--&gt; 拼接 URL: &#123;edgeUrl&#125;/#flow/&#123;row.id&#125;?token=&#123;getToken()&#125;</span><br><span class="line">       |</span><br><span class="line">       v</span><br><span class="line">dialogFlowVisible = true</span><br><span class="line">       |</span><br><span class="line">       v</span><br><span class="line">el-dialog 渲染 iframe (src = ruleEngineSrc)</span><br><span class="line">       |</span><br><span class="line">       v</span><br><span class="line">外部规则引擎服务加载并渲染流程编辑器</span><br><span class="line">       |</span><br><span class="line">       v</span><br><span class="line">用户关闭弹窗 --&gt; handleDialogClosed() --&gt; ruleEngineSrc = &#x27;&#x27;</span><br><span class="line">                                       --&gt; destroy-on-close 销毁 DOM</span><br></pre></td></tr></table></figure>

<p><strong>关键设计点</strong>：</p>
<ul>
<li>复用已有 <code>website.links.edgeUrl</code> 配置，无需新增配置项</li>
<li>使用 <code>destroy-on-close</code> 确保关闭时销毁 DOM</li>
<li>通过 <code>@closed</code> 事件清理 iframe src，防止内存泄漏</li>
</ul>
<p><img data-src="/images/posts/%E7%89%A9%E8%81%94%E7%BD%91%E5%B9%B3%E5%8F%B0%E8%AE%BE%E5%A4%87%E8%A7%84%E5%88%99%E9%85%8D%E7%BD%AE%E6%8C%89%E9%92%AE%E5%BC%80%E5%8F%91%E5%AE%9E%E5%BD%95/fig1.png" alt="架构流程图"><br><em>图1：iframe 嵌入弹窗交互流程</em></p>
<hr>
<h2 id="核心实现"><a href="#核心实现" class="headerlink" title="核心实现"></a>核心实现</h2><h3 id="1-自定义操作列插槽"><a href="#1-自定义操作列插槽" class="headerlink" title="1. 自定义操作列插槽"></a>1. 自定义操作列插槽</h3><p>使用 Avue 的 <code>#menu</code> 插槽替换默认操作按钮，新增四个功能按钮：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">template</span> #<span class="attr">menu</span>=<span class="string">&quot;&#123; row, index &#125;&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">el-button</span> <span class="attr">text</span> <span class="attr">type</span>=<span class="string">&quot;primary&quot;</span> <span class="attr">icon</span>=<span class="string">&quot;el-icon-view&quot;</span></span></span><br><span class="line"><span class="tag">             <span class="attr">v-if</span>=<span class="string">&quot;permission.engine_device_view&quot;</span></span></span><br><span class="line"><span class="tag">             @<span class="attr">click</span>=<span class="string">&quot;$refs.crud.rowView(row, index)&quot;</span>&gt;</span>查看<span class="tag">&lt;/<span class="name">el-button</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">el-button</span> <span class="attr">text</span> <span class="attr">type</span>=<span class="string">&quot;primary&quot;</span> <span class="attr">icon</span>=<span class="string">&quot;el-icon-setting&quot;</span></span></span><br><span class="line"><span class="tag">             <span class="attr">v-if</span>=<span class="string">&quot;permission.engine_device_edit&quot;</span></span></span><br><span class="line"><span class="tag">             @<span class="attr">click</span>=<span class="string">&quot;handleFlowConfig(row)&quot;</span>&gt;</span>配置<span class="tag">&lt;/<span class="name">el-button</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">el-button</span> <span class="attr">text</span> <span class="attr">type</span>=<span class="string">&quot;primary&quot;</span> <span class="attr">icon</span>=<span class="string">&quot;el-icon-edit&quot;</span></span></span><br><span class="line"><span class="tag">             <span class="attr">v-if</span>=<span class="string">&quot;permission.engine_device_edit&quot;</span></span></span><br><span class="line"><span class="tag">             @<span class="attr">click</span>=<span class="string">&quot;$refs.crud.rowEdit(row, index)&quot;</span>&gt;</span>编辑<span class="tag">&lt;/<span class="name">el-button</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">el-button</span> <span class="attr">text</span> <span class="attr">type</span>=<span class="string">&quot;primary&quot;</span> <span class="attr">icon</span>=<span class="string">&quot;el-icon-delete&quot;</span></span></span><br><span class="line"><span class="tag">             <span class="attr">v-if</span>=<span class="string">&quot;permission.engine_device_delete&quot;</span></span></span><br><span class="line"><span class="tag">             @<span class="attr">click</span>=<span class="string">&quot;$refs.crud.rowDel(row, index)&quot;</span>&gt;</span>删除<span class="tag">&lt;/<span class="name">el-button</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">template</span>&gt;</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p><strong>权限策略</strong>：配置按钮复用 <code>engine_device_edit</code> 权限，与编辑按钮保持一致的权限级别。</p>
</blockquote>
<p><img data-src="/images/posts/%E7%89%A9%E8%81%94%E7%BD%91%E5%B9%B3%E5%8F%B0%E8%AE%BE%E5%A4%87%E8%A7%84%E5%88%99%E9%85%8D%E7%BD%AE%E6%8C%89%E9%92%AE%E5%BC%80%E5%8F%91%E5%AE%9E%E5%BD%95/fig2.png" alt="权限控制按钮组"><br><em>图2：四个操作按钮（查看、配置、编辑、删除）</em></p>
<h3 id="2-iframe-弹窗组件"><a href="#2-iframe-弹窗组件" class="headerlink" title="2. iframe 弹窗组件"></a>2. iframe 弹窗组件</h3><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">el-dialog</span> <span class="attr">v-model</span>=<span class="string">&quot;dialogFlowVisible&quot;</span> <span class="attr">title</span>=<span class="string">&quot;物联感知模型设计&quot;</span> <span class="attr">width</span>=<span class="string">&quot;90%&quot;</span> <span class="attr">top</span>=<span class="string">&quot;3vh&quot;</span></span></span><br><span class="line"><span class="tag">           <span class="attr">:close-on-click-modal</span>=<span class="string">&quot;false&quot;</span></span></span><br><span class="line"><span class="tag">           <span class="attr">class</span>=<span class="string">&quot;flow-dialog&quot;</span></span></span><br><span class="line"><span class="tag">           <span class="attr">:destroy-on-close</span>=<span class="string">&quot;true&quot;</span></span></span><br><span class="line"><span class="tag">           @<span class="attr">closed</span>=<span class="string">&quot;handleDialogClosed&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">iframe</span> <span class="attr">:src</span>=<span class="string">&quot;ruleEngineSrc&quot;</span> <span class="attr">class</span>=<span class="string">&quot;flow-engine-iframe&quot;</span> /&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">el-dialog</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p><strong>关键参数说明</strong>：</p>
<table>
<thead>
<tr>
<th>参数</th>
<th>值</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td><code>destroy-on-close</code></td>
<td>true</td>
<td>关闭时销毁 DOM，释放 iframe 内存</td>
</tr>
<tr>
<td><code>close-on-click-modal</code></td>
<td>false</td>
<td>防止误触遮罩导致编辑内容丢失</td>
</tr>
<tr>
<td><code>width</code></td>
<td>90%</td>
<td>弹窗宽度占屏幕 90%</td>
</tr>
<tr>
<td><code>height</code></td>
<td>85vh</td>
<td>确保充足的编辑空间</td>
</tr>
</tbody></table>
<h3 id="3-核心方法实现"><a href="#3-核心方法实现" class="headerlink" title="3. 核心方法实现"></a>3. 核心方法实现</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="title function_">handleFlowConfig</span>(<span class="params">row</span>) &#123;</span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">ruleEngineSrc</span> = <span class="string">`<span class="subst">$&#123;website.links.edgeUrl&#125;</span>/#flow/<span class="subst">$&#123;row.id&#125;</span>?token=<span class="subst">$&#123;getToken()&#125;</span>`</span>;</span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">dialogFlowVisible</span> = <span class="literal">true</span>;</span><br><span class="line">&#125;,</span><br><span class="line"><span class="title function_">handleDialogClosed</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">ruleEngineSrc</span> = <span class="string">&#x27;&#x27;</span>;</span><br><span class="line">&#125;,</span><br></pre></td></tr></table></figure>

<p><strong>URL 格式</strong>：<code>&#123;edgeUrl&#125;/#flow/&#123;规则ID&#125;?token=&#123;用户token&#125;</code></p>
<table>
<thead>
<tr>
<th>组成部分</th>
<th>来源</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>website.links.edgeUrl</code></td>
<td><code>src/config/website.js</code></td>
<td>规则引擎服务地址，默认 <code>http://localhost:1880</code></td>
</tr>
<tr>
<td><code>/#flow/</code></td>
<td>路由路径</td>
<td>Node-RED 风格的流程编辑页面路由</td>
</tr>
<tr>
<td><code>&#123;row.id&#125;</code></td>
<td>当前行数据</td>
<td>设备规则的唯一标识</td>
</tr>
<tr>
<td><code>getToken()</code></td>
<td><code>src/utils/auth.js</code></td>
<td>从本地存储获取当前用户 token</td>
</tr>
</tbody></table>
<h3 id="4-Avue-配置调整"><a href="#4-Avue-配置调整" class="headerlink" title="4. Avue 配置调整"></a>4. Avue 配置调整</h3><p>使用 <code>#menu</code> 自定义插槽后，需要禁用 Avue 默认按钮：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="attr">viewBtn</span>: <span class="literal">false</span>,</span><br><span class="line"><span class="attr">editBtn</span>: <span class="literal">false</span>,</span><br><span class="line"><span class="attr">delBtn</span>: <span class="literal">false</span>,</span><br></pre></td></tr></table></figure>

<p>同时将 <code>menuWidth</code> 设置为 300，以容纳新增的”配置”按钮。</p>
<hr>
<h2 id="风险与应对"><a href="#风险与应对" class="headerlink" title="风险与应对"></a>风险与应对</h2><h3 id="跨域限制风险"><a href="#跨域限制风险" class="headerlink" title="跨域限制风险"></a>跨域限制风险</h3><blockquote>
<p>[!danger] iframe 嵌入受限<br>如果外部规则引擎服务设置了 <code>X-Frame-Options: DENY/SAMEORIGIN</code> 或 <code>Content-Security-Policy: frame-ancestors &#39;none&#39;</code>，iframe 将无法加载。</p>
</blockquote>
<p><strong>降级方案</strong>：将 iframe 嵌入改为 <code>window.open</code> 新窗口打开：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="title function_">handleFlowConfig</span>(<span class="params">row</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> url = <span class="string">`<span class="subst">$&#123;website.links.edgeUrl&#125;</span>/#flow/<span class="subst">$&#123;row.id&#125;</span>?token=<span class="subst">$&#123;getToken()&#125;</span>`</span>;</span><br><span class="line">  <span class="variable language_">window</span>.<span class="title function_">open</span>(url, <span class="string">&#x27;_blank&#x27;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="内存泄漏风险"><a href="#内存泄漏风险" class="headerlink" title="内存泄漏风险"></a>内存泄漏风险</h3><p>长时间打开多个 iframe 弹窗可能导致浏览器内存增长。已通过以下措施缓解：</p>
<ul>
<li><code>destroy-on-close: true</code> 关闭时销毁 DOM</li>
<li><code>@closed</code> 事件清理 <code>ruleEngineSrc</code>，停止 iframe 后台请求</li>
</ul>
<h3 id="权限一致性"><a href="#权限一致性" class="headerlink" title="权限一致性"></a>权限一致性</h3><p>使用 <code>#menu</code> 插槽后，Avue 内置的 <code>permissionList</code> 不再自动控制按钮显隐。操作列按钮的显隐完全由模板中的 <code>v-if=&quot;permission.xxx&quot;</code> 控制。</p>
<hr>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><ul>
<li><strong>最小侵入原则</strong>：只修改 <code>src/views/iot/engine/device.vue</code> 一个文件，通过插槽和配置扩展现有功能</li>
<li><strong>复用优先</strong>：复用了 <code>website.links.edgeUrl</code> 和 <code>getToken()</code>，无需新增配置</li>
<li><strong>防御式编程</strong>：考虑跨域限制，提供降级方案</li>
<li><strong>资源管理</strong>：iframe 必须及时清理，防止内存泄漏</li>
<li><strong>权限一致性</strong>：使用自定义插槽时，需注意 Avue 内置权限机制不再生效</li>
</ul>
<hr>
<h2 id="修改汇总"><a href="#修改汇总" class="headerlink" title="修改汇总"></a>修改汇总</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">src/views/iot/engine/device.vue  +35 行</span><br><span class="line">  - 模板：新增 #menu 插槽和 iframe 弹窗</span><br><span class="line">  - 脚本：新增 data 字段和两个方法</span><br><span class="line">  - 样式：iframe 尺寸样式</span><br></pre></td></tr></table></figure>

<p><strong>涉及文件</strong>：</p>
<table>
<thead>
<tr>
<th>文件</th>
<th>改动</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>src/views/iot/engine/device.vue</code></td>
<td>修改</td>
<td>唯一修改文件</td>
</tr>
<tr>
<td><code>src/config/website.js</code></td>
<td>无修改</td>
<td>复用已有配置</td>
</tr>
<tr>
<td><code>src/utils/auth.js</code></td>
<td>无修改</td>
<td>复用已有方法</td>
</tr>
</tbody></table>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>南沙物联网</category>
      </categories>
      <tags>
        <tag>Vue3</tag>
        <tag>Avue</tag>
        <tag>iframe</tag>
        <tag>规则引擎</tag>
        <tag>IoT</tag>
      </tags>
  </entry>
  <entry>
    <title>股票交易新手入门：从 YouTube 十大教学视频提炼的实战指南</title>
    <url>/2026/03/31/%E8%82%A1%E7%A5%A8%E4%BA%A4%E6%98%93%E6%96%B0%E6%89%8B%E5%85%A5%E9%97%A8%E6%8C%87%E5%8D%97/</url>
    <content><![CDATA[<p>基于 YouTube 50 个搜索结果的深度研究，精选 10 个高质量教学视频，覆盖 Jason Graystone、projectoption、Adam Khoo 等顶级教育频道。本文从风险管理、技术分析、交易心理三个维度，为零基础新手提炼出一条经过验证的股票交易学习路径。</p>
<span id="more"></span>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>股票交易不是简单的”买低卖高”，而是一项需要系统化学习的专业技能。很多人带着”快速致富”的幻想进入市场，结果往往是血本无归。</p>
<p>我花了大量时间研究了 YouTube 上 50 个炒股教学视频，通过多维度评分筛选出 10 个最优质的入门教学内容，并用 NotebookLM 进行了十维 AI 深度分析。本文将所有精华浓缩成一篇完整的入门指南，帮你少走弯路。</p>
<h2 id="研究概览"><a href="#研究概览" class="headerlink" title="研究概览"></a>研究概览</h2><table>
<thead>
<tr>
<th>项目</th>
<th>数据</th>
</tr>
</thead>
<tbody><tr>
<td>搜索范围</td>
<td>YouTube 50 个视频</td>
</tr>
<tr>
<td>精选分析</td>
<td>10 个高质量教学视频</td>
</tr>
<tr>
<td>覆盖频道</td>
<td>10 个不同专业频道</td>
</tr>
<tr>
<td>分析方法</td>
<td>NotebookLM 十维 AI 分析</td>
</tr>
<tr>
<td>研究方法</td>
<td>YouTube Pipeline + 多维度评分筛选</td>
</tr>
</tbody></table>
<p><img data-src="/images/posts/%E8%82%A1%E7%A5%A8%E4%BA%A4%E6%98%93%E6%96%B0%E6%89%8B%E5%85%A5%E9%97%A8%E6%8C%87%E5%8D%97/mindmap.png" alt="研究知识图谱"><br><em>图：炒股教学入门研究知识图谱</em></p>
<h2 id="一、五大核心发现"><a href="#一、五大核心发现" class="headerlink" title="一、五大核心发现"></a>一、五大核心发现</h2><p>通过 NotebookLM 的深度分析，从 10 个视频中提炼出五个最重要的入门要点：</p>
<h3 id="1-预定义每笔交易的风险和仓位"><a href="#1-预定义每笔交易的风险和仓位" class="headerlink" title="1. 预定义每笔交易的风险和仓位"></a>1. 预定义每笔交易的风险和仓位</h3><p>永远不要在没有设置三个订单的情况下进入交易：<strong>入场价、止损价、止盈目标</strong>。计算确切仓位，确保止损触发时损失不超过账户的 1-2%。始终追求 <strong>1:2 或 1:3</strong> 的风险收益比。</p>
<blockquote>
<p>专业交易者首先考虑的是”我能承受多少亏损”，而非”我能赚多少钱”。—— Adam Khoo</p>
</blockquote>
<h3 id="2-建立策略手册并大量回测"><a href="#2-建立策略手册并大量回测" class="headerlink" title="2. 建立策略手册并大量回测"></a>2. 建立策略手册并大量回测</h3><p>像职业运动一样对待交易，开发包含特定、可重复设置的策略手册。使用图表平台的”K 线回放”功能在历史数据上测试策略，验证具有<strong>正向期望值</strong>后再使用模拟账户执行。</p>
<h3 id="3-将亏损视为标准业务成本"><a href="#3-将亏损视为标准业务成本" class="headerlink" title="3. 将亏损视为标准业务成本"></a>3. 将亏损视为标准业务成本</h3><p>必须放弃”亏损 &#x3D; 失败”的心态。即使最精英的交易系统也会经历连续亏损。为了防止情绪破坏账户，保持详细的<strong>交易日志</strong>，记录执行质量、市场环境和情绪状态。</p>
<h3 id="4-交易是一项技能，不是快速致富计划"><a href="#4-交易是一项技能，不是快速致富计划" class="headerlink" title="4. 交易是一项技能，不是快速致富计划"></a>4. 交易是一项技能，不是快速致富计划</h3><p>掌握这项技能并实现持续盈利通常需要 <strong>12-18 个月</strong>的练习。新手应该期待这个学习曲线，并相应调整期望。</p>
<h3 id="5-交易你看到的，而非你认为的"><a href="#5-交易你看到的，而非你认为的" class="headerlink" title="5. 交易你看到的，而非你认为的"></a>5. 交易你看到的，而非你认为的</h3><p>严格遵守技术分析规则，不要让个人观点或情绪凌驾于图表信号之上。<strong>价格已经打折了所有新闻</strong>。</p>
<h2 id="二、风险管理：所有专家的共识"><a href="#二、风险管理：所有专家的共识" class="headerlink" title="二、风险管理：所有专家的共识"></a>二、风险管理：所有专家的共识</h2><p>所有 10 个视频的<strong>唯一共识</strong>是——保护本金比追求利润更重要。这不是可选项，而是交易的生存法则。</p>
<h3 id="核心原则"><a href="#核心原则" class="headerlink" title="核心原则"></a>核心原则</h3><table>
<thead>
<tr>
<th>原则</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>单笔风险限制</strong></td>
<td>不超过账户总额的 1-2%</td>
</tr>
<tr>
<td><strong>必须设止损</strong></td>
<td>每笔交易都设置自动止损订单</td>
</tr>
<tr>
<td><strong>风险收益比</strong></td>
<td>至少 1:2（冒 1 元风险争取 2 元收益）</td>
</tr>
<tr>
<td><strong>不满仓操作</strong></td>
<td>保持资金灵活性</td>
</tr>
</tbody></table>
<blockquote>
<p><strong>关键认知</strong>：你不需要高胜率也能盈利。40-50% 的胜率配合 1:3 的风险收益比，依然可以稳定盈利。</p>
</blockquote>
<h3 id="杠杆是双刃剑"><a href="#杠杆是双刃剑" class="headerlink" title="杠杆是双刃剑"></a>杠杆是双刃剑</h3><p>保证金账户、差价合约（CFD）和期权等工具可以放大收益，但<strong>同样放大损失</strong>。所有专家一致建议新手在建立稳固的交易系统之前，<strong>完全避免使用杠杆</strong>。</p>
<h2 id="三、技术分析入门"><a href="#三、技术分析入门" class="headerlink" title="三、技术分析入门"></a>三、技术分析入门</h2><p>技术分析是短期交易者的核心工具。以下是入门阶段需要掌握的关键概念：</p>
<h3 id="趋势识别"><a href="#趋势识别" class="headerlink" title="趋势识别"></a>趋势识别</h3><p>市场只有三种状态：<strong>上升趋势、下降趋势、震荡区间</strong>。识别趋势是所有交易决策的基础。</p>
<h3 id="核心分析工具"><a href="#核心分析工具" class="headerlink" title="核心分析工具"></a>核心分析工具</h3><table>
<thead>
<tr>
<th>工具</th>
<th>用途</th>
<th>来源</th>
</tr>
</thead>
<tbody><tr>
<td>K 线图</td>
<td>显示价格走势</td>
<td>Jason Graystone</td>
</tr>
<tr>
<td>移动平均线</td>
<td>识别趋势方向</td>
<td>Adam Khoo</td>
</tr>
<tr>
<td>支撑&#x2F;阻力位</td>
<td>判断价格关键位置</td>
<td>Jason Graystone</td>
</tr>
<tr>
<td>RSI</td>
<td>衡量超买超卖</td>
<td>Jason Graystone</td>
</tr>
<tr>
<td>MACD</td>
<td>趋势变化信号</td>
<td>Adam Khoo</td>
</tr>
<tr>
<td>成交量</td>
<td>确认趋势强度</td>
<td>SMB Capital</td>
</tr>
<tr>
<td>斐波那契回撤</td>
<td>预测支撑阻力位</td>
<td>Jason Graystone</td>
</tr>
</tbody></table>
<h3 id="分析方法的分歧"><a href="#分析方法的分歧" class="headerlink" title="分析方法的分歧"></a>分析方法的分歧</h3><p>有趣的是，专家们在技术指标的使用上存在分歧：</p>
<ul>
<li><strong>极简派</strong>（Jason Graystone）：主张裸图交易，越少指标越好，认为太多指标会导致分析瘫痪</li>
<li><strong>综合派</strong>（Adam Khoo）：主张多指标确认，认为单一指标容易产生假信号</li>
</ul>
<blockquote>
<p><strong>建议</strong>：新手应该尝试两种方法，找到适合自己的风格。这不是对错问题，而是交易风格的差异。</p>
</blockquote>
<h2 id="四、交易心理学：决定成败的隐形因素"><a href="#四、交易心理学：决定成败的隐形因素" class="headerlink" title="四、交易心理学：决定成败的隐形因素"></a>四、交易心理学：决定成败的隐形因素</h2><p>技术分析工具固然重要，但<strong>交易心理学往往是决定成败的关键因素</strong>。SMB Capital 在其近 2 小时的权威指南中花了整整 30 分钟讲解心理因素。</p>
<h3 id="四大心理陷阱"><a href="#四大心理陷阱" class="headerlink" title="四大心理陷阱"></a>四大心理陷阱</h3><table>
<thead>
<tr>
<th>心理陷阱</th>
<th>表现</th>
<th>危害</th>
</tr>
</thead>
<tbody><tr>
<td><strong>确认偏误</strong></td>
<td>只寻找支持自己观点的信息</td>
<td>忽视风险信号</td>
</tr>
<tr>
<td><strong>锚定效应</strong></td>
<td>过度依赖买入价格做决策</td>
<td>不愿止损</td>
</tr>
<tr>
<td><strong>损失厌恶</strong></td>
<td>对亏损的痛苦远强于盈利的快乐</td>
<td>死扛亏损单</td>
</tr>
<tr>
<td><strong>FOMO</strong></td>
<td>害怕错过导致追高买入</td>
<td>高位接盘</td>
</tr>
</tbody></table>
<h3 id="心理训练方法"><a href="#心理训练方法" class="headerlink" title="心理训练方法"></a>心理训练方法</h3><ol>
<li><strong>交易日志</strong>：不仅记录交易数据，更要记录当时的<strong>情绪状态</strong>和决策原因</li>
<li><strong>接受亏损</strong>：将亏损视为做生意的”运营成本”，而非个人失败</li>
<li><strong>纪律优先</strong>：成功的交易者不一定是智商最高的人，但一定是最有纪律的人</li>
</ol>
<blockquote>
<p><strong>异常观点</strong>：Craig Percoco 认为，如果交易者在市场上没有统计优势，心理学根本不重要。交易者经常错误地把亏损归咎于情绪，而实际根本原因是缺乏真正有效的交易系统。</p>
</blockquote>
<h2 id="五、三大交易风格对比"><a href="#五、三大交易风格对比" class="headerlink" title="五、三大交易风格对比"></a>五、三大交易风格对比</h2><p>根据研究，股票交易主要涵盖三种风格：</p>
<table>
<thead>
<tr>
<th>交易风格</th>
<th>时间投入</th>
<th>资金要求</th>
<th>适合人群</th>
</tr>
</thead>
<tbody><tr>
<td><strong>长期投资</strong></td>
<td>低（每周几小时）</td>
<td>中等</td>
<td>上班族、稳健型</td>
</tr>
<tr>
<td><strong>波段交易</strong></td>
<td>中等（每天 1-2 小时）</td>
<td>中等</td>
<td>有一定时间研究市场</td>
</tr>
<tr>
<td><strong>日内交易</strong></td>
<td>高（全天盯盘）</td>
<td>高</td>
<td>全职交易者</td>
</tr>
</tbody></table>
<blockquote>
<p><strong>重要建议</strong>：大多数新手应该从长期投资或波段交易开始，而非日内交易。日内交易虽然”看起来很酷”，但失败率极高。</p>
</blockquote>
<h2 id="六、推荐学习路径"><a href="#六、推荐学习路径" class="headerlink" title="六、推荐学习路径"></a>六、推荐学习路径</h2><p>根据所有专家的建议，我整理了一条循序渐进的入门路径：</p>
<h3 id="阶段-1：建立基础（1-2-周）"><a href="#阶段-1：建立基础（1-2-周）" class="headerlink" title="阶段 1：建立基础（1-2 周）"></a>阶段 1：建立基础（1-2 周）</h3><ul>
<li><input disabled="" type="checkbox"> 观看 TED-Ed 的《How does the stock market work?》（4 分钟）</li>
<li><input disabled="" type="checkbox"> 学习基础术语：K 线、均线、支撑阻力、止损止盈</li>
<li><input disabled="" type="checkbox"> 开通模拟交易账户（推荐 TradingView 免费版）</li>
</ul>
<p><img data-src="/images/posts/%E8%82%A1%E7%A5%A8%E4%BA%A4%E6%98%93%E6%96%B0%E6%89%8B%E5%85%A5%E9%97%A8%E6%8C%87%E5%8D%97/video-jason-graystone.webp" alt="Jason Graystone 教学视频"><br><em>图：Jason Graystone 的 2 小时完整交易课程，最系统化的免费入门教程</em></p>
<h3 id="阶段-2：技术学习（1-3-个月）"><a href="#阶段-2：技术学习（1-3-个月）" class="headerlink" title="阶段 2：技术学习（1-3 个月）"></a>阶段 2：技术学习（1-3 个月）</h3><ul>
<li><input disabled="" type="checkbox"> 系统学习技术分析（观看 Jason Graystone 完整课程）</li>
<li><input disabled="" type="checkbox"> 在模拟账户中练习 K 线形态和指标</li>
<li><input disabled="" type="checkbox"> 建立自己的策略手册</li>
</ul>
<h3 id="阶段-3：模拟实战（3-6-个月）"><a href="#阶段-3：模拟实战（3-6-个月）" class="headerlink" title="阶段 3：模拟实战（3-6 个月）"></a>阶段 3：模拟实战（3-6 个月）</h3><ul>
<li><input disabled="" type="checkbox"> 在模拟账户中严格执行策略</li>
<li><input disabled="" type="checkbox"> 记录详细交易日志</li>
<li><input disabled="" type="checkbox"> 验证策略具有正向期望值</li>
</ul>
<p><img data-src="/images/posts/%E8%82%A1%E7%A5%A8%E4%BA%A4%E6%98%93%E6%96%B0%E6%89%8B%E5%85%A5%E9%97%A8%E6%8C%87%E5%8D%97/video-adam-khoo.webp" alt="Adam Khoo 教学视频"><br><em>图：Adam Khoo 的专业股票交易课程，融合东西方投资理念</em></p>
<h3 id="阶段-4：小额实战（6-12-个月）"><a href="#阶段-4：小额实战（6-12-个月）" class="headerlink" title="阶段 4：小额实战（6-12 个月）"></a>阶段 4：小额实战（6-12 个月）</h3><ul>
<li><input disabled="" type="checkbox"> 用可承受损失的小额资金开始真实交易</li>
<li><input disabled="" type="checkbox"> 只有连续 3-6 个月模拟盈利后才考虑增加资金</li>
<li><input disabled="" type="checkbox"> 持续记录和分析交易</li>
</ul>
<blockquote>
<p><strong>如果你在模拟账户中无法稳定盈利，你就不应该在真实账户中交易。</strong> —— ClearValue Tax</p>
</blockquote>
<h2 id="七、重点创作者推荐"><a href="#七、重点创作者推荐" class="headerlink" title="七、重点创作者推荐"></a>七、重点创作者推荐</h2><h3 id="Jason-Graystone"><a href="#Jason-Graystone" class="headerlink" title="Jason Graystone"></a>Jason Graystone</h3><p><img data-src="/images/posts/%E8%82%A1%E7%A5%A8%E4%BA%A4%E6%98%93%E6%96%B0%E6%89%8B%E5%85%A5%E9%97%A8%E6%8C%87%E5%8D%97/video-projectoption.webp" alt="projectoption 教学视频"><br><em>图：projectoption 的期权交易终极指南（1756 万观看）</em></p>
<p>专注于日内交易和波段交易的技术分析教学，风格直接务实。他提出的”价格行动交易法”强调通过裸图理解市场，而非依赖复杂指标。<strong>最完整的免费入门课程之一。</strong></p>
<h3 id="projectoption"><a href="#projectoption" class="headerlink" title="projectoption"></a>projectoption</h3><p>期权交易教学的权威，近 3 小时的终极指南以 1756 万观看量证明了其质量。不同于许多只讲买方策略的教程，他详细讲解了风险更低的卖方策略。</p>
<h3 id="Adam-Khoo"><a href="#Adam-Khoo" class="headerlink" title="Adam Khoo"></a>Adam Khoo</h3><p>新加坡知名投资教育家，融合东西方投资理念。他提出的投资成功三大支柱：<strong>知识（20%）+ 心态（30%）+ 系统（50%）</strong>。</p>
<h3 id="SMB-Capital"><a href="#SMB-Capital" class="headerlink" title="SMB Capital"></a>SMB Capital</h3><p>来自专业交易公司的权威内容，最大的特点是<strong>机构级思维</strong>。区分”好的交易”（按规则执行）和”赚钱的交易”（结果导向），长期来看前者必然导致后者。</p>
<h3 id="CA-Rachana-Phadke-Ranade"><a href="#CA-Rachana-Phadke-Ranade" class="headerlink" title="CA Rachana Phadke Ranade"></a>CA Rachana Phadke Ranade</h3><p><img data-src="/images/posts/%E8%82%A1%E7%A5%A8%E4%BA%A4%E6%98%93%E6%96%B0%E6%89%8B%E5%85%A5%E9%97%A8%E6%8C%87%E5%8D%97/video-rachana.webp" alt="CA Rachana 教学视频"><br><em>图：CA Rachana Phadke Ranade 的股票市场基础课（3179 万观看）</em></p>
<p>印度市场专家，以通俗易懂的方式讲解财务概念。她的名言：**”如果你不能向一个 12 岁的孩子解释你的投资策略，那说明你自己也不理解。”**</p>
<h2 id="八、推荐工具与资源"><a href="#八、推荐工具与资源" class="headerlink" title="八、推荐工具与资源"></a>八、推荐工具与资源</h2><h3 id="交易平台"><a href="#交易平台" class="headerlink" title="交易平台"></a>交易平台</h3><table>
<thead>
<tr>
<th>工具</th>
<th>用途</th>
<th>备注</th>
</tr>
</thead>
<tbody><tr>
<td>TradingView</td>
<td>图表分析 + 模拟交易</td>
<td>免费版功能强大</td>
</tr>
<tr>
<td>ThinkorSwim</td>
<td>专业模拟交易</td>
<td>功能最全，学习曲线陡峭</td>
</tr>
<tr>
<td>Investopedia</td>
<td>金融术语学习</td>
<td>最全面的免费金融百科</td>
</tr>
</tbody></table>
<h3 id="推荐书籍"><a href="#推荐书籍" class="headerlink" title="推荐书籍"></a>推荐书籍</h3><table>
<thead>
<tr>
<th>书名</th>
<th>作者</th>
<th>难度</th>
</tr>
</thead>
<tbody><tr>
<td>《聪明的投资者》</td>
<td>Benjamin Graham</td>
<td>入门</td>
</tr>
<tr>
<td>《漫步华尔街》</td>
<td>Burton Malkiel</td>
<td>入门</td>
</tr>
<tr>
<td>《日本蜡烛图技术》</td>
<td>Steve Nison</td>
<td>进阶</td>
</tr>
<tr>
<td>《交易心理分析》</td>
<td>Mark Douglas</td>
<td>进阶</td>
</tr>
</tbody></table>
<h2 id="九、新手常见错误"><a href="#九、新手常见错误" class="headerlink" title="九、新手常见错误"></a>九、新手常见错误</h2><table>
<thead>
<tr>
<th>错误</th>
<th>后果</th>
<th>解决方案</th>
</tr>
</thead>
<tbody><tr>
<td><strong>不设止损</strong></td>
<td>单笔亏损清空账户</td>
<td>每笔交易必设止损</td>
</tr>
<tr>
<td><strong>过度交易</strong></td>
<td>增加成本，降低胜率</td>
<td>减少交易频率</td>
</tr>
<tr>
<td><strong>使用杠杆</strong></td>
<td>放大损失风险</td>
<td>新手避免杠杆</td>
</tr>
<tr>
<td><strong>追逐热门</strong></td>
<td>FOMO 导致高位接盘</td>
<td>遵循系统，不追涨</td>
</tr>
<tr>
<td><strong>没有计划</strong></td>
<td>随机交易</td>
<td>建立策略手册</td>
</tr>
<tr>
<td><strong>忽视心理</strong></td>
<td>情绪失控</td>
<td>坚持写交易日志</td>
</tr>
</tbody></table>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><ul>
<li><strong>风险管理是不可协商的</strong>——所有专家的唯一共识</li>
<li><strong>模拟交易是必须的</strong>——模拟账户无法盈利就不要碰真实账户</li>
<li><strong>12-18 个月学习曲线</strong>——交易是技能，不是彩票</li>
<li><strong>交易心理学和技术分析同等重要</strong>——管住自己比分析图表更难</li>
<li><strong>免费资源已经非常丰富</strong>——不必一开始就购买昂贵课程</li>
</ul>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>股票交易入门没有捷径，但有正确的方法。与其在市场中交学费，不如先花时间系统学习。记住 Jason Graystone 的话：”大多数新手失败不是因为他们不够聪明，而是因为他们试图在理解基础之前就开始交易。”</p>
<p>祝你在交易之路上稳步前行！</p>
<hr>
<blockquote>
<p><strong>免责声明</strong>：本文仅供教育和信息目的，不构成投资建议。股票交易涉及重大风险，可能导致本金损失。在做出任何投资决定之前，请咨询合格的财务顾问。</p>
</blockquote>
<hr>
<p><strong>参考资料</strong>：本研究基于 YouTube 10 个精选教学视频的深度分析，总观看量超过 8000 万。完整视频列表和引用资源可在文末评论区获取。</p>
]]></content>
      <categories>
        <category>投资理财</category>
      </categories>
      <tags>
        <tag>股票入门</tag>
        <tag>投资学习</tag>
        <tag>风险管理</tag>
        <tag>技术分析</tag>
        <tag>交易心理</tag>
      </tags>
  </entry>
  <entry>
    <title>Java 后端实战：视频接口在线离线统计功能设计与实现</title>
    <url>/2026/05/11/%E8%A7%86%E9%A2%91%E5%88%97%E8%A1%A8%E6%8E%A5%E5%8F%A3%E5%9C%A8%E7%BA%BF%E7%A6%BB%E7%BA%BF%E7%BB%9F%E8%AE%A1/</url>
    <content><![CDATA[<p>在视频监控模块中，前端列表页需要展示在线&#x2F;离线摄像头的全局总数。本文记录了在现有 <code>/api/video/list</code> 接口中添加 <code>onlineCount</code> 和 <code>offlineCount</code> 字段的完整设计与实现过程，包括方案对比、核心代码实现以及开发中遇到的两个典型问题。</p>
<div class="audio-player">
  <div class="audio-section">
    <h4>🎧 文章导读</h4>
    <audio controls preload="metadata">
      <source src="/audio/视频列表接口在线离线统计/narration.mp3" type="audio/mpeg">
    </audio>
  </div>
  <div class="audio-section">
    <h4>🎵 背景音乐</h4>
    <audio controls loop preload="metadata">
      <source src="/audio/视频列表接口在线离线统计/bgm.mp3" type="audio/mpeg">
    </audio>
  </div>
</div>

<span id="more"></span>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>视频监控模块的列表页面有一个常见需求：展示在线和离线摄像头的总数。原有的 <code>/api/video/list</code> 接口只返回过滤后的视频列表，前端无法得知全局的在线&#x2F;离线视频总数。</p>
<p>这个需求看起来简单，但有一个关键点：<strong>统计数据必须基于全量数据，而不是过滤后的子集</strong>。也就是说，无论用户当前筛选了什么条件（只看在线视频、按关键字搜索），<code>onlineCount</code> 和 <code>offlineCount</code> 始终反映全局数据。</p>
<hr>
<h2 id="方案对比"><a href="#方案对比" class="headerlink" title="方案对比"></a>方案对比</h2><p><img data-src="/images/posts/%E8%A7%86%E9%A2%91%E5%88%97%E8%A1%A8%E6%8E%A5%E5%8F%A3%E5%9C%A8%E7%BA%BF%E7%A6%BB%E7%BA%BF%E7%BB%9F%E8%AE%A1/architecture-comparison.png" alt="Controller 层 vs Service 层架构对比"><br><em>图1：两种实现方案的架构对比</em></p>
<p>在设计阶段，我考虑了两种实现方案：</p>
<table>
<thead>
<tr>
<th>方案</th>
<th>实现位置</th>
<th>改动范围</th>
<th>优点</th>
<th>缺点</th>
</tr>
</thead>
<tbody><tr>
<td><strong>方案A：Controller 层处理</strong></td>
<td><code>VideoController.java</code></td>
<td>仅改一个文件</td>
<td>改动最小，逻辑集中</td>
<td>每次请求获取全量数据</td>
</tr>
<tr>
<td><strong>方案B：Service 层统计方法</strong></td>
<td>新增 <code>getVideoStats()</code></td>
<td>改 Service + DTO</td>
<td>职责分离更清晰</td>
<td>改动较大，过度设计</td>
</tr>
</tbody></table>
<p>最终选择了<strong>方案A</strong>，理由是：</p>
<ul>
<li>视频数据量通常在几百到几千条，全量查询的性能开销可以接受</li>
<li>只改一个文件，逻辑集中在一个方法里，容易理解和维护</li>
<li>方案B 需要修改 Service 层和可能的 DTO，对于这个简单需求来说过度设计</li>
</ul>
<blockquote>
<p><strong>后续优化思路</strong>：如果数据量增长导致性能问题，可以在 Service 层加缓存或改用数据库 <code>COUNT</code> 查询。</p>
</blockquote>
<hr>
<h2 id="核心实现"><a href="#核心实现" class="headerlink" title="核心实现"></a>核心实现</h2><h3 id="接口设计"><a href="#接口设计" class="headerlink" title="接口设计"></a>接口设计</h3><p>接口地址不变，仍然是 <code>GET /api/video/list</code>，返回数据中新增两个字段：</p>
<table>
<thead>
<tr>
<th>字段</th>
<th>类型</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>onlineCount</code></td>
<td><code>long</code></td>
<td>全量数据中在线视频数量</td>
</tr>
<tr>
<td><code>offlineCount</code></td>
<td><code>long</code></td>
<td>全量数据中离线视频数量</td>
</tr>
</tbody></table>
<p><strong>关键约束</strong>：这两个字段的统计范围是全量数据，不受 <code>isOnline</code>、<code>keyword</code>、<code>pageNum</code>、<code>pageSize</code> 参数影响。</p>
<h3 id="实现逻辑"><a href="#实现逻辑" class="headerlink" title="实现逻辑"></a>实现逻辑</h3><p>实现分为四步：<strong>获取全量数据 → 统计 → 过滤 → 分页返回</strong>。</p>
<p>核心代码在 <code>VideoController.java</code> 的 <code>getVideoList</code> 方法中：</p>
<p><img data-src="/images/posts/%E8%A7%86%E9%A2%91%E5%88%97%E8%A1%A8%E6%8E%A5%E5%8F%A3%E5%9C%A8%E7%BA%BF%E7%A6%BB%E7%BA%BF%E7%BB%9F%E8%AE%A1/code-stream.png" alt="Java Stream API 实现在线离线统计"><br><em>图2：使用 Stream API 统计在线&#x2F;离线数量</em></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 1. 获取全量数据用于统计在线/离线数量</span></span><br><span class="line"><span class="type">VideoListDTO</span> <span class="variable">allDataResult</span> <span class="operator">=</span> videoService.getVideoList(<span class="literal">null</span>);</span><br><span class="line">List&lt;VideoListDTO.VideoInfo&gt; allVideoList = allDataResult != <span class="literal">null</span> &amp;&amp; allDataResult.getData() != <span class="literal">null</span></span><br><span class="line">        ? allDataResult.getData()</span><br><span class="line">        : Collections.emptyList();</span><br><span class="line"></span><br><span class="line"><span class="comment">// 2. 统计全量数据的在线/离线数量（status == 1 为在线）</span></span><br><span class="line"><span class="type">long</span> <span class="variable">onlineCount</span> <span class="operator">=</span> allVideoList.stream()</span><br><span class="line">        .filter(v -&gt; v.getStatus() != <span class="literal">null</span> &amp;&amp; v.getStatus() == <span class="number">1</span>)</span><br><span class="line">        .count();</span><br><span class="line"><span class="type">long</span> <span class="variable">offlineCount</span> <span class="operator">=</span> allVideoList.size() - onlineCount;</span><br></pre></td></tr></table></figure>

<p><code>videoService.getVideoList(null)</code> 传 <code>null</code> 表示不按在线状态过滤，获取全部数据。然后通过 Java Stream 统计 <code>status == 1</code> 的数量作为在线数，总数减去在线数即为离线数。</p>
<p>接下来按参数过滤、分页，最后在返回结果中带上统计字段：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">return</span> AjaxResult.success(<span class="string">&quot;获取视频列表成功&quot;</span>, pagedList)</span><br><span class="line">        .put(<span class="string">&quot;total&quot;</span>, total)</span><br><span class="line">        .put(<span class="string">&quot;pageNum&quot;</span>, normalizedPageNum)</span><br><span class="line">        .put(<span class="string">&quot;pageSize&quot;</span>, normalizedPageSize)</span><br><span class="line">        .put(<span class="string">&quot;onlineCount&quot;</span>, onlineCount)</span><br><span class="line">        .put(<span class="string">&quot;offlineCount&quot;</span>, offlineCount);</span><br></pre></td></tr></table></figure>

<blockquote>
<p><strong>注意区分</strong>：<code>total</code> 是过滤后的总数，<code>onlineCount</code>&#x2F;<code>offlineCount</code> 是全量统计，两者含义不同。</p>
</blockquote>
<hr>
<h2 id="测试验证"><a href="#测试验证" class="headerlink" title="测试验证"></a>测试验证</h2><p>可以通过以下场景验证功能正确性：</p>
<table>
<thead>
<tr>
<th>场景</th>
<th>请求</th>
<th>预期结果</th>
</tr>
</thead>
<tbody><tr>
<td>无参数调用</td>
<td><code>GET /api/video/list</code></td>
<td><code>onlineCount + offlineCount</code> 等于列表长度</td>
</tr>
<tr>
<td>只看在线</td>
<td><code>GET /api/video/list?isOnline=1</code></td>
<td><code>data</code> 只包含在线视频，统计字段不受影响</td>
</tr>
<tr>
<td>关键字搜索</td>
<td><code>GET /api/video/list?keyword=xxx</code></td>
<td><code>data</code> 是搜索结果，统计字段不受影响</td>
</tr>
<tr>
<td>分页</td>
<td><code>GET /api/video/list?pageNum=1&amp;pageSize=5</code></td>
<td><code>data</code> 最多 5 条，统计字段不受影响</td>
</tr>
</tbody></table>
<hr>
<h2 id="踩坑记录"><a href="#踩坑记录" class="headerlink" title="踩坑记录"></a>踩坑记录</h2><h3 id="坑1：错误创建-Mapper-XML-导致启动失败"><a href="#坑1：错误创建-Mapper-XML-导致启动失败" class="headerlink" title="坑1：错误创建 Mapper XML 导致启动失败"></a>坑1：错误创建 Mapper XML 导致启动失败</h3><p>在之前的开发中，我凭猜测创建了 <code>StReachInfraMapper.xml</code> 文件（以为缺少 SQL 映射），结果 Spring Boot 启动时报错：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">IOException: Failed to parse mapping resource</span><br></pre></td></tr></table></figure>

<p>删除该文件后恢复正常。</p>
<blockquote>
<p><strong>经验教训</strong>：在不确定是否真正缺少某个文件时，应该先验证接口是否能正常工作，而不是凭猜测创建文件。先跑通再补全，比先补全再排查要高效得多。</p>
</blockquote>
<h3 id="坑2：PUT-请求-405-错误"><a href="#坑2：PUT-请求-405-错误" class="headerlink" title="坑2：PUT 请求 405 错误"></a>坑2：PUT 请求 405 错误</h3><p>调用 <code>PUT /basic/station/archive/reachInfra</code> 时报错 <code>不支持&#39;PUT&#39;请求</code>。</p>
<p>排查后发现原因是调用方<strong>漏掉了路径参数</strong> <code>stcd</code>，正确的路径应该是：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">PUT /basic/station/archive/reachInfra/&#123;stcd&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p><strong>经验教训</strong>：遇到 405 错误，除了检查 HTTP 方法是否正确，还要检查 URL 路径是否完整，特别是路径参数。</p>
</blockquote>
<hr>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>这个改动的核心设计决策是**”统计全量、过滤返回”**——<code>onlineCount</code> 和 <code>offlineCount</code> 始终反映全局数据，不受任何筛选条件影响。</p>
<p>从实现来看，方案A 确实是最小改动方案，只改了 <code>VideoController.java</code> 一个文件。如果后续性能成为瓶颈，可以考虑在 Service 层添加专门的统计方法或缓存。</p>
<p><strong>相关文件</strong>：</p>
<table>
<thead>
<tr>
<th>文件</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>cnsci-admin/.../video/VideoController.java</code></td>
<td>接口层，主要改动文件</td>
</tr>
<tr>
<td><code>cnsci-admin/.../dto/video/VideoListDTO.java</code></td>
<td>视频列表 DTO，未改动</td>
</tr>
</tbody></table>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>数据归集</category>
      </categories>
      <tags>
        <tag>Spring Boot</tag>
        <tag>Java</tag>
        <tag>视频监控</tag>
        <tag>API设计</tag>
        <tag>数据统计</tag>
      </tags>
  </entry>
  <entry>
    <title>视频平台图片鉴权接口开发踩坑记录</title>
    <url>/2026/03/06/%E8%A7%86%E9%A2%91%E5%B9%B3%E5%8F%B0%E5%9B%BE%E7%89%87%E9%89%B4%E6%9D%83%E6%8E%A5%E5%8F%A3%E5%BC%80%E5%8F%91%E8%B8%A9%E5%9D%91%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<p>在水文监测后台管理系统中，视频设备接口需要返回缩略图鉴权信息，看似简单的需求却踩了几个坑：双重鉴权体系混淆、API 响应类型转换失败、缓存时间设计不当……本文记录完整的开发过程与问题解决。</p>
<span id="more"></span>

<h2 id="问题背景"><a href="#问题背景" class="headerlink" title="问题背景"></a>问题背景</h2><p>项目需要为 <code>api/station/material/videoDevices</code> 接口增加 <code>thumbnailAuth</code> 字段，用于返回访问缩略图图片所需的鉴权信息。</p>
<p>根据广东省水利视频共享接口文档：</p>
<ul>
<li>图片鉴权接口：<code>/lisa/access/sign</code>（POST）</li>
<li>鉴权有效期：10 分钟</li>
<li>返回字段：<code>x-apig-nonce</code>、<code>x-apig-timestamp</code>、<code>x-apig-appid</code>、<code>x-apig-sign</code>、<code>x-datax-code</code></li>
</ul>
<p>看起来是个简单的需求，但实际开发中踩了不少坑。</p>
<h2 id="双重鉴权体系"><a href="#双重鉴权体系" class="headerlink" title="双重鉴权体系"></a>双重鉴权体系</h2><p>视频平台包含<strong>两套并行的鉴权逻辑</strong>，这是理解整个问题的关键：</p>
<table>
<thead>
<tr>
<th>鉴权类型</th>
<th>用途</th>
<th>接口</th>
<th>有效期</th>
</tr>
</thead>
<tbody><tr>
<td>Token 鉴权</td>
<td>调用视频列表、播放、云台控制等接口</td>
<td><code>/resourceSharing/oauth/token</code></td>
<td>25 分钟</td>
</tr>
<tr>
<td>Sign 鉴权</td>
<td>访问缩略图、告警图片等资源</td>
<td><code>/lisa/access/sign</code></td>
<td>10 分钟</td>
</tr>
</tbody></table>
<p><strong>关键点</strong>：Sign 鉴权接口本身也需要 Token 鉴权才能调用！这个细节在文档中没有明确说明，导致后续踩坑。</p>
<h2 id="踩坑过程"><a href="#踩坑过程" class="headerlink" title="踩坑过程"></a>踩坑过程</h2><h3 id="坑一：请求缺少-Token"><a href="#坑一：请求缺少-Token" class="headerlink" title="坑一：请求缺少 Token"></a>坑一：请求缺少 Token</h3><p><strong>现象</strong>：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span><span class="attr">&quot;data&quot;</span><span class="punctuation">:</span><span class="string">&quot;请求缺少token&quot;</span><span class="punctuation">,</span><span class="attr">&quot;desc&quot;</span><span class="punctuation">:</span><span class="string">&quot;无权限&quot;</span><span class="punctuation">,</span><span class="attr">&quot;status&quot;</span><span class="punctuation">:</span><span class="number">401</span><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p><strong>原因</strong>：最初误认为图片鉴权接口不需要 Token，专门写了一个 <code>sendPostWithoutToken()</code> 方法来调用。</p>
<p><strong>解决</strong>：Sign 鉴权接口本身也需要 Token，改用 <code>sendPost()</code> 方法，先获取 Token 再请求：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 错误写法</span></span><br><span class="line"><span class="type">String</span> <span class="variable">response</span> <span class="operator">=</span> VideoApiClient.sendPostWithoutToken(videoApiProperties, <span class="string">&quot;/lisa/access/sign&quot;</span>, requestBody);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 正确写法</span></span><br><span class="line"><span class="type">String</span> <span class="variable">token</span> <span class="operator">=</span> getToken();</span><br><span class="line"><span class="type">String</span> <span class="variable">response</span> <span class="operator">=</span> VideoApiClient.sendPost(videoApiProperties, token, <span class="string">&quot;/lisa/access/sign&quot;</span>, requestBody);</span><br></pre></td></tr></table></figure>

<h3 id="坑二：ClassCastException-类型转换失败"><a href="#坑二：ClassCastException-类型转换失败" class="headerlink" title="坑二：ClassCastException 类型转换失败"></a>坑二：ClassCastException 类型转换失败</h3><p><strong>现象</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">java.lang.ClassCastException: java.lang.String cannot be cast to java.util.Map</span><br></pre></td></tr></table></figure>

<p><strong>原因</strong>：API 返回的 <code>data</code> 字段在不同情况下类型不同：</p>
<ul>
<li>正常时是 Map（包含鉴权信息）</li>
<li>错误时是 String（错误信息）</li>
</ul>
<p><strong>解决</strong>：添加 <code>instanceof</code> 类型检查：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (dataObj <span class="keyword">instanceof</span> Map) &#123;</span><br><span class="line">    Map&lt;String, String&gt; authData = (Map&lt;String, String&gt;) dataObj;</span><br><span class="line">    <span class="comment">// 正常处理</span></span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    log.warn(<span class="string">&quot;data字段类型不是Map，实际类型: &#123;&#125;, 值: &#123;&#125;&quot;</span>, dataObj.getClass().getSimpleName(), dataObj);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="坑三：thumbnailAuth-始终为-null"><a href="#坑三：thumbnailAuth-始终为-null" class="headerlink" title="坑三：thumbnailAuth 始终为 null"></a>坑三：thumbnailAuth 始终为 null</h3><p><strong>现象</strong>：接口返回正常，但 <code>thumbnailAuth</code> 字段始终为 <code>null</code>。</p>
<p><strong>原因</strong>：原逻辑存在设计缺陷——只有当 <code>thumbnail</code> 不为空时才去获取鉴权信息。但有些设备没有缩略图，却仍然需要鉴权信息来访问告警图片。</p>
<p><strong>解决</strong>：将获取鉴权信息<strong>移到循环外部</strong>，预先获取一次，统一设置给所有设备：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 1.5 预先获取图片鉴权信息（对所有设备通用，只需获取一次）</span></span><br><span class="line">Map&lt;String, String&gt; imageAuth = getImageAccessSign();</span><br><span class="line"></span><br><span class="line"><span class="comment">// 2. 为每个设备获取详细信息</span></span><br><span class="line"><span class="keyword">for</span> (String cameraId : cameraIds) &#123;</span><br><span class="line">    <span class="comment">// ... 获取播放地址、缩略图等 ...</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 统一设置 thumbnailAuth（无论缩略图是否为空）</span></span><br><span class="line">    <span class="keyword">if</span> (imageAuth != <span class="literal">null</span> &amp;&amp; !imageAuth.isEmpty()) &#123;</span><br><span class="line">        deviceInfo.setThumbnailAuth(imageAuth);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="坑四：账户权限不足"><a href="#坑四：账户权限不足" class="headerlink" title="坑四：账户权限不足"></a>坑四：账户权限不足</h3><p><strong>现象</strong>：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span><span class="attr">&quot;data&quot;</span><span class="punctuation">:</span><span class="string">&quot;&quot;</span><span class="punctuation">,</span><span class="attr">&quot;desc&quot;</span><span class="punctuation">:</span><span class="string">&quot;共享用户无权限访问该接口&quot;</span><span class="punctuation">,</span><span class="attr">&quot;status&quot;</span><span class="punctuation">:</span><span class="number">401</span><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p><strong>原因</strong>：这不是代码问题，是<strong>视频平台账户权限问题</strong>。当前的共享用户账户没有 <code>/lisa/access/sign</code> 接口的访问权限。</p>
<p><strong>解决</strong>：需要联系视频平台提供方开通接口权限。</p>
<h2 id="核心代码实现"><a href="#核心代码实现" class="headerlink" title="核心代码实现"></a>核心代码实现</h2><h3 id="获取图片鉴权信息（带缓存）"><a href="#获取图片鉴权信息（带缓存）" class="headerlink" title="获取图片鉴权信息（带缓存）"></a>获取图片鉴权信息（带缓存）</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 获取图片访问鉴权信息（带缓存）</span></span><br><span class="line"><span class="comment"> * 根据文档，鉴权信息10分钟有效</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> Map&lt;String, String&gt; <span class="title function_">getImageAccessSign</span><span class="params">()</span> <span class="keyword">throws</span> ServiceException &#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="comment">// 1. 优先从Redis缓存获取</span></span><br><span class="line">        <span class="keyword">if</span> (redisCache != <span class="literal">null</span>) &#123;</span><br><span class="line">            Map&lt;String, String&gt; cachedAuth = redisCache.getCacheObject(IMAGE_AUTH_CACHE_KEY);</span><br><span class="line">            <span class="keyword">if</span> (cachedAuth != <span class="literal">null</span> &amp;&amp; !cachedAuth.isEmpty()) &#123;</span><br><span class="line">                <span class="keyword">return</span> cachedAuth;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2. 缓存未命中，调用API获取</span></span><br><span class="line">        <span class="type">String</span> <span class="variable">appId</span> <span class="operator">=</span> videoApiProperties.getImageAppId();</span><br><span class="line">        <span class="keyword">if</span> (StringUtils.isEmpty(appId)) &#123;</span><br><span class="line">            log.warn(<span class="string">&quot;图片访问鉴权的appId未配置&quot;</span>);</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 3. 调用 /lisa/access/sign 接口（需要Token鉴权）</span></span><br><span class="line">        <span class="type">String</span> <span class="variable">token</span> <span class="operator">=</span> getToken();</span><br><span class="line">        Map&lt;String, String&gt; requestBody = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;();</span><br><span class="line">        requestBody.put(<span class="string">&quot;x-apig-appid&quot;</span>, appId);</span><br><span class="line"></span><br><span class="line">        <span class="type">String</span> <span class="variable">response</span> <span class="operator">=</span> VideoApiClient.sendPost(videoApiProperties, token, <span class="string">&quot;/lisa/access/sign&quot;</span>, requestBody);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 4. 解析响应（带类型检查）</span></span><br><span class="line">        Map&lt;String, Object&gt; responseMap = objectMapper.readValue(response, Map.class);</span><br><span class="line">        <span class="keyword">if</span> (responseMap != <span class="literal">null</span> &amp;&amp; responseMap.get(<span class="string">&quot;data&quot;</span>) <span class="keyword">instanceof</span> Map) &#123;</span><br><span class="line">            Map&lt;String, String&gt; authData = (Map&lt;String, String&gt;) responseMap.get(<span class="string">&quot;data&quot;</span>);</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 5. 存入缓存（9分钟，留1分钟缓冲）</span></span><br><span class="line">            <span class="keyword">if</span> (redisCache != <span class="literal">null</span> &amp;&amp; authData != <span class="literal">null</span> &amp;&amp; !authData.isEmpty()) &#123;</span><br><span class="line">                redisCache.setCacheObject(IMAGE_AUTH_CACHE_KEY, authData, IMAGE_AUTH_EXPIRE_TIME, TimeUnit.SECONDS);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> authData;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">        log.error(<span class="string">&quot;获取图片鉴权信息失败&quot;</span>, e);</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="缓存时间设计"><a href="#缓存时间设计" class="headerlink" title="缓存时间设计"></a>缓存时间设计</h2><p>当存在多层缓存时，<strong>内层缓存的过期时间应小于外层缓存</strong>：</p>
<table>
<thead>
<tr>
<th>缓存层</th>
<th>有效期</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>图片鉴权缓存</td>
<td>9 分钟</td>
<td>留 1 分钟缓冲</td>
</tr>
<tr>
<td>接口整体缓存</td>
<td>9 分钟</td>
<td>原来是 10 分钟，已调整</td>
</tr>
</tbody></table>
<p>如果不这样设计，可能会出现：外层缓存还活着，但内层的鉴权信息已经过期，导致返回无效的鉴权参数。</p>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><h3 id="1-第三方接口鉴权要确认清楚"><a href="#1-第三方接口鉴权要确认清楚" class="headerlink" title="1. 第三方接口鉴权要确认清楚"></a>1. 第三方接口鉴权要确认清楚</h3><p>文档可能不完整或有误，实际联调前应该：</p>
<ol>
<li>确认接口是否需要鉴权</li>
<li>确认鉴权方式（Token、Sign、API Key 等）</li>
<li>确认账户是否有对应权限</li>
</ol>
<h3 id="2-外部-API-响应要做类型检查"><a href="#2-外部-API-响应要做类型检查" class="headerlink" title="2. 外部 API 响应要做类型检查"></a>2. 外部 API 响应要做类型检查</h3><p>外部 API 的响应结构可能因为各种原因（错误、权限、参数）发生变化，使用 <code>instanceof</code> 进行类型检查可以避免运行时异常。</p>
<h3 id="3-缓存时间要层层递减"><a href="#3-缓存时间要层层递减" class="headerlink" title="3. 缓存时间要层层递减"></a>3. 缓存时间要层层递减</h3><p>多层缓存场景下，内层缓存有效期必须小于外层，避免返回过期数据。</p>
<h3 id="4-日志是调试的好帮手"><a href="#4-日志是调试的好帮手" class="headerlink" title="4. 日志是调试的好帮手"></a>4. 日志是调试的好帮手</h3><p>在排查问题时，添加原始响应日志非常关键：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">log.info(<span class="string">&quot;图片鉴权API原始响应: &#123;&#125;&quot;</span>, response);</span><br></pre></td></tr></table></figure>

<p>这帮助快速定位是代码问题还是平台权限问题。</p>
<h2 id="当前状态"><a href="#当前状态" class="headerlink" title="当前状态"></a>当前状态</h2><table>
<thead>
<tr>
<th>项目</th>
<th>状态</th>
</tr>
</thead>
<tbody><tr>
<td>代码实现</td>
<td>✅ 完成</td>
</tr>
<tr>
<td>单元测试</td>
<td>✅ 完成</td>
</tr>
<tr>
<td>缓存逻辑</td>
<td>✅ 完成</td>
</tr>
<tr>
<td>平台权限</td>
<td>❌ 待开通</td>
</tr>
</tbody></table>
<p><strong>下一步</strong>：联系视频平台方开通 <code>/lisa/access/sign</code> 接口权限，然后验证完整链路。</p>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>水文</category>
      </categories>
      <tags>
        <tag>Spring Boot</tag>
        <tag>鉴权</tag>
        <tag>视频平台</tag>
        <tag>Redis缓存</tag>
      </tags>
  </entry>
  <entry>
    <title>视频流代理：从 Java 到 Nginx 的性能优化实践</title>
    <url>/2026/05/27/%E8%A7%86%E9%A2%91%E6%B5%81%E4%BB%A3%E7%90%86-%E4%BB%8EJava%E5%88%B0Nginx/</url>
    <content><![CDATA[<p>数据归集系统的视频流代理（FLV&#x2F;HLS）由 Java Controller 实现，每路视频流占用一个 Tomcat 线程做 8KB 缓冲区转发。并发路数多时会耗尽线程池。本文记录了将 FLV 代理迁移到 Nginx 的完整方案，以及 HLS 仍需走 Java 代理的技术原因。</p>
<div class="audio-player">
  <div class="audio-section">
    <h4>🎧 文章导读</h4>
    <audio controls preload="metadata">
      <source src="/audio/视频流代理-从Java到Nginx/narration.mp3" type="audio/mpeg">
      您的浏览器不支持音频播放。
    </audio>
  </div>
  <div class="audio-section">
    <h4>🎵 背景音乐</h4>
    <audio controls loop preload="metadata">
      <source src="/audio/视频流代理-从Java到Nginx/bgm.mp3" type="audio/mpeg">
      您的浏览器不支持音频播放。
    </audio>
  </div>
</div>

<span id="more"></span>

<h2 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h2><p>水文监测系统需要播放 Lisa 视频平台的实时监控视频。后端通过反向代理解决前端无法直接访问内网视频流的问题。</p>
<p>原有实现是纯 Java 方案：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">前端 → Java(play接口) → FLV/HLS 都返回 Java 代理地址</span><br><span class="line">前端 → Java(流代理)  → Lisa上游</span><br></pre></td></tr></table></figure>

<p><strong>问题</strong>：每路视频流占用一个 Tomcat 线程，做 8KB 缓冲区转发。当同时播放 20 路视频时，Tomcat 线程池压力很大。</p>
<h2 id="方案设计"><a href="#方案设计" class="headerlink" title="方案设计"></a>方案设计</h2><p><img data-src="/images/posts/%E8%A7%86%E9%A2%91%E6%B5%81%E4%BB%A3%E7%90%86-%E4%BB%8EJava%E5%88%B0Nginx/fig1.png" alt="架构对比图"><br><em>图1：Java 代理模式 vs Nginx 代理模式的架构对比</em></p>
<p>新增 Nginx 流代理，与 Java 代理并存，通过配置切换：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">【Nginx 代理模式】（新增，生产环境推荐）</span><br><span class="line">前端 → Java(play接口) → FLV 返回 Nginx 代理地址，HLS 返回 Java 代理地址</span><br><span class="line">前端 → Nginx(flv代理)  → Lisa上游    ← FLV 走 Nginx，不吃 Tomcat 线程</span><br><span class="line">前端 → Java(hls代理)   → Lisa上游    ← HLS 走 Java，需要改写 M3U8 内容</span><br><span class="line"></span><br><span class="line">【Java 代理模式】（当前，保持不变）</span><br><span class="line">前端 → Java(play接口) → FLV/HLS 都返回 Java 代理地址</span><br><span class="line">前端 → Java(流代理)  → Lisa上游</span><br></pre></td></tr></table></figure>

<p><strong>关键决策</strong>：FLV 走 Nginx，HLS 走 Java。</p>
<h3 id="为什么-HLS-不能走-Nginx？"><a href="#为什么-HLS-不能走-Nginx？" class="headerlink" title="为什么 HLS 不能走 Nginx？"></a>为什么 HLS 不能走 Nginx？</h3><p>Nginx 是纯透传代理，无法改写 M3U8 文件中的 TS 片段路径。Lisa 平台 M3U8 中的 TS 为相对路径，经 Nginx 代理后 HLS.js 解析出的 TS 地址没有 <code>url</code> 参数，请求会失败。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Lisa 原始 M3U8:</span><br><span class="line">#EXTINF:10.0,</span><br><span class="line">segment001.ts          ← 相对路径</span><br><span class="line"></span><br><span class="line">经 Nginx 代理后，HLS.js 解析出:</span><br><span class="line">http://nginx:18001/segment001.ts  ← 缺少 url 参数，404</span><br></pre></td></tr></table></figure>

<p>Java 代理会改写 M3U8 内容，将 TS 地址替换为 Java 代理地址：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">改写后的 M3U8:</span><br><span class="line">#EXTINF:10.0,</span><br><span class="line">http://java:9223/video/proxy/hls/ts?url=xxx&amp;segment=001.ts  ← 完整代理地址</span><br></pre></td></tr></table></figure>

<h3 id="为什么-FLV-可以走-Nginx？"><a href="#为什么-FLV-可以走-Nginx？" class="headerlink" title="为什么 FLV 可以走 Nginx？"></a>为什么 FLV 可以走 Nginx？</h3><p>FLV 是单条连续流，不需要改写内容，Nginx 透传完全没问题。而且 FLV 才是长时间占用 Tomcat 线程的大头（一个摄像头可能持续播放几小时）。</p>
<h2 id="Nginx-配置"><a href="#Nginx-配置" class="headerlink" title="Nginx 配置"></a>Nginx 配置</h2><figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="section">server</span> &#123;</span><br><span class="line">    <span class="attribute">listen</span>       <span class="number">18001</span>;</span><br><span class="line">    <span class="attribute">server_name</span>  _;</span><br><span class="line"></span><br><span class="line">    <span class="section">location</span> /video-proxy/ &#123;</span><br><span class="line">        <span class="comment"># CORS 预检请求</span></span><br><span class="line">        <span class="attribute">if</span> (<span class="variable">$request_method</span> = <span class="string">&#x27;OPTIONS&#x27;</span>) &#123;</span><br><span class="line">            <span class="attribute">add_header</span> <span class="string">&#x27;Access-Control-Allow-Origin&#x27;</span> <span class="string">&#x27;*&#x27;</span> always;</span><br><span class="line">            <span class="attribute">add_header</span> <span class="string">&#x27;Access-Control-Allow-Methods&#x27;</span> <span class="string">&#x27;GET, OPTIONS&#x27;</span> always;</span><br><span class="line">            <span class="attribute">add_header</span> <span class="string">&#x27;Access-Control-Allow-Headers&#x27;</span> <span class="string">&#x27;Range, If-Range&#x27;</span> always;</span><br><span class="line">            <span class="attribute">add_header</span> <span class="string">&#x27;Access-Control-Max-Age&#x27;</span> <span class="number">3600</span> always;</span><br><span class="line">            <span class="attribute">add_header</span> <span class="string">&#x27;Content-Length&#x27;</span> <span class="number">0</span>;</span><br><span class="line">            <span class="attribute">return</span> <span class="number">204</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 动态代理：$arg_url 从查询参数获取目标地址</span></span><br><span class="line">        <span class="attribute">resolver</span> <span class="number">211.136.192.6</span> valid=<span class="number">30s</span>;</span><br><span class="line">        <span class="attribute">set</span> <span class="variable">$target</span> <span class="variable">$arg_url</span>;</span><br><span class="line">        <span class="attribute">proxy_pass</span> <span class="variable">$target</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 代理配置</span></span><br><span class="line">        <span class="attribute">proxy_http_version</span> <span class="number">1</span>.<span class="number">1</span>;</span><br><span class="line">        <span class="attribute">proxy_set_header</span> Host <span class="variable">$proxy_host</span>;</span><br><span class="line">        <span class="attribute">proxy_set_header</span> X-Real-IP <span class="variable">$remote_addr</span>;</span><br><span class="line">        <span class="attribute">proxy_set_header</span> X-Forwarded-For <span class="variable">$proxy_add_x_forwarded_for</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment"># Range 请求支持（视频拖拽）</span></span><br><span class="line">        <span class="attribute">proxy_set_header</span> Range <span class="variable">$http_range</span>;</span><br><span class="line">        <span class="attribute">proxy_set_header</span> If-Range <span class="variable">$http_if_range</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 关闭缓冲，实时转发</span></span><br><span class="line">        <span class="attribute">proxy_buffering</span> <span class="literal">off</span>;</span><br><span class="line">        <span class="attribute">proxy_read_timeout</span> <span class="number">3600s</span>;</span><br><span class="line">        <span class="attribute">proxy_send_timeout</span> <span class="number">3600s</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment"># CORS 响应头</span></span><br><span class="line">        <span class="attribute">add_header</span> <span class="string">&#x27;Access-Control-Allow-Origin&#x27;</span> <span class="string">&#x27;*&#x27;</span> always;</span><br><span class="line">        <span class="attribute">add_header</span> <span class="string">&#x27;Access-Control-Expose-Headers&#x27;</span> <span class="string">&#x27;Content-Length, Content-Range&#x27;</span> always;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>关键配置说明：</p>
<table>
<thead>
<tr>
<th>配置项</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td><code>proxy_buffering off</code></td>
<td>关闭缓冲，实时转发视频流</td>
</tr>
<tr>
<td><code>proxy_read_timeout 3600s</code></td>
<td>长连接超时 1 小时</td>
</tr>
<tr>
<td><code>set $target $arg_url</code></td>
<td>从查询参数动态获取代理目标</td>
</tr>
<tr>
<td><code>resolver</code></td>
<td>DNS 解析器，用于解析 <code>$target</code> 中的域名</td>
</tr>
</tbody></table>
<h2 id="Java-代码改动"><a href="#Java-代码改动" class="headerlink" title="Java 代码改动"></a>Java 代码改动</h2><p>修改 <code>LisaVideoServiceImpl.java</code> 的 <code>rewriteUrls()</code> 方法，根据配置决定 FLV 的代理目标：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Value(&quot;$&#123;lisa.video.proxy-type:java&#125;&quot;)</span></span><br><span class="line"><span class="keyword">private</span> String proxyType;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Value(&quot;$&#123;lisa.video.proxy-base-url&#125;&quot;)</span></span><br><span class="line"><span class="keyword">private</span> String proxyBaseUrl;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Value(&quot;$&#123;lisa.video.nginx-base-url:&#125;&quot;)</span></span><br><span class="line"><span class="keyword">private</span> String nginxBaseUrl;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">rewriteUrls</span><span class="params">(JSONObject data)</span> <span class="keyword">throws</span> UnsupportedEncodingException &#123;</span><br><span class="line">    <span class="type">JSONObject</span> <span class="variable">urls</span> <span class="operator">=</span> data.getJSONObject(<span class="string">&quot;urls&quot;</span>);</span><br><span class="line">    <span class="keyword">if</span> (urls == <span class="literal">null</span>) <span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (String key : urls.keySet()) &#123;</span><br><span class="line">        <span class="type">String</span> <span class="variable">url</span> <span class="operator">=</span> urls.getString(key);</span><br><span class="line">        <span class="keyword">if</span> (url == <span class="literal">null</span> || !url.startsWith(upstreamHost)) <span class="keyword">continue</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (key.equals(<span class="string">&quot;flvPlayUrl&quot;</span>)) &#123;</span><br><span class="line">            <span class="comment">// FLV 根据配置选择代理</span></span><br><span class="line">            <span class="keyword">if</span> (<span class="string">&quot;nginx&quot;</span>.equals(proxyType)) &#123;</span><br><span class="line">                urls.put(key, nginxBaseUrl + <span class="string">&quot;/video-proxy/?url=&quot;</span> + URLEncoder.encode(url, <span class="string">&quot;UTF-8&quot;</span>));</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                urls.put(key, proxyBaseUrl + <span class="string">&quot;/dataCollection/video/proxy/flv?url=&quot;</span> + URLEncoder.encode(url, <span class="string">&quot;UTF-8&quot;</span>));</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (key.equals(<span class="string">&quot;hlsPlayUrl&quot;</span>)) &#123;</span><br><span class="line">            <span class="comment">// HLS 始终走 Java 代理（需要改写 M3U8 内容）</span></span><br><span class="line">            urls.put(key, proxyBaseUrl + <span class="string">&quot;/dataCollection/video/proxy/hls/m3u8?url=&quot;</span> + URLEncoder.encode(url, <span class="string">&quot;UTF-8&quot;</span>));</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="配置项"><a href="#配置项" class="headerlink" title="配置项"></a>配置项</h2><table>
<thead>
<tr>
<th>配置项</th>
<th>值</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>lisa.video.proxy-type</code></td>
<td><code>nginx</code> 或 <code>java</code></td>
<td>决定 FLV 走 Nginx 还是 Java，默认 <code>java</code></td>
</tr>
<tr>
<td><code>lisa.video.proxy-base-url</code></td>
<td><code>http://10.144.32.219:9223</code></td>
<td>Java 代理地址</td>
</tr>
<tr>
<td><code>lisa.video.nginx-base-url</code></td>
<td><code>http://10.144.32.219:18001</code></td>
<td>Nginx 代理地址</td>
</tr>
</tbody></table>
<p>切换方式：修改 Consul 配置 <code>lisa.video.proxy-type=nginx</code>，重启或热更新即可。</p>
<h2 id="验证步骤"><a href="#验证步骤" class="headerlink" title="验证步骤"></a>验证步骤</h2><h3 id="阶段一：Nginx-代理单独验证"><a href="#阶段一：Nginx-代理单独验证" class="headerlink" title="阶段一：Nginx 代理单独验证"></a>阶段一：Nginx 代理单独验证</h3><ol>
<li>部署 Nginx 配置，确认 18001 端口可访问</li>
<li>调 play 接口拿一个上游 FLV 地址</li>
<li>直接访问 <code>http://nginx:18001/video-proxy/?url=&lt;上游flv地址&gt;</code>，确认返回 <code>video/x-flv</code> 流</li>
</ol>
<h3 id="阶段二：Java-配置切换验证"><a href="#阶段二：Java-配置切换验证" class="headerlink" title="阶段二：Java 配置切换验证"></a>阶段二：Java 配置切换验证</h3><ol start="4">
<li>Consul 配置 <code>lisa.video.proxy-type=nginx</code></li>
<li>重启 Java 服务</li>
<li>调用 <code>POST /dataCollection/video/play</code>，确认返回的 FLV URL 指向 Nginx，HLS URL 指向 Java</li>
<li>用 flv.js 播放 FLV 验证</li>
<li>用 hls.js 播放 HLS 验证</li>
</ol>
<h3 id="阶段三：回退验证"><a href="#阶段三：回退验证" class="headerlink" title="阶段三：回退验证"></a>阶段三：回退验证</h3><ol start="9">
<li>Consul 改回 <code>lisa.video.proxy-type=java</code></li>
<li>确认 FLV 和 HLS 都恢复为 Java 代理模式</li>
</ol>
<h2 id="风险点"><a href="#风险点" class="headerlink" title="风险点"></a>风险点</h2><table>
<thead>
<tr>
<th>风险</th>
<th>应对</th>
</tr>
</thead>
<tbody><tr>
<td>Nginx DNS 解析器配置不对</td>
<td>根据服务器环境配置正确的 <code>resolver</code>，或直接用 IP</td>
</tr>
<tr>
<td>Nginx 未部署或端口不通</td>
<td><code>proxy-type</code> 保持 <code>java</code>，不影响现有功能</td>
</tr>
<tr>
<td>Nginx 无 host 白名单（开放代理）</td>
<td>内网环境风险低；如需加固，可在 Nginx 层加 <code>if</code> 校验</td>
</tr>
</tbody></table>
<h2 id="性能对比"><a href="#性能对比" class="headerlink" title="性能对比"></a>性能对比</h2><p><img data-src="/images/posts/%E8%A7%86%E9%A2%91%E6%B5%81%E4%BB%A3%E7%90%86-%E4%BB%8EJava%E5%88%B0Nginx/fig2.png" alt="性能对比图表"><br><em>图2：Java 代理 vs Nginx 代理的性能指标对比</em></p>
<table>
<thead>
<tr>
<th>指标</th>
<th>Java 代理</th>
<th>Nginx 代理</th>
</tr>
</thead>
<tbody><tr>
<td>每路视频占用</td>
<td>1 个 Tomcat 线程</td>
<td>Nginx worker 连接</td>
</tr>
<tr>
<td>内存开销</td>
<td>8KB 缓冲区 × 并发数</td>
<td>极低</td>
</tr>
<tr>
<td>最大并发路数</td>
<td>受 Tomcat 线程池限制（默认 200）</td>
<td>数千路</td>
</tr>
<tr>
<td>CPU 开销</td>
<td>较高（Java 内存拷贝）</td>
<td>极低（内核态转发）</td>
</tr>
</tbody></table>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><ol>
<li><strong>Nginx 适合纯透传场景</strong>——不需要改写内容的代理（如 FLV）交给 Nginx，需要改写内容的（如 HLS M3U8）留在 Java</li>
<li><strong>配置切换比代码部署更灵活</strong>——通过 Consul 配置 <code>proxy-type</code> 可以随时切换代理模式，不需要重新部署</li>
<li><strong>视频流代理要关闭缓冲</strong>——<code>proxy_buffering off</code> 是关键配置，否则 Nginx 会缓冲整个视频再返回</li>
<li><strong>超时时间要足够长</strong>——视频流可能持续几小时，<code>proxy_read_timeout</code> 要设为 3600s 或更长</li>
</ol>
<hr>
<p><em>本文涉及文件：<code>LisaVideoServiceImpl.java</code>、Nginx 配置、Consul 配置。</em></p>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>数据归集</category>
      </categories>
      <tags>
        <tag>Nginx</tag>
        <tag>FLV</tag>
        <tag>Spring Boot</tag>
        <tag>性能优化</tag>
        <tag>视频流</tag>
        <tag>HLS</tag>
      </tags>
  </entry>
  <entry>
    <title>跨数据源查询：VD 设备列表的改造实践</title>
    <url>/2026/05/28/%E8%B7%A8%E6%95%B0%E6%8D%AE%E6%BA%90%E6%9F%A5%E8%AF%A2-VD%E8%AE%BE%E5%A4%87%E5%88%97%E8%A1%A8%E6%94%B9%E9%80%A0/</url>
    <content><![CDATA[<p>前端视频巡查任务配置页需要展示”有视频设备的测站列表 + 每个测站下的设备”。但设备表在主库，测站表在 slave 库，无法一条 SQL JOIN。本文记录了使用 <code>@DS</code> 注解 + Java 内存组装的跨数据源查询方案。</p>
<div class="audio-player">
  <div class="audio-section">
    <h4>🎧 文章导读</h4>
    <audio controls preload="metadata">
      <source src="/audio/跨数据源查询-VD设备列表改造/narration.mp3" type="audio/mpeg">
      您的浏览器不支持音频播放。
    </audio>
  </div>
  <div class="audio-section">
    <h4>🎵 背景音乐</h4>
    <audio controls loop preload="metadata">
      <source src="/audio/跨数据源查询-VD设备列表改造/bgm.mp3" type="audio/mpeg">
      您的浏览器不支持音频播放。
    </audio>
  </div>
</div>

<span id="more"></span>

<h2 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h2><p>视频巡查功能需要展示测站及其下的视频设备。数据分布在两个数据库中：</p>
<table>
<thead>
<tr>
<th>表</th>
<th>数据库</th>
<th>用途</th>
</tr>
</thead>
<tbody><tr>
<td><code>bs_sw_cy_dh_equipmentb</code>（设备表）</td>
<td>主库 (gdsw)</td>
<td>存储设备信息</td>
</tr>
<tr>
<td><code>st_stbprp_b</code>（测站表）</td>
<td>slave 库 (water)</td>
<td>存储测站信息</td>
</tr>
</tbody></table>
<p>由于两张表在不同的数据库中，无法使用 SQL JOIN，需要在 Java 层组装数据。</p>
<h2 id="方案设计"><a href="#方案设计" class="headerlink" title="方案设计"></a>方案设计</h2><p>使用 <code>dynamic-datasource</code> 的 <code>@DS</code> 注解切换数据源，两次查询 + Java 内存组装：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">1. 查设备表（主库）→ 获取设备列表</span><br><span class="line">2. 收集 stcd → 批量查测站名称（slave 库）</span><br><span class="line">3. Java 内存回填 stnm</span><br></pre></td></tr></table></figure>

<h2 id="核心实现"><a href="#核心实现" class="headerlink" title="核心实现"></a>核心实现</h2><p><img data-src="/images/posts/%E8%B7%A8%E6%95%B0%E6%8D%AE%E6%BA%90%E6%9F%A5%E8%AF%A2-VD%E8%AE%BE%E5%A4%87%E5%88%97%E8%A1%A8%E6%94%B9%E9%80%A0/fig1.png" alt="查询流程图"><br><em>图1：跨数据源查询的三步流程</em></p>
<h3 id="Step-1-新建测站实体和-Mapper"><a href="#Step-1-新建测站实体和-Mapper" class="headerlink" title="Step 1: 新建测站实体和 Mapper"></a>Step 1: 新建测站实体和 Mapper</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// StStbprpB.java - 测站实体</span></span><br><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="meta">@TableName(&quot;st_stbprp_b&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">StStbprpB</span> &#123;</span><br><span class="line">    <span class="meta">@TableId(&quot;STCD&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> String stcd;</span><br><span class="line">    <span class="keyword">private</span> String stnm;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// StStbprpBMapper.java - 切换到 slave 数据源</span></span><br><span class="line"><span class="meta">@Mapper</span></span><br><span class="line"><span class="meta">@DS(&quot;slave&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">StStbprpBMapper</span> <span class="keyword">extends</span> <span class="title class_">BaseMapper</span>&lt;StStbprpB&gt; &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>关键点：<code>@DS(&quot;slave&quot;)</code> 注解让这个 Mapper 的所有查询都走 slave 数据源。</p>
<h3 id="Step-2-修改设备-DTO"><a href="#Step-2-修改设备-DTO" class="headerlink" title="Step 2: 修改设备 DTO"></a>Step 2: 修改设备 DTO</h3><p>在 <code>VdEquipment.java</code> 中添加非数据库字段 <code>stnm</code>：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@TableField(exist = false)</span></span><br><span class="line"><span class="keyword">private</span> String stnm;</span><br></pre></td></tr></table></figure>

<p><code>@TableField(exist = false)</code> 告诉 MBP 这个字段不对应数据库列，避免映射错误。</p>
<h3 id="Step-3-实现跨数据源查询"><a href="#Step-3-实现跨数据源查询" class="headerlink" title="Step 3: 实现跨数据源查询"></a>Step 3: 实现跨数据源查询</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Resource</span></span><br><span class="line"><span class="keyword">private</span> StStbprpBMapper stStbprpBMapper;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> IPage&lt;VdEquipment&gt; <span class="title function_">getVdDevices</span><span class="params">(<span class="type">int</span> current, <span class="type">int</span> size, String stcd)</span> &#123;</span><br><span class="line">    <span class="comment">// 1. 查设备表（主库）</span></span><br><span class="line">    Page&lt;VdEquipment&gt; page = <span class="keyword">new</span> <span class="title class_">Page</span>&lt;&gt;(current, size);</span><br><span class="line">    LambdaQueryWrapper&lt;VdEquipment&gt; wrapper = <span class="keyword">new</span> <span class="title class_">LambdaQueryWrapper</span>&lt;&gt;();</span><br><span class="line">    wrapper.like(VdEquipment::getSymbol, <span class="string">&quot;vd&quot;</span>);</span><br><span class="line">    wrapper.orderByAsc(VdEquipment::getSort);</span><br><span class="line">    <span class="keyword">if</span> (stcd != <span class="literal">null</span> &amp;&amp; !stcd.isEmpty()) &#123;</span><br><span class="line">        wrapper.eq(VdEquipment::getStcd, stcd);</span><br><span class="line">    &#125;</span><br><span class="line">    IPage&lt;VdEquipment&gt; result = <span class="built_in">this</span>.page(page, wrapper);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (result.getRecords().isEmpty()) &#123;</span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 2. 收集 stcd，批量查测站名称（slave 库）</span></span><br><span class="line">    List&lt;String&gt; stcds = result.getRecords().stream()</span><br><span class="line">            .map(VdEquipment::getStcd).distinct().collect(Collectors.toList());</span><br><span class="line">    List&lt;StStbprpB&gt; stations = stStbprpBMapper.selectList(</span><br><span class="line">            <span class="keyword">new</span> <span class="title class_">QueryWrapper</span>&lt;StStbprpB&gt;().in(<span class="string">&quot;STCD&quot;</span>, stcds).select(<span class="string">&quot;STCD&quot;</span>, <span class="string">&quot;STNM&quot;</span>));</span><br><span class="line">    Map&lt;String, String&gt; stnmMap = stations.stream()</span><br><span class="line">            .collect(Collectors.toMap(StStbprpB::getStcd, StStbprpB::getStnm, (a, b) -&gt; a));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 3. 回填 stnm</span></span><br><span class="line">    result.getRecords().forEach(e -&gt;</span><br><span class="line">            e.setStnm(stnmMap.getOrDefault(e.getStcd(), <span class="literal">null</span>)));</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="查询优化"><a href="#查询优化" class="headerlink" title="查询优化"></a>查询优化</h3><p>几个细节值得注意：</p>
<ol>
<li><strong>批量查询而非逐条查询</strong>——收集所有 stcd 后一次 <code>IN</code> 查询，避免 N+1 问题</li>
<li><strong>只查需要的字段</strong>——<code>select(&quot;STCD&quot;, &quot;STNM&quot;)</code> 减少数据传输量</li>
<li><strong>去重</strong>——<code>distinct()</code> 避免重复查询同一个测站</li>
<li><strong>容错处理</strong>——<code>getOrDefault(e.getStcd(), null)</code> 处理测站不存在的情况</li>
</ol>
<h2 id="涉及文件汇总"><a href="#涉及文件汇总" class="headerlink" title="涉及文件汇总"></a>涉及文件汇总</h2><table>
<thead>
<tr>
<th>操作</th>
<th>文件</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>新建</td>
<td><code>StStbprpB.java</code></td>
<td>测站实体</td>
</tr>
<tr>
<td>新建</td>
<td><code>StStbprpBMapper.java</code></td>
<td>@DS(“slave”) 切换数据源</td>
</tr>
<tr>
<td>修改</td>
<td><code>VdEquipment.java</code></td>
<td>新增 stnm 字段</td>
</tr>
<tr>
<td>修改</td>
<td><code>VdDeviceServiceImpl.java</code></td>
<td>跨数据源查询逻辑</td>
</tr>
</tbody></table>
<p>Controller 和 Service 接口不需要改，返回类型 <code>IPage&lt;VdEquipment&gt;</code> 不变。</p>
<h2 id="跨数据源查询的几种方案对比"><a href="#跨数据源查询的几种方案对比" class="headerlink" title="跨数据源查询的几种方案对比"></a>跨数据源查询的几种方案对比</h2><p><img data-src="/images/posts/%E8%B7%A8%E6%95%B0%E6%8D%AE%E6%BA%90%E6%9F%A5%E8%AF%A2-VD%E8%AE%BE%E5%A4%87%E5%88%97%E8%A1%A8%E6%94%B9%E9%80%A0/fig2.png" alt="方案对比图"><br><em>图2：四种跨数据源查询方案的优缺点对比</em></p>
<table>
<thead>
<tr>
<th>方案</th>
<th>优点</th>
<th>缺点</th>
<th>适用场景</th>
</tr>
</thead>
<tbody><tr>
<td>@DS 注解 + Java 组装</td>
<td>简单直观，MBP 原生支持</td>
<td>需要两次查询</td>
<td>数据量不大，关联逻辑简单</td>
</tr>
<tr>
<td>聚合视图&#x2F;物化视图</td>
<td>一条 SQL 完成</td>
<td>需要 DBA 配置，实时性差</td>
<td>查询频繁，数据变化少</td>
</tr>
<tr>
<td>数据同步（ETL）</td>
<td>查询性能最好</td>
<td>架构复杂，有延迟</td>
<td>数据量大，分析型查询</td>
</tr>
<tr>
<td>分布式查询引擎</td>
<td>统一 SQL</td>
<td>引入新组件</td>
<td>多数据源场景多</td>
</tr>
</tbody></table>
<p>本场景选择 @DS 注解方案，因为：</p>
<ul>
<li>数据量不大（设备表几千条）</li>
<li>关联逻辑简单（只需回填测站名称）</li>
<li>不想引入额外组件</li>
</ul>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><ol>
<li><strong>@DS 注解要加在 Mapper 上</strong>——加在 Service 上不生效，因为 MBP 的动态数据源是基于 Mapper 代理的</li>
<li><strong>批量查询避免 N+1</strong>——先收集所有 ID，一次 IN 查询，而不是逐条查询</li>
<li><strong>只查需要的字段</strong>——<code>select()</code> 减少数据传输量，尤其是大表</li>
<li><strong>容错处理</strong>——关联数据可能不存在，用 <code>getOrDefault</code> 处理</li>
</ol>
<hr>
<p><em>本文涉及代码在 digital-twin 模块中，已在 <code>master-b_sp_20251216-delDec</code> 分支提交。</em></p>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>数据归集</category>
      </categories>
      <tags>
        <tag>Spring Boot</tag>
        <tag>多数据源</tag>
        <tag>MyBatis-Plus</tag>
        <tag>动态数据源</tag>
        <tag>跨库查询</tag>
      </tags>
  </entry>
  <entry>
    <title>黑名单审核权限回收修复实战：从时机错误到完整回收</title>
    <url>/2026/03/27/%E9%BB%91%E5%90%8D%E5%8D%95%E5%AE%A1%E6%A0%B8%E6%9D%83%E9%99%90%E5%9B%9E%E6%94%B6%E4%BF%AE%E5%A4%8D%E5%AE%9E%E6%88%98/</url>
    <content><![CDATA[<p>在物联网门禁系统的黑名单审核流程中，我们发现了一个”审核未通过就删权”的严重 Bug。本文记录了从问题定位到完整修复的全过程，包括两次迭代：第一次解决”时机错误”，第二次解决”删权不完整”。</p>
<span id="more"></span>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>在物联网门禁系统中，黑名单审核流程的核心逻辑应该是：</p>
<ol>
<li>发起拉黑申请 → 仅进入待审核状态</li>
<li>审核通过 → 下发权限回收</li>
<li>审核拒绝 → 权限保持不变</li>
</ol>
<p>然而在实际排查中发现，系统存在<strong>两个问题</strong>：</p>
<blockquote>
<p>[!warning]</p>
<ol>
<li><strong>时机错误</strong>：拉黑申请阶段就提前下发了权限删除任务</li>
<li><strong>删权不完整</strong>：审核通过后只回收了部分权限（卡、黑名单记录），漏掉了二维码、人脸和临时权限</li>
</ol>
</blockquote>
<p>本文详细记录了这两轮修复的完整过程。</p>
<h2 id="问题背景"><a href="#问题背景" class="headerlink" title="问题背景"></a>问题背景</h2><h3 id="理想流程-vs-实际流程"><a href="#理想流程-vs-实际流程" class="headerlink" title="理想流程 vs 实际流程"></a>理想流程 vs 实际流程</h3><table>
<thead>
<tr>
<th>阶段</th>
<th>理想流程</th>
<th>实际流程（修复前）</th>
</tr>
</thead>
<tbody><tr>
<td>拉黑申请</td>
<td>仅记录待审核状态</td>
<td>❌ 直接下发权限删除任务</td>
</tr>
<tr>
<td>审核通过</td>
<td>完整回收所有权限</td>
<td>⚠️ 只回收了部分权限</td>
</tr>
<tr>
<td>审核拒绝</td>
<td>保持权限不变</td>
<td>✓ 符合预期</td>
</tr>
</tbody></table>
<h3 id="业务影响"><a href="#业务影响" class="headerlink" title="业务影响"></a>业务影响</h3><ul>
<li><strong>误删权限</strong>：未审核就删权，可能导致正常用户无法通行</li>
<li><strong>数据不一致</strong>：审核通过后部分权限残留，黑名单用户仍可通过二维码&#x2F;人脸进出</li>
<li><strong>审核失败</strong>：历史脏数据导致空指针，审核接口返回 <code>400 + msg:null</code></li>
</ul>
<p><img data-src="/images/posts/%E9%BB%91%E5%90%8D%E5%8D%95%E5%AE%A1%E6%A0%B8%E6%9D%83%E9%99%90%E5%9B%9E%E6%94%B6%E4%BF%AE%E5%A4%8D%E5%AE%9E%E6%88%98/fig3-root-cause.png" alt="Bug根因分析图"><br><em>图1：Bug 根因分析 - 时机错误、空指针、删权不完整</em></p>
<h2 id="第一轮修复：解决”时机错误”"><a href="#第一轮修复：解决”时机错误”" class="headerlink" title="第一轮修复：解决”时机错误”"></a>第一轮修复：解决”时机错误”</h2><h3 id="问题定位"><a href="#问题定位" class="headerlink" title="问题定位"></a>问题定位</h3><h4 id="1-拉黑申请阶段提前下发权限删除"><a href="#1-拉黑申请阶段提前下发权限删除" class="headerlink" title="1. 拉黑申请阶段提前下发权限删除"></a>1. 拉黑申请阶段提前下发权限删除</h4><p>排查 <code>EmployeeInfoController.pullBlack()</code> 发现：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 原代码逻辑（问题）</span></span><br><span class="line"><span class="meta">@PostMapping(&quot;/pull-black&quot;)</span></span><br><span class="line"><span class="keyword">public</span> Result <span class="title function_">pullBlack</span><span class="params">(<span class="meta">@RequestBody</span> BlackListDTO dto)</span> &#123;</span><br><span class="line">    <span class="comment">// 1. 更新人员状态为&quot;拉黑审核中&quot;</span></span><br><span class="line">    employeeInfoService.updateStatus(employeeId, <span class="number">7</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 2. ❌ 立即创建权限删除下发任务</span></span><br><span class="line">    privilegeDeployService.deploy(employeeId, Arrays.asList(<span class="number">5</span>, <span class="number">9</span>, <span class="number">14</span>, <span class="number">18</span>));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 3. 写入待审核记录</span></span><br><span class="line">    blackListService.save(blackListEntity);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>问题分析</strong>：</p>
<ul>
<li>定时任务只认”待下发记录”，不认”黑名单审核状态”</li>
<li>一旦创建下发任务，就会直接开始执行权限回收</li>
<li>这与”审核通过后才删权”的业务期望完全不符</li>
</ul>
<h4 id="2-黑名单审核通过时空指针"><a href="#2-黑名单审核通过时空指针" class="headerlink" title="2. 黑名单审核通过时空指针"></a>2. 黑名单审核通过时空指针</h4><p>接口 <code>/v1/employeewhitelist/black-audit</code> 返回 <code>400 + msg:null</code>，最终定位到：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">NullPointerException at DbServiceImpl.getSqlSegment(DbServiceImpl.java:225)</span><br></pre></td></tr></table></figure>

<p><strong>根因</strong>：</p>
<ul>
<li><code>blackAudit()</code> 最后会同步僵尸用户状态</li>
<li>按 <code>phone_no + company_id</code> 查询 <code>visitor_employee_zombie</code></li>
<li>旧的黑名单记录里 <code>companyId</code> 没有被写入</li>
<li>对 <code>null</code> 做 <code>toString()</code> 导致空指针</li>
</ul>
<h3 id="修复方案"><a href="#修复方案" class="headerlink" title="修复方案"></a>修复方案</h3><h4 id="改动-1：移除申请阶段的权限下发"><a href="#改动-1：移除申请阶段的权限下发" class="headerlink" title="改动 1：移除申请阶段的权限下发"></a>改动 1：移除申请阶段的权限下发</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// EmployeeInfoController.java</span></span><br><span class="line"><span class="meta">@PostMapping(&quot;/pull-black&quot;)</span></span><br><span class="line"><span class="keyword">public</span> Result <span class="title function_">pullBlack</span><span class="params">(<span class="meta">@RequestBody</span> BlackListDTO dto)</span> &#123;</span><br><span class="line">    <span class="comment">// 1. 更新人员状态为&quot;拉黑审核中&quot;</span></span><br><span class="line">    employeeInfoService.updateStatus(employeeId, <span class="number">7</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 2. ✓ 不再提前下发权限删除</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 3. 写入待审核记录（补充 companyId）</span></span><br><span class="line">    blackListEntity.setCompanyId(companyId);  <span class="comment">// 新增：防止后续空指针</span></span><br><span class="line">    blackListService.save(blackListEntity);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="改动-2：在审核通过时统一下发"><a href="#改动-2：在审核通过时统一下发" class="headerlink" title="改动 2：在审核通过时统一下发"></a>改动 2：在审核通过时统一下发</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// EmployeeWhiteListServiceImpl.java</span></span><br><span class="line"><span class="meta">@Transactional</span></span><br><span class="line"><span class="keyword">public</span> Result <span class="title function_">blackAudit</span><span class="params">(BlackAuditDTO dto)</span> &#123;</span><br><span class="line">    <span class="comment">// ... 审核逻辑 ...</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (approved) &#123;</span><br><span class="line">        <span class="comment">// ✓ 审核通过时才下发权限回收</span></span><br><span class="line">        privilegeDeployService.deploy(employeeId, Arrays.asList(<span class="number">5</span>, <span class="number">9</span>, <span class="number">14</span>));</span><br><span class="line">        <span class="comment">// 9 携带电梯操作 18</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// ... 同步僵尸用户 ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="改动-3：增强日志与容错"><a href="#改动-3：增强日志与容错" class="headerlink" title="改动 3：增强日志与容错"></a>改动 3：增强日志与容错</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 新增多处日志，便于问题定位</span></span><br><span class="line">log.info(<span class="string">&quot;黑名单记录查询结果: &#123;&#125;&quot;</span>, blackListEntity);</span><br><span class="line">log.info(<span class="string">&quot;当前登录用户: &#123;&#125;&quot;</span>, currentUser);</span><br><span class="line">log.info(<span class="string">&quot;关联员工信息: &#123;&#125;&quot;</span>, employeeInfo);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 对历史脏数据增加兜底</span></span><br><span class="line"><span class="keyword">if</span> (detail.getPhoneNo() == <span class="literal">null</span> || detail.getCompanyId() == <span class="literal">null</span>) &#123;</span><br><span class="line">    log.warn(<span class="string">&quot;跳过僵尸用户同步，数据不完整: phoneNo=&#123;&#125;, companyId=&#123;&#125;&quot;</span>,</span><br><span class="line">             detail.getPhoneNo(), detail.getCompanyId());</span><br><span class="line">    <span class="keyword">return</span>; <span class="comment">// 跳过同步，不打断主流程</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="第一轮验证"><a href="#第一轮验证" class="headerlink" title="第一轮验证"></a>第一轮验证</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mvn <span class="built_in">test</span> -Dtest=BlacklistAuditTimingTest</span><br><span class="line"></span><br><span class="line"><span class="comment"># 测试结果</span></span><br><span class="line">Tests run: 3, Failures: 0, Errors: 0, Skipped: 0</span><br></pre></td></tr></table></figure>

<p>验证通过：</p>
<ul>
<li>✓ 拉黑申请阶段不再调用 <code>privilegeDeployService.deploy()</code></li>
<li>✓ 新写入的黑名单记录会带上 <code>companyId</code></li>
<li>✓ 审核通过时才会创建 <code>5 / 9 / 14</code> 任务</li>
<li>✓ 黑名单记录缺 <code>companyId</code> 时，不再因为同步僵尸用户而抛异常</li>
</ul>
<h2 id="第二轮修复：解决”删权不完整”"><a href="#第二轮修复：解决”删权不完整”" class="headerlink" title="第二轮修复：解决”删权不完整”"></a>第二轮修复：解决”删权不完整”</h2><h3 id="问题发现"><a href="#问题发现" class="headerlink" title="问题发现"></a>问题发现</h3><p>第一轮修复后，人工联调发现：</p>
<blockquote>
<p>黑名单审核通过后，用户的门禁服务、梯控授权、设备授权状态看起来仍然正常。</p>
</blockquote>
<p><strong>排查发现</strong>：原实现只下发了部分回收动作：</p>
<table>
<thead>
<tr>
<th>操作类型</th>
<th>编码</th>
<th>是否下发</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>注销卡</td>
<td>5</td>
<td>✓</td>
<td>已下发</td>
</tr>
<tr>
<td>黑名单</td>
<td>9</td>
<td>✓</td>
<td>已下发，带电梯 18</td>
</tr>
<tr>
<td>权限组删除门禁</td>
<td>14</td>
<td>✓</td>
<td>已下发</td>
</tr>
<tr>
<td><strong>注销二维码</strong></td>
<td><strong>15</strong></td>
<td><strong>✗</strong></td>
<td><strong>遗漏</strong></td>
</tr>
<tr>
<td><strong>注销人脸</strong></td>
<td><strong>16</strong></td>
<td><strong>✗</strong></td>
<td><strong>遗漏</strong></td>
</tr>
<tr>
<td><strong>临时权限清理</strong></td>
<td>-</td>
<td><strong>✗</strong></td>
<td><strong>遗漏</strong></td>
</tr>
</tbody></table>
<p><strong>影响</strong>：如果员工主要靠二维码或人脸通行，看起来就像”没删权限”。</p>
<h3 id="根因分析"><a href="#根因分析" class="headerlink" title="根因分析"></a>根因分析</h3><h4 id="1-下发语义不完整"><a href="#1-下发语义不完整" class="headerlink" title="1. 下发语义不完整"></a>1. 下发语义不完整</h4><p>原代码只处理了 <code>5 / 9 / 14</code>，没有处理 <code>15 / 16</code>，也没有清理 <code>priviledge_temp</code> 临时权限关系。</p>
<h4 id="2-纯删除设备请求被拦截"><a href="#2-纯删除设备请求被拦截" class="headerlink" title="2. 纯删除设备请求被拦截"></a>2. 纯删除设备请求被拦截</h4><p><code>PrivilegeDeployServiceImpl.deploy()</code> 有一个限制：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 原代码逻辑（问题）</span></span><br><span class="line"><span class="keyword">public</span> DeployResult <span class="title function_">deploy</span><span class="params">(DeployRequest request)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (CollectionUtils.isEmpty(request.getDeviceIds()) &amp;&amp; !request.isIncludeElevator()) &#123;</span><br><span class="line">        <span class="keyword">return</span> DeployResult.fail(<span class="string">&quot;没有可下发的设备&quot;</span>);  <span class="comment">// ❌ 拦截了纯删除请求</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>问题</strong>：黑名单审核后的删除门禁，语义应该是”只删不保留”。旧逻辑把”只有删除设备、没有保留设备”的请求拦掉了。</p>
<h3 id="修复方案-1"><a href="#修复方案-1" class="headerlink" title="修复方案"></a>修复方案</h3><h4 id="改动-1：完整回收所有权限类型"><a href="#改动-1：完整回收所有权限类型" class="headerlink" title="改动 1：完整回收所有权限类型"></a>改动 1：完整回收所有权限类型</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// EmployeeWhiteListServiceImpl.java</span></span><br><span class="line"><span class="meta">@Transactional</span></span><br><span class="line"><span class="keyword">public</span> Result <span class="title function_">blackAudit</span><span class="params">(BlackAuditDTO dto)</span> &#123;</span><br><span class="line">    <span class="comment">// ... 审核逻辑 ...</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (approved) &#123;</span><br><span class="line">        <span class="comment">// 1. 先收集当前所有授权设备</span></span><br><span class="line">        List&lt;Long&gt; allDevices = privilegeDeployService.collectAllDevices(employeeId);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2. 依次下发完整的权限回收</span></span><br><span class="line">        privilegeDeployService.deploy(createRequest(employeeId, <span class="number">5</span>));   <span class="comment">// 注销卡</span></span><br><span class="line">        privilegeDeployService.deploy(createRequest(employeeId, <span class="number">15</span>));  <span class="comment">// ✓ 注销二维码</span></span><br><span class="line">        privilegeDeployService.deploy(createRequest(employeeId, <span class="number">16</span>));  <span class="comment">// ✓ 注销人脸</span></span><br><span class="line">        privilegeDeployService.deploy(createRequest(employeeId, <span class="number">9</span>));   <span class="comment">// 黑名单</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 3. 清理业务关系</span></span><br><span class="line">        clearPrivilegeRelations(employeeId);  <span class="comment">// ✓ 删除权限组关系 + 临时权限关系</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 4. 最后下发删除门禁（携带收集到的设备作为 deletedDeviceIds）</span></span><br><span class="line">        <span class="type">DeployRequest</span> <span class="variable">deleteRequest</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">DeployRequest</span>();</span><br><span class="line">        deleteRequest.setEmployeeId(employeeId);</span><br><span class="line">        deleteRequest.setOpType(<span class="number">14</span>);  <span class="comment">// 权限组删除门禁</span></span><br><span class="line">        deleteRequest.setDeletedDeviceIds(allDevices);  <span class="comment">// ✓ 纯删除语义</span></span><br><span class="line">        deleteRequest.setIncludeElevator(<span class="literal">true</span>);         <span class="comment">// ✓ 同时注销电梯</span></span><br><span class="line">        deleteRequest.setElevatorOpType(<span class="number">18</span>);</span><br><span class="line">        privilegeDeployService.deploy(deleteRequest);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">clearPrivilegeRelations</span><span class="params">(Long employeeId)</span> &#123;</span><br><span class="line">    <span class="comment">// 删除权限组关系</span></span><br><span class="line">    privilegeGroupRelationService.deleteByEmployeeId(employeeId);</span><br><span class="line">    <span class="comment">// 删除临时权限关系</span></span><br><span class="line">    privilegeTempService.deleteByEmployeeId(employeeId);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="改动-2：支持纯删除请求"><a href="#改动-2：支持纯删除请求" class="headerlink" title="改动 2：支持纯删除请求"></a>改动 2：支持纯删除请求</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// PrivilegeDeployServiceImpl.java</span></span><br><span class="line"><span class="keyword">public</span> DeployResult <span class="title function_">deploy</span><span class="params">(DeployRequest request)</span> &#123;</span><br><span class="line">    <span class="type">boolean</span> <span class="variable">hasDevices</span> <span class="operator">=</span> !CollectionUtils.isEmpty(request.getDeviceIds());</span><br><span class="line">    <span class="type">boolean</span> <span class="variable">hasDeletedDevices</span> <span class="operator">=</span> !CollectionUtils.isEmpty(request.getDeletedDeviceIds());</span><br><span class="line"></span><br><span class="line">    <span class="comment">// ✓ 允许&quot;没有保留设备，但有 deletedDeviceIds&quot;的请求继续下发</span></span><br><span class="line">    <span class="keyword">if</span> (!hasDevices &amp;&amp; !request.isIncludeElevator() &amp;&amp; !hasDeletedDevices) &#123;</span><br><span class="line">        <span class="keyword">return</span> DeployResult.fail(<span class="string">&quot;没有可下发的设备&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// ... 执行下发 ...</span></span><br><span class="line"></span><br><span class="line">    result.setSuccess(<span class="literal">true</span>);  <span class="comment">// ✓ 补上成功状态赋值</span></span><br><span class="line">    <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="第二轮验证"><a href="#第二轮验证" class="headerlink" title="第二轮验证"></a>第二轮验证</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mvn -gs E:\maven363\pai-settings.xml -s E:\maven363\pai-settings.xml \</span><br><span class="line">  -Dmaven.repo.local=C:\Users\张\.m2\repository \</span><br><span class="line">  -Dtest=BlacklistAuditTimingTest <span class="built_in">test</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 测试结果</span></span><br><span class="line">Tests run: 4, Failures: 0, Errors: 0, Skipped: 0</span><br><span class="line">BUILD SUCCESS</span><br></pre></td></tr></table></figure>

<p>完整验证通过：</p>
<ul>
<li>✓ 审核通过后完整回收卡、二维码、人脸、门禁、梯控</li>
<li>✓ 删除权限组关系和临时权限关系</li>
<li>✓ 纯删除设备任务能够落库（<code>is_delete=1</code>）</li>
<li>✓ 历史不完整黑名单数据不会导致审核崩溃</li>
</ul>
<h2 id="关键代码对比"><a href="#关键代码对比" class="headerlink" title="关键代码对比"></a>关键代码对比</h2><h3 id="修复前后流程对比"><a href="#修复前后流程对比" class="headerlink" title="修复前后流程对比"></a>修复前后流程对比</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">修复前：</span><br><span class="line">pull-black ──→ 改状态(7) ──→ 立即下发(5/9/14/18) ──→ 写入待审核记录</span><br><span class="line">                                              ↓</span><br><span class="line">black-audit ──→ 审核通过 ──→ 下发空操作 ──→ 结束</span><br><span class="line"></span><br><span class="line">修复后：</span><br><span class="line">pull-black ──→ 改状态(7) ──→ 写入待审核记录（带 companyId）</span><br><span class="line">                                              ↓</span><br><span class="line">black-audit ──→ 审核通过 ──→ 下发(5/15/16/9) ──→ 清理关系 ──→ 下发(14+18) ──→ 结束</span><br></pre></td></tr></table></figure>

<p><img data-src="/images/posts/%E9%BB%91%E5%90%8D%E5%8D%95%E5%AE%A1%E6%A0%B8%E6%9D%83%E9%99%90%E5%9B%9E%E6%94%B6%E4%BF%AE%E5%A4%8D%E5%AE%9E%E6%88%98/fig1-flow-comparison.png" alt="修复前后流程对比"><br><em>图2：修复前后流程对比 - 从”提前下发”到”审核后完整回收”</em></p>
<h3 id="权限回收覆盖对比"><a href="#权限回收覆盖对比" class="headerlink" title="权限回收覆盖对比"></a>权限回收覆盖对比</h3><table>
<thead>
<tr>
<th>权限类型</th>
<th>修复前</th>
<th>修复后</th>
</tr>
</thead>
<tbody><tr>
<td>卡</td>
<td>✓</td>
<td>✓</td>
</tr>
<tr>
<td>二维码</td>
<td>✗</td>
<td>✓</td>
</tr>
<tr>
<td>人脸</td>
<td>✗</td>
<td>✓</td>
</tr>
<tr>
<td>黑名单记录</td>
<td>✓</td>
<td>✓</td>
</tr>
<tr>
<td>门禁删除</td>
<td>✓</td>
<td>✓</td>
</tr>
<tr>
<td>电梯注销</td>
<td>✓</td>
<td>✓</td>
</tr>
<tr>
<td>权限组关系</td>
<td>✓</td>
<td>✓</td>
</tr>
<tr>
<td><strong>临时权限关系</strong></td>
<td><strong>✗</strong></td>
<td><strong>✓</strong></td>
</tr>
</tbody></table>
<p><img data-src="/images/posts/%E9%BB%91%E5%90%8D%E5%8D%95%E5%AE%A1%E6%A0%B8%E6%9D%83%E9%99%90%E5%9B%9E%E6%94%B6%E4%BF%AE%E5%A4%8D%E5%AE%9E%E6%88%98/fig2-permission-coverage.png" alt="权限维度覆盖对比"><br><em>图3：权限维度覆盖对比 - 修复后完整覆盖 8 个维度</em></p>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><h3 id="1-时机把控是权限流程的核心"><a href="#1-时机把控是权限流程的核心" class="headerlink" title="1. 时机把控是权限流程的核心"></a>1. 时机把控是权限流程的核心</h3><p>权限操作必须严格对齐业务状态流转，任何”提前”或”延迟”都可能导致严重的安全隐患。</p>
<h3 id="2-完整的权限维度清单"><a href="#2-完整的权限维度清单" class="headerlink" title="2. 完整的权限维度清单"></a>2. 完整的权限维度清单</h3><p>在设计权限回收功能时，必须有一份完整的”权限维度清单”：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">物理凭证：卡、二维码、人脸</span><br><span class="line">权限记录：黑名单、白名单、临时权限</span><br><span class="line">设备维度：门禁、梯控、停车场</span><br><span class="line">关系数据：权限组关系、临时权限关系</span><br></pre></td></tr></table></figure>

<h3 id="3-历史数据兼容性设计"><a href="#3-历史数据兼容性设计" class="headerlink" title="3. 历史数据兼容性设计"></a>3. 历史数据兼容性设计</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 好做法：对可能为空的字段做防御</span></span><br><span class="line"><span class="keyword">if</span> (detail.getPhoneNo() == <span class="literal">null</span> || detail.getCompanyId() == <span class="literal">null</span>) &#123;</span><br><span class="line">    log.warn(<span class="string">&quot;数据不完整，跳过附属逻辑&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span>; <span class="comment">// 不打断主流程</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 更好做法：逐步推动数据治理，回填缺失字段</span></span><br></pre></td></tr></table></figure>

<h3 id="4-测试覆盖要全面"><a href="#4-测试覆盖要全面" class="headerlink" title="4. 测试覆盖要全面"></a>4. 测试覆盖要全面</h3><p>这次问题本质是”时机错误 + 历史脏数据兼容不足”，测试需要覆盖：</p>
<ul>
<li>正常路径：新数据完整流程</li>
<li>边界条件：历史脏数据、缺失字段</li>
<li>异常输入：空指针、非法状态</li>
</ul>
<h3 id="5-日志是排查问题的关键"><a href="#5-日志是排查问题的关键" class="headerlink" title="5. 日志是排查问题的关键"></a>5. 日志是排查问题的关键</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 关键位置必须打日志</span></span><br><span class="line">log.info(<span class="string">&quot;入口参数: &#123;&#125;&quot;</span>, dto);</span><br><span class="line">log.info(<span class="string">&quot;查询结果: &#123;&#125;&quot;</span>, entity);</span><br><span class="line">log.info(<span class="string">&quot;当前用户: &#123;&#125;&quot;</span>, currentUser);</span><br><span class="line">log.info(<span class="string">&quot;操作结果: &#123;&#125;&quot;</span>, result);</span><br></pre></td></tr></table></figure>

<h2 id="推荐后续动作"><a href="#推荐后续动作" class="headerlink" title="推荐后续动作"></a>推荐后续动作</h2><ol>
<li><p><strong>人工再验证一次完整业务流</strong></p>
<ul>
<li>发起拉黑申请</li>
<li>确认申请阶段不产生权限删除下发</li>
<li>审核通过后再确认产生 <code>5 / 15 / 16 / 9 / 14 / 18</code></li>
</ul>
</li>
<li><p><strong>抽样检查历史黑名单数据</strong></p>
<ul>
<li>关注 <code>visit_white_list.type = 2</code> 的记录里 <code>company_id</code> 是否存在为空的情况</li>
</ul>
</li>
<li><p><strong>数据治理（如需要）</strong></p>
<ul>
<li>如果业务强依赖 <code>visitor_employee_zombie</code> 的黑名单状态，建议增加一次性数据修复脚本，回填历史黑名单记录的 <code>company_id</code></li>
</ul>
</li>
</ol>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>本次修复经历了两轮迭代：</p>
<ol>
<li><strong>第一轮</strong>：解决了”时机错误”，确保权限回收只在审核通过后执行，同时兼容了历史脏数据</li>
<li><strong>第二轮</strong>：解决了”删权不完整”，扩展了权限回收覆盖面，从”卡 + 黑名单 + 门禁”升级到”卡 + 二维码 + 人脸 + 临时权限 + 门禁 + 梯控”的完整回收</li>
</ol>
<p>两次修复都遵循”最小影响面”原则，只修改黑名单审核这条链路，不动全局的停用、离职、注销等其他流程。这种精准打击的策略，既能快速修复问题，又能控制回归风险。</p>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>南沙物联网</category>
      </categories>
      <tags>
        <tag>Spring Boot</tag>
        <tag>Bug修复</tag>
        <tag>Java</tag>
        <tag>物联网</tag>
        <tag>权限管理</tag>
      </tags>
  </entry>
  <entry>
    <title>水文视频代理统一改造实战：nginx代理踩坑记</title>
    <url>/2026/05/08/%E6%B0%B4%E6%96%87%E8%A7%86%E9%A2%91%E4%BB%A3%E7%90%86%E7%BB%9F%E4%B8%80%E6%94%B9%E9%80%A0%E5%AE%9E%E6%88%98/nginx%E4%BB%A3%E7%90%86%E8%B8%A9%E5%9D%91%E8%AE%B0/</url>
    <content><![CDATA[<p>在水文监测系统中，视频监控是重要的数据来源之一。最近在项目中遇到了一个视频代理的需求：将原本直接访问视频服务器的架构改为通过 nginx 统一代理。看似简单的改造，却在 URL 编码和 nginx 配置上踩了不少坑。本文详细记录了这次改造的全过程，希望能为遇到类似问题的开发者提供参考。</p>
<audio controls>
  <source src="/images/posts/水文视频代理统一改造实战/nginx代理踩坑记/audio_guide.mp3" type="audio/mpeg">
  您的浏览器不支持音频播放。
</audio>

<span id="more"></span>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>水文监测系统需要实时查看各监测站点的视频画面，这些视频流来自不同的视频服务器。原有的实现方式是直接在前端拼接视频服务器地址，但这种方式存在安全隐患和维护困难的问题。为了统一管理和安全控制，我们需要将所有视频请求通过 nginx 代理转发。</p>
<p>这个需求看起来很简单：把视频地址从 <code>http://19.15.67.125/video/stream</code> 改成 <code>http://10.144.32.219:18001/video-proxy?url=http://19.15.67.125/video/stream</code>。但实际操作中，URL 编码、nginx 变量解码、DNS 解析等问题接踵而至，让我深刻体会到了”魔鬼在细节中”这句话的含义。</p>
<h2 id="背景：为什么要统一代理"><a href="#背景：为什么要统一代理" class="headerlink" title="背景：为什么要统一代理"></a>背景：为什么要统一代理</h2><p><img data-src="/images/posts/%E6%B0%B4%E6%96%87%E8%A7%86%E9%A2%91%E4%BB%A3%E7%90%86%E7%BB%9F%E4%B8%80%E6%94%B9%E9%80%A0%E5%AE%9E%E6%88%98/nginx%E4%BB%A3%E7%90%86%E8%B8%A9%E5%9D%91%E8%AE%B0/fig1.jpg" alt="nginx代理架构图"><br><em>图1：nginx代理架构示意</em></p>
<h3 id="原有架构的问题"><a href="#原有架构的问题" class="headerlink" title="原有架构的问题"></a>原有架构的问题</h3><p>在改造之前，系统采用的是简单的字符串替换方案：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">String</span> <span class="variable">proxyUrl</span> <span class="operator">=</span> videoUrl.replace(</span><br><span class="line">    <span class="string">&quot;http://19.15.67.125&quot;</span>,</span><br><span class="line">    <span class="string">&quot;http://10.144.32.219:18001/video-proxy&quot;</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<p>这种方式有明显的局限性：</p>
<ol>
<li><strong>只能代理固定域名</strong>：如果视频源地址变化，必须修改代码</li>
<li><strong>无法支持多视频源</strong>：不同站点可能使用不同的视频服务器</li>
<li><strong>安全隐患</strong>：前端直接暴露视频服务器地址</li>
<li><strong>维护困难</strong>：每次视频源变更都需要重新部署</li>
</ol>
<h3 id="统一代理方案设计"><a href="#统一代理方案设计" class="headerlink" title="统一代理方案设计"></a>统一代理方案设计</h3><p>新的设计方案是通过 <code>?url=</code> 参数传递原始视频地址，nginx 根据这个参数动态转发：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">前端请求：http://10.144.32.219:18001/video-proxy?url=http://19.15.67.125/video/stream</span><br><span class="line">nginx 转发：http://19.15.67.125/video/stream</span><br></pre></td></tr></table></figure>

<p>这样的好处是：</p>
<ul>
<li>支持代理任意视频源</li>
<li>前端无需知道真实视频地址</li>
<li>nginx 统一处理认证、限流、日志</li>
</ul>
<h2 id="问题一：nginx-返回-500-错误"><a href="#问题一：nginx-返回-500-错误" class="headerlink" title="问题一：nginx 返回 500 错误"></a>问题一：nginx 返回 500 错误</h2><h3 id="现象描述"><a href="#现象描述" class="headerlink" title="现象描述"></a>现象描述</h3><p>配置好 nginx 后，访问代理地址直接返回 500 错误，查看 nginx 错误日志：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[error] no resolver defined to resolve &quot;19.15.67.125&quot;</span><br></pre></td></tr></table></figure>

<h3 id="根因分析"><a href="#根因分析" class="headerlink" title="根因分析"></a>根因分析</h3><p>当 <code>proxy_pass</code> 使用变量时，nginx 不会使用系统的 DNS 解析器，必须显式配置 <code>resolver</code> 指令。</p>
<p>在我们的配置中，<code>proxy_pass $target</code> 使用了变量 <code>$target</code>，nginx 需要知道如何解析这个变量中包含的域名，但配置中缺少 <code>resolver</code> 指令。</p>
<h3 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h3><p>在 nginx 配置中添加 <code>resolver</code> 指令：</p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="section">location</span> /video-proxy &#123;</span><br><span class="line">    <span class="comment"># 关键：使用变量的proxy_pass必须配置resolver</span></span><br><span class="line">    <span class="attribute">resolver</span> <span class="number">211.136.192.6</span> valid=<span class="number">30s</span>;</span><br><span class="line"></span><br><span class="line">    <span class="attribute">set</span> <span class="variable">$target</span> <span class="variable">$arg_url</span>;</span><br><span class="line">    <span class="attribute">proxy_pass</span> <span class="variable">$target</span>;</span><br><span class="line"></span><br><span class="line">    <span class="attribute">proxy_set_header</span> Host <span class="variable">$proxy_host</span>;</span><br><span class="line">    <span class="attribute">proxy_set_header</span> X-Real-IP <span class="variable">$remote_addr</span>;</span><br><span class="line">    <span class="attribute">proxy_set_header</span> X-Forwarded-For <span class="variable">$proxy_add_x_forwarded_for</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p><strong>注意</strong>：<code>resolver</code> 地址需要根据实际网络环境配置。我们使用的是内网 DNS <code>211.136.192.6</code>，如果你的环境不同，请替换为对应的 DNS 服务器地址。</p>
</blockquote>
<h2 id="问题二：-arg-url-不自动解码"><a href="#问题二：-arg-url-不自动解码" class="headerlink" title="问题二：$arg_url 不自动解码"></a>问题二：$arg_url 不自动解码</h2><p><img data-src="/images/posts/%E6%B0%B4%E6%96%87%E8%A7%86%E9%A2%91%E4%BB%A3%E7%90%86%E7%BB%9F%E4%B8%80%E6%94%B9%E9%80%A0%E5%AE%9E%E6%88%98/nginx%E4%BB%A3%E7%90%86%E8%B8%A9%E5%9D%91%E8%AE%B0/fig3.jpg" alt="nginx变量解码行为对比图"><br><em>图2：nginx变量URL解码行为对比</em></p>
<h3 id="现象描述-1"><a href="#现象描述-1" class="headerlink" title="现象描述"></a>现象描述</h3><p>解决了 resolver 问题后，nginx 不再返回 500，但代理请求仍然失败。通过调试发现，<code>$arg_url</code> 变量的值是 URL 编码后的字符串，nginx 没有自动解码。</p>
<h3 id="nginx-变量的-URL-解码行为"><a href="#nginx-变量的-URL-解码行为" class="headerlink" title="nginx 变量的 URL 解码行为"></a>nginx 变量的 URL 解码行为</h3><p>这是一个容易被忽视的细节。nginx 的不同变量在 URL 解码方面行为不一致：</p>
<table>
<thead>
<tr>
<th>nginx 变量</th>
<th>是否自动 URL 解码</th>
</tr>
</thead>
<tbody><tr>
<td><code>$args</code> &#x2F; <code>$query_string</code></td>
<td>否</td>
</tr>
<tr>
<td><code>$arg_NAME</code></td>
<td>否</td>
</tr>
<tr>
<td>regex 捕获组 <code>$1</code></td>
<td>否</td>
</tr>
<tr>
<td><code>$uri</code></td>
<td><strong>是</strong></td>
</tr>
<tr>
<td><code>$request_uri</code></td>
<td>否</td>
</tr>
</tbody></table>
<p>这意味着 <code>$arg_url</code> 中的 <code>http%3a%2f%2f19.15.67.125%2f...</code> 会被原样传递给 <code>proxy_pass</code>，nginx 会尝试连接 <code>http%3a%2f%2f19.15.67.125</code> 这个”域名”，显然会失败。</p>
<h3 id="尝试的解决方案"><a href="#尝试的解决方案" class="headerlink" title="尝试的解决方案"></a>尝试的解决方案</h3><p><strong>方案一：使用 map + regex</strong></p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="attribute">map</span> <span class="variable">$arg_url</span> <span class="variable">$decoded_url</span> &#123;</span><br><span class="line">    ~^(.+)$ $1;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>测试发现 regex 捕获组 <code>$1</code> 同样不会自动解码，此方案不可行。</p>
<p><strong>方案二：使用 rewrite</strong></p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="attribute">rewrite</span><span class="regexp"> ^/video-proxy/(.*)$</span> /video-proxy?url=<span class="variable">$1</span> <span class="literal">last</span>;</span><br></pre></td></tr></table></figure>

<p>rewrite 的正则匹配的是 URI 部分，不包含 query string，无法处理 <code>?url=</code> 参数，此方案也不可行。</p>
<h2 id="问题三：URLEncoder-encode-编码过度"><a href="#问题三：URLEncoder-encode-编码过度" class="headerlink" title="问题三：URLEncoder.encode 编码过度"></a>问题三：URLEncoder.encode 编码过度</h2><p><img data-src="/images/posts/%E6%B0%B4%E6%96%87%E8%A7%86%E9%A2%91%E4%BB%A3%E7%90%86%E7%BB%9F%E4%B8%80%E6%94%B9%E9%80%A0%E5%AE%9E%E6%88%98/nginx%E4%BB%A3%E7%90%86%E8%B8%A9%E5%9D%91%E8%AE%B0/fig2.jpg" alt="URL编码流程图"><br><em>图3：URL编码流程示意</em></p>
<h3 id="现象描述-2"><a href="#现象描述-2" class="headerlink" title="现象描述"></a>现象描述</h3><p>在 Java 端使用 <code>URLEncoder.encode(videoUrl, &quot;UTF-8&quot;)</code> 对视频地址进行编码后，nginx 收到的 URL 变成了：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">http%3a%2f%2f19.15.67.125%2fvideo%2fstream</span><br></pre></td></tr></table></figure>

<p>nginx 无法识别这是一个 URL，代理失败。</p>
<h3 id="根因分析-1"><a href="#根因分析-1" class="headerlink" title="根因分析"></a>根因分析</h3><p>Java 的 <code>URLEncoder.encode</code> 会将所有非字母数字字符编码，包括 URL 的结构字符：</p>
<table>
<thead>
<tr>
<th>字符</th>
<th>编码后</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>:</code></td>
<td><code>%3A</code></td>
<td>URL 协议和端口分隔符</td>
</tr>
<tr>
<td><code>/</code></td>
<td><code>%2F</code></td>
<td>URL 路径分隔符</td>
</tr>
<tr>
<td><code>?</code></td>
<td><code>%3F</code></td>
<td>query string 分隔符</td>
</tr>
<tr>
<td><code>&amp;</code></td>
<td><code>%26</code></td>
<td>多参数分隔符</td>
</tr>
</tbody></table>
<p>这些字符是 URL 的结构组成部分，不应该被编码。</p>
<h3 id="关键发现：哪些字符需要编码"><a href="#关键发现：哪些字符需要编码" class="headerlink" title="关键发现：哪些字符需要编码"></a>关键发现：哪些字符需要编码</h3><p>经过测试，我发现了 URL 作为 query 参数传递时的编码规则：</p>
<table>
<thead>
<tr>
<th>字符</th>
<th>是否需要编码</th>
<th>原因</th>
</tr>
</thead>
<tbody><tr>
<td><code>?</code></td>
<td><strong>不需要</strong></td>
<td>nginx 只认第一个 <code>?</code> 作为 query string 分隔符</td>
</tr>
<tr>
<td><code>:</code></td>
<td><strong>不需要</strong></td>
<td>不会被 nginx 特殊处理</td>
</tr>
<tr>
<td><code>/</code></td>
<td><strong>不需要</strong></td>
<td>不会被 nginx 特殊处理</td>
</tr>
<tr>
<td><code>&amp;</code></td>
<td><strong>必须编码</strong></td>
<td>否则 nginx 会当作多个参数的分隔符</td>
</tr>
</tbody></table>
<h3 id="最终方案"><a href="#最终方案" class="headerlink" title="最终方案"></a>最终方案</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 1. 先用URLEncoder.encode编码（会过度编码）</span></span><br><span class="line"><span class="type">String</span> <span class="variable">encodedUrl</span> <span class="operator">=</span> URLEncoder.encode(videoUrl, <span class="string">&quot;UTF-8&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 2. 还原不需要编码的结构字符</span></span><br><span class="line">encodedUrl = encodedUrl.replace(<span class="string">&quot;%3A&quot;</span>, <span class="string">&quot;:&quot;</span>)  <span class="comment">// 还原 :</span></span><br><span class="line">                      .replace(<span class="string">&quot;%2F&quot;</span>, <span class="string">&quot;/&quot;</span>);  <span class="comment">// 还原 /</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 3. 拼接代理地址</span></span><br><span class="line"><span class="type">String</span> <span class="variable">proxyUrl</span> <span class="operator">=</span> <span class="string">&quot;http://10.144.32.219:18001/video-proxy?url=&quot;</span> + encodedUrl;</span><br></pre></td></tr></table></figure>

<p>这样处理后，传递给 nginx 的 URL 是：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">http://10.144.32.219:18001/video-proxy?url=http://19.15.67.125/video/stream&amp;other=param</span><br></pre></td></tr></table></figure>

<p>其中原始 URL 中的 <code>&amp;</code> 已经被编码为 <code>%26</code>，不会被 nginx 误解为多个参数。</p>
<h2 id="完整配置方案"><a href="#完整配置方案" class="headerlink" title="完整配置方案"></a>完整配置方案</h2><h3 id="Java-端代码"><a href="#Java-端代码" class="headerlink" title="Java 端代码"></a>Java 端代码</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> String <span class="title function_">buildProxyUrl</span><span class="params">(String videoUrl)</span> &#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="comment">// URL编码，只保留&amp;被编码</span></span><br><span class="line">        <span class="type">String</span> <span class="variable">encodedUrl</span> <span class="operator">=</span> URLEncoder.encode(videoUrl, <span class="string">&quot;UTF-8&quot;</span>);</span><br><span class="line">        <span class="comment">// 还原 : 和 /</span></span><br><span class="line">        encodedUrl = encodedUrl.replace(<span class="string">&quot;%3A&quot;</span>, <span class="string">&quot;:&quot;</span>).replace(<span class="string">&quot;%2F&quot;</span>, <span class="string">&quot;/&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;http://10.144.32.219:18001/video-proxy?url=&quot;</span> + encodedUrl;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (UnsupportedEncodingException e) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">RuntimeException</span>(<span class="string">&quot;URL编码失败&quot;</span>, e);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="nginx-配置"><a href="#nginx-配置" class="headerlink" title="nginx 配置"></a>nginx 配置</h3><figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="section">server</span> &#123;</span><br><span class="line">    <span class="attribute">listen</span> <span class="number">18001</span>;</span><br><span class="line">    <span class="attribute">server_name</span> _;</span><br><span class="line"></span><br><span class="line">    <span class="section">location</span> /video-proxy &#123;</span><br><span class="line">        <span class="comment"># 使用变量的proxy_pass必须配置resolver</span></span><br><span class="line">        <span class="attribute">resolver</span> <span class="number">211.136.192.6</span> valid=<span class="number">30s</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment"># $arg_url 不会自动URL解码</span></span><br><span class="line">        <span class="attribute">set</span> <span class="variable">$target</span> <span class="variable">$arg_url</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 代理转发</span></span><br><span class="line">        <span class="attribute">proxy_pass</span> <span class="variable">$target</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 传递原始请求头</span></span><br><span class="line">        <span class="attribute">proxy_set_header</span> Host <span class="variable">$proxy_host</span>;</span><br><span class="line">        <span class="attribute">proxy_set_header</span> X-Real-IP <span class="variable">$remote_addr</span>;</span><br><span class="line">        <span class="attribute">proxy_set_header</span> X-Forwarded-For <span class="variable">$proxy_add_x_forwarded_for</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 超时设置</span></span><br><span class="line">        <span class="attribute">proxy_connect_timeout</span> <span class="number">10s</span>;</span><br><span class="line">        <span class="attribute">proxy_read_timeout</span> <span class="number">60s</span>;</span><br><span class="line">        <span class="attribute">proxy_send_timeout</span> <span class="number">30s</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="测试验证"><a href="#测试验证" class="headerlink" title="测试验证"></a>测试验证</h2><p><img data-src="/images/posts/%E6%B0%B4%E6%96%87%E8%A7%86%E9%A2%91%E4%BB%A3%E7%90%86%E7%BB%9F%E4%B8%80%E6%94%B9%E9%80%A0%E5%AE%9E%E6%88%98/nginx%E4%BB%A3%E7%90%86%E8%B8%A9%E5%9D%91%E8%AE%B0/fig4.jpg" alt="视频代理测试结果图"><br><em>图4：视频代理测试结果对比</em></p>
<p>改造完成后，我们进行了详细的测试：</p>
<table>
<thead>
<tr>
<th>测试项</th>
<th>预期结果</th>
<th>实际结果</th>
</tr>
</thead>
<tbody><tr>
<td>直接访问视频服务器</td>
<td>HTTP 200</td>
<td>HTTP 200，10秒下载1.3MB</td>
</tr>
<tr>
<td>nginx 代理（修复后）</td>
<td>HTTP 200</td>
<td>HTTP 200，15秒下载1.7MB</td>
</tr>
<tr>
<td>代理正常工作</td>
<td>无 500 错误</td>
<td>通过</td>
</tr>
<tr>
<td>多视频源代理</td>
<td>支持任意域名</td>
<td>通过</td>
</tr>
</tbody></table>
<p>测试结果表明，代理功能正常工作，虽然由于多了一层代理，下载速度略有下降（从 10 秒增加到 15 秒），但这是可以接受的性能损失。</p>
<h2 id="经验总结"><a href="#经验总结" class="headerlink" title="经验总结"></a>经验总结</h2><h3 id="1-nginx-使用变量的-proxy-pass-必须配置-resolver"><a href="#1-nginx-使用变量的-proxy-pass-必须配置-resolver" class="headerlink" title="1. nginx 使用变量的 proxy_pass 必须配置 resolver"></a>1. nginx 使用变量的 proxy_pass 必须配置 resolver</h3><p>这是一个常见的坑。当 <code>proxy_pass</code> 使用变量时，nginx 不会使用系统的 DNS 解析器，必须显式配置 <code>resolver</code> 指令。否则会返回 500 错误。</p>
<h3 id="2-nginx-变量的-URL-解码行为不一致"><a href="#2-nginx-变量的-URL-解码行为不一致" class="headerlink" title="2. nginx 变量的 URL 解码行为不一致"></a>2. nginx 变量的 URL 解码行为不一致</h3><p>不同 nginx 变量的 URL 解码行为不同，需要特别注意：</p>
<ul>
<li><code>$uri</code> 会自动解码</li>
<li><code>$arg_*</code>、<code>$args</code>、regex 捕获组都不会自动解码</li>
</ul>
<h3 id="3-URL-作为-query-参数的编码规则"><a href="#3-URL-作为-query-参数的编码规则" class="headerlink" title="3. URL 作为 query 参数的编码规则"></a>3. URL 作为 query 参数的编码规则</h3><p>URL 作为 query 参数传递时，编码规则与普通 URL 编码不同：</p>
<ul>
<li><code>?</code> 不需要编码：nginx 只认第一个 <code>?</code></li>
<li><code>:</code> 和 <code>/</code> 不需要编码：它们是 URL 结构字符</li>
<li><code>&amp;</code> 必须编码为 <code>%26</code>：否则会被当作参数分隔符</li>
</ul>
<h3 id="4-URLEncoder-encode-会过度编码"><a href="#4-URLEncoder-encode-会过度编码" class="headerlink" title="4. URLEncoder.encode 会过度编码"></a>4. URLEncoder.encode 会过度编码</h3><p>Java 的 <code>URLEncoder.encode</code> 会将所有非字母数字字符编码，包括 URL 结构字符。在使用时需要反向还原这些字符。</p>
<h3 id="5-调试技巧"><a href="#5-调试技巧" class="headerlink" title="5. 调试技巧"></a>5. 调试技巧</h3><p>遇到 nginx 代理问题时，可以使用以下调试方法：</p>
<ul>
<li>查看 nginx 错误日志：<code>/var/log/nginx/error.log</code></li>
<li>使用 <code>curl -v</code> 查看详细的请求和响应</li>
<li>在 nginx 配置中添加 <code>add_header X-Debug-Target $target;</code> 查看变量值</li>
</ul>
<h2 id="扩展思考"><a href="#扩展思考" class="headerlink" title="扩展思考"></a>扩展思考</h2><h3 id="安全性考虑"><a href="#安全性考虑" class="headerlink" title="安全性考虑"></a>安全性考虑</h3><p>统一代理方案虽然解决了功能问题，但也引入了新的安全风险：</p>
<ul>
<li>任意 URL 代理可能被滥用（SSRF 攻击）</li>
<li>需要添加白名单或验证机制</li>
</ul>
<p>建议在生产环境中：</p>
<ol>
<li>添加视频源域名白名单</li>
<li>验证 URL 格式和协议</li>
<li>限制代理的响应大小</li>
<li>添加访问日志和监控</li>
</ol>
<h3 id="性能优化"><a href="#性能优化" class="headerlink" title="性能优化"></a>性能优化</h3><p>对于视频流代理，可以考虑：</p>
<ol>
<li>使用 nginx 的 <code>proxy_buffering off</code> 减少延迟</li>
<li>配置 <code>proxy_cache</code> 缓存热点视频</li>
<li>使用 <code>X-Accel-Redirect</code> 实现更灵活的转发</li>
</ol>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>这次视频代理改造虽然功能简单，但在 URL 编码和 nginx 配置上踩了不少坑。通过这次经历，我深刻理解了 nginx 变量的解码机制和 URL 编码的细节。希望这篇文章能为遇到类似问题的开发者提供参考，少走一些弯路。</p>
<p>技术细节往往决定了一个功能的成败，作为开发者，我们需要对这些”小事”保持敬畏之心。正如这次改造所展示的，一个简单的 URL 编码问题，如果不深入了解其机制，可能会花费大量时间去排查。</p>
<hr>
<p><strong>相关资源</strong>：</p>
<ul>
<li><a href="http://nginx.org/en/docs/http/ngx_http_proxy_module.html#proxy_pass">nginx 官方文档 - proxy_pass</a></li>
<li><a href="http://nginx.org/en/docs/http/ngx_http_core_module.html#resolver">nginx 官方文档 - resolver</a></li>
<li><a href="https://docs.oracle.com/javase/8/docs/api/java/net/URLEncoder.html">Java URLEncoder 文档</a></li>
</ul>
<hr>
<p><strong>背景音乐</strong>：</p>
<audio controls>
  <source src="/images/posts/水文视频代理统一改造实战/nginx代理踩坑记/song.mp3" type="audio/mpeg">
  您的浏览器不支持音频播放。
</audio>
]]></content>
      <categories>
        <category>项目笔记</category>
        <category>水文</category>
      </categories>
      <tags>
        <tag>踩坑记录</tag>
        <tag>水文项目</tag>
        <tag>nginx</tag>
        <tag>视频代理</tag>
        <tag>URL编码</tag>
      </tags>
  </entry>
</search>
