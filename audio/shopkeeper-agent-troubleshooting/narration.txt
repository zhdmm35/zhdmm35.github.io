大家好，今天给大家分享一篇实战排障笔记：在 16G 内存的 Windows 本机上，把 AI 电商问数项目 shopkeeper-agent 完整跑通的全部过程。整个过程踩了十三个坑，按启动顺序排成一条故事线。从最开始的 uv 环境解析报错、asyncmy 在 Windows 上无法编译，到 LangChain 的 JSON 解析器撞上推理模型的思考块，再到 Docker 内存爆掉导致整机黑屏。每一个坑的背后都有具体的根因和可复现的解决命令。其中最值得展开的是第七章——模型选型的完整心路。从 MiniMax 的 M2.7 到 M3，再换到通义千问，最后用 extra_body 参数关掉 M3 的思考输出，拿到内部强推理加干净 JSON 的最佳组合。无论你是 Windows 用户、国内网络环境，还是初次跑 LLM 应用，这篇博客都能帮你少走弯路。
